---
milestone: v1.7
scope: Phase 57 (standalone, post-v1.7-ship) — Demo FakeIdP Browser-Login Proof
audited: 2026-06-14
status: tech_debt
scores:
  requirements: 1/1
  phases: 1/1
  integration: 4/4
  flows: 2/2
nyquist:
  compliant_phases: ["57"]
  partial_phases: []
  missing_phases: []
  overall: compliant
gaps:
  requirements: []   # none — SEED-003 satisfied
  integration: []    # none — 4/4 seams wired, 0 blockers
  flows: []          # none — success + tampered round-trips both green
tech_debt:
  - phase: 57-demo-fakeidp-browser-login-proof
    items:
      - "WR-01 (warning): InResponseTo (attacker-influenced, from inbound SAMLRequest) interpolated into emitted XML with no escaping — signer.ex:139. Bounded (capture class excludes quote; re-parse fails closed) but an unhandled crash path on untrusted input. Still open: no xml_escape in signer.ex."
      - "WR-02 (warning): Login form advertises 'Valid Login (evaluator@example.com)' but controller emits sarah@northstar.example.com (login.html.heex:34 vs fake_idp_controller.ex:31). evaluator@ is not a seeded SAMLIdentity. UI promise the system does not keep. Still open."
      - "WR-03 (warning): `case action` has only \"success\"/\"failure\" clauses, no catch-all (fake_idp_controller.ex:75-98). Any other idp_action value → CaseClauseError → 500 on an unauthenticated POST. Still open."
      - "WR-04 (warning): inflate/1 single-shot :zlib.inflate with no decompressed-size cap on browser-supplied bytes (fake_idp_controller.ex:126-139) — zip-bomb amplification. Local-demo-bounded. Still open."
      - "WR-05 (warning): tamper/1 regex matches only bare <NameID> with no attrs/prefix (signer.ex:104). Silent no-op if template later adds Format attr or prefix — would turn the rejection proof into a false pass. No drift-detection guard. Still open."
      - "IN-01..IN-04 (info): cert_pem/0 uncached vs documented caching (keypair.ex:44); load_private_key/0 opaque MatchError on multi-entry PEM (keypair.ex:58); extract_in_response_to first-ID-anywhere match (fake_idp_controller.ex:119); relay_state HEEx-escape reliance undocumented. All cosmetic/diagnosability."
      - "INT-W2 (integration warning): vestigial ConnectionId=\"valid\" attribute hardcoded on emitted <Response> (signer.ex:139); inert (relyra scopes by ACS URL path, not this attribute). Cosmetic."
      - "Repo hygiene: demo/ledger_loop/erl_crash.dump (7.8 MB) present on disk and untracked test/support/poll.ex shows in git status. Neither is in the Phase 57 diff; both should be gitignored/removed before any packaging."
---

# v1.7 Milestone Audit — Phase 57 (Demo FakeIdP Browser-Login Proof)

**Audited:** 2026-06-14
**Status:** `tech_debt` — requirement satisfied, zero blockers, accumulated non-critical debt needs review

## Scope Note

v1.7 "Adoption Evidence Demo" (Phases 51–56, 23 plans, 30/30 requirements) **shipped and was
archived 2026-06-13** (`.planning/milestones/v1.7-ROADMAP.md`). Phase 57 is a **standalone MVP
phase added after that ship** to close SEED-003 (the `/fake_idp/*` browser-login WIP). It is the
only active, non-archived phase, so this audit's effective scope is Phase 57 alone — the 51–56
phases were verified at their own ship and are not re-audited here.

## Requirements Coverage (3-source cross-reference)

There is **no `.planning/REQUIREMENTS.md`** in this repo. The sole requirement, **SEED-003**, is
tracked in the ROADMAP Phase 57 header (`**Requirements**: SEED-003`) and declared in all three
plans' frontmatter (`requirements: [SEED-003]`).

| REQ-ID | VERIFICATION.md | SUMMARY frontmatter | ROADMAP | → Final Status |
|--------|-----------------|---------------------|---------|----------------|
| SEED-003 | passed (13/13 truths) | listed in 57-01/02/03 (`provides`) | Phase 57 `[x]` 3/3 plans | **satisfied** |

No orphaned requirements: SEED-003 is claimed and verified by every plan in the phase. FAIL gate
not triggered — zero `unsatisfied` requirements.

## Phase Verification Aggregate

| Artifact | Status | Evidence |
|----------|--------|----------|
| 57-VERIFICATION.md | **passed** | 13/13 must-have truths verified; 0 overrides; both success and tampered user-flow variants proven in-process |
| 57-VALIDATION.md (Nyquist) | **compliant** | `nyquist_compliant: true`; 8/8 verification-map rows COVERED by green tests; 0 manual-only; 0 gaps |
| 57-SECURITY.md | **verified** | 14 threats, 14 closed, 0 open; 2 accepted risks logged; `git diff main...HEAD -- lib/` empty |
| 57-REVIEW.md | **issues_found** | 0 critical, 5 warning, 4 info (the warnings/info form the tech-debt stack below) |

## Cross-Phase Integration (gsd-integration-checker)

**4/4 seams WIRED · 0 orphaned · 0 missing · 0 broken.**

1. **SP-initiated chain** — affordance → `/saml/…J0/login` (relyra macro) → redirect to
   `idp_sso_url` (`/fake_idp/login`) → `/fake_idp/sso` → connection-scoped POST `/saml/…J0/acs`
   → relyra `do_verify/4` → SessionAdapter LoginReceipt → `/`. Every hop wired; proven by
   `fake_idp_flow_test.exs` (302 → `/`, receipt inserted for sarah).
2. **Cert-trust alignment (the crux)** — enabled …J0 fixture cert `pem`/`fingerprint_sha256` are
   compile-time single-sourced from `Keypair.cert_pem()` (fixtures.ex:8,115-116); signer signs
   with the matching `Keypair.private_key()`. Cert/key are a verified RSA-2048 pair. **No drift
   possible by construction.**
3. **Tampered/trace flow** — `tamper/1` → `:digest_mismatch` at the crypto gate →
   `LoginTrace.attach(repo:)` writes `domain: :login` AuditEvent → `ConnectionTraceLive` renders
   it. Full read path proven (400, no receipt, `error_code="digest_mismatch"`, trace row rendered).
4. **Zero leakage** — `git diff main...HEAD -- lib/ mix.exs` empty; relyra published trust posture
   untouched; demo consumes only relyra PUBLIC modules.

## E2E Flows

| Flow | Status | Evidence |
|------|--------|----------|
| Success round-trip (valid signed assertion → logged-in session) | ✓ green | fake_idp_flow_test.exs:43-102 |
| Tampered variant (digest_mismatch → typed rejection in trace UI) | ✓ green | fake_idp_flow_test.exs:110-204 |

Full demo suite: **57 tests / 0 failures**.

## Tech Debt (non-blocking — see frontmatter for file:line detail)

All 5 code-review **warnings** (WR-01..05) and 4 **info** notes (IN-01..04) remain **unaddressed**
in the working tree — confirmed by grep during this audit (no `xml_escape` in signer.ex; `tamper/1`
guard not added; `case action` still lacks a catch-all; login label still `evaluator@example.com`).
They were known at phase verification, which consciously passed the phase as a **local demo proof
harness** (the warnings are explicitly demo-bounded: "For a local demo the exposure is limited").
None are SEED-003 blockers.

Plus repo hygiene: a 7.8 MB `erl_crash.dump` and an untracked `test/support/poll.ex` sit in the
working tree, outside the Phase 57 diff — clean these before any packaging.

**Total: 8 debt clusters (5 warnings + 4 info + integration cosmetic + repo hygiene), 1 phase.**

## Verdict

SEED-003 is fully satisfied with zero critical gaps, zero integration blockers, Nyquist-compliant
validation, and a clean security posture. The milestone is **completable**. The open code-review
warnings are demo-scoped robustness/clarity nits, not auth-path defects — accept-and-track in the
backlog, or address them in a short cleanup phase first. WR-02 (mismatched login label) and WR-05
(silent-no-op tamper risk) are the two most worth fixing for a *proof* harness whose entire value
is that what it shows is true.

---

_Audited 2026-06-14 · gsd-audit-milestone_
