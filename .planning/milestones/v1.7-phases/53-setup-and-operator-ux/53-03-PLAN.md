---
phase: 53
plan: 03
type: execute
wave: 3
depends_on:
  - 53-02
files_modified:
  - demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex
  - demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex
  - demo/ledger_loop/test/ledger_loop_web/live/setup_live_test.exs
autonomous: true
gap_closure: true
requirements:
  - UX-01
must_haves:
  truths:
    - Customer/admin can use a nonlinear setup checklist with copyable SP settings, provider vocabulary, IdP intake, mapping preview, test login, and enablement receipt.
  artifacts:
    - path: demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex
      provides: "Checklist sections contain functional inputs and text instead of static placeholders."
      contains: "form phx-submit"
  key_links: []
---

<objective>
Fix Setup Checklist UI gaps by implementing functional SP settings, an IdP metadata intake form, mapping preview, and test login logic.

Purpose: Close verification gaps where UI sections contained static placeholder text rather than functioning inputs and buttons.
Output: Fully implemented SetupLive UI steps.
</objective>

<context>
@.planning/phases/53-setup-and-operator-ux/53-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SP Settings and IdP Metadata Form</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex, demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex</files>
  <action>
    Fix missing items from gap:
    - Implement actual copyable SP settings: Add fields for Entity ID and ACS URL with copy affordances (e.g., read-only inputs or clearly formatted text) in the `sp_settings` step.
    - Implement an IdP metadata intake form: Add a `<form phx-submit="save_metadata">` with a textarea for Metadata XML in the `idp_metadata` step. Add a `handle_event("save_metadata", ...)` in `SetupLive` to process this (store a success state in assigns for demo purposes).

    Reference existing code: `SetupLive` already has `handle_event("set_step", ...)` and basic conditional rendering.
    Gap reason: The UI renders static placeholder text. There are no actual copyable fields or forms.
  </action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop_web/live/setup_live_test.exs</automated>
  </verify>
  <done>SP settings display actual URLs and IdP Metadata has a functional submission form.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Mapping Preview and Test Login Logic</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex, demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex, demo/ledger_loop/test/ledger_loop_web/live/setup_live_test.exs</files>
  <action>
    Fix missing items from gap:
    - Implement a data-driven mapping preview: In the `mapping_preview` step, render a table or list showing how SAML attributes (e.g., `urn:oid:0.9.2342.19200300.100.1.3`) map to local fields (e.g., `email`). Define this mock mapping list in `SetupLive` assigns.
    - Implement test login logic: Make the "Start Test Login" button functional by adding `phx-click="test_login"` and a `handle_event("test_login", ...)` that redirects the user to a mock SAML login flow (e.g., `redirect(to: "/login/admin")` or similar existing route for demo logic).

    Update `SetupLiveTest` to assert the presence of these new UI elements (e.g., checking for the form, mapping table, and functional buttons).

    Gap reason: Mapping preview was placeholder text and buttons had no click handlers.
  </action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop_web/live/setup_live_test.exs</automated>
  </verify>
  <done>Mapping preview renders actual mapped fields and test login button redirects appropriately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Setup UI → Host App | Protocol artifact exposure from SAML processes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03 | Information Disclosure | SetupLive | mitigate | Strictly render redacted struct details from the DB rather than Raw XML/PEM or underlying library primitives in the receipt view. Ensure forms do not reflect unescaped payload. |
</threat_model>

<verification>
mix test passes and checklist LiveView operates safely without cryptographic exposure. Form submissions are handled cleanly.
</verification>

<success_criteria>
Customer can configure their SSO setup via nonlinear checklist with clear SP settings, IdP intake form, mapping preview, and working test login button.
</success_criteria>

<output>
Create .planning/phases/53-setup-and-operator-ux/53-03-SUMMARY.md when done
</output>