---
phase: 53
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/ledger_loop/test/ledger_loop_web/controllers/route_affordance_controller_test.exs
  - demo/ledger_loop/lib/ledger_loop_web/router.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
must_haves:
  truths:
    - Evaluator can authenticate as a mock admin to view Relyra LiveAdmin
    - Support trace scenarios deep-link directly to connection traces
  artifacts:
    - path: demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex
      provides: "Mock admin login and support redirect actions"
      contains: "def admin_login"
  key_links:
    - from: demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex
      to: "/relyra/admin"
      via: "redirect"
      pattern: "redirect.*to.*relyra/admin"
---

<objective>
Implement admin session mocking and deep link routing to LiveAdmin trace UI.

Purpose: Operators need access to mounted Relyra Admin without reinventing host-owned auth just for the demo.
Output: Working mock login route and support trace redirect endpoint.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-setup-and-operator-ux/53-RESEARCH.md
@.planning/phases/53-setup-and-operator-ux/53-VALIDATION.md
@demo/ledger_loop/lib/ledger_loop/demo/fixtures.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RouteAffordanceController tests</name>
  <files>demo/ledger_loop/test/ledger_loop_web/controllers/route_affordance_controller_test.exs</files>
  <behavior>
    - Test 1: GET /login/admin sets admin session keys ("admin_actor", "admin_actor_label", "admin_organization_id") and redirects to /relyra/admin
    - Test 2: GET /support/scenario redirects to the LiveAdmin trace URL (/relyra/admin/connections/:id/trace) using the support fixture connection ID
  </behavior>
  <action>Create the controller test module verifying that the mock admin login establishes the required session keys and redirects according to Scenario 3 of the validation plan. Also verify the support scenario endpoint issues a redirect to the correct trace path according to Scenario 4.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop_web/controllers/route_affordance_controller_test.exs</automated>
  </verify>
  <done>Failing tests created that assert the expected session mutations and redirects</done>
</task>

<task type="auto">
  <name>Task 2: Implement Admin Session Mocking and Support Trace Hand-off</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/router.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex</files>
  <action>In the controller, implement `admin_login` to put `"demo_admin"` as `admin_actor` in the session, along with `"Demo Administrator"` as `admin_actor_label` and `"northstar"` as `admin_organization_id`, then redirect to `/relyra/admin`. Modify `support` to fetch `LedgerLoop.Demo.Fixtures.relyra_support_scenario_id()` and redirect to the connection's trace URL. In the router, add `get "/login/admin", LedgerLoopWeb.RouteAffordanceController, :admin_login`.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop_web/controllers/route_affordance_controller_test.exs</automated>
  </verify>
  <done>Tests pass, proving the admin actor session injection and deep linking operate correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo UI → Admin | Mock authentication bypasses real auth purely for local test simulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Privilege Elevation | RouteAffordanceController | accept | Acceptable because LedgerLoop is a demo/test harness and explicitly used to prove LiveAdmin access capabilities locally. |
</threat_model>

<verification>
mix test in demo app passes and correctly asserts admin actor injection.
</verification>

<success_criteria>
Evaluator can securely access mounted LiveAdmin and trace paths via deterministic simulated endpoints.
</success_criteria>

<output>
Create .planning/phases/53-setup-and-operator-ux/53-01-SUMMARY.md when done
</output>