---
phase: 52-ecto-stores-and-deterministic-seed-story
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/ledger_loop/mix.exs
  - demo/ledger_loop/lib/ledger_loop/relyra/migrations.ex
  - demo/ledger_loop/lib/mix/tasks/ledger_loop.relyra.migrate.ex
  - demo/ledger_loop/test/ledger_loop/relyra/migrations_test.exs
autonomous: true
requirements:
  - ECTO-01
must_haves:
  truths:
    - "Demo setup runs Relyra shipped Ecto migrations from the repository dependency path before demo-owned migrations."
    - "No Relyra migration file is copied into demo/ledger_loop/priv/repo/migrations."
    - "Demo test/setup aliases prepare Relyra trust tables before Phase 52 seed and store tests run."
  artifacts:
    - path: "demo/ledger_loop/lib/ledger_loop/relyra/migrations.ex"
      provides: "Dependency-path Relyra migration runner"
      contains: "Ecto.Migrator.run"
    - path: "demo/ledger_loop/lib/mix/tasks/ledger_loop.relyra.migrate.ex"
      provides: "Demo-local Mix task for Relyra migrations"
      contains: "LedgerLoop.Relyra.Migrations"
    - path: "demo/ledger_loop/test/ledger_loop/relyra/migrations_test.exs"
      provides: "Automated no-copy and dependency-path proof"
      contains: "../../priv/repo/migrations"
  key_links:
    - from: "demo/ledger_loop/mix.exs"
      to: "demo/ledger_loop/lib/mix/tasks/ledger_loop.relyra.migrate.ex"
      via: "setup/test aliases call ledger_loop.relyra.migrate before ecto.migrate"
      pattern: "ledger_loop.relyra.migrate"
    - from: "demo/ledger_loop/lib/ledger_loop/relyra/migrations.ex"
      to: "../../priv/repo/migrations"
      via: "Path.expand from demo/ledger_loop working directory"
      pattern: "../../priv/repo/migrations"
---

<objective>
Create the demo-owned migration runner that executes Relyra's shipped migrations from the path dependency before any LedgerLoop migrations or seeds run.

Purpose: Phase 52 seed and Ecto-store plans need Relyra trust tables to exist without copying Relyra migration files into the demo app.
Output: A tested `LedgerLoop.Relyra.Migrations` runner, a demo-local Mix task, and setup/test aliases that run the dependency-path migrations first.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-ecto-stores-and-deterministic-seed-story/52-CONTEXT.md
@.planning/phases/52-ecto-stores-and-deterministic-seed-story/52-RESEARCH.md
@.planning/phases/52-ecto-stores-and-deterministic-seed-story/52-VALIDATION.md
@.planning/phases/52-ecto-stores-and-deterministic-seed-story/52-PATTERNS.md
@.planning/phases/51-demo-app-foundation/51-01-SUMMARY.md
@demo/ledger_loop/mix.exs
@test/support/migration_case.ex
@priv/repo/migrations/20260505120000_create_relyra_connections.exs

<interfaces>
Key contracts the executor must preserve:

From `test/support/migration_case.ex`:
```elixir
Migrator.with_repo(EctoTestRepo, fn repo ->
  Migrator.run(repo, @migrations_path, :up, all: true)
end)
```

From `demo/ledger_loop/mix.exs`:
```elixir
"ecto.setup": ["ecto.create", "ecto.migrate", "run priv/repo/seeds.exs"]
test: ["ecto.create --quiet", "ecto.migrate --quiet", "test"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove and implement dependency-path Relyra migrations</name>
  <files>demo/ledger_loop/lib/ledger_loop/relyra/migrations.ex, demo/ledger_loop/lib/mix/tasks/ledger_loop.relyra.migrate.ex, demo/ledger_loop/test/ledger_loop/relyra/migrations_test.exs</files>
  <read_first>
    - .planning/phases/52-ecto-stores-and-deterministic-seed-story/52-CONTEXT.md
    - .planning/phases/52-ecto-stores-and-deterministic-seed-story/52-RESEARCH.md
    - .planning/phases/52-ecto-stores-and-deterministic-seed-story/52-PATTERNS.md
    - test/support/migration_case.ex
    - priv/repo/migrations/20260505120000_create_relyra_connections.exs
  </read_first>
  <behavior>
    - Test 1: `LedgerLoop.Relyra.Migrations.relyra_migrations_path/0` expands to the root dependency path ending in `priv/repo/migrations`, not `demo/ledger_loop/priv/repo/migrations`.
    - Test 2: running the demo migration task creates at least `relyra_connections` and `relyra_audit_events` in `LedgerLoop.Repo`.
    - Test 3: source scan finds no copied Relyra migration modules under `demo/ledger_loop/priv/repo/migrations`.
  </behavior>
  <action>Write the failing tests first, then create `LedgerLoop.Relyra.Migrations` with `relyra_migrations_path/0` and `migrate_relyra!/0`. Implement `migrate_relyra!/0` with `Ecto.Migrator.with_repo(LedgerLoop.Repo, fn repo -> Ecto.Migrator.run(repo, path, :up, all: true) end)` using `../../priv/repo/migrations` relative to `demo/ledger_loop`, per D-04 and D-05. Add `Mix.Tasks.LedgerLoop.Relyra.Migrate` as a thin demo-local task that calls the runner and accepts but does not require `--quiet`. Do not copy, import, eval, or duplicate any root Relyra migration file per D-05 and D-06.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop/relyra/migrations_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `LedgerLoop.Relyra.Migrations.relyra_migrations_path/0` resolves to the root `priv/repo/migrations` directory.
    - `mix ledger_loop.relyra.migrate` creates Relyra trust tables in the demo repo.
    - `demo/ledger_loop/priv/repo/migrations` contains no copied Relyra migration modules or files.
  </acceptance_criteria>
  <done>Relyra migrations run from the dependency path through a tested LedgerLoop runner and task, with no copied Relyra migration files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire setup and test aliases to run Relyra migrations first</name>
  <files>demo/ledger_loop/mix.exs, demo/ledger_loop/test/ledger_loop/relyra/migrations_test.exs</files>
  <read_first>
    - demo/ledger_loop/mix.exs
    - demo/ledger_loop/lib/ledger_loop/relyra/migrations.ex
    - demo/ledger_loop/lib/mix/tasks/ledger_loop.relyra.migrate.ex
    - .planning/phases/52-ecto-stores-and-deterministic-seed-story/52-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `ecto.setup` alias includes `ledger_loop.relyra.migrate` before `ecto.migrate`.
    - Test 2: `test` alias includes `ledger_loop.relyra.migrate --quiet` before `ecto.migrate --quiet`.
    - Test 3: `ecto.reset` still delegates to `ecto.setup`.
  </behavior>
  <action>Update `demo/ledger_loop/mix.exs` so `ecto.setup` runs `ecto.create`, then `ledger_loop.relyra.migrate`, then `ecto.migrate`, then `run priv/repo/seeds.exs`. Update the `test` alias so it creates the database, runs `ledger_loop.relyra.migrate --quiet`, runs `ecto.migrate --quiet`, then invokes the real `test` task. Keep `ecto.reset` delegated through `ecto.setup`. This implements D-04 without changing root Relyra APIs or migration ownership per D-16.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop/relyra/migrations_test.exs --warnings-as-errors</automated>
    <automated>cd demo/ledger_loop && mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - Alias order in `mix.exs` is Relyra migrations before demo migrations for setup and tests.
    - The migration test command passes under warnings-as-errors.
    - Demo formatting remains clean.
  </acceptance_criteria>
  <done>Demo setup/test entrypoints prepare Relyra tables from the dependency path before any Phase 52 demo data or store tests run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mix task -> database | Local setup code creates Relyra trust tables in the demo repo. |
| Dependency path -> demo app | Demo app executes migrations from the Relyra path dependency without copying source files. |
| Demo setup -> later seed/store code | Later plans rely on these tables existing before seeded trust data or Ecto stores run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | `LedgerLoop.Relyra.Migrations.relyra_migrations_path/0` | mitigate | Resolve and test the literal dependency path `../../priv/repo/migrations`; add a no-copy source assertion for `demo/ledger_loop/priv/repo/migrations`. |
| T-52-02 | Tampering / Elevation of privilege | request/replay table selection | transfer | Plan 52-04 fixes table names in wrapper modules; this plan only prepares migration ordering. |
| T-52-03 | Spoofing | trust-source confusion | mitigate | Run only Relyra's existing trust-table migrations; do not add LedgerLoop code that changes signature sources or trusts document `KeyInfo`. |
| T-52-04 | Information disclosure | deterministic seed leakage of PEM/XML/secrets | transfer | Plans 52-03 and 52-05 seed audit/receipt content with redaction checks; this plan creates no seed rows. |
| T-52-05 | Tampering | parser bypass | mitigate | Add no XML parsing in LedgerLoop migration code; all SAML proof remains in Relyra consume path. |
| T-52-06 | Spoofing | replay acceptance | transfer | Plan 52-04 creates Ecto replay table/wrapper and Plan 52-06 proves replay insertion through Relyra. |
| T-52-SC | Tampering | package installs | accept | No npm, pip, cargo, or Hex package installation is planned; Phase 52 uses existing dependencies. |
</threat_model>

<verification>
Plan-level checks:
- `cd demo/ledger_loop && mix test test/ledger_loop/relyra/migrations_test.exs --warnings-as-errors`
- `cd demo/ledger_loop && mix format --check-formatted`
</verification>

<success_criteria>
- Demo migration runner executes Relyra migrations from `../../priv/repo/migrations`.
- Demo setup and test aliases run Relyra migrations before demo migrations.
- No root Relyra migration files are copied into `demo/ledger_loop/priv/repo/migrations`.
</success_criteria>

<output>
Create `.planning/phases/52-ecto-stores-and-deterministic-seed-story/52-01-SUMMARY.md` when done.
</output>
