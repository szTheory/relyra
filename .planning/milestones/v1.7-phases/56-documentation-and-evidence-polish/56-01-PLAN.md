---
phase: 56-documentation-and-evidence-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/ledger_loop/README.md
autonomous: true
requirements: [DOCS-02, DOCS-03]
must_haves:
  truths:
    - "DOCS-02 / SC2: demo/ledger_loop/README.md documents boot (Docker + Mix), reset, test, urls/ports, seeded credentials, key routes, Docker env overrides, and the optional Keycloak profile against the verified source surface (D-06, D-07)."
    - "DOCS-03 / SC3: README explains the host-app boundary via a 'Who owns what' table — Relyra verifies SAML trust; LedgerLoop owns tenant workflow, mapping, sessions, authorization (D-09)."
    - "SC4: README carries the asymmetric scope-honesty note (top blockquote callout + bottom 'Scope & honesty' two-list section) hitting all four ROADMAP scope items and tying security to strict-defaults (D-08)."
    - "D-12: the §4 'What you'll see' walkthrough matches the real default FakeIdP login outcome (currently signs evaluator@example.com), never falsely claiming 'you land as Dr. Sarah'."
    - "D-05: README uses 'runnable reference app, not part of the Hex package' label discipline; never 'quickstart/starter/scaffold'."
  artifacts:
    - path: "demo/ledger_loop/README.md"
      provides: "Authoritative evaluator-first 11-section demo guide (full rewrite of the phx.new scaffold)"
      contains: "not part of the Hex package"
      min_lines: 120
  key_links:
    - from: "demo/ledger_loop/README.md"
      to: "scripts/demo"
      via: "documented subcommand set in triple-backtick-fenced bash fenced blocks"
      pattern: "scripts/demo (doctor|up|reset|test|urls|down)"
    - from: "demo/ledger_loop/README.md §6"
      to: "demo/ledger_loop/lib/ledger_loop_web/router.ex"
      via: "key routes table sourced from verified registered routes"
      pattern: "/setup/sso"
---

<objective>
Replace the 18-line `mix phx.new` scaffold at `demo/ledger_loop/README.md` with the
authoritative, evaluator-first demo guide (D-01) following the 11-section outline (D-06).
The README documents the EXACT verified surface (D-07), frames the host-app boundary (D-09),
carries the asymmetric scope-honesty note (D-08), and gets the "What you'll see" walkthrough
right against the real default FakeIdP login outcome (D-12, BLOCKING).

Purpose: DOCS-02 and DOCS-03 (and SC2/SC3/SC4) require a single authoritative guide co-located
with the runnable app. This is the load-bearing content artifact of the phase.

Output: a fully rewritten `demo/ledger_loop/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-documentation-and-evidence-polish/56-CONTEXT.md
@prompts/relyra-brand-book.md

This plan is the writer plan. The brand-voice microcopy and 11-section outline are strong
defaults captured in `.planning/phases/56-documentation-and-evidence-polish/56-DISCUSSION-LOG.md`
and codified in 56-CONTEXT.md D-06/D-08/D-09. Refine wording to fit, do not invent surface.

Verified surface facts (re-confirm at write time against the read_first files — do not trust
this summary alone):
- scripts/demo subcommands: doctor | up | reset | test | urls | down
- docker-compose.yml: profiles core (default) / keycloak / browser; env overrides PORT, PGPORT, KC_PORT; app on 4000, Keycloak on 8080
- In-repo Mix path: `mix setup` (chains deps.get + ecto.setup which runs ledger_loop.relyra.migrate, ecto.migrate, priv/repo/seeds.exs) and `mix ecto.reset`; server via `mix phx.server`
- Seeded story (fixtures.ex): Northstar Health tenant (slug `northstar`); users Dr. Sarah <sarah@northstar.example.com> (admin), Nurse Chen <chen@northstar.example.com> (clinical); four connection scenarios — Enabled, Draft/Missing Metadata, Staged Rollover, Support Failure
- Registered routes (router.ex): /, /setup/sso, /login/test, /login/admin, /support/scenario, /relyra/admin (LiveAdmin), /saml/* (saml_routes()), /healthz, /readyz
- CONTENT-ACCURACY ALERT (D-12): fake_idp_controller.ex signs subject "evaluator@example.com", while seeded SAML identities are sarah@/chen@northstar.example.com. The /fake_idp/login and /fake_idp/sso routes are referenced in templates and tests but were NOT found in the committed router.ex at plan time — the executor MUST confirm the live registered route set (including any dev_routes block) before writing §4 and §6.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the real default login outcome and live route set (BLOCKING content-accuracy gate, D-12)</name>
  <read_first>
    - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex (the signed subject — confirm it is still evaluator@example.com)
    - demo/ledger_loop/lib/ledger_loop/demo/fixtures.ex (seeded users, tenant, four connection scenarios, SAML identity subjects)
    - demo/ledger_loop/lib/ledger_loop_web/router.ex (the canonical registered route set, including any dev_routes / FakeIdP block)
    - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex (the /fake_idp/sso form target referenced by the UI)
    - demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs (asserts /fake_idp/login and /fake_idp/sso behavior)
  </read_first>
  <action>
    Establish the verified facts the rest of the README depends on, by reading source — do not paraphrase from memory. Specifically: (a) confirm the exact subject the success path of FakeIdP signs; (b) determine whether that subject maps to a seeded user (it does NOT today — evaluator@example.com is not in fixtures.ex), so the §4 walkthrough must be framed as "you sign in as the evaluator test user (evaluator@example.com)" and must NOT claim "you land as Dr. Sarah"; (c) confirm the full live registered route list from router.ex, and resolve whether /fake_idp/login and /fake_idp/sso are actually registered (search the real router and any dev_routes block) — only document routes that exist. Capture these confirmed facts inline at the top of the SUMMARY as the "verified surface" the README was written against. This task gates Task 2: do not write §4 or §6 until these facts are confirmed from source.
  </action>
  <verify>
    <automated>grep -q 'evaluator@example.com' demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the exact FakeIdP success-path subject read from fake_idp_controller.ex.
    - The SUMMARY records whether that subject exists in fixtures.ex (it does not, today) and the chosen §4 framing.
    - The SUMMARY records the confirmed live registered route set read from router.ex (and whether /fake_idp/* routes are registered).
    - No claim of "you land as Dr. Sarah" is carried forward unless the source verification proves the default subject maps to Dr. Sarah.
  </acceptance_criteria>
  <done>Verified login outcome and route set are recorded from source and ready to drive §4 and §6.</done>
</task>

<task type="auto">
  <name>Task 2: Write the full evaluator-first demo README (D-01, D-06, D-07, D-08, D-09, D-05, D-12)</name>
  <read_first>
    - demo/ledger_loop/README.md (the file being rewritten — currently the 18-line phx.new scaffold)
    - scripts/demo (subcommand set doctor|up|reset|test|urls|down; the urls subcommand prints canonical URLs)
    - docker-compose.yml (profiles core/keycloak/browser; env overrides PORT/PGPORT/KC_PORT; ports 4000/8080)
    - demo/ledger_loop/mix.exs (setup / ecto.setup / ecto.reset / test aliases; ledger_loop.relyra.migrate chain; relyra path dep)
    - demo/ledger_loop/lib/ledger_loop_web/router.ex (route table source for §6)
    - demo/ledger_loop/lib/ledger_loop/demo/fixtures.ex (seeded credentials table + four connection scenarios for §5)
    - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex (default subject for §4, per Task 1)
    - README.md (root: mirror the "What Does Not Ship" two-list voice for §10; do NOT alter this file)
    - prompts/relyra-brand-book.md (voice, microcopy, scope-honesty framing)
  </read_first>
  <action>
    Rewrite demo/ledger_loop/README.md end to end as the authoritative demo guide, following the D-06 11-section outline with these exact sections, in order: (1) one-paragraph "what this is" — LedgerLoop is a fictional Phoenix B2B SaaS embedding Relyra; (2) at-a-glance scope strip (demonstrates / does not, pointing to the §10 scope section); (3) Quick start with dual options — Option A Docker (recommended) using `scripts/demo doctor` then `scripts/demo up` then `scripts/demo urls`, and Option B Local Mix using `mix setup` then `mix phx.server`; (4) "What you'll see" walkthrough — login through FakeIdP to a verified assertion, the typed-rejection scenario, and the resulting audit/receipt row, written to match the verified default subject from Task 1 (frame as the evaluator test user; do NOT claim Dr. Sarah unless Task 1 proved it); (5) Seeded data — credentials table (Dr. Sarah sarah@northstar.example.com admin, Nurse Chen chen@northstar.example.com clinical) with the no-password / identity-keyed caption, plus the four connection scenarios (Enabled, Draft/Missing Metadata, Staged Rollover, Support Failure); (6) Key routes table grouped by owner (LedgerLoop vs Relyra) plus health probes /healthz and /readyz, sourced from the verified route set; (7) Reset & test — `scripts/demo reset`, `scripts/demo test`, and the in-repo `mix ecto.reset`; (8) Optional Keycloak profile — the `keycloak` Compose profile, localhost:8080, KC_PORT override, and why it is optional; (9) "Who owns what" host-boundary table per Task-3 dependency note — actually authored here per D-09 (see acceptance criteria); (10) Scope & honesty — the bottom two-list section mirroring root README "What Does Not Ship" voice; (11) Where to go next — links UP to the library (hexdocs.pm/relyra, Getting Started, runbooks). Place the D-08 top scope blockquote callout immediately under the title (positive framing: adoption evidence, not the library, not new capability, not production hardening, not a hosted service). For §9 use the verbatim JTBD caption: "Relyra gets you to 'this assertion is valid for this connection.' LedgerLoop gets you to 'this person may now do these things in our product.'" with rows covering parse/verify-signature → Relyra, audience/recipient/replay/typed-rejection → Relyra, tenant identity & provisioning → LedgerLoop, principal→local-user mapping → LedgerLoop, session establishment → LedgerLoop, downstream authorization → LedgerLoop. For §10 hit all four ROADMAP scope items explicitly — no protocol expansion, no production IdP behavior, no hosted-broker behavior, no security relaxation — and tie the security point to the strict-defaults invariant ("the same strict defaults gate this demo; the FakeIdP signs with a real key and Relyra verifies it — nothing weakens validation"). Apply D-05 label discipline throughout: "runnable reference app, not part of the Hex package"; never "quickstart", "starter", or "scaffold". Put every `scripts/demo <subcommand>` invocation inside triple-backtick-fenced bash fenced code blocks (so the Plan 03 drift test can scope to them). Do NOT leak PEM, raw XML, real credentials, or instruct readers to weaken any gate. An optional restrained badge row linking back to relyra hex/docs is allowed (planner discretion).
  </action>
  <verify>
    <automated>test $(grep -c '' demo/ledger_loop/README.md) -ge 120 && grep -q "not part of the Hex package" demo/ledger_loop/README.md && grep -q "this person may now do these things in our product" demo/ledger_loop/README.md && grep -Eq 'scripts/demo (doctor|up|reset|test|urls|down)' demo/ledger_loop/README.md && ! grep -qiE 'quickstart|starter|scaffold' demo/ledger_loop/README.md</automated>
  </verify>
  <acceptance_criteria>
    - demo/ledger_loop/README.md contains all 11 D-06 section headings in order (at-a-glance scope, dual quick start, what you'll see, seeded data, key routes, reset & test, optional Keycloak, who owns what, scope & honesty, where to go next).
    - `grep -q "not part of the Hex package" demo/ledger_loop/README.md` succeeds; `grep -qiE 'quickstart|starter|scaffold' demo/ledger_loop/README.md` FAILS (label discipline, D-05).
    - The verbatim JTBD caption appears: `grep -q "this person may now do these things in our product" demo/ledger_loop/README.md`.
    - All six subcommands appear inside triple-backtick-fenced bash fenced blocks: every one of doctor, up, reset, test, urls, down is present as `scripts/demo <token>`.
    - Seeded creds table contains both sarah@northstar.example.com and chen@northstar.example.com; all four connection scenario names appear.
    - §4 does NOT contain "land as Dr. Sarah" unless Task 1 proved the default subject is Dr. Sarah (`! grep -qi "land.*as Dr. Sarah" demo/ledger_loop/README.md` unless verified true).
    - §10 names all four scope items (protocol, production IdP, hosted broker, security relaxation) and references strict defaults.
    - No PEM blocks, no raw `<saml:` XML, no real secrets in the file.
  </acceptance_criteria>
  <done>The demo README is a complete, source-accurate, evaluator-first guide satisfying D-01/D-06/D-07/D-08/D-09/D-05/D-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → reader | Reader copy-pastes commands and forms a mental model of the security posture from this file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Information Disclosure | demo/ledger_loop/README.md | mitigate | Acceptance criteria forbid PEM, raw XML, and real secrets; seeded deterministic test creds (sarah@/chen@ with no passwords) are safe to document. |
| T-56-02 | Tampering (posture erosion) | §3 quick start, §7 reset/test | mitigate | README documents only the shipped strict-default flow; no instruction to disable validation, skip signature checks, or relax gates. §10 ties the demo to strict-defaults explicitly. |
| T-56-03 | Repudiation / accuracy | §4 walkthrough (D-12) | mitigate | Task 1 BLOCKING gate forces the walkthrough to match the real default FakeIdP subject; a false "you land as Dr. Sarah" claim would misrepresent the verified trust path and is explicitly forbidden. |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages (docs-only). No legitimacy gate required. |
</threat_model>

<verification>
- `grep -c '' demo/ledger_loop/README.md` >= 120 (full rewrite, not the scaffold).
- All six `scripts/demo` subcommands present in fenced bash blocks.
- Label discipline grep passes (no quickstart/starter/scaffold).
- JTBD caption verbatim present; four scope items present; both seeded users present.
</verification>

<success_criteria>
DOCS-02, DOCS-03, and SC2/SC3/SC4 content lands in a single authoritative, source-accurate
demo README with the D-12 content-accuracy gate satisfied.
</success_criteria>

<output>
Create `.planning/phases/56-documentation-and-evidence-polish/56-01-SUMMARY.md` when done,
recording the verified login outcome, the live route set, and the §4 framing chosen.
</output>
