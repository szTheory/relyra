---
phase: 56-documentation-and-evidence-polish
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - guides/demo.md
  - mix.exs
  - README.md
  - guides/getting_started.md
  - test/docs/markdown_link_smoke_test.exs
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "DOCS-01 / SC1: root README and Getting Started link to the runnable demo as evaluator evidence while the {:relyra, \"~> 1.5\"} install snippet and 'Start Here' router stay untouched at the top (D-03, D-04)."
    - "D-02: a thin guides/demo.md exists, is published in ExDoc extras + a groups_for_extras group, says the demo is repo-only / not part of the Hex package, and bounces the reader to the demo README — it does NOT duplicate boot/reset/creds/routes detail."
    - "D-02b: every link from guides/demo.md into the demo uses an absolute GitHub URL pinned to main (github.com/szTheory/relyra/blob/main/demo/...), never a relative ../demo/ path."
    - "D-05: all new references use 'runnable reference app, not part of the Hex package'; never quickstart/starter/scaffold."
  artifacts:
    - path: "guides/demo.md"
      provides: "Thin hexdocs pointer to the repo-only demo, with absolute-GitHub-URL bounce"
      contains: "not part of the Hex package"
    - path: "mix.exs"
      provides: "guides/demo.md registered in docs.extras + groups_for_extras"
      contains: "guides/demo.md"
  key_links:
    - from: "mix.exs docs/0"
      to: "guides/demo.md"
      via: "extras list + groups_for_extras group"
      pattern: "guides/demo.md"
    - from: "guides/demo.md"
      to: "demo/ledger_loop/README.md"
      via: "absolute GitHub URL pinned to main"
      pattern: "github.com/szTheory/relyra/blob/main/demo/ledger_loop/README.md"
---

<objective>
Wire the demo into the published documentation as a SECONDARY evaluator side-channel without
displacing the Hex install path. Add the thin `guides/demo.md` pointer (D-02) with absolute
GitHub URLs pinned to main (D-02b), register it in ExDoc extras + groups_for_extras, and add
secondary demo references to the root README and Getting Started §5 (D-03/D-04/D-05).
Optionally harden the link-smoke test (D-02c, discretion).

Purpose: DOCS-01 / SC1 — link the runnable demo as evaluator evidence while preserving the
normal Hex installation path exactly where it is.

Output: guides/demo.md, plus edits to mix.exs, README.md, guides/getting_started.md, and
(optionally) test/docs/markdown_link_smoke_test.exs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-documentation-and-evidence-polish/56-CONTEXT.md
@.planning/phases/56-documentation-and-evidence-polish/56-01-SUMMARY.md

<interfaces>
mix.exs docs/0 already defines `extras:` (a list of markdown paths) and `groups_for_extras:`
(a keyword list of group => [paths]). guides/demo.md must be added to BOTH. The existing groups
are: Introduction, "Day-1", Recipes, Operations, Security, "Generated & audit". package.files
ships "guides" but NOT "demo" — this is exactly why demo links from guides/demo.md must be
absolute GitHub URLs (D-02b), not relative.

test/docs/markdown_link_smoke_test.exs:
- @published_extras is a hardcoded list mirroring mix.exs extras; it also globs guides/**/*.md.
- "relative markdown links in published docs resolve to files" resolves links against repo root
  on disk — a relative ../demo/... link PASSES on disk but 404s on hexdocs. external_or_anchor_only?/1
  skips http/https/mailto/# targets, so absolute GitHub URLs are correctly ignored by the smoke test.
- D-02c (discretion) would add a check that fails a relative link from a published extra that
  resolves OUTSIDE package.files (i.e. into demo/).
</interfaces>

Root README "Start Here" router (README.md:35-47) and the install snippet (README.md:13-17)
MUST stay untouched at the top. The demo reference goes BELOW "Start Here" (or into the existing
"Day-2 And Operator Guides" block) and links to guides/demo.md (relative, shipped) — NEVER directly
into demo/. Getting Started §5 "Production follow-ons" gets the demo as a follow-on reference only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author guides/demo.md and register it in ExDoc (D-02, D-02b)</name>
  <read_first>
    - guides/demo.md (the file being created — does not yet exist)
    - mix.exs (docs/0: extras list and groups_for_extras; package.files showing guides shipped / demo excluded)
    - demo/ledger_loop/README.md (the bounce target authored in Plan 01)
    - guides/overview.md (voice reference for a thin pointer extra)
    - prompts/relyra-brand-book.md (voice, scope-honesty framing)
  </read_first>
  <action>
    Create guides/demo.md as a THIN pointer (not a duplicate of the demo README). Its only jobs: (a) state that a runnable reference LedgerLoop demo app exists in the repository; (b) state plainly that it is repo-only and not part of the Hex package; (c) echo ONE sentence of the D-08 scope-honesty framing (adoption evidence, not new capability, same strict defaults); (d) bounce the reader to the full demo guide via an absolute GitHub URL pinned to main: https://github.com/szTheory/relyra/blob/main/demo/ledger_loop/README.md . Do NOT duplicate boot/reset/credentials/routes detail — that lives only in the demo README. Use D-05 label discipline ("runnable reference app, not part of the Hex package"; never quickstart/starter/scaffold). Then register guides/demo.md in mix.exs docs/0: add "guides/demo.md" to the `extras` list, and add it to `groups_for_extras` — either append to the "Day-1" group or create a dedicated single-entry group (e.g. Demo: ["guides/demo.md"]); choose one and keep it consistent. Every demo/ link in guides/demo.md MUST be an absolute github.com/szTheory/relyra/blob/main/... URL — never a relative ../demo/ path (D-02b).
  </action>
  <verify>
    <automated>mix docs >/dev/null 2>&1 && grep -q "not part of the Hex package" guides/demo.md && grep -q "github.com/szTheory/relyra/blob/main/demo/ledger_loop/README.md" guides/demo.md && grep -q '"guides/demo.md"' mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - guides/demo.md exists and `grep -q "not part of the Hex package" guides/demo.md` succeeds.
    - guides/demo.md links to the demo README via the absolute URL github.com/szTheory/relyra/blob/main/demo/ledger_loop/README.md.
    - `grep -qE '\]\(\.\./demo/' guides/demo.md` FAILS (no relative links into demo/, D-02b).
    - `grep -qiE 'quickstart|starter|scaffold' guides/demo.md` FAILS (D-05).
    - mix.exs `extras` contains "guides/demo.md" AND it appears under exactly one groups_for_extras group.
    - `mix docs` builds without error and the generated output lists the demo extra.
  </acceptance_criteria>
  <done>guides/demo.md is a thin, scope-honest, absolute-URL pointer registered as a published ExDoc extra.</done>
</task>

<task type="auto">
  <name>Task 2: Add secondary demo references to root README and Getting Started; optional link-smoke hardening (D-03, D-04, D-05, D-02c)</name>
  <read_first>
    - README.md (the file being edited — "Start Here" at :35-47 and install snippet at :13-17 must stay untouched at top; "Day-2 And Operator Guides" block at :111+)
    - guides/getting_started.md (§5 Production follow-ons at the "Useful follow-on references" list)
    - guides/demo.md (the link target created in Task 1)
    - test/docs/markdown_link_smoke_test.exs (the gate to optionally harden for D-02c; note external_or_anchor_only?/1 and resolve_link/3)
    - mix.exs (package.files — the set a D-02c relative link must resolve inside)
  </read_first>
  <action>
    Edit README.md to add a SECONDARY "see it running" demo reference BELOW the "Start Here" router (or inside the existing "Day-2 And Operator Guides" list), linking to guides/demo.md via a relative shipped link — NEVER directly into demo/. Do NOT touch the install snippet (`{:relyra, "~> 1.5"}`) or the "Start Here" numbered router; they stay exactly where they are at the top (D-03). Use D-05 label discipline ("runnable reference app, not part of the Hex package"). Edit guides/getting_started.md to add the demo ONLY to the §5 "Production follow-ons" "Useful follow-on references" list as an evaluator/evidence reference linking to guides/demo.md — do NOT insert it into the numbered install->scaffold->proof->provider spine (D-04). Optionally (D-02c, planner discretion — recommended): extend test/docs/markdown_link_smoke_test.exs with a test that FAILS any relative link from a published extra that resolves to a path outside mix.exs package.files (i.e. resolving into demo/), converting the silent disk-passes/hexdocs-404 gap into a CI failure; if implemented, keep absolute http(s) links exempt via the existing external_or_anchor_only?/1 helper.
  </action>
  <verify>
    <automated>grep -q '{:relyra, "~> 1.5"}' README.md && grep -q '## Start Here' README.md && grep -q 'guides/demo.md' README.md && grep -q 'demo' guides/getting_started.md && mix test test/docs/markdown_link_smoke_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - README.md still contains the untouched install snippet (`grep -q '{:relyra, "~> 1.5"}' README.md`) and the "## Start Here" router heading.
    - README.md contains a link to guides/demo.md placed below "Start Here" (not above the install snippet); no direct link into demo/ from README.md.
    - guides/getting_started.md references guides/demo.md only within §5 Production follow-ons; the numbered §1-§4 spine is unchanged.
    - `grep -qiE 'quickstart|starter|scaffold' README.md` near the new demo reference would be expected to FAIL for the newly-added lines (label discipline, D-05) — verify the new reference uses "runnable reference app, not part of the Hex package".
    - `mix test test/docs/markdown_link_smoke_test.exs --warnings-as-errors` exits 0 (and, if D-02c implemented, the new relative-link-outside-package check is present and green).
  </acceptance_criteria>
  <done>Root README and Getting Started link the demo as a secondary evaluator reference; the Hex install path is preserved and the link-smoke gate stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published docs (hexdocs) → reader | Hexdocs reader follows links; a relative link into demo/ 404s and silently degrades trust in the evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04 | Spoofing/Integrity (broken evidence) | guides/demo.md links into demo/ | mitigate | D-02b absolute-GitHub-URL rule + acceptance grep forbidding relative ../demo/ links; optional D-02c CI gate makes the disk-passes/hexdocs-404 gap fail loudly. |
| T-56-05 | Tampering (posture erosion) | README.md install path / Start Here | mitigate | Acceptance criteria require the install snippet and Start Here router to remain untouched; demo is strictly secondary (D-03), preserving the canonical adoption path. |
| T-56-06 | Information Disclosure | guides/demo.md, README.md, getting_started.md | mitigate | Pointer is thin; no creds/PEM/XML reproduced; detail stays in the demo README only. |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages (docs-only). No legitimacy gate required. |
</threat_model>

<verification>
- `mix docs` builds and lists guides/demo.md as a published extra in a groups_for_extras group.
- `mix test test/docs/markdown_link_smoke_test.exs --warnings-as-errors` exits 0.
- Install snippet and "Start Here" router unchanged at the top of README.md.
- No relative ../demo/ links in guides/demo.md.
</verification>

<success_criteria>
DOCS-01 / SC1 satisfied: the demo is linked as evaluator evidence from published docs without
displacing the Hex install path, using a thin absolute-URL pointer.
</success_criteria>

<output>
Create `.planning/phases/56-documentation-and-evidence-polish/56-02-SUMMARY.md` when done,
noting whether the D-02c link-smoke hardening was implemented or deferred.
</output>
