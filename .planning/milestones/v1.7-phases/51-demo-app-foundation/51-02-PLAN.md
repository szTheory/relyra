---
phase: 51-demo-app-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - demo/ledger_loop/lib/ledger_loop/relyra/admin_scope.ex
  - demo/ledger_loop/lib/ledger_loop_web.ex
  - demo/ledger_loop/lib/ledger_loop_web/router.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/page_controller.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex
autonomous: true
requirements:
  - DEMO-03
  - DEMO-04
must_haves:
  truths:
    - "Relyra SAML routes are mounted under the host-owned /saml route scope."
    - "Relyra LiveAdmin operator routes are mounted at /relyra/admin with demo-owned repo and scope provider options."
    - "The LedgerLoop root, setup, login, and support route affordances exist before their Phase 52-55 workflows are implemented."
    - "The route seams compile without FakeIdP, Keycloak, Relyra.TestSupport, ETS request/replay proof, or public Relyra API changes."
  artifacts:
    - path: "demo/ledger_loop/lib/ledger_loop_web/router.ex"
      provides: "Host-owned workspace, SAML, admin, and route affordance routes"
      contains: "scope \"/saml\""
    - path: "demo/ledger_loop/lib/ledger_loop/relyra/admin_scope.ex"
      provides: "LiveAdmin scope provider for the demo host"
      contains: "@behaviour Relyra.LiveAdmin.ScopeProvider"
    - path: "demo/ledger_loop/lib/ledger_loop_web/controllers/page_controller.ex"
      provides: "Root workspace route controller seam"
      exports: ["home"]
    - path: "demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex"
      provides: "Setup, login, and support route affordance controller seams"
      exports: ["setup", "login", "support"]
  key_links:
    - from: "demo/ledger_loop/lib/ledger_loop_web/router.ex"
      to: "Relyra.Phoenix.Router.saml_routes/1"
      via: "import and host-owned /saml scope"
      pattern: "saml_routes"
    - from: "demo/ledger_loop/lib/ledger_loop_web/router.ex"
      to: "Relyra.LiveAdmin.Router.relyra_admin_routes/2"
      via: "repo and scope_provider options"
      pattern: "relyra_admin_routes"
    - from: "demo/ledger_loop/lib/ledger_loop_web/router.ex"
      to: "LedgerLoop.Relyra.AdminScope"
      via: "LiveAdmin scope_provider option"
      pattern: "scope_provider: LedgerLoop.Relyra.AdminScope"
---

<objective>
Wire the Relyra route and operator-admin seams inside the generated LedgerLoop host app.

Purpose: Evaluators need a host-owned route surface that proves Relyra is mounted by an adopter app while preserving Relyra's existing SAML and LiveAdmin internals.
Output: Router, controller seams, and LiveAdmin scope provider for `/`, `/setup/sso`, `/login/test`, `/support/scenario`, `/saml`, and `/relyra/admin`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-demo-app-foundation/51-CONTEXT.md
@.planning/phases/51-demo-app-foundation/51-RESEARCH.md
@.planning/phases/51-demo-app-foundation/51-VALIDATION.md
@.planning/phases/51-demo-app-foundation/51-PATTERNS.md
@.planning/phases/51-demo-app-foundation/51-UI-SPEC.md
@AGENTS.md
@lib/relyra/phoenix/router.ex
@lib/relyra/live_admin/router.ex
@lib/mix/tasks/relyra.install.ex
@test/support/live_admin_test_support.ex

<interfaces>
Relyra route macro contract:
- `Relyra.Phoenix.Router.saml_routes/1` registers metadata, login, and ACS under the caller's current Phoenix scope. Phase 51 must call it inside `scope "/saml", LedgerLoopWeb`.
- `Relyra.LiveAdmin.Router.relyra_admin_routes/2` accepts a path and options. Phase 51 must call `relyra_admin_routes("/relyra/admin", repo: LedgerLoop.Repo, scope_provider: LedgerLoop.Relyra.AdminScope)`.
- `Relyra.LiveAdmin.ScopeProvider.resolve_admin_scope/3` returns `{:ok, %Relyra.LiveAdmin.Scope{}}` or `{:error, :unauthenticated}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount host-owned Relyra route seams</name>
  <files>demo/ledger_loop/lib/ledger_loop/relyra/admin_scope.ex, demo/ledger_loop/lib/ledger_loop_web.ex, demo/ledger_loop/lib/ledger_loop_web/router.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/page_controller.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/phases/51-demo-app-foundation/51-CONTEXT.md`
    - `.planning/phases/51-demo-app-foundation/51-RESEARCH.md`
    - `.planning/phases/51-demo-app-foundation/51-VALIDATION.md`
    - `.planning/phases/51-demo-app-foundation/51-PATTERNS.md`
    - `.planning/phases/51-demo-app-foundation/51-UI-SPEC.md`
    - `lib/relyra/phoenix/router.ex`
    - `lib/relyra/live_admin/router.ex`
    - `lib/mix/tasks/relyra.install.ex`
    - `test/support/live_admin_test_support.ex`
    - every file listed in `<files>` that already exists
  </read_first>
  <action>In `LedgerLoopWeb.Router`, import `Relyra.Phoenix.Router` and `Relyra.LiveAdmin.Router`, per D-05 and D-06. Under the browser pipeline, register host-owned routes `GET /`, `GET /setup/sso`, `GET /login/test`, and `GET /support/scenario`. Add `scope "/saml", LedgerLoopWeb` with `pipe_through(:browser)` and `saml_routes()` inside it, so the registered SAML paths are `/saml/:connection_id/metadata`, `/saml/:connection_id/login`, and `/saml/:connection_id/acs`. Mount LiveAdmin with `relyra_admin_routes("/relyra/admin", repo: LedgerLoop.Repo, scope_provider: LedgerLoop.Relyra.AdminScope)`. Create `LedgerLoop.Relyra.AdminScope` as a host-owned module implementing `Relyra.LiveAdmin.ScopeProvider`; it must read session keys `admin_actor`, `admin_actor_label`, and `admin_organization_id` and return `{:error, :unauthenticated}` when `admin_actor` is missing or blank. Implement `PageController.home/2` and `RouteAffordanceController.setup/2`, `login/2`, and `support/2` as compile-safe host-owned route seams; Plan 51-04 owns the full HEEx workspace and route-affordance templates. Per D-02, D-08, D-09, D-12, and D-13, these routes must not perform SAML login, seed data, FakeIdP, Keycloak, request-store, replay-store, ETS happy-path proof, setup workflow behavior, or changes to `Relyra.start_login/3`, `Relyra.consume_response/3`, or behaviour callback signatures.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix compile --warnings-as-errors</automated>
    <automated>grep -n "scope \"/saml\"\\|saml_routes\\|relyra_admin_routes(\" demo/ledger_loop/lib/ledger_loop_web/router.ex</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `demo/ledger_loop/lib/ledger_loop_web/router.ex` contains `scope "/saml", LedgerLoopWeb`.
    - Source assertion: `demo/ledger_loop/lib/ledger_loop_web/router.ex` contains `saml_routes()`.
    - Source assertion: `demo/ledger_loop/lib/ledger_loop_web/router.ex` contains `relyra_admin_routes("/relyra/admin", repo: LedgerLoop.Repo, scope_provider: LedgerLoop.Relyra.AdminScope)`.
    - Source assertion: `demo/ledger_loop/lib/ledger_loop/relyra/admin_scope.ex` contains `@behaviour Relyra.LiveAdmin.ScopeProvider`.
    - Source assertion: route seam files contain no `Relyra.TestSupport`, `FakeIdP`, `Keycloak`, `RequestStore.Ecto`, `ReplayStore.Ecto`, or ETS request/replay tokens.
    - CLI assertion: `cd demo/ledger_loop && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>LedgerLoop owns the route surface, Relyra owns SAML/admin internals through existing macros, and later phase affordance routes exist without later-phase behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> LedgerLoopWeb.Router | Untrusted HTTP requests enter the demo host app. |
| LedgerLoopWeb.Router -> Relyra.Phoenix.Router | SAML metadata/login/ACS routing crosses from host route ownership into Relyra's maintained controller seam. |
| LedgerLoopWeb.Router -> Relyra.LiveAdmin.Router | Operator UI routing crosses into mounted LiveAdmin with host repo and host scope provider. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04 | Tampering | `demo/ledger_loop/lib/ledger_loop_web/router.ex` | mitigate | Mount SAML with `Relyra.Phoenix.Router.saml_routes/1`; do not create custom metadata/login/ACS controller routes that could bypass CSRF skip or the single Relyra parse path. |
| T-51-05 | Elevation of Privilege | `LedgerLoop.Relyra.AdminScope` | mitigate | Implement `Relyra.LiveAdmin.ScopeProvider`; return `{:error, :unauthenticated}` when `session["admin_actor"]` is missing or blank. |
| T-51-06 | Spoofing | route-affordance controller seams | mitigate | Route affordance copy/actions must state host ownership and must not present FakeIdP, Keycloak, seeded data, or durable store proof as complete in Phase 51. |
| T-51-SC | Tampering | Relyra route integrations | mitigate | Use existing Relyra router macros only; no npm/pip/cargo installs and no new Hex packages beyond the approved Phoenix host-app set. |
</threat_model>

<verification>
Run after this plan:
- `cd demo/ledger_loop && mix compile --warnings-as-errors`
- `grep -n "scope \"/saml\"\\|saml_routes\\|relyra_admin_routes(" demo/ledger_loop/lib/ledger_loop_web/router.ex`
</verification>

<success_criteria>
- `LedgerLoopWeb.Router.__routes__()` can include the host-owned root/setup/login/support routes, `/saml` Relyra SAML routes, and `/relyra/admin` LiveAdmin routes once tests are added in Plan 51-03.
- `LedgerLoop.Relyra.AdminScope` implements the existing LiveAdmin scope-provider contract without changing public Relyra APIs.
- No Phase 52-56 seed, setup, FakeIdP, Keycloak, Docker, CI lane, docs, or durable store proof behavior is implemented here.
</success_criteria>

<output>
Create `.planning/phases/51-demo-app-foundation/51-02-SUMMARY.md` when done.
</output>
