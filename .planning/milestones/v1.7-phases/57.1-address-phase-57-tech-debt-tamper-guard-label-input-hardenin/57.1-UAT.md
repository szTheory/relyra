---
status: complete
phase: 57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin
source: [57.1-01-SUMMARY.md, 57.1-02-SUMMARY.md, 57.1-03-SUMMARY.md]
started: 2026-06-14T14:52:48Z
updated: 2026-06-14T15:12:19Z
---

## Current Test

[testing complete — verified by automation]

## Verification

No human UAT required. All six checkpoints are covered by named automated tests that
already run in CI on every PR + push to `main` via `mix ci.demo_app`
(`.github/workflows/demo-app-ci.yml`, OTP 27 + 28, Postgres 16). Coverage is bound to the
checkpoints — and protected against silent drift — by
`demo/ledger_loop/test/uat_coverage_test.exs`, which fails CI if any cited test is deleted
or renamed.

## Tests

### 1. Demo App Builds Green
expected: From demo/ledger_loop, `mix ci.demo_app` runs the suite with 0 failures (and `mix qa` stays green at the repo root). Confirms the 57.1 hardening landed without breaking the build.
result: pass
verified_by: mix ci.demo_app (whole demo suite; 70 tests, 0 failures) + mix qa (744 tests, 0 failures)

### 2. Corrected Login Label (WR-02)
expected: The FakeIdP "Valid Login" option reads `sarah@northstar.example.com` (the real subject emitted by conn_fields/0), NOT the old, unseeded `evaluator@example.com`.
result: pass
verified_by: test/ledger_loop_web/controllers/fake_idp_controller_test.exs:164 "WR-02 renders sarah@northstar.example.com label for valid login"

### 3. Valid Login Round-Trip Succeeds
expected: Choosing the valid-login option and submitting produces a cryptographically verified assertion; login succeeds (non-nil in_response_to, 302→/, LoginReceipt inserted).
result: pass
verified_by: test/ledger_loop_web/fake_idp_flow_test.exs:43 "login → fake IdP → ACS verifies → LoginReceipt inserted + redirect /"

### 4. Tamper Path → Typed Rejection (WR-05)
expected: Choosing the failure/tamper option yields a typed `:digest_mismatch` rejection — never a silent accept. The tamper helper actually mutates the `<NameID>`, and raises descriptively if the template drifts so it cannot.
result: pass
verified_by: test/ledger_loop_web/fake_idp_flow_test.exs:110 "tampered assertion → no session, digest_mismatch AuditEvent in trace"; test/ledger_loop/fake_idp/signer_test.exs:83 "tampered response causes Signature.verify/4 to return {:error, %Error{type: :digest_mismatch}}"; test/ledger_loop/fake_idp/signer_test.exs:106 "WR-05: raises with descriptive message when XML has no matching <NameID>"

### 5. Hostile Input Doesn't Crash the FakeIdP
expected: The unauthenticated GET/POST boundary survives crafted input with HTTP 200 / fail-closed nil, no 500/CaseClauseError: catch-all idp_action (WR-03), decompression-bomb ceiling (WR-04), NCName regex + escaping (WR-01/IN-03).
result: pass
verified_by: test/ledger_loop_web/controllers/fake_idp_controller_test.exs:69 "WR-04 oversized SAMLRequest inflating >64 KiB yields nil in_response_to (no crash)"; :80 "WR-04 garbled SAMLRequest bytes yield nil in_response_to (fail-closed contract)"; :90 "IN-03 SAMLRequest with ID containing '<' (non-NCName) yields nil in_response_to"; :101 "IN-03 SAMLRequest with ID containing a space (non-NCName) yields nil in_response_to"; :137 "WR-03 unknown idp_action resolves to success path (HTTP 200, SAMLResponse rendered)"; test/ledger_loop/fake_idp/signer_test.exs:126 "WR-01: signed_response/1 with metacharacter in_response_to returns a binary without crashing"

### 6. Descriptive PEM Decode Error (IN-02)
expected: Feeding a multi-entry / wrong-type PEM raises a descriptive RuntimeError naming the key path and entry tags — not a cryptic MatchError.
result: pass
verified_by: test/ledger_loop/fake_idp/keypair_test.exs:46 "IN-02: raises a descriptive error on a multi-entry / wrong-shape PEM"

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all checkpoints covered by automated tests; traceability enforced by demo/ledger_loop/test/uat_coverage_test.exs]
