---
phase: 57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/poll.ex
  - test/relyra/test_support/poll_test.exs
autonomous: true
requirements: [Hygiene]

must_haves:
  truths:
    - "test/support/poll.ex is committed (git-tracked) AND has a real consumer (a unit test), so it is not a dead module under --warnings-as-errors"
    - "Relyra.TestSupport.Poll.retry_until!/4 is exercised on both the success path and the exhausted-attempts (raise) path"
    - "The stale local-only wip/demo-fake-idp branch is deleted"
  artifacts:
    - path: "test/support/poll.ex"
      provides: "Relyra.TestSupport.Poll.retry_until!/4 — committed (not untracked)"
      contains: "def retry_until!"
    - path: "test/relyra/test_support/poll_test.exs"
      provides: "Unit coverage giving Poll a real consumer (success + exhausted)"
      contains: "Relyra.TestSupport.Poll"
  key_links:
    - from: "test/relyra/test_support/poll_test.exs"
      to: "Relyra.TestSupport.Poll.retry_until!/4"
      via: "direct unit test"
      pattern: "Poll.retry_until!"
---

<objective>
Resolve the dangling repo state from Phase 57:

- Commit the currently-untracked `test/support/poll.ex` (`Relyra.TestSupport.Poll`) AND give it a real
  consumer so it is not a dead module. RESEARCH Open Q1 confirmed the module is untracked AND referenced
  by nothing; the `retry_until!/4` actually exercised by the suite is
  `Mix.Tasks.Verify.ReleaseParity.retry_until!/4`. **Resolution (chosen explicitly, option (b) from
  RESEARCH Open Q1): commit `poll.ex` and add a root unit test exercising `retry_until!/4`** — lowest
  blast radius, no rewiring of the live `ReleaseParity` helper or CI probes, and the module gains
  coverage + a caller so it survives `mix test --warnings-as-errors` as a real (not dead) module.
- Delete the stale local-only `wip/demo-fake-idp` branch (Phase 57 shipped on `phase/57-demo-fake-idp`).

Purpose: clean version-control state — no untracked source under `test/support`, no dead module, no
orphaned WIP branch. This plan touches only the root project (different mix project + test command than
the demo-app plans), so it runs in parallel with Plans 01–02 (zero file overlap).

Output: tracked `poll.ex`, a new root `poll_test.exs`, and a pruned branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-CONTEXT.md
@.planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-RESEARCH.md
@.planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-VALIDATION.md
@test/support/poll.ex

<interfaces>
Relyra.TestSupport.Poll.retry_until!/4 :: (label :: String.t(), attempts :: pos_integer(),
  sleep_ms :: non_neg_integer(), fun :: (-> :ok | {:error, term()})) :: :ok | no_return()
  — retries fun until :ok, sleeps sleep_ms between attempts, raises after `attempts` exhausted with a
    message containing the label. Mirrors Mix.Tasks.Verify.ReleaseParity.retry_until!/4.

Test placement: `test/support` is in elixirc_paths(:test) (mix.exs:47), so Poll compiles under :test.
The new test lives at test/relyra/test_support/poll_test.exs (root project, run with `mix test <file>`).
For the exhausted path, use sleep_ms: 0 so the test does not actually sleep. An existing analog for the
ReleaseParity retry test is test/mix/tasks/verify_release_parity_test.exs:50 (mirror its shape).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit poll.ex and add its root unit test (success + exhausted)</name>
  <files>test/support/poll.ex, test/relyra/test_support/poll_test.exs</files>
  <read_first>
    - test/support/poll.ex (the untracked module being committed — retry_until!/4 + format_reason/1)
    - test/mix/tasks/verify_release_parity_test.exs (existing analog retry test — mirror its shape, especially line ~50)
    - .planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-RESEARCH.md (Open Q1 — resolution rationale; Validation Architecture — poll.ex row)
  </read_first>
  <behavior>
    - Success path: `Poll.retry_until!("label", 3, 0, fun)` where `fun` returns `:ok` (immediately, or
      after N<attempts transient `{:error, _}`) returns `:ok`.
    - Exhausted path: `Poll.retry_until!("label", 2, 0, fun)` where `fun` always returns `{:error, reason}`
      raises a RuntimeError whose message contains the label and "failed after 2 attempts".
  </behavior>
  <action>
    Create `test/relyra/test_support/poll_test.exs` as an `ExUnit.Case` covering
    `Relyra.TestSupport.Poll.retry_until!/4`:
    (1) success-immediate — a fun returning `:ok` returns `:ok`;
    (2) success-after-retry — a fun (using an Agent or a process-dictionary counter) returning `{:error, _}`
        on the first attempt then `:ok` returns `:ok` (use `sleep_ms: 0` to avoid real sleeping);
    (3) exhausted — a fun always returning `{:error, "boom"}` with `attempts: 2, sleep_ms: 0`
        `assert_raise RuntimeError` with message =~ the label AND =~ "failed after 2 attempts".
    Then `git add test/support/poll.ex test/relyra/test_support/poll_test.exs` so the previously-untracked
    module is committed alongside its consumer. Do NOT rewire ReleaseParity or CI probes (option (a) is
    explicitly NOT chosen — option (b) keeps blast radius minimal). Run `mix format` on the new test so
    `mix format --check-formatted` stays green.
  </action>
  <verify>
    <automated>mix test test/relyra/test_support/poll_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION row Hygiene: `mix test test/relyra/test_support/poll_test.exs` is green with success-immediate, success-after-retry, and exhausted (assert_raise =~ label + "failed after 2 attempts") rows.
    - CLI output: `git ls-files test/support/poll.ex` returns the path (file is now tracked, no longer untracked).
    - Source assertion: `grep -c "Poll.retry_until!" test/relyra/test_support/poll_test.exs` returns ≥1 (the module has a real consumer — not a dead module).
    - Gate: `mix test --warnings-as-errors test/relyra/test_support/poll_test.exs` exits 0 (no unused-module / unused-function warnings escalating to errors); `mix format --check-formatted` exits 0.
  </acceptance_criteria>
  <done>poll.ex is git-tracked, poll_test.exs exists and is green, retry_until!/4 has a real consumer on both success and exhausted paths, and formatting is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the stale local wip/demo-fake-idp branch</name>
  <files>(git state only — no source files)</files>
  <read_first>
    - .planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-RESEARCH.md (Runtime State Inventory — wip/demo-fake-idp is local-only at 579217a, no remote tracking; work shipped on phase/57-demo-fake-idp)
    - .planning/STATE.md (Roadmap Evolution — confirms Phase 57 complete, WIP parked on wip/demo-fake-idp)
  </read_first>
  <action>
    Confirm Phase 57 work is preserved before deleting: `git log --oneline phase/57-demo-fake-idp -1`
    (the shipped branch) and `git branch -r --contains wip/demo-fake-idp` (verify the WIP commit's content
    is not uniquely stranded). Then delete the stale local-only branch: `git branch -D wip/demo-fake-idp`.
    The branch has no remote tracking (RESEARCH Runtime State Inventory) so no remote prune is needed. Do
    NOT delete the current working branch `phase/57-demo-fake-idp`. If the branch is already gone, record
    that and treat the task as satisfied (idempotent).
  </action>
  <verify>
    <automated>git branch --list wip/demo-fake-idp | grep -c . | grep -qx 0 && echo "branch gone"</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `git branch --list wip/demo-fake-idp` returns empty (the stale branch is deleted).
    - CLI output: `git rev-parse --verify phase/57-demo-fake-idp` succeeds (the shipped Phase 57 branch is untouched).
  </acceptance_criteria>
  <done>The local-only wip/demo-fake-idp branch no longer exists; the shipped phase/57-demo-fake-idp branch is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| version-control state | An untracked source module and an orphaned WIP branch are repo-hygiene state, not a runtime trust boundary; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57.1-08 | (Integrity / hygiene) | test/support/poll.ex | mitigate | Commit the module and give it a real consumer (unit test) so it is not a dead, untracked module silently shipping under elixirc_paths(:test) |
| T-57.1-09 | N/A — repo hygiene | wip/demo-fake-idp branch | accept-then-remove | No runtime exposure; the stale local branch is deleted to prevent confusion. No security impact. |

**Invariant check:** No application/security code changes. Nothing under `lib/relyra/**` is touched; the
verifier gate and all six CLAUDE.md non-negotiable invariants are untouched. This plan only adds a test
and changes git tracking state.
</threat_model>

<verification>
- `mix test test/relyra/test_support/poll_test.exs` green.
- `mix test --warnings-as-errors` green (no dead-module / unused warnings from poll.ex).
- `git ls-files test/support/poll.ex` returns the path; `git branch --list wip/demo-fake-idp` empty.
- Wave gate: `mix qa` exits 0 with no unstaged `mix format` changes.
</verification>

<success_criteria>
- `poll.ex` committed AND consumed by a green unit test (no dead module under --warnings-as-errors).
- `retry_until!/4` covered on success + exhausted paths.
- Stale local `wip/demo-fake-idp` branch deleted; shipped `phase/57-demo-fake-idp` intact.
- No file under `lib/relyra/` modified.
</success_criteria>

<output>
Create `.planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-03-SUMMARY.md` when done.
</output>
