---
phase: 62-real-world-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - README.md
  - brandbook/assets/exdoc-logo.png
  - brandbook/assets/exdoc-favicon.png
  - brandbook/assets/social-card.svg
  - brandbook/assets/social-card.png
  - brandbook/assets/readme-banner.svg
  - demo/ledger_loop/priv/static/assets/css/app.css
autonomous: true
requirements: [INTEG-01, INTEG-02, INTEG-03]

must_haves:
  truths:
    - "mix.exs docs() includes a logo: and favicon: pointing at committed brandbook PNG assets; mix docs builds without error and the generated doc/assets contains the logo."
    - "An OpenGraph social card (SVG source + optimized PNG ≤90KB) and a README header banner (SVG) exist and the README references the banner above the # Relyra heading."
    - "demo/ledger_loop app.css :root remaps the --ll-* variables to the brand palette; the demo still compiles and its CSS is unchanged structurally (only the 9 :root values change)."
    - "No security/lib/protocol files changed; no public API or version bump; all brand source stays vector-first with only the two required PNGs added."
  artifacts:
    - path: "mix.exs"
      provides: "ex_doc logo + favicon wired"
      contains: "logo:"
    - path: "brandbook/assets/readme-banner.svg"
      provides: "README header banner"
      contains: "Relyra"
    - path: "demo/ledger_loop/priv/static/assets/css/app.css"
      provides: "brand-reskinned demo"
      contains: "3454D1"
---

<objective>
Wire the Relyra brand into the real world: HexDocs logo+favicon (ex_doc), an OpenGraph social card, a README header banner, and a brand reskin of the ledger_loop demo. Minimal, additive, reversible. The only files outside brandbook/ are mix.exs (docs config), README.md (banner line), and the demo's app.css (9 :root values).
</objective>

<context>
@brandbook/notes/decision-log.md
@brandbook/assets/logo-mark.svg
@brandbook/assets/logo-primary.svg

<boundary>
Touch ONLY: mix.exs, README.md, demo/ledger_loop/priv/static/assets/css/app.css, and new files under brandbook/assets/. Do NOT touch lib/, any *.ex security/protocol file, the @version, package metadata other than docs(), or any guide. No public API or version change.
</boundary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raster brand assets for ex_doc (PNG required)</name>
  <files>brandbook/assets/exdoc-logo.png, brandbook/assets/exdoc-favicon.png</files>
  <read_first>
    - brandbook/assets/logo-mark.svg (source mark — currentColor strokes + teal dot)
    - brandbook/assets/favicon.svg (favicon source)
  </read_first>
  <action>ex_doc's logo: and favicon: require raster (PNG/JPEG) — SVG is rejected. Produce a colored PNG of the mark (ex_doc sidebar is light by default): first make a temporary explicit-color copy of logo-mark.svg with the strokes set to Ink #101827 and the dot Proof Teal #147D77 (replace `currentColor` with `#101827`), render it to a 120x120 transparent PNG as `brandbook/assets/exdoc-logo.png`, and a 48x48 transparent PNG of favicon.svg (strokes Ink, dot teal) as `brandbook/assets/exdoc-favicon.png`. Use the available rasterizer (`qlmanage -t -s 120 -o <dir> file.svg` then trim/resize with `magick`, OR `magick -background none file.svg -resize 120x120 out.png`). Optimize: each PNG < 12KB (run `magick out.png -strip out.png` or oxipng/pngquant if present). Confirm both are valid PNGs with transparency.</action>
  <verify>
    <automated>cd /Users/jon/projects/relyra && file brandbook/assets/exdoc-logo.png | grep -q "PNG image" && file brandbook/assets/exdoc-favicon.png | grep -q "PNG image" && [ $(stat -f%z brandbook/assets/exdoc-logo.png) -lt 16000 ] && echo "rasters OK"</automated>
  </verify>
  <acceptance_criteria>
    - brandbook/assets/exdoc-logo.png and exdoc-favicon.png are valid PNG images.
    - exdoc-logo.png is < 16KB.
  </acceptance_criteria>
  <done>Two optimized PNG rasters of the mark exist for ex_doc.</done>
</task>

<task type="auto">
  <name>Task 2: Wire logo + favicon into mix.exs docs()</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (the defp docs function — add keys, change nothing else)
  </read_first>
  <action>In the `docs/0` keyword list in mix.exs, add two keys (leave everything else untouched): `logo: "brandbook/assets/exdoc-logo.png"` and `favicon: "brandbook/assets/exdoc-favicon.png"`. Place them near `main:`/`source_ref:`. Do not change @version, extras, groups, package, or any other function.</action>
  <verify>
    <automated>cd /Users/jon/projects/relyra && grep -q 'logo: "brandbook/assets/exdoc-logo.png"' mix.exs && grep -q 'favicon: "brandbook/assets/exdoc-favicon.png"' mix.exs && mix docs >/tmp/relyra_docs.log 2>&1 && test -f doc/assets/exdoc-logo.png -o -f doc/exdoc-logo.png && echo "mix docs OK with logo" || (echo "DOCS BUILD OUTPUT:"; tail -20 /tmp/relyra_docs.log)</automated>
  </verify>
  <acceptance_criteria>
    - mix.exs docs() contains logo: and favicon: keys pointing at the brandbook PNGs.
    - `mix docs` exits 0 and copies the logo into the doc output (doc/assets/).
    - @version and all other mix.exs content unchanged.
  </acceptance_criteria>
  <done>ex_doc renders the brand logo + favicon; mix docs builds clean.</done>
</task>

<task type="auto">
  <name>Task 3: OpenGraph social card (SVG → PNG)</name>
  <files>brandbook/assets/social-card.svg, brandbook/assets/social-card.png</files>
  <read_first>
    - brandbook/assets/logo-primary.svg
    - brandbook/notes/decision-log.md (Canonical Lock Set colors + tagline)
  </read_first>
  <action>Create `brandbook/assets/social-card.svg` at 1200x630 — an OpenGraph card: a brand-panel background (Ink #101827 or Deep Relay #1D2E82), the logo (mark + "Relyra" in Paper #FCFBF7, verification dot Proof Teal #147D77), the tagline "Enterprise SAML, calmly verified." in Paper, and a small muted sub-line "Strict-by-default SAML 2.0 SP for Elixir & Phoenix". Draw text as <text> with the IBM Plex / Atkinson font-family fallbacks; embed the mark geometry inline (Paper-colored). This is a marketing card, so a background panel is correct here (the logomark assets themselves remain cage-free). Then rasterize to `brandbook/assets/social-card.png` at 1200x630 and optimize to <= 90KB (pngquant/oxipng if present, else `magick ... -strip`). Confirm PNG dimensions 1200x630.</action>
  <verify>
    <automated>cd /Users/jon/projects/relyra && xmllint --noout brandbook/assets/social-card.svg && file brandbook/assets/social-card.png | grep -q "1200 x 630" && [ $(stat -f%z brandbook/assets/social-card.png) -lt 95000 ] && echo "social card OK"</automated>
  </verify>
  <acceptance_criteria>
    - social-card.svg is valid; social-card.png is 1200x630 and < 95KB.
    - The card shows the logo + tagline.
  </acceptance_criteria>
  <done>OG social card exists as SVG source + optimized PNG.</done>
</task>

<task type="auto">
  <name>Task 4: README header banner</name>
  <files>brandbook/assets/readme-banner.svg, README.md</files>
  <read_first>
    - README.md (top — the # Relyra heading + badges)
    - brandbook/assets/logo-primary.svg
  </read_first>
  <action>Create `brandbook/assets/readme-banner.svg` — a wide (~900x200) header banner with EXPLICIT colors (so it renders identically on GitHub light and dark): a clean Ink #101827 panel, the logo (mark + "Relyra" in Paper, teal dot) centered, and the tagline "Enterprise SAML, calmly verified." in a muted Paper tone below. (A banner with a background panel is a legitimate marketing artifact; the standalone logomark assets remain cage-free.) Then edit README.md: insert, ABOVE the `# Relyra` line, a centered banner reference: a line `<p align="center"><img src="brandbook/assets/readme-banner.svg" alt="Relyra — Enterprise SAML, calmly verified." width="720"></p>` followed by a blank line. Keep the existing `# Relyra` heading and all badges and content exactly as-is below it.</action>
  <verify>
    <automated>cd /Users/jon/projects/relyra && xmllint --noout brandbook/assets/readme-banner.svg && head -5 README.md | grep -q "readme-banner.svg" && grep -q "^# Relyra" README.md && echo "banner OK"</automated>
  </verify>
  <acceptance_criteria>
    - readme-banner.svg is valid and contains "Relyra" + the tagline.
    - README.md references the banner above the # Relyra heading; the heading and badges remain.
  </acceptance_criteria>
  <done>README shows a branded header banner; rest of README unchanged.</done>
</task>

<task type="auto">
  <name>Task 5: Reskin the ledger_loop demo with brand tokens</name>
  <files>demo/ledger_loop/priv/static/assets/css/app.css</files>
  <read_first>
    - demo/ledger_loop/priv/static/assets/css/app.css (the :root --ll-* block at the top)
    - brandbook/notes/decision-log.md (Canonical Lock Set)
  </read_first>
  <action>In `demo/ledger_loop/priv/static/assets/css/app.css`, change ONLY the 9 values in the `:root` `--ll-*` block to brand values (leave every selector and the rest of the file byte-identical): --ll-bg #FCFBF7 (Paper), --ll-panel #EEF2F6 (Mist), --ll-accent #3454D1 (Relay Blue), --ll-danger #B42318 (Error), --ll-success #027A48 (Success), --ll-warning #B45309 (Warning), --ll-text #101827 (Ink), --ll-muted #263245 (Graphite), --ll-border #7E8A9A (Accessible Border). Add a one-line comment above the block: `/* Relyra brand tokens (v1.8) — see brandbook/tokens/tokens.css */`. Do not add new vars, do not change any rule below the :root block.</action>
  <verify>
    <automated>cd /Users/jon/projects/relyra && grep -q "3454D1" demo/ledger_loop/priv/static/assets/css/app.css && grep -q "FCFBF7" demo/ledger_loop/priv/static/assets/css/app.css && grep -q "101827" demo/ledger_loop/priv/static/assets/css/app.css && echo "demo reskin OK"</automated>
  </verify>
  <acceptance_criteria>
    - app.css :root --ll-* values are the brand hexes (Relay Blue 3454D1, Paper FCFBF7, Ink 101827, etc.).
    - Only the 9 :root values + one comment line changed; every selector below :root is byte-identical.
  </acceptance_criteria>
  <done>The demo renders in brand colors via the 9 remapped --ll-* vars; no structural CSS change.</done>
</task>

</tasks>

<verification>
INTEG-01: mix.exs docs() has logo+favicon PNGs and `mix docs` builds with the logo in output. INTEG-02: social-card (SVG+PNG ≤90KB) + README banner SVG exist and README references the banner. INTEG-03: demo app.css :root remapped to brand tokens, demo still compiles. Phase goal met when the brand appears on HexDocs, in a social card, on the README, and in the demo — with no lib/protocol/version change. Post-checks (orchestrator): `mix docs` clean, demo compiles (`cd demo/ledger_loop && mix compile`), `git diff --stat` shows only the intended files.
</verification>
