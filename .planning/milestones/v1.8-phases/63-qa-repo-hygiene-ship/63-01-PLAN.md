---
phase: 63-qa-repo-hygiene-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/README.md
autonomous: true
requirements: [QA-01, QA-02]
must_haves:
  truths:
    - "All SVGs are optimized/minimal; no font binaries committed; repo-size budget held (brandbook/ < 1MB)."
    - "The milestone diff contains no unrelated changes (only brandbook/ + the 3 authorized integration files)."
    - "brandbook/README.md documents every artifact and how to preview it."
    - "mix qa exits 0."
---

<objective>
Final QA + repo hygiene for the v1.8 brand milestone, then closeout. Verify size budget, no fonts, clean diff; document the package; prove mix qa green.
</objective>

<tasks>
<task type="auto">
  <name>QA-01: hygiene</name>
  <action>Confirm: brandbook/ under 1MB; no committed .woff/.ttf/.otf; SVGs minimal (hand-authored, ~1KB each — svgo not required); PNGs small (ex_doc rasters <1KB, OG card <90KB). Audit `git diff --stat` across the milestone — only brandbook/ + README.md + mix.exs + demo app.css changed; no lib/, security, public API, or @version change.</action>
  <acceptance_criteria>
    - du -sh brandbook/ < 1MB; git ls-files shows no font binaries; diff audit shows only intended files.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>QA-02: document + prove green + close</name>
  <action>Write brandbook/README.md (artifact inventory + preview instructions + token usage + rules + license). Run mix qa to exit 0. Then close the milestone.</action>
  <acceptance_criteria>
    - brandbook/README.md exists and lists every artifact + preview steps.
    - mix qa exits 0.
  </acceptance_criteria>
</task>
</tasks>

<verification>
QA-01: 316K brandbook/, no fonts, clean diff (37 files: brandbook/ + README.md + mix.exs + demo app.css only). QA-02: brandbook/README.md written; `mix qa` → exit 0 (744 tests, 0 failures). Milestone v1.8 ready to close.
</verification>
