---
phase: 58-brand-foundation-pressure-test-decision-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/notes/contrast.exs
  - brandbook/notes/accessibility-checks.md
  - brandbook/notes/decision-log.md
  - brandbook/notes/research.md
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03]

must_haves:
  truths:
    - "Running the contrast script computes a WCAG ratio + verdict for every realistic brand color pair in both light and dark mode, and exits 0 (all must-pass pairs meet their required ratio after remediation)."
    - "accessibility-checks.md is generated from the script output (not hand-typed) and contains a row for Certificate Gold on Paper at ratio 2.93 with a remediation hex."
    - "decision-log.md records every pressure-test finding as a Decision/Options/Tradeoffs/note/Disposition/Confidence block, including the five confirmed findings and the thin-margin locks."
    - "decision-log.md ends with a Canonical Lock Set section: exactly one final hex per role, one type scale, and the locked voice/tagline, with no competing alternatives."
    - "research.md preserves the cited synthesis (WCAG 2.2, Radix, Primer, GitHub) that downstream phases can cite."
    - "No file outside brandbook/notes/ is modified (lib/, demo, README, mix.exs untouched)."
  artifacts:
    - path: "brandbook/notes/contrast.exs"
      provides: "WCAG 2.2 relative-luminance + contrast-ratio calculator that drives the pair list, emits the accessibility table, and exits 1 if any must-pass pair is below required ratio"
      contains: "0.04045"
      min_lines: 40
    - path: "brandbook/notes/accessibility-checks.md"
      provides: "Generated pair | mode | use | computed ratio | required | pass/fail | remediation table (light + dark)"
      contains: "Certificate Gold"
      min_lines: 40
    - path: "brandbook/notes/decision-log.md"
      provides: "Per-finding dispositions + Canonical Lock Set section"
      contains: "Canonical Lock Set"
      min_lines: 80
    - path: "brandbook/notes/research.md"
      provides: "Cited synthesis of WCAG/design-system/OSS-credibility standards"
      contains: "WCAG"
      min_lines: 40
  key_links:
    - from: "brandbook/notes/accessibility-checks.md"
      to: "brandbook/notes/contrast.exs"
      via: "table generated from script stdout"
      pattern: "Certificate Gold.*2\\.93"
    - from: "brandbook/notes/decision-log.md Canonical Lock Set"
      to: "brandbook/notes/accessibility-checks.md verdicts"
      via: "remediated hexes match passing rows"
      pattern: "#64748B"
---

<objective>
Pressure-test the decision-complete brand book (`prompts/relyra-brand-book.md` v0.1) and lock its palette, type scale, and voice into authoritative decision documents under `brandbook/notes/`. This phase ships NO visual assets — only the WCAG-verified source of truth that phases 59-61 consume verbatim.

Purpose: Every downstream brand artifact (logos, tokens, HTML book, integrations) must build on a single, contrast-verified, contradiction-free set of values. This phase produces that set and proves it with re-runnable math.

Output:
- `brandbook/notes/contrast.exs` — committable WCAG 2.2 contrast script (stdlib Elixir; drives the pair list; emits the accessibility table; exits 1 on any must-pass failure).
- `brandbook/notes/accessibility-checks.md` — the generated pass/fail table (light + dark), produced FROM the script, not hand-guessed.
- `brandbook/notes/decision-log.md` — every pressure-test finding with ship/reject/defer disposition + confidence, ending in a Canonical Lock Set section.
- `brandbook/notes/research.md` — the cited synthesis condensed from 58-RESEARCH.md with sources preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-brand-foundation-pressure-test-decision-lock/58-CONTEXT.md
@.planning/phases/58-brand-foundation-pressure-test-decision-lock/58-RESEARCH.md
@.planning/phases/58-brand-foundation-pressure-test-decision-lock/58-VALIDATION.md
@prompts/relyra-brand-book.md

<boundary>
HARD SCOPE: Touch ONLY files under `brandbook/notes/`. Do NOT modify `lib/`, the demo app (`demo/ledger_loop`), `README.md`, `mix.exs`, or any token/HTML/SVG file. Those are phases 59-63. No packages are installed (the contrast script is pure Elixir stdlib `:math` / `String`); the Package Legitimacy Audit is N/A for this phase — do not insert a package checkpoint.
</boundary>

<footgun>
KNOWN GSD FOOTGUN: The decision-coverage gate silently drops decision IDs when a parsed file contains literal triple-backtick fences. The contrast script is the ONLY file that may contain code; reference it by PATH from `decision-log.md` and `accessibility-checks.md` rather than re-embedding it in a fenced block. Use indented examples (not triple-backtick fences) inside the per-decision blocks of `decision-log.md`.
</footgun>

<authoritative_values>
Confirmed from `prompts/relyra-brand-book.md` and 58-RESEARCH.md (computed on-machine):

Light palette: Ink #101827, Graphite #263245, Paper #FCFBF7, Mist #EEF2F6, Relay Blue #3454D1, Deep Relay #1D2E82, Proof Teal #147D77, Keyline Violet #6D5DF2, Accessible Border #7E8A9A, Soft Line #D8E0EA, Certificate Gold #C08A2B.
Semantic: Success #027A48, Warning #B45309, Error #B42318, Info #3454D1.
Dark palette: Dark Background #0B1020, Dark Surface #111827, Dark Border #334155, Dark Text #F8FAFC, Dark Muted #CBD5E1, Dark Blue #8CA2FF, Dark Teal #61D6C8.

Confirmed FAILURES (must remediate or downgrade in decision-log):
- Certificate Gold #C08A2B on Paper #FCFBF7 = 2.93 (< 3:1 non-text FAIL) → remediate to #A8741F (3.91 non-text) or #9A6B1C (4.51 text-capable). Research default recommendation: text-capable #9A6B1C.
- Dark Border #334155 on #0B1020 = 1.83, on #111827 = 1.71 (< 3:1 FAIL) AND contradicts the book's own dark CSS block `--relyra-border: #64748B`. Resolve to #64748B (3.73-3.98). This is the canonical "contradiction + contrast failure fixed together" entry.
- Soft Line #D8E0EA on Paper = 1.29 — structural: keep decorative-only; route real control boundaries to Accessible Border #7E8A9A.
- Info #3454D1 == Relay Blue #3454D1 collision — surface as explicit decision (ship-the-collision default OR nudge Info), do not silently fix.

PASSES-BUT-FRAGILE (flag thin margin, no remediation required): Proof Teal 4.80, Keyline Violet 4.51, Warning 4.85, Accessible Border on Mist 3.12.

Voice/tagline lock: canonical tagline "Enterprise SAML, calmly verified."; demote the other five §1 options to "alternate, non-canonical." Type scale: IBM Plex Sans Condensed (display), Atkinson Hyperlegible (body), JetBrains Mono (code).

WCAG thresholds: text 4.5:1, large text 3:1, non-text/UI 3:1. Use the 0.04045 linearization threshold. Verdict compares UNROUNDED ratio; display rounds to 2dp.
</authoritative_values>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Create the committed WCAG 2.2 contrast script</name>
  <files>brandbook/notes/contrast.exs</files>
  <read_first>
    - 58-RESEARCH.md sections "WCAG 2.2 Contrast — The Hard Gate", "Code Examples" (the verified script + table generator), and "Computed results for the actual Relyra palette" (the expected ratios).
    - prompts/relyra-brand-book.md color sections (≈ lines 455-484, 516-545) for the canonical hex values and dark CSS block.
  </read_first>
  <behavior>
    - srgb_to_lin uses the 0.04045 threshold (cs/12.92 below, ((cs+0.055)/1.055)^2.4 above).
    - luminance("#101827") and luminance("101827") both work (strip leading #).
    - check(fg, bg, required) returns {rounded_ratio, passes?} where passes? compares the UNROUNDED ratio (per W3C no-rounding rule), display rounds to 2dp.
    - check("#101827", "#FCFBF7", 4.5) yields display ratio 17.16 and pass=true (Ink on Paper).
    - check("#C08A2B", "#FCFBF7", 3.0) yields 2.93 and pass=false (Certificate Gold on Paper, the load-bearing failure).
    - check("#64748B", "#0B1020", 3.0) passes (~3.98) and check("#334155", "#0B1020", 3.0) fails (~1.83) — proving the dark-border remediation.
    - The script drives a list of {fg, bg, mode, use, required, must_pass?} tuples covering at minimum: body text, secondary text, links/primary action, success/warning/error/info on their surfaces, focus ring, interactive border, soft-line divider, disabled — in BOTH light and dark mode. It includes the remediated hexes (#9A6B1C gold, #64748B dark border) as passing rows.
    - The script prints a Markdown table (pair | mode | use | computed ratio | required | pass/fail | remediation) to stdout.
    - After printing, the script exits 1 if any tuple marked must_pass? is below its required ratio (unremediated); exits 0 otherwise.
  </behavior>
  <action>Create `brandbook/notes/contrast.exs` as a self-contained Elixir stdlib script (no ExUnit, no deps). Implement the WCAG module per 58-RESEARCH.md "Code Examples": srgb_to_lin with the 0.04045 threshold, luminance/1 accepting "#RRGGBB" or "RRGGBB", and check/3 returning {Float.round(raw,2), raw >= required} with the verdict on the UNROUNDED value. Define a pairs list of {fg, bg, mode, use, required, must_pass} tuples covering all realistic light+dark pairings named in 58-RESEARCH.md (body text Ink/Graphite on Paper; Relay Blue link and Paper-on-Relay-Blue button; Success #027A48 / Warning #B45309 / Error #B42318 / Info #3454D1 on Paper; Keyline Violet #6D5DF2 focus ring; Accessible Border #7E8A9A on Paper and Mist #EEF2F6; Soft Line #D8E0EA divider on Paper; the dark set Dark Text #F8FAFC / Dark Muted #CBD5E1 / Dark Blue #8CA2FF / Dark Teal #61D6C8 / Dark Violet focus on #0B1020 and #111827; the two FAILING rows Certificate Gold #C08A2B-on-Paper and Dark Border #334155-on-dark; and the REMEDIATED rows #9A6B1C gold and #64748B dark border). Mark the must_pass flag true for intended-use pairs and include a remediation note column for any failing row. Map each pair to its required ratio per WCAG thresholds (text 4.5, large/non-text 3.0). Print a Markdown table to stdout, then `System.halt(1)` if any must_pass tuple is below required (using the remediated hexes so the committed script exits 0), else `System.halt(0)`. Use the 0.04045 constant and document the choice in a comment.</action>
  <verify>
    <automated>cd /Users/jon/projects/relyra && elixir brandbook/notes/contrast.exs; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `elixir brandbook/notes/contrast.exs` exits 0 (all must-pass pairs meet required ratio with remediated hexes).
    - stdout contains a Markdown table with a Certificate Gold on Paper row showing ratio 2.93 and a FAIL/remediation note.
    - stdout contains both a Dark Border #334155 FAIL row and a #64748B PASS row.
    - The file contains the literal string `0.04045`.
    - The file is at least 40 lines and uses only Elixir stdlib (no `Mix.install`, no external deps).
  </acceptance_criteria>
  <done>The contrast script runs, reproduces the research ratios (Ink/Paper 17.16, Gold/Paper 2.93, Dark Border #334155 fail, #64748B pass), emits the table, and exits 0.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 1): Generate accessibility-checks.md and write research.md</name>
  <files>brandbook/notes/accessibility-checks.md, brandbook/notes/research.md</files>
  <read_first>
    - brandbook/notes/contrast.exs (created in Task 1) — its stdout is the source of accessibility-checks.md.
    - 58-RESEARCH.md sections "Computed results", "Remediation hex candidates", "Design-System Audit Rubrics", "OSS Devtool Brand Credibility", "Don't Hand-Roll", "Sources".
  </read_first>
  <action>Run `elixir brandbook/notes/contrast.exs` and capture its stdout. Write `brandbook/notes/accessibility-checks.md` by taking the GENERATED table verbatim (do not hand-type ratios) with a short header that names the source script by PATH (`brandbook/notes/contrast.exs`), states the WCAG 2.2 thresholds (text 4.5:1, large 3:1, non-text 3:1), the 0.04045 linearization constant, and the no-rounding verdict rule. Group rows into a Light-mode section and a Dark-mode section. Below the table add a "Confirmed failures and remediations" subsection listing Certificate Gold #C08A2B (2.93 → #9A6B1C 4.51 text-capable, alt #A8741F 3.91 non-text), Dark Border #334155 (1.71-1.83 → #64748B 3.73-3.98), and Soft Line #D8E0EA (1.29, structural: decorative-only, route boundaries to Accessible Border #7E8A9A), plus a "Passes-but-fragile" subsection (Proof Teal 4.80, Keyline Violet 4.51, Warning 4.85, Accessible Border-on-Mist 3.12). Do NOT embed the script in a fenced block here — reference it by path. Then write `brandbook/notes/research.md` as a condensed cited synthesis derived from 58-RESEARCH.md: the WCAG 2.2 gate (cite w3.org 1.4.3 + 1.4.11), the Radix 5-tier + Primer 3-layer token model (cite radix-ui.com/colors, primer.style), GitHub monochrome-first OSS-credibility lessons (cite brand.github.com), the four-lens pressure-test method, and the APCA-is-not-the-gate note (cite adrianroselli.com). Preserve the Sources list (Primary/Secondary/Tertiary) from 58-RESEARCH.md so downstream phases can cite it.</action>
  <verify>
    <automated>cd /Users/jon/projects/relyra && elixir brandbook/notes/contrast.exs > /tmp/contrast_out.txt 2>&1 && awk -F'|' 'NF>3 && /[0-9]\.[0-9]/' /tmp/contrast_out.txt | while IFS= read -r r; do grep -qF "$r" brandbook/notes/accessibility-checks.md || { echo "HAND-TYPED OR MISSING ROW: $r"; exit 1; }; done && grep -q "Certificate Gold" brandbook/notes/accessibility-checks.md && grep -q "2.93" brandbook/notes/accessibility-checks.md && grep -q "#64748B" brandbook/notes/accessibility-checks.md && grep -q "WCAG" brandbook/notes/research.md && grep -qi "radix\|primer" brandbook/notes/research.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - brandbook/notes/accessibility-checks.md contains the GENERATED table (rows for Certificate Gold on Paper at 2.93, Dark Border #334155 FAIL, #64748B PASS) — ratios match contrast.exs stdout, not hand-typed.
    - accessibility-checks.md names the source script by path (brandbook/notes/contrast.exs), states WCAG thresholds (4.5:1 / 3:1) and the 0.04045 constant, and has separate Light-mode and Dark-mode sections.
    - accessibility-checks.md has a "Confirmed failures and remediations" subsection (Certificate Gold, Dark Border, Soft Line) and a "Passes-but-fragile" subsection.
    - brandbook/notes/research.md contains the string "WCAG" and at least one of "Radix"/"Primer", and preserves a Sources list with at least the W3C WCAG 2.2 URLs.
    - Neither file contains a triple-backtick fenced block.
  </acceptance_criteria>
  <done>accessibility-checks.md is generated verbatim from the script (no hand-typed ratios) with failures+remediations and fragile flags; research.md is the condensed cited synthesis with sources preserved.</done>
</task>

<task type="auto">
  <name>Task 3 (Wave 1): Write decision-log.md with dispositions and the Canonical Lock Set</name>
  <files>brandbook/notes/decision-log.md</files>
  <read_first>
    - brandbook/notes/accessibility-checks.md (created in Task 2) — the verdicts every disposition must agree with.
    - 58-RESEARCH.md sections "Pressure-test findings", "Semantic collision", "Token sprawl / neutrals", "Design-System Audit Rubrics".
    - prompts/relyra-brand-book.md sections 1 (taglines), 8 (color), 9 (type), 22 (do/don't) for the values being locked.
    - .planning/phases/58-brand-foundation-pressure-test-decision-lock/58-CONTEXT.md (locked constraints — forbidden imagery/claims must not be relitigated).
  </read_first>
  <action>Write `brandbook/notes/decision-log.md`. Open with a one-paragraph method note naming the four lenses (graphic-design director, design-system lead, accessibility specialist, OSS-credibility red-team). Then record each finding as a block using indented plain-text fields (NEVER triple-backtick fences) in this exact shape: a bold "Decision D-NN: <title>" line, then indented lines "Options:", "Tradeoffs:", "A11y/ecosystem note:", "Disposition: ship|reject|defer", "Confidence: HIGH|MEDIUM|LOW". Include at minimum these decisions: D-01 Certificate Gold #C08A2B fails non-text (2.93) → remediate to text-capable #9A6B1C (ship, HIGH); D-02 Dark Border #334155 fails (1.71-1.83) AND contradicts the book's own dark CSS `--relyra-border: #64748B` → adopt #64748B everywhere, delete #334155 (ship, HIGH); D-03 Soft Line #D8E0EA (1.29) stays decorative-divider-only, real control boundaries use Accessible Border #7E8A9A (ship, HIGH); D-04 Info #3454D1 is byte-identical to Relay Blue/primary action → ship the collision WITH documentation that Info uses an icon+label affordance (not color alone) to disambiguate from a clickable primary (ship, MEDIUM; record the alternate of nudging Info as deferred); D-05 thin-margin passes (Proof Teal 4.80, Keyline Violet 4.51, Warning 4.85, Accessible-Border-on-Mist 3.12) are locked but flagged do-not-darken-surfaces (ship, MEDIUM); D-06 canonical tagline is "Enterprise SAML, calmly verified." and the other five §1 options are demoted to non-canonical alternates (ship, HIGH); D-07 type scale locked to IBM Plex Sans Condensed (display) / Atkinson Hyperlegible (body+UI) / JetBrains Mono (code), one role each (ship, HIGH; note OFL license verification is deferred to the token/integration phase); D-08 neutral sprawl resolved — keep Ink/Graphite/Paper/Mist plus one accessible border + one decorative soft-line, no extra near-duplicate greys (ship, HIGH). End the file with a clearly-labeled "## Canonical Lock Set" section that lists EXACTLY ONE final hex per role (light + dark) reflecting the remediations (gold #9A6B1C, dark border #64748B), the locked type scale, and the locked voice/tagline — formatted as a simple table or indented list, no competing alternatives, so phases 60 and 61 lift it verbatim.</action>
  <verify>
    <automated>cd /Users/jon/projects/relyra && grep -q "Canonical Lock Set" brandbook/notes/decision-log.md && grep -q "#9A6B1C" brandbook/notes/decision-log.md && grep -q "#64748B" brandbook/notes/decision-log.md && grep -Eq "Disposition: (ship|reject|defer)" brandbook/notes/decision-log.md && ! grep -q "$(printf '\140\140\140')" brandbook/notes/decision-log.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - brandbook/notes/decision-log.md contains a "## Canonical Lock Set" section with exactly one final hex per role (including remediated gold #9A6B1C and dark border #64748B) plus the locked type scale and tagline.
    - Every finding block has a "Disposition: ship|reject|defer" line and a "Confidence:" line.
    - decision-log.md includes decisions D-01 through D-08 (Certificate Gold, Dark Border contradiction, Soft Line, Info collision, thin-margin locks, tagline, type scale, neutral sprawl).
    - decision-log.md dispositions agree with accessibility-checks.md verdicts (no remediated hex below its required ratio).
    - decision-log.md contains no triple-backtick fenced block.
  </acceptance_criteria>
  <done>decision-log.md records D-01..D-08 with ship/reject/defer + confidence and ends in a single Canonical Lock Set (one hex per role, locked type scale + voice) that phases 60-61 lift verbatim.</done>
</task>

</tasks>

<verification>
Phase goal is met when: (1) `elixir brandbook/notes/contrast.exs` exits 0; (2) accessibility-checks.md is generated from that script and shows every realistic pair (light+dark) with a pass/fail verdict; (3) decision-log.md dispositions every finding and ends in a single Canonical Lock Set; (4) research.md preserves the cited synthesis; (5) nothing outside `brandbook/notes/` changed. Requirements covered: BRAND-01 (decision-log dispositions + research synthesis), BRAND-02 (accessibility-checks + contrast.exs), BRAND-03 (Canonical Lock Set with one definition per role).
</verification>
