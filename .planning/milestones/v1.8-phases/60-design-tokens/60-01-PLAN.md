---
phase: 60-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tokens/tokens.json
  - brandbook/tokens/tokens.css
  - brandbook/tokens/tailwind.example.js
autonomous: true
requirements: [TOKEN-01, TOKEN-02]

must_haves:
  truths:
    - "tokens.json is valid JSON and covers color (primitive + semantic), light/dark, focus ring, type, spacing, radius, border, shadow, and motion."
    - "tokens.css exposes the semantic tokens as CSS custom properties with a light default and a dark override (prefers-color-scheme + a [data-theme=dark] hook)."
    - "tailwind.example.js maps the tokens for a Phoenix consumer without re-deriving values."
    - "Every color value matches the Phase 58 Canonical Lock Set exactly (gold #9A6B1C, dark border #64748B, etc.) — no contrast-failing values reintroduced."
    - "No token sprawl: every token has a role; no near-duplicate neutrals beyond the locked set."
  artifacts:
    - path: "brandbook/tokens/tokens.json"
      provides: "Primitive + semantic design tokens, light/dark"
      contains: "9A6B1C"
      min_lines: 60
    - path: "brandbook/tokens/tokens.css"
      provides: "CSS custom properties with light default + dark override"
      contains: "prefers-color-scheme"
      min_lines: 40
    - path: "brandbook/tokens/tailwind.example.js"
      provides: "Tailwind theme.extend mapping for Phoenix consumers"
      contains: "extend"
      min_lines: 30
---

<objective>
Publish the Relyra design tokens as `brandbook/tokens/{tokens.json, tokens.css, tailwind.example.js}`, derived verbatim from the Phase 58 Canonical Lock Set and the brand book scales. No token sprawl — every token earns its place.
</objective>

<context>
@brandbook/notes/decision-log.md
@brandbook/notes/accessibility-checks.md

<boundary>
Touch ONLY `brandbook/tokens/`. Do not modify lib/, demo, README, mix.exs, or other brandbook files. Source of truth = the "Canonical Lock Set" in decision-log.md. Re-confirm no value below its WCAG requirement (accessibility-checks.md).
</boundary>
</context>

<token_spec>
Authoritative values (from Canonical Lock Set + brand book §9/§10):

PRIMITIVES (light): ink #101827, graphite #263245, paper #FCFBF7, mist #EEF2F6, relay-blue #3454D1, deep-relay #1D2E82, proof-teal #147D77, keyline-violet #6D5DF2, accessible-border #7E8A9A, soft-line #D8E0EA, certificate-gold #9A6B1C.
PRIMITIVES (dark): dark-bg #0B1020, dark-surface #111827, dark-border #64748B, dark-text #F8FAFC, dark-muted #CBD5E1, dark-blue #8CA2FF, dark-teal #61D6C8, dark-violet #A99BFF, dark-amber #FBBF24, dark-red #FDA29B, dark-green #86EFAC, dark-emphasis #A9B7FF.
SEMANTIC (light): bg=paper, surface=#fff (cards) / mist (subtle), text=ink, text-muted=graphite, border=accessible-border #7E8A9A, divider=soft-line #D8E0EA, action/link=relay-blue, action-emphasis=deep-relay, verified=proof-teal, focus-ring=keyline-violet #6D5DF2, caution=certificate-gold #9A6B1C, success #027A48, warning #B45309, error #B42318, info #3454D1 (note: = action; disambiguate by icon+label, never color alone).
SEMANTIC (dark): bg=dark-bg, surface=dark-surface, text=dark-text, text-muted=dark-muted, border=dark-border #64748B, divider=dark-border, action/link=dark-blue, action-emphasis=dark-emphasis #A9B7FF, verified=dark-teal, focus-ring=dark-violet #A99BFF, caution=dark-amber, success=dark-green, warning=dark-amber, error=dark-red, info=dark-blue.

TYPE: families — display "IBM Plex Sans Condensed","IBM Plex Sans",system-ui,sans-serif; body "Atkinson Hyperlegible",system-ui,-apple-system,"Segoe UI",sans-serif; code "JetBrains Mono",ui-monospace,SFMono-Regular,Menlo,Consolas,monospace.
Scale (size / line-height / weight): hero 64px(4rem)/1.0/700, h1 44px(2.75rem)/1.05/700, h2 32px(2rem)/1.12/650→use 600, h3 24px(1.5rem)/1.2/600, body-lg 19px(1.1875rem)/1.55/400, body 16px(1rem)/1.55/400, small 14px(0.875rem)/1.45/500, caption 12.5px(0.78rem)/1.4/500, code 14px(0.875rem)/1.55/450→use 500. (CSS weights: 400/500/600/700 — Atkinson/Plex only ship those; pick nearest.)

SPACE (px): 2,4,8,12,16,24,32,48,64,96,128 → name 0.5,1,2,3,4,6,8,12,16,24,32 (×4 base) or keep raw px keys. Use raw px keys: "2","4","8","12","16","24","32","48","64","96","128".
RADIUS: sm 8px (controls), md 12px (cards), lg 16px (panels), full 9999px (badges only).
BORDER-WIDTH: hairline 1px, regular 2px.
SHADOW: sm "0 1px 2px rgba(16,24,39,0.06)", md "0 1px 2px rgba(16,24,39,0.06), 0 8px 24px rgba(16,24,39,0.06)". (dark: bump alpha to ~0.4 over near-black.)
MOTION: duration fast 120ms, base 180ms, slow 220ms; easing standard "cubic-bezier(0.2,0,0,1)" (calm ease-out); honor prefers-reduced-motion (reduce to ~0.01ms).
FOCUS RING: color = focus-ring semantic; width 2px; offset 2px; style solid.
</token_spec>

<tasks>

<task type="auto">
  <name>Task 1: tokens.json</name>
  <files>brandbook/tokens/tokens.json</files>
  <read_first>
    - brandbook/notes/decision-log.md (Canonical Lock Set — the authoritative values)
    - brandbook/notes/accessibility-checks.md (confirm no value below its WCAG requirement)
  </read_first>
  <action>Create `brandbook/tokens/tokens.json` as valid JSON with top-level groups: color (with nested primitive {light, dark} and semantic {light, dark}), typography (fontFamily {display,body,code}, fontSize, lineHeight, fontWeight), space, radius, borderWidth, boxShadow {light,dark}, motion {duration,easing,reducedMotion}, focusRing {color,width,offset,style}. Use the exact hexes from token_spec. Add a leading "$meta" key noting source = "Phase 58 Canonical Lock Set" and that semantic.info equals action by design (icon+label affordance). Keep it flat and non-sprawling — one entry per role.</action>
  <verify>
    <automated>cd /Users/jon/projects/relyra && node -e "const t=require('./brandbook/tokens/tokens.json'); if(JSON.stringify(t).indexOf('#9A6B1C')<0) throw new Error('missing remediated gold'); if(JSON.stringify(t).indexOf('#C08A2B')>=0) throw new Error('failed gold reintroduced'); if(JSON.stringify(t).indexOf('#334155')>=0) throw new Error('failed dark border reintroduced'); console.log('tokens.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('./brandbook/tokens/tokens.json')"` parses (valid JSON).
    - Contains #9A6B1C and #64748B; does NOT contain #C08A2B or #334155 (the contrast-failing originals).
    - Has color.semantic.light AND color.semantic.dark, typography, space, radius, boxShadow, motion, focusRing groups.
  </acceptance_criteria>
  <done>tokens.json is valid, lock-set-accurate, and covers all groups.</done>
</task>

<task type="auto">
  <name>Task 2: tokens.css</name>
  <files>brandbook/tokens/tokens.css</files>
  <read_first>
    - brandbook/tokens/tokens.json (Task 1 — mirror its semantic values)
  </read_first>
  <action>Create `brandbook/tokens/tokens.css`. Define primitives as `--rl-<name>` custom properties, then map semantic tokens (`--rl-bg`, `--rl-surface`, `--rl-text`, `--rl-text-muted`, `--rl-border`, `--rl-divider`, `--rl-action`, `--rl-action-emphasis`, `--rl-link`, `--rl-verified`, `--rl-focus`, `--rl-caution`, `--rl-success`, `--rl-warning`, `--rl-error`, `--rl-info`) to the LIGHT primitives in `:root`. Add the type/space/radius/border/shadow/motion tokens as `--rl-*` vars too. Provide the DARK overrides twice for robustness: once under `@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) { ... } }` and once under `[data-theme="dark"] { ... }` (manual hook wins). Add a `@media (prefers-reduced-motion: reduce)` block zeroing `--rl-duration-*`. Add a short top comment naming the source and the info=action note.</action>
  <verify>
    <automated>cd /Users/jon/projects/relyra && grep -q "prefers-color-scheme" brandbook/tokens/tokens.css && grep -q "data-theme=\"dark\"" brandbook/tokens/tokens.css && grep -q "prefers-reduced-motion" brandbook/tokens/tokens.css && grep -q -- "--rl-focus" brandbook/tokens/tokens.css && echo "tokens.css OK"</automated>
  </verify>
  <acceptance_criteria>
    - Contains `:root`, a `prefers-color-scheme: dark` block, a `[data-theme="dark"]` block, and a `prefers-reduced-motion` block.
    - Defines the semantic `--rl-*` vars including `--rl-focus`, `--rl-action`, `--rl-verified`.
    - Light default values match tokens.json semantic.light.
  </acceptance_criteria>
  <done>tokens.css exposes semantic CSS vars with light default + dark + reduced-motion.</done>
</task>

<task type="auto">
  <name>Task 3: tailwind.example.js</name>
  <files>brandbook/tokens/tailwind.example.js</files>
  <read_first>
    - brandbook/tokens/tokens.css (Task 2 — reference the var names)
    - brandbook/tokens/tokens.json (Task 1 — values)
  </read_first>
  <action>Create `brandbook/tokens/tailwind.example.js` — a documented example Tailwind config fragment (module.exports = { theme: { extend: { ... } } }) for a Phoenix consumer. Map colors (relay, ink, paper, proof-teal, etc. + semantic names pointing at the `var(--rl-*)` CSS vars so dark mode follows the CSS), fontFamily (display/body/code), spacing (the scale), borderRadius (sm/md/lg), boxShadow (sm/md), and transitionTimingFunction/transitionDuration (motion). Add a top comment explaining: import tokens.css for the vars, set darkMode:'class' or rely on prefers-color-scheme, and that this is an EXAMPLE to copy, not a dependency. Reference var(--rl-*) for semantic colors so a consumer gets dark mode for free.</action>
  <verify>
    <automated>cd /Users/jon/projects/relyra && node -e "const c=require('./brandbook/tokens/tailwind.example.js'); if(!c.theme||!c.theme.extend) throw new Error('no theme.extend'); console.log('tailwind example OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('./brandbook/tokens/tailwind.example.js')"` loads and exposes theme.extend.
    - References `var(--rl-` for semantic colors and includes fontFamily + spacing + borderRadius + boxShadow.
  </acceptance_criteria>
  <done>tailwind.example.js is a copy-ready theme.extend mapping the tokens.</done>
</task>

</tasks>

<verification>
TOKEN-01: tokens.json + tokens.css cover all token groups, light/dark, no sprawl, lock-set-accurate. TOKEN-02: tailwind.example.js maps tokens for Phoenix consumers via the CSS vars. Phase goal met when the three files exist, validate, and reproduce the locked palette with dark-mode parity.
</verification>
