---
phase: 66-demo-fakeidp-disposition
plan: 03
type: execute
wave: 3
depends_on:
  - 66-01
  - 66-02
files_modified:
  - demo/ledger_loop/lib/ledger_loop_web/router.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex
  - demo/ledger_loop/lib/ledger_loop/fake_idp/keypair.ex
  - demo/ledger_loop/lib/ledger_loop/fake_idp/signer.ex
  - demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs
  - demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs
  - demo/ledger_loop/test/browser/fake_idp.spec.ts
  - .planning/STATE.md
autonomous: true
requirements:
  - DEMO-03
user_setup: []

must_haves:
  truths:
    - "No `/fake_idp` routes are present in the application router."
    - "All FakeIdP related controller, view, and supporting files are deleted."
    - "All FakeIdP related test files are deleted."
    - "The test suite passes without errors from missing files."
    - "SEED-003 is formally resolved in `.planning/STATE.md`."
  artifacts:
    - path: "demo/ledger_loop/lib/ledger_loop_web/router.ex"
      contains: "scope "/fake_idp""
      check_presence: false
    - path: "demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex"
      check_presence: false
    - path: "demo/ledger_loop/lib/ledger_loop/fake_idp/keypair.ex"
      check_presence: false
    - path: ".planning/STATE.md"
      contains: "SEED-003: RESOLVED"
  key_links:
    - from: "demo/ledger_loop/lib/ledger_loop_web/router.ex"
      to: "FakeIdPController"
      via: "no reference"
      pattern: "FakeIdPController"
      check_presence: false
---

<objective>
If the user decided to **remove** the LedgerLoop FakeIdP in Plan 02, this plan will systematically delete all related code and update `STATE.md` to reflect the resolution of SEED-003.

Purpose: To eliminate a deprecated or unwanted demo feature, simplifying the codebase and ensuring a single, intentional login story for the demo as per the phase goal.
Output: All FakeIdP related files removed, `/fake_idp` routes no longer active, and SEED-003 marked as resolved in `STATE.md`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-demo-fakeidp-disposition/66-01-SUMMARY.md
@.planning/phases/66-demo-fakeidp-disposition/66-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove FakeIdP Routes and Code Modules</name>
  <files>
    demo/ledger_loop/lib/ledger_loop_web/router.ex
    demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex
    demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html.ex
    demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex
    demo/ledger_loop/lib/ledger_loop/fake_idp/keypair.ex
    demo/ledger_loop/lib/ledger_loop/fake_idp/signer.ex
  </files>
  <action>
    Edit `demo/ledger_loop/lib/ledger_loop_web/router.ex` to remove the `scope "/fake_idp", LedgerLoopWeb.FakeIdPController do ... end` block and any associated `import` or `alias` statements related to `FakeIdPController`.
    Delete the controller file `demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex`.
    Delete the view file `demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html.ex` and the template file `demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex`.
    Delete supporting modules `demo/ledger_loop/lib/ledger_loop/fake_idp/keypair.ex` and `demo/ledger_loop/lib/ledger_loop/fake_idp/signer.ex`.
  </action>
  <verify>
    <automated>
      grep -cE 'fake_idp|/fake_idp' demo/ledger_loop/lib/ledger_loop_web/router.ex | grep -v '^#' | grep -c '=' && exit 1 || exit 0
      ls demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex 2>/dev/null && exit 1 || exit 0
      ls demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex 2>/dev/null && exit 1 || exit 0
      ls demo/ledger_loop/lib/ledger_loop/fake_idp/keypair.ex 2>/dev/null && exit 1 || exit 0
    </automated>
  </verify>
  <done>
    FakeIdP routes and associated controller, view, template, and supporting files are successfully removed from the codebase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove FakeIdP Tests</name>
  <files>
    demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs
    demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs
    demo/ledger_loop/test/browser/fake_idp.spec.ts
  </files>
  <action>
    Delete `demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs`.
    Delete `demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs`.
    Delete `demo/ledger_loop/test/browser/fake_idp.spec.ts`.
  </action>
  <verify>
    <automated>
      ls demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs 2>/dev/null && exit 1 || exit 0
      ls demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs 2>/dev/null && exit 1 || exit 0
      ls demo/ledger_loop/test/browser/fake_idp.spec.ts 2>/dev/null && exit 1 || exit 0
      mix test
    </automated>
  </verify>
  <done>
    All FakeIdP related test files are removed, and the test suite remains clean (i.e., no errors due to missing test files).
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve SEED-003 in STATE.md</name>
  <files>
    .planning/STATE.md
  </files>
  <action>
    Edit `.planning/STATE.md` to locate the reference to `SEED-003` and update its status to `RESOLVED`. Add a comment indicating its removal as part of the FakeIdP disposition.
  </action>
  <verify>
    <automated>
      grep -c "SEED-003: RESOLVED" .planning/STATE.md
    </automated>
  </verify>
  <done>
    SEED-003 is formally resolved in `.planning/STATE.md`, reflecting its removal with the FakeIdP.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo app → local filesystem | The demo application interacts with files in its local filesystem for code and tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
No FakeIdP specific routes are accessible, all related code files are removed, tests pass, and SEED-003 is marked as resolved in `STATE.md`.
</verification>

<success_criteria>
The LedgerLoop FakeIdP browser flow and all its associated code have been successfully removed, the codebase is simplified, and SEED-003's resolution is documented.
</success_criteria>

<output>
Create `.planning/phases/66-demo-fakeidp-disposition/66-03-SUMMARY.md` when done
</output>
