---
phase: 66-demo-fakeidp-disposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - DEMO-01
user_setup: []

must_haves:
  truths:
    - "The current status and behavior of the FakeIdP flow is clearly understood."
    - "The purpose and context of SEED-003 are fully understood."
  artifacts: []
  key_links: []
---

<objective>
Thoroughly audit the existing LedgerLoop FakeIdP browser flow (routes, controllers, views, and tests) and investigate the context of SEED-003 to prepare for a disposition decision.

Purpose: Provide the necessary factual basis to make an informed decision on whether to retain or remove the FakeIdP demo components.
Output: A clear understanding of the FakeIdP's current implementation, test coverage, and the status/context of SEED-003.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map FakeIdP Routes, Controllers, and Views</name>
  <files></files>
  <action>
    Locate and analyze the definitions for `/fake_idp/login` and `/fake_idp/sso` routes within `demo/ledger_loop/lib/ledger_loop_web/router.ex`.
    Identify the associated controller (`demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex`) and any view/template files it renders (e.g., `demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html.ex` and `demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex`).
    Also identify any supporting modules for the FakeIdP logic like `demo/ledger_loop/lib/ledger_loop/fake_idp/keypair.ex` and `demo/ledger_loop/lib/ledger_loop/fake_idp/signer.ex`.
    Document their basic structure and function, noting specific file paths and entry points.
  </action>
  <verify>
    <automated>
      grep -cE 'fake_idp|/fake_idp' demo/ledger_loop/lib/ledger_loop_web/router.ex
      ls demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex
      ls demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex
    </automated>
  </verify>
  <done>
    A comprehensive understanding of FakeIdP related routes, controller actions, view files, and supporting modules is available in the planning context.
  </done>
</task>

<task type="auto">
  <name>Task 2: Identify and Verify FakeIdP Test Coverage</name>
  <files></files>
  <action>
    Search for test files (e.g., `demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs`, `demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs`, `demo/ledger_loop/test/browser/fake_idp.spec.ts`) that exercise the identified FakeIdP components.
    Run these tests (e.g., `mix test test/ledger_loop_web/controllers/fake_idp_controller_test.exs`) and record their current status (passing/failing, last run time, purpose). If specific browser tests for the LedgerLoop FakeIdP exist, note their status.
  </action>
  <verify>
    <automated>
      mix test demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs || true
      mix test demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs || true
    </automated>
  </verify>
  <done>
    A report on test file locations, their passing/failing status, and their relevance to the FakeIdP flow is available in the planning context.
  </done>
</task>

<task type="auto">
  <name>Task 3: Investigate SEED-003 Context</name>
  <files></files>
  <action>
    Read the details and context provided for SEED-003 within `.planning/STATE.md` (or any other `.planning/` file that might reference it more specifically if not in STATE.md).
    Understand why it was created and what its current implication is for the FakeIdP flow. If `.planning/STATE.md` only briefly mentions it, note that the full context is missing for later consideration.
  </action>
  <verify>
    <automated>
      grep -c "SEED-003" .planning/STATE.md
    </automated>
  </verify>
  <done>
    The available context of SEED-003 is understood and available in the planning context.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo app → local filesystem | The demo application reads files from its local filesystem, specifically test assets and configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
All audit findings for FakeIdP routes, controllers, views, and tests, including the context of SEED-003, are comprehensively documented and available to inform the decision phase.
</verification>

<success_criteria>
The current state of the LedgerLoop FakeIdP demo components and the historical context of SEED-003 are fully understood and clearly articulated for the next decision-making step.
</success_criteria>

<output>
Create `.planning/phases/66-demo-fakeidp-disposition/66-01-SUMMARY.md` when done
</output>
