---
phase: 46-adopter-dx-ergonomics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DX-01
must_haves:
  truths:
    - "README answers 'what does Relyra look like in 30 seconds?' with a runnable apply_defaults(:okta, …) snippet above the Day-1 walkthrough."
    - "Provider-count copy remains '4 first-class presets + generic SAML runbook' — no '8 presets' regression."
    - "Start Here section links to guides/overview.md as the navigation hub."
  artifacts:
    - path: "README.md"
      provides: "Above-the-fold preset snippet and overview navigation link."
---

<objective>
Deliver DX-01: insert the canonical `Relyra.Provider.apply_defaults(:okta, …)` snippet immediately after the opening tagline (before "## Start Here") and wire Start Here to `guides/overview.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/46-adopter-dx-ergonomics/46-CONTEXT.md
@.planning/phases/46-adopter-dx-ergonomics/46-RESEARCH.md
@guides/recipes/okta.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Insert above-the-fold apply_defaults snippet</name>
  <read_first>
    - README.md
    - guides/recipes/okta.md (lines 57-63 canonical shape)
    - .planning/phases/46-adopter-dx-ergonomics/46-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    In `README.md`, after the opening tagline paragraph (lines 1-4, ending with "without becoming SAML experts.") and **before** `## Start Here`, insert a new section `## Quick Look` containing a fenced Elixir code block:

    ```elixir
    connection =
      Relyra.Provider.apply_defaults(:okta, [
        sp_entity_id: "https://sp.example.com/metadata",
        acs_url: "https://sp.example.com/saml/acs",
        idp_sso_url: "https://example.okta.com/app/.../sso/saml",
        idp_certificates: ["-----BEGIN CERTIFICATE-----..."]
      ])
    ```

    Add one sentence below the block: presets fill safe defaults underneath — see the [Okta runbook](guides/recipes/okta.md) for the full receipt. Do not move or rewrite the existing "## Start Here" content except for the overview link in the next task.
  </action>
  <verify>grep -n "apply_defaults(:okta" README.md && grep -n "## Start Here" README.md</verify>
  <acceptance_criteria>
    - `grep "apply_defaults(:okta" README.md` exits 0.
    - Line number of `apply_defaults(:okta` is less than line number of `## Start Here` in README.md.
    - README.md contains all four keys: `sp_entity_id`, `acs_url`, `idp_sso_url`, `idp_certificates`.
    - README.md does NOT contain the phrase "8 presets" or "eight presets".
    - README.md still contains "4 first-class presets".
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Link Start Here to overview hub</name>
  <read_first>
    - README.md
    - .planning/phases/46-adopter-dx-ergonomics/46-CONTEXT.md (D-10)
  </read_first>
  <action>
    In README.md `## Start Here` section, add as the first bullet (before the install step): "Browse the [documentation overview](guides/overview.md) — Day-1, Day-2, and Reference sections." Keep existing numbered list items intact.
  </action>
  <verify>grep -n "guides/overview.md" README.md</verify>
  <acceptance_criteria>
    - `grep "guides/overview.md" README.md` exits 0.
    - Link text references overview or documentation overview.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `grep apply_defaults README.md` exits 0
- `grep "4 first-class presets" README.md` exits 0
- `grep -i "8 presets" README.md` exits 1 (no match)
</verification>

<success_criteria>
DX-01 satisfied: README opens with runnable preset snippet above the Day-1 walkthrough and points adopters to the overview hub.
</success_criteria>

## PLANNING COMPLETE
