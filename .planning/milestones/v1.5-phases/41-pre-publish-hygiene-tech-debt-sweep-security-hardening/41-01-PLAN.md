---
phase: 41-pre-publish-hygiene-tech-debt-sweep-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/protocol/metadata.ex
  - lib/relyra/security/xml/attribute_escape.ex
  - test/security/metadata_attribute_injection_test.exs
  - mix.exs
  - test/security/ci_gate_integrity_test.exs
autonomous: true
requirements:
  - TD-01
must_haves:
  truths:
    - "Dynamic SP metadata attribute values (`entityID`, `Location`, `AuthnRequestsSigned`) escape XML metacharacters before serialization."
    - "`mix ci.security` runs `metadata_attribute_injection_test.exs` as its own `cmd mix test` process."
    - "`ci_gate_integrity_test.exs` lists the new suite in `@gated_suites`."
  artifacts:
    - path: "lib/relyra/security/xml/attribute_escape.ex"
      provides: "Public `escape_attribute/1` mirroring C14N attribute rules without coupling to private C14n internals."
    - path: "test/security/metadata_attribute_injection_test.exs"
      provides: "Proves `& < > \" '` and control chars are escaped in attribute position on emitted metadata."
---

<objective>
Close TD-01 (WR-03): route every dynamic attribute interpolation in `Relyra.Protocol.Metadata.build_sp_metadata/2` through a single XML attribute escaper and gate it in `mix ci.security`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/41-pre-publish-hygiene-tech-debt-sweep-security-hardening/41-CONTEXT.md
@.planning/phases/41-pre-publish-hygiene-tech-debt-sweep-security-hardening/41-RESEARCH.md
@lib/relyra/protocol/metadata.ex
@lib/relyra/security/xml/c14n.ex
</context>

<tasks>

<task type="auto">
  <name>Add attribute escaper module</name>
  <read_first>
    - lib/relyra/security/xml/c14n.ex (private escape_attr/1 rules)
    - lib/relyra/protocol/metadata.ex
  </read_first>
  <action>
    Create `lib/relyra/security/xml/attribute_escape.ex` with `@moduledoc` and `def escape_attribute(value) when is_binary(value)` implementing the same replacements as `c14n.ex` `escape_attr/1`: `&` → `&amp;`, `<` → `&lt;`, `"` → `&quot;`, tab → `&#x9;`, LF → `&#xA;`, CR → `&#xD;`. Non-binary input returns `""`.
  </action>
  <verify>mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - `lib/relyra/security/xml/attribute_escape.ex` exists and exports `escape_attribute/1`.
    - Module compiles with zero warnings.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Wire metadata interpolations through escaper</name>
  <read_first>
    - lib/relyra/protocol/metadata.ex
    - lib/relyra/security/xml/attribute_escape.ex
  </read_first>
  <action>
    In `metadata.ex`, alias `Relyra.Security.XML.AttributeEscape` and wrap dynamic attribute interpolations: `entityID` from `issuer`, `Location` from `acs_url`, and any dynamic `AuthnRequestsSigned` attribute value. Certificate element text bodies (`X509Certificate` inner base64) stay unchanged — TD-01 targets attribute positions only (CONTEXT D-01).
  </action>
  <verify>mix test test/relyra/protocol/ --warnings-as-errors</verify>
  <acceptance_criteria>
    - `build_sp_metadata/2` contains no raw `#{issuer}` or `#{acs_url}` inside double-quoted attribute values without `escape_attribute/1`.
    - Existing metadata-related tests pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Add security corpus and ci.security lane</name>
  <read_first>
    - test/security/ci_gate_integrity_test.exs
    - mix.exs (ci.security alias)
    - test/security/xml_enc_adversarial_test.exs (style reference)
  </read_first>
  <action>
    Add `test/security/metadata_attribute_injection_test.exs` asserting adversarial `entityID` and `Location` values containing `& < > " '` and at least one control character (e.g. `\x01` or newline in attribute) produce escaped output in `build_sp_metadata/2` result — use `assert` on substring presence of `&amp;`, `&lt;`, `&quot;`, etc., not on full golden XML unless needed.
    Append to `mix.exs` `ci.security` list: `"cmd mix test test/security/metadata_attribute_injection_test.exs --warnings-as-errors"`.
    Add `{"test/security/metadata_attribute_injection_test.exs", nil}` to `@gated_suites` in `ci_gate_integrity_test.exs`.
  </action>
  <verify>mix test test/security/metadata_attribute_injection_test.exs test/security/ci_gate_integrity_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - New test file passes.
    - `ci_gate_integrity_test.exs` passes (structural gate sees new suite in alias + @gated_suites).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/security/metadata_attribute_injection_test.exs --warnings-as-errors` exits 0
- `mix test test/security/ci_gate_integrity_test.exs --warnings-as-errors` exits 0
</verification>

<success_criteria>
TD-01 satisfied: metadata attribute injection corpus exists and is a dedicated `cmd mix test` step in `mix ci.security`.
</success_criteria>
