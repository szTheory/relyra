---
phase: 41-pre-publish-hygiene-tech-debt-sweep-security-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/security/xml/saxy_tree.ex
  - lib/relyra/protocol/validation_pipeline.ex
  - test/relyra/protocol/decrypt_assertion_test.exs
  - test/security/xml_enc_adversarial_test.exs
autonomous: true
requirements:
  - TD-03
must_haves:
  truths:
    - "Encrypted assertion byte extraction uses parse-tree node byte spans, not regex."
    - "ENC-01 behavior unchanged: ambiguous cleartext+encrypted, multiple EncryptedAssertion, opaque :decryption_failed."
    - "No `locate_encrypted_assertion` regex remains in validation_pipeline.ex."
  artifacts:
    - path: "lib/relyra/security/xml/saxy_tree.ex"
      provides: "Optional start_byte/end_byte on Node populated during single Saxy parse."
    - path: "lib/relyra/protocol/validation_pipeline.ex"
      provides: "Tree-bound slice for decrypt input and String.replace splice."
---

<objective>
Close TD-03 (WR-01/02): retire regex-alongside-tree `locate_encrypted_assertion/1`; extract EncryptedAssertion wire bytes from SaxyTree node spans on the same parse pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/41-pre-publish-hygiene-tech-debt-sweep-security-hardening/41-CONTEXT.md
@.planning/phases/41-pre-publish-hygiene-tech-debt-sweep-security-hardening/41-RESEARCH.md
@lib/relyra/protocol/validation_pipeline.ex
@lib/relyra/security/xml/saxy_tree.ex
@test/relyra/protocol/decrypt_assertion_test.exs
</context>

<tasks>

<task type="auto">
  <name>Add byte spans to SaxyTree parse</name>
  <read_first>
    - lib/relyra/security/xml/saxy_tree.ex
    - deps/saxy (Saxy handler events if needed)
  </read_first>
  <action>
    Extend `Node` struct with optional `start_byte` and `end_byte` (nil default). Thread `source :: binary` and `pos :: non_neg_integer` through handler state; advance `pos` using event payload sizes (UTF-8 byte length for text/cdata; element tags from source slice or incremental offset). On `:start_element`, set `start_byte`; on `:end_element` finalize `end_byte` for the closed node. Update `@type` and moduledoc field list. Keep `parse/1` API unchanged.
  </action>
  <verify>mix test test/relyra/security/xml/ test/security/xml_enc_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - Golden ENC fixture parse yields non-nil spans on EncryptedAssertion node.
    - Existing XML parse / C14N tests stay green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Replace regex locator with tree-bound slice</name>
  <read_first>
    - lib/relyra/protocol/validation_pipeline.ex (decrypt_assertion/4, detect_encrypted/1, locate_encrypted_assertion/1)
    - lib/relyra/security/xml/saxy_tree.ex
  </read_first>
  <action>
    Change `locate_encrypted_assertion/1` to accept `response_payload` and `%Node{}` from `{:single, node}`. When `start_byte` and `end_byte` are non-nil integers and start_byte is at least 0 and end_byte is greater than start_byte and at most byte_size(payload), return `{:ok, binary_part(payload, start_byte, end_byte - start_byte)}`. Otherwise return `:ambiguous`. Update `decrypt_assertion/4` call site to pass the node from `detect_encrypted`. Delete the regex implementation entirely. Grep `validation_pipeline.ex` for other regex+tree paired detectors on encrypted path — remove if any.
  </action>
  <verify>mix test test/relyra/protocol/decrypt_assertion_test.exs test/security/xml_enc_adversarial_test.exs test/security/xml_enc_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - `locate_encrypted_assertion` has no `~r/` regex.
    - All decrypt_assertion and xml_enc adversarial tests pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Regression: span matches wire element</name>
  <read_first>
    - test/relyra/protocol/decrypt_assertion_test.exs
  </read_first>
  <action>
    Add one test (in existing file) that for a representative encrypted Response fixture, the tree-bound slice equals the substring found by the old regex behavior OR decrypt+reparse still succeeds end-to-end — proves byte fidelity without reintroducing regex in production code.
  </action>
  <verify>mix test test/relyra/protocol/decrypt_assertion_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - New or updated test documents span fidelity.
    - Test exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/relyra/protocol/decrypt_assertion_test.exs test/security/xml_enc_adversarial_test.exs --warnings-as-errors`
- `rg 'locate_encrypted_assertion|~r/.*EncryptedAssertion' lib/relyra/protocol/validation_pipeline.ex` shows no regex locator
</verification>

<success_criteria>
TD-03 satisfied: encrypted-assertion path uses parse tree as sole locator; one trust path invariant restored.
</success_criteria>
