---
phase: 47-onboarding-truth-getting-started-production-ecto-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/getting_started.md
  - guides/overview.md
autonomous: true
requirements:
  - ADOPT-01
must_haves:
  truths:
    - "Getting Started §3 teaches the TestSupport macro round-trip (setup_saml_connection, post_saml_response, assert_saml_login) with a minimal stub ACS router — not builder-only snippets."
    - "Canonical copy-paste reference points to test/test_support_demo_test.exs; manual build_saml_response/sign_saml_response lives in an appendix, not deleted."
    - "overview.md Day-1 step 2 frames the macro path as the first local proof receipt."
    - "§3 clarifies stub ACS controller ≠ saml_routes()/ACSController used in §2 scaffold."
  artifacts:
    - path: "guides/getting_started.md"
      provides: "§3 TestSupport macro path + Advanced appendix; Receipt uses assert_saml_login or saml_login."
    - path: "guides/overview.md"
      provides: "Day-1 step 2 aligned with Getting Started §3 macro framing."
---

<objective>
Deliver ADOPT-01: rewrite Getting Started §3 around the `Relyra.TestSupport` macro pattern and align overview Day-1 step 2 so adopters can reach first verified browser login without reading source.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-CONTEXT.md
@.planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-RESEARCH.md
@.planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-PATTERNS.md
@test/test_support_demo_test.exs
@lib/relyra/test_support.ex
@guides/getting_started.md
@guides/overview.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Doc drift: §3 omits router dispatch, adopters still stuck at signed XML | high | Require setup_saml_connection, post_saml_response, stub router/controller snippets mirroring demo test; link canonical test file |
| Misleading integration path: stub ACS presented as production ACSController | medium | Explicit prose contrast: §3 stub assigns :current_user; §2 saml_routes() mounts ACSController/consume_response |
| TestSupport used in prod build | medium | Document test-only guard from lib/relyra/test_support.ex ensure_not_prod! |
| Removing manual builder breaks power-user docs | low | Appendix retains build_saml_response/sign_saml_response with FakeIdP context |
</threat_model>

<tasks>

<task type="auto">
  <name>Rewrite Getting Started §3 around TestSupport macro</name>
  <read_first>
    - guides/getting_started.md (current §3 lines 67–95)
    - test/test_support_demo_test.exs
    - lib/relyra/test_support.ex (post_saml_response/3 path requirement)
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-CONTEXT.md (D-01, D-03, D-04)
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-RESEARCH.md (§3 gap analysis)
  </read_first>
  <action>
    Replace `## 3. Prove local login with FakeIdP` through its Receipt paragraph with a new section titled **`## 3. Prove local login with TestSupport`**.

    Structure the section in this order:

    1. **Intent paragraph:** Before a real IdP, prove the local trust path with `use Relyra.TestSupport, endpoint: …` — FakeIdP signing is internal to the macro helpers.

    2. **Prerequisites bullet list:** host ExUnit test module; Phoenix test deps; minimal test router + ACS controller (not production `saml_routes()`).

    3. **Stub router + controller fenced Elixir block** mirroring `test/test_support_demo_test.exs` lines 1–15 (adapt module names to `MyAppWeb.TestRouter` / `MyAppWeb.TestAcsController`; keep `post("/:connection_id/acs", …)` path).

    4. **Integration test fenced Elixir block** mirroring demo test lines 17–36:
       - `use Relyra.TestSupport, endpoint: MyAppWeb.TestRouter`
       - `setup_saml_connection(conn, connection_id: "demo")`
       - `build_saml_response() |> sign_saml_response()` then `Base.decode64!(response, padding: false)` before `post_saml_response/2`
       - `assert_saml_login(conn, %{email: "alice@example.com"})` as primary receipt

    5. **Canonical reference sentence:** "Copy-paste source of truth: `test/test_support_demo_test.exs` in the Relyra repo (also gated in `mix ci.docs`)."

    6. **Stub vs production contrast paragraph:** §3 stub assigns `:current_user` directly for a fast receipt; §2 install scaffold wires `import Relyra.Phoenix.Router; saml_routes()` to `Relyra.Phoenix.Controllers.ACSController` and `consume_response/3` — use that for real integration after this proof.

    7. **Receipt block:** "Receipt: a host-side test passes with `assert_saml_login/2` (or `saml_login/1` returns `{:ok, …}`) after `post_saml_response/2` dispatches to your stub ACS route."

    Do NOT modify §1, §2, §4, or §5 in this task.
  </action>
  <verify>grep -n "setup_saml_connection\|post_saml_response\|test_support_demo_test" guides/getting_started.md</verify>
  <acceptance_criteria>
    - `grep "setup_saml_connection" guides/getting_started.md` exits 0.
    - `grep "post_saml_response" guides/getting_started.md` exits 0.
    - `grep "test_support_demo_test" guides/getting_started.md` exits 0.
    - `grep "assert_saml_login" guides/getting_started.md` exits 0.
    - `grep "Base.decode64!" guides/getting_started.md` exits 0.
    - Section heading is `## 3. Prove local login with TestSupport` (not FakeIdP-primary title).
    - File contains prose distinguishing stub ACS from `saml_routes()` / `ACSController`.
    - §3 no longer ends at builder-only snippet without router dispatch.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Demote manual builder to appendix in Getting Started</name>
  <read_first>
    - guides/getting_started.md (post-§3 sections)
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-CONTEXT.md (D-02)
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-RESEARCH.md (§3 current builder snippet)
  </read_first>
  <action>
    Add a new appendix section at the end of `guides/getting_started.md` (after §5 Production follow-ons), titled **`## Appendix: Advanced manual response construction`**.

    Content:
    - One paragraph: power users may call FakeIdP builders directly when debugging signing or metadata without ConnTest dispatch.
    - Fenced Elixir block with the former §3 snippet:
      ```elixir
      metadata = Relyra.TestSupport.fake_idp_metadata()

      response =
        []
        |> Relyra.TestSupport.build_saml_response()
        |> Relyra.TestSupport.sign_saml_response()
      ```
    - Note: `sign_saml_response/2` returns base64; decode before `post_saml_response/2` in macro tests.
    - Cross-link back to §3 for the recommended round-trip path.

    Remove any duplicate standalone builder-only block from §3 if still present after task 1.
  </action>
  <verify>grep -n "Appendix: Advanced manual response construction" guides/getting_started.md && grep -c "build_saml_response" guides/getting_started.md</verify>
  <acceptance_criteria>
    - `grep "## Appendix: Advanced manual response construction" guides/getting_started.md` exits 0.
    - `grep "build_saml_response" guides/getting_started.md` exits 0 (retained in appendix).
    - `grep "sign_saml_response" guides/getting_started.md` exits 0.
    - Appendix appears after `## 5. Production follow-ons` (line number of Appendix > line number of §5 heading).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Align overview.md Day-1 step 2 with TestSupport macro path</name>
  <read_first>
    - guides/overview.md (Day-1 proof journey lines 10–16)
    - guides/getting_started.md (rewritten §3 from prior tasks)
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-CONTEXT.md (D-09)
  </read_first>
  <action>
    In `guides/overview.md`, update **Proof journey step 2** (currently "Prove local sign-in with `Relyra.TestSupport.FakeIdP`") to:

    "Prove local sign-in with the `Relyra.TestSupport` macro (`setup_saml_connection/2`, `post_saml_response/2`) and stub ACS router — see [Getting Started §3](getting_started.md#3-prove-local-login-with-testsupport)."

    Update the Day-1 **Receipt** line to mention `assert_saml_login/2` instead of "local FakeIdP proof passes" as the primary local receipt.

    Do NOT modify Day-2 section in this plan (deferred to 47-03).
  </action>
  <verify>grep "setup_saml_connection" guides/overview.md && grep "Getting Started §3\|getting_started.md#3" guides/overview.md</verify>
  <acceptance_criteria>
    - `grep "setup_saml_connection" guides/overview.md` exits 0.
    - `grep -E "getting_started.md#3|Getting Started §3" guides/overview.md` exits 0.
    - `grep "TestSupport.FakeIdP" guides/overview.md` exits 1 (FakeIdP no longer primary framing in step 2).
    - `grep "assert_saml_login" guides/overview.md` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/test_support_demo_test.exs --warnings-as-errors` exits 0
- `grep setup_saml_connection guides/getting_started.md` exits 0
- `grep "Appendix: Advanced" guides/getting_started.md` exits 0
- `grep setup_saml_connection guides/overview.md` exits 0
</verification>

<success_criteria>
ADOPT-01 satisfied: Getting Started §3 promotes TestSupport macro round-trip with stub router; manual builder demoted to appendix; overview Day-1 aligned; demo test contract unchanged.
</success_criteria>
