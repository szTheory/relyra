---
phase: 49-adoption-honesty-conformance-jtbd-map-preset-taxonomy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/generic_saml.md
  - guides/getting_started.md
autonomous: true
requirements:
  - ADOPT-06
must_haves:
  truths:
    - "generic_saml.md decoder table includes Keycloak and OneLogin rows."
    - "Getting Started §4 lists four batteries-included presets including ADFS."
    - "Ping/PingFederate naming cross-reference resolves README vs table drift."
    - "generic_saml.md intro mentions four first-class presets plus ADFS runbook."
    - "README preset taxonomy unchanged (4 + 7 families) — verify alignment only."
  artifacts:
    - path: "guides/recipes/generic_saml.md"
      provides: "Complete 7-family decoder table + intro taxonomy alignment."
    - path: "guides/getting_started.md"
      provides: "§4 support taxonomy with 4 batteries-included presets."
---

<objective>
Deliver ADOPT-06: align preset taxonomy across Getting Started §4 and generic_saml.md decoder table with README's 4 first-class + 7-family framing — add Keycloak/OneLogin rows without narrowing README claims.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-CONTEXT.md
@.planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-RESEARCH.md
@README.md
@guides/getting_started.md
@guides/recipes/generic_saml.md
@.planning/phases/41-pre-publish-hygiene-tech-debt-sweep-security-hardening/41-CONTEXT.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Narrowing README 7-family claim | high | Add decoder rows only; do not edit README support matrix downward |
| Getting Started omits ADFS from batteries-included | medium | Explicit four-preset list per D-12 |
| Ping vs PingFederate confuses adopters | medium | Cross-ref note in generic runbook intro or Ping row footgun |
| Shibboleth not findable in table | low | Cross-link Shibboleth notes section from table intro |
</threat_model>

<tasks>

<task type="auto">
  <name>Add Keycloak and OneLogin decoder table rows</name>
  <read_first>
    - guides/recipes/generic_saml.md (decoder table lines 122-128)
    - README.md (lines 41-65 seven IdP families list)
    - .planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-CONTEXT.md (D-11)
  </read_first>
  <action>
    In `guides/recipes/generic_saml.md`, extend the vendor decoder table (after CA SiteMinder row, before "Common claim decoder") with two new rows:

    **Keycloak row** (operator-facing labels):
    | Keycloak | `Client ID` or `Entity ID` | `Client authentication / ACS URL` or `Valid redirect URIs` (map to ACS) | `Sign-in URL` or `Master SAML Processing URL` | `Signing certificate` or realm keys export | email, given_name, family_name, groups | Often persistent or email-style | Keycloak realm vs client SAML settings split — verify ACS URL on the client, not just realm metadata |

    **OneLogin row**:
    | OneLogin | `Audience (Entity ID)` | `ACS (Consumer) URL` | `SAML 2.0 Endpoint (HTTP)` | `X.509 Certificate` | Email, First Name, Last Name, Member Of | Often email-style | OneLogin connector vs custom SAML app templates use different label sets — match the connector you created |

    Adjust cell wording if admin UI labels differ slightly, but all six column concepts (entity, ACS, SSO URL, cert, claims, NameID, footgun) MUST be present per row.

    Add intro sentence before the table: "Seven IdP families named in README (Ping, OneLogin, Shibboleth, Keycloak, IBM Security Verify, CyberArk, Oracle) — decoder rows below; Shibboleth-specific notes follow the table."
  </action>
  <verify>grep -i "keycloak\|onelogin" guides/recipes/generic_saml.md</verify>
  <acceptance_criteria>
    - `grep -i "| Keycloak |" guides/recipes/generic_saml.md` exits 0.
    - `grep -i "| OneLogin |" guides/recipes/generic_saml.md` exits 0.
    - Table has at least 7 vendor rows (IBM, CyberArk, Oracle, PingFederate, CA SiteMinder, Keycloak, OneLogin).
    - Intro mentions seven IdP families from README.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Resolve Ping naming and Shibboleth findability</name>
  <read_first>
    - guides/recipes/generic_saml.md (PingFederate row line 127, Shibboleth notes section)
    - README.md (Ping in seven families list)
    - .planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-CONTEXT.md (D-13, D-14)
  </read_first>
  <action>
    In `guides/recipes/generic_saml.md`:

    1. **Ping cross-reference:** In the PingFederate row footgun cell, append: "README lists this family as **Ping** — same IdP class; admin UI says PingFederate."

    2. **Shibboleth findability (choose cross-link approach):** After the decoder table, add one line: "For **Shibboleth** (README seventh family), see [Shibboleth-specific notes](#shibboleth-specific-notes) below — no separate decoder row because metadata-exchange-first deployments vary widely."

    Do NOT remove the existing Shibboleth-specific notes section.
  </action>
  <verify>grep -n "Ping\|Shibboleth" guides/recipes/generic_saml.md | head -20</verify>
  <acceptance_criteria>
    - `grep -i "README lists this family as.*Ping" guides/recipes/generic_saml.md` exits 0.
    - `grep "Shibboleth-specific notes" guides/recipes/generic_saml.md` exits 0.
    - Shibboleth cross-link appears between decoder table and Shibboleth section.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Align generic_saml intro and Getting Started §4 taxonomy</name>
  <read_first>
    - guides/recipes/generic_saml.md (lines 8-11)
    - guides/getting_started.md (§4 lines 130-156)
    - README.md (four first-class presets list)
    - .planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-CONTEXT.md (D-12)
  </read_first>
  <action>
    **guides/recipes/generic_saml.md** intro (lines 8-11):

    Replace "stops at Okta, Microsoft Entra ID, and Google Workspace" with:

    "First-class batteries-included support covers **Okta, Microsoft Entra ID, Google Workspace, and ADFS** (each has a shipped preset module and verified runbook). ADFS also has a dedicated runbook at [guides/recipes/adfs.md](adfs.md). All other IdPs use this generic/custom-SAML path."

    **guides/getting_started.md** §4 Support taxonomy (lines 144-151):

    Replace batteries-included bullet with:

    `- **Batteries included:** Okta, Microsoft Entra ID, Google Workspace, and ADFS — each has a shipped preset module and repo-native runbook (Okta, Entra, Google, [ADFS](recipes/adfs.md)).`

    Demote ADFS from standalone "special case" bullet to part of batteries-included list. Keep a note under the list:

    `- **ADFS note:** prefer [guides/recipes/adfs.md](recipes/adfs.md) when signed AuthnRequests or ADFS-specific encoding are part of the contract.`

    Keep Custom SAML and Not yet shipped bullets; ensure Custom SAML points to generic_saml.md.
  </action>
  <verify>grep -n "Batteries included\|four\|ADFS" guides/getting_started.md guides/recipes/generic_saml.md</verify>
  <acceptance_criteria>
    - Getting Started §4 batteries-included line lists Okta, Entra, Google Workspace, AND ADFS together.
    - `grep "Google Workspace, and ADFS" guides/getting_started.md` exits 0.
    - generic_saml.md intro lists four first-class presets including ADFS.
    - `grep "three shipped presets\|stops at Okta, Microsoft Entra ID, and Google Workspace" guides/recipes/generic_saml.md` exits 1.
    - README.md NOT modified (verify-only in this plan).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify README alignment and ci.docs</name>
  <read_first>
    - README.md (preset taxonomy section)
    - mix.exs ci.docs alias
  </read_first>
  <action>
    1. Read README preset section — confirm it still claims 4 first-class + 7 IdP families including Keycloak and OneLogin. Do NOT edit README unless a direct contradiction remains after generic_saml/getting_started edits.

    2. Run `mix ci.docs`.

    3. Optional (low cost): add one cross-link line in `guides/jtbd_user_flows.md` pointing to updated gap map — only if a natural anchor exists; not required for success criteria.
  </action>
  <verify>mix ci.docs</verify>
  <acceptance_criteria>
    - `grep "4 first-class presets" README.md` exits 0.
    - `grep -i keycloak README.md` exits 0.
    - `grep -i onelogin README.md` exits 0.
    - `mix ci.docs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Keycloak and OneLogin in decoder table
- Getting Started §4 lists 4 batteries-included presets
- mix ci.docs green
</verification>
