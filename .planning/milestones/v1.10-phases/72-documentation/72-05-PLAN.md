---
phase: 72-documentation
plan: "05"
type: execute
wave: 5
depends_on:
  - "72-04"
files_modified:
  - Makefile
  - guides/docker_dev_dx.md
  - test/docs/demo_guide_drift_test.exs
autonomous: true
gap_closure: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "D-01/D-03/D-06: A Solo reader who selects a non-default `PORT` can carry that same value through `make doctor`, `make url`, and `make up-build`, then use the loopback origin emitted by `make url` for the login-test and operator-trace routes."
    - "D-03/D-04: `make doctor` probes the exported `PORT` value and classifies it as the Solo demo listener by an explicit role argument, while PostgreSQL 5432 remains diagnostic-only and Traefik 8080 remains the shared-proxy listener."
    - "D-06: An occupied configured Solo port produces a nonzero doctor result that names the actual port and gives a supported same-value recovery sequence; it is never misclassified as the Traefik listener merely because it differs from 4000."
    - "D-08: `guides/docker_dev_dx.md` keeps the complete Solo journey copy-pasteable for both the default and an overridden port without prescribing a stale fixed origin after override advice."
    - "D-02/D-05/D-07/D-09: Fleet and Keycloak remain optional follow-ons, cache and receipt ownership claims remain unchanged, and the three already-verified routers plus Local Mix/Day-1 routing do not regress."
    - "The deterministic docs helper finds each ordered token strictly after the preceding match, including repeated tokens, so documentation contracts cannot fail by restarting every lookup at byte zero."
  artifacts:
    - path: Makefile
      provides: "configured-port-aware `doctor` target with role-based `check_port` classification"
      contains: "check_port \"$${PORT}\" demo"
    - path: guides/docker_dev_dx.md
      provides: "same-PORT doctor/url/launch instructions and emitted-loopback navigation"
      contains: "make url"
    - path: test/docs/demo_guide_drift_test.exs
      provides: "owned PORT=4101 doctor/url fixture plus repeated-token ordering-helper regression coverage"
      contains: "doctor carries the configured Solo port through diagnosis and URL recovery"
  key_links:
    - from: Makefile#PORT
      to: Makefile#doctor
      via: "exported `PORT` passed to `check_port` with the `demo` role"
      pattern: "check_port.*PORT.*demo"
    - from: Makefile#url
      to: guides/docker_dev_dx.md
      via: "the guide runs `make url` under the same override and uses its emitted `Loopback:` origin"
      pattern: "PORT=.*make (doctor|url|up-build)"
    - from: test/docs/demo_guide_drift_test.exs
      to: Makefile#doctor
      via: "`fixture_bin!/0`, an owned `lsof` executable, and `run_make/2` execute the real recipe with `PORT=4101`"
      pattern: "run_make.*doctor"
    - from: test/docs/demo_guide_drift_test.exs#assert_in_order/2
      to: "ordered guide and launcher assertions"
      via: "a monotonically advancing byte cursor searches only the remaining suffix after each match"
      pattern: "binary_part|:binary.match"
  prohibitions:
    - requirement_id: DOC-01
      category: safety
      status: flagged-unverified
      flagged: true
      statement: "MUST NOT modify Compose YAML, provisioning, application routes, browser harnesses, `lib/`, public APIs, algorithm policy, configured-certificate trust, replay behavior, `mix.exs`, or Hex package files; the verifier-authorized exception is limited to the Make doctor correction, guide recovery prose, and deterministic docs tests."
    - requirement_id: DOC-01
      category: safety
      status: flagged-unverified
      flagged: true
      statement: "MUST NOT classify every non-5432 configured Solo port as the shared proxy listener; demo, diagnostic PostgreSQL, and proxy roles must remain explicit independently of their numeric values."
    - requirement_id: DOC-02
      category: transparency
      status: flagged-unverified
      flagged: true
      statement: "MUST NOT replace the canonical library Day-1 route, remove Local Mix, make Fleet or Keycloak a Solo prerequisite, or transfer mapping, session-establishment, or authorization ownership from LedgerLoop to Relyra."
  flagged_assumptions:
    - requirement_id: DOC-01
      category: concurrency
      status: unresolved
      verification: null
      resolution: null
      probe: "If interrupted or run in parallel, what is guaranteed?"
      statement: "No SPEC defines interruption semantics. This Wave-5 plan assumes exclusive ownership of its three listed files after 72-04; atomic task commits and rerunnable owned fixtures are the recovery boundary, and the executor must stop on overlapping edits rather than merge launcher semantics implicitly."
    - requirement_id: DOC-02
      category: unclassified
      status: unresolved
      verification: null
      resolution: null
      probe: "unclassified — review manually"
      statement: "No SPEC classifies an additional DOC-02 edge. This closure keeps DOC-02 visibly represented and uses the existing focused router contracts as the deterministic backstop; it does not silently dismiss the unresolved edge."
---

<objective>
Close the configured Solo-port recovery regression end to end and make ordered documentation assertions reliable (DOC-01, DOC-02; D-01 through D-09).

Purpose: Compose already honors `PORT`, but the public diagnostic and guide currently break that value flow. This plan takes the same narrow verifier-authorized launcher exception as 72-03/72-04: correct only `doctor`, its owned fixture coverage, and the guide text required to make DOC-01 truthful. Preserve all verified DOC-02 routing and every security/package boundary.
Output: A role-aware configured-port doctor path, same-PORT guide recovery through `make url`, and deterministic launcher/order-helper regression tests.
</objective>

<api_coverage_decision>
The deterministic external-API detector is false: this is a repository-local Make/docs/test correction over an existing Compose environment variable, not a new external API, SDK, or service integration. Do not create `COVERAGE.md` or add an integration checkpoint.
</api_coverage_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-documentation/72-CONTEXT.md
@.planning/phases/72-documentation/72-RESEARCH.md
@.planning/phases/72-documentation/72-PATTERNS.md
@.planning/phases/72-documentation/72-VALIDATION.md
@.planning/phases/72-documentation/72-VERIFICATION.md
@.planning/phases/72-documentation/72-03-SUMMARY.md
@.planning/phases/72-documentation/72-04-SUMMARY.md
@.planning/phases/71-launcher-dx-banner/71-CONTEXT.md
</context>

## Artifacts this phase produces

- Modified public Make target: `doctor` in `Makefile`, probing `$${PORT}` for the Solo listener.
- Modified recipe-local shell helper contract: `check_port <port> <role>` with explicit `demo`, `postgres`, and `proxy` roles rather than numeric-role inference.
- Preserved public Make target: `url`, used unchanged as the authoritative emitter of `Loopback: http://localhost:$${PORT}`.
- Modified guide paths in `guides/docker_dev_dx.md`: Solo prerequisites, launch, login-test navigation, operator-trace navigation, URL/topology map, recovery ladder, and port-conflict symptom row.
- New ExUnit fixture test `doctor carries the configured Solo port through diagnosis and URL recovery` in `test/docs/demo_guide_drift_test.exs`.
- Test-owned runtime fixture path from `fixture_bin!/0`, including stub executable `<fixture_bin>/lsof` that records/probes port 4101 and existing `<fixture_bin>/docker` dependency/network behavior.
- Modified private test helper `assert_in_order/2` in `test/docs/demo_guide_drift_test.exs` with a forward-only byte cursor.
- New ExUnit regression test `assert_in_order advances beyond repeated tokens` in `test/docs/demo_guide_drift_test.exs`.
- No new public Elixir symbol, Compose service, provisioning symbol, application route, browser harness, dependency, schema, package file, or security surface.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry an overridden Solo port from doctor through the emitted login URL</name>
  <reversibility rating="reversible">The recipe-local role parameter, guide correction, and owned fixture assertions can be reverted without migration, published API break, Compose redesign, or security-policy change.</reversibility>
  <precondition>`docker-compose.override.yml` still maps the Solo host listener from `${PORT:-4000}`, and `make url` still emits `Loopback: http://localhost:$${PORT}`.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72-documentation/72-VERIFICATION.md
    - .planning/phases/72-documentation/72-CONTEXT.md
    - .planning/phases/72-documentation/72-04-SUMMARY.md
    - .planning/phases/71-launcher-dx-banner/71-CONTEXT.md
    - Makefile
    - docker-compose.override.yml
    - guides/docker_dev_dx.md
    - test/docs/demo_guide_drift_test.exs
    - test/docs/adopter_voice_test.exs
    - test/docs/markdown_link_smoke_test.exs
  </read_first>
  <files>Makefile, guides/docker_dev_dx.md, test/docs/demo_guide_drift_test.exs</files>
  <behavior>
    - "`PORT=4101 make doctor` invokes the owned port probe for 4101, not 4000, and identifies an occupied 4101 as the Solo demo listener with a nonzero result."
    - "The same role-aware helper continues to treat 5432 as diagnostic-only PostgreSQL and 8080 as the shared Traefik proxy even when the Solo value is not 4000."
    - "`PORT=4101 make url` emits `Loopback: http://localhost:4101`, and the fixture contract proves the guide's recovery commands carry the same override through doctor, url, and launch."
    - "The guide tells overridden-port readers to use the loopback origin emitted by `make url` for `/login/test` and `/relyra/admin`, while retaining 4000 only as the documented default/Local Mix origin."
    - "No Compose, provisioning, application, API, security, package, or Keycloak launcher behavior changes."
  </behavior>
  <action>
    Start RED in `test/docs/demo_guide_drift_test.exs` by adding `doctor carries the configured Solo port through diagnosis and URL recovery`. Reuse `fixture_bin!/0`, `write_executable!/3`, `run_make/2`, and the existing doctor fixture pattern; execute the real Make recipe with `PORT=4101`. The owned `lsof` stub must record its arguments, report 4101 occupied, report 5432 diagnostic occupancy, and report 8080 free. Assert that the probe log contains 4101 and does not substitute 4000, the output names occupied port 4101 as the Solo demo listener with supported remediation, the process exits nonzero, and a same-environment `make url` call emits `Loopback: http://localhost:4101`.

    Then change only the `doctor` recipe in `Makefile`. Give its recipe-local helper the exact contract `check_port <port> <role>`; pass `"$${PORT}" demo`, `5432 postgres`, and `8080 proxy`. Branch occupied/free/unavailable/probe-failed messages on `role`, using the numeric value only for the probe and displayed port. Keep PostgreSQL occupancy diagnostic-only, keep proxy occupancy a blocking shared-proxy warning, and keep the configured demo listener a blocking warning with a recovery sequence that carries one chosen free port through `make doctor` and `make url`. Do not change the default `PORT`, export behavior, `url`, Compose variables, or any other Make target.

    Update `guides/docker_dev_dx.md` per D-01, D-03, D-04, D-06, and D-08. In the Solo prerequisites/recovery path, show one copy-pasteable override example that uses the same chosen value for `make doctor`, `make url`, and `make up-build`. Tell the reader to take the `Loopback:` origin emitted by `make url` and append `/login/test` or `/relyra/admin`; restructure fixed links/table text so the default 4000 origin is clearly a default, never the destination prescribed after an override. Keep Fleet/Keycloak, cache, receipt, split-horizon, and destructive recovery text unchanged except where necessary to preserve coherent ordering.
  </action>
  <acceptance_criteria>
    - "Source assertion: `target_body(File.read!(\"Makefile\"), \"doctor\")` contains role-bearing calls for exported PORT/demo, 5432/postgres, and 8080/proxy."
    - "Behavior assertion: the owned fixture proves `PORT=4101 make doctor` probes 4101, emits an occupied Solo-listener warning for 4101, returns nonzero, and does not probe 4000."
    - "URL assertion: the same fixture proves `PORT=4101 make url` emits `Loopback: http://localhost:4101`; guide assertions require one same-value doctor/url/up-build sequence and loopback-derived login-test/operator routes."
    - "Scope assertion: outside workflow-owned `.planning/`/`.gsd/` artifacts, the task allowlist permits only `Makefile`, `guides/docker_dev_dx.md`, and `test/docs/demo_guide_drift_test.exs`; any Compose, provisioning, application, API, security, package, `lib/`, browser-harness, or other path fails the gate."
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted &amp;&amp; changed_raw=$(mktemp) &amp;&amp; changed_sorted=$(mktemp) &amp;&amp; trap 'rm -f "$changed_raw" "$changed_sorted"' EXIT &amp;&amp; { git diff --name-only &amp;&amp; git diff --cached --name-only &amp;&amp; git ls-files --others --exclude-standard; } &gt;"$changed_raw" &amp;&amp; sort -u "$changed_raw" &gt;"$changed_sorted" &amp;&amp; if changed=$(grep -Ev '^(\.planning/.*|\.gsd/.*|Makefile|guides/docker_dev_dx\.md|test/docs/demo_guide_drift_test\.exs)$' "$changed_sorted"); then test -z "$changed"; else status=$?; test "$status" -eq 1; fi</automated>
  </verify>
  <done>The configured Solo port flows through diagnosis, URL emission, launch guidance, and browser navigation under deterministic fixture coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make ordered documentation contracts advance across repeated tokens</name>
  <reversibility rating="reversible">The private ExUnit helper and its regression test have no production or public contract impact.</reversibility>
  <precondition>Task 1's configured-port fixture and guide contract pass with the existing helper call sites.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72-documentation/72-VERIFICATION.md
    - test/docs/demo_guide_drift_test.exs
    - guides/docker_dev_dx.md
    - Makefile
  </read_first>
  <files>test/docs/demo_guide_drift_test.exs</files>
  <behavior>
    - "A sequence such as `repeat`, `middle`, `repeat` passes when the second occurrence is after `middle`, even though the first occurrence precedes it."
    - "A token absent from the remaining suffix fails with a message identifying the token and the byte cursor reached."
    - "All existing guide, router, Fleet, doctor, Keycloak, and launcher ordering assertions retain their intended behavior."
  </behavior>
  <action>
    Start RED with `assert_in_order advances beyond repeated tokens`, using a short string whose requested sequence must select a later occurrence of the same token. Refactor private `assert_in_order/2` to carry the byte offset immediately after each successful match, search only the remaining binary suffix with `:binary.match/2`, translate the suffix-relative match back to an absolute byte position for diagnostics, and advance by the matched token's byte length. Preserve a deterministic failure message for a token missing after the current cursor. Do not change existing call sites merely to accommodate the old helper behavior; the corrected helper must make their ordering intent reliable.
  </action>
  <acceptance_criteria>
    - "Regression assertion: `mix test test/docs/demo_guide_drift_test.exs --warnings-as-errors` includes and passes the repeated-token ordering case that fails under the prior byte-zero lookup."
    - "Source assertion: `assert_in_order/2` searches a suffix derived from its accumulated cursor and advances the cursor past the full matched token."
    - "Compatibility assertion: the complete focused documentation/voice/link suite passes without weakening or deleting any existing ordered contract."
    - "Repository assertion: `mix qa`, `mix ci.security`, `mix test --warnings-as-errors`, and `mix format --check-formatted` all exit 0; the security alias remains independent `cmd mix test` processes and `test/security/xml/adversarial_crypto_test.exs` is unchanged."
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors &amp;&amp; mix qa &amp;&amp; mix ci.security &amp;&amp; mix test --warnings-as-errors &amp;&amp; mix format --check-formatted &amp;&amp; git diff --exit-code -- mix.exs test/security/xml/adversarial_crypto_test.exs</automated>
  </verify>
  <done>Ordered documentation contracts handle repeated tokens deterministically, and all focused plus mandatory repository gates pass with the configured-port closure intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment → Make shell recipe | The user-controlled `PORT` value crosses into host probe commands and diagnostic output. |
| diagnostic output → operator recovery | A reader uses doctor classification and remediation to choose the listener and browser origin. |
| guide → executable launcher | Documentation must carry the exact same configured value that Make and Compose consume. |
| test fixture → host process state | Owned stubs must prevent ambient Docker/listener state from determining acceptance. |

## STRIDE Threat Register — ASVS L1, block on high

Unresolved critical/high-severity threats block execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-15 | Tampering / Denial of service | `doctor` PORT probe and role classification | high | mitigate | Pass exported PORT as quoted shell data and pass an explicit fixed role to `check_port`; owned fixtures prove the actual probe arguments and reject numeric-role inference. |
| T-72-16 | Spoofing / Repudiation | doctor and guide success/recovery claims | high | mitigate | Require nonzero occupied-port behavior, same-value doctor/url/launch guidance, and the exact loopback origin emitted by the real `url` recipe before the guide directs browser navigation. |
| T-72-17 | Denial of service | 5432/8080 diagnostic boundaries | medium | mitigate | Keep PostgreSQL diagnostic-only and Traefik blocking by explicit roles; run the existing default, nc-fallback, unavailable-probe, and new override fixtures. |
| T-72-18 | Information disclosure | host listener diagnostics | medium | mitigate | Preserve existing bounded command output: report the configured port and probe owner detail only; add no credentials, SAML payloads, cookies, headers, or key material. |
| T-72-19 | Tampering | ordering-test evidence | medium | mitigate | Add a fail-first repeated-token regression and make the helper search only the unconsumed suffix, preventing earlier occurrences from satisfying later steps. |
</threat_model>

<verification>
- Focused tracer/helper lane: `mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors`.
- Configured-port behavior: owned fixture execution of `PORT=4101 make doctor` and `PORT=4101 make url`; no ambient Docker, browser, or listener state is acceptance evidence.
- Mandatory phase gates: `mix qa`, `mix ci.security`, `mix test --warnings-as-errors`, and `mix format --check-formatted` all exit 0.
- Security continuity: keep `mix ci.security` as its existing independent `cmd mix test` processes; `git diff --exit-code -- mix.exs test/security/xml/adversarial_crypto_test.exs` proves the package manifest and permanent adversarial corpus are unchanged.
- Scope continuity: only `Makefile`, `guides/docker_dev_dx.md`, and `test/docs/demo_guide_drift_test.exs` may change outside workflow-owned planning artifacts; Compose/provisioning/app/API/security/package/browser files are prohibited.
- Acceptance is fully deterministic. Do not add a human checkpoint, required UAT artifact, browser-host dependency, Docker-daemon dependency, or `human_needed` completion state.
</verification>

<success_criteria>
- DOC-01 is truthful for the default and configured Solo ports: doctor probes/classifies the actual listener, `make url` emits the corresponding loopback origin, and the guide carries one value end to end.
- DOC-02 remains represented and verified: all three routers, Local Mix, Day-1 ordering, Fleet/Keycloak optionality, and LedgerLoop ownership continue to pass their existing focused contracts.
- D-01 through D-09 remain covered; the only bounded launcher change is the role-aware doctor correction authorized by `72-VERIFICATION.md`.
- The repeated-token warning is closed with deterministic fail-first coverage and no weakened existing assertions.
- Both unresolved spec-less assumptions and every descriptor-less kept prohibition remain visible, flagged-unverified, and carry no `check_*` descriptor fields.
- All focused and mandatory repository gates pass with no changes to Compose, provisioning, application/API/security/package surfaces, `mix.exs`, or the adversarial crypto corpus.
</success_criteria>

<source_coverage_audit>

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Zero-to-login guide and coherent Make/Fleet routing in house voice | 72-05 | COVERED | Configured-port data flow is repaired; verified routing remains gated. |
| REQ | DOC-01 | Accurate Solo/Fleet guide, URL map, port recovery, receipts, and voice | 72-05 | COVERED | Tracer closes the sole blocking regression. |
| REQ | DOC-02 | Demo/published/root routing with Local Mix retained | 72-05 | COVERED | No router edit; existing focused contracts and mandatory gates preserve it. |
| RESEARCH | — | Make is operational truth; deterministic owned fixtures; browser/service-DNS and receipt boundaries; no package dependency | 72-05 | COVERED | Role-aware doctor plus same-PORT guide/test contract. |
| CONTEXT | D-01 | Complete deterministic Solo/FakeIdP journey | 72-05 | COVERED | Same-value doctor/url/launch/navigation tracer. |
| CONTEXT | D-02 | Fleet/Keycloak follows Solo | 72-05 | COVERED | Existing ordering tests remain green; no follow-on edits. |
| CONTEXT | D-03 | Public Make interface is canonical | 72-05 | COVERED | Doctor and url are the only operational instructions. |
| CONTEXT | D-04 | Exact public origins and split horizon | 72-05 | COVERED | URL emitter supplies the configured loopback origin; Fleet origins unchanged. |
| CONTEXT | D-05 | Three-part cache model | 72-05 | COVERED | Existing guide assertions remain mandatory and unchanged in meaning. |
| CONTEXT | D-06 | Graduated and destructive recovery | 72-05 | COVERED | Port-conflict branch becomes executable end to end. |
| CONTEXT | D-07 | Exact Relyra/LedgerLoop ownership | 72-05 | COVERED | Existing receipt/ownership contracts remain in the focused lane. |
| CONTEXT | D-08 | House voice and evidence-labelled proof | 72-05 | COVERED | Guide correction runs adopter-voice and receipt contracts. |
| CONTEXT | D-09 | Three routers coherent; Local Mix and Day-1 preserved | 72-05 | COVERED | Existing router test plus full gates remain mandatory. |

No source item is missing. The original context exclusion of launcher changes is superseded only for the verifier-required configured-port diagnosis correction, following the bounded precedent of Plans 72-03 and 72-04. Deferred TLS/mkcert, hashed hosts, production Dockerfile, protocol work, and every other runtime/API/security/package change remain excluded.
</source_coverage_audit>

<output>
Create `.planning/phases/72-documentation/72-05-SUMMARY.md` when done.
</output>
