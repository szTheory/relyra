---
phase: 72-documentation
plan: "02"
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - demo/ledger_loop/README.md
  - guides/demo.md
  - README.md
  - test/docs/demo_guide_drift_test.exs
autonomous: true
requirements:
  - DOC-02
must_haves:
  truths:
    - "D-09: `demo/ledger_loop/README.md` remains the detailed evaluator entry point, teaches Make-first Solo/FakeIdP and Fleet/optional Keycloak, and retains the complete Local Mix option."
    - "D-09: `guides/demo.md` stays concise and routes published HexDocs/source readers to the canonical repository Docker guide and demo README through absolute GitHub links."
    - "D-09: top-level `README.md` adds Docker demo/Fleet evaluation and Day-2/operator routing without displacing the canonical library Day-1 Getting Started sequence."
    - "D-03/D-04: All three routers point at the new Make targets and current Fleet/Keycloak origins; stale primary `scripts/demo`, raw Keycloak Compose, and direct Keycloak-on-8080 routing are removed from normal-flow prose."
    - "D-07/D-08: Router summaries preserve exact evidence ownership and house voice while the detailed proof remains in the canonical guide/README."
    - "Local Mix remains a supported alternate evaluator path and Fleet plus Keycloak remain optional follow-ons to Solo/FakeIdP."
  artifacts:
    - path: demo/ledger_loop/README.md
      provides: "Make-first detailed evaluator Quick Start with retained Local Mix"
      contains: "### Option B — Local Mix"
    - path: guides/demo.md
      provides: "published-doc-safe canonical repository routing"
      contains: "https://github.com/szTheory/relyra/blob/main/guides/docker_dev_dx.md"
    - path: README.md
      provides: "GitHub evaluator and Day-2 Docker/Fleet routing"
      contains: "guides/docker_dev_dx.md"
    - path: test/docs/demo_guide_drift_test.exs
      provides: "three-router coherence and absolute-link contract"
      contains: "demo and repository routers converge on the Make-first guide"
  key_links:
    - from: demo/ledger_loop/README.md
      to: guides/docker_dev_dx.md
      via: "relative detailed Docker/Fleet guide link"
      pattern: "guides/docker_dev_dx.md"
    - from: guides/demo.md
      to: "canonical GitHub repository guide and demo README"
      via: "absolute `https://github.com/szTheory/relyra/blob/main/` links safe from HexDocs"
      pattern: "https://github.com/szTheory/relyra/blob/main/(guides/docker_dev_dx.md|demo/ledger_loop/README.md)"
    - from: README.md
      to: guides/docker_dev_dx.md
      via: "evaluator and Day-2/operator routing alongside the existing demo route"
      pattern: "guides/docker_dev_dx.md"
    - from: test/docs/demo_guide_drift_test.exs
      to: "demo/ledger_loop/README.md, guides/demo.md, README.md"
      via: "exact links, commands, origins, and Local Mix assertions"
      pattern: "File.read!"
  prohibitions:
    - requirement_id: DOC-02
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT present the LedgerLoop demo or repository-only Docker guide as shipped Hex package runtime surface, or replace the canonical library Day-1 Getting Started route with demo evaluation."
  flagged_assumptions:
    - requirement_id: DOC-02
      category: unclassified
      status: flagged-unverified
      probe: "unclassified — review manually"
      statement: "The spec-less DOC-02 edge remains unclassified and unresolved. Planning assumes exact three-router links, Make-first command assertions, Local Mix retention, and Day-1 ordering are the complete deterministic coherence contract; no additional edge category was supplied."
---

<objective>
Route every existing evaluator/documentation entry point to the proven Make-first Docker guide and Fleet path while preserving Local Mix and the library's Day-1 onboarding route (DOC-02; D-03, D-04, D-07, D-08, D-09).

Purpose: Ensure GitHub, HexDocs, and demo readers enter one coherent operational narrative without stale launcher or Keycloak instructions.
Output: Updated demo README, published demo router, top-level README routes, and deterministic three-router drift coverage.
</objective>

<api_coverage_decision>
The deterministic external-API detector is false: these files only route to existing repository documentation and local Docker/IdP proof surfaces. Do not create `COVERAGE.md` or an integration checkpoint.
</api_coverage_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-documentation/72-CONTEXT.md
@.planning/phases/72-documentation/72-RESEARCH.md
@.planning/phases/72-documentation/72-PATTERNS.md
@.planning/phases/72-documentation/72-VALIDATION.md
@.planning/phases/72-documentation/72-01-SUMMARY.md
</context>

## Artifacts this phase produces

- Modified `demo/ledger_loop/README.md`: Make-first Docker Quick Start, current Solo/Fleet/Keycloak origins, exact receipt ownership, retained Local Mix, and route to `guides/docker_dev_dx.md`.
- Modified `guides/demo.md`: absolute canonical repository links to `guides/docker_dev_dx.md` and `demo/ledger_loop/README.md`; no `mix.exs`/ExDoc-extra mutation.
- Modified `README.md`: Docker demo/Fleet evaluator route and Day-2/operator guide entry while preserving Getting Started ordering.
- New ExUnit test `demo and repository routers converge on the Make-first guide` in `test/docs/demo_guide_drift_test.exs`.
- No new helper function is required; direct `File.read!/1`, string assertions, and existing `assert_in_order/2` cover router contracts.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refresh the detailed LedgerLoop evaluator route and retain Local Mix</name>
  <reversibility rating="reversible">The README and its static contract are repository documentation with no migration, published API, or security-posture impact.</reversibility>
  <precondition>Plan 72-01 completed and `guides/docker_dev_dx.md` contains the passing Solo/Fleet guide contract.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72-documentation/72-CONTEXT.md
    - .planning/phases/72-documentation/72-RESEARCH.md
    - .planning/phases/72-documentation/72-PATTERNS.md
    - .planning/phases/72-documentation/72-01-SUMMARY.md
    - guides/docker_dev_dx.md
    - demo/ledger_loop/README.md
    - Makefile
    - .env.example
    - demo/ledger_loop/lib/ledger_loop/relyra/session_adapter.ex
    - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex
    - scripts/test_fleet_proxy_e2e.sh
    - scripts/test_keycloak_proxy_e2e.sh
    - test/docs/demo_guide_drift_test.exs
    - test/docs/adopter_voice_test.exs
    - test/docs/markdown_link_smoke_test.exs
    - brandbook/notes/decision-log.md
  </read_first>
  <files>demo/ledger_loop/README.md, test/docs/demo_guide_drift_test.exs</files>
  <behavior>
    - Test 1: Docker Option A uses `make doctor` and `make up-build`, sends detail to `guides/docker_dev_dx.md`, and presents Solo/FakeIdP before Fleet and optional Keycloak.
    - Test 2: Local Mix Option B retains `mix setup`, `mix phx.server`, and `http://localhost:4000` in the Quick Start.
    - Test 3: stale primary launcher and Keycloak instructions are absent from the normal Docker/Keycloak sections, while `scripts/demo` remains only a compatibility note.
    - Test 4: the success walkthrough and ownership matrix retain Relyra verification versus LedgerLoop mapping/session/authz and use the exact approved receipt sentence.
  </behavior>
  <action>Start red by adding the router test named in this plan with assertions for the detailed demo README, leaving root/published-router assertions to Task 2. Apply the mapped existing `demo/ledger_loop/README.md` analog: preserve its evaluator framing, At a Glance adoption-honesty table, two-option Quick Start hierarchy, Local Mix alternative, walkthrough/route tables, owner matrix, and separate proof/non-goal lists while refreshing only the Docker entry path and stale topology details. Update D-09's evaluator Quick Start without flattening that structure: Option A uses `make doctor` then `make up-build`, opens the Solo loopback login-test route, and links to `../../guides/docker_dev_dx.md` for the complete operational journey; Option B retains the existing Local Mix commands and explanation. Refresh Docker reset/test/down guidance to public Make targets, with database-refresh and destructive-volume semantics matching the guide. Replace the stale direct Keycloak profile/port material with Fleet-first `make proxy` and the proxy-hosted optional Keycloak origin, while keeping FakeIdP as the deterministic primary proof. Preserve the owner matrix and update success prose to the exact D-07 receipt sentence. Mention `scripts/demo` once as the retained compatibility entry point, never as a second canonical workflow. Do not change routes, Compose, Makefile, browser tests, application code, or package metadata.</action>
  <acceptance_criteria>
    - Source: the detailed README's Docker Quick Start contains `make doctor`, `make up-build`, and the new guide link before Fleet/Keycloak material.
    - Behavior: the Local Mix heading and all three Local Mix tokens remain; optional Keycloak uses `http://keycloak.relyra.localhost` only after Fleet setup.
    - Security: the exact receipt sentence and ownership matrix attribute verification to Relyra and mapping/session/authz to LedgerLoop.
    - Test command: `mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The canonical evaluator README teaches current Make-first Solo/Fleet/Keycloak operation, retains Local Mix, and keeps the trust/session boundary exact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Converge HexDocs and repository routers on the canonical guide</name>
  <reversibility rating="reversible">Router copy and link contracts are documentation-only and can be reverted without changing package contents or executable behavior.</reversibility>
  <precondition>Task 1's detailed evaluator README and Plan 72-01's Docker guide both exist at their canonical repository paths.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72-documentation/72-CONTEXT.md
    - .planning/phases/72-documentation/72-RESEARCH.md
    - .planning/phases/72-documentation/72-PATTERNS.md
    - guides/docker_dev_dx.md
    - demo/ledger_loop/README.md
    - guides/demo.md
    - guides/batteries_included.md
    - README.md
    - guides/overview.md
    - mix.exs
    - test/docs/demo_guide_drift_test.exs
    - test/docs/markdown_link_smoke_test.exs
    - test/docs/adopter_voice_test.exs
    - brandbook/notes/decision-log.md
  </read_first>
  <files>guides/demo.md, README.md, test/docs/demo_guide_drift_test.exs</files>
  <behavior>
    - Test 1: `guides/demo.md` contains exact absolute GitHub links to both `guides/docker_dev_dx.md` and `demo/ledger_loop/README.md`, avoiding a published-doc relative route into repository-only material.
    - Test 2: root README preserves the existing Day-1 Getting Started order and adds the Docker guide under evaluator and Day-2/operator routing.
    - Test 3: all three routers point to one Make-first guide and describe Fleet/Keycloak as optional without presenting demo evaluation as Hex package runtime surface.
    - Test 4: `mix.exs` and `test/docs/markdown_link_smoke_test.exs` published-extra inventory remain unchanged; existing smoke coverage proves all authored links valid.
  </behavior>
  <action>Complete the named router test red with exact contracts for `guides/demo.md` and root `README.md`, then update both documents per D-09. Apply the mapped `guides/batteries_included.md` published-router pattern to keep the HexDocs-facing route concise, source-repository-oriented, and absolute-link safe; apply the mapped root `README.md` `Start Here` and `Day-2 And Operator Guides` patterns by preserving the canonical library Day-1 sequence and using compact sibling link-and-purpose routing. Keep `guides/demo.md` concise: explain that LedgerLoop and the Docker developer guide live in the source repository, link to `https://github.com/szTheory/relyra/blob/main/guides/docker_dev_dx.md` as the Make-first operational route, and retain `https://github.com/szTheory/relyra/blob/main/demo/ledger_loop/README.md` as the detailed evaluator route. This absolute-link choice is locked by the phase boundary: leave `mix.exs` and its ExDoc extras unchanged, and do not add a Hex package surface. In root `README.md`, preserve the existing `Start Here` library Day-1 order, add a concise Docker demo/Fleet evaluator route where readers choose proof surfaces, and add `guides/docker_dev_dx.md` beside the LedgerLoop demo in `Day-2 And Operator Guides`. Use D-08 house voice and D-07 boundary-safe summary text; route rather than duplicate the full guide.</action>
  <acceptance_criteria>
    - Source: `guides/demo.md` contains both exact canonical GitHub URLs; root README contains a relative `guides/docker_dev_dx.md` route without moving or replacing Getting Started.
    - Behavior: the focused test proves all three routers converge on Make-first Docker/Fleet and that Local Mix remains present in the detailed README.
    - Package boundary: `git diff --exit-code -- mix.exs test/docs/markdown_link_smoke_test.exs` succeeds after implementation, proving no ExDoc/package contract mutation.
    - Test command: focused docs, link, and voice tests pass; then full repository gates run at phase completion.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors &amp;&amp; git diff --exit-code -- mix.exs test/docs/markdown_link_smoke_test.exs</automated>
  </verify>
  <done>Published and repository routers point coherently at the canonical source-repository guide and evaluator README, preserve library Day-1 and Local Mix, and add no ExDoc or Hex package surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HexDocs page → source repository | Published `guides/demo.md` must use durable absolute repository links for source-only evaluator material. |
| library onboarding → demo evaluation | Root routing must keep canonical library integration separate from optional demo/Fleet evaluation. |
| router summary → trust/session ownership | Concise entry points can accidentally overstate what Relyra proves or owns. |

## STRIDE Threat Register — ASVS L1, block on high

Unresolved high-severity threats block plan execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-04 | Spoofing / Elevation of privilege | demo README and router proof summaries | high | mitigate | Reuse the exact D-07 ownership sentence in the detailed README, keep router copy scope-limited, and statically assert the host-owned mapping/session/authz boundary. |
| T-72-05 | Tampering / Repudiation | HexDocs-to-repository links | medium | mitigate | Require exact absolute canonical GitHub URLs and run the existing markdown link/package-boundary smoke test. |
| T-72-06 | Information disclosure / Spoofing | package and onboarding claims | medium | mitigate | State that the demo is repository-only, preserve Day-1 Getting Started ordering, and prove `mix.exs` plus published-extra inventory remain unchanged. |

No package installation or external API implementation occurs. Generic link spoofing, web-content integrity, and SAML security concerns remain canon covered by repository review and `mix ci.security`; the phase-specific repository/package honesty prohibition remains explicitly flagged above.
</threat_model>

<verification>
- Per task: `mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors`.
- Wave 2: `mix test --warnings-as-errors` and `mix format --check-formatted`.
- Phase gate: `mix qa`, `mix ci.security`, `mix format --check-formatted`, and `mix test --warnings-as-errors` must all exit 0.
- Scope gate: `git diff --exit-code -- mix.exs` plus review of `git diff --name-only` confirms no launcher/runtime, Compose, browser-harness, `lib/`, public API, schema, or package-surface file changed.
- No human verification, required UAT, external service state, schema push, assumption-delta checkpoint, or one-way decision checkpoint exists.
</verification>

<success_criteria>
- DOC-02 and D-09 are complete across all three routers; D-03, D-04, D-07, and D-08 remain consistent with the guide.
- Local Mix is retained, Fleet/Keycloak remain optional, and Getting Started remains the canonical library Day-1 route.
- The canonical guide and demo README links are valid from published HexDocs without `mix.exs` or ExDoc-extra changes.
- The descriptor-less DOC-02 transparency prohibition and unresolved unclassified assumption remain visible without silent dismissal.
- Focused docs gates and all four mandatory repository commands pass with no manual acceptance state.
</success_criteria>

<source_coverage_audit>

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Existing demo/README routes point to Make/Fleet in house voice | 72-02 | COVERED | Both tasks update and test all routers. |
| REQ | DOC-02 | Demo README, guides/demo, and root README routing; Local Mix retained | 72-02 | COVERED | Exact static contract. |
| RESEARCH | — | Detailed demo owner; concise published router; README router; absolute repo links; no ExDoc/package mutation | 72-02 | COVERED | Tasks 1-2. |
| CONTEXT | D-03 | Make remains primary; script compatibility only | 72-02 | COVERED | Task 1. |
| CONTEXT | D-04 | Current Solo/Fleet/Keycloak origins | 72-02 | COVERED | Task 1. |
| CONTEXT | D-07 | Exact receipt/session ownership | 72-02 | COVERED | Task 1 plus router summaries. |
| CONTEXT | D-08 | House voice and reproducible routing | 72-02 | COVERED | Both tasks. |
| CONTEXT | D-09 | Three coherent routers; Local Mix; Day-1 preserved | 72-02 | COVERED | Both tasks. |

Across Plans 72-01 and 72-02, the phase goal, DOC-01, DOC-02, every research constraint, and D-01 through D-09 are covered. No source-audit item is missing. Deferred TLS, hashed hosts, production Dockerfile, and all explicit runtime/API/security/package exclusions remain outside the plan set.
</source_coverage_audit>

<output>
Create `.planning/phases/72-documentation/72-02-SUMMARY.md` when done.
</output>
