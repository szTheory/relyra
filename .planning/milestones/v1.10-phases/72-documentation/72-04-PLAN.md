---
phase: 72-documentation
plan: "04"
type: execute
wave: 4
depends_on:
  - "72-03"
files_modified:
  - demo/ledger_loop/README.md
  - guides/docker_dev_dx.md
  - test/docs/demo_guide_drift_test.exs
autonomous: true
gap_closure: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "The detailed evaluator README says the FakeIdP emits `sarah@northstar.example.com`, maps the seeded Sarah identity, and inserts a LedgerLoop `LoginReceipt`; it contains no evaluator@example.com or unresolved-mapping narrative."
    - "Deterministic coverage ties that prose to `FakeIdPController`'s emitted NameID and the real `fake_idp_flow_test.exs` receipt assertion instead of freezing documentation literals in isolation."
    - "Both `guides/docker_dev_dx.md` and `demo/ledger_loop/README.md` prescribe the now-executable `make keycloak` command after Solo, and no longer claim `make proxy` plus Solo `make up-build` activates the proxy overlay/profile."
    - "The docs preserve D-01/D-02/D-07: Solo/FakeIdP remains the complete first journey; optional Keycloak is a separate follow-on; Relyra verifies while LedgerLoop maps Sarah and records the host-owned receipt."
    - "Bounded gap-closure exception: docs may name the Wave-3 public Make target, but no Compose topology, provisioning, application route, `lib/`, public API, security policy, or Hex package surface change is allowed."
  artifacts:
    - path: demo/ledger_loop/README.md
      provides: "truthful Sarah FakeIdP success/receipt narrative and executable optional Keycloak follow-on"
      contains: "sarah@northstar.example.com"
    - path: guides/docker_dev_dx.md
      provides: "canonical guide routing optional Keycloak through `make keycloak`"
      contains: "make keycloak"
    - path: test/docs/demo_guide_drift_test.exs
      provides: "controller/flow-backed identity contract and executable-command documentation contract"
      contains: "detailed evaluator README matches the FakeIdP Sarah receipt contract"
  key_links:
    - from: demo/ledger_loop/README.md
      to: demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex
      via: "documented subject equals the controller's `conn_fields/0` NameID"
      pattern: "sarah@northstar\.example\.com"
    - from: demo/ledger_loop/README.md
      to: demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs
      via: "documented success outcome matches the database-backed LoginReceipt assertion"
      pattern: "LoginReceipt"
    - from: guides/docker_dev_dx.md
      to: Makefile
      via: "the optional Keycloak section invokes the public `keycloak` target implemented in 72-03"
      pattern: "make keycloak"
    - from: test/docs/demo_guide_drift_test.exs
      to: "README/controller/flow/guide/Makefile"
      via: "runtime file reads and exact cross-artifact assertions"
      pattern: "File.read!"
  prohibitions:
    - requirement_id: DOC-01
      category: safety
      status: kept
      statement: "MUST NOT document a Keycloak success path that omits the proxy overlay, `keycloak` profile, provisioning wait, or public-route validation embodied by `make keycloak`."
    - requirement_id: DOC-02
      category: values
      status: kept
      statement: "MUST NOT imply Relyra establishes LedgerLoop's browser session or owns downstream authorization; mapping, LoginReceipt persistence, and authorization remain host-owned."
    - requirement_id: DOC-02
      category: transparency
      status: kept
      statement: "MUST NOT describe FakeIdP as emitting `evaluator@example.com`, claim Sarah cannot map, or present the successful exercised receipt as a future exercise."
    - requirement_id: DOC-01
      category: safety
      status: kept
      statement: "MUST NOT modify Compose YAML, Keycloak provisioning, application routes, browser harnesses, `lib/`, public APIs, security policy, `mix.exs`, or Hex package files while correcting documentation and tests."
  flagged_assumptions:
    - requirement_id: DOC-01
      category: concurrency
      status: unresolved
      verification: null
      resolution: null
      probe: "If interrupted or run in parallel, what is guaranteed?"
      statement: "No SPEC defines interruption semantics. Wave-4 dependency on 72-03 and exclusive ownership of the three listed files prevent concurrent plan writes; task commits plus rerunnable focused tests are the available recovery boundary."
    - requirement_id: DOC-02
      category: unclassified
      status: unresolved
      verification: null
      resolution: null
      probe: "unclassified — review manually"
      statement: "No SPEC classifies another DOC-02 edge. The plan keeps this visibly unresolved while deterministically covering the verifier's concrete identity, receipt, command, and routing failures."
---

<objective>
Close both documentation truth gaps after the executable launcher lands: correct the deterministic Sarah receipt path and route optional Keycloak through `make keycloak` (DOC-01, DOC-02; D-01, D-02, D-03, D-04, D-07, D-08, D-09).

Purpose: A reader must see exactly what the real FakeIdP/controller/flow does and must receive a command that can actually make the public Keycloak route available. This plan documents the bounded Wave-3 exception without expanding it.
Output: Correct evaluator and Docker-guide prose plus cross-artifact deterministic coverage in the existing mandatory docs test seam.
</objective>

<api_coverage_decision>
The deterministic external-API detector is false: these changes align repository-local docs/tests with existing FakeIdP, Make, Compose, and Keycloak contracts and introduce no external API, SDK, or service integration. Do not create `COVERAGE.md` unless the detector actually returns `detected=true` over the final phase scope.
</api_coverage_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-documentation/72-CONTEXT.md
@.planning/phases/72-documentation/72-RESEARCH.md
@.planning/phases/72-documentation/72-PATTERNS.md
@.planning/phases/72-documentation/72-VALIDATION.md
@.planning/phases/72-documentation/72-VERIFICATION.md
@.planning/phases/72-documentation/72-02-SUMMARY.md
@.planning/phases/72-documentation/72-03-SUMMARY.md
</context>

## Artifacts this phase produces

- Updated `demo/ledger_loop/README.md` success narrative for `sarah@northstar.example.com`, seeded identity mapping, and persisted `LoginReceipt`.
- Updated optional Keycloak command surface in `guides/docker_dev_dx.md` and `demo/ledger_loop/README.md`: `make keycloak`.
- New ExUnit test `detailed evaluator README matches the FakeIdP Sarah receipt contract` in `test/docs/demo_guide_drift_test.exs`.
- New ExUnit test `Keycloak follow-on documentation uses the executable public Make target` in `test/docs/demo_guide_drift_test.exs`.
- No new helper function is required; reuse `assert_in_order/2` and direct `File.read!/1` cross-artifact assertions.
- No new file, Elixir function, application route, Compose symbol, schema, package, public library API, or security surface is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind the evaluator narrative to Sarah and the real LoginReceipt flow</name>
  <reversibility rating="reversible">The README correction and focused cross-artifact test can be reverted locally with no runtime migration or public API change.</reversibility>
  <precondition>The demo controller still emits `sarah@northstar.example.com`, and the focused FakeIdP flow test still proves a persisted LoginReceipt for Sarah.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72-documentation/72-VERIFICATION.md
    - .planning/phases/72-documentation/72-CONTEXT.md
    - demo/ledger_loop/README.md
    - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex
    - demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs
    - demo/ledger_loop/lib/ledger_loop/relyra/session_adapter.ex
    - test/docs/demo_guide_drift_test.exs
  </read_first>
  <behavior>
    - "The detailed README names `sarah@northstar.example.com` as the valid FakeIdP subject, says it maps the seeded Sarah identity, and says LedgerLoop inserts the host-owned LoginReceipt after Relyra verification."
    - "The stale evaluator@example.com, identity-mismatch, cannot-map, and future-exercise claims are absent from the evaluator success section."
    - "The drift test reads the README, controller, and database-backed FakeIdP flow test at runtime and proves they agree on Sarah plus LoginReceipt persistence."
    - "The actual demo flow test remains green and continues to exercise signed assertion verification, mapping, receipt persistence, and redirect behavior; no production/demo Elixir source is edited."
  </behavior>
  <files>demo/ledger_loop/README.md, test/docs/demo_guide_drift_test.exs</files>
  <action>
    Start RED by adding `detailed evaluator README matches the FakeIdP Sarah receipt contract` to `test/docs/demo_guide_drift_test.exs`. Read all three artifacts at runtime. Assert the controller contains the Sarah NameID source, the flow test contains its successful LoginReceipt assertion for Sarah, and the README contains the same subject plus seeded mapping and receipt outcome. Scope negative assertions to the README success-path block so unrelated archival/planning content cannot satisfy or invalidate the gate; do not place forbidden stale literals in comments or explanatory test messages. Then replace the false evaluator identity and unresolved-outcome paragraphs in `demo/ledger_loop/README.md` with the actual Sarah path. Preserve the exact D-07 sentence and distinguish Relyra assertion verification from LedgerLoop mapping, LoginReceipt persistence, session representation, and authorization. Do not change the controller, flow, session adapter, routes, security code, or seeds.
  </action>
  <acceptance_criteria>
    - "The evaluator success section agrees with `FakeIdPController.conn_fields/0` on Sarah and with `fake_idp_flow_test.exs` on the inserted LoginReceipt."
    - "The focused cross-artifact contract fails if the controller subject, executable receipt assertion, or README narrative drifts independently."
    - "The actual `SUCCESS: SP-initiated full round-trip` demo test passes with warnings as errors."
    - "The automated task allowlist permits only `demo/ledger_loop/README.md` and `test/docs/demo_guide_drift_test.exs` outside workflow-owned `.planning/`/`.gsd/` artifacts and rejects every other tracked, staged, or untracked path."
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs --warnings-as-errors &amp;&amp; (cd demo/ledger_loop &amp;&amp; mix test test/ledger_loop_web/fake_idp_flow_test.exs:63 --warnings-as-errors) &amp;&amp; changed=$( { git diff --name-only; git diff --cached --name-only; git ls-files --others --exclude-standard; } | sort -u | grep -Ev '^(\.planning/.*|\.gsd/.*|demo/ledger_loop/README\.md|test/docs/demo_guide_drift_test\.exs)$' || true ) &amp;&amp; test -z "$changed"</automated>
  </verify>
  <done>The detailed evaluator route truthfully describes the deterministic Sarah mapping and receipt that the real signed FakeIdP flow proves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route both Keycloak follow-ons through the executable public target</name>
  <reversibility rating="reversible">The documentation and static contract changes are local and can be reverted without altering the Wave-3 launcher implementation or any published API.</reversibility>
  <precondition>`make help` lists `keycloak`, and the Wave-3 fixture contract proves that target activates the proxy overlay/profile, waits for provisioning, and validates the public descriptor.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72-documentation/72-VERIFICATION.md
    - .planning/phases/72-documentation/72-03-SUMMARY.md
    - Makefile
    - guides/docker_dev_dx.md
    - demo/ledger_loop/README.md
    - test/docs/demo_guide_drift_test.exs
    - test/docs/adopter_voice_test.exs
    - test/docs/markdown_link_smoke_test.exs
  </read_first>
  <behavior>
    - "After the complete Solo receipt, the guide and detailed README tell readers to run `make keycloak` for the optional real-IdP Fleet proof."
    - "The prose explains that this public target starts/reuses Traefik, launches the proxy overlay under the Keycloak profile, waits for provisioning, and validates the public descriptor before printing routes."
    - "Neither document presents `make proxy` plus the Solo `make up-build` target as sufficient to activate Keycloak or Fleet ingress."
    - "The docs contract checks the public target exists and appears in both Keycloak follow-on sections after Solo, while existing house-voice and Markdown-link gates remain green."
  </behavior>
  <files>guides/docker_dev_dx.md, demo/ledger_loop/README.md, test/docs/demo_guide_drift_test.exs</files>
  <action>
    Start RED with `Keycloak follow-on documentation uses the executable public Make target`. Read `Makefile`, the canonical guide, and detailed README at runtime. Assert `keycloak` is a documented public target, both optional Keycloak sections use `make keycloak` after the Solo proof, and the prose names profile activation, provisioning wait, and public route validation at an appropriate operator-facing level. Replace the incomplete Fleet/Keycloak command blocks in both documents with the Wave-3 public command. Keep ordinary Fleet discovery accurate: `make fleet` lists routed containers, while `make keycloak` is the command that activates this checkout's overlay/profile and proves its public Keycloak route. Preserve D-01 through D-09, existing cache/recovery text, Local Mix, absolute published-doc links, and exact ownership language. Do not change `README.md` or `guides/demo.md`; their routers already verify correctly. Do not edit the launcher, Compose, provisioning, E2E harness, application, `lib/`, security policy, or package surface in this task.
  </action>
  <acceptance_criteria>
    - "Both documents provide `make keycloak` as the runnable optional follow-on and accurately summarize what success means."
    - "Solo remains first and complete; Keycloak remains optional; Fleet discovery and launcher responsibilities are not conflated."
    - "The docs drift test, adopter-voice test, and Markdown link smoke test all pass with warnings as errors."
    - "The automated plan allowlist permits only `demo/ledger_loop/README.md`, `guides/docker_dev_dx.md`, and `test/docs/demo_guide_drift_test.exs` outside workflow-owned `.planning/`/`.gsd/` artifacts; it rejects tracked, staged, or untracked changes anywhere else, including the launcher, Compose/provisioning, E2E/security/API/package paths, root `lib/`, `demo/ledger_loop/lib/`, `demo/ledger_loop/test/browser/`, and demo Mix/package/runtime surfaces."
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted &amp;&amp; changed=$( { git diff --name-only; git diff --cached --name-only; git ls-files --others --exclude-standard; } | sort -u | grep -Ev '^(\.planning/.*|\.gsd/.*|demo/ledger_loop/README\.md|guides/docker_dev_dx\.md|test/docs/demo_guide_drift_test\.exs)$' || true ) &amp;&amp; test -z "$changed"</automated>
  </verify>
  <done>The canonical guide and evaluator README make only executable, tested Keycloak/Fleet claims and preserve the complete Solo/Sarah receipt path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → executable FakeIdP flow | Identity and receipt claims must derive from controller and database-backed flow evidence. |
| documentation → Make/Compose launcher | Optional Keycloak instructions must name a command that activates and validates the actual public topology. |
| Relyra verification → LedgerLoop host responsibilities | Prose must not transfer mapping, session establishment, or authorization ownership into the library. |

## STRIDE Threat Register — ASVS L1, block on high

Unresolved critical/high-severity threats block execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-11 | Spoofing / Repudiation | FakeIdP evaluator narrative | high | mitigate | Cross-read controller, real flow test, and README; require one Sarah subject and an executable LoginReceipt assertion. |
| T-72-12 | Tampering / Denial of service | Keycloak guide commands | high | mitigate | Require both docs to invoke the Wave-3 public target and statically bind that prose to the Make target that profiles, provisions, and validates. |
| T-72-13 | Elevation of privilege | session/authorization ownership prose | high | mitigate | Preserve exact D-07 receipt language and explicit LedgerLoop ownership; run focused docs contracts. |
| T-72-14 | Information disclosure | demo credentials and proof prose | medium | mitigate | Do not add credential values, SAML payloads, cookies, authorization headers, or raw diagnostic contents to either guide. |
</threat_model>

<verification>
- Task 1: root docs contract plus `cd demo/ledger_loop &amp;&amp; mix test test/ledger_loop_web/fake_idp_flow_test.exs:63 --warnings-as-errors`.
- Task 2: `mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors`.
- Mandatory phase gate: `mix qa`, `mix ci.security`, `mix format --check-formatted`, and `mix test --warnings-as-errors` all exit 0. Keep every `mix ci.security` suite in its own `cmd mix test` process and do not weaken `test/security/xml/adversarial_crypto_test.exs`.
- Exact scope gates combine unstaged, staged, and untracked paths and ignore only workflow-owned `.planning/`/`.gsd/` artifacts. Task 1 allows only `demo/ledger_loop/README.md` plus `test/docs/demo_guide_drift_test.exs`; Task 2 allows only those files plus `guides/docker_dev_dx.md`. Any other path—including Compose, Keycloak provisioning, application routes, browser harnesses, root or demo `lib/`, E2E/security/API surfaces, `mix.exs`/`mix.lock`, package manifests/locks, or demo runtime/configuration—fails deterministically.
- No manual-only validation, browser state, blocking human verification, UAT artifact, or `human_needed` status is accepted.
</verification>

<success_criteria>
- DOC-01 and DOC-02 no longer depend on hollow prose: the public Keycloak path is executable and the detailed FakeIdP outcome matches real behavior.
- A new reader sees Sarah map and receive a LedgerLoop-owned receipt after Relyra verifies the assertion.
- Optional Keycloak instructions in both detailed documents use `make keycloak` and stay after the complete Solo proof.
- The two unresolved spec-less assumptions remain explicitly flagged, and kept safety/values prohibitions remain under `must_haves.prohibitions`.
- All focused and repository-wide mandatory gates pass without human acceptance evidence.
</success_criteria>

<source_coverage_audit>

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Zero-to-login guide plus truthful Make/Fleet router | 72-03, 72-04 | COVERED | Executable target precedes accurate prose. |
| REQ | DOC-01 | Accurate guide path, URL map, troubleshooting, receipts | 72-03, 72-04 | COVERED | Keycloak gap closed; previously verified guide content retained. |
| REQ | DOC-02 | Accurate detailed demo routing with Local Mix retained | 72-04 | COVERED | Sarah and executable Keycloak follow-on corrected. |
| RESEARCH | — | Make is operational truth; deterministic docs tests; controller/session receipt boundary; no package change | 72-03, 72-04 | COVERED | Fixture and cross-artifact contracts. |
| CONTEXT | D-01 | Complete deterministic Solo/FakeIdP journey | 72-04 | COVERED | Sarah success narrative matches execution. |
| CONTEXT | D-02 | Fleet/Keycloak follows Solo | 72-03, 72-04 | COVERED | Public optional target and ordered prose. |
| CONTEXT | D-03 | Public Make interface is canonical | 72-03, 72-04 | COVERED | Target lands before docs name it. |
| CONTEXT | D-04 | Exact browser and internal hostname boundary | 72-03, 72-04 | COVERED | Public descriptor probe and unchanged service-DNS explanation. |
| CONTEXT | D-05 | Three-part cache model | 72-01 | COVERED | Existing verified content retained. |
| CONTEXT | D-06 | Graduated/destructive recovery | 72-01 | COVERED | Existing verified content retained. |
| CONTEXT | D-07 | Exact Relyra/LedgerLoop ownership | 72-04 | COVERED | Sarah receipt prose plus drift test. |
| CONTEXT | D-08 | House voice and evidence-labelled receipts | 72-04 | COVERED | Existing voice gates retained. |
| CONTEXT | D-09 | Three routers coherent; Local Mix and Day-1 preserved | 72-02, 72-04 | COVERED | Only the failed detailed route needs correction. |

No source item is missing. The bounded launcher exception is necessary because `72-VERIFICATION.md` classifies it as an implementation-and-documentation gap with no later owner. All other original exclusions and deferred ideas remain excluded.
</source_coverage_audit>

<output>
Create `.planning/phases/72-documentation/72-04-SUMMARY.md` when done.
</output>
