---
phase: 72-documentation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/docker_dev_dx.md
  - test/docs/demo_guide_drift_test.exs
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "D-01: A reader using only `guides/docker_dev_dx.md` can check Docker/Compose prerequisites, run `make doctor` and `make up-build`, open `http://localhost:4000/login/test`, complete the deterministic FakeIdP sign-in, and inspect the validation trace plus LedgerLoop receipt."
    - "D-02: Fleet and optional Keycloak appear only after the complete Solo receipt and are labelled follow-on proofs, never prerequisites for Solo."
    - "D-03/D-04: The guide teaches the repo-root Make targets and exact Solo, Fleet, Keycloak, and Traefik browser origins while distinguishing browser-facing `*.localhost` from container service DNS."
    - "D-05: The guide explains fast edits through bind-mounted source, Linux-only named `deps/` and `_build/` volumes, and lock-hash-triggered dependency resolution; BuildKit download caches remain supporting detail."
    - "D-06: Recovery is graduated from doctor output through normal restart, database refresh, and explicitly destructive `make nuke`, with exact coverage of port, proxy-network, split-horizon, and Keycloak-host symptoms."
    - "D-07: Every proof preserves the assertion-verification, mapping, and host-session boundary using the exact approved receipt sentence and makes no Relyra-owned authorization claim."
    - "D-08: The guide opens with a short gameplan and persona/JTBD framing, uses copy-pasteable commands and URLs, and ends journeys with reproducible `Receipt:` evidence in the Canonical Lock Set voice."
  artifacts:
    - path: guides/docker_dev_dx.md
      provides: "complete Make-first Solo/Fleet Docker developer guide"
      contains: "Relyra verified the assertion; LedgerLoop mapped the user and recorded the session-establishment receipt."
    - path: test/docs/demo_guide_drift_test.exs
      provides: "focused deterministic guide content, ordering, command, URL, recovery, and receipt contract"
      contains: "Docker DX guide carries the complete Solo FakeIdP journey"
  key_links:
    - from: guides/docker_dev_dx.md
      to: Makefile
      via: "documented public Make targets, route map, doctor output, and recovery semantics"
      pattern: "make (doctor|up-build|up|url|proxy|fleet|down|reset|reseed|nuke)"
    - from: guides/docker_dev_dx.md
      to: demo/ledger_loop/lib/ledger_loop/relyra/session_adapter.ex
      via: "exact ownership-safe receipt wording"
      pattern: "Relyra verified the assertion; LedgerLoop mapped the user"
    - from: test/docs/demo_guide_drift_test.exs
      to: guides/docker_dev_dx.md
      via: "File.read!/1 static contract and ordered-token assertions"
      pattern: "guides/docker_dev_dx.md"
  prohibitions:
    - requirement_id: DOC-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT imply that Relyra establishes the host application's browser session or owns downstream authorization; LedgerLoop owns mapping, its session representation, and authorization."
    - requirement_id: DOC-01
      category: safety
      status: unverified
      flagged: true
      statement: "MUST NOT present `reset`, `reseed`, or `nuke` as ordinary non-destructive restart operations; database refresh and named-volume deletion must remain explicitly labelled."
  flagged_assumptions:
    - requirement_id: DOC-01
      category: concurrency
      status: flagged-unverified
      probe: "If interrupted or run in parallel, what is guaranteed?"
      statement: "The spec-less concurrency edge is unresolved. Planning assumes the executor owns both guide files in one Wave-1 plan and relies on Git-visible partial edits plus rerunnable focused tests; no stronger interruption or parallel-write guarantee was supplied."
---

<objective>
Create the production-quality documentation tracer and expand it into the complete Docker developer guide (DOC-01; D-01 through D-08).

Purpose: Prove that a fresh reader can reach the deterministic Solo/FakeIdP receipt from one guide before documenting optional Fleet, Keycloak, caching, and recovery depth.
Output: `guides/docker_dev_dx.md` and deterministic focused guide-contract coverage in `test/docs/demo_guide_drift_test.exs`.
</objective>

<api_coverage_decision>
The deterministic external-API detector is false: this plan documents existing local FakeIdP, Keycloak, Docker, and Make surfaces and implements no external API, SDK, or service integration. Do not create `COVERAGE.md` or an integration checkpoint.
</api_coverage_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-documentation/72-CONTEXT.md
@.planning/phases/72-documentation/72-RESEARCH.md
@.planning/phases/72-documentation/72-PATTERNS.md
@.planning/phases/72-documentation/72-VALIDATION.md
@.planning/phases/68-build-caching-correctness/68-CONTEXT.md
@.planning/phases/69-compose-split-fleet-proxy/69-CONTEXT.md
@.planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
@.planning/phases/71-launcher-dx-banner/71-CONTEXT.md
</context>

## Artifacts this phase produces

- New file `guides/docker_dev_dx.md`: canonical repository Docker developer guide with Solo/FakeIdP, Fleet, optional Keycloak, cache model, URL map, and recovery ladder.
- New ExUnit test `Docker DX guide carries the complete Solo FakeIdP journey` in `test/docs/demo_guide_drift_test.exs`.
- New ExUnit test `Docker DX guide keeps Fleet Keycloak cache and recovery subordinate and exact` in `test/docs/demo_guide_drift_test.exs`.
- No new helper function is required: use the existing private `assert_in_order/2` helper for journey and topology ordering.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the complete Solo FakeIdP zero-to-login guide path</name>
  <reversibility rating="reversible">The new repository guide and static ExUnit assertions can be reverted without migration, runtime, security-policy, or published API impact.</reversibility>
  <precondition>The Phase 71 `Makefile` still exposes `doctor`, `up-build`, `url`, and the loopback login-test route used by the guide.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72-documentation/72-CONTEXT.md
    - .planning/phases/72-documentation/72-RESEARCH.md
    - .planning/phases/72-documentation/72-PATTERNS.md
    - .planning/phases/72-documentation/72-VALIDATION.md
    - .planning/phases/71-launcher-dx-banner/71-CONTEXT.md
    - Makefile
    - .env.example
    - demo/ledger_loop/README.md
    - demo/ledger_loop/lib/ledger_loop/relyra/session_adapter.ex
    - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex
    - test/docs/demo_guide_drift_test.exs
    - test/docs/adopter_voice_test.exs
    - test/docs/markdown_link_smoke_test.exs
    - brandbook/notes/decision-log.md
    - brandbook/README.md
    - /Users/jon/projects/scoria/docs/docker_dev_dx.md
  </read_first>
  <files>guides/docker_dev_dx.md, test/docs/demo_guide_drift_test.exs</files>
  <behavior>
    - Test 1: the guide's ordered Solo section contains prerequisites, `make doctor`, `make up-build`, `http://localhost:4000/login/test`, FakeIdP selection, return to LedgerLoop, validation-trace inspection, and the exact receipt sentence.
    - Test 2: Fleet, `make proxy`, `relyra.localhost`, and Keycloak occur after the Solo receipt, so no optional topology is needed to complete the first journey.
    - Test 3: the guide identifies configured IdP trust and cryptographic assertion verification as Relyra's proof, then identifies mapping and the `LoginReceipt` as LedgerLoop-owned without claiming browser-cookie session or authorization ownership.
  </behavior>
  <action>Start red by adding the named focused ExUnit test that reads `guides/docker_dev_dx.md` and uses the existing `assert_in_order/2` helper to encode the D-01 journey and D-02 optional-path ordering. Apply the mapped `/Users/jon/projects/scoria/docs/docker_dev_dx.md` analog's short gameplan, persona/JTBD, and compact mental-model structure, adapting every operational fact and command from Relyra's `Makefile` and the locked Phase 68–71 artifacts rather than copying Scoria-specific behavior. Create the guide in the D-08 house structure: short two-path gameplan, explicit persona/JTBD, prerequisite check, then a self-contained `Solo: prove one local login` walkthrough. Use only the D-03 public Make vocabulary for normal operation, with `scripts/demo` mentioned once as compatibility. Specify `make doctor`, follow its exact `Next:` remediation if needed, run `make up-build`, open the loopback login-test route, choose FakeIdP, finish the deterministic sign-in, inspect the operator validation trace and LedgerLoop receipt, and finish with a `Receipt:` line containing exactly “Relyra verified the assertion; LedgerLoop mapped the user and recorded the session-establishment receipt.” Explain that configured IdP certificates are the trust source and that the receipt is the host application's persisted session-establishment evidence, not a claim about a Relyra-created cookie or authorization decision. Keep Local Docker facts sourced from `Makefile`; do not alter launcher/runtime/Compose/browser harness, `lib/`, `mix.exs`, or any package surface.</action>
  <acceptance_criteria>
    - Source: `guides/docker_dev_dx.md` exists and its Solo journey presents the D-01 tokens in executable order before any Fleet or Keycloak setup.
    - Behavior: the exact approved receipt sentence is present after the Solo walkthrough and the text assigns validation to Relyra and mapping/session receipt/authorization to LedgerLoop.
    - CLI: every normal-operation command in the tracer resolves to an existing public Make target; `scripts/demo` appears only as compatibility wording.
    - Test command: `mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors</automated>
  </verify>
  <done>A new reader can follow the guide alone from prerequisites to a deterministic FakeIdP validation trace and truthfully owned LedgerLoop receipt, with focused automated evidence proving the sequence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the guide with Fleet Keycloak cache URL and recovery contracts</name>
  <reversibility rating="reversible">All changes remain prose and static test assertions over existing runtime contracts; reverting them changes no executable surface.</reversibility>
  <precondition>Task 1's Solo tracer and focused documentation test pass.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72-documentation/72-CONTEXT.md
    - .planning/phases/72-documentation/72-RESEARCH.md
    - .planning/phases/72-documentation/72-PATTERNS.md
    - .planning/phases/68-build-caching-correctness/68-CONTEXT.md
    - .planning/phases/69-compose-split-fleet-proxy/69-CONTEXT.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
    - .planning/phases/71-launcher-dx-banner/71-CONTEXT.md
    - guides/docker_dev_dx.md
    - Makefile
    - docker-compose.yml
    - docker-compose.override.yml
    - docker-compose.proxy.yml
    - docker/traefik/compose.yml
    - demo/ledger_loop/Dockerfile.dev
    - demo/ledger_loop/docker-entrypoint.sh
    - scripts/test_fleet_proxy_e2e.sh
    - scripts/test_keycloak_proxy_e2e.sh
    - test/docs/demo_guide_drift_test.exs
    - test/docs/adopter_voice_test.exs
    - test/docs/markdown_link_smoke_test.exs
    - /Users/jon/projects/scoria/docs/docker_dev_dx.md
  </read_first>
  <files>guides/docker_dev_dx.md, test/docs/demo_guide_drift_test.exs</files>
  <behavior>
    - Test 1: Fleet is introduced with `make proxy`, `make up-build`, `make url`, and `make fleet`; its application origin is `http://relyra.localhost`, Keycloak is optional at `http://keycloak.relyra.localhost`, and Traefik dashboard is `http://localhost:8080/dashboard/`.
    - Test 2: the URL map says `*.localhost` is browser-facing and internal health/bootstrap/probe traffic uses Docker service DNS.
    - Test 3: the cache explanation includes bind-mounted source, named Linux `deps/` and `_build/`, `mix.lock`-hash dependency refresh, and subordinate BuildKit Hex/rebar caches.
    - Test 4: recovery orders doctor remediation, normal down/relaunch, reset/reseed data refresh, then confirmed nuke cold rebuild; it names port conflicts, missing proxy network, browser-only localhost, and Keycloak host mismatch.
  </behavior>
  <action>Add the second named test red, then expand the proven guide per D-02 through D-06 and D-08. Apply the mapped Scoria guide's cache-and-recovery rhythm—heading, commands, expected mental model, footguns, then graduated recovery—but derive every Relyra adaptation from the `Makefile` and locked Phase 68–71 artifacts. Add a compact URL/topology table with exact Solo loopback, Fleet, optional Keycloak, and Traefik dashboard origins plus the browser-versus-service-DNS rule. Add the Fleet follow-on using `make proxy`, `make up-build`, `make url`, and `make fleet`, followed by a separately labelled optional Keycloak proof and its own truthful `Receipt:` line; keep FakeIdP and Keycloak evidence distinct. Explain the D-05 cache model in the required order—source moves, Linux artifacts stay in named volumes, dependencies refresh only when `mix.lock` changes—then name BuildKit Hex/rebar download caches as build-time support. Encode D-06's recovery ladder using the literal Make target semantics: doctor and its printed correction, `make down` plus normal relaunch, reset/reseed as destructive database refresh, and confirmed `make nuke` as deletion of demo data and build/dependency volumes. Pair the four required symptoms with only launcher-supported next steps. Preserve copy-pasteable commands and avoid raw Compose as a parallel user workflow.</action>
  <acceptance_criteria>
    - Source: the guide contains all four exact public origins and the split-horizon browser/service-DNS explanation.
    - Behavior: optional Fleet/Keycloak content stays after Solo; the cache model names all three D-05 mechanisms; the recovery ladder orders and labels every D-06 step and symptom.
    - Security: proof language continues to distinguish assertion verification from host mapping/session receipt and never promotes Keycloak over deterministic FakeIdP.
    - Test command: `mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors` passes, followed by `mix test --warnings-as-errors` for the wave.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors &amp;&amp; mix test --warnings-as-errors</automated>
  </verify>
  <done>The guide fully covers the asymmetric Solo/Fleet journeys, exact origins, cache mechanics, and graduated recovery without redefining any upstream behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → adopter trust decision | Readers may treat guide claims as authoritative descriptions of cryptographic verification and session ownership. |
| browser origin → container network | Public `*.localhost` names and internal service-DNS names have different consumers and must not be interchanged. |
| recovery prose → local persistent data | Reset, reseed, and nuke instructions can destroy database or build/dependency volume state. |

## STRIDE Threat Register — ASVS L1, block on high

Unresolved high-severity threats block plan execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-01 | Spoofing / Elevation of privilege | Solo and Keycloak proof language | high | mitigate | Require the exact D-07 receipt sentence, configured-IdP-certificate trust wording, separate validation trace and host receipt evidence, and focused assertions that retain LedgerLoop mapping/session/authz ownership. |
| T-72-02 | Tampering / Denial of service | URL map and split-horizon guidance | medium | mitigate | Derive all origins from the Phase 71 Makefile and assert exact browser-facing versus Docker-service-DNS language in the focused test. |
| T-72-03 | Denial of service | troubleshooting and destructive recovery | medium | mitigate | Assert the graduated D-06 order and explicit database/volume deletion labels; document only existing guarded Make targets. |

No npm, pip, cargo, Mix dependency, or Hex package installation occurs. Generic XML, signature, injection, and Docker-daemon authorization concerns are security canon preserved by existing `mix ci.security` and later `$gsd-secure-phase`; they are breadcrumbs rather than newly minted prohibitions.
</threat_model>

<verification>
- Per task: `mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors`.
- Wave 1: `mix test --warnings-as-errors` and `mix format --check-formatted`.
- Final phase gate after Plan 72-02: `mix qa`, `mix ci.security`, `mix format --check-formatted`, and `mix test --warnings-as-errors` all exit 0.
- All acceptance is deterministic; there is no human verification, UAT, live external-service dependency, schema push, or assumption-delta checkpoint.
</verification>

<success_criteria>
- DOC-01 and D-01 through D-08 are implemented in a single self-contained guide and guarded by named focused tests.
- The tracer proves Solo/FakeIdP zero-to-login before optional Fleet/Keycloak expansion.
- Both descriptor-less kept prohibitions and the unresolved DOC-01 concurrency assumption remain visible without a silent drop.
- `mix.exs`, ExDoc extras, package files, launcher/runtime, Compose, browser harnesses, `lib/`, public APIs, and security posture remain unchanged.
</success_criteria>

<source_coverage_audit>

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Zero-to-login from one guide | 72-01 | COVERED | Tracer task proves the complete Solo journey. |
| REQ | DOC-01 | Docker guide, topology, cache, URLs, troubleshooting, voice, receipts | 72-01 | COVERED | Tasks 1-2 plus focused tests. |
| RESEARCH | — | Make as operational truth; asymmetric paths; exact receipt; static docs gates; no ExDoc/package change | 72-01 | COVERED | Both tasks follow the recommended architecture. |
| CONTEXT | D-01 | Complete Solo/FakeIdP journey | 72-01 | COVERED | Tracer task. |
| CONTEXT | D-02 | Fleet and Keycloak follow-ons | 72-01 | COVERED | Expansion task. |
| CONTEXT | D-03 | Make-first command vocabulary | 72-01 | COVERED | Both tasks. |
| CONTEXT | D-04 | Exact public origins and split horizon | 72-01 | COVERED | Expansion task. |
| CONTEXT | D-05 | Three-part cache model | 72-01 | COVERED | Expansion task. |
| CONTEXT | D-06 | Graduated recovery ladder | 72-01 | COVERED | Expansion task. |
| CONTEXT | D-07 | Exact evidence ownership | 72-01 | COVERED | Tracer and optional proof. |
| CONTEXT | D-08 | House structure and Receipt lines | 72-01 | COVERED | Both tasks. |

No source item is missing. TLS/mkcert, per-checkout host hashing, production Dockerfile, runtime changes, Hex/ExDoc surface changes, and all other deferred/out-of-scope items remain excluded.
</source_coverage_audit>

<output>
Create `.planning/phases/72-documentation/72-01-SUMMARY.md` when done.
</output>
