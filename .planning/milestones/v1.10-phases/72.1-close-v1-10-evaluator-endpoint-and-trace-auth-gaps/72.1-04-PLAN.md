---
phase: 72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps
plan: "04"
type: execute
wave: 3
depends_on:
  - "72.1-02"
files_modified:
  - demo/ledger_loop/test/browser/keycloak.spec.ts
  - scripts/test_keycloak_proxy_e2e.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-03/D-05: the optional Keycloak browser proof observes denied trace access first, then authenticates through `/login/admin` with runtime credentials and reaches the intended trace."
    - "D-04/D-05: the owned harness forwards per-run credentials without printing or retaining them and does not create a stable seeded trace shortcut."
    - "All focused demo/docs lanes, `mix qa`, and `mix ci.security` pass without changes to Relyra's cryptographic or published surface."
  artifacts:
    - path: demo/ledger_loop/test/browser/keycloak.spec.ts
      provides: "real-IdP denied-then-credentialed trace browser proof"
      contains: "/login/admin"
    - path: scripts/test_keycloak_proxy_e2e.sh
      provides: "owned ephemeral credential forwarding and retained-artifact redaction checks"
      contains: "DEMO_ADMIN_PASSWORD"
  key_links:
    - from: scripts/test_keycloak_proxy_e2e.sh
      to: demo/ledger_loop/test/browser/keycloak.spec.ts
      via: "runtime `DEMO_ADMIN_*` environment forwarded into Playwright"
      pattern: "DEMO_ADMIN_(USERNAME|PASSWORD)"
    - from: demo/ledger_loop/test/browser/keycloak.spec.ts
      to: demo/ledger_loop/lib/ledger_loop_web/router.ex
      via: "denied trace then authenticated `/login/admin` session entry"
      pattern: "/login/admin"
---

<objective>
Close the optional real-IdP trace evidence and run the full phase security gates (D-03, D-04, D-05).

Purpose: Keycloak must demonstrate the same fail-closed credentialed trace journey as Solo without making CI-generated credentials part of the public contract.
Output: Denied-then-authenticated Playwright evidence, hardened harness assertions, and green phase gates.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-CONTEXT.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-RESEARCH.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-PATTERNS.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-VALIDATION.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-02-SUMMARY.md
@.planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
</context>

## Artifacts this phase produces

- Modified Playwright test `Keycloak signs Sarah into LedgerLoop through the public scoped ACS` with explicit unauthenticated denial and authenticated `/login/admin` entry before trace assertions.
- Modified shell harness self-tests/render checks for `DEMO_ADMIN_USERNAME` and `DEMO_ADMIN_PASSWORD` forwarding, per-run generation, log redaction, and retained-artifact rejection.
- No new production symbol, helper, route, CLI/config key, dependency, Compose service, schema, public API, or new file path.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove Keycloak trace denial and credentialed entry, then seal the phase</name>
  <reversibility rating="reversible">The browser and shell-harness assertions are test-only and can be changed without migration or published/security contract impact.</reversibility>
  <precondition>Docker and the external `proxy` network are available to the owned Keycloak harness; Plan 72.1-02 has established runtime credential forwarding.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-CONTEXT.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-RESEARCH.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-PATTERNS.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-VALIDATION.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-02-SUMMARY.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
    - demo/ledger_loop/lib/ledger_loop_web/router.ex
    - demo/ledger_loop/lib/ledger_loop_web/plugs/demo_admin_auth.ex
    - docker-compose.yml
    - docker-compose.proxy.yml
    - demo/ledger_loop/test/browser/keycloak.spec.ts
    - scripts/test_keycloak_proxy_e2e.sh
    - playwright.keycloak-proxy.config.mjs
  </read_first>
  <files>demo/ledger_loop/test/browser/keycloak.spec.ts, scripts/test_keycloak_proxy_e2e.sh</files>
  <behavior>
    - "D-03/D-05: after the signed Keycloak ACS and LedgerLoop receipt, an unauthenticated request to the exact connection trace is challenged and does not render trace evidence."
    - "D-03/D-05: configured runtime credentials are submitted through `/login/admin`; only then does the browser reach the exact connection trace and find the persisted verifier steps."
    - "D-04/D-05: operator-provided values are accepted; absent test-run values may be replaced only by per-run ephemeral harness values that are neither public defaults nor persisted shortcuts."
    - "D-04: credentials never appear in browser attachments, command output, retained diagnostics, or repository files."
  </behavior>
  <action>
    Start RED in `keycloak.spec.ts` by extending the existing single real-IdP scenario. After the signed ACS and LoginReceipt assertions, visit the exact trace path without authorization and assert the Basic-auth challenge/denied response cannot expose `login-trace-row-*`. Then require both runtime credential variables, set the Basic header, visit `/login/admin`, assert redirect to `/relyra/admin`, and visit the exact trace path to retain the three persisted verifier-step assertions. Use `/login/admin` only; do not add a seeded session, query flag, test-only public route, or trace bypass.

    Extend `scripts/test_keycloak_proxy_e2e.sh` only where needed to make the D-04/D-05 contract deterministic: preserve caller-provided credentials; when the owned test run receives none, generate an ephemeral per-run pair, export it only to the Compose/Playwright process, and keep the existing teardown/redaction policy. Add self-test/render assertions proving both keys reach `demo_app` and Playwright, partial values fail the harness preflight rather than creating mismatched credentials, and neither exact value survives output or the allowlisted retained diagnostic files. Do not print values, add stable credentials, alter Keycloak realm defaults, or weaken the protected XML/PEM/header/cookie redaction checks.

    Run the owned Keycloak harness, then the focused LedgerLoop and docs lanes followed by `mix qa` and `mix ci.security`. Preserve security-gates' independent `cmd mix test` process structure and the adversarial crypto corpus unchanged. Treat Docker unavailability as an unmet precondition, not as authorization for manual approval.
  </action>
  <acceptance_criteria>
    - "D-03/D-05 browser: unauthenticated exact-trace navigation is denied; credentialed `/login/admin` entry reaches the trace and its three verifier steps."
    - "D-04/D-05 harness: operator values are preserved, absent values become per-run ephemeral test credentials, partial values fail, and no exact value appears in retained/output artifacts."
    - "Real topology: `scripts/test_keycloak_proxy_e2e.sh` exits 0 under its owned Compose project and cleanup contract."
    - "Phase gates: `mix ci.demo_app`, `mix ci.docs`, `mix qa`, and `mix ci.security` all exit 0."
    - "Security/package scope: root `lib/`, `mix.exs`, and `test/security/xml/adversarial_crypto_test.exs` have no diff."
  </acceptance_criteria>
  <verify>
    <automated>scripts/test_keycloak_proxy_e2e.sh &amp;&amp; mix ci.demo_app &amp;&amp; mix ci.docs &amp;&amp; mix qa &amp;&amp; mix ci.security &amp;&amp; mix format --check-formatted &amp;&amp; git diff --exit-code -- lib mix.exs test/security/xml/adversarial_crypto_test.exs</automated>
  </verify>
  <done>D-03, D-04, and D-05 have real Keycloak denied/allowed trace evidence, credentials remain ephemeral/runtime-only, and every mandatory phase/security gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → DemoAdminAuth | Unauthenticated and credentialed requests cross the protected admin boundary. |
| host harness → Compose/Playwright | Ephemeral or operator-provided secrets enter isolated test processes. |
| containers → retained diagnostics | Failure output could contain credentials, SAML payloads, cookies, headers, or key material. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72.1-13 | Elevation of privilege | Keycloak trace browser flow | high | mitigate | Assert denial first and authenticate exclusively through `/login/admin` before trace access. |
| T-72.1-14 | Information disclosure | browser/harness credentials | high | mitigate | Disable attachments, redact all output/diagnostics, use per-run values only, and scan retained allowlisted files for exact sentinels. |
| T-72.1-15 | Tampering | credential forwarding | high | mitigate | Fail partial-value preflight and assert exact key forwarding into demo_app and Playwright without logging values. |
| T-72.1-16 | Tampering / Information disclosure | SAML and crypto evidence | high | mitigate | Preserve exact protected-root redaction, configured-cert trust, signed ACS proof, security corpus, and all root library seams unchanged. |
| T-72.1-17 | Denial of service | owned Compose cleanup | medium | mitigate | Retain unique project/network ownership checks, traps, bounded readiness, and cleanup on success/failure. |
</threat_model>

<verification>
- Owned Keycloak browser/container proof passes with denied then credentialed trace access.
- Focused demo/docs lanes and mandatory `mix qa`/`mix ci.security` pass.
- No manual approval, retained secret, public bypass, library change, or package change closes the phase.
</verification>

<success_criteria>
- Optional Keycloak agrees with the public runtime credential journey and proves both sides of the boundary.
- All D-01 through D-05 coverage remains green across Plans 01-04.
- The v1.10 demo/docs-only invariant and every cryptographic/security seam remain unchanged.
</success_criteria>

<source_coverage_audit>

| SOURCE | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | Mounted evaluator endpoints and authenticated trace journey | 72.1-01..04 | COVERED |
| REQ | — | No phase requirement IDs; SPEC-less probe visibly skipped and no IDs invented | all | EXCLUDED — source has none |
| RESEARCH | R-ROUTES | One route-derived URL set plus dispatch evidence | 72.1-01 | COVERED |
| RESEARCH | R-AUTH | Existing Basic Auth boundary and explicit runtime handoff | 72.1-01..04 | COVERED |
| CONTEXT | D-01 | Connection-scoped displayed URLs | 72.1-01, 72.1-03 | COVERED |
| CONTEXT | D-02 | Route behavior evidence | 72.1-01 | COVERED |
| CONTEXT | D-03 | Fail-closed Basic Auth | 72.1-01, 72.1-02, 72.1-04 | COVERED |
| CONTEXT | D-04 | Visible credential prerequisite/recovery | 72.1-02, 72.1-03, 72.1-04 | COVERED |
| CONTEXT | D-05 | Valid and denied evidence across journeys | 72.1-01..04 | COVERED |

Deferred ideas: none. Package installs: none. Schema-push patterns: none. Assumption-delta detector: false.
</source_coverage_audit>

<output>
Create `.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-04-SUMMARY.md` when done.
</output>
