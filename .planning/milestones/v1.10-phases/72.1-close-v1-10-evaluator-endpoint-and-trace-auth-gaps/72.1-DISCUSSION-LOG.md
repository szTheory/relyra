# Phase 72.1: Close v1.10 evaluator endpoint and trace-auth gaps - Discussion Log (Assumptions Mode)

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the analysis.

**Date:** 2026-08-27
**Phase:** 72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps
**Mode:** assumptions
**Areas analyzed:** Connection-scoped SAML settings, Evaluator route evidence, Admin authentication boundary, Credential handoff journey

## Assumptions Presented

### Connection-scoped SAML settings

| Assumption | Confidence | Evidence |
|------------|------------|----------|
| Setup advertises metadata, ACS, and login URLs for one enabled connection through `/saml/:connection_id/...`. | Confident | `demo/ledger_loop/lib/ledger_loop_web/router.ex`; `demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex`; FakeIdP and Keycloak flow tests |

### Evaluator route evidence

| Assumption | Confidence | Evidence |
|------------|------------|----------|
| Tests prove rendered endpoint values resolve to the mounted route contract rather than checking text alone. | Likely | `demo/ledger_loop/test/ledger_loop_web/live/setup_live_test.exs`; `demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs` |

### Admin authentication boundary

| Assumption | Confidence | Evidence |
|------------|------------|----------|
| Every admin and trace route retains fail-closed, runtime-supplied Basic Auth with no usable default or bypass. | Confident | `demo/ledger_loop/lib/ledger_loop_web/plugs/demo_admin_auth.ex`; `demo/ledger_loop/lib/ledger_loop_web/router.ex`; Phase 70 context |

### Credential handoff journey

| Assumption | Confidence | Evidence |
|------------|------------|----------|
| Launcher and evaluator docs make admin credential setup visible before trace access and tests cover configured success plus fail-closed absence. | Unclear | `Makefile`; `.env.example`; `docker-compose.proxy.yml`; `guides/docker_dev_dx.md`; existing auth and Keycloak E2E tests |

## Corrections Made

No corrections — the user approved all recommendations and directed the workflow to follow them automatically. The unclear credential-handoff item was resolved in favor of operator-chosen `DEMO_ADMIN_USERNAME` and `DEMO_ADMIN_PASSWORD` documented as a pre-launch prerequisite, rather than launcher-generated per-run credentials.

## External Research

No external research was needed; the repository, prior locked decisions, and milestone audit fully establish the closure contract.
