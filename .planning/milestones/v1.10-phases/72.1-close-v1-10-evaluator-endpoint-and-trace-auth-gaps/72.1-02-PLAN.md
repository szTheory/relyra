---
phase: 72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps
plan: "02"
type: execute
wave: 2
depends_on:
  - "72.1-01"
files_modified:
  - docker-compose.yml
  - docker-compose.proxy.yml
  - Makefile
  - .env.example
  - test/docs/demo_guide_drift_test.exs
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-03/D-04: Solo and Fleet forward `DEMO_ADMIN_USERNAME` and `DEMO_ADMIN_PASSWORD` only from operator-provided runtime environment values with empty defaults."
    - "D-04: `make url` presents the credential prerequisite before promising operator-trace access and explains recovery from a 401 without echoing or persisting values."
    - "D-05: Deterministic source and launcher tests prove base/proxy forwarding, prerequisite ordering, and the absence of usable credential defaults."
  artifacts:
    - path: docker-compose.yml
      provides: "Solo runtime forwarding for both demo admin credential keys"
      contains: "DEMO_ADMIN_USERNAME"
    - path: Makefile
      provides: "credential preflight and 401 recovery in the public route banner"
      contains: "DEMO_ADMIN_USERNAME"
    - path: .env.example
      provides: "commented operator-chosen credential contract"
      contains: "# DEMO_ADMIN_PASSWORD="
    - path: test/docs/demo_guide_drift_test.exs
      provides: "Compose/Make/env drift tests"
      contains: "DEMO_ADMIN_USERNAME"
  key_links:
    - from: .env.example
      to: docker-compose.yml
      via: "same two runtime-only key names"
      pattern: "DEMO_ADMIN_(USERNAME|PASSWORD)"
    - from: Makefile
      to: demo/ledger_loop/lib/ledger_loop_web/plugs/demo_admin_auth.ex
      via: "preflight names the values consumed by the existing plug"
      pattern: "DEMO_ADMIN_USERNAME"
---

<objective>
Make runtime admin credentials a coherent launcher and Compose prerequisite (D-03, D-04, D-05).

Purpose: Solo/FakeIdP evaluators must receive the same explicit credential handoff already used by Fleet/Keycloak before the route banner promises trace access.
Output: Empty-forwarded Compose values, Make preflight/recovery guidance, and deterministic drift coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-CONTEXT.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-RESEARCH.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-PATTERNS.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-01-SUMMARY.md
</context>

## Artifacts this phase produces

- Modified config keys `DEMO_ADMIN_USERNAME` and `DEMO_ADMIN_PASSWORD` in `docker-compose.yml` and preserved equivalent forwarding in `docker-compose.proxy.yml`; both remain empty-forwarded.
- Modified public Make target `url` with a credentials-before-trace preflight and 401 recovery wording that names variables only.
- Modified `.env.example` comments defining operator-chosen, runtime-only values with no usable example.
- New/expanded ExUnit tests in `test/docs/demo_guide_drift_test.exs` for Compose parity, Make ordering, 401 recovery, and secret/default rejection.
- No new symbol, helper, route, dependency, service, schema, public API, or new file path.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry operator-chosen credentials from preflight into Solo and Fleet runtime</name>
  <reversibility rating="reversible">The environment forwarding and launcher copy can be changed locally without migration or published/security contract change; the existing fail-closed plug remains authoritative.</reversibility>
  <precondition>Plan 72.1-01 has proven `/login/admin` is the only admin scope-establishing entry and that missing credentials fail closed.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-CONTEXT.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-RESEARCH.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-PATTERNS.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-01-SUMMARY.md
    - demo/ledger_loop/lib/ledger_loop_web/plugs/demo_admin_auth.ex
    - docker-compose.yml
    - docker-compose.proxy.yml
    - Makefile
    - .env.example
    - test/docs/demo_guide_drift_test.exs
  </read_first>
  <files>docker-compose.yml, docker-compose.proxy.yml, Makefile, .env.example, test/docs/demo_guide_drift_test.exs</files>
  <behavior>
    - "D-03/D-04: base and proxy Compose render both admin keys from the caller environment and render empty when the caller provides nothing."
    - "D-04: `make url` names both required keys before its route map/walkthrough and tells a challenged operator to set both values, restart the demo, and authenticate at `/login/admin`."
    - "D-04: launcher output, `.env.example`, and tests never print or persist credential values."
    - "D-05: real Make fixture output and YAML/source assertions enforce parity without relying on CI-injected values."
  </behavior>
  <action>
    Start RED in `test/docs/demo_guide_drift_test.exs`. Add assertions that base and proxy Compose both forward `DEMO_ADMIN_USERNAME` and `DEMO_ADMIN_PASSWORD` through empty-default expansion, `.env.example` retains commented empty assignments only, and the real `url` target presents the prerequisite before the Admin route and trace walkthrough. Use owned Make fixtures and sentinel environment values only to assert that output does not reveal their contents; assert documented 401 recovery names `/login/admin`, setting both variables, and restarting the active demo.

    Implement D-03/D-04 by adding both environment entries to `demo_app.environment` in `docker-compose.yml` with empty-default expansion; preserve equivalent entries in `docker-compose.proxy.yml` and do not put usable values in either file. Update `.env.example` comments to say the operator chooses both values before launch, leaving the assignment lines commented and empty.

    Update only the `url` target's public journey in `Makefile`: add a short Admin trace prerequisite before the route map/walkthrough, name both environment keys without expanding their values, state that both must be set before `make up-build`, `make up-d-build`, or `make keycloak` if trace access is promised, and explain that a 401 is recovered by setting both values, restarting, and entering through `/login/admin`. Preserve quoted runtime expansion, route origins, topology, targets, and all existing launcher semantics. Do not modify `DemoAdminAuth` or add a credential-generation/default path.
  </action>
  <acceptance_criteria>
    - "D-03/D-04 Compose: base and proxy services contain empty-default forwarding for both keys and no non-empty default."
    - "D-04 Make: prerequisite text precedes Admin/trace instructions; sentinel credential contents do not appear in target output."
    - "D-04 env: `.env.example` exposes only commented empty assignments and explicitly says values are operator-chosen and runtime-only."
    - "D-05 test: focused docs/config tests execute the real `url` target and validate the handoff/recovery contract."
    - "Scope: only the five declared files plus workflow-owned planning artifacts change in this plan."
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs --warnings-as-errors &amp;&amp; mix ci.docs &amp;&amp; mix format --check-formatted &amp;&amp; git diff --exit-code -- lib mix.exs test/security/xml/adversarial_crypto_test.exs</automated>
  </verify>
  <done>D-03, D-04, and D-05 are encoded consistently across Solo/Fleet runtime configuration, launcher preflight, environment example, and deterministic tests without secret values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host environment → Compose | Operator-selected secrets enter the demo container. |
| Make output → operator | Public launcher text directs authentication and recovery without revealing values. |
| repository config → runtime | Checked-in defaults could accidentally enable the protected surface. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72.1-05 | Information disclosure | Make/config/docs output | high | mitigate | Name keys only, keep empty forwarding, execute sentinel leak tests, and never echo or persist values. |
| T-72.1-06 | Elevation of privilege | Compose admin credential configuration | high | mitigate | Provide no usable default and retain `DemoAdminAuth` denial when either value is absent/empty. |
| T-72.1-07 | Spoofing / Repudiation | launcher trace promise | medium | mitigate | Put prerequisite and credentialed `/login/admin` recovery before the trace route; test ordering and exact route. |
| T-72.1-08 | Tampering | shell environment handling | medium | mitigate | Preserve escaped runtime expansion and treat environment values as quoted shell data; fixtures use sentinels and inspect output only. |
</threat_model>

<verification>
- Focused configuration/launcher drift tests and `mix ci.docs` pass.
- No root library, crypto corpus, package, or auth plug changes occur.
- No human validation or CI-only credential injection is completion evidence.
</verification>

<success_criteria>
- Solo and Fleet pass the same operator-supplied keys to the demo.
- The public launcher exposes the prerequisite and recovery before trace access.
- Empty or absent values remain disabled; no secret content is printed or stored.
</success_criteria>

<source_coverage_audit>

| SOURCE | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | Mounted evaluator endpoints and authenticated trace journey | 72.1-01..04 | COVERED |
| REQ | — | No phase requirement IDs; SPEC-less probe visibly skipped and no IDs invented | all | EXCLUDED — source has none |
| RESEARCH | R-ROUTES | One route-derived URL set plus dispatch evidence | 72.1-01 | COVERED |
| RESEARCH | R-AUTH | Existing Basic Auth boundary and explicit runtime handoff | 72.1-01..04 | COVERED |
| CONTEXT | D-01 | Connection-scoped displayed URLs | 72.1-01, 72.1-03 | COVERED |
| CONTEXT | D-02 | Route behavior evidence | 72.1-01 | COVERED |
| CONTEXT | D-03 | Fail-closed Basic Auth | 72.1-01, 72.1-02, 72.1-04 | COVERED |
| CONTEXT | D-04 | Visible credential prerequisite/recovery | 72.1-02, 72.1-03 | COVERED |
| CONTEXT | D-05 | Valid and denied evidence across journeys | 72.1-01..04 | COVERED |

Deferred ideas: none. Package installs: none. Schema-push patterns: none. Assumption-delta detector: false.
</source_coverage_audit>

<output>
Create `.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-02-SUMMARY.md` when done.
</output>
