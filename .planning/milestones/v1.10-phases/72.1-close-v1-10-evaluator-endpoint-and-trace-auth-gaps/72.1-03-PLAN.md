---
phase: 72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps
plan: "03"
type: execute
wave: 3
depends_on:
  - "72.1-02"
files_modified:
  - guides/docker_dev_dx.md
  - demo/ledger_loop/README.md
  - test/docs/demo_guide_drift_test.exs
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-01: LedgerLoop documentation uses `/saml/:connection_id/{metadata,login,acs}` and contains no unscoped operator SAML route guidance."
    - "D-04: Solo/FakeIdP and optional Fleet/Keycloak journeys state the same operator-chosen credential prerequisite before trace inspection and the same `/login/admin` 401 recovery."
    - "D-05: Documentation tests prove ordering and agreement while preserving Relyra-validation versus LedgerLoop-mapping/session ownership."
  artifacts:
    - path: guides/docker_dev_dx.md
      provides: "credentialed evaluator journeys for Solo, Fleet, and Keycloak"
      contains: "/login/admin"
    - path: demo/ledger_loop/README.md
      provides: "connection-scoped route table and admin prerequisite"
      contains: "/saml/:connection_id/metadata"
    - path: test/docs/demo_guide_drift_test.exs
      provides: "cross-guide scoped-route and auth-handoff contract"
      contains: "DEMO_ADMIN_PASSWORD"
  key_links:
    - from: Makefile
      to: guides/docker_dev_dx.md
      via: "same two credential key names and 401 recovery entry"
      pattern: "DEMO_ADMIN_(USERNAME|PASSWORD)"
    - from: demo/ledger_loop/README.md
      to: demo/ledger_loop/lib/ledger_loop_web/router.ex
      via: "documented scoped SAML family"
      pattern: "/saml/:connection_id/"
---

<objective>
Make every public evaluator journey match the scoped SAML routes and authenticated trace contract (D-01, D-04, D-05).

Purpose: Evaluators should see the prerequisite before launch and recover deterministically from a Basic challenge without confusing Relyra validation with LedgerLoop session ownership.
Output: Corrected Docker guide, demo README, and cross-artifact drift tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-CONTEXT.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-RESEARCH.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-PATTERNS.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-02-SUMMARY.md
@.planning/phases/72-documentation/72-CONTEXT.md
</context>

## Artifacts this phase produces

- Modified Solo/FakeIdP and optional Fleet/Keycloak sections in `guides/docker_dev_dx.md` with credential setup before launch/trace and `/login/admin` recovery.
- Modified route inventory and evaluator proof in `demo/ledger_loop/README.md` for `/saml/:connection_id/metadata`, `/login`, and `/acs` plus protected admin access.
- Expanded ExUnit tests in `test/docs/demo_guide_drift_test.exs` for cross-guide ordering, key-name parity, scoped routes, no-secret examples, and receipt ownership.
- No new symbol, helper, CLI/config key, route, dependency, schema, public API, or new file path.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the documented Solo and Keycloak trace journeys executable from operator credentials</name>
  <reversibility rating="reversible">Documentation wording and drift assertions are local changes with no migration or published runtime-contract cost.</reversibility>
  <precondition>Plan 72.1-02 has established the canonical two-key launcher and Compose handoff without printing credential values.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-CONTEXT.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-RESEARCH.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-PATTERNS.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-02-SUMMARY.md
    - .planning/phases/72-documentation/72-CONTEXT.md
    - demo/ledger_loop/lib/ledger_loop_web/router.ex
    - Makefile
    - .env.example
    - guides/docker_dev_dx.md
    - demo/ledger_loop/README.md
    - test/docs/demo_guide_drift_test.exs
  </read_first>
  <files>guides/docker_dev_dx.md, demo/ledger_loop/README.md, test/docs/demo_guide_drift_test.exs</files>
  <behavior>
    - "D-04: each journey names both environment keys before launch or operator-trace instructions and never supplies a reusable value."
    - "D-04: a 401 recovery tells the operator to set both keys, restart the relevant topology, and enter at `/login/admin` using the emitted origin."
    - "D-01: the README route table documents metadata, login, and ACS with a connection identifier."
    - "D-05: Solo remains the complete deterministic first proof; Fleet and Keycloak remain optional follow-ons that use the same tested auth handoff."
    - "D-05: documentation continues to attribute cryptographic validation to Relyra and mapping, receipt persistence, session establishment, and authorization to LedgerLoop."
  </behavior>
  <action>
    Start RED in `test/docs/demo_guide_drift_test.exs` with ordered assertions for both documents: credential keys before launch/trace, `/login/admin` recovery after the 401 explanation, exact connection-scoped route-family tokens in the README, and consistent Solo/Fleet/Keycloak language. Assert examples contain only empty variable assignments or variable names and preserve the existing receipt-ownership sentence.

    Update `guides/docker_dev_dx.md` per D-04/D-05. Before the Solo launch command, instruct the operator to choose and export both `DEMO_ADMIN_USERNAME` and `DEMO_ADMIN_PASSWORD` without supplying values in repository text. Explain that `/login/admin` consumes browser Basic Auth and redirects to `/relyra/admin`; if challenged with 401, set both keys, restart with the same Make topology, and retry `/login/admin`. Repeat the same prerequisite by reference before optional `make keycloak`, without turning Fleet/Keycloak into prerequisites or relying on CI injection. Continue using the origin emitted by `make url` for overridden hosts/ports.

    Update `demo/ledger_loop/README.md` per D-01/D-04/D-05. Correct the route inventory to show GET metadata, GET login, and POST ACS under `/saml/:connection_id/...`; describe `/login/admin` as the authenticated entry for the protected `/relyra/admin` mount. Put the two-key prerequisite before the audit-trace instruction and include the same restart/retry recovery without example secrets. Remove unscoped operator SAML guidance while preserving the library Day-1 route, FakeIdP-first chronology, and LedgerLoop/Relyra ownership boundary.
  </action>
  <acceptance_criteria>
    - "D-01: docs tests require all three `/saml/:connection_id/...` route forms and reject unscoped route-table entries through positive exact-row assertions."
    - "D-04: both guides name both keys before trace instructions, describe 401 restart/retry recovery, and contain no usable demo-admin value."
    - "D-05: Solo is still first and complete, optional journeys share the same auth handoff, and receipt/authorization ownership remains LedgerLoop's."
    - "Mandatory docs lane: `mix ci.docs` exits 0 with the new cross-artifact tests included."
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs test/docs/adopter_voice_test.exs test/docs/markdown_link_smoke_test.exs --warnings-as-errors &amp;&amp; mix ci.docs &amp;&amp; mix format --check-formatted &amp;&amp; git diff --exit-code -- lib mix.exs test/security/xml/adversarial_crypto_test.exs</automated>
  </verify>
  <done>D-01, D-04, and D-05 are visible and executable in both public evaluator documents under deterministic drift coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator shell | Copyable instructions affect credential handling and launched topology. |
| documentation → browser | Route guidance determines whether the operator reaches mounted or protected behavior. |
| trace/receipt prose → evaluator | Ownership claims can overstate what Relyra authenticates or authorizes. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72.1-09 | Information disclosure | credential examples | high | mitigate | Use variable names/empty assignments only and enforce no-secret examples in docs tests. |
| T-72.1-10 | Spoofing | documented SAML endpoints | high | mitigate | Require exact connection-scoped metadata/login/ACS route rows backed by Plan 01 dispatch evidence. |
| T-72.1-11 | Elevation of privilege | documented trace entry | high | mitigate | Direct all journeys through credentialed `/login/admin`; document no public trace shortcut or pre-seeded access. |
| T-72.1-12 | Repudiation | validation/session ownership claims | medium | mitigate | Preserve explicit Relyra validation versus LedgerLoop mapping/receipt/session/authorization wording under drift tests. |
</threat_model>

<verification>
- Focused guide, voice, and link tests pass.
- `mix ci.docs` and formatting pass.
- No manual gate, secret-bearing example, root library, public API, or package change is introduced.
</verification>

<success_criteria>
- A fresh evaluator can set credentials before launch, authenticate through `/login/admin`, and recover from 401.
- All documented SAML routes are connection-scoped and match mounted behavior.
- Solo/FakeIdP and optional Fleet/Keycloak tell one tested, security-honest story.
</success_criteria>

<source_coverage_audit>

| SOURCE | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | Mounted evaluator endpoints and authenticated trace journey | 72.1-01..04 | COVERED |
| REQ | — | No phase requirement IDs; SPEC-less probe visibly skipped and no IDs invented | all | EXCLUDED — source has none |
| RESEARCH | R-ROUTES | One route-derived URL set plus dispatch evidence | 72.1-01 | COVERED |
| RESEARCH | R-AUTH | Existing Basic Auth boundary and explicit runtime handoff | 72.1-01..04 | COVERED |
| CONTEXT | D-01 | Connection-scoped displayed URLs | 72.1-01, 72.1-03 | COVERED |
| CONTEXT | D-02 | Route behavior evidence | 72.1-01 | COVERED |
| CONTEXT | D-03 | Fail-closed Basic Auth | 72.1-01, 72.1-02, 72.1-04 | COVERED |
| CONTEXT | D-04 | Visible credential prerequisite/recovery | 72.1-02, 72.1-03 | COVERED |
| CONTEXT | D-05 | Valid and denied evidence across journeys | 72.1-01..04 | COVERED |

Deferred ideas: none. Package installs: none. Schema-push patterns: none. Assumption-delta detector: false.
</source_coverage_audit>

<output>
Create `.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-03-SUMMARY.md` when done.
</output>
