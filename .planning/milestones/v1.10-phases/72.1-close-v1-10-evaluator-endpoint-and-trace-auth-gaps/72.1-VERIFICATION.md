---
phase: 72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps
verified: 2026-08-27T21:14:40Z
status: passed
score: 8/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 72.1: Close v1.10 evaluator endpoint and trace-auth gaps — Verification Report

**Phase Goal:** Evaluators can configure the mounted connection-scoped SAML endpoints and reach validation traces through documented runtime Basic Auth, with deterministic allow/deny evidence.

**Verified:** 2026-08-27T21:14:40Z  
**Status:** passed  
**Re-verification:** No — initial verification

## Goal Achievement

The roadmap intentionally maps no requirement IDs to this inserted audit-closure phase. Its executable contract is therefore the five decisions in `72.1-CONTEXT.md` (D-01 through D-05), augmented by all four PLAN frontmatter `must_haves` blocks. No prior VERIFICATION.md existed.

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | **D-01:** Setup advertises metadata, login, and ACS as one mounted `/saml/:connection_id/...` set for an enabled connection. | ✓ VERIFIED | `SetupLive.saml_urls/1` derives all three values from the selected enabled `Connection` and Phoenix verified paths; the template renders only `@saml_urls.*` readonly fields. Focused test passed with exact absolute values. |
| 2 | **D-02:** Rendered endpoint values dispatch to the real metadata/login/ACS contract, not merely matching HTML suffixes. | ✓ VERIFIED | `setup_live_test` GETs the rendered metadata path (200) and login path (302 to FakeIdP); `fake_idp_flow_test` posts the signed response to the same rendered scoped ACS path and asserts redirect/receipt. 12 focused tests passed. |
| 3 | **D-03:** `/login/admin` and the exact connection trace fail closed for missing, partial, empty, absent-header, and invalid credentials. | ✓ VERIFIED | `DemoAdminAuth` requires non-empty runtime username *and* password and otherwise challenges/halt. Route test covers both paths for absent, username-only, password-only, empty values, absent header, and invalid header; it asserts 401, `WWW-Authenticate`, and nil admin session keys. |
| 4 | **D-03/D-04:** Solo and Fleet pass only operator-supplied runtime credentials, with no usable checked-in/default pair. | ✓ VERIFIED | Both Compose files use `${DEMO_ADMIN_USERNAME:-}` and `${DEMO_ADMIN_PASSWORD:-}`. `.env.example` retains only commented empty assignments and calls values operator-chosen/runtime-only. Docs drift tests and the Keycloak credential-policy self-test passed. |
| 5 | **D-04:** The Make-first and public evaluator journeys make the prerequisite and 401 recovery visible before trace access, without disclosing values. | ✓ VERIFIED | `make url` prints the two variable names and recovery via `/login/admin` before its route map; it never expands either value. Docker guide and demo README state the same Solo/Fleet/Keycloak handoff. The real-Make sentinel test passed and rejects disclosure. |
| 6 | **D-05:** Deterministic source/launcher/docs tests enforce forwarding parity, ordering, scoped-route documentation, and ownership wording. | ✓ VERIFIED | `test/docs/demo_guide_drift_test.exs` reads actual Compose, Make, env, and guide sources and runs `make url` with sentinels. `mix test test/docs/demo_guide_drift_test.exs` (27 tests) and `mix ci.docs` passed. |
| 7 | **D-03/D-05:** The optional Keycloak journey proves a denied exact trace before Basic Auth and an authenticated trace after `/login/admin`, with per-run credentials redacted. | ✓ VERIFIED | Playwright test explicitly checks 401/no trace rows, then authenticates only at `/login/admin` before visiting the exact trace. Harness generates an absent pair, rejects partial pairs, forwards both values to Playwright, and self-test passed. The E2E command is wired to required PR/push and scheduled `keycloak-proxy-e2e` CI. |
| 8 | Phase gates and scope remain intact: demo/docs evidence passes and no Relyra cryptographic or published surface changed. | ✓ VERIFIED | `mix ci.demo_app`, `mix ci.docs`, `mix format --check-formatted`, `mix qa` (794 tests, 0 failures), and `mix ci.security` passed. `git diff --exit-code -- lib mix.exs test/security/xml/adversarial_crypto_test.exs` exited 0. |

**Score:** 8/8 truths verified (0 present-but-behavior-unverified)

### Decision-Based Audit Closure

| Decision | Contract | Verification outcome |
| --- | --- | --- |
| D-01 | Same enabled connection supplies all three mounted SAML settings; stale unscoped guidance is absent. | VERIFIED — one data-derived URL assign, mounted router family, and scoped public docs. |
| D-02 | Route behavior proves rendered values, not suffix-only output. | VERIFIED — real metadata/login GETs plus signed scoped ACS POST in focused tests. |
| D-03 | Runtime Basic Auth remains fail closed for login and the whole admin mount. | VERIFIED — plug configuration requires a complete non-empty pair; exact login/trace deny matrix passes. |
| D-04 | Operator chooses runtime-only credentials; launcher/docs explain preflight and 401 recovery without secret persistence/disclosure. | VERIFIED — empty-default Compose forwarding, commented empty example, ordered Make/docs contract, and sentinel non-disclosure test. |
| D-05 | Deterministic allowed and denied evidence covers the intended evaluator journey. | VERIFIED — controller, docs/launcher, harness, and CI-wired Keycloak browser proof cover both sides. |

### Required Artifacts

All 13 artifacts declared across the four plans passed existence and substantive-content checks. Wiring was independently checked through the consumers below.

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex` | Connection-scoped URL derivation | ✓ VERIFIED | Queries an enabled connection, assigns one `saml_urls` map, and redirects `test_login` through its `login_path`. |
| `demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex` | Readonly SAML settings | ✓ VERIFIED | Renders metadata/login/ACS from `@saml_urls`; no hardcoded endpoint values. |
| `demo/ledger_loop/test/ledger_loop_web/live/setup_live_test.exs` | Render-to-router evidence | ✓ VERIFIED | Tests rendered values and sends GETs to their parsed paths. |
| `demo/ledger_loop/test/ledger_loop_web/controllers/route_affordance_controller_test.exs` | Exact trace allow/deny matrix | ✓ VERIFIED | Exercises host-auth entry and exact connection trace. |
| `docker-compose.yml`, `docker-compose.proxy.yml` | Runtime-only credential forwarding | ✓ VERIFIED | Both keys are empty-default forwarded in Solo and Fleet. |
| `Makefile`, `.env.example` | Public credential handoff | ✓ VERIFIED | Ordered prerequisite/recovery text and commented empty contract; values are not printed. |
| `guides/docker_dev_dx.md`, `demo/ledger_loop/README.md` | Evaluator journey and scoped route inventory | ✓ VERIFIED | Publish scoped SAML family and `/login/admin` recovery for the documented flows. |
| `test/docs/demo_guide_drift_test.exs` | Launcher/config/docs drift coverage | ✓ VERIFIED | Static source contracts plus real `make url` sentinel-output check. |
| `demo/ledger_loop/test/browser/keycloak.spec.ts`, `scripts/test_keycloak_proxy_e2e.sh` | Keycloak denied-then-authenticated proof | ✓ VERIFIED | Browser test and harness are connected through exported runtime credentials and mandatory E2E workflow. |

### Key Link Verification

`verify.key-links` reported all **9/9** declared links verified.

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `SetupLive` | router | `/saml/:connection_id/{metadata,login,acs}` | ✓ WIRED | Verified paths in `saml_urls/1`; router mounts `saml_routes()` under `/saml`. |
| HEEx template | `SetupLive` | `@saml_urls` assign | ✓ WIRED | Template consumes the one assign for all displayed endpoints. |
| route affordance test | `DemoAdminAuth` | `/login/admin` then trace | ✓ WIRED | Test obtains host-owned session only after valid Basic Auth. |
| `.env.example` | both Compose definitions | exact `DEMO_ADMIN_*` names | ✓ WIRED | Drift test enforces same names and empty-default semantics. |
| Makefile/guides | auth plug | prerequisite mirrors consumed runtime keys | ✓ WIRED | The displayed names are the plug's configured `:username`/`:password` inputs. |
| Keycloak harness | Playwright browser test | exported `DEMO_ADMIN_*` environment | ✓ WIRED | Harness passes both variables to `npx playwright test`; test refuses either missing value. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `SetupLive` / HEEx | `@saml_urls` | `Repo.one()` enabled `Relyra.Ecto.Connection` → `saml_urls/1` | Connection id drives three paths; tested against seeded connection/router | ✓ FLOWING |
| trace pages | admin session / trace records | complete runtime Basic Auth pair → `DemoAdminAuth` → `relyra_admin_routes` | Valid credentials set host-owned session; exact trace responds 200 in focused test | ✓ FLOWING |
| Keycloak browser proof | `DEMO_ADMIN_*` | caller pair or per-run generated pair → Compose → Playwright | Harness self-test verifies pair forwarding; browser test consumes both before auth | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Rendered metadata/login/ACS dispatch and Basic Auth deny/allow matrix | `mix test` for SetupLive, FakeIdP flow, and route-affordance files (from `demo/ledger_loop`) | 12 tests, 0 failures | ✓ PASS |
| Docs/config/Make credential contract | `mix test test/docs/demo_guide_drift_test.exs` | 27 tests, 0 failures | ✓ PASS |
| Harness runtime credential policy | `KEYCLOAK_PROXY_CREDENTIAL_POLICY_SELF_TEST=1 bash scripts/test_keycloak_proxy_e2e.sh` | Credential-policy self-test passed | ✓ PASS |
| Focused demo/docs lanes and scope guard | `mix ci.demo_app && mix ci.docs && mix format --check-formatted && git diff --exit-code -- lib mix.exs test/security/xml/adversarial_crypto_test.exs` | Exit 0 | ✓ PASS |
| Workspace and security gates | `mix qa && mix ci.security` | `mix qa`: 794 tests, 0 failures; security lane exit 0 | ✓ PASS |

### Probe Execution

**SKIPPED:** No `probe-*.sh` files or phase-declared probes exist. The phase uses ExUnit, Make fixtures, and the CI-wired Keycloak E2E harness instead.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| None | 72.1-01 through 72.1-04 | Roadmap and every plan declare this is an audit-closure phase with no mapped REQ IDs. | ✓ SATISFIED | `REQUIREMENTS.md` contains no Phase 72.1 mapping; all plans declare `requirements: []`; D-01–D-05 are explicitly covered above. |

No orphaned requirements were found: the roadmap does not assign requirements to Phase 72.1, and no plan invents an ID.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `setup_live.ex` | 42–48 | Review warnings WR-01/WR-02: metadata save is in-memory and forged `set_step` can raise. | ⚠️ Advisory | These are real demo-UX/resilience defects, but neither changes the D-01 route-derived endpoint set nor the D-03–D-05 authenticated trace journey. No goal-blocking endpoint or auth bypass was found. |
| `setup_live.html.heex` | 203–205 | Review warning WR-03: `Enable Connection` control is inert. | ⚠️ Advisory | The phase is explicitly a seeded-enabled evaluator route/trace closure, not metadata ingestion or connection enablement. It does not invalidate the tested mounted endpoints or Basic Auth boundary. |

The remaining scan matches were an input placeholder, documentation prose, and `mktemp` templates; none is a completion/debt marker or a user-visible stub in the delivered D-01–D-05 path. No unreferenced `TBD`, `FIXME`, or `XXX` marker was found in phase-modified files.

### Human Verification Required

None. The project requires deterministic automated evidence; this phase provides it through focused tests, source/fixture contracts, and a required PR/push/scheduled Keycloak E2E lane.

### Gaps Summary

No gaps found. The review's three warnings should be handled as follow-up demo UX/resilience work, but they are outside the decision-scoped audit closure and do not weaken the mounted endpoint or runtime Basic Auth contracts verified here.

---

_Verified: 2026-08-27T21:14:40Z_  
_Verifier: the agent (gsd-verifier)_
