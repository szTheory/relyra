---
phase: 72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps
plan: "02"
subsystem: infra
tags: [docker-compose, make, demo, basic-auth, documentation-tests]
requires:
  - phase: 72.1-01
    provides: "Scoped admin authentication proof and fail-closed credential behavior"
provides:
  - "Solo Compose forwards operator-provided demo admin credentials with empty defaults"
  - "The URL banner gives credential preflight and 401 recovery without revealing values"
  - "Deterministic tests enforce Compose, launcher, and environment-example parity"
affects: [72.1-03, 72.1-04, docker-demo-launcher]
tech-stack:
  added: []
  patterns: ["Empty-default runtime environment forwarding", "Sentinel-value non-disclosure assertions"]
key-files:
  created: []
  modified: [docker-compose.yml, Makefile, .env.example, test/docs/demo_guide_drift_test.exs]
key-decisions:
  - "Forward both admin keys with empty-default Compose expansion so absent credentials remain fail-closed."
  - "Keep credential values out of Make output; document only variable names and the authenticated recovery route."
patterns-established:
  - "Demo credential configuration is operator-chosen, runtime-only, and testable with non-disclosing sentinels."
requirements-completed: []
coverage:
  - id: D-03
    description: "Solo and Fleet Compose sources forward both operator-supplied admin credential keys with empty defaults."
    verification:
      - kind: unit
        ref: "test/docs/demo_guide_drift_test.exs#admin credentials are empty-forwarded and kept out of the route banner"
        status: pass
    human_judgment: false
  - id: D-04
    description: "The public URL launcher explains credential preflight and 401 recovery without printing credential values."
    verification:
      - kind: unit
        ref: "test/docs/demo_guide_drift_test.exs#admin credentials are empty-forwarded and kept out of the route banner"
        status: pass
    human_judgment: false
  - id: D-05
    description: "Focused documentation CI executes the real Make URL target and enforces the runtime credential contract."
    verification:
      - kind: other
        ref: "mix ci.docs"
        status: pass
    human_judgment: false
duration: 4min
completed: 2026-08-27
status: complete
---

# Phase 72.1 Plan 02: Runtime Admin Credential Handoff Summary

**Empty-default Solo/Fleet demo credential forwarding with a non-disclosing launcher preflight and 401 recovery path.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-08-27T20:46:00Z
- **Completed:** 2026-08-27T20:50:00Z
- **Tasks:** 1/1
- **Files modified:** 4

## Accomplishments

- Forwarded `DEMO_ADMIN_USERNAME` and `DEMO_ADMIN_PASSWORD` from the operator environment into Solo Compose with empty defaults, matching the existing Fleet overlay.
- Added `make url` guidance before the route map so trace access is never promised before credential setup; 401 recovery names the authenticated `/login/admin` entry.
- Added deterministic source and real-Make-output coverage that rejects usable defaults and sentinel credential disclosure.

## Task Commits

1. **Task 1: Carry operator-chosen credentials from preflight into Solo and Fleet runtime** - `5fdb1a9` (test), `3194477` (feat)

## Files Created/Modified

- `docker-compose.yml` - Empty-default environment forwarding for the Solo demo service.
- `Makefile` - Credential prerequisite, non-disclosure statement, and 401 recovery in `url`.
- `.env.example` - Operator-chosen, runtime-only, commented empty credential contract.
- `test/docs/demo_guide_drift_test.exs` - Compose parity, ordering, and sentinel non-disclosure assertions.

## Decisions Made

- Reused the existing Fleet empty-default forwarding contract in the Solo base file, preserving absent/empty credential denial.
- Printed credential variable names only; values are neither interpolated into Make recipes nor persisted in the example environment file.

## TDD Gate Compliance

- RED: `5fdb1a9` added the handoff contract and failed because the Solo Compose keys were absent.
- GREEN: `3194477` added forwarding and launcher guidance; focused tests and `mix ci.docs` pass.
- REFACTOR: Not needed.

## Deviations from Plan

None - plan executed exactly as written. `docker-compose.proxy.yml` already contained the required empty-default forwarding and was protected by the new parity test, so it required no content change.

## Known Stubs

None.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plans 72.1-03 and 72.1-04 can rely on an explicit, fail-closed runtime credential handoff and launcher recovery wording.

## Self-Check: PASSED

- Confirmed all four implementation/test files and this summary exist.
- Confirmed task commits `5fdb1a9` and `3194477` exist with no tracked-file deletions.
