---
phase: 69-compose-split-fleet-proxy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - docker-compose.override.yml
  - demo/ledger_loop/config/runtime.exs
  - demo/ledger_loop/config/dev.exs
autonomous: true
requirements: [FLEET-01, FLEET-03]
user_setup: []

must_haves:
  truths:
    - "A plain `docker compose up` selects the unprofiled `db` and `demo_app` services, publishes only `demo_app` as `127.0.0.1:${PORT:-4000}:4000`, and leaves PostgreSQL reachable only as `db:5432` on the project network (FLEET-01, D-01..D-04)."
    - "The Phase-68 `demo_app` build, entrypoint, healthcheck, command, working directory, dependency, and four nested named-volume mounts remain intact after the Compose split (D-01)."
    - "For non-test runtime, Phoenix derives endpoint `url` and `check_origin` from `PHX_HOST`, `PHX_SCHEME`, `PHX_PORT`, and `DEMO_CHECK_ORIGINS`; defaults produce `http://localhost:${PORT:-4000}` and explicitly allow the solo and Relyra proxy origins (FLEET-03, D-05/D-06)."
    - statement: "When solo and fleet browser sessions are active concurrently, each public host retains its own correct generated endpoint URL and established LiveView WebSocket without origin-policy cross-talk (FLEET-03 concurrency edge)."
      verification: backstop
    - "The existing LedgerLoop UI remains visually and behaviorally unchanged; this phase introduces no loading treatment, proxy status indicator, CTA, banner, or application error surface, and transport failures remain browser/Traefik failures (UI Considerations: loading/error)."
    - "Existing read-only endpoint URL fields remain selectable and horizontally scrollable without visual truncation at either supported host (UI Considerations: long-text)."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Portable internal service graph with unprofiled db/demo_app and no host ports for either service"
      contains: "name: relyra-demo"
    - path: "docker-compose.override.yml"
      provides: "Auto-loaded solo ingress for demo_app only"
      contains: "127.0.0.1:${PORT:-4000}:4000"
    - path: "demo/ledger_loop/config/runtime.exs"
      provides: "Non-test runtime public URL and explicit origin-list configuration"
      contains: "DEMO_CHECK_ORIGINS"
    - path: "demo/ledger_loop/config/dev.exs"
      provides: "Development endpoint settings without the broad check_origin bypass"
  key_links:
    - from: "docker-compose.override.yml services.demo_app.ports"
      to: "docker-compose.yml services.demo_app"
      via: "Compose automatic base+override merge for the plain solo command"
      pattern: "127.0.0.1:.*:4000"
    - from: "docker-compose.proxy.yml services.demo_app.environment (Plan 69-02)"
      to: "demo/ledger_loop/config/runtime.exs"
      via: "PHX_HOST/PHX_SCHEME/PHX_PORT/DEMO_CHECK_ORIGINS runtime environment"
      pattern: "PHX_(HOST|SCHEME|PORT)|DEMO_CHECK_ORIGINS"
  assumptions:
    - statement: "FLEET-01 was classified as an unclassified edge by the spec-less probe; the plan treats the rendered solo graph plus a real bare-up receipt as the acceptance boundary, but the taxonomy row remains unresolved rather than silently dismissed."
      status: unresolved
      flagged: true
  prohibitions: []
---

<objective>
Establish the production-quality solo tracer: split the portable Compose graph from its auto-loaded loopback ingress, remove the machine-wide PostgreSQL port, and make the existing Phoenix UI boot with an explicit runtime URL/origin policy.

Purpose: Prove the zero-setup path first, preserving Phase 68 exactly while establishing the endpoint seam the fleet overlay consumes in Plan 69-02 (FLEET-01/FLEET-03, D-01..D-07).
Output: Modified `docker-compose.yml`, new `docker-compose.override.yml`, and env-driven endpoint policy in `demo/ledger_loop/config/runtime.exs`/`dev.exs`.
</objective>

<assumption_delta_decision>
Primary generalized noun: local demo ingress identity.
Decision: promote.
Rationale: The neutral shared identities `dev_proxy` and `proxy` become primary for fleet ingress; `relyra-demo`, `relyra.localhost`, and `relyra-*` remain details of the Relyra participant, so no sibling project is modeled as subordinate to Relyra.
</assumption_delta_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-compose-split-fleet-proxy/69-CONTEXT.md
@.planning/phases/69-compose-split-fleet-proxy/69-RESEARCH.md
@.planning/phases/69-compose-split-fleet-proxy/69-PATTERNS.md
@.planning/phases/69-compose-split-fleet-proxy/69-VALIDATION.md
@.planning/phases/69-compose-split-fleet-proxy/69-UI-SPEC.md
@.planning/phases/68-build-caching-correctness/68-01-SUMMARY.md
@.planning/phases/68-build-caching-correctness/68-02-SUMMARY.md
</context>

## Artifacts this phase produces

- New file `docker-compose.override.yml`: `services.demo_app.ports` with `127.0.0.1:${PORT:-4000}:4000`.
- New file `docker-compose.proxy.yml`: `services.demo_app.environment`, Traefik labels for router/service `relyra-local-demo`, dual `default` + `proxy` networks, and external network name `${DEMO_PROXY_NETWORK:-proxy}`.
- New file `docker/traefik/compose.yml`: Compose project `dev_proxy`, service `traefik`, image `traefik:v3.7.1`, entrypoint `web`, loopback ports 80/8080, read-only `/var/run/docker.sock`, external network `proxy`.
- Modified `docker-compose.yml`: `db` and `demo_app` become default services; their host-port declarations leave the base; Phase-68 `build`, `command`, entrypoint behavior, healthcheck, dependency, and `relyra_deps`/`relyra_build`/`relyra_hex`/`relyra_mix` volumes remain.
- Modified `demo/ledger_loop/config/runtime.exs`: endpoint `url` and `check_origin` consume `PORT`, `PHX_HOST`, `PHX_SCHEME`, `PHX_PORT`, and `DEMO_CHECK_ORIGINS`.
- Modified `demo/ledger_loop/config/dev.exs`: broad development origin bypass removed while existing HTTP/reload/debug/fs-poll behavior remains.
- Compose substitution hooks: `PORT`, `RELYRA_HOST`, `DEMO_PROXY_NETWORK`, `DEMO_CHECK_ORIGINS`; container endpoint keys: `PHX_HOST`, `PHX_SCHEME`, `PHX_PORT`.
- Planning-only `COVERAGE.md`: reasoned declaration that the API detector's Endpoint/API terms are false positives because this phase has no external API/SDK capability surface.

<tasks>

<task type="tracer">
  <name>Task 1: Prove the bare-up solo path through the split Compose graph</name>
  <reversibility rating="costly">The base/automatic-overlay boundary is consumed by Phases 70-72 and sibling tooling, so undoing it later requires coordinated launcher and documentation changes, but no public or persisted-data migration.</reversibility>
  <files>docker-compose.yml, docker-compose.override.yml</files>
  <read_first>
    - docker-compose.yml (modified target; preserve the exact Phase-68 demo_app build, command, nested volumes, healthcheck, environment, working_dir, and db dependency)
    - docker-compose.override.yml (new target; confirm absent before creation)
    - .planning/phases/69-compose-split-fleet-proxy/69-CONTEXT.md (D-01..D-04, D-07/D-08/D-10/D-11 scope fences)
    - .planning/phases/69-compose-split-fleet-proxy/69-PATTERNS.md (`docker-compose.yml` and automatic solo-overlay assignments)
    - .planning/phases/68-build-caching-correctness/68-02-SUMMARY.md (upstream build/volume/fs-poll contract that must survive)
    - /Users/jon/projects/rulestead/docker-compose.override.yml (closest maintained auto-overlay analog)
  </read_first>
  <action>
    Implement FLEET-01 per D-01..D-04. In `docker-compose.yml`, keep `name: relyra-demo`; remove `profiles: ["core"]` from exactly `db` and `demo_app` so a bare command selects both, while retaining the `keycloak` and `playwright` profiles unchanged. Remove the host-port declarations from `db` and `demo_app`; do not attach `db` to any new network. Preserve every Phase-68 `demo_app` build/command/volume/working_dir/depends_on/environment/healthcheck field and all four top-level `relyra_*` volume declarations byte-for-behavior. D-02: Create `docker-compose.override.yml` with only `services.demo_app.ports` and the single mapping `127.0.0.1:${PORT:-4000}:4000`; include a short comment stating that plain Compose auto-loads it and explicit fleet `-f` commands omit it. Add no proxy, Keycloak, database, or endpoint configuration to the automatic overlay. Per D-07/D-08/D-10/D-11, do not edit fixtures, realm files, `Makefile`, `.env.example`, docs, or any `lib/` path.
  </action>
  <acceptance_criteria>
    - `docker compose config --format json` contains both `services.db` and `services.demo_app` without any profile activation.
    - In the rendered solo JSON, `.services.db.ports == null`, `.services.demo_app.ports` has exactly one item, that item has `host_ip == "127.0.0.1"` and `target == 4000`.
    - Source `docker-compose.yml` retains `demo/ledger_loop/Dockerfile.dev`, `command: mix phx.server`, all four Phase-68 named-volume mounts, the healthcheck, and `depends_on.db.condition: service_healthy`.
    - `keycloak` retains profile `keycloak`, its current host port and environment; `playwright` retains profile `browser`, its internal `BASE_URL=http://demo_app:4000`, and current dependency graph.
    - `docker-compose.override.yml` declares no service other than `demo_app`.
  </acceptance_criteria>
  <verify>
    <automated>solo_json=$(docker compose config --format json) &amp;&amp; printf '%s' "$solo_json" | jq -e '.services | has("db") and has("demo_app")' &gt;/dev/null &amp;&amp; printf '%s' "$solo_json" | jq -e '.services.db.ports == null and (.services.demo_app.ports | length == 1) and .services.demo_app.ports[0].host_ip == "127.0.0.1" and .services.demo_app.ports[0].target == 4000' &gt;/dev/null &amp;&amp; printf '%s' "$solo_json" | jq -e '.services.keycloak.profiles == ["keycloak"] and .services.playwright.profiles == ["browser"]' &gt;/dev/null</automated>
    <runtime_receipt>`docker compose up -d --wait` exits 0; `curl -fsS http://127.0.0.1:${PORT:-4000}/` succeeds; `docker compose ps --format json` shows healthy `db`/`demo_app`; `docker port relyra-demo-db-1` prints no binding. Stop with `docker compose down` without deleting Phase-68 named volumes.</runtime_receipt>
  </verify>
  <done>A real user can run plain `docker compose up`, load the unchanged demo at the loopback URL, and incur no host PostgreSQL binding; the Phase-68 container graph remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce one runtime URL/origin policy for solo and fleet hosts</name>
  <reversibility rating="costly">Phases 70-72 consume these environment keys and endpoint semantics; changing them later requires coordinated Compose, Keycloak, launcher, and documentation edits, but no public Relyra API break.</reversibility>
  <files>demo/ledger_loop/config/runtime.exs, demo/ledger_loop/config/dev.exs</files>
  <read_first>
    - demo/ledger_loop/config/runtime.exs (modified target; current all-env HTTP port and prod-only URL block)
    - demo/ledger_loop/config/dev.exs (modified target; remove only `check_origin: false`, preserve the measured container-wide `http: [ip: {0, 0, 0, 0}]` bind required by Docker port mapping and Traefik, plus the remaining endpoint behavior and Phase-68 `:fs_poll` block)
    - demo/ledger_loop/config/config.exs (source of truth for the preserved compile-time `url: [host: "localhost"]` fallback; do not modify)
    - .planning/phases/69-compose-split-fleet-proxy/69-CONTEXT.md (D-05..D-07 and D-11)
    - .planning/phases/69-compose-split-fleet-proxy/69-RESEARCH.md (Runtime public-endpoint pattern, Pitfall 4, Phoenix origin threat)
    - .planning/phases/69-compose-split-fleet-proxy/69-PATTERNS.md (runtime/config/dev responsibility assignments)
    - /Users/jon/projects/rulestead/docker-compose.proxy.yml (maintained env-key analog consumed by the runtime policy)
  </read_first>
  <action>
    Implement FLEET-03 per D-05/D-06. In `runtime.exs`, add a non-test runtime endpoint block outside the existing production-only branch. Derive the bound HTTP port from `PORT` default `4000`; derive public URL host from `PHX_HOST` default `localhost`, scheme from `PHX_SCHEME` default `http`, and public port from `PHX_PORT` defaulting to the bound port; parse both ports with `String.to_integer/1`. Derive `check_origin` by reading `DEMO_CHECK_ORIGINS` with default `//localhost,//relyra.localhost,//*.relyra.localhost` and splitting on commas with trimming. Configure `LedgerLoopWeb.Endpoint` with `url: [host:, scheme:, port:]` and that explicit list. Order the block so the existing production configuration can retain its current database/secret/HTTPS behavior. In `dev.exs`, remove the broad origin bypass but preserve `http: [ip: {0, 0, 0, 0}]`: this is the container-side listener needed for both Docker port forwarding and Traefik reachability, distinct from the host-side loopback publication supplied by `docker-compose.override.yml` and the loopback-only Traefik ingress. Leave code reload, debug settings, secret, watchers, LiveReload patterns, and the user’s existing Phase-68 `config :phoenix_live_reload`/`dirs`/`:fs_poll` work untouched. Preserve `config.exs` as the localhost fallback. Per D-07, do not change FakeIdP fixture/controller data: its Destination/Recipient behavior is intentionally host-independent.
  </action>
  <acceptance_criteria>
    - Under default `MIX_ENV=dev`, endpoint URL resolves to host `localhost`, scheme `http`, and public port equal to `PORT` (default 4000).
    - With `PHX_HOST=relyra.localhost PHX_SCHEME=http PHX_PORT=80`, endpoint URL resolves to exactly those public values without changing the bound container HTTP port.
    - Endpoint `check_origin` is a list containing `//localhost`, `//relyra.localhost`, and `//*.relyra.localhost`; a comma-separated `DEMO_CHECK_ORIGINS` override replaces the default list.
    - `dev.exs` has no boolean origin bypass and retains `http: [ip: {0, 0, 0, 0}]` for container-network reachability, while host exposure remains limited to `127.0.0.1` by the solo override or Traefik publication; reload/debug settings and the Phase-68 `config :phoenix_live_reload` block including `dirs`, `backend: :fs_poll`, and 500ms interval remain intact.
    - `mix format --check-formatted config/runtime.exs config/dev.exs` exits 0 from `demo/ledger_loop`.
  </acceptance_criteria>
  <verify>
    <automated>cd demo/ledger_loop &amp;&amp; mix format --check-formatted config/runtime.exs config/dev.exs &amp;&amp; MIX_ENV=dev PHX_HOST=relyra.localhost PHX_SCHEME=http PHX_PORT=80 DEMO_CHECK_ORIGINS='//localhost,//relyra.localhost,//*.relyra.localhost' mix run --no-start -e 'cfg = Application.fetch_env!(:ledger_loop, LedgerLoopWeb.Endpoint); url = Keyword.fetch!(cfg, :url); origins = Keyword.fetch!(cfg, :check_origin); unless Keyword.fetch!(url, :host) == "relyra.localhost" and Keyword.fetch!(url, :scheme) == "http" and Keyword.fetch!(url, :port) == 80 and Enum.all?(["//localhost", "//relyra.localhost", "//*.relyra.localhost"], &amp;(&amp;1 in origins)), do: System.halt(1)'</automated>
    <human_check>After Plan 69-02 is running, load the existing operator UI at both public hosts; confirm the LiveView socket remains connected, no origin error appears, no new proxy/error/loading UI was introduced, and read-only endpoint URL fields remain selectable and horizontally scrollable.</human_check>
  </verify>
  <done>The existing UI loads under the solo endpoint policy, and the same runtime seam is ready to accept the fleet overlay’s public host without disabling Phoenix origin checks or altering any Relyra verification code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser → loopback-published Phoenix port | Browser HTTP and cookie-backed LiveView WebSocket traffic crosses into the dev container. |
| demo_app → db on Compose default network | PostgreSQL credentials/data remain internal to the project network and are not host-published. |
| browser Origin header → Phoenix socket origin policy | Untrusted Origin input is matched against the explicit runtime allowlist. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-01 | Information Disclosure / DoS | PostgreSQL host publication | high | mitigate | Task 1 removes the database host binding from every solo/base path; rendered-config assertion requires `services.db.ports == null` (D-04). |
| T-69-02 | Spoofing | LiveView WebSocket Origin | high | mitigate | Task 2 replaces the development boolean bypass with explicit `DEMO_CHECK_ORIGINS` values and verifies the parsed list (D-06). |
| T-69-03 | Information Disclosure | Demo app reachable from LAN | medium | mitigate | Task 1’s only solo mapping sets `host_ip` to `127.0.0.1`; rendered JSON asserts loopback binding (D-02). |
| T-69-05 | Tampering | Existing FakeIdP trust-path fixtures | low | accept | Phase 69 does not modify them per D-07; host-independent Destination/Recipient equality remains unchanged, so there is no new trust mutation. |

No package-manager install occurs, no ORM/schema path is in scope, and no public/security-posture API changes are made.
</threat_model>

<verification>
- Static gate: `docker compose config --format json` proves unprofiled solo services, loopback-only app ingress, and absent PostgreSQL host ingress.
- Endpoint gate: the scoped `mix run --no-start` assertion proves proxy env becomes Phoenix’s public URL and explicit origin list; `mix format --check-formatted` stays green.
- Runtime receipt: plain `docker compose up -d --wait`, loopback HTTP request, healthy services, and no database port binding.
- UI receipt: the unchanged operator UI loads, LiveView connects, no new UI state/copy appears, and existing long URL fields retain native selection/overflow behavior.
- Scope gate: `git diff --name-only` for this plan contains only the four declared files and no `lib/`, fixture, realm, `Makefile`, `.env.example`, guide, or README path.
</verification>

<success_criteria>
- FLEET-01: plain Compose is zero-setup, publishes only the loopback app port, and never publishes PostgreSQL.
- FLEET-03 foundation: endpoint URL/origin values are explicit and env-driven for both public-host shapes without weakening origin checks.
- Phase-68 build/entrypoint/healthcheck/nested volumes and all existing browser UI remain intact.
- All plan truths, including UI Considerations and the unresolved FLEET-01 edge flag, are carried forward without silent dismissal.
</success_criteria>

<output>
Create `.planning/phases/69-compose-split-fleet-proxy/69-01-SUMMARY.md` when done.
</output>
