---
phase: 69-compose-split-fleet-proxy
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - docker/traefik/compose.yml
  - docker-compose.proxy.yml
autonomous: true
requirements: [FLEET-02, FLEET-03]
user_setup: []

must_haves:
  truths:
    - "A neutral shared Traefik project named `dev_proxy` runs `traefik:v3.7.1` on the external `proxy` network, publishes web/dashboard only on loopback ports 80/8080, mounts the Docker socket read-only, and disables exposed-by-default (FLEET-02, D-09)."
    - "The explicit base+proxy overlay publishes no `demo_app` or `db` host port, joins only `demo_app` to both `default` and external `proxy`, and routes `Host(`${RELYRA_HOST:-relyra.localhost}`)` to container port 4000 through router/service `relyra-local-demo` (D-03/D-04/D-12)."
    - "Fleet environment values `PHX_HOST=${RELYRA_HOST:-relyra.localhost}`, `PHX_SCHEME=http`, `PHX_PORT=80`, and the explicit origin list feed Plan 69-01’s runtime endpoint policy, so the unchanged operator UI and LiveView socket work at the proxy host (FLEET-03, D-05/D-06)."
    - statement: "Starting or stopping Relyra alongside another sibling demo does not contend for app/database host ports, mutate the neutral shared proxy identity, or remove the long-lived external `proxy` network (FLEET-02 concurrency edge)."
      verification: backstop
    - "The existing LedgerLoop UI at `relyra.localhost` is the same UI as the solo path: no new loading state, proxy indicator, CTA, banner, or application error presentation is introduced (UI Considerations: loading/error)."
    - "Existing read-only endpoint URL fields remain selectable and horizontally scrollable without truncation when populated with the proxy-host URL (UI Considerations: long-text)."
  artifacts:
    - path: "docker/traefik/compose.yml"
      provides: "Independent shared local reverse proxy project"
      contains: "name: dev_proxy"
    - path: "docker-compose.proxy.yml"
      provides: "Opt-in Relyra demo routing overlay without host ports"
      contains: "traefik.http.routers.relyra-local-demo.rule"
  key_links:
    - from: "docker-compose.proxy.yml services.demo_app.networks.proxy"
      to: "docker/traefik/compose.yml services.traefik.networks.proxy"
      via: "same external network name `${DEMO_PROXY_NETWORK:-proxy}` / `proxy`"
      pattern: "external: true"
    - from: "Traefik router relyra-local-demo"
      to: "Traefik service relyra-local-demo"
      via: "explicit router `.service` label and backend port 4000 label"
      pattern: "relyra-local-demo"
    - from: "docker-compose.proxy.yml services.demo_app.environment"
      to: "demo/ledger_loop/config/runtime.exs"
      via: "PHX_HOST/PHX_SCHEME/PHX_PORT/DEMO_CHECK_ORIGINS consumed by Plan 69-01"
      pattern: "PHX_HOST"
  prohibitions:
    - statement: "The shared proxy project and external network MUST NOT use Relyra as their identity anchor or require sibling demos to adopt Relyra-specific shared-resource names."
      status: unresolved
      flagged: true
---

<objective>
Expand the proven solo tracer into the opt-in fleet path: create the neutral long-lived Traefik project, then attach only Relyra’s demo application with globally unique routing labels and no host-port contention.

Purpose: Multiple sibling demos can share one ingress without turning the proxy into a Relyra-owned resource or making it a dependency of the solo path (FLEET-02/FLEET-03, D-03/D-05/D-06/D-08..D-12).
Output: New `docker/traefik/compose.yml` and `docker-compose.proxy.yml`.
</objective>

<assumption_delta_decision>
Primary generalized noun: shared local ingress identity.
Decision: promote.
Rationale: `dev_proxy` plus external network `proxy` is the generalized primary identity; each sibling contributes its own namespaced routers/services, and `relyra-local-demo` is only Relyra’s participant detail.
</assumption_delta_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-compose-split-fleet-proxy/69-CONTEXT.md
@.planning/phases/69-compose-split-fleet-proxy/69-RESEARCH.md
@.planning/phases/69-compose-split-fleet-proxy/69-PATTERNS.md
@.planning/phases/69-compose-split-fleet-proxy/69-VALIDATION.md
@.planning/phases/69-compose-split-fleet-proxy/69-UI-SPEC.md
@.planning/phases/69-compose-split-fleet-proxy/69-01-SUMMARY.md
</context>

## Artifacts this phase produces

- New file `docker-compose.override.yml`: `services.demo_app.ports` with `127.0.0.1:${PORT:-4000}:4000`.
- New file `docker-compose.proxy.yml`: `services.demo_app.environment`, Traefik labels for router/service `relyra-local-demo`, dual `default` + `proxy` networks, and external network name `${DEMO_PROXY_NETWORK:-proxy}`.
- New file `docker/traefik/compose.yml`: Compose project `dev_proxy`, service `traefik`, image `traefik:v3.7.1`, entrypoint `web`, loopback ports 80/8080, read-only `/var/run/docker.sock`, external network `proxy`.
- Modified `docker-compose.yml`: `db` and `demo_app` become default services; their host-port declarations leave the base; Phase-68 `build`, `command`, entrypoint behavior, healthcheck, dependency, and `relyra_deps`/`relyra_build`/`relyra_hex`/`relyra_mix` volumes remain.
- Modified `demo/ledger_loop/config/runtime.exs`: endpoint `url` and `check_origin` consume `PORT`, `PHX_HOST`, `PHX_SCHEME`, `PHX_PORT`, and `DEMO_CHECK_ORIGINS`.
- Modified `demo/ledger_loop/config/dev.exs`: broad development origin bypass removed while existing HTTP/reload/debug/fs-poll behavior remains.
- Compose substitution hooks: `PORT`, `RELYRA_HOST`, `DEMO_PROXY_NETWORK`, `DEMO_CHECK_ORIGINS`; container endpoint keys: `PHX_HOST`, `PHX_SCHEME`, `PHX_PORT`.
- Planning-only `COVERAGE.md`: reasoned declaration that the API detector's Endpoint/API terms are false positives because this phase has no external API/SDK capability surface.

<tasks>

<task type="auto">
  <name>Task 1: Create the neutral shared Traefik project</name>
  <reversibility rating="costly">Sibling repositories converge on the neutral project/network names and pinned proxy behavior; renaming later requires coordinated changes across repos, but no public API or data migration.</reversibility>
  <files>docker/traefik/compose.yml</files>
  <read_first>
    - docker/traefik/compose.yml (new target; confirm absent before creation)
    - /Users/jon/projects/scoria/docker/traefik/compose.yml (mandated verbatim maintained analog, including explanatory comments)
    - .planning/phases/69-compose-split-fleet-proxy/69-CONTEXT.md (D-09/D-10/D-12)
    - .planning/phases/69-compose-split-fleet-proxy/69-PATTERNS.md (`docker/traefik/compose.yml` exact pattern assignment)
    - .planning/phases/69-compose-split-fleet-proxy/69-RESEARCH.md (Traefik 3.7.1/Docker 29 compatibility and threat patterns)
  </read_first>
  <action>
    Create `docker/traefik/compose.yml` from the scoria analog verbatim per D-09, preserving the neutral `name: dev_proxy` identity and its explanatory comments. Define service `traefik` with exact image `traefik:v3.7.1`, `restart: unless-stopped`, Docker provider enabled, exposed-by-default disabled, entrypoint `web` at `:80`, and insecure dashboard enabled for loopback-only local development. Publish `127.0.0.1:80:80` and `127.0.0.1:8080:8080`; mount `/var/run/docker.sock:/var/run/docker.sock:ro`; join external network `proxy`. Do not create the network in Compose because the resource is intentionally long-lived/shared. D-10 leaves the `Makefile` wrapper to Phase 71; the executor’s raw idempotent setup is `docker network inspect proxy >/dev/null 2>&1 || docker network create proxy`, followed by this Compose project’s `up -d`.
  </action>
  <acceptance_criteria>
    - Rendered project name is `dev_proxy`, service image is exactly `traefik:v3.7.1`, and restart policy is `unless-stopped`.
    - Rendered command enables Docker discovery, disables exposed-by-default, defines web `:80`, and enables the dashboard/insecure API.
    - Both host publications use `host_ip == "127.0.0.1"` with targets/published ports 80 and 8080.
    - Docker socket target is `/var/run/docker.sock` with read-only mode; service joins only external network `proxy`.
    - `docker compose -f docker/traefik/compose.yml config` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>proxy_json=$(docker compose -f docker/traefik/compose.yml config --format json) &amp;&amp; printf '%s' "$proxy_json" | jq -e '.name == "dev_proxy" and .services.traefik.image == "traefik:v3.7.1" and .services.traefik.restart == "unless-stopped"' &gt;/dev/null &amp;&amp; printf '%s' "$proxy_json" | jq -e '[.services.traefik.ports[] | select(.host_ip == "127.0.0.1") | .target] | sort == [80,8080]' &gt;/dev/null &amp;&amp; printf '%s' "$proxy_json" | jq -e '.networks.proxy.external == true' &gt;/dev/null</automated>
    <runtime_receipt>`docker network inspect proxy >/dev/null 2>&1 || docker network create proxy`; run `docker compose -f docker/traefik/compose.yml up -d`; request `http://127.0.0.1:8080/dashboard/`; repeat both commands and confirm the existing network/project are reused without replacement.</runtime_receipt>
  </verify>
  <done>A real user can start or reuse the neutral shared proxy and view its loopback-only dashboard before any individual sibling demo joins it.</done>
</task>

<task type="auto">
  <name>Task 2: Route the Relyra participant through the shared proxy with no host ports</name>
  <reversibility rating="costly">The static Relyra router/service identifiers and env hooks are consumed by Phases 70-72; changing them later is coordinated but does not break a published library API.</reversibility>
  <files>docker-compose.proxy.yml</files>
  <read_first>
    - docker-compose.proxy.yml (new target; confirm absent before creation)
    - docker-compose.yml (Plan 69-01 base target; verify no app/db host ports and preserve default network service discovery)
    - docker/traefik/compose.yml (Task 1 target; shared entrypoint/network names this overlay must match)
    - demo/ledger_loop/config/runtime.exs (Plan 69-01 consumer of PHX_HOST/PHX_SCHEME/PHX_PORT/DEMO_CHECK_ORIGINS)
    - /Users/jon/projects/rulestead/docker-compose.proxy.yml (closest maintained Phoenix overlay analog)
    - .planning/phases/69-compose-split-fleet-proxy/69-CONTEXT.md (D-03..D-06 and D-08..D-12)
    - .planning/phases/69-compose-split-fleet-proxy/69-PATTERNS.md (`docker-compose.proxy.yml` label/network/env assignment)
  </read_first>
  <action>
    Create the explicit fleet overlay per D-03/D-05/D-06/D-08/D-11/D-12. Under `services.demo_app.environment`, set `PHX_HOST: ${RELYRA_HOST:-relyra.localhost}`, `PHX_SCHEME: http`, `PHX_PORT: "80"`, and `DEMO_CHECK_ORIGINS: ${DEMO_CHECK_ORIGINS:-//localhost,//relyra.localhost,//*.relyra.localhost}`. Add list-form labels: enable Traefik; select `${DEMO_PROXY_NETWORK:-proxy}`; define `traefik.http.routers.relyra-local-demo.rule=Host(`${RELYRA_HOST:-relyra.localhost}`)`; set entrypoint `web`; bind that router explicitly to service `relyra-local-demo`; set `traefik.http.services.relyra-local-demo.loadbalancer.server.port=4000`. Attach only `demo_app` to `default` and `proxy`. Declare top-level network `proxy` as external with name `${DEMO_PROXY_NETWORK:-proxy}`. Add no host publication. Do not attach `db`, do not route or modify Keycloak, and do not add `KC_*` settings. Keep the neutral shared-resource identity separate from these Relyra-namespaced participant identifiers. Fleet commands must name only the base and proxy overlay so the automatic solo override is excluded.
  </action>
  <acceptance_criteria>
    - `docker compose -f docker-compose.yml -f docker-compose.proxy.yml config --format json` renders with `.services.db.ports == null` and `.services.demo_app.ports == null`.
    - Only `demo_app` joins rendered network `proxy`; `db` remains on the project default network and has no proxy membership.
    - Rendered labels include enabled discovery, selected Docker network, router rule for default host `relyra.localhost`, entrypoint `web`, explicit router-to-service `relyra-local-demo`, and backend port 4000.
    - Rendered demo environment has `PHX_HOST=relyra.localhost`, `PHX_SCHEME=http`, `PHX_PORT=80`, and an origin list containing the solo and Relyra wildcard values; `RELYRA_HOST` and `DEMO_PROXY_NETWORK` overrides render deterministically.
    - No Keycloak service overlay, label, hostname, proxy-header, or realm change is introduced.
  </acceptance_criteria>
  <verify>
    <automated>fleet_json=$(docker compose -f docker-compose.yml -f docker-compose.proxy.yml config --format json) &amp;&amp; printf '%s' "$fleet_json" | jq -e '.services.db.ports == null and .services.demo_app.ports == null and (.services.demo_app.networks | has("default") and has("proxy")) and (.services.db.networks | has("proxy") | not)' &gt;/dev/null &amp;&amp; printf '%s' "$fleet_json" | jq -e '.services.demo_app.labels["traefik.enable"] == "true" and .services.demo_app.labels["traefik.docker.network"] == "proxy" and .services.demo_app.labels["traefik.http.routers.relyra-local-demo.entrypoints"] == "web" and .services.demo_app.labels["traefik.http.routers.relyra-local-demo.service"] == "relyra-local-demo" and .services.demo_app.labels["traefik.http.services.relyra-local-demo.loadbalancer.server.port"] == "4000"' &gt;/dev/null &amp;&amp; printf '%s' "$fleet_json" | jq -e '.services.demo_app.environment.PHX_HOST == "relyra.localhost" and .services.demo_app.environment.PHX_SCHEME == "http" and .services.demo_app.environment.PHX_PORT == "80"' &gt;/dev/null</automated>
    <runtime_receipt>With Task 1 running, execute `docker compose -f docker-compose.yml -f docker-compose.proxy.yml up -d --wait`; request Relyra via `curl -fsS -H 'Host: relyra.localhost' http://127.0.0.1/` (the Host header avoids relying on browser-only wildcard DNS); confirm `docker compose ... ps` exposes no app/db host port. Start one maintained sibling demo on the same `proxy` network, request both browser hostnames, and verify neither project reports a bind conflict. Stop only the Relyra project; confirm the sibling route, `dev_proxy`, and external `proxy` network remain.</runtime_receipt>
    <human_check>Load the existing operator UI at `http://relyra.localhost`; confirm LiveView stays connected without origin errors, the UI/copy has not changed, and read-only absolute URL fields remain selectable and horizontally scrollable.</human_check>
  </verify>
  <done>Relyra is an independently namespaced participant on the neutral shared proxy, reachable at its configurable hostname without app/database host ports; a sibling route can coexist and remains alive when Relyra stops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local browser → Traefik loopback ingress | Host-based HTTP traffic enters the shared proxy on 127.0.0.1:80. |
| Traefik Docker provider → Docker socket | The pinned local proxy discovers labeled containers through the mounted engine socket. |
| shared `proxy` network → project-private default network | Only `demo_app` spans both networks; PostgreSQL remains private. |
| sibling labels → global Traefik router/service namespace | Multiple independently managed projects contribute routes to one proxy instance. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-04 | Tampering | Cross-project Traefik route namespace | medium | mitigate | Task 2 uses static globally namespaced router/service `relyra-local-demo`, explicit router-service binding, selected Docker network, and backend port (D-12). |
| T-69-06 | Information Disclosure | Traefik web/dashboard publication | medium | mitigate | Task 1 binds both ports to `127.0.0.1`; rendered JSON asserts both host IPs (D-09). |
| T-69-07 | Elevation of Privilege | Docker socket mounted into proxy | medium | mitigate | The locked scoria convention pins `traefik:v3.7.1`, mounts only the required socket read-only, disables exposed-by-default, and runs only as local dev infrastructure (D-09). |
| T-69-08 | Information Disclosure | PostgreSQL reachable from shared network | high | mitigate | Task 2 attaches only `demo_app` to `proxy`; rendered JSON proves `db` has neither proxy membership nor host publication (D-03/D-04). |
| T-69-09 | Spoofing | Unapproved containers auto-routed | medium | mitigate | Task 1 sets `--providers.docker.exposedbydefault=false`; Task 2 explicitly opts in only the Relyra application. |

No package-manager install occurs, the Traefik image pin is already a locked/audited project decision, no ORM/schema path is touched, and no Keycloak/public Relyra security posture is changed.
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Zero-setup solo localhost plus opt-in concurrent sibling fleet | 01-02 | COVERED | Solo tracer then fleet expansion and two-sibling receipt. |
| REQ | FLEET-01 | Bare up, localhost app, no host PostgreSQL | 01 | COVERED | D-01..D-04 rendered + runtime receipt. |
| REQ | FLEET-02 | Neutral shared Traefik, explicit overlay, concurrent sibling routes | 02 | COVERED | D-03/D-09..D-12 and backstop receipt. |
| REQ | FLEET-03 | Correct endpoint URL/origin and LiveView at both hosts | 01-02 | COVERED | D-05..D-07 plus browser receipt. |
| RESEARCH | — | Remove core profiles; exclusive base+solo/base+fleet render paths | 01-02 | COVERED | Tracer proves bare selection; fleet command excludes override. |
| RESEARCH | — | Explicit Traefik network/router/service/backend port | 02 | COVERED | Task 2 labels and JSON assertions. |
| RESEARCH | — | Env parsing in non-test runtime; no broad origin bypass | 01 | COVERED | Task 2 runtime assertion. |
| CONTEXT | D-01 | Portable base loses app/db host ports while preserving Phase-68 graph | 01 | COVERED | Tracer Task 1. |
| CONTEXT | D-02 | Automatic solo overlay publishes only loopback app port | 01 | COVERED | Tracer Task 1. |
| CONTEXT | D-03 | Explicit fleet overlay adds app labels/network/env and no host port | 02 | COVERED | Task 2. |
| CONTEXT | D-04 | PostgreSQL remains unpublished in both paths | 01-02 | COVERED | Both rendered-config gates. |
| CONTEXT | D-05 | Runtime public host/scheme/port plumbing | 01-02 | COVERED | Plan 01 Task 2 and Plan 02 env wiring. |
| CONTEXT | D-06 | Explicit env-driven Phoenix origin allowlist | 01-02 | COVERED | Runtime assertion plus proxy env. |
| CONTEXT | D-07 | FakeIdP Destination/Recipient fixtures remain unchanged | 01 | COVERED | Scope fence and diff gate. |
| CONTEXT | D-08 | Phase 69 routes only demo_app, not Keycloak | 02 | COVERED | Proxy-overlay acceptance criteria. |
| CONTEXT | D-09 | Scoria Traefik project copied verbatim | 02 | COVERED | Task 1 exact analog. |
| CONTEXT | D-10 | Raw proxy commands only; no Makefile | 02 | COVERED | Task 1 runtime receipt and scope gate. |
| CONTEXT | D-11 | Inline defaults; no `.env.example` | 01-02 | COVERED | Both overlays and scope gates. |
| CONTEXT | D-12 | Static `relyra-*` participant namespace | 02 | COVERED | `relyra-local-demo` labels. |
| UI | covered | Loading/error/long-text considerations | 01-02 | COVERED | Lifted as plain must-have truths and human checks; no UI files scheduled. |
| EDGE | FLEET-01 | Unclassified row | 01 | FLAGGED | Explicit unresolved assumption retained. |
| EDGE | FLEET-02/FLEET-03 | Concurrent/interrupted operation | 01-02 | COVERED | Flat-scalar backstop truths plus runtime/browser receipts. |
| PROHIBITION | FLEET-02 | Shared resource must not use Relyra as identity anchor | 02 | FLAGGED | Descriptor-less, unverified prohibition; no fabricated check. |

Canon-referral: generic Docker exposure, CSWSH, injection, and compliance candidates are owned by the ASVS/STRIDE security gate and ordinary correctness checks; they were not duplicated as fallback prohibitions. Routine Compose merge/idempotency candidates are covered by edge truths and verification.

<verification>
- Render the independent proxy project and explicit base+proxy application graph as JSON; assert loopback ingress, external network, app-only dual-network membership, no host app/database ports, and every required namespaced label/env value.
- Execute the raw Phase-69 proxy startup equivalent twice to prove reuse, then start Relyra with only the base+proxy files.
- Request the proxy route with an explicit Host header; inspect published ports; then exercise both browser hostnames with a maintained sibling demo and confirm route/network survival when one participant stops.
- Run `mix qa`, `mix test --warnings-as-errors`, `mix ci.security`, and `mix format --check-formatted`; no `lib/`, fixture, realm, Keycloak, `Makefile`, `.env.example`, guide, or README file may change.
</verification>

<success_criteria>
- FLEET-02: the neutral proxy and external network are reusable, Relyra routes through explicit `relyra-*` labels, and sibling demos coexist without host-port contention.
- FLEET-03: the proxy overlay feeds the runtime URL/origin seam and the unchanged LiveView UI connects at `relyra.localhost`.
- The solo override remains absent from fleet commands and the proxy remains absent from plain solo commands.
- All D-01..D-12 decisions, FLEET-01..03 requirements, research constraints, UI rows, edge rows, and the descriptor-less identity-anchor prohibition are covered or explicitly flagged.
</success_criteria>

<output>
Create `.planning/phases/69-compose-split-fleet-proxy/69-02-SUMMARY.md` when done.
</output>
