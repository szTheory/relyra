---
phase: 70-keycloak-behind-the-proxy
plan: "13"
type: execute
wave: 10
depends_on:
  - 70-10
files_modified:
  - demo/ledger_loop/lib/ledger_loop/demo/reset.ex
  - demo/ledger_loop/test/ledger_loop/demo/reset_test.exs
  - lib/relyra/live_admin/connection_trace_live.ex
  - test/relyra/live_admin/phase15_ui_contract_test.exs
autonomous: true
gap_closure: true
gap_ids: [G-70-2]
requirements:
  - KC-01
must_haves:
  truths:
    - "G-70-2: an opt-in demo reset produces one deterministic, redaction-safe long Login Trace fixture without changing the ordinary FakeIdP reset story."
    - "G-70-2 and D-25: Login Trace preserves native details semantics and exposes its four-column evidence through a labelled, keyboard-focusable horizontal overflow region at narrow widths."
    - "The scoped `ConnectionTraceLive` presentation correction changes no query, export/redaction, parser, crypto, replay, algorithm-policy, audit-write, public API, or behaviour seam."
  artifacts:
    - path: demo/ledger_loop/lib/ledger_loop/demo/reset.ex
      provides: "explicit opt-in safe long-value visual fixture"
    - path: lib/relyra/live_admin/connection_trace_live.ex
      provides: "narrow-width evidence access around the existing four-column trace table"
    - path: test/relyra/live_admin/phase15_ui_contract_test.exs
      provides: "render-contract regression for native details and overflow-region semantics"
  key_links:
    - from: demo/ledger_loop/lib/ledger_loop/demo/reset.ex
      to: lib/relyra/live_admin/connection_trace_live.ex
      via: "AuditWriter row exported by the existing Query/LoginTrace.Export path"
      pattern: "login_trace"
    - from: lib/relyra/live_admin/connection_trace_live.ex
      to: demo/ledger_loop/test/browser/trace_visual.spec.ts
      via: "stable details, step, Back, and overflow-region selectors consumed by Plan 70-14"
      pattern: "data-testid"
---

<objective>
Create the deterministic data and narrowly scoped presentation seam required to automate G-70-2.

Purpose: Browser automation needs safe held-out long values and an operable narrow-width table; neither exists in the current inherited Login Trace surface.
Output: Opt-in visual fixture, responsive evidence region, and ExUnit contract coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
@.planning/phases/70-keycloak-behind-the-proxy/70-UI-SPEC.md
@.planning/phases/70-keycloak-behind-the-proxy/70-UAT.md
@.planning/debug/phase-70-visual-failure-automation-gap.md
@.planning/phases/70-keycloak-behind-the-proxy/70-08-SUMMARY.md
@demo/ledger_loop/lib/ledger_loop/demo/reset.ex
@lib/relyra/live_admin/connection_trace_live.ex
@lib/relyra/login_trace/export.ex
@test/relyra/live_admin/phase15_ui_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add the safe long-trace fixture and narrow-width evidence access end-to-end</name>
  <files>demo/ledger_loop/lib/ledger_loop/demo/reset.ex, demo/ledger_loop/test/ledger_loop/demo/reset_test.exs, lib/relyra/live_admin/connection_trace_live.ex, test/relyra/live_admin/phase15_ui_contract_test.exs</files>
  <behavior>
    - "`LedgerLoop.Demo.Reset.reset!/0` retains its current four scenarios and seed output when `DEMO_TRACE_VISUAL_FIXTURE` is absent."
    - "With `DEMO_TRACE_VISUAL_FIXTURE=1`, reset appends one canonical failed login audit row through `AuditWriter` containing a safe long cause and a safe long known-step error code; the export hashes correlation and exposes no XML, PEM, credential, or assertion data."
    - "Every trace table is nested in a labelled focusable overflow region with a stable test id, while the enclosing native details and Back link remain unchanged."
  </behavior>
  <action>
    Start with failing assertions in the two named test files. In `LedgerLoop.Demo.Reset`, gate one additional held-out support trace on the exact opt-in value `DEMO_TRACE_VISUAL_FIXTURE=1`. Append a single `domain: :login`, `action: :failed` canonical audit event via `Relyra.Ecto.AuditWriter` using the existing support connection record id, a deliberately long but non-sensitive cause, a deliberately long non-sensitive error code on a known exported step, and an innocuous correlation value that the existing export hashes. Keep the default reset byte-for-behavior compatible and do not seed raw XML, SAML payloads, PEMs, credentials, or arbitrary unknown step names. Test both the default absence and exact opt-in shape.

    Then make the only permitted `lib/relyra/**` correction: in `ConnectionTraceLive.render/1`, wrap the inherited fixed four-column table in a region with horizontal overflow, a stable `data-testid`, keyboard focus (`tabindex="0"`), and an accessible label describing the step evidence. Preserve the table, native open `<details>/<summary>`, stable row/step selectors, Back link, copy, query, and export path. Do not alter any trust/security logic or introduce a component system. Extend `phase15_ui_contract_test.exs` to prove the semantic wrapper and native details contract around long safe values. This narrowly scoped presentation edit is demonstrably required by G-70-2 because the current fixed table has no accessible narrow-viewport overflow treatment.
  </action>
  <verify>
    <automated>mix cmd --cd demo/ledger_loop mix test test/ledger_loop/demo/reset_test.exs --warnings-as-errors &amp;&amp; mix test test/relyra/live_admin/phase15_ui_contract_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>Opt-in reset yields safe long rendered evidence and the same Login Trace presents its table through a semantic, focusable overflow region without changing any security seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo seed → audit storage | Held-out fixture data enters the same persisted login-audit shape rendered to operators. |
| Audit export → LiveAdmin browser | Security evidence crosses redaction/export and authenticated admin presentation boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-39 | Information disclosure | long-value fixture | high | mitigate | Allow only synthetic cause/error-code/correlation data, route through AuditWriter and existing Export redaction, and test absence of protected material. |
| T-70-40 | Tampering | ordinary reset story | medium | mitigate | Exact opt-in environment gate plus tests proving default seed behavior is unchanged. |
| T-70-41 | Denial of service | narrow trace table | medium | mitigate | Bound evidence in a horizontal overflow region so long values do not force page-wide overflow or hide columns. |
| T-70-42 | Elevation of privilege | LiveAdmin presentation edit | high | mitigate | Change render markup only; preserve authenticated router, query scope, export, and every trust/security seam. |
</threat_model>

<verification>
Run the focused demo reset tests, root LiveAdmin render-contract tests, formatting, and inspect the diff to confirm the sole library change is table presentation markup.
</verification>

<success_criteria>
G-70-2 has a safe deterministic long-value fixture and an operable narrow-width Login Trace contract ready for credential-safe browser automation.
</success_criteria>

<output>
Create `.planning/phases/70-keycloak-behind-the-proxy/70-13-SUMMARY.md` when done.
</output>
