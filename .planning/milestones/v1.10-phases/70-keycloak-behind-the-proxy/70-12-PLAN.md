---
phase: 70-keycloak-behind-the-proxy
plan: "12"
type: execute
wave: 10
depends_on:
  - 70-10
files_modified:
  - mix.lock
autonomous: true
gap_closure: true
gap_ids: [G-70-1]
requirements:
  - KC-01
must_haves:
  truths:
    - "G-70-1: the root lock resolves Req and its Finch/Mint transport graph to versions accepted by the current Hex advisory database."
    - "No dependency advisory is ignored or suppressed, and `mix ci.security` remains unchanged."
    - "The dependency-only change does not modify `mix.exs`, the Hex package whitelist, public APIs, or any `lib/relyra/**` source."
  artifacts:
    - path: mix.lock
      provides: "solver-produced patched Req/Finch/Mint resolution"
  key_links:
    - from: mix.exs
      to: mix.lock
      via: "existing optional `{:req, \"~> 0.5\"}` constraint resolved by `mix deps.update req`"
      pattern: "req"
    - from: mix.lock
      to: .github/workflows/security-gates.yml
      via: "`mix ci.security` dependency audit reads the committed lock"
      pattern: "ci.security"
---

<objective>
Close the dependency-security half of G-70-1 as an independent lockfile remediation.

Purpose: The focused Keycloak proof must not hide repository advisories, and the repository security lane must return green on a genuinely patched dependency graph.
Output: A solver-generated root `mix.lock` update with clean audit and regression evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/70-keycloak-behind-the-proxy/70-UAT.md
@.planning/debug/phase-70-keycloak-gate-not-green.md
@mix.exs
@mix.lock
@demo/ledger_loop/mix.lock
@.github/workflows/security-gates.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Update the advisory-vulnerable Req transport graph and prove repository gates</name>
  <files>mix.lock</files>
  <behavior>
    - "The root dependency audit no longer reports the locked Req 0.5.18 or Mint 1.8.0 advisories."
    - "Req-dependent tests, the warnings-as-errors suite, QA, and every isolated `mix ci.security` process pass with the new resolution."
    - "Only the root lockfile changes; dependency declarations, package files, public contracts, and security code remain byte-identical."
  </behavior>
  <action>
    Run `mix deps.update req` from the repository root so Hex solves the existing direct optional Req constraint and its Finch/Mint graph to current compatible stable releases. Use the solver-produced lock entries; do not hand-edit checksums, broaden the `mix.exs` constraint, add packages, or copy the demo lock wholesale. Confirm the resulting Req and Mint versions are outside every currently reported affected range and that Finch resolves compatibly. Run `mix deps.audit` without ignore flags, then the focused Req consumers plus full repository warnings, QA, security, and formatting gates. If the solver reveals a real compatibility failure, fix only the lock resolution supported by the existing constraint; do not suppress the advisory or weaken a test. Keep this commit independent from Plan 70-11 because the focused Keycloak scenario is already behavior-green and security CI owns repository advisories.
  </action>
  <verify>
    <automated>mix deps.audit &amp;&amp; mix test --warnings-as-errors &amp;&amp; mix qa &amp;&amp; mix ci.security &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>The root lock contains a patched Req/Finch/Mint resolution, the unmodified dependency audit accepts it, and all repository security/quality gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex registry/solver → committed lock | External package metadata and checksums determine the local dependency graph. |
| HTTP client graph → metadata/bootstrap callers | Req, Finch, and Mint process remote or container-local HTTP data used by existing integrations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-35 | Tampering | `mix.lock` | high | mitigate | Generate via Hex solver, retain registry checksums, and reject hand-edited lock entries. |
| T-70-36 | Elevation of privilege | advisory suppression | high | mitigate | Run `mix deps.audit` with no new ignore and leave `mix ci.security` unchanged. |
| T-70-37 | Denial of service | Req/Finch/Mint compatibility | medium | mitigate | Run full warnings, QA, isolated security, and formatting gates against the resolved graph. |
| T-70-38 | Information disclosure | HTTP client regression | medium | mitigate | Preserve existing strict callers and verify all repository tests; no request/logging behavior changes. |
</threat_model>

<verification>
Inspect the `mix.lock` diff, confirm it is solver-limited to Req and required transitive updates, then run the unsuppressed audit and all repository gates.
</verification>

<success_criteria>
The repository-wide security lane is green because the vulnerable lock resolution was replaced, not because advisories were ignored or coupled to a scenario command.
</success_criteria>

<output>
Create `.planning/phases/70-keycloak-behind-the-proxy/70-12-SUMMARY.md` when done.
</output>
