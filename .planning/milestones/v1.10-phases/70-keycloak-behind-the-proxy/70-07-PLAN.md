---
phase: 70-keycloak-behind-the-proxy
plan: "07"
type: execute
wave: 6
depends_on:
  - 70-06
files_modified:
  - playwright.keycloak-proxy.config.mjs
  - scripts/test_keycloak_proxy_e2e.sh
autonomous: true
gap_closure: true
requirements:
  - KC-01
must_haves:
  truths:
    - "D-26: Keycloak Playwright trace, video, screenshot, snapshot, request body, and form-data capture are disabled for the credential-bearing project rather than sanitized after collection."
    - "D-26: browser output is isolated in an exact per-run temporary directory and removed before retained diagnostics are assembled; only validated, redacted, allowlisted text logs can be promoted for retention."
    - "Failed diagnostics contain no Sarah password, raw SAMLResponse/assertion, descriptor XML, PEM, authorization/cookie value, or database credential; policy uncertainty deletes the candidate diagnostics and emits only the named failure layer."
    - "D-17..D-25: the existing optional-link → public Keycloak → scoped ACS → exact receipt → canonical three-step trace journey is unchanged, and a failure cannot leave a false-success UI claim."
    - "The FakeIdP path remains first, deterministic, independently runnable, and protected by its success/tamper plus root security gates."
    - { statement: "A route/browser check must confirm a failed login destination cannot leave a false-success state and exposes a recoverable typed or transport failure.", verification: backstop }
    - { statement: "A narrow-viewport visual check must confirm trace cards and their four-column step table remain operable and no evidence becomes inaccessible when content exceeds the inherited container.", verification: backstop }
    - { statement: "A held-out visual check with long correlation, cause, step, and error-code values must confirm wrapping or horizontal access without hiding security evidence.", verification: backstop }
  artifacts:
    - path: playwright.keycloak-proxy.config.mjs
      provides: "credential-bearing Playwright project with every sensitive attachment channel disabled"
      contains: "trace: \"off\""
    - path: scripts/test_keycloak_proxy_e2e.sh
      provides: "ephemeral browser-output lifecycle and fail-closed allowlisted diagnostic retention"
      contains: "KEYCLOAK_PROXY_ARTIFACT_POLICY_SELF_TEST"
  key_links:
    - from: scripts/test_keycloak_proxy_e2e.sh
      to: playwright.keycloak-proxy.config.mjs
      via: "an exact mktemp-owned output directory is passed only for the current run and removed by the trap"
      pattern: "PLAYWRIGHT.*TMP|KEYCLOAK_PROXY_PLAYWRIGHT"
    - from: playwright.keycloak-proxy.config.mjs
      to: demo/ledger_loop/test/browser/keycloak.spec.ts
      via: "the selected credential-bearing spec runs with trace/video/screenshot capture disabled"
      pattern: "keycloak.spec.ts|trace|video|screenshot"
    - from: scripts/test_keycloak_proxy_e2e.sh
      to: retained diagnostic directory
      via: "redact into staging, validate the exact filename/content policy, then promote or discard all"
      pattern: "redact_diagnostics|validate.*diagnostic|capture_diagnostics"
  prohibitions:
    - statement: "MUST NOT retain any Playwright trace, video, screenshot, snapshot, HAR, request body, form data, or other browser artifact from the credential-bearing Keycloak flow."
      status: unresolved
      verification: null
    - statement: "MUST NOT promote any diagnostic file except the validated and redacted container-state.log, relyra.log, and audit-actions.log allowlist."
      status: unresolved
      verification: null
    - statement: "MUST NOT partially retain an uncertain diagnostic candidate; any filename, content, capture, validation, or promotion uncertainty discards the whole candidate tree."
      status: unresolved
      verification: null
    - statement: "MUST NOT leave or claim a false-success UI state when the optional Keycloak login destination or browser journey fails."
      status: unresolved
      verification: null
  flagged_assumptions:
    - id: EDGE-KC-01-BOUNDARY
      status: inapplicable
      statement: "This closure introduces no numeric product threshold; the relevant boundary is categorical and fail-closed: approved text file versus any other artifact, with zero sensitive matches allowed."
    - id: EDGE-KC-01-PRECISION
      status: inapplicable
      statement: "This closure performs no arithmetic or rounding; filename allowlists and sensitive-token/content checks are exact boolean policy decisions."
---

<objective>
Close the credential/assertion disclosure blocker with a zero-sensitive-browser-artifact policy and executable fail-closed retention tests.

Purpose: A failed real-Keycloak proof must remain actionable without preserving any browser evidence capable of containing Sarah's password or the genuine SAML response.
Output: Attachment-disabled Playwright configuration, ephemeral browser output, validated text-only diagnostics, and a deterministic artifact-policy self-test.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
@.planning/phases/70-keycloak-behind-the-proxy/70-UI-SPEC.md
@.planning/phases/70-keycloak-behind-the-proxy/70-VERIFICATION.md
@.planning/phases/70-keycloak-behind-the-proxy/70-03-SUMMARY.md
@.planning/phases/70-keycloak-behind-the-proxy/70-05-SUMMARY.md
</context>

<assumption_delta_decision>
Primary noun: SAML connection, identified by stable `connection_id`.
Decision: no-change — optional Keycloak is already an independent connection variant beside FakeIdP; the artifact-policy repair does not alter identity or selection modeling.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Disable sensitive browser capture and isolate all Playwright output ephemerally</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-UI-SPEC.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-VERIFICATION.md
    - playwright.keycloak-proxy.config.mjs
    - scripts/test_keycloak_proxy_e2e.sh
    - demo/ledger_loop/test/browser/keycloak.spec.ts
  </read_first>
  <files>playwright.keycloak-proxy.config.mjs, scripts/test_keycloak_proxy_e2e.sh</files>
  <behavior>
    - "Imported Playwright config reports trace, video, and screenshot modes off for the Keycloak project, with list-only reporter output and no HTML/blob attachment reporter."
    - "The harness creates one exact mktemp directory for Playwright output, passes it to the config, and removes it before collecting retained diagnostics on success, ordinary failure, interrupt, or termination."
    - "A forced browser-layer failure leaves no trace ZIP, video, screenshot, snapshot, HAR, request-body, form-data, or other browser attachment under the retained diagnostic path."
  </behavior>
  <action>
    Start RED by extending the harness's deterministic policy self-test mode to import the Playwright config and fail unless trace, video, and screenshot capture are all disabled and only the non-persisting list reporter is active. Create representative forbidden browser attachment files beneath a harness-owned `mktemp -d` path, invoke the same cleanup function used by the real trap, and assert the exact temporary directory is absent afterward and nothing was copied beneath `ARTIFACT_DIR`.

    In GREEN, set the credential-bearing config's `use.trace`, `use.video`, and `use.screenshot` modes to `off`; retain one worker, the exact Keycloak spec selector, host resolver rules, and list reporter. Remove the retained Playwright artifact directory contract. In the shell harness, create a uniquely owned Playwright temp directory with `mktemp -d`, pass only that exact path through a renamed temporary-output environment variable, and register cleanup before the browser can launch. Remove that directory before any diagnostic capture and on every trap path, validating its ownership/prefix before recursive deletion. Do not store the temp directory beneath the retained diagnostic directory, add an HTML/blob reporter, log form values or response bodies, or alter the browser journey/UI contract.
  </action>
  <verify>
    <automated>bash -n scripts/test_keycloak_proxy_e2e.sh &amp;&amp; KEYCLOAK_PROXY_ARTIFACT_POLICY_SELF_TEST=1 bash scripts/test_keycloak_proxy_e2e.sh &amp;&amp; npx playwright test --config=playwright.keycloak-proxy.config.mjs --list</automated>
  </verify>
  <acceptance_criteria>
    - Trace, video, screenshot, snapshot, request/form payload, HAR, and attachment retention are impossible through the Keycloak Playwright configuration used by the harness.
    - Browser output exists only in one validated per-run temporary directory and is removed before retained diagnostics on every normal trap path.
    - The public-host mapping, single selected Keycloak spec, one-worker execution, and exact user-visible proof remain unchanged.
  </acceptance_criteria>
  <done>The credential-bearing browser run has no persistent browser-evidence channel, including on failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retain only policy-validated redacted text diagnostics and rerun security regressions</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-VERIFICATION.md
    - scripts/test_keycloak_proxy_e2e.sh
    - playwright.keycloak-proxy.config.mjs
    - demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs
    - test/security/xml/adversarial_crypto_test.exs
  </read_first>
  <files>scripts/test_keycloak_proxy_e2e.sh</files>
  <behavior>
    - "Failure capture writes into a private staging directory, redacts streams into exactly the approved text filenames, validates filenames and content, and promotes the directory only after all checks pass."
    - "Sentinels representing password/form data, encoded SAMLResponse/request bodies, raw Response/Assertion/EntityDescriptor XML, certificate/private-key PEM, Authorization/Cookie values, and PostgreSQL credentials never survive retained output."
    - "An unknown filename, binary/attachment extension, failed redaction validation, or sensitive match discards the entire staging tree and leaves only the named failure layer on stderr."
    - "FakeIdP success/tamper, focused provisioner tests, warnings-as-errors, QA, security subprocesses, and formatting remain blocking after the policy change."
  </behavior>
  <action>
    Expand `KEYCLOAK_PROXY_ARTIFACT_POLICY_SELF_TEST=1` into an offline, Docker-free policy proof. Feed unique generated sentinels for credentials, form fields, authorization/cookie headers, raw SAML response/request payloads, assertion/descriptor XML, PEM material, and database URLs through the production redactor. Exercise a clean staged diagnostic set and prove only `container-state.log`, `relyra.log`, and `audit-actions.log` are promoted with every sentinel absent. Then exercise an unknown file, representative browser/binary attachment names, and deliberately unredacted sensitive content; each case must reject and remove the entire candidate retained tree.

    Refactor `capture_diagnostics` to capture only the current harness-owned Compose state/logs and audit action names into a fresh private staging directory, sanitize during write, then validate both the exact filename allowlist and all file contents before atomically moving the staging directory to `ARTIFACT_DIR`. If capture, validation, or promotion cannot establish safety, delete staging and any partial destination and print only `layer=<name> status=failed diagnostics=discarded` without the unsafe content. Keep actionable named layers and redacted container/audit vocabulary, but never retain descriptor bodies, PEMs, assertions, request bodies, test credentials, environment dumps, DB connection strings, or browser files. Finally run the existing FakeIdP-first and permanent repository gates unchanged; do not weaken the adversarial corpus, crypto policy, assertion `KeyInfo` rejection, or `mix ci.security` subprocess topology.
  </action>
  <verify>
    <automated>KEYCLOAK_PROXY_ARTIFACT_POLICY_SELF_TEST=1 bash scripts/test_keycloak_proxy_e2e.sh &amp;&amp; KEYCLOAK_PROXY_STATIC_ONLY=1 bash scripts/test_keycloak_proxy_e2e.sh &amp;&amp; cd demo/ledger_loop &amp;&amp; mix test test/ledger_loop/demo/keycloak_provisioner_test.exs test/ledger_loop_web/fake_idp_flow_test.exs --warnings-as-errors &amp;&amp; cd ../.. &amp;&amp; mix test --warnings-as-errors &amp;&amp; mix ci.security &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - Only the three approved redacted `.log` files can be retained; any browser/binary/unknown file or sensitive content causes whole-candidate deletion rather than partial retention.
    - The self-test explicitly covers credentials/form data, SAML request/response bodies, raw assertion/descriptor XML, PEM, authorization/cookie values, database credentials, and browser trace/video/screenshot/snapshot/HAR attachments.
    - Named failure layers remain actionable without false-success UI state or secret-bearing evidence.
    - FakeIdP typed tamper rejection and all root security/formatting gates remain unchanged and green.
  </acceptance_criteria>
  <done>Failed E2E diagnostics are text-only, allowlisted, redacted, validated before retention, and fail closed on every uncertain artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser automation → Keycloak/ACS | Sarah's credential and a genuine signed SAMLResponse cross the automated browser. |
| Playwright process → filesystem | Test runners can persist snapshots, network/request payloads, screenshots, videos, and traces unless explicitly disabled. |
| Docker/BEAM diagnostics → retained directory | Container logs can contain descriptor, PEM, assertion, header, environment, or database material. |
| descriptor candidate → configured trust | The parallel closure plan must preserve one-parse audited trust and configured-certificate-only assertion verification. |
| optional Keycloak proof → FakeIdP/security suite | Artifact hardening must not create a false green by skipping existing gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-29 | Information disclosure | Playwright trace/video/screenshot/network snapshots | high | mitigate | Disable all attachment modes, isolate output in validated mktemp storage, and remove it before retained diagnostics. |
| T-70-30 | Information disclosure | staged text diagnostics | high | mitigate | Redact into private staging, enforce exact text filename/content policy, promote only after validation, and discard all on uncertainty. |
| T-70-31 | Tampering | descriptor double-parse/parser differential | high | mitigate | Depend on 70-06's one-candidate architecture and rerun the focused provisioner suite; make no parser or `lib/` change here. |
| T-70-32 | Repudiation | unaudited trust mutation | high | mitigate | Make no trust mutation change; focused provisioner tests continue asserting connection/metadata/certificate audit evidence. |
| T-70-33 | Spoofing | assertion document `KeyInfo` | high | mitigate | Preserve configured-certificate verification and run unchanged `mix ci.security`; no browser artifact rule changes trust evaluation. |
| T-70-34 | Tampering | FakeIdP/permanent security regression gates | high | mitigate | Keep FakeIdP-first typed tamper coverage and every `mix ci.security` subprocess blocking after artifact cleanup. |
</threat_model>

## Artifacts this phase produces

- Playwright Keycloak project policy with `trace`, `video`, and `screenshot` capture disabled and no persistent attachment reporter.
- Harness-private `cleanup_playwright_tmp` helper for one validated `mktemp -d` browser output tree.
- Harness-private staged diagnostic capture/validation/promotion helpers with an exact three-file text allowlist and fail-closed discard behavior.
- `KEYCLOAK_PROXY_ARTIFACT_POLICY_SELF_TEST=1` offline policy test mode covering browser attachments and every forbidden sensitive content class.
- No new file path, UI component/style, external API matrix, database schema, dependency, or package artifact.

<source_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Real Keycloak round trip succeeds without compromising the verified trust path | 70-06,70-07 | COVERED | Trust parse and failure evidence blockers both close. |
| REQ | KC-01 | Optional proxy Keycloak completes a verified SAML login | 70-06,70-07 | COVERED | Same requirement, no added product scope. |
| RESEARCH | — | Existing metadata/audit seams and host-side browser harness remain authoritative | 70-06,70-07 | COVERED | No package or external API integration added. |
| CONTEXT | D-03,D-05,D-06 | Descriptor trust is generated, audited, idempotent, and fail-closed | 70-06 | COVERED | One canonical candidate. |
| CONTEXT | D-17..D-22 | Exact browser/ACS/receipt/trace proof plus independent FakeIdP lane | executed 70-05, protected by 70-07 | COVERED | Journey unchanged; retention removed. |
| CONTEXT | D-23..D-26 | Honest optional UI, exact receipt, accessible inherited UI, safe diagnostics | executed 70-04/05, protected by 70-07 | COVERED | Three UI backstops remain explicit; no restyle. |
| UI | covered states | Native navigation, wrapping/reflow, trace empty/error/populated/partial/0-1-many behavior | executed 70-04/05, protected by 70-07 | COVERED | Closure changes no UI; failed destination must not leave false success. |
| UI | backstops | Failed destination, narrow trace table, long trace evidence | 70-07 | COVERED | All three retained as structured backstop markers. |

All other Phase 70 decisions remain satisfied by executed Plans 70-01 through 70-05. Deferred launcher/docs work, inherited Login Trace restyling, cookie-session redesign, and discretionary genuine-response replay remain excluded.
</source_audit>

<api_coverage_decision>
Deterministic gap-scope detector result: `detected=false`; this plan changes local Playwright and shell diagnostic policy and introduces no external API/SDK capability surface, so no COVERAGE.md is required.
</api_coverage_decision>

<verification>
`KEYCLOAK_PROXY_ARTIFACT_POLICY_SELF_TEST=1 bash scripts/test_keycloak_proxy_e2e.sh &amp;&amp; KEYCLOAK_PROXY_STATIC_ONLY=1 bash scripts/test_keycloak_proxy_e2e.sh &amp;&amp; npx playwright test --config=playwright.keycloak-proxy.config.mjs --list &amp;&amp; cd demo/ledger_loop &amp;&amp; mix test test/ledger_loop/demo/keycloak_provisioner_test.exs test/ledger_loop_web/fake_idp_flow_test.exs --warnings-as-errors &amp;&amp; cd ../.. &amp;&amp; mix test --warnings-as-errors &amp;&amp; mix ci.security &amp;&amp; mix format --check-formatted`; after 70-06 is complete, run `npm run demo:keycloak-proxy` for the owned live proof.
</verification>

<success_criteria>
A credential-bearing Keycloak failure retains no browser artifact or sensitive diagnostic class, unsafe candidate evidence is discarded as a unit, named failure layers remain actionable, the exact user-facing proof is unchanged, and FakeIdP plus permanent security gates remain blocking.
</success_criteria>

<output>
Create `.planning/phases/70-keycloak-behind-the-proxy/70-07-SUMMARY.md` when done.
</output>
