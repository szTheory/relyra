---
phase: 70-keycloak-behind-the-proxy
plan: "05"
type: execute
wave: 4
depends_on:
  - 70-03
  - 70-04
files_modified:
  - demo/ledger_loop/test/browser/keycloak.spec.ts
  - playwright.keycloak-proxy.config.mjs
  - scripts/test_keycloak_proxy_e2e.sh
autonomous: true
requirements:
  - KC-01
must_haves:
  truths:
    - "D-17 D-18: host-side Playwright begins at the conditional semantic link, visits the public Keycloak origin, authenticates Sarah, observes the exact stable ACS POST/redirect, and lands on the workspace."
    - "D-19 D-20 D-24: the journey asserts exact visible receipt copy, the newest correlation-specific Login Trace contains validation, signature, and replay success, and the workspace plus durable receipt separately prove host mapping/session establishment."
    - "D-21 D-22: the proof makes no cookie-session claim, never mutates browser XML, and leaves the existing FakeIdP success/tamper lane independently green."
    - "D-26: failed E2E evidence is actionable and redacted, and the browser test never queries Postgres."
  artifacts:
    - path: demo/ledger_loop/test/browser/keycloak.spec.ts
      provides: "full public-host Keycloak ACS, receipt, and correlation-specific trace proof"
      contains: "Test with Keycloak (optional real IdP)"
    - path: playwright.keycloak-proxy.config.mjs
      provides: "single-spec host resolver and retained redacted browser artifacts"
      contains: "keycloak.relyra.localhost"
    - path: scripts/test_keycloak_proxy_e2e.sh
      provides: "phase gate combining browser, focused Ecto, FakeIdP, and repo security checks"
      contains: "fake_idp_flow_test.exs"
  key_links:
    - from: demo/ledger_loop/test/browser/keycloak.spec.ts
      to: demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex
      via: "accessible link name starts the supported optional journey"
      pattern: "Test with Keycloak"
    - from: demo/ledger_loop/test/browser/keycloak.spec.ts
      to: lib/relyra/live_admin/connection_trace_live.ex
      via: "admin-login session then stable connection trace route and correlation-specific verifier-step assertions"
      pattern: "login/admin|trace|correlation"
    - from: scripts/test_keycloak_proxy_e2e.sh
      to: demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs
      via: "independent deterministic success/tamper regression command after Keycloak profile proof"
      pattern: "fake_idp_flow_test.exs"
  prohibitions:
    - requirement_id: KC-01
      category: safety
      status: unresolved
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT treat a redirect away from Keycloak, a structurally shaped response, or color-only success UI as proof that cryptographic verification, replay checking, mapping, and receipt establishment completed."
  flagged_assumptions:
    - id: EDGE-KC-01-BOUNDARY
      status: flagged-unverified
      statement: "No numeric threshold contract exists; the browser boundary is exact request method/path/status and exact zero/one receipt semantics."
    - id: EDGE-KC-01-PRECISION
      status: flagged-unverified
      statement: "No arithmetic precision applies; exact origins, ACS URL, correlation identity, trace step set, and receipt sentence are asserted."
---

<objective>
Complete KC-01 with evaluator-grade browser evidence and preserve every existing security/regression gate.

Purpose: The final proof must demonstrate the actual trust outcome, not merely a redirect, while leaving FakeIdP tamper evidence and the permanent adversarial crypto corpus untouched.
Output: Exact host-side Playwright assertions, correlation-specific Login Trace evidence, redacted artifacts, and the complete Phase 70 gate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
@.planning/phases/70-keycloak-behind-the-proxy/70-RESEARCH.md
@.planning/phases/70-keycloak-behind-the-proxy/70-PATTERNS.md
@.planning/phases/70-keycloak-behind-the-proxy/70-03-SUMMARY.md
@.planning/phases/70-keycloak-behind-the-proxy/70-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace redirect smoke coverage with exact ACS, receipt, and Login Trace proof</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
    - demo/ledger_loop/test/browser/keycloak.spec.ts
    - demo/ledger_loop/test/browser/fake_idp.spec.ts
    - playwright.keycloak-proxy.config.mjs
    - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex
    - demo/ledger_loop/lib/ledger_loop_web/controllers/page_html/home.html.heex
    - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex
    - lib/relyra/live_admin/connection_trace_live.ex
    - lib/relyra/telemetry/handlers/login_trace.ex
  </read_first>
  <files>demo/ledger_loop/test/browser/keycloak.spec.ts, playwright.keycloak-proxy.config.mjs</files>
  <behavior>
    - "Accessible initiation → public Keycloak origin → Sarah credentials → exact POST scoped ACS with successful redirect → relyra.localhost workspace."
    - "Workspace shows the exact receipt sentence; admin-login then stable connection trace shows one newest correlation containing validation, signature verification, and replay success."
    - "Spec contains no obsolete setup/global-auth routes, admin/admin persona, ambiguous selector fallback, XML mutation, direct database query, or credential logging."
  </behavior>
  <action>
    Replace the tracer's direct-login browser path with the full locked D-17 through D-21 journey. Begin at `/login/test`; locate the native link by role/name `Test with Keycloak (optional real IdP)`; assert the next origin is exactly `http://keycloak.relyra.localhost`; fill Keycloak `#username`, `#password`, and submit `#kc-login` using the dedicated Sarah realm fixture. Before submit, register `page.waitForResponse` for method `POST` and exact pathname `/saml/01H0B4Y1A2B3C4D5E6F7G8H9J4/acs`; assert its successful redirect status and final `http://relyra.localhost/` workspace. Assert `#workspace-title` and exact D-24 receipt sentence as the host mapping/session-establishment proof. Continue through `/login/admin`, then `/relyra/admin/connections/01H0B4Y1A2B3C4D5E6F7G8H9J4/trace`; identify the newest successful correlation row rather than the pre-seeded support-failure trace and assert `Validate response`, `Verify signature`, and `Replay check` succeed under that correlation. Do not synthesize pre-consume decode or post-consume mapper/session events into that row. Update Playwright host rules to map both `relyra.localhost` and `keycloak.relyra.localhost`, select only `keycloak.spec.ts`, run one worker, and retain trace/screenshot/video only under the harness-owned redacted artifact directory. Test code must not print form values or response bodies and must not query the database.
  </action>
  <verify>
    <automated>npx playwright test --config=playwright.keycloak-proxy.config.mjs --list &amp;&amp; npm run demo:keycloak-proxy</automated>
  </verify>
  <acceptance_criteria>
    - Test begins from the exact optional link and asserts exact public origins plus exact ACS POST path/method/status.
    - Workspace exact receipt sentence is visible after return.
    - The newest matching trace correlation contains validation, signature verification, and replay success and is not the seeded support-failure trace; workspace/receipt evidence separately proves mapping and session establishment.
    - Browser code has no database access, XML mutation, response-body logging, ambiguous selector chain, or cookie-session assertion.
  </acceptance_criteria>
  <done>The browser proof demonstrates the entire verified trust→mapping→receipt→trace chain rather than redirect structure.</done>
</task>

<task type="auto">
  <name>Task 2: Run the independent FakeIdP and permanent repository security gates</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
    - scripts/test_keycloak_proxy_e2e.sh
    - demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs
    - test/security/xml/adversarial_crypto_test.exs
    - mix.exs
  </read_first>
  <files>scripts/test_keycloak_proxy_e2e.sh</files>
  <action>
    Per D-22 and D-26, add post-browser phase-gate commands to the harness (or its documented final verification sequence) for `cd demo/ledger_loop && mix test test/ledger_loop_web/fake_idp_flow_test.exs --warnings-as-errors`, the focused Keycloak provisioner/controller tests, root `mix test --warnings-as-errors`, `mix qa`, `mix ci.security`, and `mix format --check-formatted`. Run the focused FakeIdP check first while the Keycloak profile is absent to prove ordinary reset/boot does not depend on Keycloak, then run the required Keycloak E2E and permanent root gates. Require `mix qa` to exit 0; if it emits the already-recorded mint 1.8.0/req 0.5.18 advisory without failing, report that advisory distinctly but do not suppress or override its exit status. Do not edit or weaken `test/security/xml/adversarial_crypto_test.exs`, collapse `mix ci.security` subprocesses, add a real-Keycloak negative by mutating XML, or change dependencies in this phase.
  </action>
  <verify>
    <automated>cd demo/ledger_loop &amp;&amp; mix test test/ledger_loop_web/fake_idp_flow_test.exs --warnings-as-errors &amp;&amp; cd ../.. &amp;&amp; npm run demo:keycloak-proxy &amp;&amp; mix test --warnings-as-errors &amp;&amp; mix qa &amp;&amp; mix ci.security &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - FakeIdP success and typed digest-tamper rejection pass after the Keycloak profile is absent.
    - Root warnings-as-errors, each `mix ci.security` subprocess suite, and formatting pass without modifying the adversarial corpus.
    - `mix qa` exits 0; any non-failing mint 1.8.0/req 0.5.18 advisory is reported distinctly rather than hidden or fixed out of scope.
  </acceptance_criteria>
  <done>KC-01 proof is complete while deterministic FakeIdP, adversarial crypto, replay, and repository quality gates remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser automation → Keycloak/ACS | Test credentials and genuine assertion traverse public proxy routes. |
| telemetry/audit → Login Trace UI | Correlation-specific security evidence must be selected without confusing seeded failures. |
| Keycloak profile → deterministic regression suite | Optional infrastructure must not contaminate FakeIdP or permanent crypto gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-19 | Spoofing | Redirect-only browser test | high | mitigate | Exact public origin, ACS method/path/status, durable receipt copy, and correlation-specific verifier-step assertions. |
| T-70-20 | Repudiation | Seeded versus genuine trace | high | mitigate | Select newest stable-connection correlation and assert all required successful steps under one ID. |
| T-70-21 | Information disclosure | Playwright artifacts | high | mitigate | No response bodies/form values; harness-owned redacted artifact paths and failure-only retention. |
| T-70-22 | Tampering | FakeIdP/adversarial regressions | high | mitigate | Run independent FakeIdP success/tamper and unchanged `mix ci.security`; never edit permanent corpus. |
| T-70-23 | Elevation of privilege | Receipt versus authorization | high | mitigate | Exact receipt-only assertion and no cookie/authenticated-session check or behaviour change. |
</threat_model>

## Artifacts this phase produces

- Final `keycloak.spec.ts` selectors/assertions: optional link, `#username`, `#password`, `#kc-login`, exact stable ACS response, `#workspace-title`, exact receipt sentence, and correlation-specific validation/signature/replay trace.
- Final Playwright project mapping `relyra.localhost` and `keycloak.relyra.localhost`, one Keycloak spec, one worker, redacted failure artifacts.
- Harness phase-gate stages for Keycloak E2E, focused provisioner/UI tests, independent FakeIdP regression, warnings-as-errors, `mix ci.security`, formatting, and separately reported `mix qa` advisory state.

<source_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Optional Keycloak behind shared proxy completes a real signed SAML round trip | 01-05 | COVERED | Tracer first; robustness, topology, UI, and final evidence expand it. |
| REQ | KC-01 | Public Keycloak host, public realm/SP URLs, genuine verified login/session evidence | 01-05 | COVERED | Every plan carries KC-01. |
| RESEARCH | — | Fixed full hostname + xforwarded, management 9000 internal, 8080 private route | 01,03 | COVERED | Static and live assertions. |
| RESEARCH | — | Descriptor-derived audited trust, enable-last, generated-key reconciliation | 01,02 | COVERED | Existing Relyra seams only. |
| RESEARCH | — | Host-side browser, exact ACS/receipt/trace, redacted diagnostics | 01,03,05 | COVERED | No container browser or redirect-only proof. |
| CONTEXT | D-01..D-07 | Separate idempotent audited Keycloak provisioning and Sarah identity | 01,02 | COVERED | FakeIdP fixtures untouched. |
| CONTEXT | D-08..D-16 | Proxy-only split-horizon topology and fresh realm import | 01,03 | COVERED | One host input; no direct/management exposure. |
| CONTEXT | D-17..D-22 | Genuine browser/receipt/trace proof and independent FakeIdP negative lane | 01,04,05 | COVERED | Browser does not query DB or mutate XML. |
| CONTEXT | D-23..D-26 | Conditional affordance, exact copy, accessible reuse, layered diagnostics | 03,04,05 | COVERED | Existing pages/component system retained. |

Deferred Phase 71 launcher/banner and Phase 72 guide/README work are excluded by source contract. The optional genuine-response replay companion is discretionary and is not planned because KC-01 already receives a genuine positive proof plus the required independent FakeIdP typed-negative lane; no security policy is weakened to create it.
</source_audit>

<prohibition_probe_breadcrumbs>
Generic OWASP/injection/path-traversal concerns are canon and remain covered by the Phase 70 STRIDE registers, existing repository security gates, and `$gsd-secure-phase`; they are not minted as bespoke prohibitions. Routine engineering negatives (no exception on missing row, shell syntax, cleanup mechanics) remain task acceptance criteria rather than values/safety prohibitions. The three kept bespoke intents—optional-lane honesty, receipt/session truthfulness, and diagnostic secret safety—appear as descriptor-less flagged-unverified `must_haves.prohibitions` across Plans 01-05; no kept item was silently dropped.
</prohibition_probe_breadcrumbs>

<verification>
`npm run demo:keycloak-proxy &amp;&amp; cd demo/ledger_loop &amp;&amp; mix test test/ledger_loop/demo/keycloak_provisioner_test.exs test/ledger_loop_web/controllers/route_affordance_controller_test.exs test/ledger_loop_web/controllers/page_controller_test.exs test/ledger_loop_web/fake_idp_flow_test.exs --warnings-as-errors &amp;&amp; cd ../.. &amp;&amp; mix test --warnings-as-errors &amp;&amp; mix ci.security &amp;&amp; mix format --check-formatted`; run `mix qa` separately and report the recorded advisory-only state if unchanged.
</verification>

<success_criteria>
An evaluator starts from the labelled optional link, authenticates Sarah at the public Keycloak host, observes the exact genuine ACS success, sees the truthful durable receipt, and confirms the same correlation's validation, signature-verification, and replay steps; FakeIdP tamper and all permanent security gates remain independent and green.
</success_criteria>

<output>
Create `.planning/phases/70-keycloak-behind-the-proxy/70-05-SUMMARY.md` when done.
</output>
