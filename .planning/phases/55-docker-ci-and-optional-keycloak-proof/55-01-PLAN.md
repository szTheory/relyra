---
phase: "55"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "docker-compose.yml"
  - "docker/keycloak/docker-compose.yml"
  - "docker/keycloak/realm-demo-app.json"
  - "playwright.demo.config.mjs"
  - "demo/ledger_loop/test/browser/keycloak.spec.ts"
  - "demo/ledger_loop/config/dev.exs"
  - "demo/ledger_loop/config/test.exs"
autonomous: true
requirements:
  - DX-02
  - IDP-03
  - IDP-04
must_haves:
  truths:
    - "Compose profiles correctly isolate core, keycloak, and browser"
    - "Playwright tests run reliably via Docker without crashing"
    - "Keycloak 26 accepts redirect URIs from localhost"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Root orchestration"
      contains: "profiles:"
    - path: "playwright.demo.config.mjs"
      provides: "Playwright configuration for demo"
    - path: "demo/ledger_loop/test/browser/keycloak.spec.ts"
      provides: "Playwright test for Keycloak login"
  key_links:
    - from: "docker-compose.yml"
      to: "quay.io/keycloak/keycloak:26.0.7"
      via: "image definition"
      pattern: "keycloak:26.0.7"
    - from: "docker-compose.yml"
      to: "mcr.microsoft.com/playwright:v1.54.2-jammy"
      via: "image definition"
      pattern: "playwright"
---

<objective>
Consolidate Docker orchestrations into a single top-level `docker-compose.yml` supporting `core`, `keycloak`, and `browser` profiles, and add browser proofing for Keycloak.

Purpose: Improve DX and standardize cross-environment behavior by making local Keycloak testing and browser E2E isolation easy.
Output: A new root `docker-compose.yml`, Playwright demo test files, and updated Ecto configs to resolve PostgreSQL hosts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-docker-ci-and-optional-keycloak-proof/55-CONTEXT.md
@.planning/phases/55-docker-ci-and-optional-keycloak-proof/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create top-level Docker Compose and Demo Realm</name>
  <files>docker-compose.yml, docker/keycloak/docker-compose.yml, docker/keycloak/realm-demo-app.json</files>
  <action>Create a new Keycloak realm export at `docker/keycloak/realm-demo-app.json` (you can base it on the existing adoption realm). Ensure it has correct ACS URLs (`http://demo_app:4000/saml/sso/acs` and `http://localhost:4000/saml/sso/acs`) and the appropriate SP entity ID for the demo application.

  Create `docker-compose.yml` in the root directory. Add a top-level `name: relyra-demo` for explicit project-name isolation. It must define profiles: `core` (for Postgres and the Demo App), `keycloak` (for IdP proof), and `browser` (for Playwright).
  
  The `db` service (postgres:15-alpine) should use the `core` profile, set `environment: POSTGRES_PASSWORD=postgres`, configure env-driven ports for its exposed port (e.g., `ports: ["${PGPORT:-5432}:5432"]`), and include a healthcheck using `pg_isready -U postgres`.
  The `demo_app` service (hexpm/elixir:1.15.7-erlang-26.1.2-alpine-3.18.4) should use the `core` profile, mount `.:/app`, use `working_dir: /app/demo/ledger_loop`, depend on `db` (with `condition: service_healthy`), configure env-driven ports (e.g., `ports: ["${PORT:-4000}:4000"]`), and run `apk add --no-cache inotify-tools curl && mix local.hex --force && mix local.rebar --force && mix deps.get && mix ecto.setup && mix phx.server`. Add a healthcheck using `curl -f http://localhost:4000/ || exit 1`.
  The `keycloak` service (quay.io/keycloak/keycloak:26.0.7) should use the `keycloak` profile, retain existing `KC_HOSTNAME_STRICT="false"` environment variables from the old compose file, mount `./docker/keycloak/realm-demo-app.json:/opt/keycloak/data/import/realm-demo-app.json`, configure env-driven ports (e.g., `ports: ["${KC_PORT:-8080}:8080"]`), and include a healthcheck using `curl -f http://localhost:8080/health/ready || exit 1`.
  The `playwright` service (mcr.microsoft.com/playwright:v1.54.2-jammy) should use the `browser` profile, depend on `demo_app` and `keycloak` (both with `condition: service_healthy`), set `ipc: host`, mount `.:/app`, `working_dir: /app`, and run `npm install && npx playwright test --config playwright.demo.config.mjs`.

  Remove `docker/keycloak/docker-compose.yml` as it is now replaced.</action>
  <verify>
    <automated>grep 'profiles:' docker-compose.yml | grep -c -v '^#'</automated>
  </verify>
  <done>Root docker-compose.yml exists, defines correct profiles, includes healthchecks/readiness probes, uses env-driven ports, and mounts the demo app realm JSON. Old compose file deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic PostgreSQL Host in Demo App Config</name>
  <files>demo/ledger_loop/config/dev.exs, demo/ledger_loop/config/test.exs</files>
  <action>Update `hostname: "localhost"` in both config files to `hostname: System.get_env("PGHOST") || "localhost"`. This allows the application to connect to the Docker service `db` when running inside Compose, but still fallback to `localhost` when run directly on the host in CI. Additionally, update `ip: {127, 0, 0, 1}` to `ip: {0, 0, 0, 0}` in both config files to ensure the demo app is reachable from outside its container.</action>
  <verify>
    <automated>grep -c 'System.get_env("PGHOST")' demo/ledger_loop/config/dev.exs</automated>
  </verify>
  <done>Database configurations use PGHOST env var.</done>
</task>

<task type="auto">
  <name>Task 3: Demo App Browser Proofing</name>
  <files>playwright.demo.config.mjs, demo/ledger_loop/test/browser/keycloak.spec.ts</files>
  <action>Create `playwright.demo.config.mjs` (similar to `playwright.admin-ui.config.mjs` but without the `webServer` block, since the backend runs via Compose) setting `baseURL: process.env.BASE_URL || 'http://demo_app:4000'`. 
  
  Create a basic browser test `demo/ledger_loop/test/browser/keycloak.spec.ts`. First, have the test automate the Setup UI to create the Keycloak SAML connection in the demo app (since the demo app has no Keycloak connection seeded by default). Then, navigate to the Demo App `/`, click the SAML login option, log into Keycloak with `admin/admin` (based on the realm JSON), and expect to be redirected back successfully.</action>
  <verify>
    <automated>ls playwright.demo.config.mjs demo/ledger_loop/test/browser/keycloak.spec.ts</automated>
  </verify>
  <done>Playwright configuration and test for demo app external Keycloak login exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker Network | Demo app to Keycloak IDP |
| CI Environment | Sandbox isolation for e2e tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | Docker image pinning | mitigate | Pin to explicit versions for postgres, elixir, keycloak, and playwright |
| T-55-02 | Info Disclosure | CI Secrets | mitigate | Use dummy test credentials for E2E proofs |
</threat_model>

<verification>
Check that `docker compose --profile core config` evaluates correctly.
</verification>

<success_criteria>
`docker-compose.yml` provides a functional local cluster with Playwright testing support without conflicts.
</success_criteria>

<output>
Create `.planning/phases/55-docker-ci-and-optional-keycloak-proof/55-01-SUMMARY.md` when done
</output>