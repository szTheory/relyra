---
phase: "55"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - "mix.exs"
  - ".github/workflows/demo-app-ci.yml"
autonomous: true
requirements:
  - CI-01
  - E2E-02
must_haves:
  truths:
    - "mix ci.demo_app compiles and tests the demo application"
    - "Demo application CI runs isolated in GitHub Actions"
  artifacts:
    - path: "mix.exs"
      provides: "Mix aliases for CI"
      contains: "\"ci.demo_app\":"
    - path: ".github/workflows/demo-app-ci.yml"
      provides: "GitHub workflow for demo CI"
      contains: "mix ci.demo_app"
  key_links:
    - from: ".github/workflows/demo-app-ci.yml"
      to: "mix.exs"
      via: "command execution"
      pattern: "mix ci.demo_app"
---

<objective>
Wire the Demo app into GitHub CI with a new isolated pipeline executing `mix ci.demo_app`.

Purpose: Prove Demo App behaviors (FakeIdP, Ecto storage) structurally without coupling it to core cryptographic validation (`ci.security`).
Output: Root `mix.exs` changes and a standalone `.github/workflows/demo-app-ci.yml`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-docker-ci-and-optional-keycloak-proof/55-CONTEXT.md
@.planning/phases/55-docker-ci-and-optional-keycloak-proof/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ci.demo_app Mix Alias</name>
  <files>mix.exs</files>
  <action>In the root `mix.exs`, add a new alias `"ci.demo_app": ["cmd --cd demo/ledger_loop mix deps.get", "cmd --cd demo/ledger_loop mix ecto.setup", "cmd --cd demo/ledger_loop mix test"]`.
  Also add `"ci.demo_app": :test` to the `preferred_envs` in `cli/0`.</action>
  <verify>
    <automated>mix help ci.demo_app || grep -q 'ci.demo_app' mix.exs</automated>
  </verify>
  <done>Alias ci.demo_app exists and invokes demo/ledger_loop tests correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Setup GitHub CI Workflow for Demo App</name>
  <files>.github/workflows/demo-app-ci.yml</files>
  <action>Create `.github/workflows/demo-app-ci.yml`. Configure it to trigger on `pull_request` and `push` to `main`, ignoring `.planning/**` and `prompts/**`.
  
  The workflow must define a job with `services: postgres:16` (matching the `security-gates.yml` postgres setup), check out the repository, setup BEAM via `erlef/setup-beam@v1`, install dependencies in both root and demo/ledger_loop, and finally run `mix ci.demo_app`. Define necessary environment variables like `PGHOST=127.0.0.1`, `PGUSER=postgres`, and `PGPASSWORD=postgres` for the step.</action>
  <verify>
    <automated>grep -q 'mix ci.demo_app' .github/workflows/demo-app-ci.yml</automated>
  </verify>
  <done>Workflow definition file for demo app CI is present and configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI Environment | Sandbox isolation for e2e tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04 | Spoofing | CI Testing | accept | Local testing db uses static `postgres:postgres` credentials, acceptable for testing only |
</threat_model>

<verification>
Ensure `mix ci.demo_app` can execute locally if Postgres is running on localhost.
</verification>

<success_criteria>
`mix ci.demo_app` runs Demo tests and the GitHub workflow configuration correctly wires it up.
</success_criteria>

<output>
Create `.planning/phases/55-docker-ci-and-optional-keycloak-proof/55-03-SUMMARY.md` when done
</output>