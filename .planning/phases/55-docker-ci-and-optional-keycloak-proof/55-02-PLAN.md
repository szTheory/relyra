---
phase: "55"
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - "scripts/demo"
autonomous: true
requirements:
  - DX-01
  - DX-03
must_haves:
  truths:
    - "CLI supports doctor, up, reset, test, urls, and down"
    - "Doctor command checks environment readiness and port collisions"
  artifacts:
    - path: "scripts/demo"
      provides: "Demo orchestrator CLI"
      contains: "case \"$COMMAND\" in"
  key_links:
    - from: "scripts/demo"
      to: "docker compose"
      via: "shell execution"
      pattern: "docker compose"
---

<objective>
Provide a low-friction CLI wrapper for Demo lifecycle and Docker Compose orchestration.

Purpose: Abstract away the Docker Compose profile syntax to simplify reviewer adoption.
Output: A Bash script at `scripts/demo` that provides `doctor`, `up`, `reset`, `test`, `urls`, and `down` commands.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-docker-ci-and-optional-keycloak-proof/55-CONTEXT.md
@.planning/phases/55-docker-ci-and-optional-keycloak-proof/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/demo</name>
  <files>scripts/demo</files>
  <action>Create a new executable bash script at `scripts/demo` (`chmod +x`). 
  It must implement a `case "$COMMAND" in` structure matching the following commands:
  - `doctor`: check for `docker` and `mix` dependencies. Print remediation steps if missing. Additionally, add port collision checks for common ports (e.g., check if port 4000 or 8080 is already in use using `lsof -i :4000` or a similar tool) and warn the user.
  - `up`: run `docker compose --profile core up -d`
  - `reset`: run `docker compose --profile core exec demo_app mix do ecto.drop, ecto.setup`
  - `test`: run `docker compose --profile keycloak --profile browser run --rm playwright`
  - `urls`: echo the URLs for the Demo App (`http://localhost:4000`) and Keycloak (`http://localhost:8080`)
  - `down`: run `docker compose --profile core --profile keycloak --profile browser down -v`
  
  Set `set -euo pipefail` at the top of the script.</action>
  <verify>
    <automated>test -x scripts/demo && ./scripts/demo doctor</automated>
  </verify>
  <done>scripts/demo script is present, executable, checks dependencies and port collisions, and accepts the required commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Workstation | The environment executing scripts/demo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Elevation | scripts/demo execution | mitigate | Avoid sudo or privileged commands within bash script |
</threat_model>

<verification>
Ensure `./scripts/demo` runs without syntax errors.
</verification>

<success_criteria>
`scripts/demo doctor` exits 0 when dependencies are met and ports are free, and other commands map to valid docker compose invocations.
</success_criteria>

<output>
Create `.planning/phases/55-docker-ci-and-optional-keycloak-proof/55-02-SUMMARY.md` when done
</output>