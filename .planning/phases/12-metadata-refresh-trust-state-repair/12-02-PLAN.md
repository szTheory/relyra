---
phase: 12-metadata-refresh-trust-state-repair
plan: 12-02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - lib/relyra/metadata/refresh.ex
  - lib/relyra/ecto/metadata_apply.ex
  - test/relyra/metadata_refresh_test.exs
  - test/relyra/ecto/metadata_apply_test.exs
autonomous: true
requirements: [CFG-03]
must_haves:
  truths:
    - "Operator-triggered metadata refresh succeeds on valid metadata without certificate decode failures."
    - "Successful metadata apply stages new signing certificates as `:next` while leaving active runtime trust unchanged."
    - "Failed refresh/apply attempts preserve last-known-good runtime state and keep redacted observability intact."
  artifacts:
    - path: "lib/relyra/metadata/refresh.ex"
      provides: "Shared refresh orchestration through the repaired candidate -> apply seam"
    - path: "lib/relyra/ecto/metadata_apply.ex"
      provides: "Single transactional apply seam preserving staged trust-state semantics"
    - path: "test/relyra/metadata_refresh_test.exs"
      provides: "Focused refresh smoke coverage with valid X.509 fixture material and failure-path redaction checks"
    - path: "test/relyra/ecto/metadata_apply_test.exs"
      provides: "Apply-path staging and rollback invariants after the contract repair"
  key_links:
    - from: "lib/relyra/metadata/refresh.ex"
      to: "lib/relyra/metadata/import.ex"
      via: "refresh reuses `Import.build_candidate/1` per D-03"
      pattern: "Import.build_candidate"
    - from: "lib/relyra/metadata/refresh.ex"
      to: "lib/relyra/ecto/metadata_apply.ex"
      via: "refresh calls `MetadataApply.apply_revision/4` for the only trust-state mutation path"
      pattern: "MetadataApply.apply_revision"
    - from: "lib/relyra/ecto/metadata_apply.ex"
      to: "lib/relyra/ecto/certificate_inventory.ex"
      via: "stages metadata-introduced certs as `:next` and never auto-promotes them"
      pattern: "stage_metadata_certificates"
---

<objective>
Re-prove the shared import/refresh apply seam and preserve the staged-trust model introduced in Phase 10.

Purpose: implement D-08 through D-10 by ensuring the repaired metadata candidate contract flows through refresh and apply unchanged, with no auto-promotion into runtime trust and no regression in last-known-good preservation or redacted observability.
Output: green import/apply/refresh smoke coverage on valid cert inputs plus preserved staging/rollback semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.2-MILESTONE-AUDIT.md
@.planning/phases/09-metadata-import-export-refresh/09-02-SUMMARY.md
@.planning/phases/09-metadata-import-export-refresh/09-04-SUMMARY.md
@.planning/phases/10-certificate-inventory-rollover/10-03-SUMMARY.md
@.planning/phases/12-metadata-refresh-trust-state-repair/12-CONTEXT.md
@.planning/phases/12-metadata-refresh-trust-state-repair/12-RESEARCH.md
@.planning/phases/12-metadata-refresh-trust-state-repair/12-PATTERNS.md
@lib/relyra/metadata/refresh.ex
@lib/relyra/ecto/metadata_apply.ex
@lib/relyra/ecto/certificate_inventory.ex
@lib/relyra/ecto/connection_snapshot.ex
@test/relyra/metadata_refresh_test.exs
@test/relyra/ecto/metadata_apply_test.exs

<interfaces>
From `lib/relyra/metadata/refresh.ex`:
```elixir
candidate = Import.build_candidate(parsed)
MetadataApply.apply_revision(connection.connection_id, Map.from_struct(candidate), revision_attrs, repo: repo)
```

From `lib/relyra/ecto/metadata_apply.ex`:
```elixir
CertificateInventory.stage_metadata_certificates(
  repo,
  repo.preload(updated_connection, :certificates),
  revision,
  candidate,
  audit: audit_context_from_revision(revision)
)
```

From `test/relyra/ecto/metadata_apply_test.exs`:
```elixir
assert resolved.idp_certificates == [
  "-----BEGIN CERTIFICATE-----\\nOLD\\n-----END CERTIFICATE-----"
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reuse the repaired candidate seam for refresh success paths</name>
  <files>lib/relyra/metadata/refresh.ex, test/relyra/metadata_refresh_test.exs</files>
  <read_first>
    <item>.planning/phases/12-metadata-refresh-trust-state-repair/12-CONTEXT.md</item>
    <item>.planning/phases/12-metadata-refresh-trust-state-repair/12-RESEARCH.md</item>
    <item>lib/relyra/metadata/refresh.ex</item>
    <item>lib/relyra/metadata/import.ex</item>
    <item>test/relyra/metadata_refresh_test.exs</item>
  </read_first>
  <action>Keep `Relyra.Metadata.Refresh.refresh/2` on the shared `Import.build_candidate/1` -> `MetadataApply.apply_revision/4` path per D-03; do not fork a Phase 12-only normalization path. Update `test/relyra/metadata_refresh_test.exs` to replace the synthetic refresh certificate body with real X.509-backed metadata so the focused refresh smoke proves the same repaired candidate seam used by local import. Preserve the failure-path assertions for redacted logs/telemetry and last-known-good protection. If minor code adjustments in `lib/relyra/metadata/refresh.ex` are needed to keep the refresh path consuming the clarified candidate shape from 12-01, keep them narrowly scoped and do not change the explicit operator-triggered refresh contract or provenance behavior per D-02 and D-12.</action>
  <verify>
    <automated>mix test test/relyra/metadata_refresh_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`test/relyra/metadata_refresh_test.exs` proves a successful manual refresh with valid X.509-backed metadata.</criterion>
    <criterion>Refresh still records redacted failure telemetry/log context and preserves the applied runtime snapshot on failure.</criterion>
    <criterion>`lib/relyra/metadata/refresh.ex` still routes through `Import.build_candidate/1` and `MetadataApply.apply_revision/4` rather than a special-case path.</criterion>
  </acceptance_criteria>
  <done>The explicit refresh flow succeeds for valid metadata without certificate decode regressions and still preserves the existing failure-path safeguards.</done>
</task>

<task type="auto">
  <name>Task 2: Reassert transactional apply and staged runtime trust invariants</name>
  <files>lib/relyra/ecto/metadata_apply.ex, test/relyra/ecto/metadata_apply_test.exs</files>
  <read_first>
    <item>.planning/phases/10-certificate-inventory-rollover/10-03-SUMMARY.md</item>
    <item>.planning/phases/12-metadata-refresh-trust-state-repair/12-CONTEXT.md</item>
    <item>lib/relyra/ecto/metadata_apply.ex</item>
    <item>lib/relyra/ecto/certificate_inventory.ex</item>
    <item>lib/relyra/ecto/connection_snapshot.ex</item>
    <item>test/relyra/ecto/metadata_apply_test.exs</item>
  </read_first>
  <action>Keep `lib/relyra/ecto/metadata_apply.ex` as the only transactional apply boundary per D-03 and preserve the Phase 10 trust semantics per D-08 through D-10: metadata-introduced signing certs stage as `:next`, existing active certs remain active, and runtime hydration continues to expose only active certificates. Extend `test/relyra/ecto/metadata_apply_test.exs` so the repaired metadata candidate contract from 12-01 is exercised through `apply_revision/4`, including one success-path assertion that staged metadata certs are persisted as `:next` only and one failure-path assertion that malformed cert input still rolls back revision/certificate changes with typed `:invalid_certificate_pem`. Make only the minimal code changes in `lib/relyra/ecto/metadata_apply.ex` required to consume the clarified candidate contract; do not move validation later in the pipeline or auto-promote staged certs into live runtime trust.</action>
  <verify>
    <automated>mix test test/relyra/ecto/metadata_apply_test.exs test/relyra/metadata_refresh_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Successful `apply_revision/4` runs stage metadata-introduced certificates as `:next` and leave active runtime trust unchanged.</criterion>
    <criterion>Malformed certificate input still rolls back revision and certificate changes with typed `:invalid_certificate_pem` details.</criterion>
    <criterion>No test or code path implicitly promotes a metadata-introduced certificate into the runtime `idp_certificates` set.</criterion>
  </acceptance_criteria>
  <done>The shared apply seam is green again, and the staged-trust / rollback invariants remain exactly aligned with the Phase 10 lifecycle model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote metadata fetch -> parsed candidate | Untrusted remote metadata enters the explicit refresh path |
| metadata apply transaction -> persisted certificate inventory | Trust-bearing staged certificate state is committed to durable storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | T | `Relyra.Metadata.Refresh.refresh/2` | mitigate | Keep refresh on the shared candidate/apply seam per D-03 so import and refresh cannot drift into different certificate semantics. |
| T-12-02-02 | E | `Relyra.Ecto.MetadataApply.apply_revision/4` | mitigate | Preserve Phase 10 staging semantics per D-08 through D-10 so metadata refresh cannot auto-promote new trust into runtime. |
| T-12-02-03 | I | refresh observability path | mitigate | Keep failure telemetry and logs redacted and preserve last-known-good runtime state on refresh/apply failures. |
</threat_model>

<verification>
Run the focused apply/refresh smoke commands serially and confirm the valid refresh path is green, failure paths preserve last-known-good state, and runtime still exposes active certs only.
</verification>

<success_criteria>
The shared import/refresh apply path is repaired for valid metadata, and the live runtime trust window remains active-only with staged metadata certs held in `:next`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-metadata-refresh-trust-state-repair/12-02-SUMMARY.md`
</output>
