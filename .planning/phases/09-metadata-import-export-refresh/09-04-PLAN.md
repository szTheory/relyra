---
phase: 09-metadata-import-export-refresh
plan: 04
type: execute
wave: 4
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - mix.exs
  - lib/relyra/metadata.ex
  - lib/relyra/metadata/refresh.ex
  - lib/relyra/log.ex
  - lib/relyra/telemetry.ex
  - test/phoenix/metadata_controller_test.exs
  - test/relyra/metadata_refresh_test.exs
  - test/relyra/telemetry_test.exs
autonomous: true
requirements: [CFG-03]
must_haves:
  truths:
    - "Operators can trigger refresh explicitly and only explicitly per D-01 and D-08."
    - "Failed refreshes never change the resolver-fed runtime snapshot, preserving last-known-good state per D-03 and D-04."
    - "Public SP metadata export continues to render from `Relyra.ConnectionResolver` snapshots only per D-11 and D-12."
  artifacts:
    - path: "lib/relyra/metadata/refresh.ex"
      provides: "Explicit remote fetch and apply orchestration"
    - path: "test/relyra/metadata_refresh_test.exs"
      provides: "Refresh isolation and optional Req coverage"
    - path: "test/phoenix/metadata_controller_test.exs"
      provides: "Export regression proving snapshot-only rendering"
  key_links:
    - from: "lib/relyra/metadata/refresh.ex"
      to: "lib/relyra/metadata/import.ex"
      via: "refresh reuses metadata-specific parse and candidate normalization"
      pattern: "Parser|Candidate|import"
    - from: "lib/relyra/phoenix/controllers/metadata_controller.ex"
      to: "lib/relyra/connection_resolver/ecto.ex"
      via: "metadata export still resolves a runtime snapshot, never a revision row"
      pattern: "resolve_connection|build_sp_metadata"
---

<objective>
Finish Phase 09 with explicit remote refresh orchestration, optional Req wiring, redacted metadata lifecycle observability, and a regression lock on the snapshot-only public export path.

Purpose: implement the operator-triggered refresh verb that fetches remote metadata only when asked, then prove failures preserve last-known-good runtime trust and export behavior.
Output: refresh service, telemetry/log updates, refresh tests, and export regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-metadata-import-export-refresh/09-CONTEXT.md
@.planning/phases/09-metadata-import-export-refresh/09-RESEARCH.md
@.planning/phases/09-metadata-import-export-refresh/09-VALIDATION.md
@.planning/phases/08-resolver-adapter-snapshotting/08-03-SUMMARY.md
@.planning/phases/09-metadata-import-export-refresh/09-02-PLAN.md
@.planning/phases/09-metadata-import-export-refresh/09-03-PLAN.md

<interfaces>
From lib/relyra/phoenix/controllers/metadata_controller.ex:
```elixir
case Relyra.ConnectionResolver.resolve_connection(request_context, opts) do
  {:ok, connection} ->
    xml = Relyra.Protocol.Metadata.build_sp_metadata(connection, opts)
```

From lib/relyra/telemetry.ex:
```elixir
def execute(event, measurements, metadata \\ %{}) do
  :telemetry.execute([:relyra, :saml | List.wrap(event)], measurements, metadata)
end
```

From lib/relyra/log.ex:
```elixir
@sensitive_keys [:xml, :response_xml, :assertion_xml, :signed_xml, :relay_state, :private_key]
```
</interfaces>
</context>

<tasks>

<task type="auto" id="09-04-T01" tdd="true">
  <name>Task 1: Implement explicit remote refresh with optional Req and last-known-good isolation</name>
  <read_first>
    - /Users/jon/projects/relyra/.planning/phases/09-metadata-import-export-refresh/09-VALIDATION.md
    - /Users/jon/projects/relyra/.planning/phases/09-metadata-import-export-refresh/09-CONTEXT.md
    - /Users/jon/projects/relyra/lib/relyra/metadata.ex
    - /Users/jon/projects/relyra/lib/relyra/ecto/metadata_apply.ex
    - /Users/jon/projects/relyra/mix.exs
  </read_first>
  <files>mix.exs, lib/relyra/metadata.ex, lib/relyra/metadata/refresh.ex, test/relyra/metadata_refresh_test.exs</files>
  <behavior>
    - Test 1: `Relyra.Metadata.refresh/2` looks up the single registered source for one connection, fetches XML only under explicit invocation, and applies a new revision when parse/normalize/validate succeed.
    - Test 2: when `Req` is unavailable or `opts[:req]` is not configured, refresh returns a typed optional-dependency/configuration error while local XML import still works.
    - Test 3: fetch, parse, validation, or apply failures persist a failed revision row with outcome metadata but keep the connection’s active runtime fields and last-known-good pointer unchanged.
  </behavior>
  <action>Add optional `:req` to `mix.exs` in this plan, not earlier, so XML import remains dependency-light until the explicit refresh slice lands. Then add `Relyra.Metadata.refresh/2` to `lib/relyra/metadata.ex` and implement orchestration in `lib/relyra/metadata/refresh.ex`. Follow the option-validation and optional-dependency gate posture mapped in `09-PATTERNS.md` from `lib/relyra/request_store/ecto.ex`, and keep typed failure normalization consistent with the repo-access rescue/reporting posture mapped from `lib/relyra/ecto/connection_loader.ex`. The refresh path must: load the single registered metadata source row for one `connection_id`, relying on the unique `connection_record_id` constraint and replacement semantics established in 09-01 and 09-03; require `repo: ...`; gate remote fetch behind `Code.ensure_loaded?(Req)` and return a typed optional-dependency error when Req is absent; fetch over HTTPS only; parse through the metadata parser from 09-03; normalize to the same candidate endpoint rule from 09-03; persist a revision row for every attempt per D-18; and call `Relyra.Ecto.MetadataApply.apply_revision/4` only after candidate validation succeeds per D-02. Use `Req.Test` fixtures in `test/relyra/metadata_refresh_test.exs` instead of live HTTP. Do not add any refresh hook to `Relyra.ConnectionResolver`, `Relyra.Phoenix.Controllers.LoginController`, `Relyra.Phoenix.Controllers.MetadataController`, or cache-miss logic; D-01 and D-09 forbid runtime fetches on those paths. Do not add runtime-path refreshes or resolver reads from metadata revision/source tables.</action>
  <acceptance_criteria>
    - `rg -n "def refresh|Code.ensure_loaded\\?\\(Req\\)|Req\\.Test|optional_dependency" lib/relyra/metadata.ex lib/relyra/metadata/refresh.ex test/relyra/metadata_refresh_test.exs` shows the explicit refresh API, optional dependency gate, and test stubs.
    - `rg -n "single registered source|connection_record_id|unique source" lib/relyra/metadata/refresh.ex test/relyra/metadata_refresh_test.exs` shows deterministic source selection for refresh.
    - `rg -n "failed revision|last_known_good_metadata_revision_id|unchanged" test/relyra/metadata_refresh_test.exs` shows preservation of last-known-good state on refresh failure.
    - `rg -n "LoginController|MetadataController|ConnectionResolver" lib/relyra/metadata/refresh.ex` returns no matches, proving refresh is not wired into runtime request paths.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/relyra/metadata_refresh_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Remote refresh exists as an explicit operator-triggered write-side action, remains optional via Req, and never alters runtime trust unless apply succeeds.</done>
</task>

<task type="auto" id="09-04-T02" tdd="true">
  <name>Task 2: Lock export and observability to applied snapshot state only</name>
  <read_first>
    - /Users/jon/projects/relyra/.planning/phases/09-metadata-import-export-refresh/09-VALIDATION.md
    - /Users/jon/projects/relyra/lib/relyra/phoenix/controllers/metadata_controller.ex
    - /Users/jon/projects/relyra/lib/relyra/log.ex
    - /Users/jon/projects/relyra/lib/relyra/telemetry.ex
    - /Users/jon/projects/relyra/test/phoenix/metadata_controller_test.exs
  </read_first>
  <files>lib/relyra/log.ex, lib/relyra/telemetry.ex, test/phoenix/metadata_controller_test.exs, test/relyra/telemetry_test.exs, test/relyra/metadata_refresh_test.exs</files>
  <behavior>
    - Test 1: the metadata controller still renders SP metadata from the resolved `%Relyra.Connection{}` snapshot even after failed refresh attempts and registered-but-unapplied sources.
    - Test 2: metadata lifecycle telemetry emits only identifiers, counts, timings, outcome, source kind, and error code fields; raw XML and PEM content never appear.
    - Test 3: log redaction covers metadata XML and certificate material used by import and refresh paths.
  </behavior>
  <action>Extend `Relyra.Telemetry` with metadata lifecycle event names under the existing `[:relyra, :saml, ...]` namespace, using redaction-safe metadata only: `connection_id`, `metadata_source_id`, `source_kind`, `trigger`, `outcome`, `error_code`, `certificate_count`, and timing fields. Keep the event shape aligned with the exact-analog utility posture called out in `09-PATTERNS.md` for `lib/relyra/telemetry.ex`, and extend `Relyra.Log` sensitive keys using the same exact-analog redaction posture mapped there for `lib/relyra/log.ex`. Add or adjust tests in `test/relyra/telemetry_test.exs` and `test/relyra/metadata_refresh_test.exs` so refresh/import paths assert redaction and event shape. Extend `test/phoenix/metadata_controller_test.exs` with a regression proving the controller still resolves via `Relyra.ConnectionResolver` and does not read `Relyra.Ecto.MetadataRevision` or raw imported XML. Do not add any new public endpoint for raw IdP metadata, normalized IdP config, or provenance export; D-11 through D-15 defer those surfaces, and do not introduce resolver reads from metadata revision/source tables.</action>
  <acceptance_criteria>
    - `rg -n "metadata.*start|metadata.*stop|source_kind|certificate_count" lib/relyra/telemetry.ex test/relyra/telemetry_test.exs` shows metadata lifecycle events and assertions.
    - `rg -n "metadata_xml|certificate_pem|\\[REDACTED\\]" lib/relyra/log.ex test/relyra/metadata_refresh_test.exs` shows Phase 09 redaction coverage.
    - `rg -n "resolve_connection|build_sp_metadata" test/phoenix/metadata_controller_test.exs` confirms export regression coverage still goes through the resolver snapshot path.
    - `rg -n "raw IdP|normalized IdP|provenance export" lib test` returns no new public endpoint implementation for deferred export surfaces.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/relyra/metadata_refresh_test.exs test/relyra/telemetry_test.exs test/phoenix/metadata_controller_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Refresh observability is redacted and attributable, and public SP metadata export remains a pure read of the applied runtime snapshot only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote metadata source -> refresh service | network-delivered XML enters trust-change workflow |
| metadata lifecycle service -> telemetry/logging | sensitive metadata content can leak through observability if not redacted |
| runtime export route -> metadata provenance storage | export path could accidentally bypass resolver and leak raw provenance artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | T | `lib/relyra/metadata/refresh.ex` | mitigate | Keep refresh operator-triggered only, fetch over HTTPS via optional Req, and apply changes only after parse/normalize/validate succeeds per D-01, D-02, and D-08. |
| T-09-04-02 | I | `lib/relyra/log.ex`, `lib/relyra/telemetry.ex` | mitigate | Redact XML and PEM material and emit summary-only metadata so provenance is durable in DB, not in logs or telemetry per D-20 and D-23. |
| T-09-04-03 | E | `lib/relyra/phoenix/controllers/metadata_controller.ex` | mitigate | Preserve resolver-only export reads and add regression tests proving the controller still renders from `%Relyra.Connection{}` instead of revision/source tables per D-12 and D-15. |
</threat_model>

<verification>
- `mix test test/relyra/metadata_refresh_test.exs --warnings-as-errors`
- `mix test test/relyra/metadata_refresh_test.exs test/relyra/telemetry_test.exs test/phoenix/metadata_controller_test.exs --warnings-as-errors`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- `Relyra.Metadata.refresh/2` fetches remote metadata only when explicitly invoked and only through optional Req.
- Failed refreshes preserve the currently applied runtime snapshot and last-known-good revision pointer.
- SP metadata export and observability remain snapshot-only and redacted.
</success_criteria>

<output>
After completion, create `.planning/phases/09-metadata-import-export-refresh/09-04-SUMMARY.md`
</output>
