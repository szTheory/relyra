---
phase: 09-metadata-import-export-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/ecto/connection.ex
  - lib/relyra/ecto/metadata_revision.ex
  - lib/relyra/ecto/metadata_source.ex
  - priv/repo/migrations/20260505130000_add_metadata_revision_pointers_to_relyra_connections.exs
  - priv/repo/migrations/20260505130100_create_relyra_metadata_sources.exs
  - priv/repo/migrations/20260505130200_create_relyra_metadata_revisions.exs
  - test/relyra/ecto/metadata_revision_schema_test.exs
  - test/relyra/ecto/metadata_source_schema_test.exs
  - test/relyra/ecto/migration_constraints_test.exs
autonomous: true
requirements: [CFG-03]
must_haves:
  truths:
    - "Every metadata import or refresh attempt remains durably attributable to an operator-visible source, trigger, and outcome per D-18."
    - "The currently applied trust state and the last-known-good trust state remain explicitly recoverable without replaying logs or guessing from runtime behavior."
    - "Remote source registration stays deterministic per connection, so later refreshes have one clear provenance target instead of ambiguous competing sources."
  artifacts:
    - path: "lib/relyra/ecto/metadata_revision.ex"
      provides: "Append-only metadata revision schema and validation"
    - path: "lib/relyra/ecto/metadata_source.ex"
      provides: "Remote metadata source schema and HTTPS provenance storage"
    - path: "priv/repo/migrations/20260505130200_create_relyra_metadata_revisions.exs"
      provides: "Revision ledger table"
  key_links:
    - from: "priv/repo/migrations/20260505130000_add_metadata_revision_pointers_to_relyra_connections.exs"
      to: "lib/relyra/ecto/connection.ex"
      via: "schema fields and migration columns stay aligned on active_metadata_revision_id and last_known_good_metadata_revision_id"
      pattern: "active_metadata_revision_id|last_known_good_metadata_revision_id"
    - from: "lib/relyra/ecto/metadata_source.ex"
      to: "lib/relyra/ecto/metadata_revision.ex"
      via: "source rows can be referenced from append-only revision rows without becoming a runtime read path"
      pattern: "metadata_source_id|connection_record_id"
---

<objective>
Establish the write-side persistence foundation for metadata provenance without changing the Phase 08 runtime read path.

Purpose: deliver the append-only revision ledger, explicit metadata source records, and connection pointer storage that later apply/import/refresh plans can call.
Output: metadata migrations, Ecto schemas, and Wave 0 schema/migration tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-metadata-import-export-refresh/09-CONTEXT.md
@.planning/phases/09-metadata-import-export-refresh/09-RESEARCH.md
@.planning/phases/09-metadata-import-export-refresh/09-PATTERNS.md
@.planning/phases/09-metadata-import-export-refresh/09-VALIDATION.md
@.planning/phases/07-schema-connection-aggregate/07-CONTEXT.md
@.planning/phases/08-resolver-adapter-snapshotting/08-CONTEXT.md
@.planning/phases/08-resolver-adapter-snapshotting/08-02-SUMMARY.md

<interfaces>
From lib/relyra/ecto/connection.ex:
```elixir
schema "relyra_connections" do
  field :connection_id, :string
  field :status, Ecto.Enum, values: [:draft, :enabled, :disabled], default: :draft
  field :sp_entity_id, :string
  field :acs_url, :string
  field :idp_entity_id, :string
  field :idp_sso_url, :string
  has_many :certificates, Certificate, foreign_key: :connection_record_id, on_replace: :delete
end
```

From lib/relyra/ecto/connection_loader.ex:
```elixir
@spec fetch(module(), binary(), keyword()) :: {:ok, Connection.t()} | {:error, Error.t()}
```

From lib/relyra/ecto/connection_snapshot.ex:
```elixir
runtime_attrs
|> Map.put(:idp_certificates, certificates)
|> Map.put(:cert_chain, certificates)
```
</interfaces>
</context>

<tasks>

<task type="auto" id="09-01-T01" tdd="true">
  <name>Task 1: Create metadata ledger persistence and Wave 0 schema coverage</name>
  <read_first>
    - /Users/jon/projects/relyra/.planning/phases/09-metadata-import-export-refresh/09-VALIDATION.md
    - /Users/jon/projects/relyra/.planning/phases/09-metadata-import-export-refresh/09-CONTEXT.md
    - /Users/jon/projects/relyra/lib/relyra/ecto/connection.ex
    - /Users/jon/projects/relyra/lib/relyra/ecto/certificate.ex
    - /Users/jon/projects/relyra/test/relyra/ecto/migration_constraints_test.exs
  </read_first>
  <files>lib/relyra/ecto/connection.ex, lib/relyra/ecto/metadata_revision.ex, lib/relyra/ecto/metadata_source.ex, priv/repo/migrations/20260505130000_add_metadata_revision_pointers_to_relyra_connections.exs, priv/repo/migrations/20260505130100_create_relyra_metadata_sources.exs, priv/repo/migrations/20260505130200_create_relyra_metadata_revisions.exs, test/relyra/ecto/metadata_revision_schema_test.exs, test/relyra/ecto/metadata_source_schema_test.exs, test/relyra/ecto/migration_constraints_test.exs</files>
  <behavior>
    - Test 1: metadata revision changesets reject missing `connection_record_id`, `source_kind`, `trigger`, `outcome`, and `trust_summary`.
    - Test 2: metadata source changesets reject non-HTTPS URLs and missing provenance fields required by D-08 and D-18, and enforce one registered remote source row per connection in v0.2.
    - Test 3: migrations create `active_metadata_revision_id` and `last_known_good_metadata_revision_id` foreign-key columns on `relyra_connections`, plus append-only `relyra_metadata_sources` and `relyra_metadata_revisions` tables with a single-source-per-connection constraint for refresh registration.
  </behavior>
  <action>Per D-16 through D-23, extend `Relyra.Ecto.Connection` with nullable `active_metadata_revision_id` and `last_known_good_metadata_revision_id` fields and `belongs_to` associations that point at `Relyra.Ecto.MetadataRevision` without affecting `runtime_ready/1`. Mirror the association, changeset, and constraint posture mapped in `09-PATTERNS.md` from `lib/relyra/ecto/certificate.ex` for the new `Relyra.Ecto.MetadataSource` and `Relyra.Ecto.MetadataRevision` schemas, and mirror the migration/index posture from the existing connection/certificate migrations called out in the pattern map. Create `Relyra.Ecto.MetadataSource` with `connection_record_id`, `url`, `kind`, `registered_by`, `registered_reason`, `last_fetched_at`, `last_outcome`, and `metadata` fields; constrain `kind` to a remote-URL enum and validate HTTPS-only URLs per D-08. Lock the Phase 09 source contract to one registered remote refresh source per connection: enforce uniqueness on `connection_record_id`, not merely `(connection_record_id, url)`, so `register_source/3` can replace the existing source row deterministically and `refresh/2` never has to guess which source wins. Create `Relyra.Ecto.MetadataRevision` as append-only storage with `connection_record_id`, optional `metadata_source_id`, `source_kind`, `trigger`, `outcome`, `content_hash_sha256`, `effective_idp_entity_id`, `effective_idp_sso_url`, `certificate_fingerprints`, `trust_summary`, `actor`, `cause`, and `details` fields. Use foreign keys to `relyra_connections`, index revision rows by `connection_record_id` and inserted timestamp, index active/last-known-good pointer columns, and keep the single-source constraint explicit in both schema and migration tests. Do not add `Req`, apply-transaction code, or any resolver reads from revision/source tables in this plan; this slice is persistence-only so Phase 08 runtime behavior remains unchanged.</action>
  <acceptance_criteria>
    - `rg -n "active_metadata_revision_id|last_known_good_metadata_revision_id" lib/relyra/ecto/connection.ex priv/repo/migrations/20260505130000_add_metadata_revision_pointers_to_relyra_connections.exs` finds both pointer names in schema and migration.
    - `rg -n "schema \\\"relyra_metadata_sources\\\"|validate_change\\(:url" lib/relyra/ecto/metadata_source.ex` shows the source schema and explicit HTTPS validation.
    - `rg -n "unique_constraint\\(:connection_record_id|unique_index\\(:relyra_metadata_sources, \\[:connection_record_id\\]" lib/relyra/ecto/metadata_source.ex priv/repo/migrations/20260505130100_create_relyra_metadata_sources.exs` shows the single-source-per-connection rule for refresh registration.
    - `rg -n "schema \\\"relyra_metadata_revisions\\\"|certificate_fingerprints|trust_summary" lib/relyra/ecto/metadata_revision.ex` shows append-only revision fields required for D-18 through D-21.
    - `rg -n "metadata revision changeset|metadata source changeset" test/relyra/ecto/metadata_revision_schema_test.exs test/relyra/ecto/metadata_source_schema_test.exs` confirms Wave 0 schema tests exist.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/relyra/ecto/metadata_revision_schema_test.exs test/relyra/ecto/metadata_source_schema_test.exs test/relyra/ecto/migration_constraints_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Metadata pointer fields, ledger/source tables, and schema tests exist and compile without introducing any apply logic or runtime reads from revision or source tables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-invoked metadata persistence -> ledger schemas | untrusted metadata provenance fields cross into durable storage |
| migration/schema layer -> runtime aggregate | pointer fields and new tables can destabilize runtime reads if miswired into resolver paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | T | `lib/relyra/ecto/metadata_source.ex`, `lib/relyra/ecto/metadata_revision.ex` | mitigate | Validate HTTPS-only source URLs, required provenance fields, and append-only revision facts at the schema boundary before any later apply logic uses them. |
| T-09-01-02 | R | `lib/relyra/ecto/metadata_revision.ex` | mitigate | Persist append-only revision rows with source, actor, cause, outcome, and trust summary per D-18 and D-22. |
| T-09-01-03 | E | `lib/relyra/ecto/connection.ex` | mitigate | Keep metadata pointers separate from runtime-readiness validation and do not add resolver reads from revision/source tables, so draft/disabled rows cannot become runtime-active by pointer manipulation alone. |
</threat_model>

<verification>
- `mix test test/relyra/ecto/metadata_revision_schema_test.exs test/relyra/ecto/metadata_source_schema_test.exs test/relyra/ecto/migration_constraints_test.exs --warnings-as-errors`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- Metadata revision and source schemas exist with migrations and schema-level tests.
- Connection rows carry explicit active and last-known-good revision pointers.
</success_criteria>

<output>
After completion, create `.planning/phases/09-metadata-import-export-refresh/09-01-SUMMARY.md`
</output>
