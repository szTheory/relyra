---
phase: 09-metadata-import-export-refresh
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - lib/relyra/metadata.ex
  - lib/relyra/metadata/candidate.ex
  - lib/relyra/metadata/import.ex
  - lib/relyra/metadata/parser.ex
  - lib/relyra/metadata/source_registry.ex
  - test/relyra/metadata_test.exs
autonomous: true
requirements: [CFG-03]
must_haves:
  truths:
    - "Operators can import reviewed XML immediately through `Relyra.Metadata.import_xml/3` per D-07 and D-10."
    - "Operators can register a remote HTTPS metadata source separately from import per D-06 and D-08."
    - "Imported metadata collapses multiple SSO endpoints to one deterministic runtime value that matches the current redirect-based login path."
  artifacts:
    - path: "lib/relyra/metadata.ex"
      provides: "Public metadata API for import and source-registration verbs"
    - path: "lib/relyra/metadata/parser.ex"
      provides: "Metadata-specific XML parser seam"
    - path: "lib/relyra/metadata/candidate.ex"
      provides: "Pure normalized metadata candidate struct"
  key_links:
    - from: "lib/relyra/metadata/import.ex"
      to: "lib/relyra/ecto/metadata_apply.ex"
      via: "validated candidate handed to apply boundary"
      pattern: "MetadataApply\\.apply_revision"
    - from: "lib/relyra/metadata/source_registry.ex"
      to: "lib/relyra/ecto/metadata_source.ex"
      via: "register_source persists HTTPS metadata URL without fetching"
      pattern: "MetadataSource"
---

<objective>
Add the local XML import and source-registration APIs that Phase 09 exposes publicly, while keeping parse/normalize logic metadata-specific and deterministic.

Purpose: establish the operator-facing verbs and candidate normalization rules that feed the persistence foundation from 09-01 and the apply boundary from 09-02.
Output: `Relyra.Metadata` public API, metadata parser and candidate modules, source registration service, and Wave 0 import tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-metadata-import-export-refresh/09-CONTEXT.md
@.planning/phases/09-metadata-import-export-refresh/09-RESEARCH.md
@.planning/phases/09-metadata-import-export-refresh/09-PATTERNS.md
@.planning/phases/09-metadata-import-export-refresh/09-VALIDATION.md
@.planning/phases/08-resolver-adapter-snapshotting/08-CONTEXT.md
@.planning/phases/09-metadata-import-export-refresh/09-02-PLAN.md

<interfaces>
From lib/relyra/connection.ex:
```elixir
defstruct [:connection_id, :idp_entity_id, :sp_entity_id, :acs_url, :idp_sso_url, :idp_certificates, ...]
```

From lib/relyra/protocol/authn_request.ex:
```elixir
@default_protocol_binding "urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST"
required_field(connection, [:destination, :idp_sso_url], "destination")
```

From lib/relyra/phoenix/controllers/login_controller.ex:
```elixir
redirect_to_idp(conn, connection.idp_sso_url, redirect_params)
```
</interfaces>
</context>

<tasks>

<task type="auto" id="09-03-T01" tdd="true">
  <name>Task 1: Build metadata-specific import parsing and deterministic candidate normalization</name>
  <read_first>
    - /Users/jon/projects/relyra/.planning/phases/09-metadata-import-export-refresh/09-VALIDATION.md
    - /Users/jon/projects/relyra/.planning/phases/09-metadata-import-export-refresh/09-RESEARCH.md
    - /Users/jon/projects/relyra/lib/relyra/metadata.ex
    - /Users/jon/projects/relyra/lib/relyra/protocol/authn_request.ex
    - /Users/jon/projects/relyra/lib/relyra/connection.ex
    - /Users/jon/projects/relyra/test/security/xml/error_atoms_test.exs
  </read_first>
  <files>lib/relyra/metadata.ex, lib/relyra/metadata/candidate.ex, lib/relyra/metadata/import.ex, lib/relyra/metadata/parser.ex, test/relyra/metadata_test.exs</files>
  <behavior>
    - Test 1: `Relyra.Metadata.import_xml/3` accepts XML bytes plus `connection_id` and `repo`, parses IdP metadata through a metadata-specific seam, and returns `{:ok, revision}` only after apply succeeds.
    - Test 2: malformed XML, wrong root element, missing entity ID, missing SSO service, and missing certificates map to stable `%Relyra.Error{}` tuples instead of response-parser atoms reused accidentally.
    - Test 3: malformed or validation-failed local XML imports still create durable metadata revision rows before returning their typed errors, satisfying D-18 even when live apply never runs.
    - Test 4: when metadata publishes multiple `SingleSignOnService` endpoints, normalization selects the first `HTTP-Redirect` binding URL; if none exists, the first `HTTP-POST` binding URL; if neither binding exists, the first remaining `SingleSignOnService` URL in document order.
  </behavior>
  <action>Create a new public metadata candidate pipeline. Implement `Relyra.Metadata.import_xml/3` in `lib/relyra/metadata.ex` using the tuple-first public API and `with` flow mapped in `09-PATTERNS.md` from `lib/relyra/ecto/connections.ex`, and delegate to `Relyra.Metadata.Import.import_xml/3` in `lib/relyra/metadata/import.ex`. Build the parser seam in the fail-closed style mapped from `lib/relyra/security/xml/pure_beam.ex` without reusing its response-root assumptions. `Relyra.Metadata.Import.import_xml/3` must parse XML into a pure `%Relyra.Metadata.Candidate{}` struct, use the `record_attempt/4` helper from 09-02 to persist a durable revision row for malformed XML, missing entity ID, missing SSO service, missing certificates, or other validation failures before returning an error, and call `Relyra.Ecto.MetadataApply.apply_revision/4` only when the candidate is valid. Candidate fields must include `idp_entity_id`, `idp_sso_url`, `sso_binding`, `certificate_pems`, `certificate_fingerprints`, `source_kind`, and a redaction-safe `trust_summary`, and the pure struct/type shape should follow the value-object pattern mapped in `09-PATTERNS.md` from `lib/relyra/connection.ex`. Keep normalization explicitly compatible with the shared \"Provider defaults after normalization\" pattern from `09-PATTERNS.md`: the candidate must preserve or carry forward `provider_preset`-compatible fields so apply-time runtime shaping still flows through the same `Relyra.Provider` defaults and canonical `idp_certificates` expectations as the Phase 08 snapshot path. Encode the exact endpoint rule in code comments and tests: prefer `urn:oasis:names:tc:SAML:2.0:bindings:HTTP-Redirect`, else `urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST`, else first remaining `SingleSignOnService` by XML document order. This rule exists because the current login path always redirects to `connection.idp_sso_url`; imported metadata must resolve to the endpoint shape runtime already uses. Import must mark revision attempts with `source_kind: :xml_import`, `trigger: :manual_import`, and distinct actor/cause fields per D-18 and D-22.</action>
  <acceptance_criteria>
    - `rg -n "def import_xml" lib/relyra/metadata.ex lib/relyra/metadata/import.ex` shows the public entrypoint and delegation path for `Relyra.Metadata.import_xml/3`.
    - `rg -n "record_attempt|manual_import|xml_import" lib/relyra/metadata.ex lib/relyra/metadata/import.ex test/relyra/metadata_test.exs` shows durable failed-attempt logging for local XML import paths.
    - `rg -n "Provider defaults after normalization|provider_preset|idp_certificates" .planning/phases/09-metadata-import-export-refresh/09-03-PLAN.md lib/relyra/metadata/import.ex lib/relyra/metadata/candidate.ex` shows normalization compatibility with the shared provider-default pattern.
    - `rg -n "HTTP-Redirect|HTTP-POST|SingleSignOnService" lib/relyra/metadata/import.ex lib/relyra/metadata/parser.ex test/relyra/metadata_test.exs` shows the exact deterministic endpoint rule in code and tests.
    - `rg -n "defstruct .*idp_entity_id|sso_binding|certificate_fingerprints|trust_summary" lib/relyra/metadata/candidate.ex` shows a pure candidate struct with normalized fields only.
    - `rg -n "wrong root|missing SSO service|malformed XML" test/relyra/metadata_test.exs` shows explicit typed-failure coverage for metadata-specific parsing errors.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/relyra/metadata_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Local XML import exists as the primary onboarding path, uses a metadata-specific parser seam, and collapses multiple SSO endpoints into one predictable runtime URL.</done>
</task>

<task type="auto" id="09-03-T02" tdd="true">
  <name>Task 2: Add explicit remote source registration without implicit fetches</name>
  <read_first>
    - /Users/jon/projects/relyra/.planning/phases/09-metadata-import-export-refresh/09-VALIDATION.md
    - /Users/jon/projects/relyra/.planning/phases/09-metadata-import-export-refresh/09-CONTEXT.md
    - /Users/jon/projects/relyra/lib/relyra/metadata.ex
    - /Users/jon/projects/relyra/lib/relyra/provider.ex
    - /Users/jon/projects/relyra/lib/relyra/log.ex
    - /Users/jon/projects/relyra/.planning/phases/09-metadata-import-export-refresh/09-01-PLAN.md
  </read_first>
  <files>lib/relyra/metadata.ex, lib/relyra/metadata/source_registry.ex, test/relyra/metadata_test.exs</files>
  <behavior>
    - Test 1: `Relyra.Metadata.register_source/3` accepts `connection_id`, a remote HTTPS metadata URL, actor/cause fields, and `repo`, then upserts or replaces the single registered metadata source row for that connection without performing any network request.
    - Test 2: non-HTTPS URLs, blank URLs, and missing repo configuration return typed errors.
    - Test 3: source registration leaves `active_metadata_revision_id`, `last_known_good_metadata_revision_id`, and runtime resolver behavior unchanged until a later explicit refresh applies a revision.
  </behavior>
  <action>Create `Relyra.Metadata.register_source/3` in `lib/relyra/metadata.ex` as the public API for D-08 and D-10, delegating to `Relyra.Metadata.SourceRegistry.register_source/3`. Follow the public API error/option discipline mapped in `09-PATTERNS.md` from `lib/relyra/ecto/connections.ex` for `lib/relyra/metadata.ex`, and mirror the source-schema persistence posture mapped from `lib/relyra/ecto/certificate.ex` for `lib/relyra/metadata/source_registry.ex`. Accept a map with `url`, `actor`, `cause`, and optional metadata labels; persist a `Relyra.Ecto.MetadataSource` row against the connection from 09-01, storing `last_outcome: :registered` and redaction-safe metadata only. Because 09-01 locks one remote refresh source per connection, `register_source/3` must replace or upsert the existing source row for that connection rather than accumulate multiple competing rows. Return typed tuples consistent with the rest of the codebase. Do not call `Req`, `HTTPoison`, or any fetch helper here; registration is a write-only provenance action. Add regression assertions in `test/relyra/metadata_test.exs` that resolver-based runtime reads still come only from the applied connection aggregate, not from newly registered source rows. Keep source semantics distinct from import: `import_xml/3` applies reviewed XML now, while `register_source/3` only remembers where a later `refresh/2` may fetch from.</action>
  <acceptance_criteria>
    - `rg -n "def register_source|last_outcome|registered" lib/relyra/metadata.ex lib/relyra/metadata/source_registry.ex` shows a public API and registration outcome persistence.
    - `rg -n "replace|upsert|connection_record_id" lib/relyra/metadata/source_registry.ex test/relyra/metadata_test.exs` shows deterministic single-source-per-connection registration semantics.
    - `rg -n "HTTPoison|Req\\." lib/relyra/metadata/source_registry.ex` returns no matches, proving registration never fetches.
    - `rg -n "runtime resolver|active_metadata_revision_id|last_known_good_metadata_revision_id" test/relyra/metadata_test.exs` shows explicit assertions that registration alone does not alter runtime state.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/relyra/metadata_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The public write-side APIs distinguish immediate XML import from remote-source registration, and source registration never performs a live fetch or changes runtime trust by itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local XML input -> metadata parser | untrusted IdP-controlled XML crosses into normalization logic |
| operator source registration -> metadata source ledger | untrusted operator input becomes future fetch provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | T | `lib/relyra/metadata/parser.ex` | mitigate | Use a metadata-specific parser seam with explicit root and field validation, not the response parser contract, and fail with typed errors on malformed or incomplete metadata. |
| T-09-03-02 | S | `lib/relyra/metadata/source_registry.ex` | mitigate | Require HTTPS URLs and actor/cause provenance for source registration so future refresh attempts have attributable origin per D-08 and D-18. |
| T-09-03-03 | D | `lib/relyra/metadata/import.ex` | mitigate | Collapse multiple SSO endpoints by one documented priority rule to avoid nondeterministic endpoint selection that could destabilize login behavior. |
</threat_model>

<verification>
- `mix test test/relyra/metadata_test.exs --warnings-as-errors`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- `Relyra.Metadata.import_xml/3` exists and applies local XML through a metadata-specific parser and candidate pipeline.
- `Relyra.Metadata.register_source/3` exists and persists remote HTTPS metadata sources without fetching.
- The endpoint-selection rule is documented in tests and implementation so import and refresh choose the same effective `idp_sso_url`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-metadata-import-export-refresh/09-03-SUMMARY.md`
</output>
