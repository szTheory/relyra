---
phase: 20-bulk-operations
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - lib/relyra/live_admin/connections_live.ex
  - lib/relyra/live_admin/components/connection_list.ex
  - test/relyra/live_admin/connections_live_test.exs
autonomous: true
requirements:
  - CFG-07
must_haves:
  truths:
    - "Operator can select multiple connections in the admin UI"
    - "Operator can trigger bulk actions from the connections list"
  artifacts:
    - path: "lib/relyra/live_admin/connections_live.ex"
      provides: "Bulk action event handlers"
---

<objective>
Implement the UI for bulk operations in the LiveView admin surface.
Purpose: Provide operators an interactive way to select and manage connections in batches.
Output: Updated `ConnectionsLive` and `ConnectionList` modules.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add multi-select UI to ConnectionList</name>
  <files>lib/relyra/live_admin/components/connection_list.ex, lib/relyra/live_admin/connections_live.ex</files>
  <behavior>
    - Test 1: connections list renders a checkbox for each connection.
    - Test 2: clicking a checkbox sends a `toggle_selection` event.
    - Test 3: `ConnectionsLive` tracks `selected_ids` MapSet.
  </behavior>
  <action>
    Update `Relyra.LiveAdmin.Components.ConnectionList.connection_list/1` to include a checkbox next to each connection name.
    Bind the checkbox to `phx-click="toggle_selection"` with `phx-value-connection_id`.
    Update `Relyra.LiveAdmin.ConnectionsLive.mount/3` to initialize `selected_ids: MapSet.new()`.
    Implement `handle_event("toggle_selection", %{"connection-id" => id}, socket)` to update the MapSet.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Multi-selection UI is functional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bulk action triggers and coordination</name>
  <files>lib/relyra/live_admin/connections_live.ex</files>
  <behavior>
    - Test 1: A "Bulk Actions" menu appears when one or more IDs are selected.
    - Test 2: Selecting "Enable" from bulk menu calls `BulkActions.run/4` with `Connections.enable/2`.
    - Test 3: Success/Failure feedback is shown via flashes.
  </behavior>
  <action>
    Add a conditional "Bulk Actions" menu to the `ConnectionsLive` template or a shared component.
    The menu should have options for "Bulk Enable", "Bulk Disable", and "Bulk Refresh Metadata".
    Implement `handle_event("bulk_action", %{"action" => action}, socket)`:
    - Map action string to domain functions (`Connections.enable/2`, etc.).
    - Call `BulkActions.run/4`.
    - Gather results and show a summary flash (e.g., "Updated 5 connections, 1 failed.").
    - Clear `selected_ids` on success.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Bulk actions are fully operational in the UI.</done>
</task>
</tasks>
