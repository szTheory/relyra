---
phase: 56-documentation-and-evidence-polish
plan: 03
type: execute
wave: 3
depends_on: ["56-01", "56-02"]
files_modified:
  - test/docs/demo_guide_drift_test.exs
  - mix.exs
autonomous: true
requirements: [DOCS-02]
must_haves:
  truths:
    - "D-10: a bidirectional drift gate (test/docs/demo_guide_drift_test.exs) asserts the scripts/demo subcommand set matches the demo README's documented scripts/demo commands — runtime-extracted from the `case \"$COMMAND\"` arms, never hardcoded, scanning only triple-backtick-fenced bash fenced blocks, handling {:error, :enoent} as an empty doc set."
    - "D-11: the drift test runs under the ci.docs alias as its own dedicated `cmd mix test ... --warnings-as-errors` line right after the logout_recipe line, preceded by a `cmd test -f demo/ledger_loop/README.md` presence guard (NOT ci.demo_app, which runs --cd demo/ledger_loop and cannot read repo-root scripts/demo)."
    - "Phase 30 hollow-gate invariant preserved: the new drift test is its own `cmd mix test` process line."
  artifacts:
    - path: "test/docs/demo_guide_drift_test.exs"
      provides: "Bidirectional scripts/demo subcommand drift gate for the demo README"
      contains: "scripts/demo"
    - path: "mix.exs"
      provides: "ci.docs alias wired with the presence guard + dedicated drift-test cmd line"
      contains: "demo_guide_drift_test.exs"
  key_links:
    - from: "test/docs/demo_guide_drift_test.exs"
      to: "scripts/demo"
      via: "runtime extraction of subcommands from the case \"$COMMAND\" arms"
      pattern: "scripts/demo"
    - from: "test/docs/demo_guide_drift_test.exs"
      to: "demo/ledger_loop/README.md"
      via: "scan of scripts/demo <token> mentions inside triple-backtick-fenced bash fenced blocks"
      pattern: "demo/ledger_loop/README.md"
---

<objective>
Add the tightly-scoped doc-drift gate (D-10) that keeps the demo README's `scripts/demo`
subcommand list in sync with the script itself, and wire it into the `ci.docs` alias (D-11)
preserving the Phase 30 hollow-gate invariant.

Purpose: the `scripts/demo` subcommand set is the unique closed enumerable surface evaluators
copy-paste and the most likely to be renamed without a doc update (freshly added in Phase 55).
A bidirectional gate stops the demo README's commands (DOCS-02) from rotting.

Output: test/docs/demo_guide_drift_test.exs, plus a ci.docs alias edit in mix.exs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-documentation-and-evidence-polish/56-CONTEXT.md
@.planning/phases/56-documentation-and-evidence-polish/56-01-SUMMARY.md
@.planning/phases/56-documentation-and-evidence-polish/56-02-SUMMARY.md

<interfaces>
Template tests to model on (read both):
- test/docs/troubleshooting_drift_test.exs — bidirectional MapSet.difference set-diff pattern,
  {:error, :enoent} handling for a missing doc, named-source failure formatting.
- test/docs/logout_recipe_drift_test.exs — fenced-code-block scoping via
  `~r/triple-backtick-fenced elixir\n(.*?)triple-backtick/s` (adapt to triple-backtick-fenced bash), runtime introspection (never hardcode literals),
  dedicated-cmd-mix-test CI-lane convention.

scripts/demo defines its subcommands as `case "$COMMAND"` arms; the arms to enumerate are the
literal labels `doctor)`, `up)`, `reset)`, `test)`, `urls)`, `down)` (excluding the `*)` default
and any helper-function names). Extract these at runtime by reading scripts/demo and matching the
case arm labels — do NOT hardcode the six names in the test.

The demo README (demo/ledger_loop/README.md) documents subcommands as `scripts/demo <token>`
inside triple-backtick-fenced bash fenced blocks (guaranteed by Plan 01). Scan only triple-backtick-fenced bash fenced blocks; ignore
prose mentions to avoid brittleness.

mix.exs ci.docs alias (current order, last drift lines):
  "cmd mix test test/docs/troubleshooting_drift_test.exs --warnings-as-errors",
  "cmd mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors",
The new presence guard + drift line go immediately after the logout_recipe line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the bidirectional scripts/demo subcommand drift gate (D-10)</name>
  <read_first>
    - test/docs/demo_guide_drift_test.exs (the file being created — does not yet exist)
    - test/docs/troubleshooting_drift_test.exs (bidirectional set-diff + {:error, :enoent} template)
    - test/docs/logout_recipe_drift_test.exs (fenced-block scoping + runtime-introspection template)
    - scripts/demo (the `case "$COMMAND"` arms = the canonical subcommand set, extracted at runtime)
    - demo/ledger_loop/README.md (the doc whose triple-backtick-fenced bash fenced blocks are scanned)
  </read_first>
  <behavior>
    - Canonical set extraction: reading scripts/demo at runtime yields exactly {doctor, up, reset, test, urls, down} from the case arms (the `*` default arm and helper function names like check_port are excluded). The list is NOT hardcoded — renaming an arm in scripts/demo changes the canonical set the test sees.
    - Doc set extraction: only `scripts/demo <token>` mentions inside triple-backtick-fenced bash fenced blocks of demo/ledger_loop/README.md count; a prose mention outside a bash fence does NOT count.
    - Missing-in-doc: a subcommand present in scripts/demo but absent from the README's bash fences fails the test with a named-subcommand message.
    - Stale-in-doc: a `scripts/demo <token>` documented in the README whose token is not a real script subcommand fails the test.
    - Missing README: File.read returning {:error, :enoent} is treated as an empty doc set (clean missing-in-doc failures), matching the troubleshooting template.
    - Green state: with the Plan 01 README documenting all six subcommands in bash fences, the test passes.
  </behavior>
  <action>
    Create test/docs/demo_guide_drift_test.exs modeled on troubleshooting_drift_test.exs (bidirectional MapSet.difference both directions, {:error, :enoent} → empty set) and logout_recipe_drift_test.exs (fenced-block scoping, runtime introspection, no hardcoded literals). The test must: (1) read scripts/demo and runtime-extract the subcommand set from the `case "$COMMAND"` arms — match the arm labels (e.g. a regex over lines like `  doctor)` / `  up)` capturing the token before `)`), excluding the `*)` default arm and any function definitions; NEVER hardcode the six names (project convention D-05). (2) read demo/ledger_loop/README.md, extract only triple-backtick-fenced bash fenced block bodies, and scan them for `scripts/demo <token>` mentions to build the documented set; handle {:error, :enoent} as an empty set. (3) assert bidirectional parity: every script subcommand appears in the README bash fences (missing-in-doc) AND every documented `scripts/demo <token>` is a real subcommand (stale-in-doc), using MapSet.difference both ways with named, actionable failure messages mirroring the template vocabulary. Add a module @moduledoc explaining the D-10 scope (subcommands only — routes/creds are prose, not contracts) and the D-11 ci.docs lane rationale. Do NOT assert routes or seeded creds.
  </action>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - test/docs/demo_guide_drift_test.exs exists and `mix test test/docs/demo_guide_drift_test.exs --warnings-as-errors` exits 0 against the Plan 01 README.
    - The test reads scripts/demo at runtime — `grep -q 'scripts/demo' test/docs/demo_guide_drift_test.exs` succeeds and there is NO hardcoded literal list of the six subcommand names (`! grep -qE 'doctor.*up.*reset.*test.*urls.*down' test/docs/demo_guide_drift_test.exs`).
    - The test scopes the README scan to bash fenced blocks (a triple-backtick triple-backtick-fenced bash triple-backtick block regex is present).
    - The test handles `{:error, :enoent}` for a missing README as an empty doc set.
    - Assertions run in both directions (two MapSet.difference / equivalent checks).
  </acceptance_criteria>
  <done>The drift gate is live and green: scripts/demo subcommands and the demo README's documented commands are in bidirectional sync, runtime-extracted.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the drift test into the ci.docs alias with a README presence guard (D-11)</name>
  <read_first>
    - mix.exs (aliases/0 ci.docs list — locate the `logout_recipe_drift_test.exs` line; note the existing `cmd test -f` presence-guard idiom)
    - test/docs/demo_guide_drift_test.exs (the suite being wired, from Task 1)
    - test/security/ci_gate_integrity_test.exs (the meta-gate enforcing the one-dedicated-cmd-mix-test-per-suite hollow-gate invariant — confirm the new line conforms)
  </read_first>
  <action>
    Edit the `ci.docs` alias in mix.exs aliases/0. Immediately AFTER the `"cmd mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors",` line, insert two new steps in order: first a presence guard `"cmd test -f demo/ledger_loop/README.md",` (consistent with the existing `cmd test -f` guards), then the dedicated drift line `"cmd mix test test/docs/demo_guide_drift_test.exs --warnings-as-errors",`. Keep it as its OWN dedicated `cmd mix test` line — do NOT fold it into any bare `test` step (Phase 30 hollow-gate invariant). Do NOT add it to ci.demo_app — that lane runs every step with `--cd demo/ledger_loop` and physically cannot read the repo-root scripts/demo (D-11 rationale). Run `mix ci.docs` to confirm the full lane is green end to end.
  </action>
  <verify>
    <automated>grep -q 'cmd mix test test/docs/demo_guide_drift_test.exs --warnings-as-errors' mix.exs && grep -q 'cmd test -f demo/ledger_loop/README.md' mix.exs && mix ci.docs</automated>
  </verify>
  <acceptance_criteria>
    - mix.exs ci.docs contains, in order, the `cmd test -f demo/ledger_loop/README.md` presence guard then the `cmd mix test test/docs/demo_guide_drift_test.exs --warnings-as-errors` line, both immediately after the logout_recipe_drift_test line.
    - The drift line is a dedicated `cmd mix test` process line (hollow-gate invariant preserved).
    - The drift test is NOT present in the ci.demo_app alias.
    - `mix ci.docs` exits 0.
  </acceptance_criteria>
  <done>The demo-guide drift gate runs in the ci.docs lane behind a README presence guard, as its own process, and the lane is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → maintainer | A hollow or mis-laned gate would ship a rotted doc green, eroding trust in the evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-07 | Tampering (hollow gate) | ci.docs alias new line | mitigate | New drift test is its own dedicated `cmd mix test` process line (Phase 30 invariant); ci_gate_integrity_test.exs read first to confirm conformance; `mix ci.docs` must exit 0. |
| T-56-08 | Repudiation (silent drift) | test/docs/demo_guide_drift_test.exs | mitigate | Bidirectional set-diff with runtime extraction (D-10) means a renamed subcommand or stale doc command fails CI rather than silently diverging. |
| T-56-09 | Spoofing (wrong lane) | lane selection | mitigate | Test wired to ci.docs (repo-root cwd) not ci.demo_app (--cd demo/ledger_loop), so scripts/demo is reachable; presence guard prevents a confusing failure when the README is absent. |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages. No legitimacy gate required. |
</threat_model>

<verification>
- `mix test test/docs/demo_guide_drift_test.exs --warnings-as-errors` exits 0.
- `mix ci.docs` exits 0 with the new presence guard + dedicated drift line.
- The drift test runtime-extracts subcommands from scripts/demo (no hardcoded list) and scans only bash fences in the demo README.
</verification>

<success_criteria>
A live, runtime-extracted, bidirectional drift gate keeps the demo README's scripts/demo command
surface honest (DOCS-02), wired into ci.docs as its own process per the hollow-gate invariant.
</success_criteria>

<output>
Create `.planning/phases/56-documentation-and-evidence-polish/56-03-SUMMARY.md` when done.
</output>
