---
phase: 35-signed-authnrequests-adfs-preset
plan: 07
type: execute
wave: 3
depends_on:
  - 35-01
  - 35-02
  - 35-03
  - 35-04
  - 35-05
  - 35-06
files_modified:
  - test/security/authn_request_signing_test.exs
  - test/fixtures/security/authn_request_signing/golden_authnrequest.xml
  - test/fixtures/security/authn_request_signing/golden_redirect.txt
  - test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt
  - test/fixtures/security/authn_request_signing/PROVENANCE.md
autonomous: true
requirements:
  - AUTHN-01
  - AUTHN-02
tags:
  - phase-35
  - authn-01
  - authn-02
  - adversarial-corpus
  - golden-fixtures

must_haves:
  truths:
    - "test/security/authn_request_signing_test.exs is a NEW, SEPARATE file — adversarial_crypto_test.exs is NOT modified (CLAUDE.md 'Never weaken')"
    - "The suite declares @moduletag :authn_request_signing — distinct tag from adversarial_crypto so Plan 30 hollow-gate fix can wire it as its OWN `cmd mix test` line in Plan 08"
    - "Row 1 (:row_golden) — Relyra.start_login/3 with :rfc3986_upper encoding produces bytes byte-identical to File.read!(\"test/fixtures/security/authn_request_signing/golden_redirect.txt\") (load-bearing AUTHN-01 byte-equality)"
    - "Row 2 (:row_adfs_lower) — Relyra.start_login/3 with :adfs_lower encoding produces bytes byte-identical to File.read!(\"test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt\") — and the two golden files differ ONLY in percent-encoded hex case"
    - "Row 3 (:row_reserialization_regression) — re-signing the OASIS octets via URI.encode_query/1 of a %{...} map produces a signature that MUST != the Row 1 golden signature (mutation test — proves T-35-01 / OASIS §3.4.4.1 lines 620-625 raw-octet invariant)"
    - "Row 4 (:row_roundtrip_verify) — :public_key.verify/4 of the Row 1 signed octets + signature against the committed PEM's derived public key returns true (proves Plan 01 sign_redirect_query/3 is verifiable by a peer)"
    - "Row 5 (:row_toggle_off_noop) — connection with sign_authn_requests: false (default) returns {:ok, %{redirect_params: %{...}}} with NO 'SigAlg' / 'Signature' / 'redirect_query' keys"
    - "Each row pins an EXACT %{...} or %Error{type: ...} shape (Phase 30 adversarial-crypto discipline) — bare {:ok, _} / {:error, _} are forbidden"
    - "The corpus reuses production code paths: Rows 1, 2, 5 call Relyra.start_login/3 (the production entry); Row 3 calls :public_key.sign/3 directly with the committed PEM (no parallel signer module); Row 4 calls :public_key.verify/4 directly (single-signer-discipline anti-divergent-signer guarantee)"
    - "Signing key material is loaded via Application.put_env(:relyra, :sp_signing_key_pem, File.read!(\"test/fixtures/security/authn_request_signing/golden_signing_key.pem\")) in setup; on_exit deletes the env to prevent test bleed"
    - "PROVENANCE.md records: golden_signing_key.pem sha256 + byte count; each golden file's sha256 + byte count; Elixir + OTP versions; spec citation chain (OASIS Bindings §3.4.4.1 lines 601-605/624-625, RFC 1951, RFC 3986 §2.1, python3-saml lowercase_urlencoding); verbatim mint procedure from RESEARCH.md §2; no-trailing-newline rule"
    - "golden_authnrequest.xml is committed and uses FIXED ID + IssueInstant (no :crypto.strong_rand_bytes/1 in the XML) so re-mints reproduce"
    - "Neither golden_redirect.txt nor golden_redirect_adfs.txt has a trailing newline (Phase 28 PROVENANCE Pitfall 4/6: last byte is whatever the final percent-encoded value's last byte happens to be)"
    - "Phase 30 hollow-gate fix is preserved — the new suite declares its OWN @moduletag and Plan 08 will wire it as its OWN `cmd mix test` line; no collapse into a multi-file test step"
    - "mix test --warnings-as-errors stays green across the full project (Row 5 smoke + suite-wide green is the proof that existing Okta/Google/Entra/Ping/OneLogin provider tests do NOT regress)"
  artifacts:
    - path: "test/security/authn_request_signing_test.exs"
      provides: "5-row adversarial corpus for AUTHN-01 (Rows 1-4) + AUTHN-02 (Row 5)"
      contains: "@moduletag :authn_request_signing"
    - path: "test/fixtures/security/authn_request_signing/golden_authnrequest.xml"
      provides: "Canonical input AuthnRequest XML with fixed ID + IssueInstant for deterministic re-minting"
      contains: "<samlp:AuthnRequest"
    - path: "test/fixtures/security/authn_request_signing/golden_redirect.txt"
      provides: "Row 1 expected wire bytes (:rfc3986_upper encoding) — bit-for-bit golden"
      contains: "SAMLRequest="
    - path: "test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt"
      provides: "Row 2 expected wire bytes (:adfs_lower encoding) — bit-for-bit golden; differs from Row 1 ONLY in percent-encoded hex case"
      contains: "SAMLRequest="
    - path: "test/fixtures/security/authn_request_signing/PROVENANCE.md"
      provides: "Fingerprint + byte-count manifest + spec citation chain + mint procedure (Phase 28 fixture-commit pattern)"
      contains: "sha256"
  key_links:
    - from: "test/security/authn_request_signing_test.exs"
      to: "test/fixtures/security/authn_request_signing/golden_redirect.txt"
      via: "File.read! inside Row 1 (:row_golden) assert byte-equality"
      pattern: "File\\.read!\\(.+golden_redirect\\.txt"
    - from: "test/security/authn_request_signing_test.exs"
      to: "test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt"
      via: "File.read! inside Row 2 (:row_adfs_lower) assert byte-equality"
      pattern: "File\\.read!\\(.+golden_redirect_adfs\\.txt"
    - from: "test/security/authn_request_signing_test.exs"
      to: "test/fixtures/security/authn_request_signing/golden_signing_key.pem"
      via: "setup block: Application.put_env(:relyra, :sp_signing_key_pem, File.read!(...))"
      pattern: "Application\\.put_env.+:sp_signing_key_pem"
    - from: "test/security/authn_request_signing_test.exs"
      to: "Relyra.start_login/3"
      via: "Rows 1, 2, 5 drive the production entry point — no parallel code path"
      pattern: "Relyra\\.start_login"
    - from: "test/security/authn_request_signing_test.exs"
      to: ":public_key.sign/3"
      via: "Row 3 mutation: directly call :public_key.sign/3 over re-serialized octets to prove signature divergence"
      pattern: ":public_key\\.sign\\("
    - from: "test/security/authn_request_signing_test.exs"
      to: ":public_key.verify/4"
      via: "Row 4: verify Row 1 signature against pubkey derived from committed PEM"
      pattern: ":public_key\\.verify\\("
---

<objective>
Land the 5-row adversarial corpus that is the LOAD-BEARING proof of AUTHN-01: for a fixed
AuthnRequest XML + fixed RelayState + the committed `golden_signing_key.pem`, `Relyra.start_login/3`
produces a redirect query whose bytes are byte-identical to a committed golden file, AND whose
Signature parameter verifies under `:public_key.verify/4`. Row 3 is the mutation test that pins
the OASIS §3.4.4.1 raw-octet invariant against future regression. Row 5 pins AUTHN-02's
default-false toggle-off no-op contract.

Purpose: Without this corpus, AUTHN-01 cannot be claimed (CONTEXT.md D-19/D-20/D-21). The corpus
is the structural guard against the CVE-class re-serialization auth-bypass (T-35-01) and against
the ADFS lowercase-hex interop bug (T-35-05); it also pins single-signer discipline (T-35-06)
by exercising production code paths instead of a parallel signer module. The corpus is a NEW,
SEPARATE file — `test/security/xml/adversarial_crypto_test.exs` is PERMANENTLY frozen per
CLAUDE.md "Never weaken" and is not touched by this plan.

Output: One new ExUnit test file at `test/security/authn_request_signing_test.exs` with 5
corpus rows under `@moduletag :authn_request_signing`; four new committed fixtures under
`test/fixtures/security/authn_request_signing/` (`golden_authnrequest.xml`,
`golden_redirect.txt`, `golden_redirect_adfs.txt`, `PROVENANCE.md`). The `golden_signing_key.pem`
already exists (Plan 01 Task 3). The new corpus's wiring into `mix ci.security` + the Phase 30
hollow-gate meta-gate is owned by Plan 08; this plan strictly produces the corpus file + fixtures
and proves them green in isolation + via the full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/phases/35-signed-authnrequests-adfs-preset/35-CONTEXT.md
@.planning/phases/35-signed-authnrequests-adfs-preset/35-RESEARCH.md
@.planning/phases/35-signed-authnrequests-adfs-preset/35-PATTERNS.md
@.planning/phases/35-signed-authnrequests-adfs-preset/35-VALIDATION.md
@.planning/phases/35-signed-authnrequests-adfs-preset/35-01-PLAN.md
@.planning/phases/35-signed-authnrequests-adfs-preset/35-02-PLAN.md
@test/security/xml/adversarial_crypto_test.exs
@test/fixtures/security/xml/parser_differential_and_c14n/PROVENANCE.md

<interfaces>
<!-- Contracts delivered by upstream plans this corpus depends on (Wave 0 -> Wave 2): -->

From lib/relyra/security/signature.ex (Plan 01):
  @spec sign_redirect_query(binary(), String.t(), keyword()) ::
          {:ok, binary()} | {:error, Error.t()}
  def sign_redirect_query(octets, signature_method, opts \\ [])
  # Signs raw octets verbatim; reads :sp_signing_key_pem from Application env unless
  # :signing_key_pem opt overrides; returns URL-encoded base64 signature string.

From lib/relyra/security/algorithm_policy.ex (Plan 01):
  @spec signing_digest_atom(term()) ::
          {:ok, :sha256 | :sha384 | :sha512}
          | {:error, :unsupported_signing_algorithm | :unknown_signing_algorithm}
  def signing_digest_atom(uri)

From lib/relyra/protocol/binding.ex (Plan 02):
  @spec encode_redirect(binary(), binary() | nil, keyword()) ::
          {:ok, map()} | {:error, Error.t()}
  # When opts[:sign] == true, returns {:ok, %{redirect_query: bytes}}.
  # When opts[:sign] in [false, nil], returns {:ok, %{"SAMLRequest" => _, ...}}.
  # opts: :sign, :signing_key_pem, :signature_method, :encoding (:rfc3986_upper | :adfs_lower)

From lib/relyra/ecto/connection.ex (Plan 03):
  - schema field `:sign_authn_requests` (boolean; existing from Phase 32)
  - schema field `:signed_request_encoding` (Ecto.Enum [:rfc3986_upper, :adfs_lower]; new)

From lib/relyra/provider/adfs.ex (Plan 04):
  - Relyra.Provider.ADFS.default_config/0 returns the locked D-15 map with
    sign_authn_requests: true and signed_request_encoding: :adfs_lower.

From lib/relyra.ex (Plan 05):
  - Relyra.start_login/3 returns {:ok, %{redirect_query: bytes}} for signed connections
  - Relyra.start_login/3 returns {:ok, %{redirect_params: %{...}}} for unsigned (default)
  - Two return shapes are MUTUALLY EXCLUSIVE; Row 5 pins this.

From test/fixtures/security/authn_request_signing/golden_signing_key.pem (Plan 01 Task 3):
  - Committed RSA-2048 PEM; deterministic; SHA-256 fingerprint recorded in Plan 01 SUMMARY.
  - Load via File.read!/1; decode via :public_key.pem_decode/1 + :public_key.pem_entry_decode/1.

<!-- External spec verbatim (cited verbatim in PROVENANCE.md): -->
OASIS SAML 2.0 Bindings §3.4.4.1 lines 601-605 — signed octet template:
  "SAMLRequest=value&RelayState=value&SigAlg=value" (in that exact order)
OASIS SAML 2.0 Bindings §3.4.4.1 lines 620-625 — the raw-octet invariant:
  "It is not sufficient to re-encode the parameters after they have been processed
   by software because the resulting encoding may not match the signer's encoding."
   "...if there is no RelayState value, the entire parameter should be omitted from
   the signature computation (and not included as an empty parameter name)."
RFC 1951 — raw DEFLATE compressed data format.
RFC 3986 §2.1 — "URI producers and normalizers should use uppercase hexadecimal digits
                 for all percent-encodings."
python3-saml lowercase_urlencoding (utils.py escape_url) — regex post-process
  `re.sub(r"%[A-F0-9]{2}", lambda m: m.group(0).lower(), encoded)`.
</interfaces>

<corpus_row_specs>
<!-- Each row's structural model is adversarial_crypto_test.exs (one describe per row, exact
     %Error{}/%{} pinning, FakeIdP-style setup). Drawn verbatim from RESEARCH.md §2 "Corpus Row
     Detail" and CONTEXT.md D-19. -->

Row 1 — :row_golden (positive control, :rfc3986_upper):
  Input: connection with sign_authn_requests: true, signed_request_encoding: :rfc3986_upper;
         golden_authnrequest.xml; RelayState "rs_relyra_phase35_golden"; committed PEM in app env.
  Assert: {:ok, %{redirect_query: bytes}} = Relyra.start_login(...)
  Assert: bytes == File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt")

Row 2 — :row_adfs_lower (variant, :adfs_lower):
  Input: Row 1 inputs EXCEPT signed_request_encoding: :adfs_lower.
  Assert: bytes == File.read!("test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt")
  Assert (structural delta): the two golden files differ ONLY in percent-encoded hex case
    (verifiable by `String.downcase(golden_redirect) == String.downcase(golden_redirect_adfs)`)

Row 3 — :row_reserialization_regression (mutation test, T-35-01 / OASIS §3.4.4.1 lines 620-625):
  Local computation (NO Relyra.start_login/3 call). Read the committed PEM. Build the SAME
  logical query as Row 1 by URI.encode_query/1 of a %{"SAMLRequest" => ..., "RelayState" => ...,
  "SigAlg" => ...} map (which is THE wrong order + map-iteration nondeterminism — exactly the
  CVE-class footgun). Sign those re-serialized octets via :public_key.sign/3 directly.
  Assert: the mutated signature != the Row 1 golden's Signature parameter (decoded from
          golden_redirect.txt after `&Signature=`).
  Why local: this row is the structural guard against someone introducing URI.encode_query/1
  into the signing path; running Relyra.start_login/3 here would defeat the test (the production
  code path correctly does NOT use URI.encode_query/1, so the resulting signature would match
  Row 1's). Row 3's mutation must happen OUTSIDE the production path.

Row 4 — :row_roundtrip_verify (peer-verify proof, T-35-06):
  Take Row 1's signed octets (everything before `&Signature=`) by re-running Row 1 and slicing
  on `&Signature=`. Decode the Signature parameter from URL-encoded base64 to raw bytes. Derive
  the public key from the committed PEM (`:public_key.pem_decode` → RSAPrivateKey → extract
  modulus/exponent → :RSAPublicKey tuple). Call :public_key.verify(octets, :sha256, sig_bytes,
  pubkey) directly — NO Signature.do_verify/4 path (that's for IdP-side XML signatures, not
  redirect-binding outbound).
  Assert: :public_key.verify returns true.

Row 5 — :row_toggle_off_noop (AUTHN-02 default-false contract):
  Input: connection with sign_authn_requests: false (the default; no signed_request_encoding).
  Assert: {:ok, %{redirect_params: params}} = Relyra.start_login(...)
  Assert: refute Map.has_key?(params, "SigAlg")
  Assert: refute Map.has_key?(params, "Signature")
  Assert: refute Map.has_key?(%{redirect_params: params}, :redirect_query)
  (The full-suite green check `mix test --warnings-as-errors` is what proves the existing
  Okta/Google/Entra/Ping/OneLogin provider tests don't regress — Task 6 runs it.)
</corpus_row_specs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/security/authn_request_signing_test.exs with 5 corpus rows (@moduletag :authn_request_signing)</name>
  <files>test/security/authn_request_signing_test.exs</files>
  <read_first>
    - test/security/xml/adversarial_crypto_test.exs (entire file — the structural model: @moduletag declaration, one describe per row, exact %Error{type:} / %{...} pinning, helper functions at the bottom, alias block at the top, `use ExUnit.Case, async: true`)
    - test/fixtures/security/xml/parser_differential_and_c14n/PROVENANCE.md (Phase 28 fixture-commit precedent — informs the shape of PROVENANCE.md created in Task 5; also informs the no-trailing-newline rule for the golden files)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-PATTERNS.md (section "test/security/authn_request_signing_test.exs — new adversarial corpus (CREATE)" lines 680-762 — full module header skeleton + setup block + Row 1 / Row 5 example shapes; section "test/fixtures/security/authn_request_signing/ — committed fixtures (CREATE)" lines 766-818 — fixture provenance)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-RESEARCH.md (§2 "Corpus Row Detail" lines 132-160 — precise specification of all 5 rows; §6 T-35-01 / T-35-05 / T-35-06 threat-row mapping; §2 "Mint procedure" lines 181-202 — informs Task 2/3)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-CONTEXT.md (D-19 — 5 corpus rows precise spec; D-20 — Phase 30 hollow-gate fix preserved via own @moduletag; D-21 — committed-fixtures + PROVENANCE.md pattern)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-VALIDATION.md (tasks 35-07-01 through 35-07-06 per-task verify-command map)
    - test/fixtures/security/authn_request_signing/golden_signing_key.pem (committed Plan 01 Task 3; loaded in setup)
    - lib/relyra/ecto/connection.ex (Phase 32 + Plan 03 schema — the sign_authn_requests + signed_request_encoding fields; the corpus uses plain maps, not full Ecto schemas, but field names must match exactly)
  </read_first>
  <action>
    Create `test/security/authn_request_signing_test.exs` declaring `defmodule Relyra.Security.AuthnRequestSigningTest`, `use ExUnit.Case, async: true`, and `@moduletag :authn_request_signing`. The module header mirrors `test/security/xml/adversarial_crypto_test.exs` lines 1-46 verbatim in shape: @moduledoc citing CLAUDE.md "Never weaken" (this is a SEPARATE file, not new rows on the inbound corpus), CONTEXT.md D-19/D-20/D-21, RESEARCH.md §2, and Phase 30 hollow-gate fix (Plan 08 wires the own `cmd mix test` line). Alias `Relyra.Error` and any test_support helpers needed (do NOT alias `Relyra.TestSupport.FakeIdP` for keypair purposes — the corpus loads the COMMITTED PEM, not FakeIdP's non-deterministic per-process key per RESEARCH.md A4 mitigation).

    Add a module-level `setup` block that: (a) reads `pem = File.read!("test/fixtures/security/authn_request_signing/golden_signing_key.pem")`; (b) calls `Application.put_env(:relyra, :sp_signing_key_pem, pem)`; (c) registers `on_exit(fn -> Application.delete_env(:relyra, :sp_signing_key_pem) end)` to prevent test bleed into other suites; (d) returns `:ok`. This setup runs once per test (ExUnit semantics) — that's fine because Application.put_env is cheap and the on_exit is process-scoped.

    Add module attributes for stable test inputs:
    - `@golden_relay_state "rs_relyra_phase35_golden"` (per CONTEXT.md D-19 / RESEARCH.md §2)
    - `@golden_xml_path "test/fixtures/security/authn_request_signing/golden_authnrequest.xml"`
    - `@golden_redirect_path "test/fixtures/security/authn_request_signing/golden_redirect.txt"`
    - `@golden_redirect_adfs_path "test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt"`
    - `@pem_path "test/fixtures/security/authn_request_signing/golden_signing_key.pem"`
    - `@rsa_sha256_uri "http://www.w3.org/2001/04/xmldsig-more#rsa-sha256"`

    Then add five `describe` blocks, one per row, each containing exactly one `test` tagged with the row's `@tag` per CONTEXT.md D-19 / RESEARCH.md §2 / VALIDATION.md tasks 35-07-01..05:

    **Row 1 — `describe "row 1 — golden positive control (:rfc3986_upper)" do`** with `@tag :row_golden`. The test reads the committed golden file via `File.read!(@golden_redirect_path)`, calls `Relyra.start_login/3` with a connection map carrying `sign_authn_requests: true`, `signed_request_encoding: :rfc3986_upper`, `idp_sso_url: "https://idp.example.com/sso"`, `acs_url: "https://sp.example.com/saml/acs"`, `sp_entity_id: "https://sp.example.com/metadata"`, and the `relay_state: @golden_relay_state` opt. The test reads the canonical AuthnRequest XML from `@golden_xml_path` and passes it via the `:authn_request_xml` opt to bypass XML synthesis (so ID + IssueInstant come from the committed XML, not from non-deterministic builders). Asserts `{:ok, %{redirect_query: actual}} = ...` and `assert actual == expected`. Pins EXACT `%{redirect_query: _}` shape, NOT a bare `{:ok, _}`. (If Plan 05's `start_login/3` does not yet accept `:authn_request_xml` opt for fixture injection, the helper functions at the bottom of this file will wrap `Binding.encode_redirect/3` directly with `sign: true` + `signing_key_pem` from the committed PEM + the canonical XML — see Action note 4 below.)

    **Row 2 — `describe "row 2 — ADFS-lower variant (:adfs_lower)" do`** with `@tag :row_adfs_lower`. Same as Row 1 except `signed_request_encoding: :adfs_lower` and the assertion is against `@golden_redirect_adfs_path`. Additionally add the structural delta assertion: `assert String.downcase(File.read!(@golden_redirect_path)) == String.downcase(File.read!(@golden_redirect_adfs_path))` (the two goldens must differ ONLY in percent-encoded hex case — every other byte is identical because the encoder produces the same shape with only the lowercase_hex post-process applied).

    **Row 3 — `describe "row 3 — re-serialization regression (mutation test for T-35-01)" do`** with `@tag :row_reserialization_regression`. This is LOCAL computation — NO `Relyra.start_login/3` call. The test:
      1. Reads the committed PEM and decodes it: `[entry] = :public_key.pem_decode(File.read!(@pem_path))`; `private_key = :public_key.pem_entry_decode(entry)`.
      2. Computes a re-serialized octets candidate via `URI.encode_query/1` of a `%{"SAMLRequest" => b64, "RelayState" => @golden_relay_state, "SigAlg" => @rsa_sha256_uri}` map (which produces a query string with map-iteration-undefined parameter order — and even if the order happened to match, the URI.encode_query/1 byte sequence is not guaranteed equal to the literal-concatenation that production code uses).
      3. Signs THOSE octets via `:public_key.sign(mutated_octets, :sha256, private_key)` and base64-encodes + URL-encodes the result (mirrors Plan 01 sign_redirect_query/3's output shape).
      4. Reads the golden Row 1 file, splits on `"&Signature="`, takes the second element as `golden_signature_url_encoded`.
      5. Asserts `mutated_signature_url_encoded != golden_signature_url_encoded` — proving that re-serializing the octets through `URI.encode_query/1` produces a different signature than what production emits. Use `refute mutated == golden` (refute is the structurally clearer form for inequality assertions per Phase 30 conventions).
      The `b64` value for the SAMLRequest parameter is computed by deflating + base64-encoding the canonical XML (via `:zlib.deflateInit/6` with `-15` window bits — the same way Plan 02 Binding.encode_redirect/3 does it). Pull this into a local helper `defp deflate_then_b64(xml)` at the bottom of the module (NOT in `lib/`) so Row 3 stays readable.

    **Row 4 — `describe "row 4 — round-trip verify against committed PEM (T-35-06)" do`** with `@tag :row_roundtrip_verify`. The test:
      1. Re-runs the Row 1 production path (`Relyra.start_login/3` with `:rfc3986_upper`) to obtain fresh `redirect_query` bytes.
      2. Splits the bytes on `"&Signature="` → `[signed_octets, signature_url_encoded]`.
      3. Decodes the signature: `signature_b64 = URI.decode_www_form(signature_url_encoded)`; `{:ok, signature_bytes} = Base.decode64(signature_b64)`.
      4. Derives the public key from the committed PEM: `[entry] = :public_key.pem_decode(File.read!(@pem_path))`; `{:RSAPrivateKey, _v, modulus, public_exponent, _d, _p, _q, _e1, _e2, _coef, _other} = :public_key.pem_entry_decode(entry)`; `public_key = {:RSAPublicKey, modulus, public_exponent}`.
      5. Asserts `assert :public_key.verify(signed_octets, :sha256, signature_bytes, public_key) == true`.
      The exact RSAPrivateKey record tuple shape is from the Erlang `:public_key` documentation; the planner notes that the record has 11 fields including `:RSAPrivateKey` head — use pattern-match on `elem(decoded, 2)` for modulus and `elem(decoded, 3)` for public_exponent IF the tuple-shape spread becomes awkward; both forms produce the same `{:RSAPublicKey, modulus, public_exponent}` result.

    **Row 5 — `describe "row 5 — toggle-off no-op (sign_authn_requests: false; AUTHN-02 default contract)" do`** with `@tag :row_toggle_off_noop`. The test calls `Relyra.start_login/3` with a connection map that has `sign_authn_requests: false` (explicit; the default but pinned for clarity) and NO `signed_request_encoding`. Asserts `{:ok, %{redirect_params: params}} = ...`; `refute Map.has_key?(params, "SigAlg")`; `refute Map.has_key?(params, "Signature")`. Additionally, pattern-matches on the full result map to confirm the `:redirect_query` key is absent: `assert match?({:ok, result}, ...) when not is_map_key(result, :redirect_query)` OR equivalently `refute Map.has_key?(result, :redirect_query)` after the destructure.

    Add local helper functions at the bottom of the module (mirroring `adversarial_crypto_test.exs:181-198` shape):
      - `defp connection_signed(encoding)` — returns a plain map for the connection (NOT a full Ecto schema struct; the corpus tests through `Relyra.start_login/3`'s acceptance of map-shaped connections, mirroring how the existing provider tests construct connections)
      - `defp connection_unsigned()` — returns a plain map with `sign_authn_requests: false`
      - `defp canonical_authn_request_xml()` — `File.read!(@golden_xml_path)`
      - `defp deflate_then_b64(xml)` — reuses Plan 02's deflate algorithm (`:zlib.open` → `:zlib.deflateInit(z, :default, :deflated, -15, 8, :default)` → `:zlib.deflate(z, xml, :finish)` → `:zlib.deflateEnd(z)` → `:zlib.close(z)` in try/after); the helper is private and test-scoped, mirroring Plan 02's `Binding.deflate_xml/1` byte-for-byte
      - `defp decode_committed_pem()` — `[entry] = :public_key.pem_decode(File.read!(@pem_path)); :public_key.pem_entry_decode(entry)`

    **Action note 4 (re: Plan 05 `:authn_request_xml` opt acceptance):** If Plan 05 does not implement an `:authn_request_xml` opt that lets the corpus inject the fixed XML (overriding the dynamic ID/IssueInstant in `Relyra.Protocol.AuthnRequest.to_xml/1`), then Rows 1, 2, 5 MUST instead call `Relyra.Protocol.Binding.encode_redirect/3` directly with the canonical XML — this still exercises the deflate + sign + encode path (Plan 02's primary contract) and avoids the non-determinism of `to_xml/1`. The decision between "call start_login/3" and "call encode_redirect/3" is a Plan 05 alignment question; pick whichever the as-shipped API supports as of Plan 06 completion. EITHER WAY the byte-equality assertion against `golden_redirect.txt` is the load-bearing check. Document the chosen entry point in `35-07-SUMMARY.md`.

    **CRITICAL prohibitions:** (a) do NOT add `@moduletag :adversarial_crypto` — the new corpus has its OWN tag (`:authn_request_signing`) per CONTEXT.md D-20 / Phase 30 hollow-gate fix; (b) do NOT modify `test/security/xml/adversarial_crypto_test.exs` (CLAUDE.md "Never weaken"); (c) do NOT introduce a parallel signer module (T-35-06 — Row 3's mutation uses `:public_key.sign/3` directly with the committed PEM; Row 4 uses `:public_key.verify/4` directly); (d) do NOT recompute golden bytes inline anywhere — Tasks 2/3 mint them; the test reads via `File.read!/1`; (e) do NOT pin bare `{:ok, _}` or `{:error, _}` — every assertion pins the exact `%{...}` or `%Error{type: ...}` shape (adversarial_crypto_test.exs:22-25 discipline); (f) do NOT use `FakeIdP.keypair/0` for any signing/verification operation in this suite — the committed PEM is the source of truth (RESEARCH.md A4 mitigation; FakeIdP's `:persistent_term` keypair is non-deterministic per-process); (g) do NOT call `Relyra.start_login/3` inside Row 3 (the mutation test MUST run outside the production path or the test is a no-op); (h) do NOT add a fenced code block of executable Elixir as the body of any task action — this corpus IS the executable specification.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
    <source-assertions>
      - test -f test/security/authn_request_signing_test.exs (file exists)
      - grep -c "@moduletag :authn_request_signing" test/security/authn_request_signing_test.exs returns 1
      - grep -c "@moduletag :adversarial_crypto" test/security/authn_request_signing_test.exs returns 0 (wrong tag would defeat Plan 30 hollow-gate fix)
      - grep -c "@tag :row_golden" test/security/authn_request_signing_test.exs returns 1
      - grep -c "@tag :row_adfs_lower" test/security/authn_request_signing_test.exs returns 1
      - grep -c "@tag :row_reserialization_regression" test/security/authn_request_signing_test.exs returns 1
      - grep -c "@tag :row_roundtrip_verify" test/security/authn_request_signing_test.exs returns 1
      - grep -c "@tag :row_toggle_off_noop" test/security/authn_request_signing_test.exs returns 1
      - grep -c "describe \"row " test/security/authn_request_signing_test.exs returns 5
      - grep -c ":public_key.sign(" test/security/authn_request_signing_test.exs returns at least 1 (Row 3)
      - grep -c ":public_key.verify(" test/security/authn_request_signing_test.exs returns at least 1 (Row 4)
      - grep -c "Relyra.start_login" test/security/authn_request_signing_test.exs returns at least 3 (Rows 1, 2, 5; possibly 4 if Row 4 re-runs Row 1)
      - grep -c "Application.put_env(:relyra, :sp_signing_key_pem" test/security/authn_request_signing_test.exs returns 1 (setup block; not duplicated per-test)
      - grep -c "FakeIdP.keypair" test/security/authn_request_signing_test.exs returns 0 (corpus uses committed PEM, not FakeIdP per-process keypair)
      - grep -c "defmodule.+RedirectSigning\\|defmodule.+ParallelSigner\\|defmodule.+CorpusSigner" test/security/authn_request_signing_test.exs returns 0 (no parallel signer module)
    </source-assertions>
  </verify>
  <acceptance_criteria>
    - `test/security/authn_request_signing_test.exs` exists and compiles with `--warnings-as-errors`
    - `mix compile --warnings-as-errors` exits 0
    - Module declares `@moduletag :authn_request_signing` (the OWN tag per CONTEXT.md D-20)
    - Module declares EXACTLY five `describe` blocks, one per row, each with the per-row `@tag`
    - The setup block uses `Application.put_env(:relyra, :sp_signing_key_pem, File.read!(...))` and registers an `on_exit` env-delete
    - Each row's test pins an exact `%{...}` or `%Error{type: ...}` shape — no bare `{:ok, _}` / `{:error, _}`
    - Row 3 calls `:public_key.sign/3` directly (NOT via `Signature.sign_redirect_query/3`) — proving the mutation is OUTSIDE the production path
    - Row 4 calls `:public_key.verify/4` directly — no parallel signer/verifier module
    - `mix format --check-formatted` exits 0
    - `git diff test/security/xml/adversarial_crypto_test.exs` shows NO changes (CLAUDE.md "Never weaken")
  </acceptance_criteria>
  <done>
    The corpus file exists with 5 well-formed rows, the OWN @moduletag, exact shape pinning, and the setup block correctly threads the committed PEM into Application env. Tests will RED on the byte-equality assertions until Tasks 2/3 mint the golden files — that's expected; Task 6 verifies green-after-mint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mint and commit golden_authnrequest.xml (canonical input with fixed ID + IssueInstant)</name>
  <files>test/fixtures/security/authn_request_signing/golden_authnrequest.xml</files>
  <read_first>
    - lib/relyra/protocol/authn_request.ex (entire file — see how to_xml/1 normally constructs the AuthnRequest; the committed XML must be the deterministic equivalent: same element order, same attribute set, but with FIXED ID and IssueInstant attributes instead of the randomized values)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-RESEARCH.md (§2 "Mint procedure for golden bytes" lines 181-202 — explicit requirement to fix ID + IssueInstant + RelayState; explicit prohibition on :crypto.strong_rand_bytes/1)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-PATTERNS.md (lines 803-810 — reproducibility constraint; lines 776-781 — fixture role table)
    - test/fixtures/security/xml/parser_differential_and_c14n/assertion_inherited_ns.input.xml (Phase 28 fixture-commit precedent for XML fixture shape — informs XML structure conventions; this is not a structural template but shows the encoding/declaration conventions used in committed XML fixtures)
  </read_first>
  <action>
    Create `test/fixtures/security/authn_request_signing/golden_authnrequest.xml` as a canonical SAML 2.0 AuthnRequest XML with FIXED, deterministic attribute values. The directory must exist already from Plan 01 Task 3 (which committed `golden_signing_key.pem` there); if not, this task creates it.

    The XML structure mirrors what `Relyra.Protocol.AuthnRequest.to_xml/1` produces for the connection used in Row 1:
      - Root element `<samlp:AuthnRequest>` with namespaces `xmlns:samlp="urn:oasis:names:tc:SAML:2.0:protocol"` and `xmlns:saml="urn:oasis:names:tc:SAML:2.0:assertion"`.
      - FIXED `ID` attribute (e.g. `ID="_relyra-phase35-golden-authnreq"` — any stable opaque string; MUST start with a letter or underscore per XML ID convention). Do NOT use `:crypto.strong_rand_bytes/1` to generate it — type a literal string.
      - FIXED `IssueInstant` attribute set to `IssueInstant="2026-05-26T00:00:00Z"` (deterministic UTC timestamp from RESEARCH.md §2 mint procedure example).
      - `Version="2.0"` (constant).
      - `Destination="https://idp.example.com/sso"` (matches Row 1's connection idp_sso_url).
      - `ProtocolBinding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST"` (the SAML ACS binding the SP supports — this is the AuthnRequest's nested binding hint, distinct from the redirect binding used for the AuthnRequest delivery itself).
      - `AssertionConsumerServiceURL="https://sp.example.com/saml/acs"` (matches Row 1's connection acs_url).
      - Child `<saml:Issuer>https://sp.example.com/metadata</saml:Issuer>` (matches sp_entity_id).
      - (Any other elements that `Relyra.Protocol.AuthnRequest.to_xml/1` emits — read the function source to enumerate. Common additions: `<samlp:NameIDPolicy>` with `Format` + `AllowCreate`. The committed XML must EXACTLY match what to_xml/1 would emit given the Row 1 connection inputs and the fixed ID + IssueInstant.)

    Encoding: UTF-8 (no BOM). Single newline character at end-of-line within the file is acceptable for the XML itself — the no-trailing-newline rule applies to the GOLDEN REDIRECT files (Tasks 3 and 4), not to the input XML. Whitespace handling: prefer compact (no pretty-printing) so the deflate output is byte-deterministic — extra whitespace would change the deflate bytes and thus the goldens; the test reads this XML via `File.read!/1` and deflates it directly.

    **CRITICAL constraints:**
      1. The XML's `ID` and `IssueInstant` MUST match what `Relyra.Protocol.AuthnRequest.to_xml/1` produces when called with the Row 1 connection AND with these exact values fixed (i.e. when the test injects `:authn_request_xml` opt OR when the corpus calls `Binding.encode_redirect/3` directly with this XML — see Task 1 Action note 4). The chosen entry point determines the production source for the canonical shape; the planner is committing to: this XML IS the canonical input — production code reads this same XML and deflates+signs it without further modification.
      2. NO `:crypto.strong_rand_bytes/1` in the file — the literal ID and IssueInstant must be typed in.
      3. Encoding declaration: `<?xml version="1.0" encoding="UTF-8"?>` as the first line (matches conformance fixtures and what to_xml/1 produces).

    Procedure: write the XML file using the `Write` tool. The exact byte content is determined by inspecting `Relyra.Protocol.AuthnRequest.to_xml/1` first (read the function and its helpers), then producing the deterministic equivalent. If to_xml/1's output for a given connection is itself non-deterministic in element ordering or attribute order, normalize to whatever the function produces FIRST (e.g. by calling it once in iex with the fixed inputs, capturing the output, hand-editing only the ID and IssueInstant to be deterministic, and committing). Record the procedure in PROVENANCE.md (Task 5).
  </action>
  <verify>
    <automated>test -f test/fixtures/security/authn_request_signing/golden_authnrequest.xml && head -1 test/fixtures/security/authn_request_signing/golden_authnrequest.xml | grep -q "<?xml" && grep -q "ID=\"_relyra-phase35" test/fixtures/security/authn_request_signing/golden_authnrequest.xml && grep -q "IssueInstant=\"2026-05-26T00:00:00Z\"" test/fixtures/security/authn_request_signing/golden_authnrequest.xml && echo OK</automated>
    <source-assertions>
      - test -f test/fixtures/security/authn_request_signing/golden_authnrequest.xml
      - grep -c "<?xml" test/fixtures/security/authn_request_signing/golden_authnrequest.xml returns 1
      - grep -c "samlp:AuthnRequest" test/fixtures/security/authn_request_signing/golden_authnrequest.xml returns at least 2 (open + close tag)
      - grep -c "Issuer" test/fixtures/security/authn_request_signing/golden_authnrequest.xml returns at least 2 (open + close tag)
      - grep -c ":crypto.strong_rand_bytes" test/fixtures/security/authn_request_signing/golden_authnrequest.xml returns 0 (no randomness in the file itself)
      - grep -c "ID=\"" test/fixtures/security/authn_request_signing/golden_authnrequest.xml returns at least 1
      - grep -c "IssueInstant=\"" test/fixtures/security/authn_request_signing/golden_authnrequest.xml returns at least 1
      - elixir -e 'File.read!("test/fixtures/security/authn_request_signing/golden_authnrequest.xml") |> :erlang.byte_size() |> IO.inspect()' prints an integer > 100 (file has substantive content; not empty/truncated)
    </source-assertions>
  </verify>
  <acceptance_criteria>
    - File `test/fixtures/security/authn_request_signing/golden_authnrequest.xml` exists
    - File contains the XML declaration `<?xml version="1.0" encoding="UTF-8"?>`
    - File contains `<samlp:AuthnRequest ...>` with FIXED `ID="_relyra-phase35-golden-authnreq"` and `IssueInstant="2026-05-26T00:00:00Z"` (or operator-chosen deterministic equivalents documented in PROVENANCE.md)
    - File is parseable as well-formed XML (verifiable via `:saxy.parse_string/1` or similar — `mix run -e 'IO.inspect(Saxy.parse_string(File.read!("test/fixtures/security/authn_request_signing/golden_authnrequest.xml"), Relyra.Security.XML.PureBeam, []))'` does not raise)
    - File contains no `:crypto.strong_rand_bytes` call (it's an XML file, but the regex check catches accidental copy-paste of generator code)
    - File's byte_size is recorded in the task summary for PROVENANCE.md citation (Task 5)
    - `mix format --check-formatted` exits 0 (no Elixir code in this XML file, but the file MUST not break the formatter)
  </acceptance_criteria>
  <done>
    `golden_authnrequest.xml` exists with fixed ID + IssueInstant, parses as well-formed XML, and serves as the canonical deterministic input for Tasks 3 and 4 (the redirect mints) and for Task 1's Row 1/Row 2/Row 5 tests at runtime.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mint and commit golden_redirect.txt (Row 1 :rfc3986_upper expected bytes)</name>
  <files>test/fixtures/security/authn_request_signing/golden_redirect.txt</files>
  <read_first>
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-RESEARCH.md (§2 "Mint procedure for golden bytes" lines 181-202 — the iex one-liner template; deterministic constraints; the warning about :crypto.strong_rand_bytes/1)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-PATTERNS.md (lines 779-785 — Row 1 file role; lines 803-818 — reproducibility + no-trailing-newline rule)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-01-PLAN.md (Plan 01 SUMMARY records the committed PEM's sha256 + byte count; reference it here)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-02-PLAN.md (Plan 02's Binding.encode_redirect/3 contract — the code path the mint uses)
    - test/fixtures/security/authn_request_signing/golden_authnrequest.xml (Task 2 output; the input to the mint)
    - test/fixtures/security/authn_request_signing/golden_signing_key.pem (Plan 01 Task 3 output; the signing key the mint uses)
    - lib/relyra/protocol/binding.ex (Plan 02 — confirm encode_redirect/3 accepts the opts the mint command uses)
    - test/fixtures/security/xml/parser_differential_and_c14n/PROVENANCE.md (lines 22-29, 45 — the no-trailing-newline rule with the exact `0x3e` last-byte precedent)
  </read_first>
  <action>
    Mint `test/fixtures/security/authn_request_signing/golden_redirect.txt` ONCE via the production code path; commit the resulting bytes. The mint is a one-shot deterministic operation that must be reproducible by future operators.

    Mint procedure (verbatim per RESEARCH.md §2 with the planner's adaptations for the as-shipped Plan 02/05 APIs):

      1. Start an iex session: `iex -S mix`
      2. Load the committed PEM into Application env:
         `Application.put_env(:relyra, :sp_signing_key_pem, File.read!("test/fixtures/security/authn_request_signing/golden_signing_key.pem"))`
      3. Read the canonical XML: `xml = File.read!("test/fixtures/security/authn_request_signing/golden_authnrequest.xml")`
      4. Call the signed-redirect-encoding entry point with the Row 1 inputs:
         `{:ok, %{redirect_query: bytes}} = Relyra.Protocol.Binding.encode_redirect(xml, "rs_relyra_phase35_golden", sign: true, signature_method: "http://www.w3.org/2001/04/xmldsig-more#rsa-sha256", encoding: :rfc3986_upper, connection_id: "golden-fixture")`
         (If Plan 05 exposes a higher-level entry that takes the same opts and produces the same `:redirect_query` shape, use that — but the load-bearing requirement is that the function emits the SAME bytes the production controller would emit on the wire. Document the chosen entry in PROVENANCE.md.)
      5. Write the bytes to disk: `File.write!("test/fixtures/security/authn_request_signing/golden_redirect.txt", bytes)`
      6. **CRITICAL no-trailing-newline check** — verify the file's last byte is NOT `\n` (`0x0a`). The output of Binding.encode_redirect/3 does not include a trailing newline by design; if any editor or shell pipe accidentally added one, strip it: `File.read!(path) |> String.trim_trailing("\n") |> then(&File.write!(path, &1))`. Confirm via `:erlang.binary_part(File.read!(path), {byte_size(File.read!(path)) - 1, 1}) != "\n"`.
      7. Record the file's sha256 and byte_size for PROVENANCE.md (Task 5): `{:sha256, :crypto.hash(:sha256, File.read!(path)) |> Base.encode16(case: :lower), byte_size(File.read!(path))}`.

    The bytes MUST satisfy these structural invariants (Task 1 Row 1 reads this file and asserts byte-equality):
      - Starts with `SAMLRequest=` (literal prefix).
      - Contains `&RelayState=rs_relyra_phase35_golden` (the fixed RelayState).
      - Contains `&SigAlg=` followed by URL-encoded `http://www.w3.org/2001/04/xmldsig-more#rsa-sha256` (note: `:` and `/` and `#` are URL-encoded).
      - Contains `&Signature=` followed by URL-encoded base64.
      - Order is exactly: `SAMLRequest=...&RelayState=...&SigAlg=...&Signature=...` per OASIS §3.4.4.1 lines 601-605 (the Signature parameter is LAST per D-11; the four parameters appear in this order in the wire bytes).
      - All percent-encoded hex sequences use UPPERCASE letters (e.g. `%2F`, `%3D`, `%3A`) — RFC 3986 §2.1.
      - No trailing newline.

    **CRITICAL prohibitions:** (a) do NOT mint with any RelayState other than the fixed `"rs_relyra_phase35_golden"` (RESEARCH.md §2 — RelayState must be fixed for determinism); (b) do NOT mint with a different PEM than the one committed in Plan 01 Task 3 (re-minting with a different key would invalidate the golden + Plan 01's SUMMARY-recorded fingerprint); (c) do NOT hand-edit the bytes after minting — if the bytes are wrong, the bug is in Plan 02's Binding.encode_redirect/3 or Plan 01's Signature.sign_redirect_query/3, not in this fixture; (d) do NOT add a trailing newline (the Phase 28 PROVENANCE Pitfall 4/6 precedent governs); (e) do NOT commit the iex session log or any intermediate file alongside the golden — only the final bytes go in `golden_redirect.txt`.
  </action>
  <verify>
    <automated>test -f test/fixtures/security/authn_request_signing/golden_redirect.txt && head -c 12 test/fixtures/security/authn_request_signing/golden_redirect.txt | grep -q "^SAMLRequest=" && grep -q "RelayState=rs_relyra_phase35_golden" test/fixtures/security/authn_request_signing/golden_redirect.txt && grep -q "Signature=" test/fixtures/security/authn_request_signing/golden_redirect.txt && echo OK</automated>
    <source-assertions>
      - test -f test/fixtures/security/authn_request_signing/golden_redirect.txt
      - elixir -e 'b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt"); IO.inspect(String.starts_with?(b, "SAMLRequest="))' prints true
      - elixir -e 'b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt"); IO.inspect(String.contains?(b, "&RelayState=rs_relyra_phase35_golden"))' prints true
      - elixir -e 'b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt"); IO.inspect(String.contains?(b, "&SigAlg="))' prints true
      - elixir -e 'b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt"); IO.inspect(String.contains?(b, "&Signature="))' prints true
      - elixir -e 'b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt"); last = :binary.last(b); IO.inspect(last != ?\n)' prints true (no trailing newline; the Phase 28 PROVENANCE Pitfall 4/6 rule)
      - elixir -e 'b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt"); IO.inspect(b =~ ~r/%[A-F0-9]{2}/)' prints true (uppercase hex percent-encoding present)
      - elixir -e 'b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt"); IO.inspect(b =~ ~r/%[a-f][0-9a-f]|%[0-9a-f][a-f]/)' prints false (no lowercase hex in :rfc3986_upper golden)
    </source-assertions>
  </verify>
  <acceptance_criteria>
    - File `test/fixtures/security/authn_request_signing/golden_redirect.txt` exists
    - File starts with `SAMLRequest=` (literal byte prefix)
    - File contains `&RelayState=rs_relyra_phase35_golden`, `&SigAlg=`, `&Signature=` in that order
    - File has NO trailing newline (last byte is the final base64/percent-encoded char, NOT `\n`)
    - All percent-encoded hex sequences use UPPERCASE letters (RFC 3986 §2.1 compliance for :rfc3986_upper)
    - File sha256 + byte_size are recorded in the task summary for PROVENANCE.md citation (Task 5)
    - `mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_golden --warnings-as-errors` exits 0 (Row 1's byte-equality assertion passes)
  </acceptance_criteria>
  <done>
    `golden_redirect.txt` is committed; Row 1's byte-equality assertion passes against the production code path; the file has no trailing newline and uses uppercase percent-encoding throughout.
  </done>
</task>

<task type="auto">
  <name>Task 4: Mint and commit golden_redirect_adfs.txt (Row 2 :adfs_lower expected bytes)</name>
  <files>test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt</files>
  <read_first>
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-RESEARCH.md (§2 mint procedure; §3.2 lowercase_hex post-process behavior; §5 python3-saml `lowercase_urlencoding` source verbatim)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-PATTERNS.md (lines 779-785 fixture role; line 781 explicit "the two golden files MUST differ ONLY in hex-case of percent-encoded sequences")
    - test/fixtures/security/authn_request_signing/golden_redirect.txt (Task 3 output — the structural template; the :adfs_lower variant differs from this ONLY in hex case)
  </read_first>
  <action>
    Mint `test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt` via the same procedure as Task 3, with the SOLE change `encoding: :adfs_lower`. The mint produces a byte sequence identical to Task 3's output EXCEPT that every `%[A-F0-9]{2}` percent-encoded sequence is lowercase (`%2F` → `%2f`, `%3D` → `%3d`, etc.) per Plan 02's `Binding.lowercase_hex/1` post-process (which is the verbatim python3-saml `lowercase_urlencoding` regex).

    Mint procedure (the iex session can continue from Task 3 — same Application env, same XML, same PEM):

      1. (If continuing from Task 3, the Application env is already set. If restarting, repeat steps 1-3 from Task 3.)
      2. Call `Binding.encode_redirect/3` with `encoding: :adfs_lower`:
         `{:ok, %{redirect_query: bytes}} = Relyra.Protocol.Binding.encode_redirect(xml, "rs_relyra_phase35_golden", sign: true, signature_method: "http://www.w3.org/2001/04/xmldsig-more#rsa-sha256", encoding: :adfs_lower, connection_id: "golden-fixture")`
      3. Write to disk: `File.write!("test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt", bytes)`
      4. **Sanity check the structural delta** — the only difference between the two goldens MUST be percent-encoded hex case:
         `assert String.downcase(File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt")) == String.downcase(File.read!("test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt"))`
         (If this assertion fails during the mint, STOP — Plan 02's `lowercase_hex/1` is producing more than just hex-case post-processing, which is a bug in Plan 02, not this fixture. Fix Plan 02 first.)
      5. No-trailing-newline check (same as Task 3).
      6. Record sha256 + byte_size for PROVENANCE.md (Task 5).

    The two goldens must have the SAME byte count (lowercase_hex post-process replaces each `%XX` with `%xx` — same byte length; the only change is the case of the two hex digits).

    **CRITICAL prohibitions:** (a) do NOT mint with a different RelayState, signature_method, XML, or PEM than Task 3 — the only difference between the two goldens MUST be the encoding atom; (b) do NOT hand-edit the bytes — if `String.downcase(redirect) != String.downcase(redirect_adfs)`, the Plan 02 lowercase_hex/1 helper is broken; (c) do NOT add a trailing newline.
  </action>
  <verify>
    <automated>test -f test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt && head -c 12 test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt | grep -q "^SAMLRequest=" && grep -q "RelayState=rs_relyra_phase35_golden" test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt && grep -q "Signature=" test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt && echo OK</automated>
    <source-assertions>
      - test -f test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt
      - elixir -e 'b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt"); IO.inspect(String.starts_with?(b, "SAMLRequest="))' prints true
      - elixir -e 'b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt"); last = :binary.last(b); IO.inspect(last != ?\n)' prints true (no trailing newline)
      - elixir -e 'a = File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt"); b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt"); IO.inspect(String.downcase(a) == String.downcase(b))' prints true (the structural delta invariant: only difference is percent-encoded hex case)
      - elixir -e 'a = File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt"); b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt"); IO.inspect(byte_size(a) == byte_size(b))' prints true (same byte count — lowercase_hex is byte-preserving)
      - elixir -e 'a = File.read!("test/fixtures/security/authn_request_signing/golden_redirect.txt"); b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt"); IO.inspect(a != b)' prints true (the goldens are NOT byte-identical; at least one percent-encoded byte differs in case)
      - elixir -e 'b = File.read!("test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt"); IO.inspect(b =~ ~r/%[a-f0-9]{2}/)' prints true (lowercase hex present in :adfs_lower golden)
    </source-assertions>
  </verify>
  <acceptance_criteria>
    - File `test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt` exists
    - File has the same structural prefix/order as `golden_redirect.txt` (starts `SAMLRequest=`; contains `&RelayState=`, `&SigAlg=`, `&Signature=` in order)
    - File has NO trailing newline
    - File and `golden_redirect.txt` have the SAME byte count
    - `String.downcase(redirect) == String.downcase(redirect_adfs)` — the two goldens differ ONLY in percent-encoded hex case (the structural delta invariant)
    - `String.downcase(redirect_adfs) == redirect_adfs` — the :adfs_lower golden has NO uppercase percent-hex remaining
    - File sha256 + byte_size are recorded in the task summary for PROVENANCE.md citation (Task 5)
    - `mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_adfs_lower --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>
    `golden_redirect_adfs.txt` is committed; Row 2's byte-equality + structural-delta assertions pass; the lowercase_hex post-process invariant is provably the only difference from Row 1.
  </done>
</task>

<task type="auto">
  <name>Task 5: Commit PROVENANCE.md (Phase 28 fixture-commit pattern; spec citation chain; mint procedure)</name>
  <files>test/fixtures/security/authn_request_signing/PROVENANCE.md</files>
  <read_first>
    - test/fixtures/security/xml/parser_differential_and_c14n/PROVENANCE.md (entire file — Phase 28 fixture-commit precedent; mirrors the section structure, Pitfall references, sha256 + byte-count table, oracle toolchain documentation, reproduce instructions; this is the structural template — match it section-by-section)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-RESEARCH.md (§2 mint procedure lines 181-202; §5 External Spec Verbatim lines 444-486 — the OASIS / RFC / python3-saml citation chain; §A4 reproducibility constraint)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-PATTERNS.md (lines 783-810 — PROVENANCE.md template requirements)
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-01-PLAN.md (Plan 01 SUMMARY for the committed PEM's sha256 fingerprint + byte count)
    - test/fixtures/security/authn_request_signing/golden_signing_key.pem (the committed PEM — Plan 01 Task 3 output)
    - test/fixtures/security/authn_request_signing/golden_authnrequest.xml (Task 2 output)
    - test/fixtures/security/authn_request_signing/golden_redirect.txt (Task 3 output)
    - test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt (Task 4 output)
  </read_first>
  <action>
    Create `test/fixtures/security/authn_request_signing/PROVENANCE.md` mirroring the section structure of `test/fixtures/security/xml/parser_differential_and_c14n/PROVENANCE.md` line-by-line, with content adapted for AUTHN-01.

    Required sections in order:

      1. **H1 + 1-sentence purpose** — `# Golden-byte oracle provenance — `authn_request_signing` (AUTHN-01)`. One paragraph stating this directory holds the committed golden bytes that prove `Relyra.start_login/3` (via `Relyra.Protocol.Binding.encode_redirect/3` + `Relyra.Security.Signature.sign_redirect_query/3`) produces byte-exact HTTP-Redirect-binding signed AuthnRequest output matching the OASIS SAML 2.0 Bindings §3.4.4.1 specification. A byte-divergence here would defeat AUTHN-01's bit-for-bit guarantee and re-open the CVE-class re-serialization auth-bypass (T-35-01).

      2. **Fixtures table** with columns `| File | Bytes | sha256 |` listing all four golden files: `golden_signing_key.pem`, `golden_authnrequest.xml`, `golden_redirect.txt`, `golden_redirect_adfs.txt`. The byte counts and sha256 fingerprints come from Tasks 2/3/4's summaries (and Plan 01 Task 3's summary for the PEM). Use lowercase hex sha256 (matches Phase 28 precedent: `5d6d15c4...` not `5D6D15C4...`). Format the sha256 cell as ``` `<hex>` ``` (backticked) and the byte count as a bare integer.

      3. **What each golden exercises** — one subsection per file:
         - `golden_signing_key.pem` — RSA-2048 private key, deterministic (NOT FakeIdP.keypair/0 — that's `:persistent_term`-cached and non-deterministic per RESEARCH.md A4); the only signing key the AUTHN-01 corpus uses; exclusive to this directory (do not reuse for any other phase per RESEARCH.md A4 mitigation).
         - `golden_authnrequest.xml` — canonical SAML 2.0 AuthnRequest with FIXED `ID="_relyra-phase35-golden-authnreq"` and `IssueInstant="2026-05-26T00:00:00Z"` (no `:crypto.strong_rand_bytes/1`); compact (no pretty-printing — extra whitespace would shift deflate bytes); UTF-8 no BOM.
         - `golden_redirect.txt` — Row 1's expected bytes for `:rfc3986_upper` encoding; OASIS §3.4.4.1 lines 601-605 literal template `SAMLRequest=<v>&RelayState=<v>&SigAlg=<v>&Signature=<v>` in exact order; uppercase hex per RFC 3986 §2.1; NO trailing newline (last byte is the final percent-encoded base64 character, NOT `\n` — Phase 28 PROVENANCE Pitfall 4/6 precedent).
         - `golden_redirect_adfs.txt` — Row 2's expected bytes for `:adfs_lower` encoding; structural twin of `golden_redirect.txt` with the python3-saml `lowercase_urlencoding` regex applied (`re.sub(r"%[A-F0-9]{2}", lambda m: m.group(0).lower(), encoded)`); SAME byte count as `golden_redirect.txt`; the two files differ ONLY in percent-encoded hex case (provable invariant: `String.downcase(redirect) == String.downcase(redirect_adfs)`); NO trailing newline.

      4. **Spec citation chain** — verbatim block quoting the relevant spec text (mirrors RESEARCH.md §5):
         - OASIS SAML 2.0 Bindings §3.4.4.1 lines 597-611 (signed octet template, ordered parameter list, base64 encoding rule for Signature parameter)
         - OASIS SAML 2.0 Bindings §3.4.4.1 lines 620-625 (the raw-octet invariant: "It is not sufficient to re-encode the parameters after they have been processed by software because the resulting encoding may not match the signer's encoding." + "if there is no RelayState value, the entire parameter should be omitted from the signature computation (and not included as an empty parameter name).")
         - RFC 1951 — raw DEFLATE wire format (no zlib header, no Adler-32; Erlang `:zlib.deflateInit/6` with `-15` WindowBits is the documented escape hatch — cite erlang.org/doc/apps/erts/zlib.html).
         - RFC 3986 §2.1 — verbatim: "For consistency, URI producers and normalizers should use uppercase hexadecimal digits for all percent-encodings."
         - python3-saml `utils.py escape_url` with `lowercase_urlencoding` flag — verbatim regex `re.sub(r"%[A-F0-9]{2}", lambda m: m.group(0).lower(), encoded)`; cite https://github.com/SAML-Toolkits/python3-saml/blob/master/src/onelogin/saml2/utils.py.

      5. **Mint procedure (verbatim from RESEARCH.md §2)** — operator-reproducible iex commands. Include:
         - Elixir + OTP versions used to mint (record what was current at mint time — e.g. Elixir 1.18.x / OTP 27.x; capture via `elixir --version` at mint time).
         - The exact `Application.put_env(...)` + `File.read!(...)` + `Binding.encode_redirect(...)` invocations from Task 3 and Task 4.
         - The post-mint trailing-newline check + sha256/byte_size capture commands.
         - A statement that re-minting requires updating this PROVENANCE.md with new sha256 + byte counts (the file is the source of truth; the test reads the committed bytes and asserts byte-equality, so any re-mint that produces different bytes is a contract change requiring explicit operator review).

      6. **Re-mint policy** — mirrors Phase 28 PROVENANCE.md lines 73-90: never re-mint without updating this file; the goldens are immutable artifacts of a particular point-in-time production code path; if Plan 02 or Plan 01 changes the output bytes (even cosmetically), the goldens must be re-minted in lockstep AND this file's fingerprints must be updated AND the commit message must explain why.

      7. **No-trailing-newline rule** — explicit subsection citing Phase 28 PROVENANCE Pitfall 4/6: `golden_redirect.txt` and `golden_redirect_adfs.txt` MUST NOT have a trailing newline (last byte is the final percent-encoded character — likely `=` from base64 padding or a `%XX` hex digit, NOT `\n` `0x0a`). Editors that auto-append newlines on save must be configured to NOT do so for these files (`.editorconfig` already covers most editors; the test verifies the invariant on every run via the `last != ?\n` assertion shape).

    **CRITICAL constraints:** (a) record sha256 fingerprints in lowercase hex (Phase 28 precedent); (b) include the structural-delta invariant for the two redirect goldens (`String.downcase(redirect) == String.downcase(redirect_adfs)`) as a documented property — operators reading PROVENANCE.md must understand that this is a load-bearing invariant, not a coincidence; (c) cite spec sections with line numbers where possible (OASIS PDF page 17 lines 597-611, 620-625); (d) do NOT inline executable iex code that has side effects without clearly marking it as "Reproduce" / one-shot mint commands; (e) the file MUST end with a single trailing newline (Markdown convention — distinct from the no-trailing-newline rule for the binary golden files).
  </action>
  <verify>
    <automated>test -f test/fixtures/security/authn_request_signing/PROVENANCE.md && head -1 test/fixtures/security/authn_request_signing/PROVENANCE.md | grep -q "Golden-byte oracle provenance" && grep -q "OASIS SAML 2.0 Bindings" test/fixtures/security/authn_request_signing/PROVENANCE.md && grep -q "RFC 1951" test/fixtures/security/authn_request_signing/PROVENANCE.md && grep -q "RFC 3986" test/fixtures/security/authn_request_signing/PROVENANCE.md && grep -q "lowercase_urlencoding" test/fixtures/security/authn_request_signing/PROVENANCE.md && echo OK</automated>
    <source-assertions>
      - test -f test/fixtures/security/authn_request_signing/PROVENANCE.md
      - grep -c "sha256" test/fixtures/security/authn_request_signing/PROVENANCE.md returns at least 4 (one per fixture file)
      - grep -c "OASIS" test/fixtures/security/authn_request_signing/PROVENANCE.md returns at least 1
      - grep -c "RFC 1951" test/fixtures/security/authn_request_signing/PROVENANCE.md returns at least 1
      - grep -c "RFC 3986" test/fixtures/security/authn_request_signing/PROVENANCE.md returns at least 1
      - grep -c "python3-saml\\|lowercase_urlencoding" test/fixtures/security/authn_request_signing/PROVENANCE.md returns at least 1
      - grep -c "trailing newline\\|no trailing newline\\|NO trailing newline" test/fixtures/security/authn_request_signing/PROVENANCE.md returns at least 1
      - grep -c "Re-mint\\|re-mint" test/fixtures/security/authn_request_signing/PROVENANCE.md returns at least 1
      - grep -c "golden_signing_key.pem\\|golden_authnrequest.xml\\|golden_redirect.txt\\|golden_redirect_adfs.txt" test/fixtures/security/authn_request_signing/PROVENANCE.md returns at least 4 (each fixture mentioned)
      - grep -c "FakeIdP\\.keypair\\|persistent_term" test/fixtures/security/authn_request_signing/PROVENANCE.md returns at least 1 (the explicit anti-pattern callout)
    </source-assertions>
  </verify>
  <acceptance_criteria>
    - File `test/fixtures/security/authn_request_signing/PROVENANCE.md` exists
    - File contains a sha256 + byte-count table covering all four fixtures (`golden_signing_key.pem`, `golden_authnrequest.xml`, `golden_redirect.txt`, `golden_redirect_adfs.txt`)
    - File cites OASIS SAML 2.0 Bindings §3.4.4.1, RFC 1951, RFC 3986 §2.1, and python3-saml `lowercase_urlencoding` with version-anchored URLs
    - File documents the structural-delta invariant for the two redirect goldens (`String.downcase(redirect) == String.downcase(redirect_adfs)`)
    - File documents the no-trailing-newline rule for the redirect goldens (Phase 28 Pitfall 4/6 precedent)
    - File documents the verbatim mint procedure (iex commands from RESEARCH.md §2 adapted for the as-shipped Plan 02 entry point)
    - File records the Elixir + OTP versions used at mint time
    - File documents the re-mint policy (no re-mint without PROVENANCE.md update)
    - `mix format --check-formatted` exits 0 (Markdown not formatted by mix format, but the file MUST not break the run)
  </acceptance_criteria>
  <done>
    `PROVENANCE.md` exists; spec citation chain is verbatim-quoted; sha256 + byte-count manifest covers all four fixtures; mint procedure is operator-reproducible; the file mirrors Phase 28 PROVENANCE.md structurally and serves as the load-bearing operator contract for future re-mints.
  </done>
</task>

<task type="auto">
  <name>Task 6: Run the new corpus + full project suite; confirm green</name>
  <files></files>
  <read_first>
    - .planning/phases/35-signed-authnrequests-adfs-preset/35-VALIDATION.md (tasks 35-07-01 through 35-07-06 verify-command map — single-row commands + full-suite command)
    - CLAUDE.md (Testing Requirements section — `mix test --warnings-as-errors` must stay green; `mix ci.security` must stay green; `mix format --check-formatted` must exit 0)
  </read_first>
  <action>
    Run the validation gates IN THIS ORDER and confirm each is green before proceeding to the next:

      1. **Per-row sanity** (fastest feedback):
         - `mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_golden --warnings-as-errors` (Row 1)
         - `mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_adfs_lower --warnings-as-errors` (Row 2)
         - `mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_reserialization_regression --warnings-as-errors` (Row 3)
         - `mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_roundtrip_verify --warnings-as-errors` (Row 4)
         - `mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_toggle_off_noop --warnings-as-errors` (Row 5)
         Each of the five commands must exit 0 with `1 test, 0 failures` (or equivalent ExUnit summary line). If any row fails, FIX it before proceeding — do NOT proceed to the suite-wide or full-project run. Report the failure with the exact ExUnit output in the task SUMMARY.

      2. **Whole-suite run** for `:authn_request_signing` (all 5 rows together):
         - `mix test test/security/authn_request_signing_test.exs --only authn_request_signing --warnings-as-errors`
         Must exit 0 with `5 tests, 0 failures`. This catches order-of-tests issues that per-row runs cannot.

      3. **Full project suite** (the load-bearing non-regression check for Row 5's smoke claim):
         - `mix test --warnings-as-errors`
         Must exit 0. This proves that:
           (a) The new `@moduletag :authn_request_signing` does NOT exclude the suite from default `mix test` (the default runs all tags except `--exclude`d ones); the new corpus is part of the default suite.
           (b) None of the existing tests regress — particularly the Okta/Google/Entra/Ping/OneLogin provider tests under `test/relyra/provider/`, the conformance suite under `test/conformance/`, and the binding/metadata/algorithm-policy unit tests modified by Plans 02-06.
           (c) The `Application.put_env(:relyra, :sp_signing_key_pem, ...)` in the new corpus's `setup` block does NOT bleed into other tests (the `on_exit` env-delete is the mitigation; this run is the proof).

      4. **Format check**:
         - `mix format --check-formatted`
         Must exit 0. The new test file MUST be `mix format`-clean.

    If ALL FOUR steps are green, this task is done. Record in the SUMMARY the exact ExUnit output line counts (e.g. `5 tests, 0 failures, 0 excluded`) and the per-row pass/fail for traceability.

    **Note on Plan 08:** wiring the new suite into `mix ci.security` as its OWN `cmd mix test` line AND registering `:authn_request_signing` in `ci_gate_integrity_test.exs` `@gated_suites` is OUT OF SCOPE for this plan — it lives in Plan 08 per CONTEXT.md D-20. Task 6 does NOT run `mix ci.security` (it would fail because the new suite is not yet wired into the alias); Plan 08 owns that wiring and the corresponding `mix ci.security`-green verification.

    **CRITICAL prohibitions:** (a) do NOT skip the per-row runs — they isolate failures to a specific row and make debugging tractable; (b) do NOT mark this task done if `mix test --warnings-as-errors` is red even for a non-corpus reason (a regression in a Plan 03/04/05/06-touched file would surface here and is owned by this plan to catch); (c) do NOT run `mix ci.security` (out of scope; Plan 08).
  </action>
  <verify>
    <automated>mix test test/security/authn_request_signing_test.exs --only authn_request_signing --warnings-as-errors && mix test --warnings-as-errors && mix format --check-formatted</automated>
    <source-assertions>
      - mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_golden --warnings-as-errors exits 0
      - mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_adfs_lower --warnings-as-errors exits 0
      - mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_reserialization_regression --warnings-as-errors exits 0
      - mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_roundtrip_verify --warnings-as-errors exits 0
      - mix test test/security/authn_request_signing_test.exs --only authn_request_signing -t row_toggle_off_noop --warnings-as-errors exits 0
      - mix test test/security/authn_request_signing_test.exs --only authn_request_signing --warnings-as-errors exits 0
      - mix test --warnings-as-errors exits 0
      - mix format --check-formatted exits 0
    </source-assertions>
  </verify>
  <acceptance_criteria>
    - All 5 single-row commands exit 0 (`1 test, 0 failures` each)
    - The whole-suite run for `:authn_request_signing` exits 0 (`5 tests, 0 failures, 0 excluded`)
    - The full project suite `mix test --warnings-as-errors` exits 0 (no regressions in existing Okta/Google/Entra/Ping/OneLogin provider tests, binding/metadata/algorithm-policy unit tests, or conformance suite)
    - `mix format --check-formatted` exits 0
    - The task SUMMARY records the exact ExUnit output line counts for traceability
  </acceptance_criteria>
  <done>
    The 5-row adversarial corpus is green in isolation and as part of the full project suite; no existing test regresses; the load-bearing AUTHN-01 byte-equality + AUTHN-02 default-false contract are both proven; Plan 08 is now unblocked to wire the suite into `mix ci.security` and `ci_gate_integrity_test.exs`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → corpus test setup | The committed `golden_signing_key.pem` is read via `File.read!/1` and threaded into `Application.put_env(:relyra, :sp_signing_key_pem, ...)`; the env is process-scoped and cleaned up via `on_exit` |
| corpus test → `Relyra.start_login/3` | Rows 1, 2, 5 cross the corpus boundary into the production code path — no parallel signer; the production seam IS the test subject |
| corpus test → `:public_key.sign/3` (Row 3) | Row 3 deliberately bypasses the production path to mutate the octet assembly; the mutation IS the threat-model probe for T-35-01 |
| corpus test → `:public_key.verify/4` (Row 4) | Row 4 verifies that a peer (IdP) holding only the committed cert's public key can verify the SP-emitted signature; this is the IdP-side replay of the wire bytes |
| committed goldens → future re-mints | The goldens are immutable artifacts; any re-mint requires an explicit operator workflow + PROVENANCE.md update (Task 5 documents the policy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | `Relyra.Protocol.Binding.encode_redirect/3` signed-path octet assembly (and any future code path that touches the signed octets) | mitigate | Row 3 (`:row_reserialization_regression`) is the structural mutation test — it computes the OASIS octets via `URI.encode_query/1` of a map (the CVE-class footgun shape) and asserts that signing those bytes produces a DIFFERENT signature than the production-emitted Row 1 golden. If a future refactor introduces `URI.encode_query/1` into the signing path, Row 1 will start matching what Row 3 currently computes — and Row 3's `refute mutated == golden` will fail, surfacing the regression immediately. Cited verbatim: OASIS SAML 2.0 Bindings §3.4.4.1 lines 620-625 "It is not sufficient to re-encode the parameters after they have been processed by software because the resulting encoding may not match the signer's encoding." |
| T-35-05 | Tampering | `Relyra.Protocol.Binding.lowercase_hex/1` post-process for `:adfs_lower` encoding | mitigate | Row 2 (`:row_adfs_lower`) is the bit-for-bit golden for the ADFS-lower variant. The structural-delta invariant `String.downcase(redirect) == String.downcase(redirect_adfs)` is asserted alongside the byte-equality check — proving the only difference between the two encodings is percent-encoded hex case (matching python3-saml `lowercase_urlencoding` verbatim). If a future change to `lowercase_hex/1` introduces a non-cosmetic transformation (e.g. accidental double-encoding or character substitution), Row 2 fails immediately. Cited verbatim: python3-saml `utils.py escape_url` regex `re.sub(r"%[A-F0-9]{2}", lambda m: m.group(0).lower(), encoded)`. |
| T-35-06 | Tampering | SP signing key path divergence — i.e. the risk that a parallel signer module (e.g. `Relyra.Security.RedirectSigning`, or a `XmldsigSigner`-style test-only signer) would silently diverge from production `Relyra.Security.Signature.sign_redirect_query/3` and produce signatures the production verify path would also accept | mitigate | The corpus uses `:public_key.sign/3` directly (Row 3 mutation) and `:public_key.verify/4` directly (Row 4 round-trip) — both with the SAME committed PEM that the production path loads via `Application.get_env(:relyra, :sp_signing_key_pem)`. There is NO parallel signer/verifier module: Rows 1/2/5 exercise the production entry (`Relyra.start_login/3` or `Binding.encode_redirect/3` per Task 1 Action note 4); Row 3's `:public_key.sign/3` call IS the divergence proof (it explicitly diverges, and Row 3's `refute` asserts that divergence is detectable); Row 4's `:public_key.verify/4` against the production-emitted bytes proves a peer with only the public key can verify the signature. The grep-gate `defmodule.+RedirectSigning\\|defmodule.+ParallelSigner\\|defmodule.+CorpusSigner` returning 0 in Task 1's verify is the structural enforcement. Single-signer discipline is the CLAUDE.md "Key Architecture Seams" §1 invariant. |
</threat_model>

<verification>
- All 6 tasks pass their `<automated>` verify command
- `mix test test/security/authn_request_signing_test.exs --only authn_request_signing --warnings-as-errors` exits 0 (5 tests, 0 failures, 0 excluded)
- `mix test --warnings-as-errors` exits 0 (full project suite — no regressions)
- `mix format --check-formatted` exits 0
- All four committed fixtures exist with documented sha256 + byte-count:
    - `test/fixtures/security/authn_request_signing/golden_authnrequest.xml`
    - `test/fixtures/security/authn_request_signing/golden_redirect.txt` (no trailing newline; uppercase percent-hex)
    - `test/fixtures/security/authn_request_signing/golden_redirect_adfs.txt` (no trailing newline; lowercase percent-hex; structural-delta invariant holds vs `golden_redirect.txt`)
    - `test/fixtures/security/authn_request_signing/PROVENANCE.md` (cites OASIS / RFC 1951 / RFC 3986 §2.1 / python3-saml; records sha256 + byte-counts; documents mint procedure + re-mint policy)
- `git diff test/security/xml/adversarial_crypto_test.exs` shows NO changes (CLAUDE.md "Never weaken")
- `grep -rn "defmodule.+RedirectSigning\\|defmodule.+ParallelSigner\\|defmodule.+CorpusSigner" lib/ test/` returns 0 (no parallel signer module created)
- `grep -c "@moduletag :authn_request_signing" test/security/authn_request_signing_test.exs` returns 1 (the OWN tag — Plan 30 hollow-gate fix preserved)
- `grep -c "@moduletag :adversarial_crypto" test/security/authn_request_signing_test.exs` returns 0 (NOT the inbound corpus tag)
- The corpus has 5 distinct row tags: `:row_golden`, `:row_adfs_lower`, `:row_reserialization_regression`, `:row_roundtrip_verify`, `:row_toggle_off_noop` (each present exactly once via `@tag`)
</verification>

<success_criteria>
- `test/security/authn_request_signing_test.exs` exists with 5 well-formed corpus rows under `@moduletag :authn_request_signing`
- Row 1 (`:row_golden`): byte-equality `Relyra.start_login/3` output == `File.read!(golden_redirect.txt)` for `:rfc3986_upper`
- Row 2 (`:row_adfs_lower`): byte-equality vs `golden_redirect_adfs.txt` for `:adfs_lower`; structural-delta invariant holds (only diff is percent-hex case)
- Row 3 (`:row_reserialization_regression`): `URI.encode_query/1`-re-serialized + re-signed bytes produce a Signature that is `!=` the Row 1 golden's Signature (T-35-01 mutation guard)
- Row 4 (`:row_roundtrip_verify`): `:public_key.verify/4` of Row 1's signed octets + signature against the public key derived from the committed PEM returns `true` (T-35-06 single-signer-discipline proof)
- Row 5 (`:row_toggle_off_noop`): `sign_authn_requests: false` connection returns `{:ok, %{redirect_params: %{...}}}` with NO `"SigAlg"` / `"Signature"` / `:redirect_query` keys (AUTHN-02 default contract)
- Four committed fixtures exist: `golden_authnrequest.xml`, `golden_redirect.txt`, `golden_redirect_adfs.txt`, `PROVENANCE.md` (plus `golden_signing_key.pem` from Plan 01)
- `golden_redirect.txt` and `golden_redirect_adfs.txt` have NO trailing newline (Phase 28 PROVENANCE Pitfall 4/6)
- `golden_redirect.txt` uses UPPERCASE percent-encoded hex (RFC 3986 §2.1)
- `golden_redirect_adfs.txt` uses LOWERCASE percent-encoded hex (python3-saml `lowercase_urlencoding`)
- The two redirect goldens differ ONLY in percent-encoded hex case (`String.downcase(a) == String.downcase(b)` AND `a != b` AND `byte_size(a) == byte_size(b)`)
- `PROVENANCE.md` records sha256 + byte counts for all four fixtures, cites OASIS / RFC 1951 / RFC 3986 §2.1 / python3-saml, documents the verbatim mint procedure, records Elixir + OTP versions, and documents the re-mint policy
- `test/security/xml/adversarial_crypto_test.exs` is unchanged (CLAUDE.md "Never weaken")
- No parallel signer module created (single crypto seam invariant preserved per CLAUDE.md "Key Architecture Seams")
- `mix test --warnings-as-errors` exits 0 (no regressions across the full project; Row 5's smoke claim about Okta/Google/Entra/Ping/OneLogin provider tests not regressing is proven)
- `mix format --check-formatted` exits 0
- Plan 08 (CI wiring) is unblocked: `:authn_request_signing` tag exists, suite file exists, all 5 rows green
</success_criteria>

<output>
Create `.planning/phases/35-signed-authnrequests-adfs-preset/35-07-SUMMARY.md` when done with:
- Final shape of `test/security/authn_request_signing_test.exs` (module attrs, setup block, 5 describe blocks with row tags, helper functions at bottom)
- The chosen entry point for Rows 1/2/5 (`Relyra.start_login/3` with `:authn_request_xml` opt — IF Plan 05 added that opt — OR `Relyra.Protocol.Binding.encode_redirect/3` directly with `sign: true`) and the rationale for the choice
- sha256 + byte_count for each of the four committed fixtures (`golden_signing_key.pem` reference from Plan 01 Task 3 SUMMARY; `golden_authnrequest.xml`, `golden_redirect.txt`, `golden_redirect_adfs.txt` minted in this plan)
- Elixir + OTP versions at mint time (recorded in PROVENANCE.md and the SUMMARY)
- The exact ExUnit output line counts from Task 6 (per-row passes; whole-suite pass; full-project pass)
- Confirmation that `test/security/xml/adversarial_crypto_test.exs` is unchanged (CLAUDE.md "Never weaken")
- Confirmation that no parallel signer module was created (single crypto seam invariant preserved)
- Note for Plan 08: the suite file path is `test/security/authn_request_signing_test.exs`; the tag is `:authn_request_signing`; both must be added to `mix ci.security` and `ci_gate_integrity_test.exs` `@gated_suites` per CONTEXT.md D-20
</output>
