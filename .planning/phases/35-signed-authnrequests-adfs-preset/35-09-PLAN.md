---
phase: 35-signed-authnrequests-adfs-preset
plan: 09
type: execute
wave: 3
depends_on:
  - 35-04
files_modified:
  - guides/recipes/adfs.md
  - mix.exs
autonomous: true
requirements:
  - AUTHN-04
tags:
  - phase-35
  - authn-04
  - docs
  - adfs
  - runbook

must_haves:
  truths:
    - "guides/recipes/adfs.md exists at that exact path (NOT guides/providers/adfs.md — that directory does not exist per RESEARCH.md §3.5)"
    - "The runbook contains six required H2 sections: ## Overview, ## SP-side config, ## ADFS-side PowerShell, ## Claim rules, ## Interop notes, ## Troubleshooting"
    - "The ADFS-side PowerShell block uses parameter names verified against Microsoft Learn Set-AdfsRelyingPartyTrust (Server 2025) — -SignedSamlRequestsRequired, -RequestSigningCertificate, -SignatureAlgorithm, -SamlResponseSignature"
    - "The runbook documents the WantAuthnRequestsSigned (SAML spec) vs SignedSamlRequestsRequired (PowerShell flag) name divergence (AUTHN-04 acceptance criterion 5)"
    - "The runbook documents the :adfs_lower lowercase-hex quirk and how to set it per-connection"
    - "The runbook documents SHA-1-vs-SHA-256 redirect-binding interop and the independence of -SignatureAlgorithm (outbound) from inbound SigAlg verification"
    - "A PROVENANCE footer (or sibling note) in the runbook records the Microsoft Learn verification date and the snapshot of verified parameter names"
    - "The ci.docs alias in mix.exs runs `cmd test -f guides/recipes/adfs.md` so accidental deletion fails CI"
    - "Existing guides/recipes/*.md files (okta.md, entra.md, google_workspace.md) are NOT modified"
    - "mix ci.docs exits 0 after the change"
  artifacts:
    - path: "guides/recipes/adfs.md"
      provides: "AUTHN-04 operator runbook for the :adfs preset"
      contains: "## ADFS-side PowerShell"
    - path: "mix.exs"
      provides: "ci.docs alias gates the runbook's presence"
      contains: "guides/recipes/adfs.md"
  key_links:
    - from: "guides/recipes/adfs.md"
      to: "lib/relyra/provider/adfs.ex"
      via: "Documents the :adfs preset that Plan 35-04 ships (Relyra.Provider.ADFS.default_config/0)"
      pattern: "provider_preset:\\s*:adfs"
    - from: "guides/recipes/adfs.md"
      to: "Microsoft Learn Set-AdfsRelyingPartyTrust"
      via: "PowerShell block cites the Server 2025 cmdlet reference URL"
      pattern: "learn\\.microsoft\\.com.*set-adfsrelyingpartytrust"
    - from: "mix.exs (ci.docs alias)"
      to: "guides/recipes/adfs.md"
      via: "cmd test -f presence gate alongside batteries_included.md"
      pattern: "cmd test -f guides/recipes/adfs\\.md"
---

<objective>
Ship the AUTHN-04 operator runbook: `guides/recipes/adfs.md`. This is the documentation half
of the SP-advertises-vs-emits contract — operators paste the canonical PowerShell into ADFS,
match it to the `:adfs` preset (Plan 35-04), and the SAML handshake works. Without this file,
AUTHN-04 acceptance criterion 5 (runbook covers claim rules, PowerShell, SHA interop,
`WantAuthnRequestsSigned` flag) is unmet.

Purpose: Close AUTHN-04 by delivering the runbook at `guides/recipes/adfs.md` (the path
RESEARCH.md §3.5 confirmed via `ls` — `guides/providers/` does NOT exist). Verify the
Microsoft Learn `Set-AdfsRelyingPartyTrust` (Server 2025) parameter names BEFORE pasting
the canonical block, so the documentation does not silently drift from the IdP-side cmdlet
surface. Wire `ci.docs` to fail-fast on accidental deletion.

Output: One new operator-facing markdown file at `guides/recipes/adfs.md` and one new
presence-gate line in `mix.exs` `ci.docs` alias.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-signed-authnrequests-adfs-preset/35-CONTEXT.md
@.planning/phases/35-signed-authnrequests-adfs-preset/35-RESEARCH.md
@.planning/phases/35-signed-authnrequests-adfs-preset/35-PATTERNS.md
@.planning/phases/35-signed-authnrequests-adfs-preset/35-VALIDATION.md
@guides/recipes/okta.md
@guides/recipes/entra.md
@guides/recipes/google_workspace.md
@prompts/relyra-brand-book.md

<interfaces>
<!-- Key facts the executor needs. Do not explore the codebase for these. -->

From CONTEXT.md D-15 — Relyra.Provider.ADFS.default_config/0 (Plan 35-04):
  %{
    provider_preset: :adfs,
    sign_authn_requests: true,
    signed_request_encoding: :adfs_lower,
    algorithm_policy: %{signing: :rsa_sha256, digest: :sha256},
    allow_idp_initiated?: false,
    require_signed_assertions?: true,
    require_signed_response?: true
  }

From CONTEXT.md D-18 — canonical PowerShell block (paste verbatim unless Task 1 finds drift):
  $cert = New-Object System.Security.Cryptography.X509Certificates.X509Certificate2 "C:\path\to\sp-signing.cer"
  Set-AdfsRelyingPartyTrust `
    -TargetName "Relyra App" `
    -SignedSamlRequestsRequired $true `
    -RequestSigningCertificate @($cert) `
    -SignatureAlgorithm "https://www.w3.org/2001/04/xmldsig-more#rsa-sha256" `
    -SamlResponseSignature "MessageAndAssertion"

From CONTEXT.md D-06 — Application config seam for the SP signing private key:
  config :relyra, :sp_signing_key_pem, File.read!("priv/sp_signing.pem")

From mix.exs (~lines 141-146) — existing ci.docs alias to extend:
  "ci.docs": [
    "cmd test -f guides/batteries_included.md",
    "cmd test -f BATTERIES_INCLUDED.md",
    "test test/mix/tasks/relyra_batteries_included_test.exs --warnings-as-errors",
    "test test/mix/relyra_install_test.exs test/test_support_demo_test.exs --warnings-as-errors",
    "relyra.batteries_included --check"
  ],

Microsoft Learn URL to verify in Task 1:
  https://learn.microsoft.com/en-us/powershell/module/adfs/set-adfsrelyingpartytrust
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify Microsoft Learn Set-AdfsRelyingPartyTrust parameters (RESEARCH.md A1 MEDIUM-confidence)</name>
  <files>
    .planning/phases/35-signed-authnrequests-adfs-preset/35-09-NOTES.md
  </files>
  <read_first>
    @.planning/phases/35-signed-authnrequests-adfs-preset/35-CONTEXT.md (decision D-18 — the canonical PowerShell block; this verification task gates whether the block is pasted verbatim or corrected)
    @.planning/phases/35-signed-authnrequests-adfs-preset/35-RESEARCH.md (§3.5 runbook path; §5 Microsoft Learn citation; A1 lists this as a MEDIUM-confidence assumption requiring Wave 0 verification)
  </read_first>
  <action>
    Fetch `https://learn.microsoft.com/en-us/powershell/module/adfs/set-adfsrelyingpartytrust`
    (the Server 2025 cmdlet reference) using the WebFetch tool and confirm that all four
    parameter names cited in CONTEXT.md D-18 are present in the cmdlet's current parameter
    list: `-SignedSamlRequestsRequired`, `-RequestSigningCertificate`, `-SignatureAlgorithm`,
    `-SamlResponseSignature`. Also confirm `-TargetName` is still the current naming for the
    relying-party trust identifier parameter.

    Record the verification result in a new file
    `.planning/phases/35-signed-authnrequests-adfs-preset/35-09-NOTES.md` with these sections:
    (1) UTC verification date, (2) URL fetched, (3) snapshot table listing each of the five
    parameter names with status (`present` | `renamed-to-<X>` | `removed`), (4) any
    parameter-currency discrepancy callout, (5) a one-line directive for Task 2 — either
    "PASTE-VERBATIM" (no drift) or "PASTE-WITH-CORRECTIONS: <list>" (specific replacements).

    If ANY parameter has drifted, Task 2 MUST use the corrected names in the runbook's
    PowerShell block AND the runbook's PROVENANCE footer MUST document the drift. If all four
    parameters are present and unchanged, the runbook pastes the D-18 block verbatim.

    Do NOT edit `guides/recipes/adfs.md` in this task — Task 2 owns the runbook write. This
    task produces ONLY the verification note.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-signed-authnrequests-adfs-preset/35-09-NOTES.md &amp;&amp; grep -q "PASTE-VERBATIM\|PASTE-WITH-CORRECTIONS" .planning/phases/35-signed-authnrequests-adfs-preset/35-09-NOTES.md &amp;&amp; grep -cE "SignedSamlRequestsRequired|RequestSigningCertificate|SignatureAlgorithm|SamlResponseSignature" .planning/phases/35-signed-authnrequests-adfs-preset/35-09-NOTES.md | grep -qE '^[4-9]$|^[1-9][0-9]+$'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/35-signed-authnrequests-adfs-preset/35-09-NOTES.md` exists
    - The note contains a snapshot table with all five parameter names listed
    - The note contains exactly one of "PASTE-VERBATIM" or "PASTE-WITH-CORRECTIONS:" (which Task 2 reads to decide what to paste)
    - A UTC verification date in ISO 8601 form (e.g. 2026-05-26) is recorded
    - The fetched URL is recorded verbatim
  </acceptance_criteria>
  <done>
    Microsoft Learn parameter currency is verified; Task 2 has a clear directive to paste verbatim or paste with specific corrections; the verification artifact is committed for future audit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write guides/recipes/adfs.md with six required sections (AUTHN-04 D-17)</name>
  <files>
    guides/recipes/adfs.md
  </files>
  <read_first>
    @guides/recipes/okta.md (structural template — H1, Tested-against, Relyra-owns/IdP-owns/Host-owns tri-table, numbered config sections, common-failures table, day-2 notes, related case studies; same shape used here)
    @guides/recipes/entra.md (sibling template — operator-tone reference; identity-provider-specific section ordering)
    @guides/recipes/google_workspace.md (sibling template — minimal preset operator copy; parallel structure)
    @prompts/relyra-brand-book.md (voice — calm, exact, transparent, operator-friendly; no marketing fluff; each section opens with the operator's task, not Relyra's feature)
    @.planning/phases/35-signed-authnrequests-adfs-preset/35-CONTEXT.md (D-17 required sections list; D-18 canonical PowerShell block; D-08/D-09 :adfs_lower quirk; D-15 preset defaults)
    @.planning/phases/35-signed-authnrequests-adfs-preset/35-09-NOTES.md (Task 1 output — PASTE-VERBATIM or PASTE-WITH-CORRECTIONS directive)
  </read_first>
  <action>
    Create `guides/recipes/adfs.md` mirroring the structural pattern of `guides/recipes/okta.md`
    (H1, Tested-against preamble, Relyra-owns/IdP-owns/Host-owns tri-table, numbered sections,
    Common failures table, Day-2 notes, Related case studies closers). The H1 MUST be
    `# Active Directory Federation Services + Relyra`.

    The file MUST contain these six required H2 sections (exact headers used by the grep gate
    in `<acceptance_criteria>` — header text is load-bearing):
      1. `## Overview` — when to use the `:adfs` preset; ADFS 3.0 / 4.0 / 5.0 version notes;
         the strict-by-default posture (the preset opts INTO `sign_authn_requests: true`
         because ADFS rejects unsigned AuthnRequests when `WantAuthnRequestsSigned` is set on
         the trust). One paragraph, operator-tone.
      2. `## SP-side config` — minimal `:relyra` Application config showing
         `config :relyra, :sp_signing_key_pem, File.read!(...)` (the new Phase 35 config seam
         from D-06) AND a connection creation snippet using
         `Relyra.Provider.apply_defaults(:adfs, ...)`. Cite the seven defaults the preset
         applies (from D-15 / Plan 35-04 `Relyra.Provider.ADFS.default_config/0`): the field
         names verbatim are `provider_preset`, `sign_authn_requests`,
         `signed_request_encoding`, `algorithm_policy`, `allow_idp_initiated?`,
         `require_signed_assertions?`, `require_signed_response?`.
      3. `## ADFS-side PowerShell` — paste the canonical block per the Task 1 directive
         (PASTE-VERBATIM = use the D-18 block exactly; PASTE-WITH-CORRECTIONS = apply the
         specific replacements Task 1 listed). The block MUST be inside a fenced
         ` ```powershell ` code block. Place a one-paragraph operator note above the block
         explaining that the `-TargetName` value MUST match the existing relying-party trust
         display name and that the `.cer` path is the SP's PUBLIC certificate (NOT the
         private key — the private key stays in the SP via `:sp_signing_key_pem`).
      4. `## Claim rules` — paste-ready ADFS "Issuance Transform Rules" template emitting
         NameID + the four standard attributes (`emailaddress`, `givenname`, `surname`,
         `name`). Use the standard ADFS claim-rules language syntax (`c:[Type ==
         "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/..."] => issue(...)`). Two
         rules: rule 1 pass-through for the four LDAP-sourced attributes; rule 2 transforms
         the `windowsaccountname` claim into the SAML NameID (with `Format` =
         `urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress`). Operator-tone preamble.
      5. `## Interop notes` — three sub-points using `###` headers or a bullet list:
         (a) The `+`-vs-`%20` and lowercase-hex encoding quirk — ADFS 3.0+ uses lowercase
         percent-encoding (`%2f` not `%2F`); the `:adfs_lower` value of the new connection
         field `signed_request_encoding` selects this; the preset sets it for you, but
         operators can override per-connection by setting `signed_request_encoding:
         :rfc3986_upper` if they hit an ADFS deployment that has been patched to accept
         uppercase hex.
         (b) SHA-1-vs-SHA-256 redirect-binding interop — Relyra signs with RSA-SHA256
         (`http://www.w3.org/2001/04/xmldsig-more#rsa-sha256`); ADFS Server 2016+ defaults
         to SHA-256 verification; older ADFS deployments may need SHA-1 (which Relyra
         REFUSES to emit outbound — no `legacy_sha1` escape hatch on the SP signing path).
         (c) `-SignatureAlgorithm` (outbound, this PowerShell parameter) is independent of
         inbound `SigAlg` verification — the cmdlet parameter configures ADFS's OUTBOUND
         response signing algorithm; the inbound `SigAlg` query-string value Relyra emits
         is governed by Relyra's `algorithm_policy.signing` preset default (`:rsa_sha256`)
         and is verified by ADFS against the `SignedSamlRequestsRequired` trust setting.
      6. `## Troubleshooting` — table mirroring `guides/recipes/okta.md` §4 Common failures
         shape. MUST include at minimum these three rows (operator pastes the symptom they're
         seeing and finds the fix):
         - Row: ADFS rejects the AuthnRequest with `MSIS7066` / unsigned-request error →
           cause: `SignedSamlRequestsRequired` is `$true` on the trust but the SP is not
           signing → fix: confirm `sign_authn_requests: true` on the connection (the preset
           does this) and that `:sp_signing_key_pem` is set.
         - Row: Operator sees the spec name `WantAuthnRequestsSigned` in metadata but the
           PowerShell cmdlet uses `SignedSamlRequestsRequired` → cause: ADFS PowerShell
           surface and the SAML 2.0 metadata vocabulary diverge → fix: same concept, two
           names; set `SignedSamlRequestsRequired $true` in the cmdlet to satisfy the
           spec's `WantAuthnRequestsSigned="true"` on the IdP side.
         - Row: ADFS rejects with `Destination` mismatch → cause: AuthnRequest's
           `Destination` attribute does not match the ADFS SSO URL (OASIS Bindings §3.4.5.2
           requires Destination on signed redirect-binding AuthnRequests) → fix: confirm
           `idp_sso_url` on the connection exactly matches the ADFS `/adfs/ls/` endpoint
           ADFS expects; Relyra emits this URL as `Destination` (Plan 05 Task 4).

    Below the six required sections, mirror `okta.md`'s closers:
      - `## Day-2 notes` (rotation of `:sp_signing_key_pem`, claim-rules drift, ADFS server
        upgrades)
      - `## Related case studies` (link to `../case_studies/phoenix_saas_tenant_onboarding.md`
        and `../case_studies/operator_managed_rollout.md`)
      - PROVENANCE footer (italic markdown line at the very bottom) recording the
        Microsoft Learn verification date from Task 1's note AND the URL fetched, e.g.
        `_PowerShell parameter currency verified against
        https://learn.microsoft.com/en-us/powershell/module/adfs/set-adfsrelyingpartytrust
        on 2026-05-26._` This is the documentation half of the metadata-toggle-skew
        contract: if the cmdlet surface drifts on ADFS's side, future re-verification has a
        baseline to diff against.

    Voice: per the brand book — calm, exact, transparent, operator-friendly. Each H2 opens
    with the operator's TASK ("Configure the relying-party trust...") not Relyra's feature
    ("Relyra offers..."). No marketing language. No "easy" / "simple" / "magic". Use
    ADFS's exact PowerShell vocabulary when referencing cmdlet parameters.

    Cross-link to the sibling recipes at the top of the file in a one-line footer to
    `Tested against` (mirroring the Okta runbook style): "See also: Okta, Entra,
    Google Workspace" with relative links to `okta.md`, `entra.md`, `google_workspace.md`.

    The file MUST NOT modify any existing `guides/recipes/*.md` — those are sibling
    runbooks owned by other phases.
  </action>
  <verify>
    <automated>test -f guides/recipes/adfs.md &amp;&amp; grep -q "^## Overview$" guides/recipes/adfs.md &amp;&amp; grep -q "^## SP-side config$" guides/recipes/adfs.md &amp;&amp; grep -q "^## ADFS-side PowerShell$" guides/recipes/adfs.md &amp;&amp; grep -q "^## Claim rules$" guides/recipes/adfs.md &amp;&amp; grep -q "^## Interop notes$" guides/recipes/adfs.md &amp;&amp; grep -q "^## Troubleshooting$" guides/recipes/adfs.md &amp;&amp; grep -qE "SignedSamlRequestsRequired|RequestSigningCertificate|SignatureAlgorithm|SamlResponseSignature" guides/recipes/adfs.md &amp;&amp; grep -q "WantAuthnRequestsSigned" guides/recipes/adfs.md &amp;&amp; grep -q "adfs_lower" guides/recipes/adfs.md &amp;&amp; grep -q "learn\.microsoft\.com.*set-adfsrelyingpartytrust" guides/recipes/adfs.md</automated>
  </verify>
  <acceptance_criteria>
    - `guides/recipes/adfs.md` exists at exactly that path (NOT `guides/providers/adfs.md`)
    - All six required H2 sections present verbatim: `## Overview`, `## SP-side config`, `## ADFS-side PowerShell`, `## Claim rules`, `## Interop notes`, `## Troubleshooting`
    - The four PowerShell parameter names from CONTEXT.md D-18 appear in the file (matching Task 1's PASTE directive: verbatim or with corrections)
    - The string `WantAuthnRequestsSigned` appears (Troubleshooting row covers the name divergence — AUTHN-04 criterion 5)
    - The string `adfs_lower` appears (Interop notes covers the lowercase-hex quirk)
    - The Microsoft Learn URL `learn.microsoft.com/.../set-adfsrelyingpartytrust` appears in the PROVENANCE footer
    - No existing `guides/recipes/*.md` files are modified (verify by `git diff --name-only guides/recipes/` showing only the new `adfs.md`)
    - `mix format --check-formatted` still exits 0 (markdown is not formatted by `mix format` but the gate must remain green)
  </acceptance_criteria>
  <done>
    The AUTHN-04 operator runbook ships at `guides/recipes/adfs.md` with all six required sections, the canonical PowerShell block (verified against Microsoft Learn in Task 1), claim-rules paste-ready template, interop notes, troubleshooting table covering the `WantAuthnRequestsSigned`/`SignedSamlRequestsRequired` divergence and the `Destination` requirement, and a PROVENANCE footer recording the verification date.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire ci.docs presence gate + mix.exs docs extras for the new runbook</name>
  <files>
    mix.exs
  </files>
  <read_first>
    @mix.exs (lines ~100-150 — `docs/0` extras list at lines 107-126 AND `ci.docs` alias at lines 141-146; both must be extended)
    @.planning/phases/35-signed-authnrequests-adfs-preset/35-PATTERNS.md (mix.exs section — line 821-851 — confirms the two-place wiring: extras list AND ci.docs presence gate)
    @.planning/phases/35-signed-authnrequests-adfs-preset/35-RESEARCH.md (§3.5 paragraph on the ci.docs presence gate alongside batteries_included.md)
  </read_first>
  <action>
    Make two additive edits to `mix.exs`:

    Edit 1 — `docs/0` extras list (~line 107-126): append `"guides/recipes/adfs.md"` to the
    extras list, placed immediately after the existing `"guides/recipes/google_workspace.md"`
    entry (~line 125). Preserve alphabetical-by-filename order within the recipes block.

    Edit 2 — `ci.docs` alias (~line 141-146): add a new line `"cmd test -f
    guides/recipes/adfs.md",` inside the alias body, placed immediately after the existing
    `"cmd test -f guides/batteries_included.md",` line (~line 142). The comma is required
    (list element). Do NOT collapse with the existing line — each `cmd test -f` is its own
    list element (mirrors the existing per-file presence gates).

    Do NOT touch any other line in `mix.exs`. Do NOT add a new alias. Do NOT modify
    `ci.security`, `ci.fast`, `ci.verify`, or any other alias. The two edits are surgical
    and additive only.

    After the edits, run `mix format --check-formatted` to confirm formatting is preserved
    (the project enforces `mix format` exit 0 per CLAUDE.md "Testing Requirements"). Then
    run `mix ci.docs` to confirm the new presence gate passes and the existing gates do not
    regress.
  </action>
  <verify>
    <automated>grep -c '"guides/recipes/adfs.md"' mix.exs | grep -qE '^[12]$' &amp;&amp; grep -q 'cmd test -f guides/recipes/adfs\.md' mix.exs &amp;&amp; mix format --check-formatted &amp;&amp; mix ci.docs</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` contains the string `"guides/recipes/adfs.md"` in the `docs/0` extras list (1 occurrence)
    - `mix.exs` contains the string `cmd test -f guides/recipes/adfs.md` in the `ci.docs` alias (1 occurrence)
    - Combined count of `"guides/recipes/adfs.md"` occurrences in `mix.exs` is 1 or 2 (extras-list quoted form + ci.docs unquoted form within the `cmd test -f` line; the grep filter `[12]` accepts either how the shell sees it)
    - `mix format --check-formatted` exits 0
    - `mix ci.docs` exits 0 (the new presence gate fires; the existing gates remain green)
    - No other lines in `mix.exs` are modified (verify by `git diff mix.exs` showing only +2 lines in the expected blocks)
  </acceptance_criteria>
  <done>
    The runbook is wired into `mix ci.docs` so accidental deletion fails CI, and into the
    `docs/0` extras list so ExDoc publishes it alongside the other recipes. The full
    `mix ci.docs` alias remains green; `mix format --check-formatted` remains green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → ADFS PowerShell | Operator pastes the canonical block into Server PowerShell; ADFS configures the relying-party trust |
| Operator → Relyra connection | Operator runs `Relyra.Provider.apply_defaults(:adfs, ...)`; the preset defaults flow into a `Connection` row |
| Documentation → IdP behavior | The runbook documents the spec-vs-cmdlet name divergence (`WantAuthnRequestsSigned` ↔ `SignedSamlRequestsRequired`); a drifted runbook ships drifted ADFS configs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Tampering (skew) | `guides/recipes/adfs.md` — the documentation half of the SP-advertises-vs-emits contract | mitigate | Task 1 re-verifies Microsoft Learn `Set-AdfsRelyingPartyTrust` (Server 2025) parameter names BEFORE the runbook is written; the runbook records the verification date in a PROVENANCE footer so future re-verifications have a baseline to diff against. The runbook explicitly documents the `WantAuthnRequestsSigned` (spec) vs `SignedSamlRequestsRequired` (PowerShell) name divergence so operators do not silently set the wrong flag. The `Destination` requirement (Plan 05 Task 4 — OASIS Bindings §3.4.5.2) is documented in Troubleshooting so operators do not assume Relyra emits a request missing that attribute. The `ci.docs` presence gate (Task 3) catches accidental deletion of the runbook in CI. |
| T-35-04-DOC | Repudiation | Microsoft Learn parameter currency drift | accept | Microsoft updates Learn docs ad-hoc; full automated drift detection is not amenable (the cmdlet reference page is HTML, not a versioned spec). Mitigation: Task 1's manual re-verification + PROVENANCE footer makes the verification date auditable; the next time the runbook is updated, the executor can diff the page against the recorded date and flag drift. |
</threat_model>

<verification>
- Run `mix ci.docs` end-to-end; it MUST exit 0 with the new presence gate firing.
- `git diff --name-only` MUST show exactly two new/modified files: `guides/recipes/adfs.md`
  (created), `mix.exs` (modified) — plus the verification artifact at
  `.planning/phases/35-signed-authnrequests-adfs-preset/35-09-NOTES.md`. No other
  `guides/recipes/*.md` may appear in the diff.
- `grep -c '^## ' guides/recipes/adfs.md` MUST return a count ≥ 8 (six required sections +
  Day-2 + Related case studies).
- `mix format --check-formatted` MUST exit 0.
- The PROVENANCE footer in `guides/recipes/adfs.md` MUST match the verification date in
  `35-09-NOTES.md`.
</verification>

<success_criteria>
- `guides/recipes/adfs.md` exists at exactly that path (NOT `guides/providers/`)
- The runbook contains the six required H2 sections verbatim (Overview, SP-side config,
  ADFS-side PowerShell, Claim rules, Interop notes, Troubleshooting)
- The PowerShell block uses parameter names verified against Microsoft Learn
  `Set-AdfsRelyingPartyTrust` (Server 2025) on the date recorded in the PROVENANCE footer
- The runbook documents: `WantAuthnRequestsSigned` ↔ `SignedSamlRequestsRequired` name
  divergence; `:adfs_lower` lowercase-hex quirk; SHA-1-vs-SHA-256 redirect-binding interop;
  the independence of `-SignatureAlgorithm` (outbound) from inbound `SigAlg` verification;
  the `Destination` requirement
- `mix ci.docs` exits 0
- `mix format --check-formatted` exits 0
- No existing `guides/recipes/*.md` files are modified
- AUTHN-04 acceptance criterion 5 (runbook covers claim rules, PowerShell, SHA interop,
  `WantAuthnRequestsSigned` flag) is met
</success_criteria>

<output>
Create `.planning/phases/35-signed-authnrequests-adfs-preset/35-09-SUMMARY.md` when done,
recording: the verification date from Task 1, the PASTE directive that was followed
(VERBATIM or WITH-CORRECTIONS), the final byte count of `guides/recipes/adfs.md`, the
section count, and the `mix ci.docs` final exit code.
</output>
