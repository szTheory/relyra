---
phase: 49.2-v1.6-nyquist-retro-editorial-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/operations/incident_playbook.md
  - docs/jtbd_gap_map.md
  - guides/recipes/generic_saml.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Playbook Scenarios 3–6 Diagnose prose has clearer Triage→Diagnose→Recover flow without changing trace wiring."
    - "jtbd_gap_map Operator/SRE and generic SAML personas read cleanly without gap-score changes."
    - "SiteMinder decoder row has honest README seven-family drift footnote (PingFederate pattern)."
  artifacts:
    - path: "guides/operations/incident_playbook.md"
      provides: "Editorial skim on Scenarios 3–6 Diagnose sections."
    - path: "docs/jtbd_gap_map.md"
      provides: "Persona tone readability pass only."
    - path: "guides/recipes/generic_saml.md"
      provides: "SiteMinder row footnote clarifying extra decoder row vs README taxonomy."
---

<objective>
Complete optional editorial tech debt from the v1.6 milestone audit (ROADMAP success criterion #2): light prose polish on playbook scenarios, jtbd_gap_map personas, and SiteMinder decoder footnote. No structural or trace-fact changes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/49.2-v1.6-nyquist-retro-editorial-polish/49.2-CONTEXT.md
@.planning/v1.6-MILESTONE-AUDIT.md
@.planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-VERIFICATION.md
@.planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-03-SUMMARY.md
@guides/operations/incident_playbook.md
@docs/jtbd_gap_map.md
@guides/recipes/generic_saml.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Breaking 48 automated greps (login trace wiring) | high | Do not remove `login trace` / `mix relyra.trace` / LiveView route references; prose-only edits |
| ADOPT-05 persona/gap reclassification | medium | No changes to Strong/Partial labels or gap #1–#4 shipped markers |
| SiteMinder added to README seven-family | medium | Footnote only in generic_saml.md; README unchanged |
| New ci.docs drift test | low | D-10: run `mix ci.docs` only; no mix.exs change |
</threat_model>

<tasks>

<task type="auto">
  <name>Editorial skim — incident playbook Scenarios 3–6 Diagnose prose</name>
  <read_first>
    - guides/operations/incident_playbook.md (## Scenario 3 through ## Scenario 6)
    - .planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-VERIFICATION.md (automated checks must remain valid)
    - .planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-VALIDATION.md (manual-only: scenario flow)
  </read_first>
  <action>
    In `guides/operations/incident_playbook.md`, for **Scenario 3** (Replay storm), **Scenario 4** (Signature regression), **Scenario 5** (ACS misconfiguration), and **Scenario 6** (Attribute mapping):

    1. Within each scenario's **Diagnose** subsection only, tighten prose so each Diagnose step opens with an action verb and explicitly bridges from Triage (what you already know) to the next Recover prep — one short transitional sentence max per scenario if needed.

    2. **Preserve unchanged:**
       - All `login trace` / `Login trace` / `mix relyra.trace` / `connections/:connection_id/trace` strings
       - Scenario headings, Triage bullet facts, Recover steps, and evidence-surfaces table
       - Scenario 3–6 structure (no new subsections)

    3. Target: reduce redundant "use the login trace" repetition where Diagnose already names trace in the prior sentence; keep at least one explicit login trace reference per scenario Diagnose block.

    Do not edit Scenarios 1–2 or Evidence surfaces table.
  </action>
  <verify>grep -c "login trace\|Login trace" guides/operations/incident_playbook.md</verify>
  <acceptance_criteria>
    - `grep -c "login trace\|Login trace" guides/operations/incident_playbook.md` outputs `6` or greater (48-VERIFICATION baseline).
    - `grep "connections/:connection_id/trace" guides/operations/incident_playbook.md` exits 0.
    - `grep "## Scenario 3: Replay storm" guides/operations/incident_playbook.md` exits 0.
    - `grep "## Scenario 6: Attribute mapping" guides/operations/incident_playbook.md` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Editorial skim — jtbd_gap_map persona tone</name>
  <read_first>
    - docs/jtbd_gap_map.md (Operator/SRE persona section and Custom/generic SAML persona section)
    - .planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-02-SUMMARY.md (ADOPT-05 locked content)
  </read_first>
  <action>
    In `docs/jtbd_gap_map.md`, light readability pass **only** on:

    1. **Operator / SRE** persona paragraph(s) — shorten run-on sentences; keep **Strong** rating and all "Shipped (v1.x)" gap markers verbatim.

    2. **Custom / generic SAML** persona paragraph(s) — same constraints; do not change gap table scores, "What changed" section dates, or demand-gated milestone ordering.

    No edits to CONFORMANCE cross-refs, decoder table, or Biggest gaps #1–#4 demotion text beyond typo/clarity fixes.
  </action>
  <verify>grep "Strong" docs/jtbd_gap_map.md && grep "2026-05-27" docs/jtbd_gap_map.md</verify>
  <acceptance_criteria>
    - `grep "Strong" docs/jtbd_gap_map.md` exits 0 (persona ratings preserved).
    - `grep "2026-05-27" docs/jtbd_gap_map.md` exits 0 (What changed date preserved).
    - `grep "Shipped (v1.6)" docs/jtbd_gap_map.md` exits 0.
    - File `docs/jtbd_gap_map.md` still contains substring `Operator` and `generic SAML`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Add SiteMinder decoder row footnote</name>
  <read_first>
    - guides/recipes/generic_saml.md (PingFederate row ~line 131 and SiteMinder row ~line 132)
    - README.md (seven-family list — read only, do not edit)
    - .planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-03-SUMMARY.md
  </read_first>
  <action>
    In `guides/recipes/generic_saml.md`, extend the **CA SiteMinder** table row Notes cell (last column) by appending this sentence after the existing SiteMinder deployment text:

    `README lists seven SAML families (Okta, Entra, Google Workspace, ADFS, Ping, CyberArk, Shibboleth); this decoder row documents SiteMinder as an additional admin-UI label — not an eighth README family.`

    Keep the SiteMinder row in the table. Do **not** edit README.md. Do **not** add a SiteMinder row to Getting Started §4 four-preset list.

    PingFederate row footnote pattern is the style reference ("README lists this family as **Ping**").
  </action>
  <verify>grep -i "seven SAML families" guides/recipes/generic_saml.md</verify>
  <acceptance_criteria>
    - `grep -i "seven SAML families" guides/recipes/generic_saml.md` exits 0.
    - `grep "CA SiteMinder" guides/recipes/generic_saml.md` exits 0.
    - `grep -i "eighth README family" guides/recipes/generic_saml.md` exits 0.
    - `grep -i "siteminder" README.md` exits 1 OR only appears in unrelated context (README must not gain SiteMinder as eighth family).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>mix ci.docs after editorial edits</name>
  <read_first>
    - mix.exs (ci.docs alias)
    - .planning/phases/49.2-v1.6-nyquist-retro-editorial-polish/49.2-CONTEXT.md (D-10)
  </read_first>
  <action>
    Run:

    ```bash
    mix ci.docs
    ```

    Do **not** modify `mix.exs` unless ci.docs fails for a reason unrelated to these markdown edits.
  </action>
  <verify>mix ci.docs</verify>
  <acceptance_criteria>
    - `mix ci.docs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `grep -c "login trace\|Login trace" guides/operations/incident_playbook.md` ≥ 6
- `grep -i "seven SAML families" guides/recipes/generic_saml.md`
- `mix ci.docs`
</verification>

<success_criteria>
- Three editorial targets updated per D-07–D-09.
- mix ci.docs green.
</success_criteria>
