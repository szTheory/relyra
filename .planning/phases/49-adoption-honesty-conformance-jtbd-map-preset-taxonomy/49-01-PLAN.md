---
phase: 49-adoption-honesty-conformance-jtbd-map-preset-taxonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/relyra.conformance.ex
  - priv/conformance/sp_manifest.json
  - test/conformance/sp_conformance_test.exs
  - CONFORMANCE.md
autonomous: true
requirements:
  - ADOPT-04
must_haves:
  truths:
    - "CONFORMANCE.md contains Scope boundary & diminishing returns section generated by mix relyra.conformance."
    - "sp_manifest.json row sp-encrypted-assertions-pass has status pass and expected_outcome ok."
    - "Requirement summary shows pass 9 deferred 0 after regen."
    - "sp_conformance_test evaluate_row runs FakeIdP.encrypted_response positive control."
    - "mix ci.conformance exits 0."
  artifacts:
    - path: "lib/mix/tasks/relyra.conformance.ex"
      provides: "scope_boundary_section/0 appended after CVE-REG-01 table in render_report/1."
    - path: "priv/conformance/sp_manifest.json"
      provides: "ENC row flipped from deferred to pass."
    - path: "test/conformance/sp_conformance_test.exs"
      provides: "Executable encrypted assertion positive control."
    - path: "CONFORMANCE.md"
      provides: "Regenerated report with scope section and updated ENC row."
---

<objective>
Deliver ADOPT-04: add CONFORMANCE scope-boundary section via the generator, flip the ENC manifest row to pass with an executable evaluate_row, and regenerate CONFORMANCE.md so ci.conformance stays green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-CONTEXT.md
@.planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-RESEARCH.md
@.planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-PATTERNS.md
@.planning/STRATEGIC-ASSESSMENT-2026-05-23.md
@lib/mix/tasks/relyra.conformance.ex
@priv/conformance/sp_manifest.json
@test/conformance/sp_conformance_test.exs
@test/security/xml_enc_adversarial_test.exs
@lib/relyra/test_support/fake_idp.ex
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Hand-edited CONFORMANCE.md drifts from manifest | high | Generator-only edits; regen + --check gate |
| ENC pass row without crypto positive control | high | evaluate_row uses FakeIdP.encrypted_response through consume_response |
| Application.put_env race in async tests | medium | Change SPConformanceTest to async: false when adding SP key setup |
| Scope section omits demand-gated items | low | Include AUTHN-POST-01, KMS-01, SIGNED-META-01 in prose |
</threat_model>

<tasks>

<task type="auto">
  <name>Add scope boundary section to conformance generator</name>
  <read_first>
    - lib/mix/tasks/relyra.conformance.ex (render_report/1 lines 79-101)
    - .planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-CONTEXT.md (D-01, D-02)
    - .planning/STRATEGIC-ASSESSMENT-2026-05-23.md (Diminishing-returns line section)
  </read_first>
  <action>
    In `lib/mix/tasks/relyra.conformance.ex`:

    1. Add private function `scope_boundary_section/0` returning a markdown string list (same pattern as `cve_summary_lines/1`).

    2. Section heading MUST be exactly: `## Scope boundary & diminishing returns`

    3. Section body MUST include these bullets (paraphrase allowed, all items required):
       - v0.x through v1.5 shipping arc: strict SAML SP library with crypto verification, presets, generic runbook, SLO, ENC-01, login trace, incident playbook
       - Explicit out-of-scope / demand-gated (not missing): HTTP-Artifact binding, ECP profile, Attribute Query, SCIM-in-core, additional first-class presets without generic-path investment, standalone demo app, customer-admin self-service portal
       - Demand-gated protocol extensions: AUTHN-POST-01, KMS-01, SIGNED-META-01
       - Done-enough framing: ~92-95% JTBD coverage; further work is demand-gated not coverage-gated (v1.6 Adoption Truth)

    4. In `render_report/1`, append `scope_boundary_section()` AFTER the CVE-REG-01 table block and BEFORE the final `Enum.join("\n")`.

    Do NOT hand-edit CONFORMANCE.md in this task.
  </action>
  <verify>grep -n "scope_boundary_section\|Scope boundary" lib/mix/tasks/relyra.conformance.ex</verify>
  <acceptance_criteria>
    - `grep "defp scope_boundary_section" lib/mix/tasks/relyra.conformance.ex` exits 0.
    - `grep "Scope boundary & diminishing returns" lib/mix/tasks/relyra.conformance.ex` exits 0.
    - `grep "HTTP-Artifact" lib/mix/tasks/relyra.conformance.ex` exits 0.
    - `grep "demand-gated" lib/mix/tasks/relyra.conformance.ex` exits 0.
    - `render_report/1` calls `scope_boundary_section()` after `security_rows_table`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Flip ENC manifest row to pass</name>
  <read_first>
    - priv/conformance/sp_manifest.json (lines 212-224)
    - .planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-CONTEXT.md (D-04)
  </read_first>
  <action>
    In `priv/conformance/sp_manifest.json`, replace the row with id `sp-encrypted-assertions-deferred`:

    - Change `"id"` to `"sp-encrypted-assertions-pass"`
    - Change `"status"` from `"deferred"` to `"pass"`
    - Change `"expected_outcome"` from `{"result": "deferred"}` to `{"result": "ok"}`
    - Update `"notes"` to: `Encrypted assertion positive control via FakeIdP.encrypted_response/2 — ENC-01 shipped Phase 34.`

    Leave all other manifest rows unchanged.
  </action>
  <verify>grep -A5 "sp-encrypted-assertions-pass" priv/conformance/sp_manifest.json</verify>
  <acceptance_criteria>
    - `grep "sp-encrypted-assertions-pass" priv/conformance/sp_manifest.json` exits 0.
    - `grep "sp-encrypted-assertions-deferred" priv/conformance/sp_manifest.json` exits 1 (no match).
    - Row contains `"status": "pass"`.
    - Row contains `"expected_outcome": {"result": "ok"}`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Add encrypted assertion evaluate_row and SP key setup</name>
  <read_first>
    - test/conformance/sp_conformance_test.exs (evaluate_row clauses lines 58-164)
    - test/security/xml_enc_adversarial_test.exs (setup lines 63-74, positive control lines 105-116)
    - lib/relyra/test_support/fake_idp.ex (encrypted_response/2)
    - .planning/phases/49-adoption-honesty-conformance-jtbd-map-preset-taxonomy/49-CONTEXT.md (D-05)
  </read_first>
  <action>
    In `test/conformance/sp_conformance_test.exs`:

    1. Add `alias Relyra.TestSupport.FakeIdP` at top.

    2. Change `use ExUnit.Case, async: true` to `use ExUnit.Case, async: false` (SP private key env is process-global).

    3. Add `setup` block mirroring xml_enc_adversarial_test.exs:
       - Derive PEM from `FakeIdP.keypair()` via `:public_key.pem_encode/1`
       - `Application.put_env(:relyra, :sp_private_key_pem, pem)`
       - `on_exit` deletes the env key

    4. Add `evaluate_row/1` clause BEFORE the catch-all reject clause (line 155):

    ```elixir
    defp evaluate_row(%{"id" => "sp-encrypted-assertions-pass"}) do
      xml = FakeIdP.encrypted_response()

      assert {:ok, login_result} =
               Relyra.consume_response(
                 xml,
                 request_intent(),
                 consume_opts(now: @fixed_now)
               )

      assert login_result.in_response_to == "id_request_123"
      assert login_result.issuer == "https://idp.example.com/metadata"
      %{"result" => "ok"}
    end
    ```

    5. Ensure `connection/0` cert_chain uses `FakeIdP.self_signed_cert_pem()` (already delegates to XmldsigSigner — verify it matches encrypted_response signer).
  </action>
  <verify>mix test test/conformance/sp_conformance_test.exs --only conformance --warnings-as-errors</verify>
  <acceptance_criteria>
    - `grep "sp-encrypted-assertions-pass" test/conformance/sp_conformance_test.exs` exits 0.
    - `grep "FakeIdP.encrypted_response" test/conformance/sp_conformance_test.exs` exits 0.
    - `grep "async: false" test/conformance/sp_conformance_test.exs` exits 0.
    - `mix test test/conformance/sp_conformance_test.exs --only conformance --warnings-as-errors` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Regenerate CONFORMANCE.md and verify ci.conformance</name>
  <read_first>
    - lib/mix/tasks/relyra.conformance.ex
    - CONFORMANCE.md (current generated output)
    - mix.exs (ci.conformance alias)
  </read_first>
  <action>
    1. Run `mix relyra.conformance` to regenerate `CONFORMANCE.md` at repo root.

    2. Verify drift gate: `mix relyra.conformance --check`

    3. Verify full conformance CI: `mix ci.conformance`

    Do NOT manually edit CONFORMANCE.md — only the generator output is valid.
  </action>
  <verify>mix ci.conformance</verify>
  <acceptance_criteria>
    - `grep "Scope boundary & diminishing returns" CONFORMANCE.md` exits 0.
    - `grep "sp-encrypted-assertions-pass" CONFORMANCE.md` exits 0.
    - `grep "sp-encrypted-assertions-deferred" CONFORMANCE.md` exits 1.
    - CONFORMANCE.md requirement summary row shows `| CONF-01 | 9 |` for pass count (9 passes).
    - CONFORMANCE.md requirement summary shows `| 0 |` for deferred count.
    - `mix ci.conformance` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix ci.conformance` green
- `mix relyra.conformance --check` green
- CONFORMANCE.md contains scope boundary section and ENC pass row
</verification>
