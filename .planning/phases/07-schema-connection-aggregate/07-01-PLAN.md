---
phase: "07"
plan: "07-01"
title: "Add the Phase 07 Ecto test harness and validation contract"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - config/test.exs
  - mix.exs
  - .formatter.exs
  - .planning/phases/07-schema-connection-aggregate/07-VALIDATION.md
  - test/support/ecto_test_repo.ex
  - test/support/migration_case.ex
autonomous: true
requirements:
  - CFG-01
---

<objective>
Create the Wave 0 infrastructure Phase 07 needs before any aggregate code
is trusted: a real Ecto SQL test Repo, deterministic migration helpers,
and a validation contract that defines the per-task smoke commands and
wave-end verification path for the connection aggregate work.
</objective>

<threat_model>
  <threat id="TM-07-01-MIGRATION-BLINDNESS" severity="high">
    If Phase 07 relies only on fake repos or changeset unit tests,
    migrations, FKs, unique indexes, and adapter wiring can fail silently
    until execution is already deep into the slice.
  </threat>
  <mitigations>
    - Add a real test Repo and migration helper before schema and
      migration implementation begins.
    - Keep the Repo test-only and host-app-shaped; do not introduce a
      production library-owned Repo.
    - Publish `07-VALIDATION.md` now so execution can sample quickly and
      consistently.
  </mitigations>
  <verification_commands>
    - `test -f config/test.exs`
    - `test -f test/support/ecto_test_repo.ex`
    - `test -f test/support/migration_case.ex`
    - `test -f .planning/phases/07-schema-connection-aggregate/07-VALIDATION.md`
    - `rg -n "Ecto.Adapters.Postgres|sandbox|Repo" config/test.exs test/support/ecto_test_repo.ex test/support/migration_case.ex`
  </verification_commands>
</threat_model>

<tasks>
  <task id="07-01-T01" type="auto">
    <name>Task 1: Add the test-only Ecto Repo and migration harness</name>
    <files>config/test.exs, mix.exs, .formatter.exs, test/support/ecto_test_repo.ex, test/support/migration_case.ex</files>
    <read_first>
      - .planning/phases/07-schema-connection-aggregate/07-RESEARCH.md
      - .planning/phases/07-schema-connection-aggregate/07-PATTERNS.md
      - mix.exs
      - .formatter.exs
      - test/test_helper.exs
    </read_first>
    <action>
      Add the test-only Ecto harness: `config/test.exs`,
      `test/support/ecto_test_repo.ex`, and `test/support/migration_case.ex`.
      Follow the host-Repo and optional-Ecto analogs mapped in
      `07-PATTERNS.md`, especially `lib/relyra/request_store/ecto.ex`,
      `lib/relyra/replay_store/ecto.ex`, and the existing fake-Repo test
      modules. Keep `ecto`, `ecto_sql`, and `postgrex` optional and align
      with the repo’s current lock posture instead of planning dependency
      churn.
    </action>
    <verify>`test -f config/test.exs && test -f test/support/ecto_test_repo.ex && test -f test/support/migration_case.ex && rg -n "Ecto.Adapters.Postgres|pool|sandbox|migration" config/test.exs test/support/ecto_test_repo.ex test/support/migration_case.ex && rg -n "ecto|ecto_sql|postgrex" mix.exs && rg -n "import_deps: \\[.*ecto.*ecto_sql.*\\]" .formatter.exs`</verify>
    <acceptance_criteria>
      - `test -f config/test.exs` exits 0.
      - `test -f test/support/ecto_test_repo.ex` exits 0.
      - `test -f test/support/migration_case.ex` exits 0.
      - `rg -n "Ecto.Adapters.Postgres|pool|sandbox|migration" config/test.exs test/support/ecto_test_repo.ex test/support/migration_case.ex`
        returns at least 5 matches.
      - `rg -n "ecto|ecto_sql|postgrex" mix.exs` returns the optional
        dependency declarations.
      - `rg -n "import_deps: \\[.*ecto.*ecto_sql.*\\]" .formatter.exs`
        returns 1 match.
    </acceptance_criteria>
    <done>Phase 07 has a real test-only Repo harness and Ecto formatting/dependency scaffolding, without introducing a production library-owned Repo.</done>
  </task>

  <task id="07-01-T02" type="auto">
    <name>Task 2: Publish the Phase 07 validation contract</name>
    <files>.planning/phases/07-schema-connection-aggregate/07-VALIDATION.md</files>
    <read_first>
      - .planning/phases/07-schema-connection-aggregate/07-RESEARCH.md
      - .planning/phases/07-schema-connection-aggregate/07-PATTERNS.md
      - .planning/phases/03-behaviour-contracts-and-stores/03-VALIDATION.md
    </read_first>
    <action>
      Create `.planning/phases/07-schema-connection-aggregate/07-VALIDATION.md`
      with a Phase 07-specific requirement-to-test matrix, Wave 0 gaps,
      and a per-task smoke-command set under 30 seconds. Make the mapped
      test analogs explicit by referencing the existing Ecto store tests
      from `07-PATTERNS.md` as the model for integration-vs-unit splits.
    </action>
    <verify>`test -f .planning/phases/07-schema-connection-aggregate/07-VALIDATION.md && rg -n "Quick run command|Per-Task Verification Map|Wave 0 Requirements|mix qa" .planning/phases/07-schema-connection-aggregate/07-VALIDATION.md && rg -n "30 seconds|< 30|under 30" .planning/phases/07-schema-connection-aggregate/07-VALIDATION.md`</verify>
    <acceptance_criteria>
      - `test -f .planning/phases/07-schema-connection-aggregate/07-VALIDATION.md`
        exits 0.
      - `rg -n "Quick run command|Per-Task Verification Map|Wave 0 Requirements|mix qa" .planning/phases/07-schema-connection-aggregate/07-VALIDATION.md`
        returns at least 4 matches.
      - `rg -n "30 seconds|< 30|under 30" .planning/phases/07-schema-connection-aggregate/07-VALIDATION.md`
        returns at least 1 match.
    </acceptance_criteria>
    <done>The validation contract exists, maps every planned task to automated verification, and defines a sub-30-second smoke path plus wave-end/full-suite commands.</done>
  </task>
</tasks>

<verification>
  - `mix format --check-formatted`
  - `mix compile --warnings-as-errors`
</verification>

<must_haves>
  - Phase 07 execution has a real test Repo and migration helper before
    aggregate code begins.
  - The test harness remains test-only and does not become a production
    library-owned Repo.
  - The validation contract defines a fast per-task smoke path and a
    stricter wave-end/full verification path.
</must_haves>
