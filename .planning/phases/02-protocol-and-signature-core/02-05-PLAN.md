---
phase: "02"
plan: "02-05"
title: "Add regression corpus and verification gates for unsigned payload and correlation bypass closures"
type: "execute"
wave: 5
depends_on:
  - "02-04"
files_modified:
  - test/fixtures/security/protocol/manifest.json
  - test/fixtures/security/signature/manifest.json
  - test/protocol/consume_response_pipeline_test.exs
  - test/security/signed_node_binding_test.exs
  - test/relyra_test.exs
  - .planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05
  - PROT-02
  - PROT-03
  - PROT-05
---

<objective>
Close verification and test coverage gaps by adding explicit adversarial fixtures and regression tests that prove unsigned payload rejection, request-correlation guardrails, extracted-field trust binding, and deterministic typed failures for all reopened Phase 02 requirements. Full `PROT-04` closure remains Phase 3 scope.
</objective>

<threat_model>
  <threat id="TM-02-05-REGRESSION-BLIND-SPOT" severity="high">
    Critical trust-path bypasses can reappear if tests only assert tuple shapes or rely on test-only payload overrides.
  </threat>
  <threat id="TM-02-05-CORRELATION-DRIFT" severity="high">
    InResponseTo and RelayState checks can silently break without direct mismatch fixtures and end-to-end assertions.
  </threat>
  <threat id="TM-02-05-ALGORITHM-ASSERTION-DRIFT" severity="medium">
    Policy and signed-node checks can pass unit tests while failing in real parser-driven consume flows if manifests do not encode extracted-field cases.
  </threat>
  <mitigations>
    - Add manifest rows for unsigned payload, missing mandatory fields, `InResponseTo` mismatch, RelayState mismatch, and duplicate-ID/signed-node ambiguity classes.
    - Extend consume and signature tests to assert exact typed failures for the newly fixed paths.
    - Record rerun evidence in Phase 02 verification artifact with explicit command output for reopened requirements.
    - High-severity threats in this plan are blocking; do not mark plan complete until all verification commands pass.
  </mitigations>
  <verification_commands>
    - `mix test test/protocol/consume_response_pipeline_test.exs --warnings-as-errors`
    - `mix test test/security/signed_node_binding_test.exs --warnings-as-errors`
    - `mix test test/relyra_test.exs --warnings-as-errors`
    - `mix test test/protocol/authn_request_test.exs test/protocol/relay_state_test.exs test/security/signature_policy_test.exs test/security/signed_node_binding_test.exs test/protocol/consume_response_pipeline_test.exs test/relyra_test.exs --warnings-as-errors`
  </verification_commands>
</threat_model>

<tasks>
  <task id="02-05-T01">
    <read_first>
      - .planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md
      - test/fixtures/security/protocol/manifest.json
      - test/fixtures/security/signature/manifest.json
      - test/fixtures/security/relay_state/manifest.json
      - .planning/REQUIREMENTS.md
    </read_first>
    <action>
      Expand fixture manifests to cover the documented verification gaps. In `test/fixtures/security/protocol/manifest.json`, add concrete records for classes `unsigned_payload`, `missing_protocol_field`, `in_response_to_mismatch`, and `relay_state_mismatch` with keys `id`, `class`, `requirement_ids`, `expected_error_type`, and `payload`. In `test/fixtures/security/signature/manifest.json`, add rows that exercise parser-derived signature method/digest method absence, duplicate IDs, and ambiguous signed candidates so `SEC-02`/`SEC-03`/`SEC-04`/`SEC-05` are represented in parser-driven input, not only unit-level policy checks.
    </action>
    <acceptance_criteria>
      - `rg -n "\"class\": \"unsigned_payload\"|\"class\": \"in_response_to_mismatch\"|\"class\": \"relay_state_mismatch\"|\"class\": \"missing_protocol_field\"" test/fixtures/security/protocol/manifest.json` returns at least 4 matches.
      - `rg -n "\"requirement_ids\": \\[\"PROT-02\"\\]|\"requirement_ids\": \\[\"PROT-03\"\\]|\"requirement_ids\": \\[\"PROT-05\"\\]" test/fixtures/security/protocol/manifest.json` returns at least 3 matches.
      - `rg -n "\"class\": \"duplicate_xml_id\"|\"class\": \"ambiguous_signed_node\"|\"class\": \"missing_signature_method\"|\"class\": \"missing_digest_method\"" test/fixtures/security/signature/manifest.json` returns at least 4 matches.
      - `rg -n "\"requirement_ids\": \\[\"SEC-02\"\\]|\"requirement_ids\": \\[\"SEC-03\"\\]|\"requirement_ids\": \\[\"SEC-04\"\\]|\"requirement_ids\": \\[\"SEC-05\"\\]" test/fixtures/security/signature/manifest.json` returns at least 4 matches.
    </acceptance_criteria>
  </task>

  <task id="02-05-T02">
    <read_first>
      - lib/relyra.ex
      - lib/relyra/protocol/validation_pipeline.ex
      - lib/relyra/security/signature.ex
      - test/protocol/consume_response_pipeline_test.exs
      - test/security/signed_node_binding_test.exs
      - test/relyra_test.exs
    </read_first>
    <action>
      Update regression tests to assert the exact behaviors required by `02-VERIFICATION.md` gaps: (1) `Relyra.consume_response("<Fake>unsigned</Fake>", request_intent, relay_state: request_intent.relay_state)` returns `{:error, %Relyra.Error{type: :missing_signature}}` (or stricter parse-stage atom if parser rejects earlier), never `{:ok, _}`; (2) response/request mismatch for `in_response_to` returns `:in_response_to_mismatch`; (3) missing posted RelayState returns `:relay_state_missing`; (4) mismatched posted RelayState returns `:relay_state_mismatch`; (5) signature binding tests prove duplicate IDs and ambiguous signed-node payloads fail before protocol-stage success. Keep tests deterministic by asserting stable `error.type` atoms across repeated runs.
    </action>
    <acceptance_criteria>
      - `rg -n "unsigned payload|missing_signature|never returns \\{:ok" test/protocol/consume_response_pipeline_test.exs` returns at least 2 matches.
      - `rg -n ":in_response_to_mismatch|:relay_state_missing|:relay_state_mismatch" test/protocol/consume_response_pipeline_test.exs` returns at least 3 matches.
      - `rg -n ":duplicate_xml_id|:ambiguous_signed_node" test/security/signed_node_binding_test.exs` returns at least 2 matches.
      - `rg -n "consume_response\\(|\\{:ok, map\\(\\)\\}|\\{:error, %Relyra\\.Error\\{type: " test/relyra_test.exs` returns at least 3 matches.
      - `mix test test/protocol/consume_response_pipeline_test.exs --warnings-as-errors` exits 0.
      - `mix test test/security/signed_node_binding_test.exs --warnings-as-errors` exits 0.
    </acceptance_criteria>
  </task>

  <task id="02-05-T03">
    <read_first>
      - .planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md
      - .planning/REQUIREMENTS.md
      - test/protocol/consume_response_pipeline_test.exs
      - test/security/signed_node_binding_test.exs
    </read_first>
    <action>
      Re-run the focused Phase 02 command set and update `.planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md` with fresh evidence after gap closures. Preserve existing structure but replace stale `gaps_found` status and missing verdict rows only after all commands pass. The updated verification section must include explicit results for reopened requirements (`SEC-02`, `SEC-03`, `SEC-04`, `SEC-05`, `PROT-02`, `PROT-03`, `PROT-05`) plus command output summary lines. Set frontmatter lists to a closed state (`requirements_verified` includes all reopened IDs, `requirements_missing: []`, `requirements_partial: []`) and refresh `updated` timestamp.
    </action>
    <acceptance_criteria>
      - `rg -n "status: (verified|passed)|SEC-02|SEC-03|SEC-04|SEC-05|PROT-02|PROT-03|PROT-05" .planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md` returns at least 8 matches.
      - `rg -n "mix test test/protocol/consume_response_pipeline_test\\.exs|mix test test/security/signed_node_binding_test\\.exs" .planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md` returns at least 2 matches.
      - `rg -n "^status: verified$|^status: passed$" .planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md` returns 1 match in frontmatter.
      - `rg -n "SEC-02 \\| VERIFIED|SEC-03 \\| VERIFIED|SEC-04 \\| VERIFIED|SEC-05 \\| VERIFIED|PROT-02 \\| VERIFIED|PROT-03 \\| VERIFIED|PROT-05 \\| VERIFIED" .planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md` returns at least 7 matches.
      - `rg -n "^requirements_verified:|^requirements_missing: \\[\\]|^requirements_partial: \\[\\]" .planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md` returns 3 matches.
      - `rg -n "^  - SEC-02$|^  - SEC-03$|^  - SEC-04$|^  - SEC-05$|^  - PROT-02$|^  - PROT-03$|^  - PROT-05$" .planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md` returns at least 7 matches in `requirements_verified`.
      - `rg -n "^updated: " .planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md` returns 1 match with a refreshed timestamp.
      - `mix test test/protocol/authn_request_test.exs test/protocol/relay_state_test.exs test/security/signature_policy_test.exs test/security/signed_node_binding_test.exs test/protocol/consume_response_pipeline_test.exs test/relyra_test.exs --warnings-as-errors` exits 0.
    </acceptance_criteria>
  </task>

  <task id="02-05-T04">
    <read_first>
      - .planning/phases/02-protocol-and-signature-core/02-VERIFICATION.md
      - .planning/ROADMAP.md
      - .planning/REQUIREMENTS.md
      - .planning/STATE.md
    </read_first>
    <action>
      Synchronize planning/governance docs after verification closure. Update `.planning/ROADMAP.md` Phase 2 section to list all five plans (`02-01`..`02-05`) and set status back to complete only after verification evidence is green. Update `.planning/REQUIREMENTS.md` status indicators and traceability rows for `SEC-02`, `SEC-03`, `SEC-04`, `SEC-05`, `PROT-02`, `PROT-03`, and `PROT-05` so they no longer contradict verification state. Update `.planning/STATE.md` current position and progress metrics to reflect reopened-and-closed remediation cycle with final plan count of 5 for Phase 2.
    </action>
    <acceptance_criteria>
      - `rg -n "Phase 2: Protocol and Signature Core|\\*\\*Plans\\*\\*: 5 plans|02-04|02-05" .planning/ROADMAP.md` returns at least 4 matches.
      - `rg -n "SEC-02 \\| Phase 2 \\| Completed \\(02-04/02-05 gap closure\\)|SEC-03 \\| Phase 2 \\| Completed \\(02-04/02-05 gap closure\\)|SEC-04 \\| Phase 2 \\| Completed \\(02-04/02-05 gap closure\\)|SEC-05 \\| Phase 2 \\| Completed \\(02-04/02-05 gap closure\\)|PROT-02 \\| Phase 2 \\| Completed \\(02-04/02-05 gap closure\\)|PROT-03 \\| Phase 2 \\| Completed \\(02-04/02-05 gap closure\\)|PROT-05 \\| Phase 2 \\| Completed \\(02-04/02-05 gap closure\\)" .planning/REQUIREMENTS.md` returns at least 7 matches.
      - `rg -n "PROT-04 \\| Phase 3 \\| Pending" .planning/REQUIREMENTS.md` returns 1 match (Phase 3 ownership preserved).
      - `rg -n "Phase: 02 \\(protocol-and-signature-core\\)|Plan: 5 of 5|Status: Ready for phase transition to 03-01" .planning/STATE.md` returns at least 3 matches.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  - `mix format --check-formatted`
  - `mix compile --warnings-as-errors`
  - `mix test test/protocol/consume_response_pipeline_test.exs --warnings-as-errors`
  - `mix test test/security/signed_node_binding_test.exs --warnings-as-errors`
  - `mix test test/relyra_test.exs --warnings-as-errors`
  - `mix test test/protocol/authn_request_test.exs test/protocol/relay_state_test.exs test/security/signature_policy_test.exs test/security/signed_node_binding_test.exs test/protocol/consume_response_pipeline_test.exs test/relyra_test.exs --warnings-as-errors`
</verification>

<must_haves>
  - Regression corpus includes explicit unsigned-payload and request-correlation mismatch classes tied to reopened requirement IDs.
  - Consume-path tests prove fail-closed behavior for unsigned/structurally incomplete payloads and correlation mismatches.
  - Signature binding tests assert duplicate-ID and ambiguous-node rejection in parser-driven flows.
  - Verification artifact is refreshed with new evidence rather than relying on pre-gap status.
  - Phase 02 reopened requirements have test-backed closure evidence before re-marking the phase complete.
</must_haves>
