---
phase: 28-real-c14n-parser-foundation
plan: 03
type: execute
wave: 3
depends_on: ["28-01", "28-02"]
files_modified:
  - lib/relyra/security/xml/pure_beam.ex
autonomous: true
requirements: [SIGV-03]
user_setup: []

must_haves:
  truths:
    - "parse_safely/2 runs the DOCTYPE/ENTITY/size pre-parse byte guards on the raw binary BEFORE invoking Saxy, then builds the parsed_doc from the SaxyTree (no regex)"
    - "All downstream protocol fields (Issuer/Status/Destination/InResponseTo/NameID/NameID Format/Conditions times/Audience/Recipient/SessionIndex/Attributes/assertion_times) are re-derived from the saxy tree in one pass; regex extractors retired"
    - "The flat parsed_doc key contract is preserved additively (existing keys unchanged; tree attached as a new :parse_tree key)"
    - "key_info_trust, duplicate_ids, and single-signed-node selection are derived from the tree; same keys, same rejections"
    - "select_signed_node/2 returns a handle that references the EXACT tree node canonicalize/2 will serialize (no node/canonicalization differential)"
    - "canonicalize/2 delegates to the C14N engine on the bound node, and still fails closed (:canonicalization_failed) for incomplete/differential inputs"
    - "@callback arity is unchanged; the v1.0 corpus, downstream readers, and seam_contract test stay green"
  artifacts:
    - path: "lib/relyra/security/xml/pure_beam.ex"
      provides: "saxy-tree-backed seam adapter (parse/select/canonicalize) with regex retired and guards ported"
      contains: ":parse_tree"
  key_links:
    - from: "lib/relyra/security/xml/pure_beam.ex"
      to: "Relyra.Security.XML.SaxyTree"
      via: "Saxy.parse_string(xml, SaxyTree, ...) in the parse_safely true-> arm"
      pattern: "Saxy.parse_string"
    - from: "lib/relyra/security/xml/pure_beam.ex"
      to: "Relyra.Security.XML.C14N"
      via: "canonicalize/2 delegates to the C14N engine on the bound node"
      pattern: "C14N"
    - from: "select_signed_node/2 handle"
      to: "canonicalize/2 serialized node"
      via: "the same :node tree reference (D-10)"
      pattern: ":node"
---

<objective>
Re-wire the `Relyra.Security.XML.PureBeam` seam adapter onto the saxy parse tree: keep the pre-parse
byte guards verbatim and route the `true ->` arm of `parse_safely/2` to
`Saxy.parse_string(xml, Relyra.Security.XML.SaxyTree, ...)`; re-derive ALL downstream protocol fields
from the resulting tree in one pass and RETIRE every regex extractor (D-04, one trust path); preserve
the flat `parsed_doc` key contract additively (attach the tree as `:parse_tree`, D-08); derive
`key_info_trust`/`duplicate_ids`/single-node selection from the tree (D-09 guard portability); bind
the selected handle to the EXACT tree node (D-10) and delegate `canonicalize/2` to the Plan-02 C14N
engine on that node — while keeping the fail-closed `:canonicalization_failed` path for incomplete
inputs (Pitfall 9) and the unchanged `@callback` arity (D-07).

Purpose: Collapse the saxy-for-C14N + regex-for-fields differential class this milestone exists to
close (SIGV-03, ROADMAP successes #1/#3/#4). One hardened parser path; v1.0 corpus + downstream
readers + seam contract stay green.
Output: refactored `lib/relyra/security/xml/pure_beam.ex` (the only file this plan modifies).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-real-c14n-parser-foundation/28-CONTEXT.md
@.planning/phases/28-real-c14n-parser-foundation/28-RESEARCH.md
@.planning/phases/28-real-c14n-parser-foundation/28-PATTERNS.md
@.planning/phases/28-real-c14n-parser-foundation/28-01-SUMMARY.md
@.planning/phases/28-real-c14n-parser-foundation/28-02-SUMMARY.md

<interfaces>
<!-- KEEP these verbatim from the current pure_beam.ex (lines cited). Only the noted arms change. -->

parse_safely/2 pre-parse byte guards (lines 14-35) — KEEP; only the `true ->` arm changes:
  byte_size(xml) > max_bytes -> :payload_too_large
  String.contains?(xml, "<!DOCTYPE") -> :doctype_forbidden
  String.contains?(xml, "<!ENTITY") -> :entity_expansion_forbidden
  true -> parse_xml(xml)            # <-- swap regex parse_xml/1 for the Saxy-tree path
  def parse_safely(_xml, _opts), do: malformed_xml_error()   # KEEP non-binary fallback

select_signed_node/2 guard cascade (lines 40-70) — KEEP; only the SOURCE of the keys changes:
  key_info_trust == true -> :untrusted_certificate (document_keyinfo_forbidden)
  duplicate_ids != []    -> :duplicate_xml_id
  true -> select_candidate(parsed_doc)

select_candidate/1 (lines 323-350) — KEEP single-node logic; ADD :node additively to the [candidate] map (D-10):
  [] -> :missing_signature ; [candidate] -> {:ok, handle} ; _ -> :ambiguous_signed_node

canonicalize/2 (lines 72-102) — REPLACE the passthrough body; KEEP the fail-closed fallback head:
  # complete bound node -> delegate to Relyra.Security.XML.C14N (Plan 02), return %{canonical_xml:, xml_id:, xpath:}
  # any non-bindable handle -> {:error, Error.new(:canonicalization_failed, ..., %{reason: :invalid_signed_node_handle})}

Backward-compat parsed_doc keys to REPRODUCE additively (D-08, from RESEARCH.md § Backward-Compat Surface):
  Signature.do_verify reads: :duplicate_ids, :key_info_trust, :signature_method, :digest_method,
    :signed_candidates (each: :xml_id, :xpath, :signed_xml, :signature_method, :digest_method)
  ValidationPipeline reads: :in_response_to, :issuer, :status, :destination, :audiences, :recipient,
    :assertion_times{:not_before,:not_on_or_after,:subject_confirmation_not_on_or_after}, :name_id,
    :name_id_format, :session_index, :attributes, :connection_id, :signed_candidates
  SignedNode struct: [:xml_id, :xpath, :signed_xml, :signature_method, :digest_method]
  NEW additive key only: :parse_tree (do NOT remove/rename any existing key)

Guard portability (RESEARCH.md § Guard Portability Map — derive from tree, same key/rejection):
  key_info_trust := a KeyInfo element is present in the tree
  duplicate_ids  := collect all ID/id attrs across tree, return the duplicated values
  malformed_xml  := {:error, %Saxy.ParseError{}} -> Error.new(:malformed_xml, ..., %{reason: Saxy.ParseError.message(err)})
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route parse_safely onto the saxy tree, re-derive all fields, attach :parse_tree, port guards</name>
  <files>lib/relyra/security/xml/pure_beam.ex, test/relyra/security/xml/pure_beam_test.exs</files>
  <read_first>
    - lib/relyra/security/xml/pure_beam.ex (FULL FILE — keep guards lines 14-35; retire regex extractors lines 104-321; keep require_present_fields/present? lines 232-252; keep select_candidate lines 323-350; keep/adapt normalize layer lines 352-357 dropping trim)
    - lib/relyra/security/xml/saxy_tree.ex + .planning/phases/28-real-c14n-parser-foundation/28-01-SUMMARY.md (tree-node shape to walk)
    - lib/relyra/security/signature.ex (the flat parsed_doc keys do_verify/verified_signed_node read — backward-compat)
    - lib/relyra/protocol/validation_pipeline.ex (the protocol field keys it reads — backward-compat)
    - lib/relyra/security/signed_node.ex (struct shape to keep: :xml_id,:xpath,:signed_xml,:signature_method,:digest_method)
    - .planning/phases/28-real-c14n-parser-foundation/28-RESEARCH.md (§ Backward-Compat Surface; § Guard Portability Map; Pattern 1; D-04/D-08/D-09)
    - .planning/phases/28-real-c14n-parser-foundation/28-PATTERNS.md (§ pure_beam.ex MODIFIED — KEEP/RETIRE annotations with exact line numbers)
    - test/security/xml/seam_contract_test.exs (parse_safely returns non-binary parsed_doc; select_signed_node/canonicalize typed-error contract)
  </read_first>
  <behavior>
    - Test: `parse_safely/2` on a well-formed SAML Response returns `{:ok, parsed_doc}` where parsed_doc is a map (not a binary) carrying every legacy key (:issuer, :status, :destination, :in_response_to, :audiences, :recipient, :assertion_times, :name_id, :name_id_format, :session_index, :attributes, :connection_id, :signature_method, :digest_method, :signed_candidates, :duplicate_ids, :key_info_trust) PLUS the new :parse_tree key.
    - Test: each legacy field is re-derived from the tree and matches the value the regex path produced for the seam_contract fixture (e.g. issuer = "https://idp.example.com/metadata").
    - Test: `<!DOCTYPE ...>` and `<!ENTITY ...>` payloads still return :doctype_forbidden / :entity_expansion_forbidden (guards run BEFORE Saxy on the raw binary); an oversize payload returns :payload_too_large.
    - Test: a not-well-formed XML binary returns {:error, %Relyra.Error{type: :malformed_xml}} (mapped from %Saxy.ParseError{}); a non-binary input returns :malformed_xml via the kept fallback.
    - Test: a document containing a KeyInfo element yields key_info_trust == true; duplicate ID attrs yield a non-empty duplicate_ids list — both derived from the tree.
    - Test (regression): no module outside lib/relyra/security/xml/ references Saxy (parser_path_guard stays green via `mix compile --warnings-as-errors`).
  </behavior>
  <action>
    Modify ONLY `lib/relyra/security/xml/pure_beam.ex`. KEEP `parse_safely/2`'s size/DOCTYPE/ENTITY
    cond arms and the non-binary fallback verbatim (lines 14-35) — the pre-parse byte guards run on the
    raw binary BEFORE any Saxy call (XXE-before-verify, D-09). Change ONLY the `true ->` arm to call
    `Saxy.parse_string(xml, Relyra.Security.XML.SaxyTree, <initial state>)`; on `{:error, %Saxy.ParseError{} = err}`
    return `Error.new(:malformed_xml, "Malformed XML payload", %{reason: Saxy.ParseError.message(err)})`
    (reuse the existing atom; this is the only place that may newly reference Saxy — it is under the
    allowed root). On success, walk the tree once and build the flat `parsed_doc`.

    RETIRE every regex extractor (D-04): delete `parse_xml/1`'s regex shape checks and all
    `extract_*`/`first_tag_text`/`all_tag_texts`/`first_attribute`/`attribute_from_fragment` helpers
    (lines 104-321). Re-derive from the tree, in one pass, the SAME flat keys (see <interfaces>
    backward-compat list) — Issuer/Status/Destination/InResponseTo/ConnectionId, Audience/Recipient/
    Conditions times/assertion_times, NameID/Format/SessionIndex/Attributes, plus signature_method/
    digest_method/signed_candidates/duplicate_ids/key_info_trust. KEEP `require_present_fields/4` +
    `present?/1` (lines 232-252) and run the tree-derived field sets through them so the
    :missing_protocol_field / :missing_signature error shapes are byte-identical. Attach the tree as a
    NEW `:parse_tree` key (D-07/D-08); never remove or rename an existing key. Carry forward ONLY the
    `\r\n`->`\n` / `\r`->`\n` replaces from `normalize_signed_xml/1`; DROP `String.trim/1`.

    Derive guards from the tree (D-09 Guard Portability Map): `key_info_trust` from presence of a
    KeyInfo element; `duplicate_ids` from collecting all `ID`/`id` attrs across the tree and returning
    duplicated values. Keep `select_signed_node/2` (lines 40-70) and `select_candidate/1` (lines 323-350)
    logic unchanged. (Node binding `:node` and the canonicalize delegation are Task 2.)
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/relyra/security/xml/pure_beam_test.exs test/security/xml/seam_contract_test.exs test/security/xml/corpus_security_test.exs test/relyra/security/xml/corpus_gate_test.exs --only security_corpus --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `parse_safely/2` returns a non-binary parsed_doc carrying all legacy keys + `:parse_tree`; field values re-derived from the tree match the regex-era values for the seam fixture (see <behavior>).
    - DOCTYPE/ENTITY/size guards still reject (`:doctype_forbidden`/`:entity_expansion_forbidden`/`:payload_too_large`); malformed -> `:malformed_xml`; key_info_trust/duplicate_ids tree-derived.
    - `mix test ... --only security_corpus` (corpus regression L3) and `seam_contract_test` stay green; `mix compile --warnings-as-errors` clean (parser_path_guard: no Saxy reference escaped the seam).
    - No regex `extract_*`/`first_tag_text`/`first_attribute` helper remains (D-04); no new error atom.
  </acceptance_criteria>
  <done>parse_safely runs guards-before-Saxy, builds the parsed_doc from the tree, retires all regex extractors, preserves the flat key contract additively with :parse_tree, and derives guards from the tree — v1.0 corpus + seam contract green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the selected node to the exact tree node (D-10) and delegate canonicalize/2 to the C14N engine</name>
  <files>lib/relyra/security/xml/pure_beam.ex, test/relyra/security/xml/pure_beam_test.exs</files>
  <read_first>
    - lib/relyra/security/xml/pure_beam.ex (select_candidate/1 lines 323-350; canonicalize/2 lines 72-102 — keep the fail-closed fallback head)
    - lib/relyra/security/xml/c14n.ex + .planning/phases/28-real-c14n-parser-foundation/28-02-SUMMARY.md (the serialize entrypoint signature + PrefixList option)
    - .planning/phases/28-real-c14n-parser-foundation/28-RESEARCH.md (Pattern 4 node binding; Pitfall 9 fail-closed calling convention; Open Question 2 handle shape)
    - .planning/phases/28-real-c14n-parser-foundation/28-PATTERNS.md (§ pure_beam.ex canonicalize KEEP fail-closed fallback; § c14n.ex two load-bearing facts)
    - test/security/xml/corpus_security_test.exs (lines 34-60 fail-closed GATE-02 block; lines 123-126 it calls canonicalize(parsed_doc, []) with the WHOLE map — must still fail closed)
  </read_first>
  <behavior>
    - Test: `select_signed_node/2` on a single-signed-node document returns `{:ok, handle}` whose handle carries a `:node` reference to the SAME tree node present in parsed_doc[:parse_tree] (assert identity/equality of the bound node — no re-found/re-parsed substring).
    - Test: `canonicalize/2` on that handle returns `{:ok, %{canonical_xml: bytes, xml_id: ..., xpath: ...}}` where `bytes` is what `Relyra.Security.XML.C14N` serialized over the bound `:node` — i.e. the canonicalized bytes derive from the exact bound node (D-10, no node/canonicalization differential).
    - Test (fail-closed, Pitfall 9): `canonicalize/2` called with the whole parsed_doc map (as GATE-02 does, corpus_security_test.exs line 125) returns `{:error, %Relyra.Error{type: :canonicalization_failed}}` — the real engine must NOT succeed on the existing c14n-00x differential rows.
    - Test (seam contract): `canonicalize(:node, [])` (a bare atom handle) still returns `{:error, :canonicalization_failed}` (seam_contract_test.exs line 31).
  </behavior>
  <action>
    In `select_candidate/1` (lines 323-350), add a `:node` field ADDITIVELY to the returned
    `[candidate]` handle map, referencing the exact SaxyTree node for that signed candidate (the same
    node object stored in `:signed_candidates`/`:parse_tree`) — D-10. Do NOT change the other handle
    keys (`:xml_id`,`:xpath`,`:signed_xml`,`:signature_method`,`:digest_method`) — they remain for
    backward-compat.

    Replace the `canonicalize/2` passthrough body (the first head, lines 75-93): when the handle binds a
    complete tree node, delegate to the Plan-02 `Relyra.Security.XML.C14N` serialize entrypoint
    (signature per 28-02-SUMMARY.md), passing the bound `:node` and any PrefixList read from the node's
    Reference transforms; return `%{canonical_xml: <engine bytes>, xml_id: ..., xpath: ...}` (preserve
    the return shape downstream expects). KEEP the second `canonicalize/2` head (lines 95-102) as the
    fail-closed fallback returning `Error.new(:canonicalization_failed, ..., %{reason: :invalid_signed_node_handle})`
    for any handle lacking a bindable node — this is the GATE-02 contract (Pitfall 9): the existing
    c14n-00x differential rows call `canonicalize(parsed_doc, [])` with the whole map and MUST keep
    failing closed. Reuse `:canonicalization_failed`; do not invent atoms. Do not weaken the
    single-signed-node selection.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/relyra/security/xml/pure_beam_test.exs test/security/xml/seam_contract_test.exs test/security/xml/corpus_security_test.exs --only gate02_c14n --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The `select_signed_node/2` handle carries a `:node` referencing the exact tree node `canonicalize/2` serializes (D-10 node-binding test green).
    - `canonicalize/2` on a complete bound node delegates to `Relyra.Security.XML.C14N` and returns `%{canonical_xml:, xml_id:, xpath:}`.
    - GATE-02 fail-closed rows stay green: `canonicalize(parsed_doc, [])` and `canonicalize(:node, [])` both return `{:error, :canonicalization_failed}` (Pitfall 9; seam_contract line 31).
    - `mix compile --warnings-as-errors` clean; no new error atom.
  </acceptance_criteria>
  <done>The selected handle is bound to the exact tree node and canonicalize/2 delegates to the C14N engine on that node, with fail-closed preserved for incomplete inputs — no node/canonicalization differential (D-10), GATE-02 fail-closed rows green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw SAML XML binary -> parse_safely | Untrusted input; pre-parse byte guards must reject DTD/entity/oversize before any parse. |
| tree -> parsed_doc (downstream readers) | Signature/ValidationPipeline/AutoRefresh trust the flat keys; a contract break breaks the trust path. |
| selected handle -> canonicalize/2 | The node bound for verification (Phase 29) must equal the node canonicalized (anti-XSW). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07 | Information Disclosure / Tampering | XXE (DOCTYPE/ENTITY) | mitigate | Pre-parse byte guards kept verbatim and run BEFORE Saxy on the raw binary (D-09); corpus xxe family stays green (`--only security_corpus`). Mirrors the esaml CVE-2026-28809 lesson. |
| T-28-08 | Denial of Service | oversized payload (billion-laughs) | mitigate | `max_bytes` size guard kept before parse; `:payload_too_large` corpus rows green. |
| T-28-09 | Spoofing / Elevation | signature wrapping (XSW) | mitigate | Single-signed-node selection + duplicate-ID rejection preserved; handle bound to the EXACT canonicalized node (D-10) so no wrapped sibling can be verified-but-not-canonicalized. Node-binding test in Task 2. |
| T-28-10 | Spoofing | document-KeyInfo trust abuse (CVE-2024-45409 family) | mitigate | `key_info_trust` rejection preserved, now tree-derived (D-09); cve_2024_45409 corpus family stays green. |
| T-28-11 | Tampering / Spoofing | parser differential (two paths disagree) | mitigate | One trust path: ALL fields re-derived from the single saxy tree, regex extractors retired (D-04); parser_path_guard green (no Saxy reference escapes the seam). |
| T-28-12 | Tampering | C14N fail-open on incomplete input | mitigate | Fail-closed `:canonicalization_failed` head preserved for non-bindable handles (Pitfall 9); GATE-02 c14n-00x differential rows stay green. |
</threat_model>

<verification>
- `mix test test/relyra/security/xml/pure_beam_test.exs test/security/xml/seam_contract_test.exs test/security/xml/corpus_security_test.exs test/relyra/security/xml/corpus_gate_test.exs --only security_corpus --warnings-as-errors` green (L3 corpus regression).
- `mix test test/security/xml/corpus_security_test.exs --only gate02_c14n --warnings-as-errors` green (L2 fail-closed).
- `mix test test/relyra/security/signature_test.exs test/relyra/metadata/auto_refresh_test.exs --warnings-as-errors` green (L4 backward-compat downstream readers).
- `mix compile --warnings-as-errors` clean (parser_path_guard; no Saxy reference outside the seam).
</verification>

<success_criteria>
- ROADMAP success #1: the seam parses SAML XML into a structured tree (not regex), single trust path preserved.
- ROADMAP success #3: existing hardened guards (DOCTYPE/ENTITY/size/KeyInfo/dup-ID/single-node) hold against the v1.0 corpus on the new parser — no regression, no second parser path.
- ROADMAP success #4: the verified signed node is bound to the exact element consumed downstream (D-10).
- D-08 backward-compat: flat parsed_doc keys preserved additively; downstream + seam_contract tests green.
</success_criteria>

<output>
Create `.planning/phases/28-real-c14n-parser-foundation/28-03-SUMMARY.md` when done. Note the final
handle shape (incl. `:node`) and confirm which legacy parsed_doc keys are tree-derived for Plan 04's
golden test wiring.
</output>
