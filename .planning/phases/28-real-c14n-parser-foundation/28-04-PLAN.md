---
phase: 28-real-c14n-parser-foundation
plan: 04
type: execute
wave: 4
depends_on: ["28-02", "28-03"]
files_modified:
  - test/fixtures/security/xml/parser_differential_and_c14n/assertion_inherited_ns.input.xml
  - test/fixtures/security/xml/parser_differential_and_c14n/assertion_inherited_ns.c14n
  - test/fixtures/security/xml/parser_differential_and_c14n/PROVENANCE.md
  - test/security/xml/corpus_security_test.exs
autonomous: false
requirements: [SIGV-03]
user_setup: []

must_haves:
  truths:
    - "At least one committed golden fixture exists: input SAML XML (assertion with an ancestor-declared namespace) + the exact canonical bytes with NO trailing newline + a PROVENANCE note"
    - "Golden bytes were minted out-of-band by pinned lxml cross-checked against xmlsec1; PROVENANCE records tool + libxml2 versions, the exact command, and any PrefixList"
    - "GATE-02 has a NEW positive byte-equality assertion: canonicalize/2 output == committed golden bytes, byte-for-byte"
    - "The existing GATE-02 fail-closed assertions (c14n-00x rows -> :canonicalization_failed) stay green"
    - "A node-binding assertion proves the handle's node is the exact node canonicalize/2 serialized (D-10)"
    - "mix ci.security stays pure-Elixir (no native toolchain step added)"
  artifacts:
    - path: "test/fixtures/security/xml/parser_differential_and_c14n/assertion_inherited_ns.input.xml"
      provides: "golden input: SAML assertion with ancestor-declared namespace"
    - path: "test/fixtures/security/xml/parser_differential_and_c14n/assertion_inherited_ns.c14n"
      provides: "golden canonical bytes (raw UTF-8, no BOM, no trailing newline)"
    - path: "test/fixtures/security/xml/parser_differential_and_c14n/PROVENANCE.md"
      provides: "oracle provenance: lxml + libxml2 versions, exact mint command, xmlsec1 cross-check, PrefixList"
    - path: "test/security/xml/corpus_security_test.exs"
      provides: "GATE-02 positive byte-equality + node-binding assertions; existing fail-closed rows preserved"
      contains: "assertion_inherited_ns"
  key_links:
    - from: "test/security/xml/corpus_security_test.exs"
      to: "test/fixtures/security/xml/parser_differential_and_c14n/assertion_inherited_ns.c14n"
      via: "File.read! of the committed golden bytes asserted byte-equal to canonicalize/2 output"
      pattern: "assertion_inherited_ns\\.c14n"
    - from: "canonicalize/2 output"
      to: "golden bytes"
      via: "byte-exact equality assertion (D-11)"
      pattern: "assert .*== golden"
---

<objective>
Mint and commit the golden-byte oracle (D-12) and add the positive byte-equality differential gate
(D-11) that PROVES the hand-rolled exclusive-C14N engine (Plan 02), as wired through the seam (Plan
03), produces byte-exact canonical output matching an INDEPENDENT reference — while keeping the
existing GATE-02 fail-closed assertions green. This is the authoritative correctness proof for the
only net-new algorithm in the phase; a self-asserted engine proves nothing, so the oracle is
non-negotiable.

Purpose: ROADMAP success #2 + SIGV-03 — `canonicalize/2` output matches an independent reference
(xmlsec/pyXMLSec) byte-for-byte; the GATE-02 differential gate passes. A byte-divergence here would
silently defeat Phase 29's digest comparison and re-open the auth-bypass class, so this gate is the
load-bearing mitigation for the C14N-divergence threat. Plus a node-binding assertion for success #4.
Output: the committed golden fixture set + PROVENANCE + the extended GATE-02 assertions.

The golden bytes are minted OUT-OF-BAND (lxml + xmlsec1, pinned) and COMMITTED; `mix ci.security`
reads the committed bytes only and stays pure-Elixir (no native toolchain). The mint step is a
human/CI-external action — Task 1 is a checkpoint capturing that the bytes + provenance are committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-real-c14n-parser-foundation/28-CONTEXT.md
@.planning/phases/28-real-c14n-parser-foundation/28-RESEARCH.md
@.planning/phases/28-real-c14n-parser-foundation/28-PATTERNS.md
@.planning/phases/28-real-c14n-parser-foundation/28-02-SUMMARY.md
@.planning/phases/28-real-c14n-parser-foundation/28-03-SUMMARY.md

<interfaces>
<!-- GATE-02 test conventions to follow (corpus_security_test.exs). -->

Existing GATE-02 fail-closed block (lines 34-60) — PRESERVE; keeps c14n-00x rows -> :canonicalization_failed.
manifest/0 (lines 92-96): File.read!("priv/security_corpus.json") |> :json.decode().
evaluate_fixture/2 differential arm (lines 123-126): calls PureBeam.canonicalize(parsed_doc, []) (whole map -> fail-closed).
Aliases at top (lines 4-5): alias Relyra.Error ; alias Relyra.Security.XML.PureBeam.

New positive byte-equality test shape (D-11, from PATTERNS.md §corpus_security_test.exs):
  input  = File.read!(".../assertion_inherited_ns.input.xml")
  golden = File.read!(".../assertion_inherited_ns.c14n")
  {:ok, parsed_doc} = PureBeam.parse_safely(input, [])
  {:ok, signed_node} = PureBeam.select_signed_node(parsed_doc, [])
  {:ok, %{canonical_xml: out}} = PureBeam.canonicalize(signed_node, [])
  assert out == golden               # byte-exact (Pitfalls 1-8)
  refute String.ends_with?(out, "\n")  # no trailing newline (Pitfall 4)

Out-of-band mint (D-12, RESEARCH.md § Code Examples) — run on a machine/container with lxml + xmlsec1:
  lxml: etree.tostring(node, method="c14n", exclusive=True, inclusive_ns_prefixes=[...]) -> raw bytes, no trailing newline
  xmlsec1 cross-check: xmlsec1 --c14n-exc <input> ; diff must match the lxml bytes
  PROVENANCE records: lxml.__version__, etree.LIBXML_VERSION, xmlsec1 --version, exact commands, PrefixList.
  Known caveat: older lxml ignored inclusive_ns_prefixes -> pin recent lxml AND cross-check xmlsec1.

Provenance JSON-row keys (if a manifest row is added): id, class="parser_differential_and_c14n",
  family, requirement_ids (non-empty), expected_error_type, provenance{}, source_ref (the
  "every manifest row carries immutable provenance" test at corpus_security_test.exs lines 62-78 asserts these).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Mint + commit golden bytes out-of-band (lxml cross-checked with xmlsec1) — D-12</name>
  <read_first>
    - .planning/phases/28-real-c14n-parser-foundation/28-RESEARCH.md (§ Code Examples lxml/xmlsec1 commands; Pitfalls 4,6,7; § Manual-Only Verifications in 28-VALIDATION.md)
    - .planning/phases/28-real-c14n-parser-foundation/28-CONTEXT.md (D-12; specifics: inclusive_ns_prefixes; commit input + bytes + PROVENANCE)
    - .planning/phases/28-real-c14n-parser-foundation/28-PATTERNS.md (§ fixture set: file names + provenance shape)
  </read_first>
  <what-built>
    The exclusive-C14N engine (Plan 02) and the seam wiring (Plan 03) are complete and self-checked,
    but their byte-exactness is NOT yet proven against an independent oracle. This step mints the
    golden bytes OUT-OF-BAND (native toolchain MUST NOT enter CI, D-12 / ADR-0001 pure-BEAM CI) and
    commits them so Task 2's assertion can read committed bytes only.
  </what-built>
  <how-to-verify>
    On a machine/container that has pinned `lxml` AND `xmlsec1` (not in this repo's CI):
    1. Author `assertion_inherited_ns.input.xml` — a SAML assertion whose namespace is declared on an
       ancestor `<Response>` and inherited by the signed `<Assertion>` (success-criterion-#2 minimum;
       exercises Pitfall 1 over-render + inherited-ns). Optionally author per-pitfall variants
       (default-ns mix, tab/newline-in-attr, CRLF line endings, non-ASCII text, multi-namespaced attrs,
       a PrefixList case) — Claude's Discretion per CONTEXT.md.
    2. Mint canonical bytes with pinned lxml: `etree.tostring(node, method="c14n", exclusive=True, inclusive_ns_prefixes=<PrefixList or None>)`; write RAW bytes with NO trailing newline, no BOM, UTF-8.
    3. Cross-check with `xmlsec1 --c14n-exc`; `diff` must show the oracles agree byte-for-byte.
    4. Commit `assertion_inherited_ns.input.xml`, `assertion_inherited_ns.c14n` (exact bytes), and
       `PROVENANCE.md` recording lxml version, `etree.LIBXML_VERSION`, `xmlsec1 --version` + its
       libxml2 version, the exact commands, and any PrefixList used.
    5. Confirm the committed `.c14n` file has NO trailing newline (editors add one — store bytes exactly):
       e.g. `tail -c1 assertion_inherited_ns.c14n | xxd` shows `>` (0x3e), not `0a`.
  </how-to-verify>
  <acceptance_criteria>
    - The three fixture files exist and are committed: `assertion_inherited_ns.input.xml`, `assertion_inherited_ns.c14n`, `PROVENANCE.md`.
    - lxml and xmlsec1 agree byte-for-byte on the canonical bytes (cross-check recorded in PROVENANCE).
    - `tail -c1 .../assertion_inherited_ns.c14n` is `>` (0x3e), proving no trailing newline (Pitfall 4); file is raw UTF-8 with no BOM (Pitfall 6).
    - PROVENANCE.md records tool + libxml2 versions, exact command(s), and any PrefixList (D-12).
    - No native toolchain step was added to `mix ci.security`.
  </acceptance_criteria>
  <files>test/fixtures/security/xml/parser_differential_and_c14n/assertion_inherited_ns.input.xml, test/fixtures/security/xml/parser_differential_and_c14n/assertion_inherited_ns.c14n, test/fixtures/security/xml/parser_differential_and_c14n/PROVENANCE.md</files>
  <action>Out-of-band human/CI-external step ONLY — mint the golden bytes with pinned lxml cross-checked against xmlsec1 and COMMIT the three fixture files (D-12). The native toolchain must NOT enter `mix ci.security`. See <what-built>/<how-to-verify>/<acceptance_criteria> for the exact commands and checks.</action>
  <verify>Manual per <how-to-verify>: lxml and xmlsec1 agree byte-for-byte; `tail -c1 .../assertion_inherited_ns.c14n` is `>` (0x3e, no trailing newline); PROVENANCE records versions + commands + PrefixList. Task 2 carries the automated byte-equality assertion.</verify>
  <done>The three golden fixture files are committed; oracles agree; no-trailing-newline + provenance confirmed; Task 2 may assert byte-equality.</done>
  <resume-signal>Type "approved" once the golden fixture + PROVENANCE are committed and the no-trailing-newline / cross-check checks pass, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add the GATE-02 positive byte-equality assertion + node-binding assertion (keep fail-closed rows green)</name>
  <files>test/security/xml/corpus_security_test.exs</files>
  <read_first>
    - test/security/xml/corpus_security_test.exs (FULL FILE — aliases lines 4-5; fail-closed GATE-02 block lines 34-60; manifest/0 lines 92-96; evaluate_fixture lines 98-131; provenance test lines 62-78)
    - test/fixtures/security/xml/parser_differential_and_c14n/assertion_inherited_ns.input.xml + .c14n (the committed goldens from Task 1)
    - .planning/phases/28-real-c14n-parser-foundation/28-03-SUMMARY.md (the final handle shape incl. :node; the parse->select->canonicalize calling convention)
    - .planning/phases/28-real-c14n-parser-foundation/28-PATTERNS.md (§ corpus_security_test.exs: NEW positive test + node-binding shape)
    - .planning/phases/28-real-c14n-parser-foundation/28-RESEARCH.md (Pitfalls 4 + 9; D-10/D-11)
  </read_first>
  <action>
    Modify ONLY `test/security/xml/corpus_security_test.exs`. ADD a NEW `@tag :gate02_c14n` /
    `@tag :security_corpus` test that reads the committed golden via `File.read!`
    (`assertion_inherited_ns.input.xml` + `assertion_inherited_ns.c14n`), runs
    `parse_safely/2 -> select_signed_node/2 -> canonicalize/2`, and asserts `out == golden` (byte-exact,
    D-11, Pitfalls 1-8) plus `refute String.ends_with?(out, "\n")` (Pitfall 4). Use the existing aliases
    (`PureBeam`, `Error`); do NOT add a native-toolchain step (bytes are committed, D-12).

    ADD a NODE-BINDING assertion (success #4 / D-10): from the same `select_signed_node/2` handle,
    assert the handle's bound `:node` is the SAME tree node that `canonicalize/2` serialized — i.e. the
    canonicalized bytes derive from the bound node, with no string-vs-node duality (assert identity/
    equality of the bound node against the node in `parsed_doc[:parse_tree]`).

    PRESERVE the existing GATE-02 fail-closed block (lines 34-60) and the differential `evaluate_fixture`
    arm (lines 123-126) verbatim — the c14n-00x rows must still return `:canonicalization_failed`
    (Pitfall 9). If you add a manifest row for the new positive golden, it must carry the immutable-
    provenance keys (id/family/requirement_ids/provenance/source_ref) the lines-62-78 test asserts;
    otherwise keep the new golden as a standalone File.read! test (preferred — avoids changing the
    fail-closed manifest loop). Do NOT modify any other test or any production module.
  </action>
  <verify>
    <automated>mix test test/security/xml/corpus_security_test.exs --only gate02_c14n --warnings-as-errors && mix ci.security</automated>
  </verify>
  <acceptance_criteria>
    - The NEW positive test asserts `canonicalize/2` output == committed golden bytes byte-for-byte AND no trailing newline (D-11, Pitfall 4) — green.
    - The node-binding assertion proves handle `:node` ≡ the canonicalized node (D-10, success #4) — green.
    - The existing GATE-02 fail-closed c14n-00x rows still return `:canonicalization_failed` (Pitfall 9) — green.
    - `mix ci.security` is green end-to-end and contains NO native toolchain step (still pure-Elixir, D-12).
    - If a manifest row was added, the lines-62-78 immutable-provenance test passes for it.
  </acceptance_criteria>
  <done>GATE-02 now has a positive byte-equality proof against the committed independent-oracle golden plus a node-binding assertion, with the existing fail-closed assertions preserved; mix ci.security green and pure-Elixir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| out-of-band oracle (lxml/xmlsec1) -> committed golden bytes | The independent reference the engine is proven against; provenance must be recorded to keep the oracle auditable. |
| committed golden bytes -> CI assertion | CI reads committed bytes only; native toolchain must never enter `mix ci.security`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-13 | Tampering | C14N byte-divergence (silent) — the load-bearing phase threat | mitigate | Positive byte-equality differential gate (D-11) against an INDEPENDENT lxml+xmlsec1 oracle (D-12), covering the 8 pitfalls via the golden fixture(s). This is the real mitigation task (not a note) for the C14N-divergence Tampering threat in RESEARCH.md § Security Domain. |
| T-28-14 | Tampering | self-attested correctness (oracle integrity) | mitigate | Dual-tool cross-check (lxml AND xmlsec1 must agree); PROVENANCE records versions + exact commands so the golden is reproducible/auditable (counters the lxml inclusive_ns_prefixes caveat). |
| T-28-15 | Spoofing / Elevation | node/canonicalization differential (XSW) | mitigate | Node-binding assertion proves the verified handle's node ≡ the canonicalized node (D-10, success #4). |
| T-28-16 | Tampering | native toolchain entering CI (supply-chain / hermeticity) | accept-by-design | Golden bytes are committed and read by CI; lxml/xmlsec1 stay out-of-band (D-12 / ADR-0001 pure-BEAM CI rationale). Verified: `mix ci.security` adds no native step. |
| T-28-12 | Tampering | C14N fail-open on incomplete input | mitigate | Existing GATE-02 fail-closed c14n-00x rows preserved (Pitfall 9) alongside the new positive path. |
</threat_model>

<verification>
- `mix test test/security/xml/corpus_security_test.exs --only gate02_c14n --warnings-as-errors` green (positive byte-equality + fail-closed both pass).
- `mix ci.security` green end-to-end and pure-Elixir (no native toolchain step).
- `tail -c1 .../assertion_inherited_ns.c14n` == `>` (0x3e) — committed golden has no trailing newline.
- PROVENANCE.md present with tool/libxml2 versions, exact commands, PrefixList.
</verification>

<success_criteria>
- ROADMAP success #2: `canonicalize/2` output matches an independent reference byte-for-byte; the GATE-02 differential gate passes (D-11/D-12).
- ROADMAP success #4: verified node bound to the exact canonicalized element (node-binding assertion).
- Existing GATE-02 fail-closed assertions preserved (Pitfall 9); v1.0 corpus + ci.security green.
- CI remains pure-Elixir; goldens minted out-of-band and committed with provenance.
</success_criteria>

<output>
Create `.planning/phases/28-real-c14n-parser-foundation/28-04-SUMMARY.md` when done. Record which
pitfalls each committed golden fixture exercises and the recorded oracle versions, so Phase 29 can rely
on the proven canonical-bytes precondition.
</output>
