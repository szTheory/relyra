---
phase: 57-demo-fakeidp-browser-login-proof
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [SEED-003]
files_modified:
  - demo/ledger_loop/priv/fake_idp/idp_key.pem
  - demo/ledger_loop/priv/fake_idp/idp_cert.pem
  - demo/ledger_loop/lib/ledger_loop/fake_idp/keypair.ex
  - demo/ledger_loop/lib/ledger_loop/demo/fixtures.ex
  - demo/ledger_loop/lib/ledger_loop/application.ex
  - demo/ledger_loop/test/ledger_loop/fake_idp/keypair_test.exs

must_haves:
  truths:
    - "A demo RSA-2048 keypair + self-signed cert exist on disk under demo/ledger_loop/priv/fake_idp/ and are committed"
    - "The enabled connection fixture (…J0) trusts the demo cert: its :signing IdP cert :pem is the real cert, not MOCK_PEM_NOT_REAL"
    - "The enabled connection's idp_sso_url points at the local FakeIdP, while idp_entity_id stays in sync with the seeded SAMLIdentity issuer"
    - "Relyra's LoginTrace telemetry handler is attached in the demo Application so domain: :login AuditEvents are written"
  artifacts:
    - path: "demo/ledger_loop/priv/fake_idp/idp_key.pem"
      provides: "Demo IdP RSA-2048 private key (demo-only secret, committed)"
      contains: "RSA PRIVATE KEY"
    - path: "demo/ledger_loop/priv/fake_idp/idp_cert.pem"
      provides: "Demo IdP self-signed cert matching the private key"
      contains: "CERTIFICATE"
    - path: "demo/ledger_loop/lib/ledger_loop/fake_idp/keypair.ex"
      provides: "Runtime loader for the committed PEMs (private_key/0, cert_pem/0)"
      min_lines: 15
    - path: "demo/ledger_loop/lib/ledger_loop/demo/fixtures.ex"
      provides: "relyra_certificates/0 :pem set to the demo cert; …J0 idp_sso_url local"
      contains: "fake_idp"
  key_links:
    - from: "demo/ledger_loop/lib/ledger_loop/demo/fixtures.ex"
      to: "demo/ledger_loop/priv/fake_idp/idp_cert.pem"
      via: "the :pem string in relyra_certificates/0 equals the committed cert PEM"
      pattern: "BEGIN CERTIFICATE"
    - from: "demo/ledger_loop/lib/ledger_loop/application.ex"
      to: "Relyra.Telemetry.Handlers.LoginTrace"
      via: "attach(repo: LedgerLoop.Repo) after the Repo starts"
      pattern: "LoginTrace.attach"
---

<objective>
Establish the Wave-0 prerequisites for the FakeIdP browser-login proof: a committed
demo IdP keypair + self-signed cert, the fixture cert-trust alignment that makes
Relyra's strict verifier trust that cert (the CRUX), the local idp_sso_url wiring for
SP-initiated flow, and the LoginTrace handler attach that makes the tampered variant
surface in the trace UI.

Purpose: Without these, the success round-trip cannot verify (no trusted cert) and the
tampered variant writes no trace row. Every downstream plan depends on this spine.

Output: Demo keypair PEMs under priv/fake_idp/, a Keypair loader module, an updated
Fixtures (cert :pem + idp_sso_url), LoginTrace attached in Application, and a passing
keypair loader test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-demo-fakeidp-browser-login-proof/57-RESEARCH.md
@.planning/phases/57-demo-fakeidp-browser-login-proof/57-CONTEXT.md

<interfaces>
<!-- Relyra public APIs and demo seams this plan touches. Use directly; no exploration. -->

Cert + key generation (mirror relyra's own signer — do NOT call relyra TestSupport at runtime):
```elixir
# From lib/relyra/test_support/xmldsig_signer.ex:205-211 (technique to re-home, NOT to import)
priv = :public_key.generate_key({:rsa, 2048, 65_537})
key_pem = :public_key.pem_encode([:public_key.pem_entry_encode(:RSAPrivateKey, priv)])
%{cert: der} = :public_key.pkix_test_root_cert(~c"CN=ledgerloop-fake-idp", key: priv)
cert_pem = :public_key.pem_encode([{:Certificate, der, :not_encrypted}])
```

LoginTrace handler (public, prod-compiled):
```elixir
# lib/relyra/telemetry/handlers/login_trace.ex:61-64
@spec attach(keyword()) :: :ok | {:error, :already_exists}
def attach(opts) when is_list(opts)   # requires opts[:repo]; listens on [:relyra,:saml,:response,:consume,*]
```

Fixture rows that must agree (from fixtures.ex):
- relyra_connections/0 …J0: idp_entity_id="https://idp.northstar.example.com", idp_sso_url currently "https://idp.northstar.example.com/sso", acs_url="https://ledgerloop.example.com/sso/acs", sp_entity_id="https://ledgerloop.example.com/sp"
- relyra_certificates/0 @enabled_cert_id: connection_record_id=@enabled_conn_id, role: :signing, use: :signing, party: :idp, pem: "MOCK_PEM_NOT_REAL"  ← REPLACE
- saml_identities/0: subjects sarah@northstar.example.com / chen@northstar.example.com, issuer "https://idp.northstar.example.com"  ← issuer MUST equal …J0 idp_entity_id

Demo.Reset re-seeds certs directly from Fixtures.relyra_certificates/0 (reset.ex:42 `Repo.insert_all(Certificate, Fixtures.relyra_certificates())`) — updating the fixture :pem propagates on reset (Assumption A1 CONFIRMED).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit the demo IdP keypair + self-signed cert, plus the Keypair loader</name>
  <read_first>
    - demo/ledger_loop/lib/ledger_loop/fake_idp/keypair.ex (to create)
    - lib/relyra/test_support/xmldsig_signer.ex:205-211 (cert via pkix_test_root_cert — the authoritative technique to re-home)
    - test/support/mint_signing_key.exs (key generation analog)
    - demo/ledger_loop/lib/ledger_loop/application.ex (for app_dir/priv path convention)
  </read_first>
  <action>
    Generate ONE RSA-2048 keypair and its self-signed cert deterministically (run a one-off
    `mix run`/IEx snippet or a throwaway script — do NOT add a permanent mix task). Use
    `:public_key.generate_key({:rsa, 2048, 65_537})`, encode the private key with
    `:public_key.pem_encode([:public_key.pem_entry_encode(:RSAPrivateKey, priv)])`, build the
    cert with `:public_key.pkix_test_root_cert(~c"CN=ledgerloop-fake-idp", key: priv)`, and PEM-encode
    the cert as `{:Certificate, der, :not_encrypted}`. Write the key to
    `demo/ledger_loop/priv/fake_idp/idp_key.pem` and the cert to
    `demo/ledger_loop/priv/fake_idp/idp_cert.pem`; both are committed demo-only secrets (no real trust).

    Create `LedgerLoop.FakeIdP.Keypair` exposing `private_key/0` (returns the decoded
    `{:RSAPrivateKey, ...}` via `:public_key.pem_decode` → `:public_key.pem_entry_decode`) and
    `cert_pem/0` (returns the literal cert PEM string). Load PEMs from
    `:ledger_loop |> Application.app_dir("priv/fake_idp/...") |> File.read!()`. Cache the decoded
    key in `:persistent_term` keyed by module to avoid repeated disk/PEM work (mirrors
    fake_idp.ex keypair caching).

    Per D-31/CONTEXT fence: this signer/keypair lives ENTIRELY inside demo/ledger_loop. Do NOT
    reference `Relyra.TestSupport.*` — those are stripped from relyra's :prod path-dep build.
  </action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop/fake_idp/keypair_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `demo/ledger_loop/priv/fake_idp/idp_key.pem` exists and contains "RSA PRIVATE KEY"
    - `demo/ledger_loop/priv/fake_idp/idp_cert.pem` exists and contains "BEGIN CERTIFICATE"
    - `LedgerLoop.FakeIdP.Keypair.private_key/0` returns a tuple whose first element is `:RSAPrivateKey`
    - `LedgerLoop.FakeIdP.Keypair.cert_pem/0` returns the exact bytes of `idp_cert.pem`
    - The cert's SubjectPublicKeyInfo corresponds to the private key (a round-trip sign/verify with `:public_key` over arbitrary bytes succeeds) — assert this in keypair_test.exs
    - No `Relyra.TestSupport` reference anywhere in keypair.ex
  </acceptance_criteria>
  <done>Keypair PEMs committed under priv/fake_idp/; Keypair loader returns the decoded key + cert PEM; keypair_test.exs proves key↔cert pairing.</done>
</task>

<task type="auto">
  <name>Task 2: Align the …J0 fixture cert-trust + idp_sso_url, and attach LoginTrace in Application</name>
  <read_first>
    - demo/ledger_loop/lib/ledger_loop/demo/fixtures.ex (relyra_certificates/0 @enabled_cert_id row; relyra_connections/0 …J0; saml_identities/0)
    - demo/ledger_loop/lib/ledger_loop/demo/reset.ex:42 (confirms certs re-seed from Fixtures — A1)
    - demo/ledger_loop/lib/ledger_loop/application.ex (children list; where Repo starts)
    - lib/relyra/telemetry/handlers/login_trace.ex:61-64 (attach/1 contract)
    - demo/ledger_loop/lib/ledger_loop/fake_idp/keypair.ex (cert_pem/0 source of the PEM string)
  </read_first>
  <action>
    In `LedgerLoop.Demo.Fixtures.relyra_certificates/0`, replace the `@enabled_cert_id` row's
    `pem: "MOCK_PEM_NOT_REAL"` with the demo cert PEM. Source the PEM at fixture-build time from
    `LedgerLoop.FakeIdP.Keypair.cert_pem/0` (so the fixture and the signer can never drift). Recompute
    `fingerprint_sha256` from the new cert DER (sha256 of the cert's DER, uppercase hex) so the admin UI
    displays a truthful fingerprint — the verifier does not check it on the assertion path, but keep it
    honest. Leave the staged/next cert rows as-is (they are unrelated MOCK rows; this phase only enables …J0).

    In `relyra_connections/0` for `…J0`, change `idp_sso_url` from
    `"https://idp.northstar.example.com/sso"` to the local FakeIdP login URL. Use the demo endpoint's
    base URL + `/fake_idp/login` (absolute URL so `start_login`'s `redirect(external: ...)` lands on the
    FakeIdP). Keep `idp_entity_id` = `"https://idp.northstar.example.com"` UNCHANGED so it still matches
    `saml_identities/0` issuer (per Pitfall 4 / Assumption A2 — issuer drift breaks user-map). Drive
    Issuer/Audience/Destination/Recipient downstream from THESE fields, not hardcoded values.

    In `LedgerLoop.Application.start/2`, after `LedgerLoop.Repo` is in the children list and started,
    call `Relyra.Telemetry.Handlers.LoginTrace.attach(repo: LedgerLoop.Repo)`. Place the attach AFTER
    `Supervisor.start_link` succeeds (so the Repo is alive) and ignore an `{:error, :already_exists}`
    return (idempotent across app restarts in test). Do NOT attach inside the children list.
  </action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop/fake_idp/keypair_test.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `Fixtures.relyra_certificates/0` @enabled_cert_id `:pem` equals `LedgerLoop.FakeIdP.Keypair.cert_pem/0` and contains "BEGIN CERTIFICATE"; no "MOCK_PEM_NOT_REAL" remains on the @enabled_cert_id row
    - `:public_key.pem_decode/1` on that `:pem` returns a single `{:Certificate, _, _}` entry (cert parses)
    - `Fixtures.relyra_connections/0` …J0 `idp_sso_url` ends with `/fake_idp/login`; `idp_entity_id` is still `"https://idp.northstar.example.com"` and equals every `saml_identities/0` issuer
    - `LedgerLoop.Application` source contains `LoginTrace.attach(repo: LedgerLoop.Repo)` and it runs after the Repo starts (attach is NOT a supervised child)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>The enabled connection trusts the demo cert and redirects to the local FakeIdP; LoginTrace is attached so login telemetry writes domain: :login AuditEvents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo priv/ secret → committed repo | Demo IdP private key is committed; must be unmistakably demo-only (no real-world trust) |
| demo signer ↔ relyra :prod build | Demo path-dep compiles relyra in :prod; relyra TestSupport is stripped and must never be referenced |
| fixture cert → relyra connection cert_chain | The seeded cert becomes the ONLY trust anchor relyra uses for …J0 (never document KeyInfo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Elevation | demo keypair leaking into prod trust | mitigate | Keypair + signer live in demo/ledger_loop only; relyra `prod_elixirc_paths` `test_support` exclusion untouched; keypair.ex references NO `Relyra.TestSupport.*` |
| T-57-02 | Spoofing | document-KeyInfo trust bypass | accept (relyra-owned) | relyra rejects `key_info_trust` (signature.ex); demo only swaps the configured connection cert — no change to the trust source |
| T-57-03 | Tampering/Repudiation | trace rows missing → tampered attempt invisible | mitigate | `LoginTrace.attach(repo: LedgerLoop.Repo)` wired in Application so domain: :login AuditEvents persist |
| T-57-04 | Tampering | fixture/signer cert drift (verify fails for wrong reason) | mitigate | Fixture `:pem` sourced from `Keypair.cert_pem/0` at build time — single source, cannot drift |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (RESEARCH Package Legitimacy Gate: N/A) — no install tasks in this phase |
</threat_model>

<verification>
- `cd demo/ledger_loop && mix test test/ledger_loop/fake_idp/keypair_test.exs` passes
- `cd demo/ledger_loop && mix compile --warnings-as-errors` exits 0
- Fixture cert is a parseable PEM and equals the committed cert; idp_entity_id ↔ saml_identities issuer invariant holds
- No relyra source under lib/ is modified (this phase touches demo/ only)
</verification>

<success_criteria>
- Demo keypair + cert committed under demo/ledger_loop/priv/fake_idp/
- …J0 fixture trusts the demo cert and redirects to /fake_idp/login; idp_entity_id ↔ SAMLIdentity issuer preserved
- LoginTrace handler attached in Application
- keypair_test.exs green; demo app compiles warnings-as-errors clean
</success_criteria>

<output>
Create `.planning/phases/57-demo-fakeidp-browser-login-proof/57-01-SUMMARY.md` when done
</output>
