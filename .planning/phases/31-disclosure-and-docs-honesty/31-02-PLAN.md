---
phase: 31-disclosure-and-docs-honesty
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/advisories/2026-001-xmldsig-signature-not-verified.md
autonomous: true
requirements: [DISC-02]

must_haves:
  truths:
    - "One checked-in advisory draft exists at docs/advisories/2026-001-xmldsig-signature-not-verified.md (new docs/advisories/ directory)"
    - "The draft is a GHSA body that maps 1:1 onto the GitHub repository-advisory form (summary, severity/CVSS, affected/patched, weaknesses/CWE, impact, workaround, credits, references)"
    - "The draft carries the CVSS 3.1 vector CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N (9.1 Critical) and CWE-347"
    - "The draft marks affected hex >= 1.0.0, < 1.2.0 and patched 1.2.0"
    - "The draft contains a 'CVE request' subsection and the exact CHANGELOG security-note prose"
    - "The draft is STAGED, not published: internal id RELYRA-2026-001, no gh/CVE/GHSA filing, no CHANGELOG.md edit"
  artifacts:
    - path: "docs/advisories/2026-001-xmldsig-signature-not-verified.md"
      provides: "Staged GHSA advisory draft + CVE-request subsection + CHANGELOG note prose"
      contains: "CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N"
  key_links:
    - from: "docs/advisories/2026-001-xmldsig-signature-not-verified.md"
      to: "docs/security_findings.md (RELYRA-2026-001)"
      via: "shared internal finding id cross-reference"
      pattern: "RELYRA-2026-001"
    - from: "docs/advisories/2026-001-xmldsig-signature-not-verified.md References"
      to: "test/security/xml/adversarial_crypto_test.exs"
      via: "shipped regression-proof citation"
      pattern: "adversarial_crypto_test.exs"
---

<objective>
Stage one checked-in advisory draft under a new `docs/advisories/` directory that maps 1:1 onto the GitHub repository-security-advisory form, so the ship-time publication step is copy/paste rather than re-derivation. The draft contains the GHSA body, a CVE-request subsection, and the exact CHANGELOG security-note prose.

Implements DISC-02. Honors locked decisions D-07 (one checked-in draft at the named path), D-08 (do NOT hand-edit the release-please CHANGELOG; note prose lives in the draft), D-09 (fix-first — stage only, no publication).

Purpose: A coordinated advisory must exist and read like a real, citable disclosure (credibility through convention) before the fix ships — but it is NOT published until ship time.
Output: One new markdown advisory draft (no `lib/` change, no CHANGELOG edit, no `gh` invocation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-disclosure-and-docs-honesty/31-CONTEXT.md
@.planning/phases/31-disclosure-and-docs-honesty/31-RESEARCH.md
@.planning/phases/31-disclosure-and-docs-honesty/31-VALIDATION.md

<constraints>
- FROZEN: do NOT edit any `lib/relyra/security/*`, `SECURITY.md`, `SECURITY_REVIEW_EVIDENCE.md`, or `CHANGELOG.md`. The CHANGELOG note lives ONLY as prose inside the draft (D-08).
- Do NOT run `gh`, file a GHSA, request a CVE, cut a GitHub Release, or merge to `main` (D-09 fix-first). This is staging only.
- Do NOT add a "Security" section to CHANGELOG.md — the release-please conventionalcommits preset renders only `feat:`→Features and `fix:`→Bug Fixes; there is NO Security section, so such a task would be clobbered (D-08, RESEARCH.md Pitfall 1). The fix's already-landed `feat/fix(29-…)` commits surface the change at release.
- Use the internal id `RELYRA-2026-001`; state that the GHSA/CVE numbers attach at publication (D-06).
- Brand voice (LOCKED): calm, exact, falsifiable. NEVER "bulletproof"/"unhackable"/"military-grade"/"catastrophic" marketing absolutes. State the bypass plainly.
- No automated gate asserts the draft's content (D-09 / RESEARCH.md Wave 0) — do NOT add a brittle content-grep CI gate for the draft. Existence + reviewer judgment is the bar.
</constraints>

<advisory_spec>
Required draft sections, mapped to the GHSA repository-advisory form (RESEARCH.md Pattern 1/2):
- Front-matter / id block: internal id `RELYRA-2026-001` (GHSA/CVE attach at publication); Status: STAGED — not published (fix-first); ecosystem hex (Erlang) / package `relyra`; Affected `>= 1.0.0, < 1.2.0` (published 1.0.0, 1.1.0); Patched `1.2.0`; Severity Critical; Weakness CWE-347.
- Title: Relyra SAML `SignatureValue` not cryptographically verified → authentication bypass.
- Summary: one sentence — what is broken and the effect (auth bypass).
- Details: the exact primitive missing in 1.0.0/1.1.0 — `:public_key.verify` over canonicalized `SignedInfo` was not performed; `DigestValue` was not recomputed; `canonicalize/2` was an unused passthrough. Name the trust boundary.
- Impact: a forged `SignatureValue` carrying an attacker-controlled `NameID` is accepted as `{:ok}`; any relying-party app on Relyra 1.0.0/1.1.0 can be logged in as an arbitrary user.
- Patches: fixed in hex `1.2.0` (real exclusive-C14N + `:public_key.verify` + constant-time `DigestValue` recompute on both `verify/4` and `verify_metadata_root/4`).
- Workarounds: none other than upgrade — there is no safe configuration of 1.0.0/1.1.0 (D-07).
- References: the shipped regression proofs (`test/security/xml/adversarial_crypto_test.exs`, `test/security/ci_gate_integrity_test.exs`) + the fix commits as PRIMARY. Optionally mention the Phase 29/30 SECURITY.md threat verifications as internal corroboration, but do NOT make the public advisory depend on `.planning/` paths (RESEARCH.md Open Question 1).
- Credits: maintainers (Finder/Reporter).
- Severity / CVSS section: vector `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N` → 9.1 Critical, with a one-line note that an `S:C` reading reaches 10.0 (RESEARCH.md scope decision). Optional: mention CVSS 4.0 mirror is available, not required.
- Weaknesses: CWE-347 (Improper Verification of Cryptographic Signature) primary; CWE-287 (Improper Authentication) optional companion. NOT CWE-436 (Relyra's root cause was a total absence of verify, not a parser differential).
- Affected Products table: ecosystem hex (GitHub label `Erlang`, marked `[ASSUMED]` — verify dropdown label at file time), package `relyra`, vulnerable range `>= 1.0.0, < 1.2.0`, first patched `1.2.0`.
- "CVE request" subsection: pre-stage the GHSA "Request CVE" fields (RESEARCH.md CVE Request Fields table) — internal id, title, ecosystem/package, affected range, first patched, CVSS 3.1 vector, CWE, description, credits, request mechanism = GitHub "Request CVE" on the draft GHSA (ship-time, D-09).
- "CHANGELOG security note" subsection: the exact human-readable security-note prose, with a note that release-please's CHANGELOG renders the fix as conventional Features/Bug Fixes entries (no Security section); this prose surfaces at the GitHub Release notes at publish, NOT as a generated CHANGELOG Security section (D-08, RESEARCH.md Pitfall 1).
</advisory_spec>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the docs/advisories/ directory and the GHSA advisory draft body</name>
  <files>docs/advisories/2026-001-xmldsig-signature-not-verified.md</files>
  <read_first>
    - .planning/phases/31-disclosure-and-docs-honesty/31-CONTEXT.md (D-07, D-08, D-09 — locked: one staged draft, no CHANGELOG hand-edit, fix-first)
    - .planning/phases/31-disclosure-and-docs-honesty/31-RESEARCH.md (Pattern 1 GHSA form-field map; Pattern 2 body section order; CVSS 3.1 Vector section; CWE Classification section; "Advisory front-matter / id block" example; Open Question 1 + 2)
    - docs/security_findings.md (shared internal id RELYRA-2026-001 — keep the advisory consistent with the ledger row)
    - SECURITY.md (existing disclosure-workflow tone/brand to match; do NOT edit it)
  </read_first>
  <action>
    Create the new directory `docs/advisories/` and write `docs/advisories/2026-001-xmldsig-signature-not-verified.md` as a GHSA-form-mapped draft per the advisory_spec above. Concrete identifiers to include verbatim: internal id `RELYRA-2026-001`; affected hex range `>= 1.0.0, < 1.2.0`; patched `1.2.0`; CVSS 3.1 vector `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N` (9.1 Critical) with a one-line `S:C`→10.0 note; CWE-347 (primary) + optional CWE-287 companion (NOT CWE-436); ecosystem hex / GitHub label `Erlang` (mark `[ASSUMED]`, verify at file time); package `relyra`. Body sections in order: Summary, Details, Impact, Patches, Workarounds (= upgrade only; no safe config of 1.0.0/1.1.0), References (shipped proofs `test/security/xml/adversarial_crypto_test.exs` + `test/security/ci_gate_integrity_test.exs` + fix commits as primary; Phase 29/30 SECURITY.md only as optional internal corroboration, not a dependency), Credits (maintainers).
    Mark the draft Status: STAGED — not published (fix-first; publish at the 1.2.0 release). State that GHSA/CVE numbers attach at publication and replace the `RELYRA-2026-001` placeholder.
    Describe the missing primitive in 1.0.0/1.1.0 plainly (`:public_key.verify` over canonicalized `SignedInfo` not performed; `DigestValue` not recomputed; `canonicalize/2` an unused passthrough) and the fix in 1.2.0 (real exclusive-C14N + `:public_key.verify` + constant-time `DigestValue` recompute on both `verify/4` and `verify_metadata_root/4`).
    Brand voice: calm, exact, falsifiable — no marketing absolutes. Do NOT touch CHANGELOG.md, SECURITY.md, or any generated file. Do NOT run `gh` or any publish/CVE command.
  </action>
  <verify>
    <automated>test -f docs/advisories/2026-001-xmldsig-signature-not-verified.md && grep -q "CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N" docs/advisories/2026-001-xmldsig-signature-not-verified.md && grep -q "CWE-347" docs/advisories/2026-001-xmldsig-signature-not-verified.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits 0
    - `grep -q "CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N" docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits 0
    - `grep -q "CWE-347" docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits 0
    - `grep -q "RELYRA-2026-001" docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits 0
    - `grep -qE ">= ?1\.0\.0, ?< ?1\.2\.0" docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits 0 (affected range)
    - `grep -qE "[Pp]atched.*1\.2\.0" docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits 0
    - `grep -q "adversarial_crypto_test.exs" docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits 0 (shipped regression proof referenced)
    - `grep -qiE "STAGED|not published|fix-first" docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits 0 (staged posture stated)
    - `grep -qiE "bulletproof|unhackable|military-grade" docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits NON-zero (no marketing absolutes)
    - `git diff --quiet CHANGELOG.md` exits 0 (CHANGELOG NOT hand-edited)
  </acceptance_criteria>
  <done>The advisory draft exists with a GHSA-form-mapped body carrying the CVSS vector, CWE-347, affected/patched ranges, the internal id, staged posture, and shipped regression references; CHANGELOG untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CVE-request subsection and the exact CHANGELOG security-note prose to the draft</name>
  <files>docs/advisories/2026-001-xmldsig-signature-not-verified.md</files>
  <read_first>
    - docs/advisories/2026-001-xmldsig-signature-not-verified.md (the body created in Task 1 — append the two subsections)
    - .planning/phases/31-disclosure-and-docs-honesty/31-RESEARCH.md ("CVE Request Fields" table; Pitfall 1 — no auto Security section; State of the Art — GHSA→Request CVE flow)
    - .planning/phases/31-disclosure-and-docs-honesty/31-CONTEXT.md (D-08, D-09)
    - CHANGELOG.md (read-only — confirm it has only Features/Bug Fixes sections; do NOT edit it)
    - mix.exs (read-only — confirm release-please/conventionalcommits drives CHANGELOG; for context only)
  </read_first>
  <action>
    Append two subsections to the existing draft.
    (1) "CVE request" subsection: pre-stage the GHSA "Request CVE" fields per RESEARCH.md's CVE Request Fields table — internal id `RELYRA-2026-001` (until assignment), title (Relyra SAML `SignatureValue` not cryptographically verified → authentication bypass), ecosystem/package hex (`Erlang`)/`relyra`, affected range `>= 1.0.0, < 1.2.0`, first patched `1.2.0`, CVSS 3.1 `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N` (9.1 Critical), CWE-347 (+ optional CWE-287), description (GHSA summary+impact), credits (maintainers), request mechanism = GitHub "Request CVE" on the draft GHSA (ship-time, D-09). State plainly that the request is NOT submitted in this phase.
    (2) "CHANGELOG security note" subsection: the exact human-readable security-note prose to use at the GitHub Release at publish. Add a one-line note that release-please's conventionalcommits preset renders the fix as conventional Features/Bug Fixes entries and has NO Security section, so this prose lives here and in the Release notes — it is NOT added to the generated CHANGELOG.md (D-08, Pitfall 1). Do NOT edit CHANGELOG.md.
    Do NOT run `gh` or submit any CVE/GHSA request (D-09).
  </action>
  <verify>
    <automated>grep -qiE "CVE request|Request CVE" docs/advisories/2026-001-xmldsig-signature-not-verified.md && grep -qiE "CHANGELOG" docs/advisories/2026-001-xmldsig-signature-not-verified.md && git diff --quiet CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qiE "CVE request|Request CVE" docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits 0 (CVE-request subsection present)
    - `grep -qiE "CHANGELOG" docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits 0 (CHANGELOG-note subsection present)
    - the CHANGELOG-note subsection notes there is NO release-please Security section (no clobberable hand-edit)
    - `git diff --quiet CHANGELOG.md` exits 0 (CHANGELOG.md NOT hand-edited)
    - `git diff --quiet SECURITY.md SECURITY_REVIEW_EVIDENCE.md` exits 0 (no forbidden hand-edits)
    - no `gh`/publish/CVE-submission command was run (the request mechanism is documented as a ship-time action, not performed)
  </acceptance_criteria>
  <done>The draft now contains the pre-staged CVE-request fields and the exact CHANGELOG security-note prose, with the no-auto-Security-section caveat; CHANGELOG.md and all generated files remain unedited; nothing published.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-and-disclosure phase, NO `lib/` code change, no new runtime attack surface. The relevant boundary is the **disclosure-process boundary**: staged draft (internal) vs published advisory (external), and the fix-first ordering that keeps the disclosure ahead of publication.

| Boundary | Description |
|----------|-------------|
| Staged draft → published GHSA/CVE | The draft is internal/checked-in; publication is a deliberate ship-time hand-off |
| Advisory content → downstream users | Wrong affected/patched range or wrong CVSS/CWE would mislead users acting on the advisory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | Information Disclosure | premature/early publication before the fix ships (violates fix-first D-09) | mitigate | Plan stages a checked-in file only; no `gh`/GHSA/CVE/Release/merge; draft Status marked STAGED — not published; acceptance asserts the staged posture and `git diff --quiet CHANGELOG.md` |
| T-31-07 | Spoofing/Tampering | inaccurate advisory (wrong affected/patched range, wrong CVSS/CWE) misleads downstream users | mitigate | Concrete locked values: affected `>= 1.0.0, < 1.2.0`, patched `1.2.0`, `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N` (9.1), CWE-347 — all grep-asserted in acceptance and anchored to the CVE-2025-25292 precedent |
| T-31-08 | Information Disclosure | leaking a real GHSA/CVE id prematurely | mitigate | Use the internal `RELYRA-2026-001` placeholder; state ids attach at publication (D-06); acceptance greps for the placeholder id |
| T-31-09 | Tampering | hand-editing the release-please CHANGELOG.md (D-08) → clobbered at next release / misleading | mitigate | The CHANGELOG note lives only as prose inside the draft; both tasks assert `git diff --quiet CHANGELOG.md`; no "add Security section" task |
| T-31-10 | Elevation of Privilege (process) | the disclosed bug itself = forged signature → auth bypass (the vuln being documented) | accept | Already fixed in code (Phases 28–30, frozen); this plan only documents it; regression proof cited (`adversarial_crypto_test.exs`) |
</threat_model>

<verification>
- `test -f docs/advisories/2026-001-xmldsig-signature-not-verified.md` exits 0.
- `grep -q "CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N" docs/advisories/2026-001-xmldsig-signature-not-verified.md` and `grep -q "CWE-347" ...` exit 0.
- Affected `>= 1.0.0, < 1.2.0` + patched `1.2.0` + internal id `RELYRA-2026-001` all present.
- CVE-request and CHANGELOG-note subsections present; the CHANGELOG note documents no-auto-Security-section.
- `git diff --quiet CHANGELOG.md SECURITY.md SECURITY_REVIEW_EVIDENCE.md` — no forbidden hand-edits; no `gh`/publish/CVE-submission run.
</verification>

<success_criteria>
- A GHSA draft + requested-CVE staging + CHANGELOG security note are prepared, marking hex `1.0.0`/`1.1.0` affected, staged for publication at the fixed release (fix-first: not published before the fix ships) — ROADMAP success criterion 3.
- The draft maps 1:1 to the GitHub repository-advisory form (credibility through convention) and shares the `RELYRA-2026-001` id with the ledger row.
- No CHANGELOG.md / SECURITY.md / generated-file hand-edits; no publication action performed.
</success_criteria>

<output>
Create `.planning/phases/31-disclosure-and-docs-honesty/31-02-SUMMARY.md` when done.
</output>
