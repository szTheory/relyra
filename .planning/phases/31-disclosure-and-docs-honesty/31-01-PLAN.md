---
phase: 31-disclosure-and-docs-honesty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/security_boundary.md
  - SECURITY_REVIEW.md
  - docs/security_findings.md
autonomous: true
requirements: [DISC-01]

must_haves:
  truths:
    - "docs/security_boundary.md states the real post-fix crypto primitive (:public_key.verify over canonicalized SignedInfo vs configured IdP cert + constant-time DigestValue recompute), not vague 'signature verification'"
    - "SECURITY_REVIEW.md Named Code Seams row states the real post-fix crypto primitive on both verify/4 and verify_metadata_root/4"
    - "SECURITY_REVIEW.md no longer claims a zero-findings state (lines 15 + 53 corrected forward-looking) while keeping the findings-ledger cross-reference grep-matchable"
    - "docs/security_findings.md ledger has one Critical RELYRA-2026-001 row replacing the 'none yet' placeholder; the contradicting 'no external findings recorded yet' prose is corrected"
    - "Generated SECURITY_REVIEW_EVIDENCE.md is inspected and confirmed not to overstate verification; left untouched (or fixed via generator if drift found)"
    - "mix ci.security passes green after all edits (full lane: doc-gate + crypto/corpus suites + generator drift-check)"
  artifacts:
    - path: "docs/security_boundary.md"
      provides: "Corrected In-Scope line + Trust Seams Signed-content row stating real primitive"
      contains: ":public_key.verify"
    - path: "SECURITY_REVIEW.md"
      provides: "Corrected Named Code Seams row + corrected findings-state prose; findings-ledger cross-ref preserved"
      contains: "Findings Ledger"
    - path: "docs/security_findings.md"
      provides: "One Critical RELYRA-2026-001 findings-ledger row; corrected summary prose"
      contains: "RELYRA-2026-001"
  key_links:
    - from: "SECURITY_REVIEW.md"
      to: "docs/security_findings.md"
      via: "ci.security grep cross-reference"
      pattern: "docs/security_findings.md|Findings Ledger"
    - from: "docs/security_findings.md ledger row"
      to: "test/security/xml/adversarial_crypto_test.exs"
      via: "Regression Proof column citation"
      pattern: "adversarial_crypto_test.exs"
---

<objective>
Correct the three hand-maintained security docs that overstate the verification guarantee so they precisely describe the now-real post-fix crypto primitive, and replace the findings-ledger `none yet` placeholder with one Critical `RELYRA-2026-001` row. Inspect the generated evidence file (inspect-then-decide, do NOT assume an edit) and confirm it needs no change. Keep the `mix ci.security` doc-gate green throughout, and run the full lane as the closing acceptance.

Implements DISC-01. Honors locked decisions D-01 (precise primitive language), D-02 (forward-looking honesty; historical gap captured once in the ledger), D-03 (SECURITY.md left as-is), D-04/D-05/D-06 (one Critical ledger row with internal id), D-10 (preserve doc-gate cross-ref), D-11 (generated evidence only via generator).

Purpose: The shipped story must match the code — docs that overstate the guarantee are the original sin this phase corrects.
Output: Three corrected markdown docs (no `lib/` change), with the full `mix ci.security` lane green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-disclosure-and-docs-honesty/31-CONTEXT.md
@.planning/phases/31-disclosure-and-docs-honesty/31-RESEARCH.md
@.planning/phases/31-disclosure-and-docs-honesty/31-VALIDATION.md

<constraints>
- FROZEN: do NOT edit any file under `lib/relyra/security/*`, `SECURITY.md` (D-03), the generated `SECURITY_REVIEW_EVIDENCE.md` by hand (D-11), or `CHANGELOG.md` by hand (D-08).
- Do NOT run `gh`, file a GHSA, request a CVE, cut a release, or merge to `main` (D-09 fix-first).
- Brand voice (LOCKED): calm, exact, falsifiable. NEVER "bulletproof"/"unhackable"/"military-grade".
- D-02: capture the historical gap ONCE (in the ledger row), not as scattered caveats across docs. The corrected boundary/review prose describes what is NOW true post-fix.
- D-10: `mix ci.security` greps `docs/security_findings.md|Findings Ledger` over `SECURITY_REVIEW.md` — at least one of those substrings MUST remain after edits.
</constraints>

<exact_targets>
Overstatement sites (from RESEARCH.md, A-confirmed against the repo):
- `docs/security_boundary.md` line 8 (In Scope: "...signed-node selection, signature verification, and document-provided KeyInfo trust rejection.")
- `docs/security_boundary.md` line 32 (Trust Seams, Signed-content row: "Signed nodes must bind to configured trust only, never document-provided KeyInfo.")
- `SECURITY_REVIEW.md` line 45 (Named Code Seams: "Document-provided KeyInfo and signed-node trust rejection").
- `SECURITY_REVIEW.md` line 15 ("No external findings are recorded yet.") and line 53 ("It starts in a zero-findings state...") — both now contradict the new ledger row; correct forward-looking.
- `docs/security_findings.md` line 3 ("Current state: no external findings recorded yet.") and line 16 (the `none yet` placeholder ledger row).

Factual core to encode (D-01), exact wording is executor's discretion within brand voice:
inbound Response/assertion and metadata-root (EntityDescriptor/EntitiesDescriptor) signatures are cryptographically verified — `:public_key.verify` over the exclusive-C14N canonicalized `SignedInfo` against the CONFIGURED IdP certificate's public key (never document-provided `KeyInfo`), plus a constant-time `DigestValue` recompute/compare over the canonicalized referenced element, bound to the exact consumed node — on BOTH `verify/4` and `verify_metadata_root/4`.
</exact_targets>

<ledger_row_spec>
Findings ledger has 8 columns (D-04/D-05/D-06, RESEARCH.md Pattern 3):
`Finding ID | Severity | Exploit Path | Disposition | Owner | Regression Proof | Blocker State | Revisit Date`
- Finding ID: `RELYRA-2026-001` (note GHSA/CVE ids attach at publication)
- Severity: `Critical`
- Exploit Path: forged `SignatureValue` carrying an attacker-controlled `NameID` accepted as `{:ok}` → full SAML authentication bypass
- Disposition: `Confirmed → Fixed in v1.1 (hex 1.2.0)`
- Owner: `maintainers`
- Regression Proof: `test/security/xml/adversarial_crypto_test.exs` (:adversarial_crypto) + `test/security/ci_gate_integrity_test.exs`, green under `mix ci.security`
- Blocker State: `resolved`
- Revisit Date: at v1.2.0 ship / GHSA publish
</ledger_row_spec>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the two reviewer docs to the real post-fix primitive</name>
  <files>docs/security_boundary.md, SECURITY_REVIEW.md</files>
  <read_first>
    - docs/security_boundary.md (current overstatement at lines 8 and 32)
    - SECURITY_REVIEW.md (overstatement at line 45; contradicting zero-findings prose at lines 15 and 53; grep-matchable strings to PRESERVE)
    - .planning/phases/31-disclosure-and-docs-honesty/31-CONTEXT.md (D-01, D-02, D-03, D-10 — locked)
    - .planning/phases/31-disclosure-and-docs-honesty/31-RESEARCH.md ("Corrected primitive language (D-01)" section; Pitfall 2)
    - mix.exs (the `ci.security` alias grep step over SECURITY_REVIEW.md)
  </read_first>
  <action>
    In docs/security_boundary.md, rewrite the In-Scope bullet at line 8 and the Trust Seams "Signed-content trust" row at line 32 to state the real post-fix primitive instead of the vague "signature verification" / "bind to configured trust only" phrasing: cryptographic `:public_key.verify` over the exclusive-C14N canonicalized `SignedInfo` against the CONFIGURED IdP certificate's public key (never document-provided `KeyInfo`), plus a constant-time `DigestValue` recompute/compare bound to the exact consumed node, on both `verify/4` and `verify_metadata_root/4`.
    In SECURITY_REVIEW.md, rewrite the Named Code Seams row at line 45 ("Document-provided KeyInfo and signed-node trust rejection") to the same precise primitive (cite the same seam modules already in that row). Correct the contradicting zero-findings prose at line 15 ("No external findings are recorded yet.") and line 53 ("It starts in a zero-findings state...") to forward-looking honest phrasing that acknowledges the recorded finding now exists.
    PRESERVE both grep targets in SECURITY_REVIEW.md: keep the `## Findings Ledger` heading AND at least one `docs/security_findings.md` link reference, or the D-10 doc-gate grep turns red.
    Use brand voice: calm, exact, falsifiable. Do NOT add scattered historical caveats (D-02 — the historical gap is recorded once, in the ledger, by Task 2). Do NOT touch SECURITY.md (D-03). Do not paste the surrounding doc back verbatim — edit only the named lines/rows.
  </action>
  <verify>
    <automated>mix cmd test -f SECURITY_REVIEW.md && mix cmd test -f docs/security_boundary.md && mix cmd grep -nE "docs/security_findings.md|Findings Ledger" SECURITY_REVIEW.md && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q ":public_key.verify" docs/security_boundary.md` exits 0
    - `grep -q ":public_key.verify" SECURITY_REVIEW.md` exits 0
    - `mix cmd grep -nE "docs/security_findings.md|Findings Ledger" SECURITY_REVIEW.md` exits 0 (D-10 cross-ref preserved)
    - `grep -qiE "bulletproof|unhackable|military-grade" docs/security_boundary.md SECURITY_REVIEW.md` exits NON-zero (no marketing absolutes)
    - SECURITY_REVIEW.md no longer asserts a zero-findings state at the corrected lines (the phrases "No external findings are recorded yet" and "starts in a zero-findings state" are removed or corrected)
    - SECURITY.md is byte-unchanged (git diff shows no change to SECURITY.md)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>Both reviewer docs state the real `:public_key.verify` + constant-time `DigestValue` primitive on both verify paths; no zero-findings contradiction; doc-gate grep cross-ref intact; SECURITY.md untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the findings-ledger placeholder with the Critical RELYRA-2026-001 row</name>
  <files>docs/security_findings.md</files>
  <read_first>
    - docs/security_findings.md (line 3 contradicting prose; line 14 the 8-column header; line 16 the `none yet` placeholder row)
    - .planning/phases/31-disclosure-and-docs-honesty/31-CONTEXT.md (D-04, D-05, D-06 — locked ledger row content)
    - .planning/phases/31-disclosure-and-docs-honesty/31-RESEARCH.md (Pattern 3 row shape; Pitfall 4 table-column-count)
    - test/security/xml/adversarial_crypto_test.exs (regression-proof citation target — confirm it exists)
    - test/security/ci_gate_integrity_test.exs (anti-hollow gate citation target — confirm it exists)
  </read_first>
  <action>
    In docs/security_findings.md, replace the `none yet` placeholder ledger row at line 16 with one Critical finding row matching the existing 8-column header exactly (Finding ID | Severity | Exploit Path | Disposition | Owner | Regression Proof | Blocker State | Revisit Date). Use: Finding ID `RELYRA-2026-001` (note GHSA/CVE ids attach at publication, D-06); Severity `Critical` (D-05); Exploit Path = forged `SignatureValue` carrying an attacker-controlled `NameID` accepted as `{:ok}` → full SAML authentication bypass; Disposition `Confirmed → Fixed in v1.1 (hex 1.2.0)`; Owner `maintainers`; Regression Proof = `test/security/xml/adversarial_crypto_test.exs` (:adversarial_crypto) + `test/security/ci_gate_integrity_test.exs`, green under `mix ci.security`; Blocker State `resolved`; Revisit Date = at v1.2.0 ship / GHSA publish.
    Also correct the contradicting summary prose at line 3 ("Current state: no external findings recorded yet.") so it no longer contradicts the new row (forward-looking, D-02).
    Match the column count exactly so the markdown table stays valid (Pitfall 4). Keep the rest of the ledger doc (Summary, Disposition Workflow, Regression Requirements) intact. Do NOT scatter additional historical caveats elsewhere — this row is the single canonical historical record (D-02).
  </action>
  <verify>
    <automated>grep -q "RELYRA-2026-001" docs/security_findings.md && grep -q "adversarial_crypto_test.exs" docs/security_findings.md && grep -q "ci_gate_integrity_test.exs" docs/security_findings.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "RELYRA-2026-001" docs/security_findings.md` exits 0
    - `grep -qiE "\| *Critical *\|" docs/security_findings.md` exits 0 (Critical severity in a table cell)
    - `grep -q "Confirmed → Fixed in v1.1 (hex 1.2.0)" docs/security_findings.md` exits 0
    - `grep -q "adversarial_crypto_test.exs" docs/security_findings.md` AND `grep -q "ci_gate_integrity_test.exs" docs/security_findings.md` both exit 0
    - `grep -c "none yet" docs/security_findings.md` returns 0 (placeholder removed)
    - the new ledger row has exactly 8 pipe-delimited columns matching the header at line 14 (no table corruption)
    - line-3 summary no longer asserts "no external findings recorded yet"
  </acceptance_criteria>
  <done>The `none yet` placeholder is replaced by one valid 8-column Critical RELYRA-2026-001 row citing both regression-proof tests; the contradicting summary prose is corrected.</done>
</task>

<task type="auto">
  <name>Task 3: Inspect generated evidence (inspect-then-decide), then run the full ci.security lane</name>
  <files>SECURITY_REVIEW_EVIDENCE.md</files>
  <read_first>
    - SECURITY_REVIEW_EVIDENCE.md (header "Generated from executable security defaults..."; Strict Default + Escape Hatch evidence tables)
    - lib/mix/tasks/relyra.security_review.ex (the generator; `--check` drift comparator)
    - .planning/phases/31-disclosure-and-docs-honesty/31-CONTEXT.md (D-11 — generated file only via generator)
    - .planning/phases/31-disclosure-and-docs-honesty/31-RESEARCH.md (Pitfall 3; Assumption A2 — likely NO change needed)
    - mix.exs (the `ci.security` alias: doc-gate steps + `relyra.security_review --check` + crypto/corpus suites)
  </read_first>
  <action>
    INSPECT-THEN-DECIDE (do NOT assume an edit): read every claim row in SECURITY_REVIEW_EVIDENCE.md and check whether any overstates "verification". Per RESEARCH.md A2, the tables describe strict-default signature/digest policy, RelayState opacity, document-`KeyInfo` trust rejection, and escape-hatch/audit evidence — these are still true post-fix and do NOT contain the "signature verification" overstatement. Expected outcome: NO change needed.
    - If (and only if) a claim row genuinely overstates verification: do NOT hand-edit the generated file (D-11). Instead edit the generator `lib/mix/tasks/relyra.security_review.ex` so its rendered output is accurate, then regenerate (`mix relyra.security_review` without `--check`) so `mix relyra.security_review --check` reports no drift.
    - If no row overstates (expected): leave both the generated file and the generator untouched.
    Record the inspect decision (changed generator vs left untouched, with the reason) in the plan SUMMARY.
    Then run the FULL `mix ci.security` lane as the closing acceptance to prove the doc-gate survived all Task 1/2 edits AND that no crypto/corpus suite regressed and the generated-evidence drift-check passes.
  </action>
  <verify>
    <automated>mix relyra.security_review --check && mix ci.security</automated>
  </verify>
  <acceptance_criteria>
    - `mix relyra.security_review --check` exits 0 (no generated-evidence drift — whether untouched or regenerated)
    - SECURITY_REVIEW_EVIDENCE.md was NOT hand-edited (any change, if needed, came via the generator and shows the generator file in git diff)
    - `mix ci.security` exits 0 (full lane: doc-gate file-existence + cross-ref grep + generator drift-check + crypto/corpus suites all green)
    - the SUMMARY records the inspect-then-decide outcome (generator left untouched OR regenerated, with reason)
  </acceptance_criteria>
  <done>Generated evidence inspected and confirmed accurate (or fixed via generator + regenerated); full `mix ci.security` lane is green, proving the doc edits kept the gate intact with no crypto/corpus regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation-and-disclosure phase with NO `lib/` code change and no new runtime attack surface. The relevant boundary is the **honesty / doc-gate boundary**: published reviewer docs vs the actual code guarantee, and the `mix ci.security` doc-gate that enforces cross-references.

| Boundary | Description |
|----------|-------------|
| Reviewer docs → code reality | Hand-maintained prose must not overstate what `lib/relyra/security/*` actually does |
| Doc edits → `ci.security` doc-gate | Edits must keep file-existence + cross-ref grep + generated-evidence drift-check green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | docs re-introduce the verification overstatement | mitigate | Task 1 replaces vague "signature verification" with the named primitive (`:public_key.verify` over canonicalized `SignedInfo` vs configured cert + constant-time `DigestValue`); acceptance greps for `:public_key.verify` presence |
| T-31-02 | Repudiation | findings ledger fails to record the confirmed bypass / disposition | mitigate | Task 2 adds the Critical RELYRA-2026-001 row (D-04/D-05/D-06) with disposition + regression-proof citations; acceptance greps for the row + proof tests |
| T-31-03 | Denial of Service (build) | doc edit breaks the `ci.security` cross-ref grep (D-10) → red lane | mitigate | Task 1 preserves the `## Findings Ledger` heading + a `docs/security_findings.md` link; acceptance runs the exact gate grep; Task 3 runs the full lane |
| T-31-04 | Tampering | hand-editing the generated `SECURITY_REVIEW_EVIDENCE.md` causes `--check` drift (D-11) | mitigate | Task 3 is inspect-then-decide; any change goes through the generator + regenerate, never the file by hand; acceptance runs `relyra.security_review --check` |
| T-31-05 | Information Disclosure | premature publication / leaking a real GHSA/CVE id (D-09 fix-first) | mitigate | No `gh`/publish/CVE/merge in this plan; the ledger uses the internal `RELYRA-2026-001` placeholder, noting real ids attach at publication |
</threat_model>

<verification>
- `mix ci.security` exits 0 (full lane: doc-gate + crypto/corpus suites + generator drift-check).
- `mix cmd grep -nE "docs/security_findings.md|Findings Ledger" SECURITY_REVIEW.md` exits 0.
- `grep -q ":public_key.verify" docs/security_boundary.md SECURITY_REVIEW.md` finds the named primitive.
- `grep -q "RELYRA-2026-001" docs/security_findings.md` finds the Critical row; `grep -c "none yet" docs/security_findings.md` returns 0.
- `git diff --quiet SECURITY.md` and `git diff --quiet SECURITY_REVIEW_EVIDENCE.md` (unless evidence was regenerated via the generator) — no forbidden hand-edits.
</verification>

<success_criteria>
- The three named docs precisely describe the real post-fix crypto guarantee (no overstatement, brand-voice compliant) — ROADMAP success criterion 1.
- The findings ledger records one Critical RELYRA-2026-001 finding with a disposition (Confirmed → Fixed in v1.1 / hex 1.2.0) — ROADMAP success criterion 2.
- The `mix ci.security` doc checks stay green (full lane passes) — ROADMAP success criterion 1.
- No `lib/relyra/security/*`, `SECURITY.md`, generated `SECURITY_REVIEW_EVIDENCE.md` (by hand), or `CHANGELOG.md` (by hand) edits; no publication.
</success_criteria>

<output>
Create `.planning/phases/31-disclosure-and-docs-honesty/31-01-SUMMARY.md` when done. Record the Task 3 inspect-then-decide outcome (generator untouched vs regenerated, with reason).
</output>
