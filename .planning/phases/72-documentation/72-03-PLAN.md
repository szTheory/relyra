---
phase: 72-documentation
plan: "03"
type: execute
wave: 3
depends_on:
  - "72-02"
files_modified:
  - Makefile
  - test/docs/demo_guide_drift_test.exs
autonomous: true
gap_closure: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "Bounded gap-closure exception: the only launcher change is one public Make-first Keycloak/Fleet target plus deterministic launcher coverage; Compose topology, provisioning implementation, application routes, `lib/`, public Elixir APIs, security policy, and Hex package surface remain unchanged."
    - "`make keycloak` starts or reuses the shared proxy, launches `docker-compose.yml` plus `docker-compose.proxy.yml` with `--profile keycloak`, waits for `keycloak_provisioner`, validates the public realm descriptor through `keycloak.${RELYRA_HOST}`, and prints the established route map only after success."
    - "The public target fails nonzero when proxy startup, profile startup, provisioning, or public-route validation fails; it never prints successful routes after a failed prerequisite."
    - "An executable ExUnit fixture contract proves command ordering, environment-derived host handling, provisioning wait, public-route validation, and fail-closed behavior in the mandatory root test lane without requiring ambient Docker state."
  artifacts:
    - path: Makefile
      provides: "bounded public `keycloak` target and exact Fleet/profile/provisioning/public-route launcher contract"
      contains: "## keycloak:"
    - path: test/docs/demo_guide_drift_test.exs
      provides: "fixture-driven executable contract for the public Keycloak launcher"
      contains: "public Keycloak target launches provisions and validates the Fleet route"
  key_links:
    - from: Makefile
      to: docker-compose.proxy.yml
      via: "`KEYCLOAK_COMPOSE` composes the base and proxy files under the `keycloak` profile"
      pattern: "docker-compose\.proxy\.yml.*--profile keycloak"
    - from: Makefile
      to: docker-compose.proxy.yml#keycloak_provisioner
      via: "the target waits for the one-shot provisioner before advertising readiness"
      pattern: "wait keycloak_provisioner"
    - from: Makefile
      to: "http://keycloak.${RELYRA_HOST}/realms/demo-app/protocol/saml/descriptor"
      via: "bounded retry using curl `--noproxy` and `--resolve`, followed by exact public entityID validation"
      pattern: "keycloak.*realms/demo-app/protocol/saml/descriptor"
    - from: test/docs/demo_guide_drift_test.exs
      to: Makefile
      via: "fixture `docker` and `curl` executables run the real Make recipe and assert its recorded calls"
      pattern: "run_make.*keycloak"
  prohibitions:
    - requirement_id: DOC-01
      category: safety
      status: kept
      statement: "MUST NOT modify Compose service definitions, Keycloak provisioning code, application routes, `lib/`, SAML verification policy, configured-certificate trust, replay behavior, public APIs, `mix.exs`, or the Hex package whitelist; the verifier-authorized exception is limited to the Make target and its launcher contract."
    - requirement_id: DOC-01
      category: safety
      status: kept
      statement: "MUST NOT accept a failed provisioner or unreachable/mismatched public descriptor as launcher success, and MUST NOT expose credentials, SAML payloads, cookies, or authorization headers in command output."
    - requirement_id: DOC-01
      category: values
      status: kept
      statement: "MUST NOT turn optional Keycloak into a prerequisite for the deterministic Solo/FakeIdP journey."
  flagged_assumptions:
    - requirement_id: DOC-01
      category: concurrency
      status: unresolved
      verification: null
      resolution: null
      probe: "If interrupted or run in parallel, what is guaranteed?"
      statement: "No SPEC supplies a stronger concurrency contract. This plan assumes exclusive Wave-3 ownership of `Makefile` and `test/docs/demo_guide_drift_test.exs`; atomic task commits and rerunnable fixture tests are the recovery boundary, and the executor must stop on overlapping edits rather than merge launcher semantics implicitly."
    - requirement_id: DOC-02
      category: unclassified
      status: unresolved
      verification: null
      resolution: null
      probe: "unclassified — review manually"
      statement: "No SPEC classifies an additional DOC-02 edge. This plan does not auto-dismiss it; Wave 4 carries it into the documentation correction and deterministic router contract."
---

<objective>
Close the executable half of the Fleet/Keycloak documentation gap with one production-quality public launcher tracer (DOC-01).

Purpose: The verifier found that the documented public Keycloak origin has no Make command capable of activating its proxy overlay and Compose profile. This plan takes one visible, bounded exception to the original documentation-only boundary: add only the minimum `make keycloak` command and fixture coverage needed to make the documented follow-on true. Do not broaden the exception into Compose, provisioning, route, library, API, or security redesign.
Output: A fail-closed `make keycloak` surface and deterministic executable launcher evidence in the existing mandatory ExUnit lane.
</objective>

<api_coverage_decision>
The deterministic external-API detector is false for this scope: this is a repository-local Make/Compose/docs correction over already-integrated Docker and Keycloak services, not a new external API, SDK, or service integration. Do not create `COVERAGE.md` unless a rerun over the actual phase scope returns `detected=true`.
</api_coverage_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-documentation/72-CONTEXT.md
@.planning/phases/72-documentation/72-RESEARCH.md
@.planning/phases/72-documentation/72-PATTERNS.md
@.planning/phases/72-documentation/72-VALIDATION.md
@.planning/phases/72-documentation/72-VERIFICATION.md
@.planning/phases/72-documentation/72-02-SUMMARY.md
@.planning/phases/71-launcher-dx-banner/71-CONTEXT.md
@.planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
</context>

## Artifacts this phase produces

- New public Make target and CLI surface: `keycloak` / `make keycloak` in `Makefile`.
- New Make symbol: `KEYCLOAK_COMPOSE := $(FLEET_COMPOSE) --profile keycloak`.
- New test-only Make tuning symbols if needed to keep failure fixtures fast: `KEYCLOAK_ROUTE_ATTEMPTS` and `KEYCLOAK_ROUTE_SLEEP`, with production defaults preserving a bounded readiness wait.
- New ExUnit test `public Keycloak target launches provisions and validates the Fleet route` in `test/docs/demo_guide_drift_test.exs`.
- New ExUnit test `public Keycloak target fails closed before advertising routes` in `test/docs/demo_guide_drift_test.exs`.
- No new file, Elixir function, Compose service, application route, package, schema, or public library symbol is created.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ship the public Keycloak Fleet launcher from Make through a validated public descriptor</name>
  <reversibility rating="reversible">The isolated Make target and fixture assertions can be removed locally without a migration, published API break, Compose redesign, or security-policy change.</reversibility>
  <precondition>`docker-compose.yml`, `docker-compose.proxy.yml`, `docker/traefik/compose.yml`, and the `keycloak_provisioner` service still exist with the Phase 70 profile and public-host contract.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/72-documentation/72-VERIFICATION.md
    - .planning/phases/72-documentation/72-CONTEXT.md
    - .planning/phases/71-launcher-dx-banner/71-CONTEXT.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
    - Makefile
    - docker-compose.yml
    - docker-compose.proxy.yml
    - docker/traefik/compose.yml
    - scripts/test_keycloak_proxy_e2e.sh
    - .github/workflows/keycloak-proxy-e2e.yml
    - test/docs/demo_guide_drift_test.exs
  </read_first>
  <behavior>
    - "`make keycloak` invokes the existing idempotent proxy startup before the Keycloak/Fleet Compose command."
    - "The Compose command is exactly the base file plus proxy overlay plus `--profile keycloak`; it starts detached with build enabled and does not use the Solo Compose variable."
    - "The recipe waits for `keycloak_provisioner` to exit successfully before probing the browser-facing route."
    - "The public probe uses `curl --noproxy \"*\" --resolve keycloak.${RELYRA_HOST}:80:127.0.0.1` against `/realms/demo-app/protocol/saml/descriptor`, retries for a bounded interval, and requires the descriptor entityID to equal `http://keycloak.${RELYRA_HOST}/realms/demo-app`."
    - "A startup, provisioning, exhausted probe, or entityID mismatch returns nonzero and suppresses the route banner; only the fully validated path delegates to `make url`."
    - "Fixture tests execute the real Make recipe with owned stub executables, assert exact call ordering and host override propagation, and cover both success and fail-closed paths without Docker or browser state."
  </behavior>
  <files>Makefile, test/docs/demo_guide_drift_test.exs</files>
  <action>
    Start RED in `test/docs/demo_guide_drift_test.exs`. Extend `@public_targets` with `keycloak` and add the two named tests above. Use the existing `fixture_bin!/0`, `write_executable!/3`, `link_tool!/2`, `run_make/2`, `target_body/2`, and `assert_in_order/2` seams; do not build a second parser or invoke ambient Docker. The success fixture must execute `make keycloak`, log every `docker` and `curl` call, return a descriptor containing the overridden public entityID, and assert this order: network inspection/creation and Traefik start, base-plus-proxy `--profile keycloak` detached build, `wait keycloak_provisioner`, public descriptor probe with loopback resolution, then the route banner. Failure fixtures must make the provisioner wait fail and make the public probe exhaust its bounded attempts; both must return nonzero, and the provisioning failure must prove no curl call occurs while both prove the banner is absent.

    Then implement only the verifier-authorized launcher exception in `Makefile`. Add `keycloak` to `.PHONY` and the help-derived public inventory. Define `KEYCLOAK_COMPOSE` from the existing `FLEET_COMPOSE` plus `--profile keycloak`. The target must reuse `make proxy`, run the Keycloak Compose graph detached with `--build`, wait on `keycloak_provisioner`, and execute a bounded public descriptor probe using the current `RELYRA_HOST` with `--noproxy \"*\"` and `--resolve ...:80:127.0.0.1`. Validate the exact public entityID before calling `make url`. If retry tuning variables are added for fixture speed, make their normal defaults nonzero and production-safe, and keep them out of the documented public-target inventory. Preserve quoted environment-derived values as shell data per Phase 71. Do not edit Compose YAML, provisioning code, scripts, workflows, routes, `lib/`, or package configuration.
  </action>
  <acceptance_criteria>
    - "`make help` lists `keycloak` exactly once, and the public-target inventory test recognizes no undocumented target drift."
    - "The success fixture proves `make keycloak` activates `docker-compose.proxy.yml` with `--profile keycloak`, waits for `keycloak_provisioner`, validates the public descriptor entityID through loopback-resolved `keycloak.${RELYRA_HOST}`, and prints routes afterward."
    - "Provisioning and public-route failures are nonzero and cannot emit `==> Browser origins`."
    - "The automated changed-file allowlist permits only `Makefile` and `test/docs/demo_guide_drift_test.exs` outside workflow-owned `.planning/`/`.gsd/` artifacts; it rejects tracked, staged, or untracked changes anywhere else, including Compose/provisioning/E2E/security/API/package paths, root `lib/`, `demo/ledger_loop/lib/`, `demo/ledger_loop/test/browser/`, and the demo Mix/package/runtime surfaces."
    - "The focused ExUnit tests run in the existing root `mix test --warnings-as-errors` mandatory CI path; the existing owned Keycloak proxy E2E remains the live topology canary and is not weakened or replaced."
  </acceptance_criteria>
  <verify>
    <automated>mix test test/docs/demo_guide_drift_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted &amp;&amp; changed=$( { git diff --name-only; git diff --cached --name-only; git ls-files --others --exclude-standard; } | sort -u | grep -Ev '^(\.planning/.*|\.gsd/.*|Makefile|test/docs/demo_guide_drift_test\.exs)$' || true ) &amp;&amp; test -z "$changed"</automated>
  </verify>
  <done>The public Make-first Keycloak/Fleet command is production-quality, executable, fail-closed, fixture-proven, and bounded to the explicit verification exception.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Make CLI → host shell/Docker daemon | Environment-derived host/network values and launcher failures cross into Docker and curl commands. |
| Docker profile → one-shot provisioner | Readiness is not success until the existing provisioner exits zero. |
| loopback curl → browser-facing Traefik host | A reachable port is insufficient; the returned public descriptor must bind to the requested public entityID. |

## STRIDE Threat Register — ASVS L1, block on high

Unresolved critical/high-severity threats block execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-07 | Tampering / Elevation of privilege | `make keycloak` profile selection | high | mitigate | Use the literal base-plus-proxy Compose graph with `--profile keycloak`; fixture assertions reject fallback to Solo Compose or omission of either layer. |
| T-72-08 | Spoofing / Repudiation | provisioning and public-route success claim | high | mitigate | Require zero exit from `wait keycloak_provisioner`, then fetch the public descriptor through `--resolve` and match its exact public entityID before printing routes. |
| T-72-09 | Denial of service | public readiness retry | medium | mitigate | Bound attempts and sleep duration, propagate exhaustion nonzero, and provide test-only tuning without changing production defaults. |
| T-72-10 | Information disclosure | launcher output | medium | mitigate | Probe only the public metadata descriptor, emit no credentials or SAML/session material, and retain the existing credential-bearing E2E redaction policy untouched. |
</threat_model>

<verification>
- Focused tracer: `mix test test/docs/demo_guide_drift_test.exs --warnings-as-errors`.
- Mandatory deterministic lane: `mix test --warnings-as-errors` and `mix qa` include the fixture-driven ExUnit launcher contract.
- Existing live canary remains `.github/workflows/keycloak-proxy-e2e.yml` → `npm run demo:keycloak-proxy`; this plan does not weaken or replace it.
- Phase gate after all gap plans: `mix qa`, `mix ci.security`, `mix format --check-formatted`, and `mix test --warnings-as-errors` must exit 0. Preserve `mix ci.security` as separate `cmd mix test` processes and do not weaken the adversarial crypto corpus.
- Exact scope gate combines unstaged, staged, and untracked paths, ignores only workflow-owned `.planning/`/`.gsd/` artifacts, and allows only `Makefile` plus `test/docs/demo_guide_drift_test.exs`; therefore changes under Compose, provisioning, scripts, workflows, E2E/security/API/package surfaces, root `lib/`, `demo/ledger_loop/lib/`, `demo/ledger_loop/test/browser/`, or demo Mix/package/runtime files fail deterministically.
- No manual validation, UAT artifact, browser-host assumption, or human checkpoint is acceptance evidence.
</verification>

<success_criteria>
- DOC-01 has a real public command behind the Keycloak/Fleet claim.
- The target cannot succeed before profile activation, provisioning completion, and exact public-route validation.
- The bounded exception is visible and remains limited to `Makefile` plus its deterministic launcher test.
- Both unresolved spec-less fallback assumptions and all kept safety/values prohibitions remain explicit.
</success_criteria>

<source_coverage_audit>

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Guide claims correspond to a runnable Make-first Fleet/Keycloak path | 72-03 | COVERED | Public tracer closes the executable disconnection. |
| REQ | DOC-01 | Accurate Fleet/Keycloak command and URL-map guidance | 72-03 | COVERED | Target exists before Wave-4 prose names it. |
| RESEARCH | — | Makefile is the operational source; Keycloak stays optional and proxy-only; deterministic acceptance uses owned fixtures/canary | 72-03 | COVERED | Existing seams, no new dependency. |
| CONTEXT | D-01 | Solo/FakeIdP remains complete and independent | 72-03 | COVERED | New target is optional and does not alter Solo targets. |
| CONTEXT | D-02 | Fleet/Keycloak is a follow-on proof | 72-03 | COVERED | `keycloak` target is explicitly optional. |
| CONTEXT | D-03 | Makefile is the canonical interface | 72-03 | COVERED | Adds one public target at the canonical seam. |
| CONTEXT | D-04 | Browser/public and service-DNS boundaries remain exact | 72-03 | COVERED | Public descriptor is loopback-resolved through Traefik; provisioning stays service-DNS-owned. |
| CONTEXT | D-05 | Cache model remains unchanged | 72-01 | COVERED | Existing executed plan; no gap change required. |
| CONTEXT | D-06 | Recovery semantics remain unchanged | 72-01 | COVERED | Existing executed plan; no gap change required. |
| CONTEXT | D-07 | Receipt ownership remains unchanged | 72-04 | COVERED | Documentation correction follows in Wave 4. |
| CONTEXT | D-08 | House-voice proof remains deterministic | 72-04 | COVERED | Documentation correction follows in Wave 4. |
| CONTEXT | D-09 | Routers remain coherent | 72-04 | COVERED | Documentation correction follows in Wave 4. |

The original context exclusion of launcher changes is superseded only for this verifier-required minimum public target and deterministic contract because no later phase owns the gap. Deferred TLS, hashed hosts, production Dockerfile, application/security/API work, and Compose redesign remain excluded.
</source_coverage_audit>

<output>
Create `.planning/phases/72-documentation/72-03-SUMMARY.md` when done.
</output>
