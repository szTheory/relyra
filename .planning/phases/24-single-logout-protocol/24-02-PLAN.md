---
phase: 24
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/protocol/logout_request.ex
autonomous: true
requirements:
  - SLO-01
must_haves:
  truths:
    - System generates a valid SAML LogoutRequest payload to initiate a user logout.
  artifacts:
    - path: lib/relyra/protocol/logout_request.ex
      provides: `<samlp:LogoutRequest>` builder
  key_links:
    - from: lib/relyra/protocol/logout_request.ex
      to: Relyra.Protocol bindings
      via: generation of `xml` payload
---

<objective>
Implement the `Relyra.Protocol.LogoutRequest` builder to generate SAML LogoutRequests.

Purpose: To initiate a Single Logout flow, Relyra must construct valid `<samlp:LogoutRequest>` messages.
Output: Working, tested LogoutRequest protocol builder.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-single-logout-protocol/RESEARCH.md
@.planning/phases/24-single-logout-protocol/PATTERNS.md
@lib/relyra/protocol/authn_request.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LogoutRequest Builder</name>
  <files>lib/relyra/protocol/logout_request.ex, test/relyra/protocol/logout_request_test.exs</files>
  <behavior>
    The `build` function should generate a map containing required fields for a LogoutRequest, and `to_xml` should serialize it to a SAML XML string.
    
    Cases:
    - `build/3` with valid connection and subject -> Returns `{:ok, map}` with id, issue_instant, destination, issuer, name_id, and session_index (optional).
    - `build/3` missing connection issuer -> Returns `{:error, error}`.
    - `to_xml/1` with valid map -> Returns binary string containing `<samlp:LogoutRequest ...><saml:Issuer>...</saml:Issuer><saml:NameID>...</saml:NameID>...</samlp:LogoutRequest>`.
  </behavior>
  <action>
    Create `lib/relyra/protocol/logout_request.ex` following the `Relyra.Protocol.AuthnRequest` pattern.
    Provide `build(connection, subject, opts \\ [])`. The `subject` must contain a `:name_id` and an optional `:session_index`.
    Provide `to_xml(map)` which generates the XML string.
  </action>
  <verify>
    <automated>mix test test/relyra/protocol/logout_request_test.exs --warnings-as-errors</automated>
  </verify>
  <done>LogoutRequest builder generates valid XML maps from connection and subject.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relyra → Network | Creating outgoing LogoutRequests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02 | Spoofing | LogoutRequest | mitigate | Ensure ID uses a cryptographically secure random value via `:crypto.strong_rand_bytes(16)`. |
</threat_model>

<verification>
- `mix test test/relyra/protocol/logout_request_test.exs` passes.
</verification>

<success_criteria>
LogoutRequest builder generates valid XML with required attributes.
</success_criteria>

<output>
After completion, create `.planning/phases/24-single-logout-protocol/24-02-SUMMARY.md`
</output>