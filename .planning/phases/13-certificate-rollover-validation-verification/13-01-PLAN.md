---
phase: 13-certificate-rollover-validation-verification
plan: 13-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md
autonomous: true
requirements: [CFG-04]
must_haves:
  truths:
    - "Phase 10 validation truth no longer claims missing Wave 0 rollover coverage that already exists."
    - "The serial-only rollover proof surface is explicit before verification evidence is recorded."
    - "Phase 13 can point at one current validation contract for CFG-04 rather than stale partial Nyquist metadata."
  artifacts:
    - path: ".planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md"
      provides: "Current Wave 0 status, per-task verification map, and manual-only checks for CFG-04 closure"
  key_links:
    - from: ".planning/phases/10-certificate-inventory-rollover/10-03-SUMMARY.md"
      to: ".planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md"
      via: "implemented rollover proof surface reflected in validation truth per D-02 and D-04"
      pattern: "certificate_inventory_transition_test|certificate_inventory_concurrency_test|ecto_connection_resolver_test"
    - from: ".planning/phases/13-certificate-rollover-validation-verification/13-VALIDATION.md"
      to: ".planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md"
      via: "compact serial validation posture reused for CFG-04 closure per D-01 through D-04"
      pattern: "wave_0_complete: true|serial"
---

<objective>
Sync Phase 10 validation truth to the current rollover proof surface before any verification packet is captured.

Purpose: implement D-02, D-04, and D-12 by replacing stale partial-Nyquist metadata with the actual serial rollover validation contract that exists in the repo today.
Output: updated `10-VALIDATION.md` reflecting completed Wave 0 rollover coverage, the exact serial proof command, and the narrow manual semantics checks that Phase 13 will sign off.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.2-MILESTONE-AUDIT.md
@.planning/phases/10-certificate-inventory-rollover/10-CONTEXT.md
@.planning/phases/10-certificate-inventory-rollover/10-RESEARCH.md
@.planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md
@.planning/phases/10-certificate-inventory-rollover/10-03-SUMMARY.md
@.planning/phases/13-certificate-rollover-validation-verification/13-CONTEXT.md
@.planning/phases/13-certificate-rollover-validation-verification/13-VALIDATION.md
@.planning/phases/13-certificate-rollover-validation-verification/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Phase 10 validation truth to match the shipped rollover proof surface</name>
  <files>.planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md</files>
  <action>Update `10-VALIDATION.md` so it stops describing missing Wave 0 coverage and instead matches the current proof surface per D-02 and D-04. Set `wave_0_complete: true`; replace the stale quick-run command with one focused serial command covering `test/relyra/ecto/certificate_inventory_expiry_test.exs`, `test/relyra/ecto/certificate_inventory_transition_test.exs`, `test/relyra/ecto/certificate_inventory_concurrency_test.exs`, `test/relyra/ecto/ecto_connection_resolver_test.exs`, and `test/relyra/connection_snapshot_test.exs`; and update the per-task verification map so it references documentation-truth sync rather than missing-code creation. Reuse the compact table structure and Wave 0 completion language from `13-VALIDATION.md`, but keep the artifact anchored in Phase 10 and CFG-04. Preserve the serial-only posture explicitly, mark parallel evidence as invalid, and carry forward only the two locked manual semantics reviews from D-10. Do not introduce any new lifecycle implementation scope, re-open rollover semantics, or add broader milestone cleanup.</action>
  <verify>
    <automated>mix test test/relyra/ecto/certificate_inventory_expiry_test.exs test/relyra/ecto/certificate_inventory_transition_test.exs test/relyra/ecto/certificate_inventory_concurrency_test.exs test/relyra/ecto/ecto_connection_resolver_test.exs test/relyra/connection_snapshot_test.exs --warnings-as-errors && rg -n '^wave_0_complete: true$' .planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md && rg -nF '| **Quick run command** | `mix test test/relyra/ecto/certificate_inventory_expiry_test.exs test/relyra/ecto/certificate_inventory_transition_test.exs test/relyra/ecto/certificate_inventory_concurrency_test.exs test/relyra/ecto/ecto_connection_resolver_test.exs test/relyra/connection_snapshot_test.exs --warnings-as-errors` |' .planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md && rg -nF 'Wave 0 is already complete in the current repo state.' .planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md && rg -nF 'Phase 13 consumes that proof surface and closes the stale verification metadata around it.' .planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md && rg -nF 'The rollover API and typed conflict errors read as clear caller guidance for promote, retire, and rollback flows' .planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md && rg -nF 'Runtime trust still consumes only active signing certs while staged and retired rows remain durable inventory facts only' .planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md</automated>
  </verify>
  <done>`10-VALIDATION.md` no longer reports missing Wave 0 rollover coverage, names the exact serial proof surface for CFG-04, and states the two manual semantics checks without reopening implementation work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior implementation evidence -> validation truth | Existing shipped rollover behavior must be translated into current Nyquist metadata without drift |
| validation contract -> later verification artifact | The verification plan will trust the validation file to define what counts as acceptable CFG-04 evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | T | `10-VALIDATION.md` | mitigate | Rewrite the validation file from current shipped evidence only, using `10-03-SUMMARY.md`, `13-VALIDATION.md`, and the locked D-02 serial packet instead of stale placeholders. |
| T-13-01-02 | R | Phase 10 proof surface | mitigate | Name the exact five focused test files and keep the evidence contract compact so later verification can be reproduced verbatim. |
| T-13-01-03 | D | verification execution posture | mitigate | State directly in `10-VALIDATION.md` that parallel test evidence is invalid and that serial execution is required per D-04 and the milestone-audit race warning. |
</threat_model>

<verification>
Run the focused serial rollover command from the updated validation file and confirm the artifact’s proof surface matches the current repo state rather than stale Wave 0 gaps.
</verification>

<success_criteria>
Phase 10 validation metadata reflects the actual green rollover proof surface, preserves the serial-only rule, and gives Phase 13 a current validation contract to execute against.
</success_criteria>

<output>
After completion, create `.planning/phases/13-certificate-rollover-validation-verification/13-01-SUMMARY.md`
</output>
