---
phase: 48-operator-completeness-incident-playbook-trace-tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/overview.md
  - guides/getting_started.md
autonomous: true
requirements:
  - ADOPT-03
must_haves:
  truths:
    - "guides/overview.md Day-2 section links to incident playbook for login-trace / ops debugging."
    - "guides/getting_started.md §5 Useful follow-on references includes incident playbook link."
    - "mix ci.docs exits 0 after doc edits (no mix.exs change required per D-15)."
  artifacts:
    - path: "guides/overview.md"
      provides: "Day-2 hub cross-link to incident playbook evidence surfaces."
    - path: "guides/getting_started.md"
      provides: "§5 ops escalation path after first login."
---

<objective>
Wire ADOPT-03 cross-doc navigation: Day-2 hubs point operators at the updated incident playbook login-trace documentation; verify `mix ci.docs` stays green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-CONTEXT.md
@.planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-RESEARCH.md
@.planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-03-PLAN.md
@guides/overview.md
@guides/getting_started.md
@guides/operations/incident_playbook.md
@mix.exs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Trace docs exist in playbook but adopters never find them from Day-2 hubs | high | Add overview Day-2 + getting_started §5 links (D-13, D-14) |
| Accidental ci.docs / mix.exs change | low | D-15: no new drift test; do not modify mix.exs unless presence gate missing (it exists) |
| Broken relative link path | medium | Use `operations/incident_playbook.md` from guides/ and `operations/incident_playbook.md` from getting_started |
</threat_model>

<tasks>

<task type="auto">
  <name>Add Day-2 login-trace bullet to guides/overview.md</name>
  <read_first>
    - guides/overview.md (## Day-2 — Operate in production section)
    - .planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-CONTEXT.md (D-13)
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-03-PLAN.md (Day-2 link pattern)
  </read_first>
  <action>
    In `guides/overview.md` under **## Day-2 — Operate in production**, add a bullet after Production Ecto path (or after Troubleshooting — operator workflow order):

    ```markdown
    - [Incident playbook — login trace & evidence surfaces](operations/incident_playbook.md#evidence-surfaces) — per-login step timeline (`mix relyra.trace` / LiveView) for Diagnose steps in SAML incidents
    ```

    Do not remove or reorder existing bullets except to insert this line. Keep Reference section incident playbook link unchanged.
  </action>
  <verify>grep "incident_playbook.md#evidence-surfaces" guides/overview.md</verify>
  <acceptance_criteria>
    - `grep "operations/incident_playbook.md#evidence-surfaces" guides/overview.md` exits 0.
    - `grep "mix relyra.trace" guides/overview.md` exits 0.
    - `grep "## Day-2" guides/overview.md` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Add incident playbook to Getting Started §5 follow-ons</name>
  <read_first>
    - guides/getting_started.md (§5 Useful follow-on references)
    - .planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-CONTEXT.md (D-14)
    - guides/operations/incident_playbook.md (target exists)
  </read_first>
  <action>
    In `guides/getting_started.md` §5 **Useful follow-on references**, add a bullet (after Production Ecto path or before identity_mapping — match Phase 47 hub pattern):

    ```markdown
    - [Incident playbook](operations/incident_playbook.md) — evidence surfaces, `mix relyra.trace`, and scenario runbooks for Day-2 SAML incidents (login-trace LiveView at `/relyra/admin/connections/:connection_id/trace`).
    ```

    Optional one sentence in §5 intro: after first successful login, bookmark the incident playbook for on-call Diagnose workflows.

    Do not modify §1–§4 or appendix content.
  </action>
  <verify>grep "operations/incident_playbook.md" guides/getting_started.md</verify>
  <acceptance_criteria>
    - `grep "operations/incident_playbook.md" guides/getting_started.md` exits 0.
    - `grep "mix relyra.trace" guides/getting_started.md` exits 0.
    - `grep "connections/:connection_id/trace" guides/getting_started.md` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify mix ci.docs green</name>
  <read_first>
    - mix.exs (ci.docs alias lines 168–184)
    - .planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-CONTEXT.md (D-15, D-16)
  </read_first>
  <action>
    Run full docs CI gate:

    ```bash
    mix ci.docs
    ```

    Do **not** add `test/docs/incident_playbook_drift_test.exs` or modify `mix.exs` ci.docs steps unless `mix ci.docs` fails for a reason other than markdown edits (expected: pass with presence guard only).

    Do **not** move `test/security/login_trace_test.exs` into ci.docs.
  </action>
  <verify>mix ci.docs</verify>
  <acceptance_criteria>
    - `mix ci.docs` exits 0.
    - `grep "incident_playbook_drift" mix.exs` exits 1 (no new drift test added).
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
grep "operations/incident_playbook" guides/overview.md guides/getting_started.md
mix ci.docs
```
</verification>
