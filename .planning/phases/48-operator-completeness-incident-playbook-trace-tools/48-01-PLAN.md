---
phase: 48-operator-completeness-incident-playbook-trace-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/operations/incident_playbook.md
autonomous: true
requirements:
  - ADOPT-03
must_haves:
  truths:
    - "Evidence surfaces table has a sixth row for login trace (LiveView + CLI) with code anchors."
    - "LiveView routes table documents /relyra/admin/connections/:connection_id/trace."
    - "Mix tasks table includes mix relyra.trace with --repo, --connection, --last 20 default."
    - "Playbook copy says 8 mix relyra.* hand-tools (not 7) everywhere counts appear."
    - "Scenario 3 Diagnose no longer claims no LiveView for replay (v1.4 stale text removed)."
    - "Scenarios 3, 4, 5, 6 Diagnose steps reference login trace with scenario-specific step/error_code guidance."
    - "When in doubt distinguishes mix relyra.diagnostic (bundle) from login trace (per-attempt step timeline)."
  artifacts:
    - path: "guides/operations/incident_playbook.md"
      provides: "Operator centerpiece tables + scenario wiring for ADOPT-03 trace discoverability."
---

<objective>
Update `guides/operations/incident_playbook.md` so operators running the incident playbook know when to open the login-trace LiveView or run `mix relyra.trace` — without reading source or v1.5 release notes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-CONTEXT.md
@.planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-RESEARCH.md
@guides/operations/incident_playbook.md
@lib/relyra/live_admin/router.ex
@lib/relyra/live_admin/connection_trace_live.ex
@lib/mix/tasks/relyra.trace.ex
@lib/relyra/live_admin/query.ex
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Operators conflate diagnostic bundle with login trace | high | When in doubt rewrite: diagnostic = redacted bundle; trace = step timeline with error_code |
| Stale Scenario 3 text denies shipped LiveView | high | Remove v1.4 "no admin LiveView" sentence; point to trace replay.check step |
| ROADMAP shorthand omits required --repo | medium | Document full CLI: `mix relyra.trace --repo MyApp.Repo --connection ID [--last N]` |
| Audit ledger confusion for replay storms | medium | Retain explicit note: replays write no audit rows; trace shows replay.check outcomes |
| Wrong hand-tool count after adding trace | low | Update 7→8 in intro Host owns bullet, Mix tasks section header, and Evidence surfaces Mix row |
</threat_model>

<tasks>

<task type="auto">
  <name>Update intro counts and Evidence surfaces sixth row</name>
  <read_first>
    - guides/operations/incident_playbook.md (lines 1–54)
    - .planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-CONTEXT.md (D-01, D-04)
    - lib/relyra/live_admin/connection_trace_live.ex (@step_labels)
    - lib/mix/tasks/relyra.trace.ex (@moduledoc)
  </read_first>
  <action>
    1. In the opening paragraph (line ~8), change **"five evidence surfaces"** to **"six evidence surfaces"** and append **login trace** to the parenthetical list after troubleshooting decoder (or equivalent clear naming).

    2. In **## Relyra owns** bullet "Shipping the 7 `mix relyra.*` operator hand-tools" → **8** hand-tools.

    3. In **## Evidence surfaces** table, add a **sixth row** after the Troubleshooting decoder row:

    | Login trace | Per-login-attempt step timeline (`response.decode` → `response.validate` → `signature.verify` → `replay.check` → `user.map` → `session.establish`) with `:error_code` on failed stages; browser UI or headless CLI | `lib/relyra/live_admin/connection_trace_live.ex`, `lib/mix/tasks/relyra.trace.ex`, `lib/relyra/live_admin/query.ex` |

    4. In the **Mix tasks** row of the same table (row 4 today), update the "What it tells you" cell from "7 operator hand-tools" to **8 operator hand-tools** and mention login-trace inspection among purposes.

    Optional (planner discretion): add a short **### Login trace vs audit ledger** subsection after the Evidence surfaces table clarifying `domain: :login` trace rows vs trust-mutation audit vocabulary — replays appear in trace/telemetry only.
  </action>
  <verify>grep -E "six evidence surfaces|Login trace" guides/operations/incident_playbook.md</verify>
  <acceptance_criteria>
    - `grep "six evidence surfaces" guides/operations/incident_playbook.md` exits 0.
    - `grep "| Login trace |" guides/operations/incident_playbook.md` exits 0.
    - `grep "session.establish" guides/operations/incident_playbook.md` exits 0.
    - `grep "8 \`mix relyra" guides/operations/incident_playbook.md` exits 0 (intro Host owns bullet).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Extend LiveView routes and Mix tasks tables</name>
  <read_first>
    - guides/operations/incident_playbook.md (LiveView admin routes + Mix tasks sections)
    - lib/relyra/live_admin/router.ex (trace route)
    - lib/mix/tasks/relyra.trace.ex (flags and defaults)
    - .planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-CONTEXT.md (D-02, D-03, D-10, D-11)
  </read_first>
  <action>
  **LiveView admin routes table** — insert before the diagnostic bundle row (or after connection edit — keep logical flow):

    | `/relyra/admin/connections/:connection_id/trace` | `Relyra.LiveAdmin.ConnectionTraceLive` `:trace` |

    Add one sentence after the table: operators reach this page from connection detail **View Login Trace** (`lib/relyra/live_admin/components/connection_detail.ex`); path prefix remains configurable via host mount, suffix shape fixed.

  **Mix tasks section** — change header sentence from "These are the 7 Relyra operator hand-tools" to **8**.

    Add table row (alphabetical or before security_review — match existing ordering):

    | `mix relyra.trace` | Print redacted per-login step timelines for a connection (headless; same data as LiveView trace). Requires `--repo` and `--connection`; optional `--last` (default **20**). |

    Add fenced example immediately after the table (or in row footnote):

    ```bash
    mix relyra.trace --repo MyApp.Repo --connection CONNECTION_ID
    mix relyra.trace --repo MyApp.Repo --connection CONNECTION_ID --last 5
    ```

    One-line when-to-use: **LiveView** for browser triage during an incident; **CLI** for SSH/headless on-call without a browser. Both call `Query.get_login_traces/4` with shared redaction (`Relyra.LoginTrace.Export`).
  </action>
  <verify>grep -E "connections/:connection_id/trace|mix relyra.trace --repo" guides/operations/incident_playbook.md</verify>
  <acceptance_criteria>
    - `grep "connections/:connection_id/trace" guides/operations/incident_playbook.md` exits 0.
    - `grep "ConnectionTraceLive" guides/operations/incident_playbook.md` exits 0.
    - `grep "mix relyra.trace" guides/operations/incident_playbook.md` exits 0.
    - `grep "\-\-repo MyApp.Repo" guides/operations/incident_playbook.md` exits 0.
    - `grep "default **20**" guides/operations/incident_playbook.md` exits 0 OR `grep "default 20" guides/operations/incident_playbook.md` exits 0.
    - `grep "These are the 8 Relyra" guides/operations/incident_playbook.md` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Wire login trace into Scenarios 3–6 Diagnose steps</name>
  <read_first>
    - guides/operations/incident_playbook.md (Scenarios 3–6)
    - .planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-CONTEXT.md (D-05, D-06, D-07, D-08, D-09)
    - lib/relyra/live_admin/connection_trace_live.ex (@step_labels)
  </read_first>
  <action>
    **Scenario 3 (Replay storm) — Diagnose step 2:** DELETE the sentence "there is no admin LiveView surface for replay activity in v1.4" and the following sentence about volume from host logs only as the sole path. REPLACE with:

    - Open `/relyra/admin/connections/:connection_id/trace` (or `mix relyra.trace --repo MyApp.Repo --connection CONNECTION_ID`) and inspect **`replay.check`** step outcomes and volume across recent attempts.
    - Retain: replays do not write audit rows; trace + telemetry are the evidence surfaces for replay storms.
    - Host log infrastructure still useful for source-IP grouping — keep as secondary, not primary.

    **Scenario 4 (Signature regression) — Diagnose step 2:** After opening connection show, add: open login trace and inspect **`signature.verify`** step for exact `:error_code` (`:digest_mismatch`, `:invalid_signature`, `:trust_anchor_mismatch`) before/alongside cert inventory and `mix relyra.diagnostic`.

    **Scenario 5 (ACS misconfiguration) — Diagnose step 2:** Add: login trace **`response.validate`** step shows which field failed (`:destination_mismatch`, `:recipient_mismatch`, `:in_response_to_mismatch`) — use before or alongside connection edit field verification.

    **Scenario 6 (Attribute mapping) — Diagnose step 2:** Add: when mapping-stage telemetry is insufficient, login trace **`user.map`** step shows mapper-stage outcome and error detail.

    Each scenario must name at least one of: LiveView trace path, `mix relyra.trace`, or "login trace" explicitly in the Diagnose numbered step.
  </action>
  <verify>grep "v1.4" guides/operations/incident_playbook.md; grep -c "login trace\|Login trace\|mix relyra.trace" guides/operations/incident_playbook.md</verify>
  <acceptance_criteria>
    - `grep "v1.4" guides/operations/incident_playbook.md` exits 1 (no stale v1.4 replay LiveView denial).
    - `grep "replay.check" guides/operations/incident_playbook.md` exits 0 in Scenario 3 context.
    - `grep "signature.verify" guides/operations/incident_playbook.md` exits 0.
    - `grep "response.validate" guides/operations/incident_playbook.md` exits 0.
    - `grep "user.map" guides/operations/incident_playbook.md` exits 0.
    - `grep -c "login trace\|Login trace\|mix relyra.trace" guides/operations/incident_playbook.md` outputs integer ≥ 6.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Rewrite When in doubt — diagnostic vs login trace</name>
  <read_first>
    - guides/operations/incident_playbook.md (## When in doubt)
    - .planning/phases/48-operator-completeness-incident-playbook-trace-tools/48-CONTEXT.md (D-12)
    - lib/mix/tasks/relyra.diagnostic.ex (purpose — read @moduledoc only)
  </read_first>
  <action>
    Replace the opening sentence "when in doubt, the diagnostic bundle is the trace" with explicit split:

    - **`mix relyra.diagnostic`** — redacted diagnostic **bundle** for IdP/vendor handoff, security review, or Relyra issue filing (connection state + recent audit + failing response snapshot). Safe to share externally after redaction.
    - **Login trace** (LiveView `/connections/:connection_id/trace` or `mix relyra.trace`) — per-attempt **step timeline** answering "which pipeline stage failed and with what `:error_code`?" Use during active triage before escalating with a bundle.

    Keep existing routing guidance (IdP support vs security queue vs Relyra issue) attached to **diagnostic**, not login trace.

    Do not use the word "trace" alone to mean diagnostic bundle anywhere in this section.
  </action>
  <verify>grep -A5 "## When in doubt" guides/operations/incident_playbook.md | head -20</verify>
  <acceptance_criteria>
    - `grep "diagnostic bundle is the trace" guides/operations/incident_playbook.md` exits 1.
    - `grep "mix relyra.diagnostic" guides/operations/incident_playbook.md` exits 0 in When in doubt section.
    - `grep "step timeline" guides/operations/incident_playbook.md` exits 0.
    - `grep "mix relyra.trace" guides/operations/incident_playbook.md` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks:
```bash
grep -E "six evidence|Login trace|connections/:connection_id/trace|mix relyra.trace|8 .mix relyra" guides/operations/incident_playbook.md
! grep "v1.4" guides/operations/incident_playbook.md
! grep "diagnostic bundle is the trace" guides/operations/incident_playbook.md
```
</verification>
