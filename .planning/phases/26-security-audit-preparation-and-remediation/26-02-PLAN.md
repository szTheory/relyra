---
phase: 26
plan: 02
title: Generated security evidence and strict-default proof lane
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - lib/mix/tasks/relyra.security_review.ex
  - test/mix/tasks/relyra_security_review_test.exs
  - test/security/strict_default_proof_test.exs
  - test/relyra/ecto/escape_hatch_audit_test.exs
  - SECURITY_REVIEW_EVIDENCE.md
autonomous: true
requirements:
  - SEC-REVIEW-01
must_haves:
  truths:
    - "Strict defaults and risky escape hatches are proved by rerunnable tests and a generated artifact rather than prose-only claims."
    - "The generated reviewer evidence artifact fails drift checks when executable security state changes."
  artifacts:
    - path: lib/mix/tasks/relyra.security_review.ex
      provides: Generated security-review evidence task with write and drift-check modes
    - path: SECURITY_REVIEW_EVIDENCE.md
      provides: Executable-state-derived strict-default and escape-hatch evidence artifact
    - path: test/security/strict_default_proof_test.exs
      provides: Targeted proof lane for fail-closed trust-boundary behavior
    - path: test/relyra/ecto/escape_hatch_audit_test.exs
      provides: Targeted proof that risky compatibility paths stay attributable, correlated, and redaction-safe
  key_links:
    - from: lib/mix/tasks/relyra.security_review.ex
      to: SECURITY_REVIEW_EVIDENCE.md
      via: deterministic generation and `--check` drift enforcement
    - from: test/security/strict_default_proof_test.exs
      to: lib/relyra/security/algorithm_policy.ex
      via: fail-closed default and override-expiry assertions
    - from: test/relyra/ecto/escape_hatch_audit_test.exs
      to: lib/relyra/ecto/audit_writer.ex
      via: redaction-safe, attributable escape-hatch audit evidence
---

<objective>
Build the executable core of the Phase 26 packet: one generated evidence artifact plus targeted proof tests that demonstrate strict defaults and constrained escape hatches across the SAML trust boundary.

Purpose: Reviewers should be able to rerun one task and a small proof lane to verify the repo’s most security-sensitive claims without trusting hand-maintained prose.
Output: `mix relyra.security_review`, task tests, `SECURITY_REVIEW_EVIDENCE.md`, and focused proof tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-security-audit-preparation-and-remediation/26-CONTEXT.md
@.planning/phases/26-security-audit-preparation-and-remediation/26-RESEARCH.md
@.planning/phases/26-security-audit-preparation-and-remediation/26-PATTERNS.md
@.planning/phases/25-conformance-and-cve-regression-fixtures/25-03-PLAN.md
@SECURITY_REVIEW.md
@SECURITY.md
@CONFORMANCE.md
@mix.exs
@lib/mix/tasks/relyra.conformance.ex
@lib/mix/tasks/relyra.diagnostic.ex
@lib/relyra/security/algorithm_policy.ex
@lib/relyra/metadata/auto_refresh.ex
@lib/relyra/ecto/audit_writer.ex
@lib/relyra/live_admin/query.ex
@test/security/signature_policy_test.exs
@test/relyra/ecto/audit_hardening_test.exs
@test/relyra/diagnostic_test.exs
@test/relyra/diagnostic/allow_list_test.exs

<interfaces>
From `lib/relyra/security/algorithm_policy.ex`:
```elixir
def default() do
  %__MODULE__{allowed_signature_methods: [...], allowed_digest_methods: [...], legacy_sha1: nil}
end

def enforce_signature_method(policy, method)
def enforce_digest_method(policy, method)
```

From `lib/relyra/metadata/auto_refresh.ex`:
```elixir
@spec refresh(map(), keyword()) :: {:ok, struct()} | {:error, Error.t()}
def refresh(source, opts)
```

From `lib/relyra/ecto/audit_writer.ex`:
```elixir
@spec append_event(module(), map()) :: {:ok, AuditEvent.t()} | {:error, Error.t()}
def append_event(repo, attrs)
```

From `lib/mix/tasks/relyra.conformance.ex`:
```elixir
def run(args) do
  Mix.Task.run("app.start")
  {opts, _argv, invalid} = OptionParser.parse(args, strict: [output: :string, check: :boolean], aliases: [o: :output])
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement generated security-review evidence with drift checking</name>
  <read_first>
@lib/mix/tasks/relyra.conformance.ex
@lib/mix/tasks/relyra.diagnostic.ex
@test/mix/tasks/relyra_conformance_test.exs
@SECURITY_REVIEW.md
@CONFORMANCE.md
@SECURITY.md
  </read_first>
  <files>lib/mix/tasks/relyra.security_review.ex, test/mix/tasks/relyra_security_review_test.exs, SECURITY_REVIEW_EVIDENCE.md</files>
  <behavior>
    - `mix relyra.security_review` writes one deterministic evidence artifact derived from executable security state.
    - `mix relyra.security_review --check` fails when the checked-in artifact no longer matches regenerated output.
    - The artifact links strict-default claims to seams, tests, rerun commands, and constrained escape hatches rather than narrating unsupported guarantees.
  </behavior>
  <action>
    Create `lib/mix/tasks/relyra.security_review.ex` per D-10, D-11, D-13, D-14, and D-19 using the same generated-markdown pattern as `mix relyra.conformance`. Default output should be `SECURITY_REVIEW_EVIDENCE.md`, with `--output` and `--check` behavior matching the existing drift-check pattern. Render a deterministic report from executable state already owned by the repo: `AlgorithmPolicy.default/0`, named strict-default seams and proof tests, metadata-refresh escape-hatch seams, audit/redaction seams, diagnostic bundle export seams, and the existing conformance/CVE evidence entry point. Keep the artifact executable-state-derived and link-oriented: each row or section should map claim -> seam -> proof command -> artifact, not become a second prose policy surface. Add `test/mix/tasks/relyra_security_review_test.exs` proving generation, missing-file failure for `--check`, and drift detection against a temp output path. Run the task once during execution and commit the generated `SECURITY_REVIEW_EVIDENCE.md`.
  </action>
  <acceptance_criteria>
    - `rg -n "defmodule Mix.Tasks.Relyra.SecurityReview|--check|SECURITY_REVIEW_EVIDENCE.md" lib/mix/tasks/relyra.security_review.ex` finds the generator and drift-check behavior.
    - `rg -n "drift|missing|temp_output_path|SECURITY_REVIEW_EVIDENCE.md" test/mix/tasks/relyra_security_review_test.exs` finds task-level generator coverage.
    - `rg -n "^# Security Review Evidence|strict default|escape hatch|mix ci.security|mix ci.verify" SECURITY_REVIEW_EVIDENCE.md` finds the generated artifact sections and rerun commands.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/relyra_security_review_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The repo can generate and drift-check a security-review evidence artifact from current executable security state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused proof tests for strict defaults and audited escape hatches</name>
  <read_first>
@test/security/signature_policy_test.exs
@test/relyra/metadata/auto_refresh_test.exs
@test/relyra/ecto/audit_hardening_test.exs
@test/relyra/diagnostic_test.exs
@test/relyra/diagnostic/allow_list_test.exs
@lib/relyra/security/algorithm_policy.ex
@lib/relyra/metadata/auto_refresh.ex
@lib/relyra/ecto/audit_writer.ex
@lib/relyra/live_admin/query.ex
  </read_first>
  <files>test/security/strict_default_proof_test.exs, test/relyra/ecto/escape_hatch_audit_test.exs</files>
  <behavior>
    - Strict-default proof explicitly shows fail-closed behavior for unsafe algorithms, signed-content trust rules, and replay/RelayState-style trust-boundary rejections.
    - Escape-hatch proof explicitly shows a metadata-bypass path through `Relyra.Metadata.AutoRefresh` remains time-boxed or otherwise constrained, attributable, correlated, and redaction-safe.
    - Proof tests stay narrow and rerunnable as one review lane without relying on future external findings.
  </behavior>
  <action>
    Create `test/security/strict_default_proof_test.exs` per D-12 and D-15 as a compact proof lane that exercises the repo’s primary fail-closed invariants directly: SHA-1 rejection by default, expired legacy override refusal, RelayState raw-URL rejection, and one mandatory signed-content trust rejection owned by the library seam. That signed-content proof must explicitly exercise document-provided `KeyInfo` trust rejection or another exact signed-node-selection refusal where the library rejects attacker-controlled signed-content context; do not satisfy this requirement with a generic missing-trust-anchor case alone. Then create `test/relyra/ecto/escape_hatch_audit_test.exs` per D-13, D-15, D-16, and D-18 using `Relyra.TestSupport.MigrationCase, async: false` so one migration-backed proof lane explicitly drives the legacy-unsigned-metadata or equivalent metadata-bypass seam through `Relyra.Metadata.AutoRefresh` and proves the bypass remains operator-attributed, correlation-linked, and redaction-safe through `AuditWriter`/diagnostic exports and library-owned risk surfacing. Do not make the metadata bypass optional and do not invent synthetic external findings; prove the strict-default and metadata-bypass contracts that exist today. Keep repo-backed tests serialized in the plan verify command to avoid migration collisions noted in research.
  </action>
  <acceptance_criteria>
    - `rg -n "deprecated_algorithm|legacy_algorithm_override_expired|relay_state|key_info|signed_node|signed content" test/security/strict_default_proof_test.exs` finds explicit fail-closed invariant coverage including one mandatory signed-content trust rejection.
    - `rg -n "AutoRefresh|legacy_unsigned|unsigned metadata|correlation_id|actor|cause|REDACTED" test/relyra/ecto/escape_hatch_audit_test.exs` finds the metadata-bypass proof path plus attribution and redaction assertions.
    - The proof lane does not depend on external audit findings to pass.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/security/strict_default_proof_test.exs test/relyra/ecto/escape_hatch_audit_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Reviewers can rerun one focused proof lane that demonstrates strict-by-default behavior and constrained escape-hatch auditing directly from repo state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executable security state -> generated evidence markdown | The generated artifact must reflect real module defaults, proof seams, and drift-sensitive commands. |
| Risky compatibility paths -> persisted audit and reviewer evidence | Escape hatches must remain attributable, correlated, constrained, and redaction-safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | `lib/mix/tasks/relyra.security_review.ex` | mitigate | Generate `SECURITY_REVIEW_EVIDENCE.md` from executable state and fail CI on drift via `--check`. |
| T-26-05 | Elevation of Privilege | `test/security/strict_default_proof_test.exs` | mitigate | Prove fail-closed behavior for unsafe algorithms, raw RelayState, and signed-content trust-boundary violations. |
| T-26-06 | Repudiation | `test/relyra/ecto/escape_hatch_audit_test.exs` | mitigate | Require proof that risky compatibility paths preserve actor, cause, correlation, and redaction-safe summaries in audit evidence. |
| T-26-07 | Information Disclosure | `SECURITY_REVIEW_EVIDENCE.md` | mitigate | Keep evidence redaction-safe by linking to bounded audit/diagnostic seams instead of dumping raw XML, PEMs, or PII. |
</threat_model>

<verification>
- `mix test test/mix/tasks/relyra_security_review_test.exs --warnings-as-errors`
- `mix test test/security/strict_default_proof_test.exs test/relyra/ecto/escape_hatch_audit_test.exs --warnings-as-errors`
</verification>

<success_criteria>
Phase 26 has one rerunnable evidence generator and one focused proof lane that make strict-default and escape-hatch claims reviewer-verifiable from repo state.
</success_criteria>

<output>
After completion, create `.planning/phases/26-security-audit-preparation-and-remediation/26-02-SUMMARY.md`
</output>
