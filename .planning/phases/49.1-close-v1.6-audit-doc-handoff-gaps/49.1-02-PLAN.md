---
phase: 49.1-close-v1.6-audit-doc-handoff-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - ADOPT-01
must_haves:
  truths:
    - "README Start Here step 3 promotes TestSupport macro path aligned with Getting Started §3 and overview Day-1."
    - "Onboarding-path FakeIdP references in README batteries-included section align with TestSupport framing."
  artifacts:
    - path: "README.md"
      provides: "Router step 3 matches ADOPT-01 TestSupport macro promotion."
---

<objective>
Close ADOPT-01 README polish gap: align README Start Here step 3 (and dependent onboarding prose) with Getting Started §3 TestSupport macro framing.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/v1.6-MILESTONE-AUDIT.md
@.planning/phases/49.1-close-v1.6-audit-doc-handoff-gaps/49.1-RESEARCH.md
@.planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-01-SUMMARY.md
@README.md
@guides/getting_started.md
@guides/overview.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| README router sends adopters to low-level FakeIdP API instead of macro path | high | Rewrite step 3 with TestSupport macro + deep link to Getting Started §3 |
| Inconsistent onboarding vocabulary across README runbook intro | medium | Update "FakeIdP proof" onboarding phrases to "TestSupport proof" where they gate runbook access |
| Removing FakeIdP from library inventory section | low | Keep `Relyra.TestSupport.FakeIdP` in "What Ships" — only change onboarding router prose |
</threat_model>

<tasks>

<task type="auto">
  <name>Align README Start Here step 3 with TestSupport macro</name>
  <read_first>
    - README.md (## Start Here numbered list, lines 26–30)
    - guides/overview.md (Day-1 step 2 macro framing)
    - guides/getting_started.md (§3 title and anchor `#3-prove-local-login-with-testsupport`)
    - .planning/v1.6-MILESTONE-AUDIT.md (milestone-cross-cutting: README step 3)
  </read_first>
  <action>
    Replace README step 3 (currently `3. Prove a local sign-in with \`Relyra.TestSupport.FakeIdP\`.`) with:

    ```markdown
    3. Prove a local sign-in with the `Relyra.TestSupport` macro (`setup_saml_connection/2`, `post_saml_response/2`) — see [Getting Started §3](guides/getting_started.md#3-prove-local-login-with-testsupport).
    ```

    Do not renumber steps 1–5. Do not change steps 1, 2, 4, or 5 text.
  </action>
  <verify>grep "TestSupport" README.md && grep "getting_started.md#3-prove-local-login-with-testsupport" README.md</verify>
  <acceptance_criteria>
    - `grep "setup_saml_connection" README.md` exits 0.
    - `grep "guides/getting_started.md#3-prove-local-login-with-testsupport" README.md` exits 0.
    - Step 3 line does **not** contain `FakeIdP` — verify with `grep -n "FakeIdP" README.md` and confirm no match on the step-3 line number.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Align README runbook gate prose to TestSupport proof</name>
  <read_first>
    - README.md (## Batteries Included Support, ## Custom SAML sections — lines 54–67)
    - guides/getting_started.md (§3 intent paragraph)
  </read_first>
  <action>
    In README.md, replace onboarding-gate phrases that say **FakeIdP proof** with **TestSupport proof**:

    1. Line ~54: change `Use these runbooks only after you complete the local FakeIdP proof in Getting Started:` to `Use these runbooks only after you complete the local TestSupport proof in Getting Started:`

    2. Line ~67: change `after the local \`FakeIdP\` proof and before` to `after the local TestSupport proof and before`

    Do **not** change the `## What Ships In The Library` bullet that documents `Relyra.TestSupport.FakeIdP` as a shipped dev/CI helper (line ~83).
  </action>
  <verify>grep "TestSupport proof" README.md</verify>
  <acceptance_criteria>
    - `grep "TestSupport proof" README.md` exits 0 with at least 2 matches.
    - `grep "local FakeIdP proof" README.md` exits 1 (no match).
    - `grep "Relyra.TestSupport.FakeIdP" README.md` still exits 0 (library inventory preserved).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify mix ci.docs green</name>
  <read_first>
    - mix.exs (ci.docs alias)
  </read_first>
  <action>
    Run `mix ci.docs`. Do not modify mix.exs unless ci.docs fails unrelated to README edit.
  </action>
  <verify>mix ci.docs</verify>
  <acceptance_criteria>
    - `mix ci.docs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `grep "setup_saml_connection" README.md`
- `grep "TestSupport proof" README.md`
- `mix ci.docs`
</verification>

<success_criteria>
- README step 3 promotes TestSupport macro with link to Getting Started §3.
- Onboarding gate prose uses TestSupport proof, not FakeIdP proof.
- mix ci.docs exits 0.
</success_criteria>
