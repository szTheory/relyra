---
phase: 47-onboarding-truth-getting-started-production-ecto-path
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - guides/production_ecto_path.md
autonomous: true
requirements:
  - ADOPT-02
must_haves:
  truths:
    - "guides/production_ecto_path.md is the single authoritative production Ecto deployment path — migrations, resolver, store wrappers, prod warning."
    - "Guide documents host-owned store table DDL (request_intents, replay_keys) not in Relyra's 13 shipped migrations."
    - "Guide warns against flat config :relyra, table: for both Ecto stores; documents thin wrapper modules per store."
    - "prod_runtime_ets_warning documented as opt-in config, not automatic on Mix.env() == :prod."
  artifacts:
    - path: "guides/production_ecto_path.md"
      provides: "Day-2 operator guide: install ETS defaults → dep-path migrations → store DDL → wrappers → Connections delegator → prod config → receipt."
---

<objective>
Deliver ADOPT-02 core content: create `guides/production_ecto_path.md` so an adopter can upgrade from install defaults to production Ecto adapters without reading source.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-CONTEXT.md
@.planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-RESEARCH.md
@.planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-PATTERNS.md
@lib/mix/tasks/relyra.install.ex
@test/support/migration_case.ex
@lib/relyra/connection_resolver/ecto.ex
@lib/relyra/request_store/ecto.ex
@lib/relyra/replay_store/ecto.ex
@lib/relyra/replay_store/ets.ex
@lib/relyra/request_store/ets.ex
@guides/identity_mapping_and_provisioning.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Shared `:table` config key breaks one Ecto store silently | high | Document thin host wrapper modules injecting per-store table; explicitly forbid flat `config :relyra, table: …` for both stores |
| Adopter runs ETS stores in prod cluster (replay bypass) | high | Section on Ecto adapters as production requirement; opt-in `prod_runtime_ets_warning: true` as migration safety net with verbatim warning strings |
| Missing store DDL leaves Ecto adapters failing at runtime | high | Host-owned DDL section with columns from adapter SQL contracts; state 13 shipped migrations do NOT include store tables |
| Misleading "ETS warns in prod automatically" | medium | Document `Application.get_env(:relyra, :prod_runtime_ets_warning, false)` — not tied to Mix.env() |
| Connections install stub left unimplemented in prod | medium | Connections delegator example replacing `:adapter_not_configured` stub |
</threat_model>

<tasks>

<task type="auto">
  <name>Create production_ecto_path.md guide skeleton and why-upgrade section</name>
  <read_first>
    - guides/identity_mapping_and_provisioning.md (operator voice, Overview + Receipt pattern)
    - lib/mix/tasks/relyra.install.ex (lines 80–85 install defaults)
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-CONTEXT.md (D-05, D-08)
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-RESEARCH.md (§4 install vs production)
  </read_first>
  <action>
    Create `guides/production_ecto_path.md` with:

    - Title: `# Production Ecto Path`
    - **Overview** paragraph: upgrade path from `mix relyra.install` ETS + Default resolver defaults to cluster-safe Ecto adapters.
    - **## Why upgrade from install defaults** — explain install scaffolds:
      ```elixir
      config :relyra,
        connection_resolver: Relyra.ConnectionResolver.Default,
        request_store: Relyra.RequestStore.ETS,
        replay_store: Relyra.ReplayStore.ETS
      ```
      as dev/single-node convenience; production needs durable connection resolution and replay protection across nodes.
    - **Relyra owns / Host owns** subsection (brief): Relyra ships 13 connection/metadata/mapping/audit migrations; host owns store table DDL and wrapper modules.

    Keep file under 350 lines; use numbered upgrade checklist as section spine for subsequent tasks.
  </action>
  <verify>test -f guides/production_ecto_path.md && head -5 guides/production_ecto_path.md</verify>
  <acceptance_criteria>
    - `test -f guides/production_ecto_path.md` exits 0.
    - `grep "# Production Ecto Path" guides/production_ecto_path.md` exits 0.
    - `grep "ConnectionResolver.Default" guides/production_ecto_path.md` exits 0.
    - `grep "RequestStore.ETS" guides/production_ecto_path.md` exits 0.
    - `grep "ReplayStore.ETS" guides/production_ecto_path.md` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Document Relyra migrations and host Mix alias</name>
  <read_first>
    - test/support/migration_case.ex (Ecto.Migrator.run/4 pattern)
    - priv/repo/migrations/ (13 migration files — list filenames in RESEARCH §4)
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-CONTEXT.md (D-06)
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-RESEARCH.md (§4 migration runner)
  </read_first>
  <action>
    Add **`## 1. Run Relyra migrations from the dependency path`** to `guides/production_ecto_path.md`:

    1. State `mix relyra.install` does NOT copy migrations; host runs from `:relyra` dep path.

    2. Fenced Elixir snippet:
       ```elixir
       migrations_path = Application.app_dir(:relyra, "priv/repo/migrations")

       Ecto.Migrator.with_repo(MyApp.Repo, fn repo ->
         Ecto.Migrator.run(repo, migrations_path, :up, all: true)
       end)
       ```

    3. Host Mix alias example:
       ```elixir
       "relyra.migrate": ["run priv/scripts/relyra_migrate.exs"]
       ```
       or inline alias invoking the Migrator snippet — document idempotent re-runs via `schema_migrations`.

    4. Note: 13 shipped migrations cover connections, metadata, certificates, mappings, audit — NOT request/replay store tables.

    Optional: bullet list of migration purposes (from RESEARCH table) without pasting all filenames.
  </action>
  <verify>grep "Application.app_dir(:relyra" guides/production_ecto_path.md && grep "Ecto.Migrator.run" guides/production_ecto_path.md</verify>
  <acceptance_criteria>
    - `grep 'Application.app_dir(:relyra, "priv/repo/migrations")' guides/production_ecto_path.md` exits 0.
    - `grep "Ecto.Migrator.run" guides/production_ecto_path.md` exits 0.
    - `grep -i "does not copy" guides/production_ecto_path.md` exits 0 (install does not copy migrations).
    - `grep "13" guides/production_ecto_path.md` exits 0 (migration count mentioned).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Document host store DDL and Ecto store wrapper modules</name>
  <read_first>
    - lib/relyra/request_store/ecto.ex (fetch_table, column usage)
    - lib/relyra/replay_store/ecto.ex (fetch_table, column usage)
    - test/security/stores/request_store_ecto_test.exs (@table "request_intents")
    - test/security/stores/replay_store_ecto_test.exs (@table "replay_keys")
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-RESEARCH.md (§4 shared :table gotcha, wrapper pattern)
  </read_first>
  <action>
    Add to `guides/production_ecto_path.md`:

    **`## 2. Create host store tables (not shipped by Relyra)`**
    - request_intents columns: relay_state (text, unique), request_id, intent (jsonb/map), consumed_at, expires_at
    - replay_keys columns: replay_key (text, unique), inserted_at, metadata (jsonb/map)
    - Example `create table` migration skeleton (host-owned filename); unique indexes on relay_state and replay_key

    **`## 3. Implement store wrapper modules`**
    - Explain both `RequestStore.Ecto` and `ReplayStore.Ecto` require `opts[:repo]` AND `opts[:table]` on every call
    - **Critical gotcha callout:** a single `config :relyra, table: "request_intents"` cannot serve both stores — both adapters read `opts[:table]`

    - Full fenced Elixir example for `MyApp.Relyra.RequestStore` wrapper delegating to `Relyra.RequestStore.Ecto` with `@repo MyApp.Repo` and `@table "relyra_request_intents"` (or `"request_intents"`)

    - Symmetric shorter example or note for `MyApp.Relyra.ReplayStore` with `"relyra_replay_keys"` table

    - List optional deps when enabling Ecto adapters: `{:ecto, …}`, `{:ecto_sql, …}`, `{:postgrex, …}`
  </action>
  <verify>grep "relay_state\|replay_key\|opts\\[:table\\]" guides/production_ecto_path.md</verify>
  <acceptance_criteria>
    - `grep "relay_state" guides/production_ecto_path.md` exits 0.
    - `grep "replay_key" guides/production_ecto_path.md` exits 0.
    - `grep "opts[:table]" guides/production_ecto_path.md` exits 0.
    - `grep -i "cannot serve both" guides/production_ecto_path.md` exits 0 OR `grep -i "shared.*table" guides/production_ecto_path.md` exits 0.
    - `grep "RequestStore.Ecto" guides/production_ecto_path.md` exits 0.
    - `grep "ReplayStore.Ecto" guides/production_ecto_path.md` exits 0.
    - `grep -i "host-owned\|not shipped" guides/production_ecto_path.md` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Document Connections delegator, production config, and prod_runtime_ets_warning</name>
  <read_first>
    - lib/mix/tasks/relyra.install.ex (connection_template stub lines 205–216)
    - lib/relyra/connection_resolver/ecto.ex
    - lib/relyra/replay_store/ets.ex (warn_prod_ets!, exact warning text lines 84–86)
    - lib/relyra/request_store/ets.ex (exact warning text lines 153–155)
    - lib/relyra/phoenix/controllers/acs_controller.ex (controller_opts / Application.get_all_env)
    - .planning/phases/47-onboarding-truth-getting-started-production-ecto-path/47-CONTEXT.md (D-07, D-08)
  </read_first>
  <action>
    Add to `guides/production_ecto_path.md`:

    **`## 4. Wire ConnectionResolver.Ecto`**
    - Connections delegator replacing install stub:
      ```elixir
      defmodule MyApp.Relyra.Connections do
        @behaviour Relyra.ConnectionResolver

        @impl true
        def resolve_connection(request_context, opts) do
          Relyra.ConnectionResolver.Ecto.resolve_connection(
            request_context,
            Keyword.put_new(opts, :repo, MyApp.Repo)
          )
        end
      end
      ```

    **`## 5. Update production config`**
    - Target config block:
      ```elixir
      config :relyra,
        connection_resolver: MyApp.Relyra.Connections,
        request_store: MyApp.Relyra.RequestStore,
        replay_store: MyApp.Relyra.ReplayStore,
        repo: MyApp.Repo
      ```
    - Note `:relyra` config keys propagate to runtime opts via ACS controller

    **`## 6. Production ETS warning (opt-in safety net)`**
    - Document `config :relyra, prod_runtime_ets_warning: true` in `config/runtime.exs` for prod
    - Explicit: NOT automatic when `Mix.env() == :prod`; defaults false after install
    - Quote verbatim both warning strings:
      - "Relyra.ReplayStore.ETS is single-node only and provides non-durable replay protection; use an Ecto adapter for production-safe replay guarantees."
      - "Relyra.RequestStore.ETS is single-node only and provides non-durable replay protection; use an Ecto adapter for production-safe request intent semantics."
    - Explain: logs if ETS adapters still hit at runtime during migration; stops after Ecto swap

    **`## Receipt`**
    - Checklist: migrations applied; store tables exist; wrappers configured; connection resolves via Ecto; login succeeds with Ecto stores; no ETS warning after swap
  </action>
  <verify>grep "prod_runtime_ets_warning\|ConnectionResolver.Ecto\|single-node only" guides/production_ecto_path.md</verify>
  <acceptance_criteria>
    - `grep "prod_runtime_ets_warning" guides/production_ecto_path.md` exits 0.
    - `grep "ConnectionResolver.Ecto" guides/production_ecto_path.md` exits 0.
    - `grep "single-node only" guides/production_ecto_path.md` exits 0 (both warning strings contain this phrase — count >= 2).
    - `grep -i "not automatic" guides/production_ecto_path.md` exits 0 OR `grep "Mix.env()" guides/production_ecto_path.md` exits 0 (clarifies opt-in nature).
    - `grep "## Receipt" guides/production_ecto_path.md` exits 0.
    - `grep "adapter_not_configured" guides/production_ecto_path.md` exits 0 OR prose references replacing install Connections stub.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `test -f guides/production_ecto_path.md` exits 0
- `grep prod_runtime_ets_warning guides/production_ecto_path.md` exits 0
- `grep Ecto.Migrator.run guides/production_ecto_path.md` exits 0
- `wc -l guides/production_ecto_path.md` — expect 80–350 lines
</verification>

<success_criteria>
ADOPT-02 content satisfied: authoritative production Ecto guide exists with migrations, resolver, store wrappers, host DDL, and accurate prod_runtime_ets_warning documentation.
</success_criteria>
