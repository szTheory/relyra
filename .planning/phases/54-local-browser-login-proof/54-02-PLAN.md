---
phase: 54
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex
  - demo/ledger_loop/test/ledger_loop_web/controllers/route_affordance_controller_test.exs
autonomous: true
requirements:
  - IDP-01
  - E2E-01
must_haves:
  truths:
    - The "Start Test Login" page directs the user dynamically to the FakeIdP local test path.
    - Following login success, a sanitized receipt page is presented.
  artifacts:
    - path: demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex
      provides: Test login button triggering FakeIdP
  key_links:
    - from: demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex
      to: /fake_idp/:connection_id/sso
      via: UI Link
---

<objective>
Update the `RouteAffordanceController` and its login view to actually start the test login sequence via the FakeIdP created in the previous wave.
Purpose: Completes the loop by allowing evaluators to actually exercise the strict SAML workflow visually in their browser.
Output: Clickable E2E UI path for offline login proof.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex
@demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Login Affordance</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex</files>
  <action>Update `RouteAffordanceController.login` to fetch the default `@enabled_conn_ulid` from `LedgerLoop.Demo.Fixtures`. Pass it to the view. Update `login.html.heex` to contain a primary action link pointing to `/fake_idp/#{conn_id}/sso`. Keep the Phase 55 Keycloak explanation text, but enable the local simulated login capability.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>UI correctly redirects user to the FakeIdP entrypoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Affordance Tests</name>
  <files>demo/ledger_loop/test/ledger_loop_web/controllers/route_affordance_controller_test.exs</files>
  <behavior>
    - Test 1: login page contains a link pointing to the `/fake_idp/...` simulation endpoint.
  </behavior>
  <action>Update/add controller tests for `RouteAffordanceController` ensuring the FakeIdP path is properly exposed on the `/login/test` view.</action>
  <verify>
    <automated>mix test test/ledger_loop_web/controllers/route_affordance_controller_test.exs</automated>
  </verify>
  <done>Test validates affordance contains actionable start link.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Affordance Controller | Standard routing for UI affordance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02 | Spoofing | Login link | accept | FakeIdP route strictly checks dev/test only, meaning clicking this link in prod acts as a 404/Not Found. |
</threat_model>