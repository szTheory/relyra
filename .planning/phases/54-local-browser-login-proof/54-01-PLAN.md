---
phase: 54
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/ledger_loop/lib/ledger_loop_web/router.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex
  - demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex
autonomous: true
requirements:
  - IDP-01
  - IDP-02
  - E2E-01
must_haves:
  truths:
    - Evaluator can visit `/fake_idp/login` and see a warning banner that it is local test support.
    - Evaluator can complete a SAML login by submitting a form that generates a valid Relyra signed response.
    - Evaluator can simulate a failed login to see a typed rejection receipt.
  artifacts:
    - path: demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex
      provides: "FakeIdP test support logic"
      contains: "def login"
  key_links:
    - from: demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex
      to: "/fake_idp/login"
      via: "href"
      pattern: "/fake_idp/login"
---

<objective>
Implement the Local Browser Login Proof using a dev/test-only FakeIdP controller to demonstrate strict SAML login and rejection behavior.

Purpose: To fulfill the requirement of proving SAML end-to-end securely offline without confusing evaluators about production IdP requirements.
Output: A new `FakeIdPController` integrated into the demo app with visual indicators and both happy/sad login paths.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-local-browser-login-proof/54-CONTEXT.md
@lib/relyra/test_support/fake_idp.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold FakeIdP Controller and Route</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/router.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex</files>
  <action>Create `FakeIdPController` with `login` and `sso` actions. Add `get "/fake_idp/login"` and `post "/fake_idp/sso"` routes in `router.ex` (inside the browser pipeline). Create the HTML view and the template `login.html.heex`. The template MUST display a prominent "⚠️ Local Test Support / FakeIdP" banner to fulfill IDP-02. It should present a form mapping seeded identities to the `sso` action.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix compile</automated>
  </verify>
  <done>Controller and templates compile without warnings</done>
</task>

<task type="auto">
  <name>Task 2: Implement SSO Cryptographic Logic</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex</files>
  <action>In `FakeIdPController.sso/2`, build a `SAMLResponse` based on the user selection. Use `Relyra.TestSupport.FakeIdP.build_response/1` and `.sign/2` for happy paths. For failure simulation, intentionally modify the response (e.g. modify the signed XML to break the signature) to demonstrate strict rejection. Render a self-submitting HTML form that posts the response to `/saml/acs`.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix compile</automated>
  </verify>
  <done>SAML response generation using Relyra test support works</done>
</task>

<task type="auto">
  <name>Task 3: Update Setup Checklist Integration</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex, demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex</files>
  <action>Modify the Setup Checklist to link the "Test Login" step to the FakeIdP route (`/fake_idp/login`) instead of a generic mock affordance. Pass `RelayState` appropriately to maintain the UX loop.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix compile</automated>
  </verify>
  <done>Setup checklist correctly routes to FakeIdP</done>
</task>

<task type="auto">
  <name>Task 4: Add Simple Controller Test Coverage</name>
  <files>demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs</files>
  <action>Write a basic test proving the `GET /fake_idp/login` endpoint returns 200 OK and includes the "Local Test Support" warning text.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop_web/controllers/fake_idp_controller_test.exs</automated>
  </verify>
  <done>Tests pass and prove banner presence</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FakeIdP UI | Must not be confused with a legitimate production Identity Provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Spoofing | FakeIdPController | mitigate | The UI is forced to display a warning banner stating it is a local test harness. Additionally, the signing uses test-only FakeIdP keys which are rejected by production Relyra. |
</threat_model>

<verification>
mix test in demo app passes and verifies FakeIdP banner requirement.
</verification>

<success_criteria>
Evaluator can access the Setup Checklist, click "Test Login", reach the FakeIdP UI, see the test support warning, and complete the end-to-end SAML login loop successfully or observe a typed rejection on failure.
</success_criteria>

<output>
Create .planning/phases/54-local-browser-login-proof/54-01-SUMMARY.md when done
</output>