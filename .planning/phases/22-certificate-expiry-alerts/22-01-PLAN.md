---
phase: "22"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/telemetry.ex
  - lib/relyra/telemetry/handlers/log_alerts.ex
  - lib/relyra/security/certificate_expiry.ex
  - test/relyra/security/certificate_expiry_test.exs
  - test/relyra/telemetry/handlers/log_alerts_test.exs
autonomous: true
requirements:
  - CERT-EXP-01
must_haves:
  truths:
    - "The library exposes a callable function to traverse tenant connections and check certificate expiration."
    - "For any certificate near expiration, standard `:telemetry` events are emitted."
    - "The system does not force a background worker on the host app, delegating scheduling to the adopter."
  artifacts:
    - path: "lib/relyra/security/certificate_expiry.ex"
      provides: "check_all/2 pure batch traversal function"
  key_links:
    - from: "lib/relyra/security/certificate_expiry.ex"
      to: "PostgreSQL"
      via: "Ecto queries against Certificate and Connection"
      pattern: "from cert in Relyra.Ecto.Certificate"
---

<objective>
Implement proactive certificate expiry alerting to allow operators to perform timely rollovers.

Purpose: Instead of relying on manual audits or runtime failures, we provide a backend capability that adopters can hook into their own background schedulers (e.g. Oban) to emit standard `:telemetry` events when SAML certificates are nearing expiration.
Output: A pure traversal function `Relyra.Security.CertificateExpiry.check_all/2`, properly routed telemetry events, and full test coverage for both active and optional Ecto fallback paths.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-certificate-expiry-alerts/22-RESEARCH.md
@.planning/phases/22-certificate-expiry-alerts/PATTERNS.md
@lib/relyra/telemetry.ex
@lib/relyra/telemetry/handlers/log_alerts.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Telemetry setup for expiry alerts</name>
  <files>lib/relyra/telemetry.ex, lib/relyra/telemetry/handlers/log_alerts.ex, test/relyra/telemetry/handlers/log_alerts_test.exs</files>
  <behavior>
    - Test: `LogAlerts` attached handler correctly routes `[:relyra, :saml, :certificate, :expiring]` events to a `Logger.warning`.
    - Test: Redaction is applied properly to expiring events.
  </behavior>
  <action>
    - Update `lib/relyra/telemetry.ex` to document the new event `[:relyra, :saml, :certificate, :expiring]` under a new `### certificate.expiring` section.
      - Measurements: `[:days_until_expiry]`
      - Metadata: `[:connection_id, :certificate_id, :fingerprint_sha256, :not_after]`
    - Add `[:relyra, :saml, :certificate, :expiring]` to the `@events` list in `lib/relyra/telemetry/handlers/log_alerts.ex`.
    - Add a `handle_event/4` clause in `log_alerts.ex` for the new event, emitting a `Logger.warning` message like `"certificate expiring " <> inspect(redact(metadata))`.
    - Update the corresponding tests to verify the log emission and redaction.
  </action>
  <verify>
    <automated>mix test test/relyra/telemetry/handlers/log_alerts_test.exs</automated>
  </verify>
  <done>Telemetry docs are updated, handler supports the new event, and tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Certificate Expiry traversal engine</name>
  <files>lib/relyra/security/certificate_expiry.ex, test/relyra/security/certificate_expiry_test.exs</files>
  <behavior>
    - Test: `check_all/2` correctly queries active/next certificates with `not_after` approaching the threshold (default 30 days) on enabled connections.
    - Test: Traversal correctly skips certificates on disabled connections, or retired certificates.
    - Test: For matching certificates, `:telemetry.execute/3` is called with the correct event name and calculated `days_until_expiry` measurement.
    - Test: Without `Ecto.Query` compiled, `check_all/2` returns an `{:error, ...}` tuple specifying `:optional_dependency_missing`.
  </behavior>
  <action>
    - Implement `lib/relyra/security/certificate_expiry.ex` mimicking the pattern found in `lib/relyra/metadata/scheduler.ex` (importing `Ecto.Query` only if compiled).
    - Expose `check_all(repo, opts \\ [])`.
    - Define a private fetching function that uses Ecto to query `Relyra.Ecto.Certificate` joined with `Relyra.Ecto.Connection`.
    - Use options `days_to_expiry` (default `30`).
    - Filter where `conn.status == :enabled`, `cert.lifecycle_state in [:active, :next]`, and `cert.not_after <= ^threshold_date`.
    - Handle traversal: if matches exist, iterate over them sequentially with `Enum.map`.
    - For each expiring certificate, calculate `days_until_expiry` via `DateTime.diff(cert.not_after, now, :day)`, and call `Relyra.Telemetry.execute([:certificate, :expiring], %{days_until_expiry: ...}, metadata)`.
    - Return `{:ok, results}` containing a map of processed certificate ids.
    - Implement the `else` fallback block returning `{:error, Relyra.Error.new(:optional_dependency_missing, ...)}` if Ecto is not loaded.
  </action>
  <verify>
    <automated>mix test test/relyra/security/certificate_expiry_test.exs</automated>
  </verify>
  <done>Pure functional engine correctly performs batch query, sequentially loops over results, and emits telemetry without spawning any internal workers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend -> Database | Database querying via Ecto relies entirely on parameterized queries to avoid injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Denial of Service | `Relyra.Security.CertificateExpiry` | mitigate | Use batch querying and sequential processing (`Enum.map`) rather than uncontrolled concurrency or pulling the entire table into memory. Connection status is filtered to `:enabled` early at the DB level. |
</threat_model>

<verification>
- Verify telemetry events are emitted accurately with the `days_until_expiry` calculation correct.
- Verify Ecto queries are properly parameterized.
</verification>

<success_criteria>
- The library successfully provides `check_all/2`.
- Telemetry events `[:relyra, :saml, :certificate, :expiring]` flow seamlessly to LogAlerts handler.
- Tests confirm isolated behavior and optional-dependency fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/22-certificate-expiry-alerts/22-01-SUMMARY.md`
</output>