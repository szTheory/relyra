---
phase: 11-mapping-persistence-audit-hardening
plan: 11-03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - lib/relyra/ecto/audit_writer.ex
  - lib/relyra/ecto/connections.ex
  - lib/relyra/ecto/metadata_apply.ex
  - lib/relyra/ecto/certificate_inventory.ex
  - test/relyra/ecto/audit_hardening_test.exs
  - test/relyra/ecto/connection_record_test.exs
  - test/relyra/ecto/metadata_apply_test.exs
  - test/relyra/ecto/certificate_inventory_transition_test.exs
autonomous: true
requirements: [CFG-05]
must_haves:
  truths:
    - "Connection lifecycle mutations emit durable attributable audit rows in the same transaction as the write."
    - "Metadata apply and certificate lifecycle transitions emit bounded redaction-safe audit rows without replacing metadata provenance."
    - "Audit history is reviewable across connection, metadata, certificate, and mapping domains from one append-only ledger."
  artifacts:
    - path: "lib/relyra/ecto/audit_writer.ex"
      provides: "Shared append-only audit insert helper with normalization and redaction"
    - path: "lib/relyra/ecto/connections.ex"
      provides: "Audited connection create/update/enable/disable flows"
    - path: "lib/relyra/ecto/metadata_apply.ex"
      provides: "Audited metadata trust mutation path"
    - path: "lib/relyra/ecto/certificate_inventory.ex"
      provides: "Audited certificate lifecycle transition path"
    - path: "test/relyra/ecto/audit_hardening_test.exs"
      provides: "Cross-domain audit regression coverage"
  key_links:
    - from: "lib/relyra/ecto/connections.ex"
      to: "lib/relyra/ecto/audit_writer.ex"
      via: "same transaction audit append"
      pattern: "AuditWriter"
    - from: "lib/relyra/ecto/metadata_apply.ex"
      to: "lib/relyra/ecto/audit_writer.ex"
      via: "post-mutation audit append in transact closure"
      pattern: "AuditWriter"
    - from: "lib/relyra/ecto/certificate_inventory.ex"
      to: "lib/relyra/ecto/audit_writer.ex"
      via: "transition audit append"
      pattern: "AuditWriter"
---

<objective>
Wire a single authoritative audit ledger into the existing trust mutation commands.

Purpose: implement the audited write boundary for D-13, D-14, D-20, and D-21 across the existing connection, metadata, and certificate command surfaces before mapping commands land.
Output: shared `AuditWriter`, audited command flows, and rollback-safe cross-domain regressions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-CONTEXT.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-RESEARCH.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-PATTERNS.md
@.planning/phases/09-metadata-import-export-refresh/09-02-SUMMARY.md
@.planning/phases/10-certificate-inventory-rollover/10-03-SUMMARY.md
@lib/relyra/ecto/connections.ex
@lib/relyra/ecto/metadata_apply.ex
@lib/relyra/ecto/certificate_inventory.ex
@lib/relyra/log.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared audit append helper with its contract tests</name>
  <files>lib/relyra/ecto/audit_writer.ex, test/relyra/ecto/audit_hardening_test.exs</files>
  <read_first>
    <item>.planning/phases/11-mapping-persistence-audit-hardening/11-CONTEXT.md</item>
    <item>.planning/phases/11-mapping-persistence-audit-hardening/11-PATTERNS.md</item>
    <item>lib/relyra/ecto/metadata_apply.ex</item>
    <item>lib/relyra/log.ex</item>
  </read_first>
  <action>Create `Relyra.Ecto.AuditWriter` using the helper and transaction shape analogs mapped in `11-PATTERNS.md`, especially `lib/relyra/ecto/metadata_apply.ex` for repo lookup/result normalization and `lib/relyra/ecto/metadata_revision.ex` for append-only bounded-map posture. Create `test/relyra/ecto/audit_hardening_test.exs` in this task before running verification so the audit helper is proven at first use. The public `append_event(repo, attrs)` API must require `connection_record_id`, `domain`, `action`, `actor`, and `cause`; accept optional `correlation_id`, `subject_ref`, normalized `before_view`, normalized `after_view`, normalized `diff_summary`, and bounded `metadata`; and never infer actor/cause from ambient state per D-19. Normalize and redact payloads with `Relyra.Log`-compatible sensitive-key handling so the durable ledger remains the authority while logs/telemetry stay supplemental per D-14. Implement this helper at the centralized orchestration boundary per D-16 so later command modules call it explicitly, and persist normalized before/after/diff summaries, not raw params, per D-20.</action>
  <verify>
    <automated>mix test test/relyra/ecto/audit_hardening_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`AuditWriter.append_event/2` requires explicit attribution and never reads ambient process state.</criterion>
    <criterion>The helper redacts oversized or sensitive payload values before persistence.</criterion>
    <criterion>Audit helper tests exist and execute in the same task that introduces the helper.</criterion>
  </acceptance_criteria>
  <done>A shared insert-only audit helper exists for all authoritative trust mutations with explicit attribution and bounded redaction-safe summaries.</done>
</task>

<task type="auto">
  <name>Task 2: Audit connection, metadata, and certificate writes in-transaction</name>
  <files>lib/relyra/ecto/connections.ex, lib/relyra/ecto/metadata_apply.ex, lib/relyra/ecto/certificate_inventory.ex, test/relyra/ecto/audit_hardening_test.exs, test/relyra/ecto/connection_record_test.exs, test/relyra/ecto/metadata_apply_test.exs, test/relyra/ecto/certificate_inventory_transition_test.exs</files>
  <read_first>
    <item>.planning/phases/11-mapping-persistence-audit-hardening/11-CONTEXT.md</item>
    <item>.planning/phases/11-mapping-persistence-audit-hardening/11-PATTERNS.md</item>
    <item>lib/relyra/ecto/connections.ex</item>
    <item>lib/relyra/ecto/metadata_apply.ex</item>
    <item>lib/relyra/ecto/certificate_inventory.ex</item>
    <item>test/relyra/ecto/audit_hardening_test.exs</item>
  </read_first>
  <action>Apply the concrete service analogs from `11-PATTERNS.md`: follow `lib/relyra/ecto/connections.ex` for typed command entrypoints, `lib/relyra/ecto/metadata_apply.ex` for straight-line `Repo.transact/1` flows, and `lib/relyra/ecto/certificate_inventory.ex` for optimistic-lock transition handling. Extend the three command surfaces to accept `opts[:audit]` with explicit `%{actor, cause, correlation_id}`. Keep authoritative trust mutations restricted to explicit orchestration modules per D-18, and capture audit rows from those centralized transactional write boundaries per D-16. Wrap connection create/update/enable/disable in `Repo.transact/1` by default per D-21, compute normalized before/after trust views, and append `domain: :connection` events. Keep `Relyra.Ecto.MetadataRevision` as metadata-only provenance per D-12 while also appending `domain: :metadata` audit rows for live trust mutations. Append `domain: :certificate` audit rows for stage, promote, retire, and rollback. In all three modules, store bounded normalized before/after/diff summaries per D-20, cover connection/metadata/certificate domains explicitly per D-13, and keep logs/telemetry non-authoritative per D-14. Add regression tests that failed transactional writes or lock conflicts leave no orphan audit rows and that the same transaction boundary remains intact.</action>
  <verify>
    <automated>mix test test/relyra/ecto/audit_hardening_test.exs test/relyra/ecto/connection_record_test.exs test/relyra/ecto/metadata_apply_test.exs test/relyra/ecto/certificate_inventory_transition_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Connections, metadata apply, and certificate inventory accept explicit audit context and append one cross-domain audit row on success.</criterion>
    <criterion>Rollback and optimistic-lock failure paths leave no persisted audit orphan rows.</criterion>
    <criterion>`Relyra.Ecto.MetadataRevision` remains the metadata provenance ledger while audit events provide the cross-domain review surface.</criterion>
  </acceptance_criteria>
  <done>Existing authoritative trust mutation flows now write cross-domain audit events atomically with their state changes and preserve metadata provenance as a separate ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command opts -> audited transactional write | Untrusted operator context enters the authoritative persistence boundary |
| trust mutation modules -> audit ledger | Domain-specific trust state crosses into durable cross-domain review history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | S | `opts[:audit]` handling | mitigate | Require explicit `actor`, `cause`, and optional `correlation_id`, and store them directly on the audit row per D-19. |
| T-11-03-02 | T | live write -> audit append boundary | mitigate | Append audit rows inside the same `Repo.transact/1` closure as the trust mutation per D-17 and D-21. |
| T-11-03-03 | I | audit payload normalization | mitigate | Persist only bounded normalized before/after/diff summaries and redact sensitive values so logs remain supplemental per D-14 and D-20. |
| T-11-03-04 | R | cross-domain ledger coverage | mitigate | Emit audit rows for connection, metadata, and certificate mutations explicitly, preserving metadata revisions as separate provenance per D-12 and D-13. |
</threat_model>

<verification>
Run the combined audit hardening tests and confirm each authoritative command path writes one durable audit row on success and zero rows on rollback.
</verification>

<success_criteria>
Connection, metadata, and certificate trust mutations now emit explicit attributable audit rows in the same transaction, with bounded normalized summaries and no reliance on logs or telemetry as the audit source.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mapping-persistence-audit-hardening/11-03-SUMMARY.md`
</output>
