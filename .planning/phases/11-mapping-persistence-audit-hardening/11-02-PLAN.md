---
phase: 11-mapping-persistence-audit-hardening
plan: 11-02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - priv/repo/migrations/20260505190000_create_relyra_mapping_and_audit_tables.exs
  - test/relyra/ecto/attribute_mapping_schema_test.exs
  - test/relyra/ecto/group_mapping_schema_test.exs
  - test/relyra/ecto/mapping_revision_schema_test.exs
  - test/relyra/ecto/audit_event_schema_test.exs
  - test/relyra/ecto/migration_constraints_test.exs
autonomous: true
requirements: [CFG-05]
must_haves:
  truths:
    - "The database stores mapping rows and append-only ledger rows with explicit keys and constraints."
    - "Schema tests prove enum-backed mapping semantics and append-only ledger shape."
    - "Constraint tests prove mappings remain separate, attributable, and tied to a real connection row."
  artifacts:
    - path: "priv/repo/migrations/20260505190000_create_relyra_mapping_and_audit_tables.exs"
      provides: "Canonical DDL for live mapping rows and ledger tables"
    - path: "test/relyra/ecto/attribute_mapping_schema_test.exs"
      provides: "Attribute mapping schema validation coverage"
    - path: "test/relyra/ecto/group_mapping_schema_test.exs"
      provides: "Group mapping schema validation coverage"
    - path: "test/relyra/ecto/mapping_revision_schema_test.exs"
      provides: "Mapping revision schema validation coverage"
    - path: "test/relyra/ecto/audit_event_schema_test.exs"
      provides: "Audit event schema validation coverage"
    - path: "test/relyra/ecto/migration_constraints_test.exs"
      provides: "Database constraint regression coverage"
  key_links:
    - from: "priv/repo/migrations/20260505190000_create_relyra_mapping_and_audit_tables.exs"
      to: "lib/relyra/ecto/attribute_mapping.ex"
      via: "matching table and constraint shape"
      pattern: "relyra_attribute_mappings"
    - from: "priv/repo/migrations/20260505190000_create_relyra_mapping_and_audit_tables.exs"
      to: "lib/relyra/ecto/group_mapping.ex"
      via: "matching table and constraint shape"
      pattern: "relyra_group_mappings"
    - from: "test/relyra/ecto/migration_constraints_test.exs"
      to: "priv/repo/migrations/20260505190000_create_relyra_mapping_and_audit_tables.exs"
      via: "real Repo constraint assertions"
      pattern: "unique_index|foreign_key"
---

<objective>
Create the database DDL and regression coverage for the Phase 11 persistence contracts.

Purpose: turn the 11-01 contracts into enforceable schema and DB rules with real-Repo proof for D-02, D-03, D-08, D-09, D-13, D-14, and D-20.
Output: one canonical migration plus schema and constraint tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-CONTEXT.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-RESEARCH.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-PATTERNS.md
@priv/repo/migrations/20260505120100_create_relyra_connection_certificates.exs
@priv/repo/migrations/20260505130200_create_relyra_metadata_revisions.exs
@test/relyra/ecto/metadata_revision_schema_test.exs
@test/relyra/ecto/migration_constraints_test.exs
@test/support/migration_case.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical mapping and audit migration</name>
  <files>priv/repo/migrations/20260505190000_create_relyra_mapping_and_audit_tables.exs</files>
  <read_first>
    <item>.planning/phases/11-mapping-persistence-audit-hardening/11-CONTEXT.md</item>
    <item>.planning/phases/11-mapping-persistence-audit-hardening/11-PATTERNS.md</item>
    <item>priv/repo/migrations/20260505120100_create_relyra_connection_certificates.exs</item>
    <item>priv/repo/migrations/20260505130200_create_relyra_metadata_revisions.exs</item>
  </read_first>
  <action>Create one migration that follows the migration analogs mapped in `11-PATTERNS.md`, especially `20260505130200_create_relyra_metadata_revisions.exs` for append-only ledgers and `20260505120100_create_relyra_connection_certificates.exs` for child-row foreign-key/index shape. Add `relyra_attribute_mappings`, `relyra_group_mappings`, `relyra_mapping_revisions`, and `relyra_audit_events` with binary ids, `connection_record_id` FKs, `position` ordering, and `utc_datetime_usec` timestamps. Keep attribute and group rows on separate tables per D-02. Enforce enum-backed semantics at the schema layer and matching relational integrity here: uniqueness on `[:connection_record_id, :target_field]` for attribute mappings and `[:connection_record_id, :source_attribute, :source_value, :role_value]` for group mappings so explicit target and exact-match behavior stay reviewable per D-06, D-08, and D-09. Give both ledgers append-only timestamp posture, lookup indexes, and no new JSON/blob storage on `relyra_connections` per D-01, D-03, D-13, and D-20.</action>
  <verify>
    <automated>mix test test/relyra/ecto/migration_constraints_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The migration creates four new tables and leaves `relyra_connections` free of mapping blobs.</criterion>
    <criterion>Live mapping uniqueness matches the bounded exact-name and exact-group semantics.</criterion>
    <criterion>Both ledgers have append-only timestamps and indexed ownership back to connections.</criterion>
  </acceptance_criteria>
  <done>The database has four new tables with FK, uniqueness, and append-only constraints matching the Phase 11 storage model.</done>
</task>

<task type="auto">
  <name>Task 2: Prove schema and constraint behavior with real Repo tests</name>
  <files>test/relyra/ecto/attribute_mapping_schema_test.exs, test/relyra/ecto/group_mapping_schema_test.exs, test/relyra/ecto/mapping_revision_schema_test.exs, test/relyra/ecto/audit_event_schema_test.exs, test/relyra/ecto/migration_constraints_test.exs</files>
  <read_first>
    <item>.planning/phases/11-mapping-persistence-audit-hardening/11-CONTEXT.md</item>
    <item>.planning/phases/11-mapping-persistence-audit-hardening/11-PATTERNS.md</item>
    <item>test/relyra/ecto/metadata_revision_schema_test.exs</item>
    <item>test/relyra/ecto/migration_constraints_test.exs</item>
  </read_first>
  <action>Expand the schema tests created in 11-01 and extend the real-Repo constraint assertions in `test/relyra/ecto/migration_constraints_test.exs` using the exact analogs mapped in `11-PATTERNS.md`. The schema tests must explicitly verify separate attribute and group contracts per D-02, the bounded exact-name and exact-group semantics per D-06, the absence of regex/script/expression transforms per D-07, append-only mapping revision coverage per D-03, enum-constrained target/multivalue fields per D-08 and D-09, cross-domain audit-event shape for connection/metadata/certificate/mapping domains per D-13, the absence of log-or-telemetry-only proof per D-14, and normalized before/after/diff maps per D-20. The migration test must prove FK ownership, uniqueness, and that ledger inserts require a real `connection_record_id`.</action>
  <verify>
    <automated>mix test test/relyra/ecto/attribute_mapping_schema_test.exs test/relyra/ecto/group_mapping_schema_test.exs test/relyra/ecto/mapping_revision_schema_test.exs test/relyra/ecto/audit_event_schema_test.exs test/relyra/ecto/migration_constraints_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Tests fail on unsupported targets, multivalue strategies, or transform-style fields outside the Phase 11 scope.</criterion>
    <criterion>Tests prove both ledgers require valid connection ownership and bounded normalized maps.</criterion>
    <criterion>Migration assertions cover uniqueness and foreign keys for both live mapping tables.</criterion>
  </acceptance_criteria>
  <done>The Phase 11 persistence model is enforced by both changesets and the database, with decision coverage expressed directly in tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL -> host database | Schema defects can weaken ownership, uniqueness, or append-only guarantees |
| test harness -> persistence contracts | Weak tests could leave trust-bearing semantics unverified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | T | mapping/audit table DDL | mitigate | Add explicit FK and uniqueness constraints so mappings remain connection-scoped and reviewable. |
| T-11-02-02 | R | mapping revision and audit ledgers | mitigate | Prove append-only rows require normalized before/after/diff data and valid connection ownership per D-03 and D-20. |
| T-11-02-03 | I | audit storage columns | mitigate | Keep audit proofs focused on bounded normalized maps rather than raw XML, PEM, or telemetry records per D-14 and D-15. |
</threat_model>

<verification>
Run the schema and migration tests together. Confirm they fail on invalid enum values, duplicate live mappings, and missing foreign keys.
</verification>

<success_criteria>
The Phase 11 persistence model is enforceable and regression-tested at both the changeset and database layers.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mapping-persistence-audit-hardening/11-02-SUMMARY.md`
</output>
