---
phase: 37-identity-mapping-and-provisioning-guide
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - guides/identity_mapping_and_provisioning.md
  - README.md
  - guides/getting_started.md
  - guides/recipes/generic_saml.md
  - mix.exs
autonomous: true
requirements:
  - DOCS-03
tags:
  - phase-37
  - docs-03
  - docs
  - user-mapper
  - ci

must_haves:
  truths:
    - "The guide documents the real `Relyra.UserMapper` callback and includes at least one complete implementation example for each required pattern."
    - "Examples stay host-owned and code-truthful: the adapter receives validated login data, returns a mapped user shape, and does not pretend Relyra ships a user database or SCIM engine."
    - "The new guide is discoverable from the adjacent onboarding/operator docs, published in ExDoc extras, and `ci.docs` fails if the file is missing."
  artifacts:
    - path: "guides/identity_mapping_and_provisioning.md"
      provides: "Complete callback documentation and examples for all three mapping patterns"
      contains: "## `UserMapper` behaviour"
    - path: "mix.exs"
      provides: "ExDoc publication wiring plus docs-lane presence gate for the new guide"
      contains: "guides/identity_mapping_and_provisioning.md"
  key_links:
    - from: "README.md"
      to: "guides/identity_mapping_and_provisioning.md"
      via: "Day-2 identity mapping guide routing"
      pattern: "identity_mapping_and_provisioning\\.md"
    - from: "guides/getting_started.md"
      to: "guides/identity_mapping_and_provisioning.md"
      via: "Production follow-on reference"
      pattern: "identity_mapping_and_provisioning\\.md"
    - from: "guides/recipes/generic_saml.md"
      to: "guides/identity_mapping_and_provisioning.md"
      via: "Cross-link from NameID/mapping guidance into the dedicated guide"
      pattern: "identity_mapping_and_provisioning\\.md"
---

<objective>
Finish the identity-mapping guide with behaviour-backed examples, route readers to it from adjacent docs, and make it durable in `ci.docs`.

Purpose: close the full `DOCS-03` acceptance surface and ensure the guide remains attached to the real extension seam and discoverable after Phase 37 ships.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-identity-mapping-and-provisioning-guide/37-RESEARCH.md
@guides/identity_mapping_and_provisioning.md
@README.md
@guides/getting_started.md
@guides/recipes/generic_saml.md
@lib/relyra/login_result.ex
@lib/relyra/principal.ex
@lib/relyra/user_mapper.ex
@lib/relyra/user_mapper/default_attribute.ex
@lib/mix/tasks/relyra.install.ex
@lib/relyra/phoenix/controllers/acs_controller.ex
@test/relyra/user_mapper/default_attribute_test.exs
@mix.exs

<interfaces>
From lib/relyra/login_result.ex:
```elixir
defstruct [
  :principal,
  :connection,
  :mapped_user,
  :relay_state,
  :issuer,
  :in_response_to,
  :validation_trace,
  :return_to
]
```

From lib/relyra/principal.ex:
```elixir
defstruct [
  :name_id,
  :name_id_format,
  :session_index,
  :attributes,
  :authn_instant,
  :authn_context_class_ref,
  :connection_id
]
```

From lib/relyra/user_mapper.ex:
```elixir
def map_attributes(assertion, connection, opts \\ []) do
  adapter = Keyword.get(opts, :user_mapper, Relyra.UserMapper.DefaultAttribute)
  ...
end
```

From lib/relyra/user_mapper/default_attribute.ex:
```elixir
%{
  name_id: Map.get(assertion, :name_id),
  email: get_attribute(attributes, ["email", "mail", "EmailAddress"]),
  first_name: ...,
  last_name: ...,
  roles: ...
}
```

From lib/mix/tasks/relyra.install.ex:
```elixir
defmodule MyApp.Relyra.UserMapper do
  @behaviour Relyra.UserMapper
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the `UserMapper` behaviour and add three complete host-owned examples</name>
  <files>guides/identity_mapping_and_provisioning.md</files>
  <action>Extend `guides/identity_mapping_and_provisioning.md` with the implementation-facing sections that close `DOCS-03`. Add these exact H2 sections: `## \`UserMapper\` behaviour`, `## Example: NameID as local identifier`, `## Example: Attribute as local identifier`, and `## Example: JIT create or update`. Document one truthful contract explicitly: in the ACS integration path, the adapter receives a `%Relyra.LoginResult{principal: %Relyra.Principal{...}, connection: ...}` plus the resolved connection, so examples must extract identity fields from `login_result.principal.name_id`, `login_result.principal.name_id_format`, and `login_result.principal.attributes` rather than inventing top-level `login_result.name_id` or `login_result.attributes` fields. Each example must be complete enough to read as a real host-app adapter module, including `defmodule`, `@behaviour Relyra.UserMapper`, and `def map_attributes/3`, and each must preserve the library boundary: Relyra verifies SAML, the host app decides lookup, linking, creation, and authorization policy.</action>
  <verify><automated>rg -n '^## `UserMapper` behaviour$|^## Example: NameID as local identifier$|^## Example: Attribute as local identifier$|^## Example: JIT create or update$' guides/identity_mapping_and_provisioning.md && rg -n 'Relyra.LoginResult|Relyra.Principal|principal\.name_id|principal\.name_id_format|principal\.attributes|defmodule|@behaviour Relyra.UserMapper|def map_attributes' guides/identity_mapping_and_provisioning.md</automated></verify>
  <done>The guide now documents the actual adapter seam and provides one concrete implementation example per pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Route adjacent docs to the new guide and gate it in `ci.docs`</name>
  <files>README.md, guides/getting_started.md, guides/recipes/generic_saml.md, mix.exs</files>
  <action>Add lightweight routing links to `guides/identity_mapping_and_provisioning.md` from the docs a user will naturally read before needing identity-mapping decisions: `README.md`, `guides/getting_started.md`, and `guides/recipes/generic_saml.md`. Keep the messaging honest: this is a production follow-on / host-owned identity guide, not a new Day-1 prerequisite or a broad support-claim expansion. In `mix.exs`, add the guide to ExDoc `extras` so it is actually published, and extend `ci.docs` with `cmd test -f guides/identity_mapping_and_provisioning.md`. Verify the docs publication path, the cross-links, the docs lane, and the standard warnings-as-errors suite still pass.</action>
  <verify><automated>rg -n 'guides/identity_mapping_and_provisioning\.md' README.md guides/getting_started.md guides/recipes/generic_saml.md mix.exs && mix ci.docs && mix test --warnings-as-errors</automated></verify>
  <done>The guide is discoverable from the existing operator flow, published in ExDoc, and CI fails closed if the file disappears or the docs lane regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> host extension code | Example code can silently redefine the `UserMapper` seam if it drifts from the real callback contract |
| docs artifact -> CI | Without a docs gate, the guide can disappear while the rest of the docs lane stays green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | T | `UserMapper` examples | mitigate | Ground every example in the real callback and current login-result fields instead of invented host/runtime surfaces |
| T-37-05 | I | guide routing language | mitigate | Keep routing text scoped as a host-owned production follow-on, not an expansion of batteries-included support or SCIM scope |
| T-37-06 | D | docs publication / presence in CI | mitigate | Add `guides/identity_mapping_and_provisioning.md` to ExDoc extras and `cmd test -f guides/identity_mapping_and_provisioning.md` to `ci.docs`, then re-run the lane |
</threat_model>

<verification>
- `rg -n '^## \`UserMapper\` behaviour$|^## Example: NameID as local identifier$|^## Example: Attribute as local identifier$|^## Example: JIT create or update$' guides/identity_mapping_and_provisioning.md`
- `rg -n 'Relyra.LoginResult|Relyra.Principal|principal\.name_id|principal\.name_id_format|principal\.attributes|defmodule|@behaviour Relyra.UserMapper|def map_attributes' guides/identity_mapping_and_provisioning.md`
- `rg -n 'guides/identity_mapping_and_provisioning\.md' README.md guides/getting_started.md guides/recipes/generic_saml.md mix.exs`
- `mix ci.docs`
- `mix test --warnings-as-errors`
</verification>

<success_criteria>
- The guide includes complete `UserMapper` behaviour documentation and one complete example per required pattern.
- Adjacent docs route readers to the guide without changing the project's support taxonomy or scope boundary.
- The guide is published in ExDoc extras.
- `ci.docs` fails if the guide is missing, and the standard warnings-as-errors suite remains green after the docs-lane change.
</success_criteria>

<output>
After completion, create `.planning/phases/37-identity-mapping-and-provisioning-guide/37-02-SUMMARY.md`
</output>
