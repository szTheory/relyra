---
phase: 37-identity-mapping-and-provisioning-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/identity_mapping_and_provisioning.md
  - lib/relyra/user_mapper.ex
autonomous: true
requirements:
  - DOCS-03
tags:
  - phase-37
  - docs-03
  - docs
  - identity-mapping
  - jit

must_haves:
  truths:
    - "The new guide explains Relyra's trust boundary honestly: validated login data ends at a host-owned mapping/provisioning decision, not a library-owned user lifecycle."
    - "All three required patterns are covered explicitly: NameID-as-local-identifier, attribute-as-local-identifier, and JIT create-or-update."
    - "The guide includes anchor-stability guidance, an explicit JIT decision tree, an explicit SCIM non-goal statement, and a warning about JIT plus SCIM running as simultaneous sources of truth."
    - "The public `Relyra.UserMapper` docs are tightened to describe the real ACS seam and verified login payload without changing runtime behavior."
  artifacts:
    - path: "guides/identity_mapping_and_provisioning.md"
      provides: "Authoritative identity mapping and provisioning guide"
      contains: "## JIT decision tree"
    - path: "lib/relyra/user_mapper.ex"
      provides: "ExDoc-visible behaviour documentation aligned to the current mapper seam"
      contains: "Relyra.LoginResult"
  key_links:
    - from: "guides/identity_mapping_and_provisioning.md"
      to: "lib/relyra/user_mapper.ex"
      via: "Behaviour contract explanation"
      pattern: "UserMapper|map_attributes"
    - from: "guides/identity_mapping_and_provisioning.md"
      to: "guides/recipes/generic_saml.md"
      via: "Anchor-stability and NameID choice remain aligned with the generic SAML runbook"
      pattern: "NameID|anchor|generic SAML"
---

<objective>
Create the core identity-mapping guide and lock the conceptual decisions before example code lands.

Purpose: give adopters one authoritative document for choosing their local identity anchor, deciding whether JIT is appropriate, and understanding where Relyra's responsibility stops and the host application's identity policy begins.

This plan also resolves the current public-doc ambiguity in `Relyra.UserMapper` so the guide and ExDoc describe the same seam.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-identity-mapping-and-provisioning-guide/37-RESEARCH.md
@README.md
@guides/getting_started.md
@guides/recipes/generic_saml.md
@guides/recipes/adfs.md
@lib/relyra/login_result.ex
@lib/relyra/principal.ex
@lib/relyra/user_mapper.ex
@lib/relyra/phoenix/controllers/acs_controller.ex
@lib/relyra/protocol/validation_pipeline.ex

<interfaces>
From lib/relyra/user_mapper.ex:
```elixir
@callback map_attributes(assertion :: map(), connection :: map(), opts :: keyword()) ::
            {:ok, map()} | {:error, Error.t()}
```

From lib/relyra/phoenix/controllers/acs_controller.ex:
```elixir
case Relyra.UserMapper.map_attributes(login_result, login_result.connection, opts) do
  {:ok, mapped_user} -> ...
end
```

From lib/relyra/protocol/validation_pipeline.ex:
```elixir
%{
  name_id: ...,
  name_id_format: ...,
  attributes: ...,
  connection: connection,
  return_to: ...,
  relay_state: ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the core guide with ownership boundaries and the three mapping patterns</name>
  <files>guides/identity_mapping_and_provisioning.md</files>
  <action>Create `guides/identity_mapping_and_provisioning.md` as the authoritative identity-mapping and provisioning guide. Keep the tone operator-first and host-app-aware, consistent with the existing runbooks. The file must include these exact H2 sections: `## Overview`, `## Relyra owns / Host owns`, `## Choose your identity anchor first`, `## Pattern 1: NameID as local identifier`, `## Pattern 2: Attribute as local identifier`, `## Pattern 3: JIT create or update`, `## JIT decision tree`, and `## SCIM is a non-goal`. The guide must explain the actual seam: Relyra finishes at a verified login result and the host app decides local lookup, linking, creation, and authorization policy. Keep the pattern guidance concrete: when NameID is the safest anchor, when another attribute is better, and what JIT actually means in a host app.</action>
  <verify><automated>rg -n "^## Overview$|^## Relyra owns / Host owns$|^## Choose your identity anchor first$|^## Pattern 1: NameID as local identifier$|^## Pattern 2: Attribute as local identifier$|^## Pattern 3: JIT create or update$|^## JIT decision tree$|^## SCIM is a non-goal$" guides/identity_mapping_and_provisioning.md && rg -n "NameID|attribute|JIT|SCIM|UserMapper|map_attributes" guides/identity_mapping_and_provisioning.md</automated></verify>
  <done>The new guide exists and gives the host team a clear identity-model decision frame before any example code or provisioning details are introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Add anchor-stability guidance and the JIT-plus-SCIM safety warning</name>
  <files>guides/identity_mapping_and_provisioning.md, lib/relyra/user_mapper.ex</files>
  <action>Within the same guide, make anchor stability and lifecycle boundaries load-bearing. Explain what breaks when the IdP changes NameID format or source after users already exist, why email-based anchors are convenient but brittle, and why `transient` identifiers are unsuitable for local account anchoring. In the JIT decision tree and SCIM non-goal section, state explicitly that Relyra covers login-time assertion and mapping only, that SCIM lifecycle sync stays in the host app or adjacent systems, and that running JIT create-or-update plus SCIM without one clear source of truth can cause duplicate-account or account-link drift. In the same task, update `lib/relyra/user_mapper.ex` docs so ExDoc describes the real seam truthfully: the ACS path passes a verified login result plus the resolved connection into `map_attributes/3`, the adapter returns a host-shaped user map, and session establishment happens later. Do not change runtime behavior, callback arity, or return types.</action>
  <verify><automated>rg -n "stable|stability|email|transient|source of truth|SCIM|duplicate|drift|NameID format" guides/identity_mapping_and_provisioning.md && rg -n "LoginResult|login_result|SessionAdapter|verified|map_attributes" lib/relyra/user_mapper.ex</automated></verify>
  <done>The guide warns clearly about anchor churn and lifecycle-source conflicts, and the public behaviour docs no longer imply a cleaner seam than the code actually uses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> host identity policy | The guide influences which SAML fields become local identity anchors |
| docs -> provisioning ownership | The guide can overstate or blur whether Relyra or the host app owns account lifecycle decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | T | identity-anchor guidance | mitigate | Keep the anchor-choice section explicit about stable vs brittle identifiers and about migration risk when NameID source changes |
| T-37-02 | I | JIT / SCIM boundary | mitigate | State plainly that SCIM lifecycle ownership is out of scope and warn against simultaneous JIT+SCIM sources of truth |
| T-37-03 | S | trust-boundary wording | mitigate | Describe `UserMapper` as a post-validation host seam, not a protocol-verification shortcut or a library-owned user store |
</threat_model>

<verification>
- `rg -n "^## Overview$|^## Relyra owns / Host owns$|^## Choose your identity anchor first$|^## Pattern 1: NameID as local identifier$|^## Pattern 2: Attribute as local identifier$|^## Pattern 3: JIT create or update$|^## JIT decision tree$|^## SCIM is a non-goal$" guides/identity_mapping_and_provisioning.md`
- `rg -n "NameID|attribute|JIT|SCIM|UserMapper|map_attributes" guides/identity_mapping_and_provisioning.md`
- `rg -n "stable|stability|email|transient|source of truth|SCIM|duplicate|drift|NameID format" guides/identity_mapping_and_provisioning.md`
- `rg -n "LoginResult|login_result|SessionAdapter|verified|map_attributes" lib/relyra/user_mapper.ex`
</verification>

<success_criteria>
- `guides/identity_mapping_and_provisioning.md` exists and covers all three required mapping patterns.
- The guide makes the identity-anchor and lifecycle boundary decisions explicit before any host-specific implementation detail.
- The JIT decision tree, SCIM non-goal statement, and anchor-stability warning are all present and operator-usable.
- `Relyra.UserMapper` public docs describe the current runtime seam without changing runtime behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/37-identity-mapping-and-provisioning-guide/37-01-SUMMARY.md`
</output>
