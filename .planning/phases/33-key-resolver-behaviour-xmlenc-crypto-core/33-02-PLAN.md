---
phase: 33-key-resolver-behaviour-xmlenc-crypto-core
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - lib/relyra/security/xml_enc.ex
  - test/security/xml_enc_test.exs
  - mix.exs
  - test/security/ci_gate_integrity_test.exs
autonomous: true
requirements:
  - ENC-04

must_haves:
  truths:
    - "XMLEnc.decrypt/3 returns {:ok, plaintext_bytes} for a well-formed RSA-OAEP + AES-GCM encrypted assertion"
    - "RSA-PKCS1v1.5 key transport algorithm URI causes decrypt/3 to return :decryption_failed"
    - "AES-CBC content encryption algorithm URI causes decrypt/3 to return :decryption_failed"
    - "CipherValue with fewer than 28 bytes (truncated auth tag) causes decrypt/3 to return :decryption_failed"
    - "Malformed base64 in CipherValue causes decrypt/3 to return :decryption_failed"
    - "Document KeyInfo is silently ignored; private key comes only from the key_resolver argument"
    - "mix ci.security runs xml_enc_test.exs as its own cmd mix test subprocess"
    - "ci_gate_integrity_test.exs @gated_suites includes {\"test/security/xml_enc_test.exs\", nil}"
  artifacts:
    - path: "lib/relyra/security/xml_enc.ex"
      provides: "XMLEnc.decrypt/3 — RSA-OAEP CEK unwrap + AES-GCM content decryption"
      contains: "def decrypt(encrypted_assertion_bytes, key_resolver_module, opts)"
    - path: "test/security/xml_enc_test.exs"
      provides: "4-case security corpus: PKCS1v1.5, AES-CBC, truncated tag, malformed ciphertext"
      contains: ":decryption_failed"
  key_links:
    - from: "lib/relyra/security/xml_enc.ex"
      to: "lib/relyra/security/algorithm_policy.ex"
      via: "AlgorithmPolicy.enforce_key_transport_algorithm/2 and enforce_content_encryption_algorithm/3"
      pattern: "AlgorithmPolicy.enforce"
    - from: "lib/relyra/security/xml_enc.ex"
      to: "lib/relyra/security/xml/saxy_tree.ex"
      via: "SaxyTree.parse/1 for EncryptedData/EncryptedKey element extraction"
      pattern: "SaxyTree.parse"
    - from: "lib/relyra/security/xml_enc.ex"
      to: "lib/relyra/key_resolver.ex"
      via: "apply(key_resolver_module, :resolve, [connection]) inside resolve_key/2"
      pattern: "apply.*:resolve"
    - from: "mix.exs"
      to: "test/security/xml_enc_test.exs"
      via: "cmd mix test test/security/xml_enc_test.exs --warnings-as-errors in ci.security alias"
      pattern: "xml_enc_test"
---

<objective>
Build `Relyra.Security.XMLEnc.decrypt/3` — the RSA-OAEP + AES-GCM decryption primitive that parses encrypted assertion bytes through SaxyTree, gates algorithm URIs through AlgorithmPolicy, delegates key retrieval to the KeyResolver, and returns either decrypted plaintext bytes or the opaque atom `:decryption_failed` for every failure mode. Wire the 4-case security corpus into `mix ci.security` via both `mix.exs` and `ci_gate_integrity_test.exs`.

Purpose: Phase 34 calls XMLEnc.decrypt/3 from ValidationPipeline. The function contract (bytes, module, opts) must be stable and side-effect-free. All failure paths collapse to one atom to prevent padding-oracle and error-oracle attacks.

Output: `lib/relyra/security/xml_enc.ex`, `test/security/xml_enc_test.exs`, two-line additions to `mix.exs` and `test/security/ci_gate_integrity_test.exs`.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-key-resolver-behaviour-xmlenc-crypto-core/33-CONTEXT.md
@.planning/phases/33-key-resolver-behaviour-xmlenc-crypto-core/33-RESEARCH.md
@.planning/phases/33-key-resolver-behaviour-xmlenc-crypto-core/33-01-SUMMARY.md

<interfaces>
From lib/relyra/security/algorithm_policy.ex (verified Phase 32 deliverables):

  @spec enforce_key_transport_algorithm(t(), term()) :: :ok | Error.t()
  — returns :ok when URI is in allowed_key_transport_algorithms
  — returns %Error{type: :deprecated_algorithm} for any non-allowed URI
  — always returns %Error{} (never :decryption_failed) for PKCS1v1.5

  @spec enforce_content_encryption_algorithm(t(), term(), keyword()) ::
          :ok | Error.t() | :decryption_failed
  — opts accepts auth_tag: binary()
  — returns :decryption_failed (bare atom) when byte_size(auth_tag) < 16
  — returns :ok when URI is in allowed_content_encryption_algorithms AND tag length is valid
  — returns %Error{} for AES-CBC URI and for unknown URIs

  AlgorithmPolicy.default() :: %AlgorithmPolicy{
    allowed_key_transport_algorithms: ["http://www.w3.org/2001/04/xmlenc#rsa-oaep-mgf1p"],
    allowed_content_encryption_algorithms: [
      "http://www.w3.org/2001/04/xmlenc#aes128-gcm",
      "http://www.w3.org/2001/04/xmlenc#aes256-gcm"
    ]
  }

From lib/relyra/security/signature.ex (rescue pattern):
  defp safe_verify(message, digest_atom, signature, public_key) do
    :public_key.verify(message, digest_atom, signature, public_key)
  rescue
    _ -> false
  end

OTP 28 crypto (verified in RESEARCH.md):
  :public_key.decrypt_private(ciphertext, private_key, [{:rsa_padding, :rsa_pkcs1_oaep_padding}])
  :public_key.pem_decode(pem_binary) -> list of pem_entry tuples
  :public_key.pem_entry_decode(entry) -> RSAPrivateKey term
  :crypto.crypto_one_time_aead(:aes_128_gcm | :aes_256_gcm, key, iv, ciphertext, aad, auth_tag, false)
    -> plaintext_binary on success | :error (bare atom) on auth tag failure

AES-GCM CipherValue layout: IV(12 bytes) || Ciphertext (variable) || Auth Tag (16 bytes)
Minimum valid CipherValue byte size: 12 + 0 + 16 = 28 bytes

From lib/relyra/security/xml/saxy_tree.ex (SaxyTree.parse/1):
  SaxyTree.parse(binary()) :: {:ok, %SaxyTree.Node{}} | {:error, term()}
  Node struct fields: local (string, no namespace prefix), attrs (list of {name, value} tuples),
                      children (list of Node), text (string | nil)

From lib/relyra/key_resolver.ex (Plan 01 output):
  @callback resolve(connection :: map()) :: {:ok, binary()} | {:error, Error.t()}
  resolve/2 (public dispatch) not called from XMLEnc — XMLEnc calls apply(key_resolver_module, :resolve, [connection]) directly (inline, not via the dispatch function)

XMLEnc element structure inside EncryptedAssertion:
  root.local = "EncryptedAssertion" (or may be root itself as EncryptedData)
  find_first(root, "EncryptedData") -> EncryptedData node
  find_first(enc_data, "KeyInfo") -> KeyInfo node
  find_first(key_info, "EncryptedKey") -> EncryptedKey node
  EncryptionMethod attr: Enum.find_value(node.attrs, fn {"Algorithm", v} -> v; _ -> nil end)
  CipherValue text: find_first(find_first(node, "CipherData"), "CipherValue").text

From test/security/ci_gate_integrity_test.exs:
  @gated_suites [
    {"test/security/ci_gate_integrity_test.exs", nil},
    {"test/security/strict_default_proof_test.exs", nil},
    {"test/relyra/ecto/escape_hatch_audit_test.exs", nil},
    {"test/security/xml/corpus_security_test.exs", "security_corpus"},
    ...
  ]
  — add {"test/security/xml_enc_test.exs", nil} to this list

From mix.exs ci.security alias (current last lines before closing bracket):
    "cmd mix test test/security/xml/adversarial_crypto_test.exs --only adversarial_crypto --warnings-as-errors",
    "deps.audit --ignore-advisory-ids GHSA-rhv4-8758-jx7v",
    "hex.audit",
    "sobelow --config"
  — add "cmd mix test test/security/xml_enc_test.exs --warnings-as-errors" BEFORE the deps.audit line

From test/support/fake_idp.ex:
  FakeIdP.keypair() -> {:RSAPrivateKey, _, n, e, ...} (RSA-2048, stored in persistent_term)
  Public key extraction: {:RSAPrivateKey, _, n, e, _, _, _, _, _, _, _} = keypair; {:RSAPublicKey, n, e}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: XMLEnc.decrypt/3 implementation</name>
  <files>lib/relyra/security/xml_enc.ex</files>
  <read_first>
    - lib/relyra/security/algorithm_policy.ex — enforce_key_transport_algorithm/2 and enforce_content_encryption_algorithm/3 exact signatures and return-value shapes; default/0 to understand the policy struct
    - lib/relyra/security/signature.ex — safe_verify/4 rescue pattern (lines ~390-400); do_verify/4 overall error-handling discipline
    - lib/relyra/security/xml/saxy_tree.ex — SaxyTree.parse/1 signature; Node struct fields (local, attrs, children, text); find_first-style traversal pattern if any exists
    - lib/relyra/key_resolver.ex — the @callback spec; understand that XMLEnc calls apply(module, :resolve, [connection]) directly (not via the public resolve/2 dispatch)
  </read_first>
  <behavior>
    XMLEnc.decrypt/3 public contract:
    - decrypt(valid_gcm_bytes, key_resolver_module, opts) -> {:ok, plaintext_bytes} when all gates pass and crypto succeeds
    - decrypt(pkcs1_bytes, resolver, []) -> :decryption_failed when EncryptedKey Algorithm is "http://www.w3.org/2001/04/xmlenc#rsa-1_5"
    - decrypt(cbc_bytes, resolver, []) -> :decryption_failed when EncryptedData Algorithm is "http://www.w3.org/2001/04/xmlenc#aes256-cbc"
    - decrypt(truncated_tag_bytes, resolver, []) -> :decryption_failed when CipherValue auth tag < 16 bytes
    - decrypt(malformed_bytes, resolver, []) -> :decryption_failed when base64 in CipherValue is invalid
    - decrypt("not xml", resolver, []) -> :decryption_failed (parse failure)
    - decrypt(bytes, resolver, []) -> :decryption_failed when key_resolver returns {:error, _}
    - decrypt(valid_bytes, bad_module, []) -> :decryption_failed when module has no resolve/1 exported function
    - Never raises; never returns {:error, _}; never returns any atom except :decryption_failed on failure

    Gate ordering (per D-04 and revised RESEARCH.md Pattern 5):
    1. SaxyTree.parse/1 on raw bytes
    2. Extract EncryptedData algorithm URI, EncryptedKey algorithm URI, key CipherValue, content CipherValue
    3. enforce_key_transport_algorithm/2 (URI-only, no auth tag yet)
    4. Base64-decode both CipherValues
    5. Split content CipherValue: IV(12) || Ciphertext || Tag(16); guard byte_size >= 28
    6. enforce_content_encryption_algorithm/3 with auth_tag: auth_tag (URI check + tag length guard in one call)
    7. resolve_key/2: apply(key_resolver_module, :resolve, [connection])
    8. All OTP crypto (PEM decode, RSA unwrap, AES-GCM decrypt) inside do_decrypt/4 private function with outermost rescue _ -> :decryption_failed
  </behavior>
  <action>
    Create defmodule Relyra.Security.XMLEnc at lib/relyra/security/xml_enc.ex. @moduledoc false. Aliases: Relyra.Security.AlgorithmPolicy, Relyra.Security.XML.SaxyTree.

    Define URI module attributes at the top: @rsa_oaep_uri, @aes128_gcm_uri, @aes256_gcm_uri as string literals from the XMLEnc spec.

    Public function: @spec decrypt(binary(), module(), keyword()) :: {:ok, binary()} | :decryption_failed with two clauses: main clause guarding is_binary + is_atom + is_list; catch-all clause returns :decryption_failed.

    Main clause body: read policy via Keyword.get(opts, :algorithm_policy, AlgorithmPolicy.default()); read connection via Keyword.get(opts, :connection, %{}); use a with chain calling: parse_enc_fields/1 -> check_key_transport/2 -> b64_decode both cipher values (fail else -> :decryption_failed) -> split_cipher_value/1 on content bytes -> check_content_encryption/3 with auth_tag: auth_tag -> resolve_key/2; then call do_decrypt/4 with the unwrapped values.

    All with-else branches return :decryption_failed.

    Private functions:

    parse_enc_fields/1: calls SaxyTree.parse(bytes), then traverses by local name to find EncryptedData, KeyInfo, EncryptedKey, extracts both Algorithm URIs and both CipherValue texts. Returns {:ok, %{key_transport_alg:, content_alg:, key_cipher_value:, content_cipher_value:}} or :decryption_failed in else block.

    find_first/2: depth-first traversal matching node.local == target name; returns node or nil. Recursive over node.children.

    enc_method_alg/1: find_first(node, "EncryptionMethod"), then Enum.find_value on attrs for {"Algorithm", v}.

    cipher_value_text/1: find_first CipherData then CipherValue, return .text.

    split_cipher_value/1: guard byte_size(bytes) >= 28 else return :decryption_failed; binary match IV(12), rest, then ct_size = byte_size(rest) - 16, final match ciphertext(ct_size) + tag(16); return {:ok, iv, ciphertext, auth_tag}.

    check_key_transport/2: call AlgorithmPolicy.enforce_key_transport_algorithm/2; return :ok on :ok, :decryption_failed on %Error{}.

    check_content_encryption/3: call AlgorithmPolicy.enforce_content_encryption_algorithm/3 with auth_tag: auth_tag; return :ok on :ok, :decryption_failed on %Error{} or :decryption_failed.

    resolve_key/2: call apply(key_resolver_module, :resolve, [connection]) in rescue; return {:ok, pem} when is_binary(pem); return :decryption_failed for any other result or exception.

    do_decrypt/4 (key_cipher_b64, pem, content_cipher_bytes, content_alg): outermost rescue _ -> :decryption_failed wrapping the entire body. Inside: b64_decode key_cipher_b64 -> decode_pem_key/1 -> unwrap_cek/2 -> split_cipher_value on content bytes -> cipher_atom/1 -> case :crypto.crypto_one_time_aead(cipher, cek, iv, ciphertext, <<>>, auth_tag, false) of plaintext when is_binary -> {:ok, plaintext}; :error -> :decryption_failed.

    decode_pem_key/1: :public_key.pem_decode(pem) in rescue; match [entry | _] and call :public_key.pem_entry_decode(entry); return {:ok, key_term}; catch-all or empty list returns :decryption_failed.

    unwrap_cek/2: :public_key.decrypt_private(encrypted_cek, private_key, [{:rsa_padding, :rsa_pkcs1_oaep_padding}]) in rescue; wrap result as {:ok, cek}; rescue -> :decryption_failed.

    cipher_atom/1: @aes128_gcm_uri -> {:ok, :aes_128_gcm}; @aes256_gcm_uri -> {:ok, :aes_256_gcm}; _ -> :decryption_failed.

    b64_decode/1: Base.decode64(value, ignore: :whitespace) when is_binary(value); catch-all -> :decryption_failed.

    Critical constraints: no second XML parse path (no :xmerl, no regex); KeyInfo contents silently ignored (D-05); document KeyInfo attr never used for key lookup; PEM binary and decoded private key term must not appear in any map key under :private_key, :pem, :sp_private_key_pem, or similar atom that flows to Logger or telemetry.
  </action>
  <verify>
    <automated>mix test test/security/xml_enc_test.exs --warnings-as-errors</automated>
  </verify>
  <done>
    lib/relyra/security/xml_enc.ex exists; contains def decrypt(encrypted_assertion_bytes, key_resolver_module, opts); function spec declares return type {:ok, binary()} | :decryption_failed; no clause returns {:error, _} or raises; mix test --warnings-as-errors exits 0 (full suite).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: xml_enc_test.exs 4-case corpus + ci.security registration</name>
  <files>test/security/xml_enc_test.exs, mix.exs, test/security/ci_gate_integrity_test.exs</files>
  <read_first>
    - test/security/adversarial_crypto_test.exs — corpus test structure: module setup, describe blocks, individual test bodies, how FakeIdP.keypair() and TestSupport are used
    - test/support/fake_idp.ex — keypair/0 return shape; how to extract the RSAPublicKey {:RSAPublicKey, n, e} from {:RSAPrivateKey, _, n, e, ...}; PEM encoding pattern used in xmldsig_signer.ex
    - test/support/xmldsig_signer.ex — :public_key.pem_encode/1 usage; :public_key.encrypt_public/3 pattern for OAEP test setup
    - mix.exs — current ci.security alias last lines (insert new cmd line before deps.audit)
    - test/security/ci_gate_integrity_test.exs — @gated_suites list (add {"test/security/xml_enc_test.exs", nil})
    - lib/relyra/security/xml_enc.ex — decrypt/3 spec to confirm exact arguments for test assertions
  </read_first>
  <behavior>
    test/security/xml_enc_test.exs:
    - RSA-PKCS1v1.5 test: XMLEnc.decrypt(pkcs1_bytes, resolver, []) == :decryption_failed
    - AES-CBC test: XMLEnc.decrypt(cbc_bytes, resolver, []) == :decryption_failed
    - Truncated GCM auth tag test: XMLEnc.decrypt(truncated_tag_bytes, resolver, []) == :decryption_failed
    - Malformed ciphertext test: XMLEnc.decrypt(malformed_bytes, resolver, []) == :decryption_failed
    - All four tests use the same resolver module (KeyResolver.Default configured with an RSA-2048 PEM from FakeIdP.keypair())

    mix.exs ci.security alias:
    - Contains "cmd mix test test/security/xml_enc_test.exs --warnings-as-errors" as its own line
    - The new line appears before "deps.audit --ignore-advisory-ids ..." (following the pattern of other security suite lines)

    ci_gate_integrity_test.exs:
    - @gated_suites includes {"test/security/xml_enc_test.exs", nil}
    - mix test test/security/ci_gate_integrity_test.exs --warnings-as-errors exits 0 after changes
  </behavior>
  <action>
    Create test/security/xml_enc_test.exs as defmodule Relyra.Security.XMLEncTest with use ExUnit.Case, async: true.

    In setup: generate RSA-2048 keypair using FakeIdP.keypair(). Extract public key: {:RSAPrivateKey, _, n, e, _, _, _, _, _, _, _} = keypair; pub_key = {:RSAPublicKey, n, e}. PEM-encode private key: pem = :public_key.pem_encode([{:RSAPrivateKey, :public_key.der_encode(:RSAPrivateKey, keypair), :not_encrypted}]). Configure Application.put_env(:relyra, :sp_private_key_pem, pem) in setup; restore with Application.delete_env on_exit. Define resolver = Relyra.KeyResolver.Default.

    Helper function build_encrypted_assertion/2 or inline helpers that construct minimal EncryptedAssertion XML bytes:
    - Valid structure: build_assertion_xml(key_transport_alg_uri, content_alg_uri, enc_key_b64, cipher_value_b64) -> XML binary. Use a simple string interpolation template — NOT a full SAML document, just enough XML for SaxyTree.parse to produce the EncryptedData/EncryptedKey tree.
    - The function produces a binary like: "<EncryptedAssertion><xenc:EncryptedData xmlns:xenc=\"...\"><xenc:EncryptionMethod Algorithm=\"{content_uri}\"/><ds:KeyInfo xmlns:ds=\"...\"><xenc:EncryptedKey><xenc:EncryptionMethod Algorithm=\"{key_uri}\"/><xenc:CipherData><xenc:CipherValue>{enc_key_b64}</xenc:CipherValue></xenc:CipherData></xenc:EncryptedKey></ds:KeyInfo><xenc:CipherData><xenc:CipherValue>{cipher_val_b64}</xenc:CipherValue></xenc:CipherData></xenc:EncryptedData></EncryptedAssertion>"

    For the 4 test cases:

    Case 1 (RSA-PKCS1v1.5): Use key_transport_alg = "http://www.w3.org/2001/04/xmlenc#rsa-1_5" as the EncryptedKey Algorithm URI. Content algorithm and cipher value can be valid AES-GCM. Encrypt any 32-byte CEK using :public_key.encrypt_public with {:rsa_padding, :rsa_pkcs1_padding} for the key CipherValue (or simply put any base64 — the AlgorithmPolicy gate fires before decryption attempts). Assert XMLEnc.decrypt(bytes, resolver, []) == :decryption_failed.

    Case 2 (AES-CBC): Use content_alg = "http://www.w3.org/2001/04/xmlenc#aes256-cbc" as EncryptedData Algorithm URI. Key transport can be valid OAEP URI. Cipher value base64 can be random bytes of length >= 28. Assert XMLEnc.decrypt(bytes, resolver, []) == :decryption_failed.

    Case 3 (truncated GCM auth tag): Use valid OAEP key transport URI and valid AES-256-GCM content URI. Construct content CipherValue bytes as IV(12) || Ciphertext(1) || Tag(15) = 28 bytes - 1 = 27 bytes total, then base64-encode. Or use 12 + any_bytes where total < 28. Assert XMLEnc.decrypt(bytes, resolver, []) == :decryption_failed.

    Case 4 (malformed ciphertext): Use valid OAEP and AES-256-GCM URIs. Set content CipherValue to "not!valid!base64!!!!" (invalid base64 chars). Assert XMLEnc.decrypt(bytes, resolver, []) == :decryption_failed.

    In mix.exs: In the ci.security alias list, insert the string "cmd mix test test/security/xml_enc_test.exs --warnings-as-errors" on its own line immediately before the "deps.audit --ignore-advisory-ids GHSA-rhv4-8758-jx7v" line. Do not change any other line in the alias.

    In test/security/ci_gate_integrity_test.exs: Add {"test/security/xml_enc_test.exs", nil} to the @gated_suites list. The nil tag means the whole file runs with no --only filter — matching Pitfall 6 guidance in RESEARCH.md.

    Do not add @moduletag to xml_enc_test.exs since the gated_suites entry uses nil tag (whole-file run, no --only filter needed).
  </action>
  <verify>
    <automated>mix test test/security/xml_enc_test.exs --warnings-as-errors && mix test test/security/ci_gate_integrity_test.exs --warnings-as-errors</automated>
  </verify>
  <done>
    test/security/xml_enc_test.exs has 4 tests (PKCS1v1.5, AES-CBC, truncated tag, malformed ciphertext); all 4 assert == :decryption_failed; mix test test/security/xml_enc_test.exs --warnings-as-errors exits 0; mix.exs ci.security alias contains "cmd mix test test/security/xml_enc_test.exs --warnings-as-errors" as its own cmd line; @gated_suites in ci_gate_integrity_test.exs contains {"test/security/xml_enc_test.exs", nil}; mix test test/security/ci_gate_integrity_test.exs --warnings-as-errors exits 0; mix test --warnings-as-errors exits 0 (full suite).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Encrypted assertion bytes → SaxyTree | Raw untrusted bytes from the network cross into the parse layer; pre-parse guards (DTD/entity/size) already run in PureBeam before this path; XMLEnc trusts that callers have applied pre-parse guards, or the parse simply fails closed |
| Algorithm URI → AlgorithmPolicy | Document-supplied algorithm URIs cross into the policy gate; any non-allowlisted URI is rejected before crypto begins |
| KeyResolver → private key material | SP private key PEM is returned from the configured adapter; never sourced from the assertion document; decoded key term stays in defp function scope |
| do_decrypt → plaintext bytes | Decrypted plaintext is returned as {:ok, binary()} ONLY; Phase 34 is responsible for re-parsing through PureBeam + do_verify before reading any identity field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02-01 | Information Disclosure | RSA-PKCS1v1.5 padding oracle | mitigate | AlgorithmPolicy.enforce_key_transport_algorithm/2 hard-blocks the PKCS1v1.5 URI; decrypt/3 returns :decryption_failed before any RSA operation |
| T-33-02-02 | Information Disclosure | Truncated GCM auth tag oracle | mitigate | enforce_content_encryption_algorithm/3 with auth_tag: fires BEFORE :crypto.crypto_one_time_aead/7; byte_size < 16 returns :decryption_failed opaquely |
| T-33-02-03 | Information Disclosure | Error oracle via distinct error returns | mitigate | All failure paths (parse fail, policy reject, key fail, RSA fail, AEAD fail) return the same :decryption_failed atom; no structured {:error, _} return from decrypt/3 |
| T-33-02-04 | Tampering | Malformed PEM causes exception escape | mitigate | Outermost rescue _ -> :decryption_failed in do_decrypt/4 wraps all OTP crypto calls including pem_decode/pem_entry_decode/decrypt_private |
| T-33-02-05 | Elevation of Privilege | Document KeyInfo trust | mitigate | KeyInfo inside EncryptedKey is silently ignored (D-05); private key sourced exclusively from KeyResolver callback, never from document-supplied key material |
| T-33-02-06 | Tampering | Algorithm confusion (CBC vs GCM) | mitigate | AlgorithmPolicy blocks AES-CBC URI by default; no escape hatch in default policy |
| T-33-02-07 | Information Disclosure | Private key leakage via logs/telemetry | mitigate | PEM binary and decoded RSAPrivateKey term remain as local variables inside defp functions; never assigned to a map key passed to Logger, telemetry, or Error.new/3 details |
| T-33-02-08 | Tampering | Second XML parse via :xmerl or regex bypass | mitigate | XMLEnc uses only SaxyTree.parse/1 (the single hardened parse seam); no :xmerl, no regex, no secondary parse |
| T-33-02-09 | Tampering | Plaintext bytes read before re-verify | accept | Phase 33 returns {:ok, plaintext_bytes} without identity field access; Phase 34 is responsible for the decrypt-then-reparse+verify invariant; no identity fields are readable from XMLEnc output |
| T-33-02-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this plan; all crypto is OTP stdlib (no new Hex dependencies) |
</threat_model>

<verification>
After both tasks complete:

```
mix test --warnings-as-errors
mix ci.security
mix format --check-formatted
```

All three must exit 0.

Spot-check specific gates:
- mix test test/security/xml_enc_test.exs --warnings-as-errors (4 tests, all green)
- mix test test/security/ci_gate_integrity_test.exs --warnings-as-errors (meta-gate green)
- mix test test/relyra/key_resolver_test.exs --warnings-as-errors (Plan 01 tests still green)
</verification>

<success_criteria>
1. XMLEnc.decrypt/3 returns {:ok, plaintext} for a valid RSA-OAEP + AES-GCM input; returns :decryption_failed for all four adversarial inputs.
2. Document KeyInfo is never used for key lookup; private key sourced only from key_resolver_module argument.
3. All 4 corpus cases in xml_enc_test.exs return :decryption_failed with assert ==.
4. mix.exs ci.security alias contains "cmd mix test test/security/xml_enc_test.exs --warnings-as-errors" as a standalone cmd line.
5. @gated_suites in ci_gate_integrity_test.exs contains {"test/security/xml_enc_test.exs", nil}.
6. mix ci.security exits 0 (includes meta-gate and new xml_enc suite).
7. mix test --warnings-as-errors exits 0 (full suite green, no new warnings).
</success_criteria>

<output>
Create `.planning/phases/33-key-resolver-behaviour-xmlenc-crypto-core/33-02-SUMMARY.md` when done.
</output>
