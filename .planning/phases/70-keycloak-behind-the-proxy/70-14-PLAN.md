---
phase: 70-keycloak-behind-the-proxy
plan: "14"
type: execute
wave: 11
depends_on:
  - 70-13
files_modified:
  - scripts/test_trace_visual_e2e.sh
  - playwright.trace-visual.config.mjs
  - demo/ledger_loop/test/browser/trace_visual.spec.ts
  - package.json
  - .github/workflows/demo-app-e2e.yml
autonomous: true
gap_closure: true
gap_ids: [G-70-2]
requirements:
  - KC-01
must_haves:
  truths:
    - "G-70-2: one deterministic FakeIdP tamper journey proves ACS 400, typed `digest_mismatch`, no workspace, no verified receipt, and operable Back recovery in authenticated Login Trace."
    - "G-70-2 and D-25: executable Chromium assertions cover native details keyboard behavior, a narrow viewport with horizontally accessible evidence, and visible safe long cause/error-code values."
    - "D-26: ephemeral Basic credentials stay in process environment only; trace, screenshot, video, retry capture, HTML reports, and retained browser output are disabled or deleted on every exit."
    - "The existing demo-app E2E workflow runs the visual/failure proof on pull requests and main without adding a human checkpoint or credential-bearing artifact upload."
  artifacts:
    - path: scripts/test_trace_visual_e2e.sh
      provides: "credential and temporary-output lifecycle for the deterministic visual gate"
    - path: playwright.trace-visual.config.mjs
      provides: "attachment-free self-booting Chromium project"
    - path: demo/ledger_loop/test/browser/trace_visual.spec.ts
      provides: "failed-state, recovery, keyboard, viewport, and long-value assertions"
    - path: .github/workflows/demo-app-e2e.yml
      provides: "recurring execution in the existing deterministic browser lane"
  key_links:
    - from: scripts/test_trace_visual_e2e.sh
      to: playwright.trace-visual.config.mjs
      via: "ephemeral Basic credentials, opt-in seed flag, and validated temporary output directory"
      pattern: "DEMO_TRACE_VISUAL_FIXTURE"
    - from: demo/ledger_loop/test/browser/trace_visual.spec.ts
      to: lib/relyra/live_admin/connection_trace_live.ex
      via: "stable trace/details/step/Back/overflow selectors"
      pattern: "login-trace"
    - from: .github/workflows/demo-app-e2e.yml
      to: package.json
      via: "`npm run demo:trace-visual`"
      pattern: "demo:trace-visual"
---

<objective>
Close G-70-2 with a zero-human, credential-safe browser acceptance gate and recurring CI execution.

Purpose: The failed destination, recovery, keyboard, narrow-viewport, and long-evidence expectations must be executable repository contracts rather than prose backstops.
Output: Ephemeral harness, dedicated Playwright project/spec, npm command, and demo E2E CI step.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/70-keycloak-behind-the-proxy/70-UI-SPEC.md
@.planning/phases/70-keycloak-behind-the-proxy/70-UAT.md
@.planning/debug/phase-70-visual-failure-automation-gap.md
@.planning/phases/70-keycloak-behind-the-proxy/70-13-SUMMARY.md
@demo/ledger_loop/test/browser/fake_idp.spec.ts
@demo/ledger_loop/test/browser/keycloak.spec.ts
@playwright.fake-idp.config.mjs
@playwright.keycloak-proxy.config.mjs
@.github/workflows/demo-app-e2e.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Automate the failed Login Trace visual contract without retaining credentials</name>
  <files>scripts/test_trace_visual_e2e.sh, playwright.trace-visual.config.mjs, demo/ledger_loop/test/browser/trace_visual.spec.ts, package.json</files>
  <behavior>
    - "The harness generates an ephemeral non-empty demo-admin password, enables only the safe visual fixture, creates a private temporary Playwright output directory, and deletes that validated directory on success, failure, interrupt, and termination."
    - "The project selects only `trace_visual.spec.ts`, self-boots LedgerLoop, uses Chromium/list output/one worker/no retries, and disables trace, screenshot, and video capture."
    - "The spec performs the real FakeIdP tamper POST before authenticated trace inspection and proves failure, recovery, native details keyboard toggling, narrow-width horizontal evidence access, and safe long rendered values."
  </behavior>
  <action>
    Add `scripts/test_trace_visual_e2e.sh` as the lifecycle owner. Generate a per-run `DEMO_ADMIN_PASSWORD` with `openssl`, export a non-sensitive username, set `DEMO_TRACE_VISUAL_FIXTURE=1`, create a `umask 077` temporary directory under an exact `relyra-trace-visual-playwright.*` prefix, and install a trap that validates the candidate is under the expected temporary parent before deleting it for every exit path. Forward any CLI arguments after the Playwright config selection so `npm run demo:trace-visual -- --list` exercises test discovery through the same credential/output lifecycle. The script must emit no credential value and retain no browser artifact.

    Create `playwright.trace-visual.config.mjs` selecting only `trace_visual.spec.ts`, using the temporary output directory from the harness, list reporter, one worker, zero retries, and `trace`, `screenshot`, and `video` all off. Reuse the FakeIdP project's self-booting `mix ecto.setup &amp;&amp; mix phx.server` model and inherited environment, but do not extend `playwright.fake-idp.config.mjs`, whose retry trace policy is unsuitable for Basic authorization. Add `demo:trace-visual` in `package.json` pointing to the harness.

    In the new spec, first drive the actual `Simulate Login via FakeIdP` tamper selection and scoped ACS POST. Assert HTTP 400, exact typed `digest_mismatch`, no workspace, then visit the workspace route and prove no verified receipt is present. Only after that failure, require the ephemeral admin env values, construct the Basic header in memory, and navigate to the enabled connection's trace. Select the newest failed row, assert the typed rejection, focus its native summary and use keyboard activation to close and reopen it, and follow `Back to connection` to the correct detail route. Set a narrow viewport, assert the page itself does not clip evidence, focus the labelled overflow region, prove its scroll width exceeds its client width and that horizontal scrolling exposes the far columns. Visit the opt-in support fixture trace and assert the safe long cause and error-code values remain visible. Never print authorization, response bodies, SAML form fields, cookies, or credentials.
  </action>
  <verify>
    <automated>bash -n scripts/test_trace_visual_e2e.sh &amp;&amp; npm run demo:trace-visual -- --list &amp;&amp; npm run demo:trace-visual</automated>
  </verify>
  <done>The deterministic browser command exercises every G-70-2 acceptance state and leaves no credential-bearing browser output behind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the trace visual gate in the existing demo E2E workflow</name>
  <files>.github/workflows/demo-app-e2e.yml</files>
  <behavior>
    - "The existing PostgreSQL/BEAM/Node/Chromium job executes both the ordinary FakeIdP browser lane and the dedicated trace-visual lane."
    - "The workflow adds no secret, screenshot, trace, video, HTML report, or output-directory upload."
    - "Planning-only changes remain ignored consistently with the current workflow policy."
  </behavior>
  <action>
    Add a distinct `Trace visual and failed-state browser e2e` step to `.github/workflows/demo-app-e2e.yml` after the existing FakeIdP browser step, invoking `npm run demo:trace-visual`. Reuse the job's PostgreSQL service, BEAM dependencies, Node install, and Chromium install; do not create a second dependency installation or a separate credential secret. Keep the workflow's current pull-request/main triggers and path ignores. Do not upload test artifacts because the harness intentionally deletes all browser output and the command uses ephemeral Basic credentials.
  </action>
  <verify>
    <automated>ruby -e 'require "yaml"; YAML.load_file(".github/workflows/demo-app-e2e.yml"); puts "workflow yaml parsed"' &amp;&amp; rg -n 'demo:fake-idp|demo:trace-visual' .github/workflows/demo-app-e2e.yml</automated>
  </verify>
  <done>Every applicable pull request and main push runs the deterministic failure, recovery, keyboard, viewport, and long-value proof with zero human UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FakeIdP/ACS → failed browser destination | A deliberately tampered signed response must be rejected without receipt or workspace success. |
| Playwright → Basic-auth LiveAdmin | Ephemeral credentials cross into the authenticated operator evidence surface. |
| Login Trace DOM → narrow browser viewport | Long security evidence must remain visible and operable without page-wide clipping. |
| CI runner → retained artifacts/logs | Credential-bearing browser state must not survive the test process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-43 | Spoofing | failed-state assertion | high | mitigate | Drive the real FakeIdP tamper path and exact ACS 400/typed error before opening the authenticated trace. |
| T-70-44 | Elevation of privilege | LiveAdmin Basic auth | high | mitigate | Generate non-empty credentials per run, keep them in process memory/environment, and preserve the existing guarded router. |
| T-70-45 | Information disclosure | Playwright output | high | mitigate | Disable every attachment channel, use list-only output/no retries, validate temp ownership, and delete output in the trap. |
| T-70-46 | Repudiation | visual acceptance | medium | mitigate | Assert stable semantic selectors, exact typed rejection, Back destination, native details state, geometry, scrollability, and safe long values. |
| T-70-47 | Denial of service | long evidence at narrow width | medium | mitigate | Prove page bounds plus keyboard-focusable horizontal access to every table column. |
</threat_model>

<source_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Optional public Keycloak profile completes the real signed proxy round trip | 01-11 | COVERED | Plans 01-10 built it; Plan 11 makes its scenario gate truthful and recurring. |
| REQ | KC-01 | Correct public Keycloak host/URLs and successful verified round trip | 01-14 | COVERED | Existing implementation plus automated acceptance/failure regression. |
| RESEARCH | — | Fixed hostname, private bootstrap, audited descriptor trust, exact ACS/receipt/trace | 01-11 | COVERED | No trust or topology change in gap closure. |
| RESEARCH | — | Host-side Playwright and redacted diagnostics | 07, 08, 11, 14 | COVERED | Credential-bearing projects disable attachments and own ephemeral output. |
| CONTEXT | D-01..D-21 | Stable audited Keycloak topology, provisioning, and genuine proof | 01-11 | COVERED | Implemented by executed plans; preserved by focused gate. |
| CONTEXT | D-22 | Independent deterministic FakeIdP tamper lane | 05, 14 | COVERED | G-70-2 composes the real tamper path without XML mutation. |
| CONTEXT | D-23..D-25 | Truthful accessible affordance, receipt, and reused Login Trace | 04, 05, 13, 14 | COVERED | Only the demonstrably required trace overflow markup changes in `lib/`. |
| CONTEXT | D-26 | Layered actionable failures without sensitive retention | 07, 10, 11, 14 | COVERED | Both Keycloak and visual harnesses remain attachment-free. |
| UAT | G-70-1 | Separate focused Keycloak acceptance from advisory remediation and add recurring proof | 11, 12 | COVERED | Plans are independent in Wave 10; no advisory suppression. |
| UAT | G-70-2 | Automate failure, recovery, keyboard, narrow viewport, and long values | 13, 14 | COVERED | Deterministic fixture/presentation precedes browser/CI proof. |

Phase 71 launcher/banner, Phase 72 documentation, TLS, hashed hosts, production Keycloak deployment, and browser-cookie session work remain explicitly deferred and are excluded from this gap set.
</source_audit>

<verification>
Run `npm run demo:trace-visual`, confirm the temporary output directory is absent afterward, parse the modified workflow, and run the focused ExUnit/render checks from Plan 70-13.
</verification>

<success_criteria>
Every G-70-2 expectation is an executable Chromium assertion in recurring CI, with no manual checkpoint and no retained credential-bearing artifact.
</success_criteria>

<output>
Create `.planning/phases/70-keycloak-behind-the-proxy/70-14-SUMMARY.md` when done.
</output>
