---
phase: 70-keycloak-behind-the-proxy
plan: "11"
type: execute
wave: 10
depends_on:
  - 70-10
files_modified:
  - scripts/test_keycloak_proxy_e2e.sh
  - .github/workflows/keycloak-proxy-e2e.yml
autonomous: true
gap_closure: true
gap_ids: [G-70-1]
requirements:
  - KC-01
must_haves:
  truths:
    - "G-70-1: `npm run demo:keycloak-proxy` reports the focused public Keycloak/Traefik/scoped-ACS scenario independently from repository-wide QA and dependency-security status."
    - "G-70-1: the recurring Keycloak job is the CI owner for the real public-host integration proof, while `.github/workflows/security-gates.yml` remains the owner of `mix ci.security` and dependency advisories."
    - "D-22 and D-26: the scenario continues to prove genuine signed acceptance, durable receipt, and exact trace evidence without weakening FakeIdP, crypto, replay, or diagnostic redaction controls."
  artifacts:
    - path: scripts/test_keycloak_proxy_e2e.sh
      provides: "focused Keycloak lifecycle and browser acceptance gate"
    - path: .github/workflows/keycloak-proxy-e2e.yml
      provides: "recurring Docker and Chromium execution of the only public Keycloak integration proof"
  key_links:
    - from: .github/workflows/keycloak-proxy-e2e.yml
      to: scripts/test_keycloak_proxy_e2e.sh
      via: "`npm run demo:keycloak-proxy`"
      pattern: "demo:keycloak-proxy"
    - from: scripts/test_keycloak_proxy_e2e.sh
      to: demo/ledger_loop/test/browser/keycloak.spec.ts
      via: "attachment-free Playwright project after live proxy and descriptor readiness"
      pattern: "playwright.keycloak-proxy.config.mjs"
---

<objective>
Close the scenario-gate half of G-70-1 by making the public Keycloak acceptance command truthful and recurring.

Purpose: The live journey is behavior-green; its exit status must describe that scenario rather than duplicate repository-wide gates already owned by required security CI.
Output: A focused Keycloak harness and a dedicated recurring Keycloak-proxy workflow.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
@.planning/phases/70-keycloak-behind-the-proxy/70-UAT.md
@.planning/debug/phase-70-keycloak-gate-not-green.md
@.planning/phases/70-keycloak-behind-the-proxy/70-10-SUMMARY.md
@scripts/test_keycloak_proxy_e2e.sh
@playwright.keycloak-proxy.config.mjs
@.github/workflows/security-gates.yml
@.github/workflows/fleet-proxy-e2e.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the Keycloak command a focused end-to-end acceptance gate</name>
  <files>scripts/test_keycloak_proxy_e2e.sh</files>
  <behavior>
    - "A live run still renders both public-host contracts, starts owned proxy/Keycloak resources, provisions descriptor-derived trust, completes Sarah's genuine signed ACS journey, and proves one receipt plus the exact validation/signature/replay trace."
    - "A live run returns success when those scenario checks pass regardless of separately evaluated root dependency advisories."
    - "Static, diagnostic-redaction, artifact-policy, forced-failure, and owned-resource cleanup modes remain executable."
  </behavior>
  <action>
    Refactor the final phase-gate sequence in `scripts/test_keycloak_proxy_e2e.sh` according to G-70-1's diagnosed boundary. Retain the rendered topology/realm checks, owned Compose lifecycle, internal Keycloak readiness, descriptor provisioner wait, attachment-free browser authentication, ACS canonical-step query, user mapping, durable receipt, layered failure classification, redacted diagnostic promotion, and cleanup. Remove the independently owned host FakeIdP regression, focused host ExUnit invocation, and root warnings/QA/security/format invocations from this scenario command so its exit status means only “the public Keycloak integration passed.” Do not add advisory ignores, alter `mix ci.security`, change any SAML/security implementation, or weaken D-22/D-26 artifact and redaction self-tests. Update the terminal success message so it names the focused scenario evidence and does not claim repository-wide QA.
  </action>
  <verify>
    <automated>bash -n scripts/test_keycloak_proxy_e2e.sh &amp;&amp; KEYCLOAK_PROXY_STATIC_ONLY=1 bash scripts/test_keycloak_proxy_e2e.sh &amp;&amp; KEYCLOAK_PROXY_ARTIFACT_POLICY_SELF_TEST=1 bash scripts/test_keycloak_proxy_e2e.sh &amp;&amp; npm run demo:keycloak-proxy</automated>
  </verify>
  <done>The command exits from Keycloak-specific topology, browser ACS, receipt, trace, diagnostics, and cleanup assertions only, and the live scenario passes end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the recurring public Keycloak integration workflow</name>
  <files>.github/workflows/keycloak-proxy-e2e.yml</files>
  <behavior>
    - "Pull requests, pushes to main, a bounded schedule, and workflow dispatch can run the focused Keycloak scenario."
    - "The job installs Node dependencies and Chromium, then invokes exactly `npm run demo:keycloak-proxy` with a timeout."
    - "The workflow does not upload browser or credential-bearing artifacts; the harness generates ephemeral admin credentials and retains only its existing policy-validated redacted text locally during the run."
  </behavior>
  <action>
    Create `.github/workflows/keycloak-proxy-e2e.yml` as the single recurring owner of the real Keycloak/Traefik/ACS proof. Follow the established `fleet-proxy-e2e.yml` checkout, Node, `npm ci`, and Playwright Chromium setup, then run only `npm run demo:keycloak-proxy` in a named job with `contents: read` and a bounded timeout. Trigger on pull requests, pushes to `main`, a daily schedule offset from the fleet job, and manual dispatch because this cross-container public-host contract has recurring regression value. Do not add artifact upload steps: the Keycloak project is credential-bearing, its Playwright attachments remain disabled per Plan 70-07, and its harness already controls ephemeral browser output plus redacted text diagnostics. Leave `.github/workflows/security-gates.yml` unchanged as the repository dependency/security owner.
  </action>
  <verify>
    <automated>ruby -e 'require "yaml"; YAML.load_file(".github/workflows/keycloak-proxy-e2e.yml"); puts "workflow yaml parsed"' &amp;&amp; rg -n 'demo:keycloak-proxy|schedule|workflow_dispatch|timeout-minutes' .github/workflows/keycloak-proxy-e2e.yml</automated>
  </verify>
  <done>The repository has one recurring, artifact-free CI job whose sole acceptance command is the focused public Keycloak scenario.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub runner → Docker public-host topology | CI drives real Traefik, Keycloak, LedgerLoop, and Chromium traffic on an ephemeral runner. |
| Scenario harness → repository gates | Focused integration status must not impersonate or suppress independently required security status. |
| Credential-bearing browser → CI logs/artifacts | Ephemeral Keycloak and demo-admin secrets must not persist after the run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-31 | Repudiation | scenario exit status | high | mitigate | Limit the command to exact topology, signed ACS, receipt, and trace assertions; leave repo gates visibly separate. |
| T-70-32 | Tampering | dependency advisory handling | high | mitigate | Do not add ignores or edit the security workflow; Plan 70-12 remediates the lock independently. |
| T-70-33 | Information disclosure | CI browser credentials | high | mitigate | Keep attachments disabled, browser output ephemeral, redaction self-tests intact, and omit workflow uploads. |
| T-70-34 | Denial of service | Docker CI lifecycle | medium | mitigate | Use owned project names, existing cleanup traps, and a bounded workflow timeout. |
</threat_model>

<verification>
Run the three Docker-free harness modes, then `npm run demo:keycloak-proxy`; parse the workflow YAML and confirm the job invokes only the focused scenario command.
</verification>

<success_criteria>
G-70-1's genuine Keycloak behavior has a truthful local exit status and recurring CI owner, with repository dependency/security status remaining independently visible and enforced.
</success_criteria>

<output>
Create `.planning/phases/70-keycloak-behind-the-proxy/70-11-SUMMARY.md` when done.
</output>
