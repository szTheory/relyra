---
phase: 70-keycloak-behind-the-proxy
plan: "06"
type: execute
wave: 5
depends_on:
  - 70-05
files_modified:
  - demo/ledger_loop/lib/ledger_loop/demo/keycloak_provisioner.ex
  - demo/ledger_loop/test/ledger_loop/demo/keycloak_provisioner_test.exs
autonomous: true
gap_closure: true
requirements:
  - KC-01
must_haves:
  truths:
    - "D-03 D-05: one guarded metadata parse produces one canonical Relyra.Metadata.Candidate that supplies issuer, SSO URL, certificate fingerprints, unchanged detection, and audited persistence."
    - "D-05 D-06: changed descriptor trust still flows through MetadataApply, certificate inventory, and connection command transactions with actor/cause/correlation audit context, and enable remains last."
    - "Assertion-time document KeyInfo remains unusable; this demo-only descriptor bootstrap changes no lib/relyra security seam, public API, algorithm policy, replay gate, or FakeIdP lane."
    - "Malformed, wrong-issuer, apply, activation, and identity failures leave the stable Keycloak connection unavailable and create no LoginReceipt."
  artifacts:
    - path: demo/ledger_loop/lib/ledger_loop/demo/keycloak_provisioner.ex
      provides: "single-candidate descriptor preflight and audited metadata apply"
      contains: "MetadataApply.apply_revision"
    - path: demo/ledger_loop/test/ledger_loop/demo/keycloak_provisioner_test.exs
      provides: "one-parse, candidate identity, audit co-commit, idempotency, and fail-closed regression coverage"
      contains: "descriptor_parser"
  key_links:
    - from: demo/ledger_loop/lib/ledger_loop/demo/keycloak_provisioner.ex
      to: lib/relyra/metadata/parser.ex
      via: "the fetched raw descriptor crosses the guarded parser seam exactly once"
      pattern: "Parser.parse"
    - from: demo/ledger_loop/lib/ledger_loop/demo/keycloak_provisioner.ex
      to: lib/relyra/ecto/metadata_apply.ex
      via: "the same canonical candidate used for preflight is converted with Map.from_struct and applied through the audited metadata transaction"
      pattern: "MetadataApply.apply_revision"
    - from: demo/ledger_loop/lib/ledger_loop/demo/keycloak_provisioner.ex
      to: lib/relyra/ecto/certificate_inventory.ex
      via: "fingerprints derived from that candidate select imported signing trust for activation and stale-trust retirement"
      pattern: "activate_signing_certificate|retire_signing_certificate"
  prohibitions:
    - statement: "MUST NOT parse the same raw Keycloak descriptor more than once or introduce a parser differential between preflight checks and persisted trust."
      status: unresolved
      verification: null
    - statement: "MUST NOT write connection, metadata, or certificate trust state directly outside the existing audited command transactions."
      status: unresolved
      verification: null
    - statement: "MUST NOT trust assertion document KeyInfo; assertion verification continues to use only configured IdP certificates."
      status: unresolved
      verification: null
    - statement: "MUST NOT modify lib/relyra, a public API, behavior callback, security policy, protocol surface, or the Hex package whitelist."
      status: unresolved
      verification: null
  flagged_assumptions:
    - id: EDGE-KC-01-BOUNDARY
      status: inapplicable
      statement: "This closure introduces no numeric min/max threshold; metadata byte-size and XML pre-parse limits remain the existing Parser contract, while issuer/fingerprint equality remains exact."
    - id: EDGE-KC-01-PRECISION
      status: inapplicable
      statement: "This closure performs no arithmetic, rounding, overflow-prone conversion, or tie-breaking; SHA-256 fingerprints and content hashes are exact byte-derived values."
---

<objective>
Close the descriptor trust-path blocker by deriving and persisting trust from one canonical parsed metadata candidate.

Purpose: Restore Relyra's non-negotiable one-parse invariant without changing `lib/`, bypassing audited mutation seams, relaxing configured-certificate trust, or widening the demo-only Phase 70 boundary.
Output: A single-candidate Keycloak provisioner path plus focused executable regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
@.planning/phases/70-keycloak-behind-the-proxy/70-VERIFICATION.md
@.planning/phases/70-keycloak-behind-the-proxy/70-02-SUMMARY.md

<interfaces>
Existing seams to use without changing their public shape:
- `Relyra.Metadata.Parser.parse(xml, opts \\ []) :: {:ok, parsed_map} | {:error, Relyra.Error.t()}`
- `Relyra.Metadata.Import.build_candidate(parsed_map) :: Relyra.Metadata.Candidate.t()`
- `Relyra.Ecto.MetadataApply.apply_revision(connection_id, candidate_map, revision_attrs, opts \\ []) :: {:ok, MetadataRevision.t()} | {:error, Relyra.Error.t()}`
- `Relyra.Ecto.CertificateInventory.activate_signing_certificate/4` and `retire_signing_certificate/4` remain the certificate lifecycle seam.
</interfaces>
</context>

<assumption_delta_decision>
Primary noun: SAML connection, identified by stable `connection_id`.
Decision: no-change — the detector fired on “optional,” but the existing connection model already represents independent FakeIdP and Keycloak variants; this closure neither adds a second identity axis nor changes the primary key.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove and implement one-candidate descriptor trust application</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-CONTEXT.md
    - .planning/phases/70-keycloak-behind-the-proxy/70-VERIFICATION.md
    - demo/ledger_loop/lib/ledger_loop/demo/keycloak_provisioner.ex
    - demo/ledger_loop/test/ledger_loop/demo/keycloak_provisioner_test.exs
    - lib/relyra/metadata/parser.ex
    - lib/relyra/metadata/import.ex
    - lib/relyra/metadata/candidate.ex
    - lib/relyra/ecto/metadata_apply.ex
    - lib/relyra/ecto/certificate_inventory.ex
  </read_first>
  <files>demo/ledger_loop/lib/ledger_loop/demo/keycloak_provisioner.ex, demo/ledger_loop/test/ledger_loop/demo/keycloak_provisioner_test.exs</files>
  <behavior>
    - "A counting descriptor-parser test seam observes exactly one parse call for a successful provision and for an unchanged retry; persisted issuer, SSO URL, and active fingerprint equal facts from that same candidate."
    - "Wrong public issuer is rejected before any trust mutation, while valid initial apply and key rotation retain attributable connection/metadata/certificate audit events and enable last."
    - "The provisioner source has no raw-XML import call after candidate construction; audited persistence receives the candidate map and content-hash/revision metadata directly."
    - "Existing fetch/parse/apply/activation/identity fail-closed cases and no-op audit-count assertions remain green."
  </behavior>
  <action>
    Begin RED by extending `keycloak_provisioner_test.exs` with a `descriptor_parser` injection that delegates to `Relyra.Metadata.Parser.parse/1` while counting invocations, then assert one invocation per `provision!/1` call and assert the real Repo state/audit results already required by D-03, D-05, and D-06. Add a wrong-public-issuer case that proves no connection, metadata revision, certificate, identity, or receipt becomes available. Add an architecture regression assertion over the demo provisioner source so the raw XML import entry point cannot be reintroduced after candidate construction.

    In GREEN, replace `descriptor_facts/2` followed by `Import.import_xml/3` with one `descriptor_candidate/3` path: invoke the injected/default parser once, build one `%Relyra.Metadata.Candidate{}` via the existing `Import.build_candidate/1`, validate its `idp_entity_id` against `public_issuer/1`, and derive the unchanged/activation facts from that struct. Pass the same struct, converted with `Map.from_struct/1`, to a demo-private `apply_descriptor_candidate/3` that calls `MetadataApply.apply_revision/4` with the existing `repo`, actor, cause, correlation/audit context, `source_kind: :xml_import`, `trigger: :manual_import`, SHA-256 content hash of the fetched bytes, and `candidate.trust_summary`. Preserve the current error typing (`:parse` versus `:apply`), disable-before-change choreography, candidate-derived fingerprint activation, stale active-certificate retirement, Sarah identity, and enable-last ordering. Do not edit `lib/`, use a second parser, write trust with Repo directly, accept assertion document keys, or change the stable connection/public-host contract.
  </action>
  <verify>
    <automated>cd demo/ledger_loop &amp;&amp; mix test test/ledger_loop/demo/keycloak_provisioner_test.exs --warnings-as-errors &amp;&amp; cd ../.. &amp;&amp; mix ci.security &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - A successful provision and unchanged retry each execute one guarded descriptor parse and reuse that candidate for issuer/fingerprint comparison and audited persistence.
    - Metadata revision, connection, and certificate mutations remain transactionally audited with the existing actor/cause/correlation context; no bare trust-state write exists.
    - Wrong issuer and every existing injected failure leave the Keycloak connection unavailable with zero LoginReceipts.
    - No `lib/relyra/**`, public API, behavior callback, parser/crypto/replay policy, package whitelist, or FakeIdP fixture is modified.
  </acceptance_criteria>
  <done>The real descriptor-to-trust path has one parse, one canonical candidate, audited persistence, and executable regression proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keycloak descriptor bytes → metadata parser | Untrusted local-service XML must cross size/DTD/entity guards exactly once. |
| canonical metadata candidate → Ecto trust state | Issuer, SSO URL, and descriptor certificate facts become configured trust only through audited transactions. |
| configured certificate state → assertion verifier | Assertion verification must continue using persisted IdP certificates, never response `KeyInfo`. |
| closure change → browser diagnostics | The parallel artifact-policy closure must prevent credential/assertion disclosure. |
| optional Keycloak lane → FakeIdP/security gates | Demo orchestration must not weaken deterministic tamper or permanent crypto coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-24 | Tampering | descriptor parse/preflight/apply | high | mitigate | Parse once, construct one candidate, and use that candidate for both comparisons and `MetadataApply.apply_revision/4`; focused count and architecture tests block parser differentials. |
| T-70-25 | Repudiation | connection/metadata/certificate mutations | high | mitigate | Preserve Connections, MetadataApply, and CertificateInventory transactions with actor/cause/correlation audit context and enable last. |
| T-70-26 | Spoofing | assertion document `KeyInfo` | high | mitigate | Make no assertion-parser/crypto change; trust remains configured certificate inventory and `mix ci.security` stays blocking. |
| T-70-27 | Information disclosure | credential-bearing E2E diagnostics | high | mitigate | Depend on the separate no-retention closure plan and retain no new descriptor XML, PEM, assertion, credential, or DB diagnostic output here. |
| T-70-28 | Tampering | FakeIdP/adversarial regression gates | high | mitigate | Limit modifications to the demo provisioner/test and run unchanged `mix ci.security`; Plan 70-07 reruns the full browser/FakeIdP gate. |
</threat_model>

## Artifacts this phase produces

- Demo-private `descriptor_candidate/3`: the sole raw-descriptor parse and public-issuer validation seam.
- Demo-private `candidate_facts/1`: issuer, SSO URL, and fingerprint facts derived only from the canonical candidate.
- Demo-private `apply_descriptor_candidate/3`: audited `MetadataApply.apply_revision/4` adapter for that candidate and the raw-byte content hash.
- Focused parser-count, wrong-issuer, candidate-persistence, audit, idempotency, rotation, and fail-closed assertions in `keycloak_provisioner_test.exs`.
- No new file path, public module/API, database schema, dependency, or package artifact.

<source_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Real Keycloak round trip remains a cryptographically verified trust path | 70-06,70-07 | COVERED | This plan repairs trust installation; 70-07 repairs safe runtime evidence. |
| REQ | KC-01 | Optional proxy Keycloak completes a verified SAML login | 70-06,70-07 | COVERED | Both blockers must close before KC-01 can verify. |
| RESEARCH | — | Descriptor-derived trust uses existing metadata/certificate/audit seams | 70-06 | COVERED | One candidate now feeds preflight and MetadataApply. |
| RESEARCH | — | Host-side browser proof retains actionable redacted diagnostics | 70-07 | COVERED | Separate zero-sensitive-retention plan. |
| CONTEXT | D-03,D-05,D-06 | Generated descriptor trust, audit co-commit, idempotency/rotation | 70-06 | COVERED | Exact closure scope. |
| CONTEXT | D-17..D-26 | Browser proof, FakeIdP separation, truthful UI, safe diagnostics | 70-07 plus executed 70-04/05 | COVERED | No inherited UI restyling. |

All other Phase 70 decisions are already covered by executed Plans 70-01 through 70-05 and are unchanged. Deferred Phase 71/72 launcher/documentation work and the discretionary genuine-response replay companion remain excluded by their source contracts.
</source_audit>

<verification>
`cd demo/ledger_loop &amp;&amp; mix test test/ledger_loop/demo/keycloak_provisioner_test.exs --warnings-as-errors &amp;&amp; cd ../.. &amp;&amp; mix ci.security &amp;&amp; mix format --check-formatted`
</verification>

<success_criteria>
The fetched Keycloak descriptor is guarded and parsed once; one candidate drives issuer/fingerprint preflight and audited metadata persistence; generated-key rotation, fail-closed availability, configured-certificate trust, and FakeIdP/security invariants remain intact.
</success_criteria>

<output>
Create `.planning/phases/70-keycloak-behind-the-proxy/70-06-SUMMARY.md` when done.
</output>
