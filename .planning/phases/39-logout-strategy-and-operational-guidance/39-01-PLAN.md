---
phase: 39
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/logout.md
  - mix.exs
autonomous: true
requirements:
  - DOCS-04
must_haves:
  truths:
    - "Operators understand that front-channel SLO is structurally unreliable due to ITP/ETP."
    - "Operators understand that Ecto-backed sessions are a hard prerequisite for SLO."
    - "Operators know to establish absolute session timeouts as their primary security boundary."
  artifacts:
    - path: "guides/recipes/logout.md"
      provides: "SLO strategy and operational fallbacks"
      min_lines: 50
  key_links:
    - from: "mix.exs"
      to: "guides/recipes/logout.md"
      via: "docs extras list"
      pattern: "guides/recipes/logout.md"
---

<objective>
Publish explicit operator guidance on SAML Single Logout (SLO), establishing the harsh realities of front-channel failure (ITP/ETP), mandating stateful sessions, and shifting the security boundary to absolute timeouts.

Purpose: Prevent operators from relying on structurally broken browser mechanisms (front-channel cross-origin cookie drops) for their security boundary, and give them the exact vocabulary to push back on rigid compliance checklists.
Output: A published, formatted, and CI-gated operational guide.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Focus on tone and structure analogs
@guides/identity_mapping_and_provisioning.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the logout strategy guide</name>
  <files>guides/recipes/logout.md</files>
  <action>
Create `guides/recipes/logout.md`. Follow the structural patterns identified in `guides/identity_mapping_and_provisioning.md`, specifically framing the guide with clear "Title and Purpose" framing and dividing responsibility boundaries ("Relyra owns / Host owns"). Write assertively, establishing Relyra's stance that front-channel SLO is a best-effort legacy protocol, not a security boundary. 

Cover the following sections:
1. **The Compliance Trap**: Name the exact mechanisms (Safari ITP, Firefox ETP, Chrome Privacy Sandbox) that block the 3rd-party cookies required for cross-origin iframes or redirects to drop sessions. Equip developers with this vocabulary to push back on auditors.
2. **Mandatory Stateful Sessions**: Explain why stateless Plug cookies cannot be revoked reliably. Mandate Ecto-backed (or similar durable server-side) sessions if SLO is enabled.
3. **Session Index Mapping**: Show how to implement `Relyra.SessionAdapter.index_session/4` and `terminate_by_session_index/4` to map the SAML `SessionIndex` to the Postgres session.
4. **The Real Security Boundary**: Establish absolute session timeouts (e.g., 8-12 hours) and idle timeouts (e.g., 30 mins) as the mandatory fallback when SLO silently fails over the front channel. Explicitly discourage IdP polling.
  </action>
  <verify>
    <automated>mix format --check-formatted guides/recipes/logout.md || (mix format guides/recipes/logout.md && exit 1)</automated>
  </verify>
  <done>Markdown file exists, follows standard guide structure, and passes Elixir formatter rules.</done>
</task>

<task type="auto">
  <name>Task 2: Register guide in mix.exs</name>
  <files>mix.exs</files>
  <action>
Wire the new guide into the build and documentation systems. 
1. Find the `docs` function's `extras` array and append `"guides/recipes/logout.md"`.
2. Find the `ci.docs` alias in the `aliases` function and append `"cmd test -f guides/recipes/logout.md"` to ensure it isn't accidentally deleted without breaking CI.
  </action>
  <verify>
    <automated>mix ci.docs</automated>
  </verify>
  <done>mix.exs is updated and the `ci.docs` pipeline executes successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | No runtime trust boundaries introduced. Mitigates operational risk by clarifying system boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Information Disclosure | `guides/recipes/logout.md` | mitigate | Ensure the guide does not leak internal Relyra security considerations outside the documented public APIs and session concepts. |
| T-39-02 | Repudiation | SLO Documentation | mitigate | Directly document that stateless sessions cannot provide non-repudiation of logout, mandating stateful sessions and absolute timeouts. |
</threat_model>

<verification>
- `mix ci.docs` passes, confirming the file is tracked.
- `rg "ITP" guides/recipes/logout.md` returns matches.
- `rg "ETP" guides/recipes/logout.md` returns matches.
- `rg "SessionAdapter" guides/recipes/logout.md` returns matches.
</verification>

<success_criteria>
- The `guides/recipes/logout.md` file is complete and formatted.
- The file is integrated into ExDoc generation via `mix.exs`.
- The CI pipeline verifies the existence of the guide.
</success_criteria>

<output>
After completion, create `.planning/phases/39-logout-strategy-and-operational-guidance/39-01-SUMMARY.md`
</output>