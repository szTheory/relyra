---
phase: 69-compose-split-fleet-proxy
plan: 03
type: execute
wave: 3
depends_on: ["69-02"]
gap_closure: true
files_modified:
  - scripts/test_fleet_proxy_e2e.sh
  - test/integration/fleet-proxy-sibling.compose.yml
  - test/browser/fleet_proxy.spec.mjs
  - playwright.fleet-proxy.config.mjs
  - package.json
  - .github/workflows/fleet-proxy-e2e.yml
  - scripts/setup_branch_protection.sh
  - .github/workflows/release-please-automerge.yml
  - .github/workflows/release-please-pr-checks.yml
  - .github/workflows/planning-pr-checks.yml
  - test/release/fleet_proxy_ci_integrity_test.exs
  - .planning/phases/69-compose-split-fleet-proxy/69-UAT.md
  - .planning/phases/69-compose-split-fleet-proxy/69-VALIDATION.md
  - .planning/phases/69-compose-split-fleet-proxy/69-VERIFICATION.md
  - .planning/phases/69-compose-split-fleet-proxy/69-01-SUMMARY.md
  - .planning/phases/69-compose-split-fleet-proxy/69-02-SUMMARY.md
autonomous: true
requirements: [FLEET-01, FLEET-02, FLEET-03]
user_setup: []

must_haves:
  truths:
    - "A single hermetic command proves solo startup, health, HTTP reachability, no database host binding, and named-volume persistence across down/up without deleting volumes."
    - "The same command proves shared Traefik startup is idempotent, Relyra and a pinned sibling route concurrently, and stopping Relyra leaves the sibling, proxy, and external network alive."
    - "Chromium proves the setup LiveView performs a server roundtrip at both public origins, exposes the correct endpoint URLs, and keeps readonly URLs selectable and horizontally accessible at a narrow viewport."
    - "The exact fleet-proxy-e2e status context runs for every pull request without path filters and is required everywhere branch protection or release automation models required checks."
    - "A scheduled Rulestead main canary exercises real cross-project coexistence without becoming a merge dependency."
    - "Phase 69 verification and UAT cite fresh automated evidence and contain no required human verification."
  artifacts:
    - path: scripts/test_fleet_proxy_e2e.sh
      provides: Hermetic solo, proxy, sibling, persistence, and browser lifecycle proof
    - path: .github/workflows/fleet-proxy-e2e.yml
      provides: Required PR gate and scheduled real-project canary
    - path: test/release/fleet_proxy_ci_integrity_test.exs
      provides: Static drift guard for required-check and workflow invariants

<objective>
Convert Phase 69's remaining process and browser receipts into deterministic integration/E2E automation, enforce the hermetic proof as a required CI check, and preserve a scheduled real-world sibling canary.
</objective>

<tasks>

<task type="tracer">
  <name>Task 1: Build the hermetic fleet lifecycle and LiveView browser proof</name>
  <files>scripts/test_fleet_proxy_e2e.sh, test/integration/fleet-proxy-sibling.compose.yml, test/browser/fleet_proxy.spec.mjs, playwright.fleet-proxy.config.mjs, package.json</files>
  <action>Create a fail-fast shell harness that uses the shipped Compose files, a pinned traefik/whoami sibling, curl --resolve, and Playwright. Capture diagnostics before cleanup, preserve named volumes, avoid wildcard-DNS assumptions, and make all three former UAT cases falsifiable from one command.</action>
  <acceptance_criteria>
    - Solo mode is healthy, only the app is loopback-published, the database has no host binding, and a sentinel survives down/up through the named mix volume.
    - Starting the shared proxy twice preserves its container ID and exposes its dashboard.
    - Relyra and sibling Host routes work concurrently; after Relyra stops, the sibling, proxy container, and external network remain.
    - Playwright performs a LiveView state transition and validates origin-specific readonly URLs plus narrow-viewport selection/overflow behavior at solo and fleet origins.
  </acceptance_criteria>
  <verify><automated>npm run demo:fleet-proxy</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Enforce the gate in CI and close Phase 69 verification evidence</name>
  <files>.github/workflows/fleet-proxy-e2e.yml, scripts/setup_branch_protection.sh, .github/workflows/release-please-automerge.yml, .github/workflows/release-please-pr-checks.yml, .github/workflows/planning-pr-checks.yml, test/release/fleet_proxy_ci_integrity_test.exs, Phase 69 planning artifacts</files>
  <action>Add an unfiltered fleet-proxy-e2e required job for pull requests and main, a scheduled/manual Rulestead main canary, synchronize every required-check consumer, add a static drift guard, and replace human-needed Phase 69 evidence only after fresh automated checks pass.</action>
  <acceptance_criteria>
    - The workflow's exact required job context is fleet-proxy-e2e and no pull_request path filter can skip it.
    - Branch protection, release PR checks/automerge, and planning PR checks all model both security matrix contexts plus fleet-proxy-e2e.
    - The Rulestead canary is scheduled daily at 06:17 UTC, is manual-dispatchable, and does not block pull requests.
    - UAT is complete with three automated passes; validation contains no manual-only Phase 69 row; verification status is passed with independent command evidence.
  </acceptance_criteria>
  <verify><automated>mix test test/release/fleet_proxy_ci_integrity_test.exs --warnings-as-errors &amp;&amp; mix qa &amp;&amp; mix ci.security</automated></verify>
</task>

</tasks>

<verification>
- `npm run demo:fleet-proxy` passes from a clean Docker lifecycle and leaves no Relyra or fixture containers running.
- The CI integrity test validates workflow triggers, pinned fixtures, lifecycle assertions, and exact required-check synchronization.
- `mix format --check-formatted`, `mix test --warnings-as-errors`, `mix ci.security`, and `mix qa` exit zero after the final edit.
</verification>

<success_criteria>
- Phase 69 has zero pending human or UI verification.
- Hermetic PR automation covers all former UAT truths, while the moving Rulestead dependency remains scheduled and non-blocking.
- No `lib/`, public API, security posture, protocol, or Hex package surface changes.
</success_criteria>

<output>Create `69-03-SUMMARY.md` when complete.</output>
