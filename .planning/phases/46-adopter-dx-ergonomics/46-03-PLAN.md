---
phase: 46-adopter-dx-ergonomics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/overview.md
  - guides/batteries_included.md
  - mix.exs
  - lib/mix/tasks/relyra.batteries_included.ex
  - BATTERIES_INCLUDED.md
autonomous: true
requirements:
  - DX-03
must_haves:
  truths:
    - "guides/overview.md exists with Day-1, Day-2, and Reference sections linking to existing guides."
    - "guides/batteries_included.md is a stub pointing to root BATTERIES_INCLUDED.md — duplicate narrative removed."
    - "mix relyra.batteries_included generator includes ADFS and references root artifact in claim table."
    - "mix ci.docs gates overview.md presence."
  artifacts:
    - path: "guides/overview.md"
      provides: "Job-shaped documentation index."
    - path: "guides/batteries_included.md"
      provides: "Stub redirect to canonical root batteries proof."
    - path: "BATTERIES_INCLUDED.md"
      provides: "Regenerated drift-checked artifact with 4 providers including ADFS."
---

<objective>
Deliver DX-03: publish `guides/overview.md` as the job-shaped doc hub, dedupe batteries-included docs (root canonical, guide stub), fix generator ADFS omission, and wire CI/ExDoc gates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/46-adopter-dx-ergonomics/46-CONTEXT.md
@.planning/phases/46-adopter-dx-ergonomics/46-RESEARCH.md
@guides/batteries_included.md
@guides/getting_started.md
@lib/mix/tasks/relyra.batteries_included.ex
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Create guides/overview.md job-shaped index</name>
  <read_first>
    - guides/batteries_included.md (proof journey to migrate)
    - guides/getting_started.md
    - README.md (existing guide links)
    - .planning/phases/46-adopter-dx-ergonomics/46-CONTEXT.md (D-08, D-10)
  </read_first>
  <action>
    Create `guides/overview.md` with title `# Documentation Overview` and three sections:

    **## Day-1 — Install and prove**
    - Link `getting_started.md`
    - Link stub `batteries_included.md`
    - Summarize proof journey: `mix relyra.install`, FakeIdP local proof, pick one first-class runbook (migrate key steps from current `guides/batteries_included.md` sections 1-3)

    **## Day-2 — Operate in production**
    - Links: `recipes/okta.md`, `recipes/entra.md`, `recipes/google_workspace.md`, `recipes/adfs.md`, `recipes/generic_saml.md`, `identity_mapping_and_provisioning.md`, `troubleshooting.md`, `recipes/logout.md`, `case_studies/operator_managed_rollout.md`, `case_studies/phoenix_saas_tenant_onboarding.md`

    **## Reference**
    - Links: `../BATTERIES_INCLUDED.md`, `../SECURITY.md`, `../CONFORMANCE.md`, `jtbd_user_flows.md`, `operations/incident_playbook.md`

    Use relative markdown links only. Keep under ~120 lines.
  </action>
  <verify>test -f guides/overview.md && grep -c "## Day-1" guides/overview.md && grep -c "## Day-2" guides/overview.md && grep -c "## Reference" guides/overview.md</verify>
  <acceptance_criteria>
    - `guides/overview.md` exists.
    - File contains exactly one each of headings `## Day-1`, `## Day-2`, `## Reference` (grep count = 1 each).
    - File links to `getting_started.md` and at least one provider runbook.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Stub guides/batteries_included.md and wire ExDoc + ci.docs</name>
  <read_first>
    - mix.exs (docs/0 extras and ci.docs alias)
    - guides/batteries_included.md
    - .planning/phases/46-adopter-dx-ergonomics/46-CONTEXT.md (D-09, D-11, D-12, D-16)
  </read_first>
  <action>
    1. Replace `guides/batteries_included.md` content with a short stub (~15 lines max):
       - Title `# Batteries Included`
       - One paragraph: canonical proof artifact lives at root [BATTERIES_INCLUDED.md](../BATTERIES_INCLUDED.md).
       - Link to `overview.md` for the proof journey narrative.
       - Do NOT duplicate the full install/FakeIdP/runbook steps.

    2. In `mix.exs` `docs/0` `extras` list, add `"guides/overview.md"` immediately after `"README.md"`.

    3. In `mix.exs` `ci.docs` alias, add `"cmd test -f guides/overview.md"` as the first step (before batteries_included check).
  </action>
  <verify>mix ci.docs</verify>
  <acceptance_criteria>
    - `guides/batteries_included.md` contains link to `../BATTERIES_INCLUDED.md`.
    - `guides/batteries_included.md` line count < 25.
    - `grep guides/overview.md mix.exs` exits 0 in both extras and ci.docs sections.
    - `mix ci.docs` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Fix batteries generator — ADFS + root artifact refs</name>
  <read_first>
    - lib/mix/tasks/relyra.batteries_included.ex
    - lib/relyra/provider.ex (list/0 includes :adfs)
    - BATTERIES_INCLUDED.md
    - .planning/phases/46-adopter-dx-ergonomics/46-CONTEXT.md (D-13)
  </read_first>
  <action>
    In `lib/mix/tasks/relyra.batteries_included.ex`:

    1. Change provider list from `[:okta, :entra, :google_workspace]` to `[:okta, :entra, :google_workspace, :adfs]`.

    2. Add `defp local_guide_path(:adfs), do: "guides/recipes/adfs.md"`.

    3. In `claim_table/1`, replace artifact column references from `guides/batteries_included.md` to `BATTERIES_INCLUDED.md` for rows that currently cite the guide path (audit/telemetry, scheduled refresh, diagnostic bundle rows per RESEARCH §1).

    4. Update supported-provider-scope row artifact list to include `guides/recipes/adfs.md`.

    5. Run `mix relyra.batteries_included` to regenerate root `BATTERIES_INCLUDED.md`.
  </action>
  <verify>mix relyra.batteries_included --check && mix test test/mix/tasks/relyra_batteries_included_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - `grep :adfs lib/mix/tasks/relyra.batteries_included.ex` exits 0.
    - `BATTERIES_INCLUDED.md` contains "ADFS" in supported provider scope.
    - `mix relyra.batteries_included --check` exits 0.
    - `test/mix/tasks/relyra_batteries_included_test.exs` passes.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix ci.docs` exits 0
- `mix relyra.batteries_included --check` exits 0
- `grep "## Day-1" guides/overview.md` exits 0
</verification>

<success_criteria>
DX-03 satisfied: job-shaped overview published, batteries docs deduped with root as canonical, generator includes ADFS, CI gates overview presence.
</success_criteria>

## PLANNING COMPLETE
