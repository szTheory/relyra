---
phase: 46-adopter-dx-ergonomics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/install/router_injector.ex
  - lib/mix/tasks/relyra.install.ex
  - test/relyra/install/router_injector_test.exs
  - test/mix/relyra_install_test.exs
autonomous: true
requirements:
  - DX-02
must_haves:
  truths:
    - "mix relyra.install auto-injects import + saml_routes() when exactly one lib/**/*router.ex uses Phoenix.Router."
    - "Ambiguous detection (0 or 2+ routers, no anchor) leaves router files byte-unchanged and prints manual instructions."
    - "Re-running install on an already-injected router is idempotent — no duplicate saml_routes()."
  artifacts:
    - path: "lib/relyra/install/router_injector.ex"
      provides: "Marker-based inject/detect with {:ok | :already_injected | :ambiguous} contract."
    - path: "test/relyra/install/router_injector_test.exs"
      provides: "Unit coverage for inject, idempotent, and ambiguous anchor cases."
    - path: "test/mix/relyra_install_test.exs"
      provides: "Integration fixtures for single-router success and multi-router fallback."
---

<objective>
Deliver DX-02: wire `mix relyra.install` to auto-inject `saml_routes()` into an unambiguous host router using a testable `Relyra.Install.RouterInjector` module with marker idempotency.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/46-adopter-dx-ergonomics/46-CONTEXT.md
@.planning/phases/46-adopter-dx-ergonomics/46-RESEARCH.md
@.planning/phases/46-adopter-dx-ergonomics/46-PATTERNS.md
@lib/mix/tasks/relyra.install.ex
@test/phoenix/router_test.exs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Router file corruption on ambiguous detect | high | Never write when router count != 1 or anchor missing; integration test asserts byte equality |
| Duplicate route registration on re-run | medium | `# --- Relyra SAML routes ---` marker + `saml_routes()` presence check returns `{:already_injected, _}` |
| Injection inside wrong scope | medium | Anchor after first `use …Router` line at module level, not find_last_end |
</threat_model>

<tasks>

<task type="auto">
  <name>Create RouterInjector module</name>
  <read_first>
    - lib/mix/tasks/relyra.install.ex (ensure_config! sentinel pattern)
    - test/phoenix/router_test.exs (canonical usage shape)
    - /Users/jon/projects/sigra/lib/sigra/install/injector.ex (idempotency contract reference)
    - .planning/phases/46-adopter-dx-ergonomics/46-RESEARCH.md §2
  </read_first>
  <action>
    Create `lib/relyra/install/router_injector.ex` with:

    - `@marker "# --- Relyra SAML routes ---"`
    - `@inject_snippet` multiline string:
      ```
      # --- Relyra SAML routes ---
      import Relyra.Phoenix.Router
      saml_routes()
      ```
    - `detect_routers/1` (optional `root` default `File.cwd!()`): glob `lib/**/*router.ex` under root, keep files whose contents match `~r/use\s+(\w+\.)?Router/`.
    - `inject/1`: if contents contains `@marker` OR `"saml_routes()"` → `{:already_injected, contents}`; else find first line matching `~r/^\s*use\s+(\w+\.)?Router/` and insert `@inject_snippet` after that line (include trailing newline); if no match → `:ambiguous`; else `{:ok, new_contents}`.
    - `@doc false` on module — internal install seam.
  </action>
  <verify>mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - `lib/relyra/install/router_injector.ex` exists and exports `detect_routers/1` and `inject/1`.
    - Module compiles with zero warnings.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Add RouterInjector unit tests</name>
  <read_first>
    - lib/relyra/install/router_injector.ex
    - test/phoenix/router_test.exs
  </read_first>
  <action>
    Create `test/relyra/install/router_injector_test.exs` with at least:

    1. **inject success** — minimal router string with `use Phoenix.Router` → `{:ok, result}` containing `@marker`, `import Relyra.Phoenix.Router`, and `saml_routes()`.
    2. **idempotent** — pre-marked content → `{:already_injected, unchanged}`.
    3. **ambiguous anchor** — file without `use …Router` → `:ambiguous`.
    4. **already has saml_routes** — content with `saml_routes()` but no marker → `{:already_injected, _}`.
  </action>
  <verify>mix test test/relyra/install/router_injector_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - All tests in `router_injector_test.exs` pass.
    - Test count >= 4.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Wire install task auto-detect and injection</name>
  <read_first>
    - lib/mix/tasks/relyra.install.ex
    - lib/relyra/install/router_injector.ex
    - .planning/phases/46-adopter-dx-ergonomics/46-CONTEXT.md (D-04 through D-06)
  </read_first>
  <action>
    Refactor `Mix.Tasks.Relyra.Install`:

    1. Replace `maybe_update_router(nil, …)` to call `RouterInjector.detect_routers/0`:
       - `[_single]` → read file, `RouterInjector.inject/1`, on `{:ok, new}` write file and info "Injected Relyra SAML routes into …"; on `{:already_injected, _}` info "Relyra SAML routes already present"; on `:ambiguous` fall through to print-instructions (same messages as current ambiguous path at lines 108-114).
       - `[]` or `_multiple` → print manual instructions (no file writes).

    2. Update `maybe_update_router(router_path, …)` when file exists:
       - If `saml_routes()` already present → skip (existing behaviour).
       - Else attempt `RouterInjector.inject/1`; write on `{:ok, _}`; print ambiguous message on `:ambiguous` or `{:already_injected, _}` without write.

    Preserve `maybe_print_live_admin_instructions/2` calls on all exit paths.
  </action>
  <verify>mix test test/mix/relyra_install_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - `lib/mix/tasks/relyra.install.ex` aliases or calls `Relyra.Install.RouterInjector`.
    - Existing install scaffold tests still pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Add install integration fixtures for router injection</name>
  <read_first>
    - test/mix/relyra_install_test.exs
    - lib/relyra/install/router_injector.ex
    - .planning/phases/46-adopter-dx-ergonomics/46-CONTEXT.md (D-15)
  </read_first>
  <action>
    Extend `test/mix/relyra_install_test.exs` with two tests using temp dirs:

    1. **unambiguous single router** — create `lib/demo_app_web/router.ex` containing:
       ```elixir
       defmodule DemoAppWeb.Router do
         use DemoAppWeb, :router
       end
       ```
       Actually use `use Phoenix.Router` for detect compatibility. Run install without `--router`. Assert file contains `# --- Relyra SAML routes ---` and `saml_routes()`. Re-run install; assert file contents unchanged (idempotent).

    2. **ambiguous multi-router** — create `lib/demo_app_web/router.ex` AND `lib/demo_app/admin_router.ex`, both with `use Phoenix.Router`. Capture IO. Assert neither file contains `saml_routes()` after install; assert output mentions manual addition or ambiguous injection.

    Use `File.cd!/2` pattern from existing tests.
  </action>
  <verify>mix test test/mix/relyra_install_test.exs test/relyra/install/router_injector_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - Single-router test passes with injection present.
    - Multi-router test passes with zero `saml_routes()` in either file.
    - Idempotent re-run test asserts `File.read!` equality before/after second install.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/relyra/install/router_injector_test.exs test/mix/relyra_install_test.exs --warnings-as-errors` exits 0
- `mix compile --warnings-as-errors` exits 0
</verification>

<success_criteria>
DX-02 satisfied: installer auto-injects on unambiguous single router, falls back safely on ambiguity, and tests prove no corruption on edge cases.
</success_criteria>

## PLANNING COMPLETE
