---
phase: 40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio
plan: 05
type: execute
wave: 2
depends_on: [40.1-03]
files_modified:
  - guides/recipes/logout.md
  - test/docs/logout_recipe_drift_test.exs
autonomous: true
requirements: [DOCS-04, SLO-01]
tags: [docs, slo, drift-fix, blocker-1]

must_haves:
  truths:
    - "guides/recipes/logout.md SessionAdapter example uses the canonical 4-arg signature `index_session(session_index, issuer, context, opts)` — NOT the broken `(connection, login_result, local_session_id, opts)` form"
    - "guides/recipes/logout.md terminate_by_session_index example uses `(session_index, issuer, context, opts)` — NOT the broken `(connection, session_index, _name_id, _opts)` form"
    - "A new 2-3 sentence host-linkage operator paragraph (D-03) is inserted between section 3 and section 4, explicitly stating that hosts must call `MyAdapter.index_session/4` from their ACS controller after `Relyra.consume_response/3` returns, reading from `login_result.principal.session_index`"
    - "Narrative at lines 91-99 is updated post-D-01: the old misleading sentence `Relyra extracts this and passes it to your adapter` is rewritten to accurately reflect host-owned linkage (Relyra does NOT pass session_index to index_session/4; the host reads it from login_result.principal.session_index)"
    - "Sections 1, 2, and 4 are byte-untouched (D-04 — only the section-3 code block + surrounding narrative + new host-linkage paragraph are modified)"
    - "The `@tag :skip` on test/docs/logout_recipe_drift_test.exs is REMOVED in the same commit as the doc rewrite (D-12 land-test-first-then-unskip pattern)"
    - "After unskip, `mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors` exits 0 (test GREEN against the rewritten doc — proves the drift gate is live and effective)"
    - "`mix ci.docs` exits 0 end-to-end (the now-live drift test passes; presence guards still pass)"
  artifacts:
    - path: "guides/recipes/logout.md"
      provides: "Operator SLO guide with corrected SessionAdapter example and host-linkage paragraph"
      contains: "def index_session(session_index, issuer, context, opts)"
    - path: "test/docs/logout_recipe_drift_test.exs"
      provides: "Live (unskipped) drift gate now actively enforcing arity parity"
      contains: "test \"logout.md SessionAdapter"
  key_links:
    - from: "guides/recipes/logout.md SessionAdapter example"
      to: "lib/relyra/session_adapter.ex callbacks (lines 19-31)"
      via: "Function-head arity parity (4 parameters, canonical names)"
      pattern: "def (index_session|terminate_by_session_index)\\(session_index, issuer, context, opts\\)"
    - from: "guides/recipes/logout.md host-linkage paragraph (new)"
      to: "lib/relyra.ex consume_response/3 + Principal.session_index"
      via: "Explicit operator instruction to call MyAdapter.index_session/4 from ACS controller"
      pattern: "login_result.principal.session_index"
    - from: "test/docs/logout_recipe_drift_test.exs"
      to: "guides/recipes/logout.md (rewritten)"
      via: "Runtime behaviour_info introspection + arity-parity assertion (now active)"
      pattern: "behaviour_info\\(:callbacks\\)"
---

<objective>
Close BLOCKER 1 from v1.4-MILESTONE-AUDIT.md: rewrite the SessionAdapter code example at `guides/recipes/logout.md` lines 100-127 to use the actual 4-arg callback signatures from `lib/relyra/session_adapter.ex:19-31`. Rewrite the immediately preceding narrative at lines 91-99 to match the host-owned-linkage policy (per D-01) — the current sentence "Relyra extracts this and passes it to your adapter" is misleading because Relyra does NOT pass `session_index` to `index_session/4`; the host reads it from `login_result.principal.session_index` and invokes its own adapter.

Insert a new 2-3 sentence host-linkage operator paragraph (per D-03) between section 3 (Session Index Mapping) and section 4 (The Real Security Boundary), explicitly stating that hosts must call `MyAdapter.index_session/4` from their ACS controller after `Relyra.consume_response/3` returns, reading from `login_result.principal.session_index`.

In the **same commit**, remove the `@tag :skip` from `test/docs/logout_recipe_drift_test.exs` (created in Plan 03) so the drift gate flips live. The rewritten doc must make the now-active test pass (D-12: land-test-first-then-unskip).

Purpose: A host copy-pasting the current example fails `@behaviour Relyra.SessionAdapter` checks at compile time. After this plan, the example compiles cleanly against the real behaviour, AND a CI gate (the unskipped Plan 03 test) prevents this class of drift from regressing silently.

Output: Modified `guides/recipes/logout.md` (lines 91-127 rewritten + new paragraph between sections 3 and 4); modified `test/docs/logout_recipe_drift_test.exs` (skip tag removed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.4-MILESTONE-AUDIT.md
@.planning/phases/40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio/40.1-CONTEXT.md
@CLAUDE.md
@guides/recipes/logout.md
@lib/relyra/session_adapter.ex
@lib/relyra.ex
@test/protocol/logout_pipeline_test.exs
@test/docs/logout_recipe_drift_test.exs

<interfaces>
<!-- Canonical SessionAdapter callback signatures from lib/relyra/session_adapter.ex:19-31. -->
<!-- The rewritten doc example MUST use these signatures verbatim. -->

```elixir
@callback index_session(
            session_index :: binary(),
            issuer :: binary(),
            context :: map(),
            opts :: keyword()
          ) :: {:ok, term()} | {:error, Error.t()}

@callback terminate_by_session_index(
            session_index :: binary(),
            issuer :: binary(),
            context :: map(),
            opts :: keyword()
          ) :: {:ok, term()} | {:error, Error.t()}
```

<!-- The host-linkage data flow (D-01 + D-03). -->
<!-- After Relyra.consume_response/3 returns {:ok, %Relyra.LoginResult{}}, the host has: -->
<!--   login_result.principal.session_index :: binary() | nil -->
<!--   login_result.issuer :: binary() -->
<!--   login_result.connection.connection_id :: binary() -->
<!-- The host then invokes (from its ACS controller, AFTER establish_session): -->
<!--   MyAdapter.index_session( -->
<!--     login_result.principal.session_index, -->
<!--     login_result.issuer, -->
<!--     %{connection_id: login_result.connection.connection_id, local_session_id: local_id}, -->
<!--     [session_adapter: MyAdapter] -->
<!--   ) -->
<!-- Relyra dispatches but does NOT invoke this callback automatically (D-01 host-owned linkage). -->

<!-- For contrast: terminate_by_session_index/4 IS auto-wired by Relyra at lib/relyra.ex:343-361 -->
<!-- (handle_idp_initiated_logout) because it operates entirely on inbound-message data — -->
<!-- session_index, issuer, and context are all derivable from the parsed LogoutRequest -->
<!-- without needing a host-side local session ID. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite guides/recipes/logout.md sections 3 area — narrative (lines 91-99), code block (lines 100-127), and insert new host-linkage paragraph between section 3 and section 4</name>
  <files>guides/recipes/logout.md</files>
  <read_first>
    - guides/recipes/logout.md (the entire 155-line file; sections 1, 2, 4 are byte-untouched per D-04 — current state of section 3 is the rewrite target)
    - lib/relyra/session_adapter.ex (lines 19-31 — canonical callback signatures; lines 96-126 — index_session/4 dispatch with telemetry; lines 128-158 — terminate_by_session_index/4 dispatch)
    - lib/relyra.ex (lines 272-303 — consume_logout/3; lines 343-361 — handle_idp_initiated_logout auto-wires terminate; lines 363-382 — consume_response/3; lines 406-413 — normalize_consume_result populates Principal.session_index)
    - .planning/phases/40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio/40.1-CONTEXT.md (D-01 host-owned linkage; D-02 rewrite scope; D-03 host-linkage paragraph requirement; D-04 sections 1/2/4 untouched)
    - .planning/v1.4-MILESTONE-AUDIT.md (BLOCKER 1 at lines 143-165 — the failing example signatures and the recommended fix)
    - test/docs/logout_recipe_drift_test.exs (the test that will go live in Task 2 — Task 1's output must make this test pass once unskipped)
  </read_first>
  <action>
    Modify `guides/recipes/logout.md` in three coupled places. Use the Edit tool (not Write — preserve byte-identity of sections 1, 2, 4).

    ### Change A: Narrative at lines 91-99 (just above the code block)

    Current (lines 91-99):

    ```
    ## 3. Session Index Mapping

    To connect SAML SLO to your stateful sessions, implement the `Relyra.SessionAdapter`
    behaviour. Specifically, you must implement `index_session/4` and
    `terminate_by_session_index/4`.

    When a user logs in, the IdP may provide a `SessionIndex` in the `AuthnStatement`.
    Relyra extracts this and passes it to your adapter.
    ```

    Replace lines 97-99 (the "When a user logs in..." paragraph) with prose that accurately reflects the host-owned linkage policy. The H2 heading "## 3. Session Index Mapping" and the first paragraph (lines 93-95) describing the two callbacks to implement remain unchanged. The replacement text for lines 97-99:

    ```
    When a user logs in, the IdP may provide a `SessionIndex` inside the
    `AuthnStatement`. Relyra surfaces this value at
    `login_result.principal.session_index` on the `%Relyra.LoginResult{}` returned
    from `Relyra.consume_response/3`. The host application is responsible for
    invoking `index_session/4` to map the SAML `SessionIndex` to its local
    durable session — see the host-linkage note after the code example below.
    ```

    Rationale: The old sentence "Relyra extracts this and passes it to your adapter" implies auto-wiring. Per D-01, Relyra does NOT pass `session_index` to `index_session/4`; the host reads `login_result.principal.session_index` and calls its own adapter. The new prose accurately reflects this and forward-references the host-linkage paragraph being added in Change C.

    ### Change B: Code block at lines 100-127

    Replace the entire fenced ```elixir ... ``` block (currently the code example with the broken signatures `def index_session(connection, login_result, local_session_id, opts)` and `def terminate_by_session_index(connection, session_index, _name_id, _opts)`) with a new example using the canonical 4-arg signatures from `lib/relyra/session_adapter.ex:19-31`.

    The replacement code block (paste verbatim — this is the example a host copies):

    ```elixir
    defmodule MyApp.Relyra.SessionAdapter do
      @behaviour Relyra.SessionAdapter

      alias MyApp.Accounts.SessionStore

      @impl true
      def index_session(session_index, issuer, context, _opts) do
        # Called by the host's ACS controller (NOT auto-invoked by Relyra)
        # to map the IdP-issued SessionIndex to the host's local session.
        # `context` typically carries the host-side local session id and the
        # Relyra connection id; shape is host-defined.
        case SessionStore.map_saml_index(
               context.connection_id,
               session_index,
               issuer,
               context.local_session_id
             ) do
          :ok -> {:ok, %{session_index: session_index}}
          {:error, reason} -> {:error, Relyra.Error.new(:session_index_store_failed, inspect(reason))}
        end
      end

      @impl true
      def terminate_by_session_index(session_index, issuer, context, _opts) do
        # Called automatically by Relyra.consume_logout/3 when a valid
        # IdP-initiated LogoutRequest arrives carrying this SessionIndex.
        # `context` carries the Relyra connection_id derived from the inbound
        # message; the host looks up its local session and terminates it.
        case SessionStore.delete_by_saml_index(context.connection_id, session_index, issuer) do
          :ok -> {:ok, %{terminated: session_index}}
          {:error, reason} -> {:error, Relyra.Error.new(:session_terminate_failed, inspect(reason))}
        end
      end
    end
    ```

    Critical constraints for the code block:
    - `def index_session/4` parameter list is EXACTLY `(session_index, issuer, context, _opts)` — 4 parameters, names matching the callback declaration. The unused `opts` may be prefixed with `_` to silence the unused-var warning (host idiom).
    - `def terminate_by_session_index/4` parameter list is EXACTLY `(session_index, issuer, context, _opts)` — same 4-parameter shape.
    - `@behaviour Relyra.SessionAdapter` retained.
    - `@impl true` retained on both functions.
    - The bodies are illustrative (not load-bearing). Keep them realistic enough to compile if a host pastes them, but the test that gates this plan checks the **function heads**, not the bodies. The example shown uses `SessionStore.map_saml_index/4` and `SessionStore.delete_by_saml_index/3` as plausible host functions; substitute with your domain's actual store API.

    ### Change C: Insert new 2-3 sentence host-linkage paragraph between section 3 (closing prose) and section 4 (H2 heading)

    The current section-3 closing prose (lines 129-131):

    ```
    By linking the `SessionIndex` to your durable session record, you bypass the
    need for the browser to send the session cookie during the `LogoutRequest`. The
    termination happens entirely server-side.
    ```

    Keep this paragraph. Immediately after it (before the `## 4. The Real Security Boundary` heading at line 133), insert a new H3 subsection titled `### Host-owned linkage (you must invoke `index_session/4` yourself)`. The subsection body:

    ```
    Relyra does not auto-invoke `index_session/4` from `Relyra.consume_response/3`.
    Session-index registration is host-owned: after a successful login, your ACS
    controller reads `login_result.principal.session_index` from the
    `%Relyra.LoginResult{}` Relyra returns, derives a `context` map containing the
    host-side `local_session_id` and the Relyra `connection_id`, then calls
    `MyApp.Relyra.SessionAdapter.index_session/4` directly. Relyra _does_
    auto-invoke `terminate_by_session_index/4` from `Relyra.consume_logout/3`
    because terminate operates entirely on inbound-message data (the IdP's
    `LogoutRequest` carries the `SessionIndex` and issuer Relyra needs); index has
    no such inbound trigger and depends on a host-only value (the local session
    id), so the host owns the call site.
    ```

    This paragraph satisfies D-03 (host-linkage explicitly documented) and the audit's WARNING 2 disposition (the policy choice for `index_session/4` is now explicit operator documentation).

    ### Constraints on Changes A, B, C combined:

    - Sections 1 (`## 1. The Compliance Trap`, lines 43-72), 2 (`## 2. Mandatory Stateful Sessions`, lines 74-89), and 4 (`## 4. The Real Security Boundary`, lines 133-154) — and the Overview / Relyra owns / Host owns blocks at lines 1-42 — MUST be byte-untouched (D-04).
    - The section-3 H2 heading (`## 3. Session Index Mapping`, line 91) stays exactly as it is.
    - The first paragraph of section 3 (lines 93-95, the "To connect SAML SLO to your stateful sessions, implement the `Relyra.SessionAdapter` behaviour..." prose) stays unchanged — only the next paragraph (97-99) and the code block (100-127) are rewritten.
    - After the changes, `wc -l guides/recipes/logout.md` will report MORE lines than the current 155 (the new host-linkage subsection adds ~10 lines + heading + blank-line separators). This growth is intentional.
    - Markdown remains valid: fenced code block uses ```elixir / ``` delimiters; H3 heading uses `###`; blank lines separate H3 and following prose per existing style.
  </action>
  <verify>
    <automated>grep -F 'def index_session(session_index, issuer, context, _opts)' guides/recipes/logout.md && grep -F 'def terminate_by_session_index(session_index, issuer, context, _opts)' guides/recipes/logout.md && ! grep -F 'def index_session(connection, login_result, local_session_id, opts)' guides/recipes/logout.md && ! grep -F 'def terminate_by_session_index(connection, session_index, _name_id, _opts)' guides/recipes/logout.md && grep -F 'login_result.principal.session_index' guides/recipes/logout.md && grep -F 'Host-owned linkage' guides/recipes/logout.md && grep -F 'auto-invoke' guides/recipes/logout.md && grep -F '## 1. The Compliance Trap' guides/recipes/logout.md && grep -F '## 2. Mandatory Stateful Sessions' guides/recipes/logout.md && grep -F '## 4. The Real Security Boundary' guides/recipes/logout.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -F 'def index_session(session_index, issuer, context, _opts)' guides/recipes/logout.md` returns ≥ 1 (canonical 4-arg signature present)
    - `grep -c -F 'def terminate_by_session_index(session_index, issuer, context, _opts)' guides/recipes/logout.md` returns ≥ 1 (canonical 4-arg signature present)
    - `grep -c -F 'def index_session(connection, login_result, local_session_id, opts)' guides/recipes/logout.md` returns 0 (old broken signature gone)
    - `grep -c -F 'def terminate_by_session_index(connection, session_index, _name_id, _opts)' guides/recipes/logout.md` returns 0 (old broken signature gone)
    - `grep -F 'login_result.principal.session_index' guides/recipes/logout.md` returns ≥ 1 match (the host-reads-from-Principal path is explicitly documented per D-03)
    - `grep -F 'Host-owned linkage' guides/recipes/logout.md` returns ≥ 1 match (new H3 subsection per D-03)
    - `grep -F '## 1. The Compliance Trap' guides/recipes/logout.md` returns 1 (section 1 untouched per D-04)
    - `grep -F '## 2. Mandatory Stateful Sessions' guides/recipes/logout.md` returns 1 (section 2 untouched per D-04)
    - `grep -F '## 4. The Real Security Boundary' guides/recipes/logout.md` returns 1 (section 4 untouched per D-04)
    - `git diff --stat guides/recipes/logout.md` shows the modification (line count increase expected ~10-15 lines from the new H3 subsection)
    - `mix ci.docs` exits 0 (the now-active drift test from Plan 03 will be unskipped in Task 2; presence guard still passes immediately)
  </acceptance_criteria>
  <done>
    `guides/recipes/logout.md` section 3 area has been rewritten: narrative at 97-99 accurately reflects host-owned linkage (no auto-pass to adapter), the code block uses the canonical 4-arg callback signatures, and a new `### Host-owned linkage` H3 subsection between sections 3 and 4 explicitly documents that hosts must call `index_session/4` themselves from their ACS controller. Sections 1, 2, 4 are byte-identical to their pre-change state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove @tag :skip from test/docs/logout_recipe_drift_test.exs (flip the drift gate live)</name>
  <files>test/docs/logout_recipe_drift_test.exs</files>
  <read_first>
    - test/docs/logout_recipe_drift_test.exs (created skip-tagged by Plan 03 Task 1; Task 2 removes the skip tag so the test runs for real)
    - guides/recipes/logout.md (must already be rewritten by Task 1 above so the now-active test passes)
    - .planning/phases/40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio/40.1-CONTEXT.md (D-12 land-test-first-then-unskip pattern — Plan 03 landed the skip-tagged test; Plan 05 Task 2 unskips it after Task 1 fixes the doc)
  </read_first>
  <action>
    Edit `test/docs/logout_recipe_drift_test.exs`. Remove the single `@tag :skip` line that was added by Plan 03 Task 1. The single test in the module now runs for real on every `mix ci.docs` invocation.

    Verify after the edit:
    - The line `@tag :skip` is gone (zero occurrences in the file).
    - The test definition `test "logout.md SessionAdapter example function heads match Relyra.SessionAdapter callback arities" do` is preserved exactly (no rename, no body change).
    - No other line is touched.
    - File is `mix format`-clean.

    Then run `mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors`. The test MUST exit 0 with `1 test, 0 failures` (NOT `1 skipped` anymore). If the test fails, return to Task 1 and re-check the rewritten code block's function heads against `lib/relyra/session_adapter.ex:19-31` and the test's `behaviour_info(:callbacks)` introspection.

    Then run `mix ci.docs` end-to-end. Exit 0 confirms: (a) all presence guards still pass; (b) the troubleshooting drift test (mix.exs:160) still passes; (c) the now-live logout-recipe drift test (mix.exs:161, added by Plan 03 Task 2) passes against the Task-1-rewritten doc.

    This task is the moment the drift gate flips live. From this commit forward, any future regression in `guides/recipes/logout.md`'s SessionAdapter example arities causes `mix ci.docs` to exit non-zero.
  </action>
  <verify>
    <automated>grep -c '@tag :skip' test/docs/logout_recipe_drift_test.exs | awk '$1 == 0 {exit 0} {exit 1}' && grep -F 'test "logout.md SessionAdapter' test/docs/logout_recipe_drift_test.exs && mix format --check-formatted test/docs/logout_recipe_drift_test.exs && mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors && mix ci.docs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@tag :skip' test/docs/logout_recipe_drift_test.exs` returns 0 (skip tag fully removed)
    - The test definition `test "logout.md SessionAdapter example function heads match Relyra.SessionAdapter callback arities"` (or equivalent verbatim test name from Plan 03 Task 1) is still present
    - `mix format --check-formatted test/docs/logout_recipe_drift_test.exs` exits 0
    - `mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors` exits 0 with `1 test, 0 failures` (NOT skipped)
    - `mix ci.docs` exits 0 (full alias chain green; drift gate now live AND green)
    - No file other than `test/docs/logout_recipe_drift_test.exs` is touched in this task (Task 1's `guides/recipes/logout.md` changes are committed together with this task as the D-12 same-commit unskip)
  </acceptance_criteria>
  <done>
    `test/docs/logout_recipe_drift_test.exs` has its `@tag :skip` removed. The test now runs for real and passes against the rewritten `guides/recipes/logout.md` (Task 1). `mix ci.docs` exits 0. The drift gate is live: any future regression in the doc's SessionAdapter example arities will fail CI.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → guides/recipes/logout.md | Test reads markdown source from project tree; no untrusted input |
| Operator (host developer) → MyApp.Relyra.SessionAdapter | The example documents host responsibility; the host's session-store calls are out of Relyra's scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-05 | n/a | `guides/recipes/logout.md` rewrite + test/docs/logout_recipe_drift_test.exs unskip | accept | No new attack surface — documentation correction (no behavior change), runtime introspection in test (no privileged operations), VERIFICATION.md docs (no executable code). Phase 40.1 inherits Phase 38/39's threat posture without modification. The doc rewrite corrects a copy-paste hazard (BLOCKER 1) that would cause host compile failures; the unskip activates a drift guard against future recurrence. |

No package installs; supply-chain checkpoint T-40.1-SC not applicable.

**Key Architecture Seams (per CLAUDE.md):** This plan does NOT bypass the `Relyra.SessionAdapter` behaviour seam — it documents the correct host-side invocation pattern that respects the seam. The host's `MyApp.Relyra.SessionAdapter` implements `@behaviour Relyra.SessionAdapter` exactly as the contract requires.
</threat_model>

<verification>
Phase-level verification chain (this plan's slice, executed in order):
1. `grep -F 'def index_session(session_index, issuer, context, _opts)' guides/recipes/logout.md` → match (Task 1 Change B verified)
2. `grep -F 'login_result.principal.session_index' guides/recipes/logout.md` → match (Task 1 Change A + C verified)
3. `grep -F 'Host-owned linkage' guides/recipes/logout.md` → match (Task 1 Change C verified)
4. `grep -F '## 1. The Compliance Trap' guides/recipes/logout.md && grep -F '## 2. Mandatory Stateful Sessions' guides/recipes/logout.md && grep -F '## 4. The Real Security Boundary' guides/recipes/logout.md` → all three present (D-04 byte-untouched-sections invariant)
5. `grep -c '@tag :skip' test/docs/logout_recipe_drift_test.exs` → 0 (Task 2 verified)
6. `mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors` → exit 0 with `1 test, 0 failures` (drift gate live AND green)
7. `mix ci.docs` → exit 0 (full DOCS-04 acceptance chain green)
8. `mix test --warnings-as-errors` → exit 0 (no other test regressed)
</verification>

<success_criteria>
- BLOCKER 1 from v1.4-MILESTONE-AUDIT.md is closed: the SessionAdapter code example in `guides/recipes/logout.md` uses the canonical 4-arg callback signatures; a host copy-pasting the example now compiles cleanly against `@behaviour Relyra.SessionAdapter`
- WARNING 2 from v1.4-MILESTONE-AUDIT.md is closed: the host-owned linkage policy (D-01) is explicitly documented in a new `### Host-owned linkage` subsection between sections 3 and 4 of the guide
- The drift gate (Plan 03's test) is unskipped and PASSES against the rewritten doc — proving the gate works and is now live
- `mix ci.docs` exits 0 end-to-end (DOCS-04 acceptance chain green)
- Sections 1, 2, 4 of `guides/recipes/logout.md` are byte-untouched (D-04 scope fence)
- No file under `lib/` is modified (D-01 + scope fence — `consume_response/3` is NOT auto-wired to `index_session/4`)
</success_criteria>

<output>
Create `.planning/phases/40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio/40.1-05-SUMMARY.md` when done.
</output>
