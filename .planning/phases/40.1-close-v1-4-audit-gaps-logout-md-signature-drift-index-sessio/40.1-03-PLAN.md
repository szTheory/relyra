---
phase: 40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/docs/logout_recipe_drift_test.exs
  - mix.exs
autonomous: true
requirements: [DOCS-04, SLO-01]
tags: [drift-test, ci-gate, docs, slo]

must_haves:
  truths:
    - "test/docs/logout_recipe_drift_test.exs exists and uses Relyra.SessionAdapter.behaviour_info(:callbacks) for runtime introspection (D-05)"
    - "The drift test does NOT contain a hardcoded literal like `arity == 4` or `4 ==` for the expected arity — runtime introspection only"
    - "The drift test is initially skip-tagged so it lands GREEN against the current broken logout.md (per D-12 Wave 1 land-test-first-then-unskip pattern)"
    - "mix.exs ci.docs alias contains 'cmd mix test test/docs/logout_recipe_drift_test.exs' as its own dedicated line (Phase 30 hollow-gate invariant per D-06)"
    - "The new mix.exs line is positioned in ci.docs after the existing troubleshooting drift test line at mix.exs:160 (consistent with Phase 40 precedent)"
    - "mix ci.docs passes — the new drift test is skipped (not failing) and the existing alias chain is unbroken"
  artifacts:
    - path: "test/docs/logout_recipe_drift_test.exs"
      provides: "Bidirectional drift gate for guides/recipes/logout.md SessionAdapter code example"
      contains: "behaviour_info(:callbacks)"
      min_lines: 40
    - path: "mix.exs"
      provides: "ci.docs alias wiring for the new drift test"
      contains: "logout_recipe_drift_test.exs"
  key_links:
    - from: "test/docs/logout_recipe_drift_test.exs"
      to: "Relyra.SessionAdapter"
      via: "behaviour_info(:callbacks) runtime introspection"
      pattern: "Relyra.SessionAdapter.behaviour_info"
    - from: "test/docs/logout_recipe_drift_test.exs"
      to: "guides/recipes/logout.md"
      via: "File.read! + regex extraction of `def index_session(` and `def terminate_by_session_index(` function heads"
      pattern: "guides/recipes/logout.md"
    - from: "mix.exs ci.docs alias"
      to: "test/docs/logout_recipe_drift_test.exs"
      via: "cmd mix test line (Phase 30 hollow-gate invariant — own OS process per test)"
      pattern: "cmd mix test test/docs/logout_recipe_drift_test.exs"
---

<objective>
Land the drift-prevention test for `guides/recipes/logout.md`'s SessionAdapter code example: a new ExUnit test at `test/docs/logout_recipe_drift_test.exs` that reads the guide, extracts the example's function heads via regex, introspects `Relyra.SessionAdapter.behaviour_info(:callbacks)` for the real arities, and asserts the example heads match the canonical 4-arg signature (`session_index, issuer, context, opts`). Wire it into the `ci.docs` Mix alias as its own `cmd mix test` line (Phase 30 hollow-gate invariant).

The test ships **skip-tagged** in this plan — so this plan lands a GREEN ci.docs against the still-broken `logout.md`. Plan 05 (Wave 2) rewrites `logout.md` AND unskips this test in the same commit, flipping the gate live.

Purpose: Create a build-time drift gate so the BLOCKER 1 class of signature drift cannot regress silently if `SessionAdapter` callbacks evolve in the future. Runtime introspection (per D-05) means the test follows whatever the code says — no hardcoded arity literals to fall out of sync.

Output: New file `test/docs/logout_recipe_drift_test.exs` (skip-tagged); one new `cmd mix test` line in `mix.exs` `ci.docs` alias near line 160.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio/40.1-CONTEXT.md
@CLAUDE.md
@test/docs/troubleshooting_drift_test.exs
@mix.exs
@lib/relyra/session_adapter.ex
@guides/recipes/logout.md

<interfaces>
<!-- Key contracts for the executor. Extracted from lib/relyra/session_adapter.ex. -->
<!-- Use these directly — no codebase exploration needed. -->

From lib/relyra/session_adapter.ex (canonical callback signatures, lines 19-31):

```elixir
@callback index_session(
            session_index :: binary(),
            issuer :: binary(),
            context :: map(),
            opts :: keyword()
          ) :: {:ok, term()} | {:error, Error.t()}

@callback terminate_by_session_index(
            session_index :: binary(),
            issuer :: binary(),
            context :: map(),
            opts :: keyword()
          ) :: {:ok, term()} | {:error, Error.t()}
```

At runtime, `Relyra.SessionAdapter.behaviour_info(:callbacks)` returns a keyword-shaped list of `{atom, arity}` pairs:
  [
    establish_session: 3,
    index_session: 4,
    revoke_session: 4,
    terminate_by_session_index: 4
  ]
The test MUST use this introspection — never a hardcoded `== 4`.

From mix.exs ci.docs alias (lines 151-164), the anchor pattern for the new line:

```elixir
"cmd mix test test/docs/troubleshooting_drift_test.exs --warnings-as-errors",
```

This is the Phase 40 precedent. The new logout drift line goes immediately after this one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skip-tagged test/docs/logout_recipe_drift_test.exs</name>
  <files>test/docs/logout_recipe_drift_test.exs</files>
  <read_first>
    - test/docs/troubleshooting_drift_test.exs (the direct template per D-05 — copy the moduledoc framing, the `use ExUnit.Case, async: true` pattern, the regex module attributes, the bidirectional MapSet pattern, and the format_*/1 failure-vocabulary helpers)
    - lib/relyra/session_adapter.ex (canonical callbacks at lines 19-31; `behaviour_info(:callbacks)` returns `[{name, arity}, ...]` after compilation)
    - guides/recipes/logout.md (the artifact under guard; current state has the WRONG signatures at lines 107 and 121 — `def index_session(connection, login_result, local_session_id, opts)` and `def terminate_by_session_index(connection, session_index, _name_id, _opts)`; the test will fail today, so it MUST ship skip-tagged)
    - .planning/phases/40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio/40.1-CONTEXT.md (D-05 runtime-introspection rule; D-06 Phase 30 hollow-gate invariant; D-12 Wave 1 skip-tagged pattern)
    - CLAUDE.md (Key Architecture Seams: behaviour seams never bypass; Testing Requirements: hollow-gate invariant)
  </read_first>
  <action>
    Create `test/docs/logout_recipe_drift_test.exs`. Model the structure on `test/docs/troubleshooting_drift_test.exs` (the Phase 40 precedent referenced in CONTEXT D-05). Use ExUnit, `async: true`.

    The test module name is `Relyra.Docs.LogoutRecipeDriftTest`.

    Required structure:

    1. **moduledoc** explaining the gate's purpose (mirroring the troubleshooting_drift_test moduledoc voice): the `guides/recipes/logout.md` SessionAdapter example is the headline operator guide for SLO-01's adapter contract. Without a build-time gate, the example rots the first time `SessionAdapter` callbacks evolve. The test enforces that the example's `def index_session(...)` and `def terminate_by_session_index(...)` function heads have the same arity as the callbacks reported by `Relyra.SessionAdapter.behaviour_info(:callbacks)` — runtime introspection, never a hardcoded arity literal.

       In the moduledoc, also document: (a) the Phase 30 hollow-gate invariant — the test runs as its own `cmd mix test` line in `ci.docs`, not bundled into a bare `test` step; (b) the D-05 contract — `behaviour_info(:callbacks)` is the canonical truth, not a literal `== 4`; (c) the skip-tag rationale during Phase 40.1 Wave 1 (the doc still has wrong signatures at lines 107/121 until Plan 05 lands).

    2. **Module attributes** (mirroring troubleshooting_drift_test):
       - `@doc_path "guides/recipes/logout.md"`
       - `@callback_names [:index_session, :terminate_by_session_index]` — the two callbacks whose example heads must stay in sync
       - `@head_pattern ~r/^\s*def\s+(index_session|terminate_by_session_index)\s*\(([^)]*)\)/m` — extracts function name + raw parameter list from each `def` line in the markdown's elixir code block
       - `@behaviour_module Relyra.SessionAdapter`

    3. **Single test** (skip-tagged):

       ```
       @tag :skip
       test "logout.md SessionAdapter example function heads match Relyra.SessionAdapter callback arities" do
         <body>
       end
       ```

       The test body:
       a. `callbacks = @behaviour_module.behaviour_info(:callbacks)` — returns `[{name, arity}, ...]`.
       b. `expected_arities = callbacks |> Enum.filter(fn {name, _} -> name in @callback_names end) |> Map.new()` — yields `%{index_session: 4, terminate_by_session_index: 4}`. NO literal `4` ever appears in the test source for this map.
       c. `doc = File.read!(@doc_path)` — read the guide.
       d. `heads = Regex.scan(@head_pattern, doc, capture: :all_but_first)` — yields `[[name_str, params_str], ...]` for every `def index_session(...)` or `def terminate_by_session_index(...)` in the markdown.
       e. **Coverage assertion:** every callback name in `@callback_names` MUST appear at least once in `heads`. Failure message format: `"Missing doc example for callback: :<name>/<arity> — add a def #{name}(...) line to #{@doc_path} (arity from #{inspect(@behaviour_module)}.behaviour_info(:callbacks))"`.
       f. **Arity parity assertion:** for each extracted head, parse the params list — strip empty/whitespace, count the comma-separated parameters (treating `_opts` as one parameter, same as `opts`). Assert the parsed param count equals `expected_arities[name_atom]`. Failure message format: `"Arity drift in #{@doc_path}: example shows def #{name}(<params>) (arity=#{actual}) but Relyra.SessionAdapter expects arity #{expected} — runtime callback signature is the source of truth"`.

       Parameter counting helper (private function `count_params/1`): split on `,`, trim each fragment, filter `String.length > 0`, return `length`. Handle the empty-arglist case (`def foo()`) cleanly: returns 0.

    4. **Helpers** below the test:
       - `defp count_params(params_string)` — comma-split + trim + non-empty count.
       - (optional) `defp format_missing_callback/1` and `defp format_arity_drift/3` if you prefer to factor the failure-message strings into named helpers (mirroring troubleshooting_drift_test's `format_missing/2` and `format_stale/1`).

    5. **Critical constraints (must verify in self-review before commit):**
       - **No literal `4` anywhere in the test that represents the expected arity.** Search the file: the digit `4` must not appear adjacent to `arity`, `==`, `expected`, or callback-name strings. The only acceptable `4` occurrences would be inside a moduledoc sentence describing the canonical arity (e.g., "the four-arg signature `(session_index, issuer, context, opts)`") — and even there, prefer prose without the literal digit if possible.
       - The test MUST be skip-tagged with `@tag :skip` at this stage. Plan 05 removes the `@tag :skip` line in the same commit that rewrites `logout.md`.
       - `use ExUnit.Case, async: true`.
       - File aligns with `mix format` (run `mix format test/docs/logout_recipe_drift_test.exs` after creation).
  </action>
  <verify>
    <automated>test -f test/docs/logout_recipe_drift_test.exs && grep -F 'behaviour_info(:callbacks)' test/docs/logout_recipe_drift_test.exs && grep -F '@tag :skip' test/docs/logout_recipe_drift_test.exs && grep -F 'Relyra.SessionAdapter' test/docs/logout_recipe_drift_test.exs && grep -F 'guides/recipes/logout.md' test/docs/logout_recipe_drift_test.exs && ! grep -E 'arity\s*==\s*4|==\s*4\s*,|expected.*=\s*4\b' test/docs/logout_recipe_drift_test.exs && mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/docs/logout_recipe_drift_test.exs` exists
    - File contains the literal string `Relyra.SessionAdapter.behaviour_info(:callbacks)` (or the equivalent via a `@behaviour_module` attribute resolved at compile time — verifier may grep both forms)
    - File contains `@tag :skip` on the single test
    - File contains a regex or string anchor referencing `guides/recipes/logout.md`
    - File does NOT contain any of these drift-prone literal patterns: `arity == 4`, `expected = 4`, `expected_arity = 4`, `assert ... == 4`, `4 == arity`
    - `mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors` exits 0 (test is skipped, so reports `1 test, 0 failures, 1 skipped` or equivalent)
    - File uses `use ExUnit.Case, async: true`
    - File is `mix format`-clean (`mix format --check-formatted test/docs/logout_recipe_drift_test.exs` exits 0)
  </acceptance_criteria>
  <done>
    `test/docs/logout_recipe_drift_test.exs` exists, uses runtime `behaviour_info(:callbacks)` introspection per D-05, is skip-tagged per D-12 Wave 1 pattern, and `mix test` against it exits 0 (skipped). The file contains no hardcoded arity literals.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the new drift test into mix.exs ci.docs alias as its own cmd mix test line</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (current `ci.docs` alias at lines 151-164; existing troubleshooting drift line at line 160 is the anchor template)
    - .planning/phases/40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio/40.1-CONTEXT.md (D-06 Phase 30 hollow-gate invariant; D-12 — this is the same wave as the test creation, but the mix.exs line is what makes the test actually run in CI)
    - CLAUDE.md (Testing Requirements: `mix ci.security` and `ci.docs` must keep each suite as its own `cmd mix test` line — never collapse to bare `test`)
    - test/docs/troubleshooting_drift_test.exs (precedent — wired the same way at mix.exs:160)
  </read_first>
  <action>
    Edit `mix.exs`. In the `ci.docs` alias list (currently lines 151-164), insert a new line **immediately after** the existing line:

    ```
    "cmd mix test test/docs/troubleshooting_drift_test.exs --warnings-as-errors",
    ```

    The new line is:

    ```
    "cmd mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors",
    ```

    **Placement rationale:** Phase 40 established the Phase-30-hollow-gate-compliant precedent at mix.exs:160 for the troubleshooting drift test. Group both drift tests together — troubleshooting first (its phase landed first in v1.4), then logout_recipe — for chronological clarity and easy grep.

    **Required invariants:**
    - The new line MUST be its own `cmd mix test` line (NOT collapsed into a bare `test` step). CLAUDE.md's "Testing Requirements" calls this out explicitly as the Phase 30 hollow-gate invariant.
    - The `--warnings-as-errors` flag MUST be present (matches the troubleshooting precedent + project-wide convention).
    - No other `ci.docs` line is reordered or removed. The order of presence guards (lines 152-159), the existing troubleshooting drift line (160), and the trailing batteries_included/install lines (161-163) is preserved.
    - Do NOT touch any other alias (`qa`, `ci.fast`, `ci.security`, `ci.verify`, `ci.conformance`, `ci.integration`, `ci.admin_ui`, `ci.oban_smoke`, `ci.release`). Particularly, the `ci.security` alias byte-identity vs base commit c80742c is a permanent invariant per the 40-VERIFICATION.md Behavioral Spot-Check (Phase 30 invariant). This plan ONLY touches `ci.docs`.
    - File must remain `mix format`-clean.

    After editing, run `mix ci.docs` to confirm the alias chain still completes end-to-end. The new drift test is skipped (Task 1 added `@tag :skip`), so `ci.docs` exit 0 means the wiring works AND the skip-tag was honored.
  </action>
  <verify>
    <automated>grep -n 'cmd mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors' mix.exs && grep -c 'cmd mix test test/docs/logout_recipe_drift_test.exs' mix.exs | awk '$1 == 1 {exit 0} {exit 1}' && awk '/ci\.security/{flag=1} flag && /cmd mix test/{print}' mix.exs | grep -c 'test/security/ci_gate_integrity_test.exs' | awk '$1 == 1 {exit 0} {exit 1}' && mix format --check-formatted mix.exs && mix ci.docs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'cmd mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors' mix.exs` returns exactly one matching line, positioned inside the `ci.docs` alias block (lines 151-164 area)
    - The new line follows the existing line `"cmd mix test test/docs/troubleshooting_drift_test.exs --warnings-as-errors",` (group the two drift tests together)
    - `ci.security` alias is byte-identical to its previous state (no accidental edit) — verify by `git diff mix.exs` showing additions only inside the `ci.docs` block
    - `mix format --check-formatted mix.exs` exits 0
    - `mix ci.docs` exits 0 (the new test is skipped per Task 1, so the alias chain completes green)
    - The Phase 30 hollow-gate invariant is preserved: the new line is its own `cmd mix test` line, not folded into a bare `test` step
  </acceptance_criteria>
  <done>
    `mix.exs` `ci.docs` alias contains a new dedicated `cmd mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors` line positioned after the existing troubleshooting drift line. `mix ci.docs` exits 0. No other alias is touched. The Phase 30 hollow-gate invariant is preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → guides/recipes/logout.md | Test reads markdown source from project tree; no untrusted input |
| Test process → Relyra.SessionAdapter module | Compile-time + runtime introspection via behaviour_info; no privileged operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-03 | n/a | `test/docs/logout_recipe_drift_test.exs` + `mix.exs` ci.docs wiring | accept | No new attack surface — documentation correction (no behavior change), runtime introspection in test (no privileged operations), VERIFICATION.md docs (no executable code). Phase 40.1 inherits Phase 38/39's threat posture without modification. The new test only reads files and invokes `behaviour_info/1` — same risk class as the Phase 40 troubleshooting drift test. |

No package installs; supply-chain checkpoint T-40.1-SC not applicable.

**Hollow-gate invariant defense:** Task 2's `cmd mix test` line is its own OS process per the Phase 30 invariant. The 40-VERIFICATION.md test (`ci_gate_integrity_test.exs`) runs as part of `ci.security`, NOT `ci.docs`, so the new line does not require an `@gated_suites` amendment.
</threat_model>

<verification>
Phase-level verification chain (this plan's slice):
1. `test -f test/docs/logout_recipe_drift_test.exs` → exit 0
2. `grep 'behaviour_info(:callbacks)' test/docs/logout_recipe_drift_test.exs` → match (runtime introspection rule D-05)
3. `grep '@tag :skip' test/docs/logout_recipe_drift_test.exs` → match (D-12 Wave 1 skip-pattern)
4. `mix test test/docs/logout_recipe_drift_test.exs --warnings-as-errors` → exit 0 (skipped, not failing)
5. `grep -n 'cmd mix test test/docs/logout_recipe_drift_test.exs' mix.exs` → exactly one match inside ci.docs block
6. `mix ci.docs` → exit 0 (alias chain green, new test honored as skipped)
7. `git diff mix.exs` shows additions only inside the ci.docs block (ci.security byte-identity preserved)
</verification>

<success_criteria>
- New test file lands in `test/docs/` with runtime `behaviour_info(:callbacks)` introspection (D-05) and `@tag :skip` (D-12)
- The test contains NO hardcoded arity literals (`== 4`, `expected = 4`, etc.) — search must return empty
- New `cmd mix test` line lands in `mix.exs` `ci.docs` alias as its own OS process (D-06 Phase 30 hollow-gate invariant)
- `mix ci.docs` exits 0 (skipped test counts as passed; chain unbroken)
- No file outside `test/docs/logout_recipe_drift_test.exs` and `mix.exs` is touched (D-01 + scope fence)
</success_criteria>

<output>
Create `.planning/phases/40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio/40.1-03-SUMMARY.md` when done.
</output>
