---
phase: 40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md
autonomous: true
requirements: [SLO-01]
tags: [verification, closure-phase, slo]

must_haves:
  truths:
    - "38-VERIFICATION.md exists at .planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md"
    - "Document has YAML frontmatter (phase, verified, status, score, overrides_applied) matching the 40-VERIFICATION.md template"
    - "Observable Truths table has between 8 and 12 rows, each substantiated by a re-runnable grep/file-existence check (D-09)"
    - "Required Artifacts table cites lib/relyra/session_adapter.ex, lib/relyra/security/signature.ex, lib/relyra/security/logout_validator.ex, lib/relyra.ex, lib/relyra/protocol/logout_request.ex, lib/relyra/protocol/logout_response.ex, test/protocol/logout_pipeline_test.exs"
    - "Behavioral Spot-Checks table includes mix test test/protocol/logout_pipeline_test.exs"
    - "Requirements Coverage row maps SLO-01 to ✓ SATISFIED, citing all 4 plan SUMMARYs"
    - "Gaps Summary explicitly acknowledges WARNING 2 (index_session/4 host-owned linkage per D-01) as a documented, deliberate policy — NOT a gap"
  artifacts:
    - path: ".planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md"
      provides: "Retroactive goal-backward verification report for Phase 38 (SLO-01)"
      contains: "## Goal Achievement"
  key_links:
    - from: "38-VERIFICATION.md Truth rows"
      to: "test/protocol/logout_pipeline_test.exs"
      via: "Evidence column cites file paths + line ranges + grep commands"
      pattern: "test/protocol/logout_pipeline_test.exs"
    - from: "38-VERIFICATION.md Requirements Coverage"
      to: "REQUIREMENTS.md SLO-01"
      via: "Direct ID reference in Requirements Coverage table"
      pattern: "SLO-01"
---

<objective>
Retroactively generate `38-VERIFICATION.md` for Phase 38 (Single Logout Core & Security), synthesizing the goal-backward verification artifact from existing evidence (4 plan SUMMARYs, 38-VALIDATION.md, test/protocol/logout_pipeline_test.exs, and the audit's WIRED/PASS table at v1.4-MILESTONE-AUDIT.md:105-121).

Purpose: Close the verification orphan flagged in v1.4-MILESTONE-AUDIT.md so SLO-01 can be marked ✓ SATISFIED (currently `partial` because verification artifact is missing). Per D-07/D-08/D-09/D-10, this is a closure-phase pattern — generate from existing evidence, do NOT re-run goal-backward verification.

Output: `.planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md` following the structural template of `40-VERIFICATION.md` (Observable Truths → Required Artifacts → Key Link Verification → Data-Flow Trace → Behavioral Spot-Checks → Probe Execution → Requirements Coverage → Anti-Patterns → Human Verification Required → Gaps Summary).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.4-MILESTONE-AUDIT.md
@.planning/phases/40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio/40.1-CONTEXT.md
@.planning/phases/40-operational-polish-error-taxonomy/40-VERIFICATION.md
@.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-VERIFICATION.md
@.planning/phases/38-single-logout-slo-core-security/38-VALIDATION.md
@.planning/phases/38-single-logout-slo-core-security/38-01-SUMMARY.md
@.planning/phases/38-single-logout-slo-core-security/38-02-SUMMARY.md
@.planning/phases/38-single-logout-slo-core-security/38-03-SUMMARY.md
@.planning/phases/38-single-logout-slo-core-security/38-04-SUMMARY.md
@test/protocol/logout_pipeline_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate 38-VERIFICATION.md from existing Phase 38 artifacts</name>
  <files>.planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/40-operational-polish-error-taxonomy/40-VERIFICATION.md (structural template per D-08)
    - .planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-VERIFICATION.md (closure-phase precedent per D-10)
    - .planning/phases/40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio/40.1-CONTEXT.md (D-07, D-08, D-09, D-10 — the four decisions that govern this artifact's content and shape)
    - .planning/v1.4-MILESTONE-AUDIT.md (the WIRED/PASS table at lines 105-121 is essentially the pre-written Truth row source)
    - .planning/phases/38-single-logout-slo-core-security/38-VALIDATION.md (source of Truths/Artifacts/Key Links)
    - .planning/phases/38-single-logout-slo-core-security/38-01-SUMMARY.md (SessionAdapter callbacks + verify_redirect_signature/4 evidence)
    - .planning/phases/38-single-logout-slo-core-security/38-02-SUMMARY.md (LogoutRequest/LogoutResponse models evidence)
    - .planning/phases/38-single-logout-slo-core-security/38-03-SUMMARY.md (LogoutValidator strict pipeline evidence)
    - .planning/phases/38-single-logout-slo-core-security/38-04-SUMMARY.md (facade integration + E2E tests evidence)
    - test/protocol/logout_pipeline_test.exs (5 E2E flows that substantiate 5 of the Truth rows)
    - lib/relyra/session_adapter.ex (canonical callback signatures at 19-31)
    - lib/relyra.ex (consume_logout/3 at 272-303; handle_idp_initiated_logout auto-wires terminate at 343-361)
  </read_first>
  <action>
    Create `.planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md` following the section-by-section structure of `40-VERIFICATION.md`. Use the closure-phase pattern per D-10 — synthesize from existing evidence; do NOT re-run goal-backward verification.

    Required sections (in order, all present even if marked n/a):
    1. YAML frontmatter: `phase: 38-single-logout-slo-core-security`, `verified: 2026-05-27T<HH:MM:SS>Z`, `status: passed`, `score: <N>/<N> must-haves verified`, `overrides_applied: 0`.
    2. H1 header: `# Phase 38: Single Logout (SLO) Core & Security Verification Report`.
    3. **Phase Goal** quoted verbatim from ROADMAP.md: "Users and Identity Providers can securely terminate sessions across the federation via verified SAML Single Logout flows." Plus `**Verified:**`, `**Status:** passed`, `**Re-verification:** No — retroactive closure-phase verification per Phase 40.1 D-07/D-10`.
    4. `## Goal Achievement` → `### Observable Truths` table with 8-12 rows. Each row: `| # | Truth | Status | Evidence |`. Source the Truths from `38-VALIDATION.md` lines 8-12, the audit's "End-to-End Flow Coverage" table at v1.4-MILESTONE-AUDIT.md:114-121, and the audit's "Cross-Phase Integration Findings" table at v1.4-MILESTONE-AUDIT.md:105-110. The Evidence column for every row MUST include a concrete re-runnable check (per D-09): either a `grep` command with expected count, a `test -f` invocation, or a `mix test <path>` invocation with expected exit. Examples of acceptable evidence shapes: `grep -nE '^  @callback index_session' lib/relyra/session_adapter.ex` returns line 19; `test -f lib/relyra/security/logout_validator.ex` exits 0; `mix test test/protocol/logout_pipeline_test.exs --warnings-as-errors` reports `6 tests, 0 failures`.

       Required Truth rows (minimum — add more from the audit's PASS table if needed to reach 8-12):
       - SessionAdapter has `index_session/4` and `terminate_by_session_index/4` callbacks at canonical signatures `(session_index, issuer, context, opts)` (from 38-VALIDATION Truth #1; evidence: grep `lib/relyra/session_adapter.ex:19-31`).
       - HTTP-Redirect signatures verified against raw query octets via `Signature.verify_redirect_signature/4` (38-VALIDATION Truth #2; evidence: grep + `mix test test/relyra/security/signature_test.exs`).
       - `LogoutValidator` enforces strict Parse → Verify → Replay → Execute pipeline for POST and Verify → Inflate → Parse → Replay → Execute for Redirect (38-03-SUMMARY; evidence: `test -f lib/relyra/security/logout_validator.ex` + `mix test test/relyra/security/logout_validator_test.exs`).
       - Replay protection enforced on all consumed logout messages (38-VALIDATION Truth #4; evidence: `mix test test/protocol/logout_pipeline_test.exs:79-97` and `:121-138`).
       - Facade `Relyra.consume_logout/3` invokes `SessionAdapter.terminate_by_session_index/4` on valid IdP-initiated logout (audit WIRED row 2; evidence: grep `lib/relyra.ex:343-361` for `terminate_by_session_index` + `mix test test/protocol/logout_pipeline_test.exs:37-58`).
       - Tampered signature rejected as `:invalid_signature` BEFORE any session mutation (audit PASS row 2; evidence: `test/protocol/logout_pipeline_test.exs:60-77` `refute_receive` proves order).
       - Replay rejection on Redirect binding produces `:replayed_assertion` (audit PASS row 3; evidence: `test/protocol/logout_pipeline_test.exs:79-97`).
       - SP-initiated LogoutResponse round-trip + replay (audit PASS row 4; evidence: `test/protocol/logout_pipeline_test.exs:100-138`).
       - POST unsigned LogoutRequest rejected with `:missing_signature` (audit PASS row 5; evidence: `test/protocol/logout_pipeline_test.exs:140-152`).
       - `LogoutRequest`/`LogoutResponse` models built directly on SaxyTree root node — single parse path preserved (38-02-SUMMARY decisions; evidence: grep `lib/relyra/protocol/logout_request.ex` for SaxyTree usage).
       - Telemetry events `[:logout, :start]` and `[:logout, :consume]` emitted around core flows (38-04-SUMMARY accomplishment #2; evidence: grep `lib/relyra.ex` for `Relyra.Telemetry.span` with `:logout` atom).
    5. `**Score:** N/N truths verified` line after the Truths table.
    6. `### Required Artifacts` table: 7 rows minimum, one per file listed in 38-VALIDATION.md "Artifacts" section, PLUS the 4 plan SUMMARY files PLUS test/protocol/logout_pipeline_test.exs. Each row: `| Artifact | Expected | Status | Details |`. Status column = `✓ VERIFIED`. Details column cites line counts or file checksums where useful.
    7. `### Key Link Verification` table: 4-6 rows, one per "Key Links" entry in 38-VALIDATION.md plus the audit's "Cross-Phase Integration Findings" rows that show WIRED.
    8. `### Data-Flow Trace (Level 4)` table: Even if minimal, list at minimum the `session_index` data flow: Source (`%Relyra.Principal{}.session_index` from `normalize_consume_result/1` at `lib/relyra.ex:406-413`) → produces real data: Yes (verified via `test/protocol/logout_pipeline_test.exs:50-57` assertion on `req.session_index == "session_abc123"`).
    9. `### Behavioral Spot-Checks` table: 4-6 rows of `mix test`/`grep`/`test -f` commands with expected results. MUST include: `mix test test/protocol/logout_pipeline_test.exs --warnings-as-errors` exits 0; `test -f lib/relyra/security/logout_validator.ex` exits 0; `grep -nE '@callback index_session\(' lib/relyra/session_adapter.ex` returns one match at line 19.
    10. `### Probe Execution` table: List `mix test test/protocol/logout_pipeline_test.exs` and (if applicable) `mix ci.security` as the end-to-end gate.
    11. `### Requirements Coverage` table: One row for SLO-01 mapping to all four Phase 38 plans (38-01, 38-02, 38-03, 38-04). Status: `✓ SATISFIED`. Evidence column cites the four SUMMARYs + the E2E test file.
    12. `### Anti-Patterns Found` table: explicitly note "No anti-patterns introduced by Phase 38" (consistent with 40-VERIFICATION pattern). Append the standard pre-existing `mix format` drift note for `test/security/xml/adversarial_crypto_test.exs` — out of scope, byte-identical to base commit c80742c per `40-VERIFICATION.md` and Phase 40 deferred-items.md.
    13. `### Human Verification Required`: "None. Phase 38 deliverables are executable code + a deterministic E2E test suite. All verification steps are programmatically checkable via the commands above."
    14. `### Gaps Summary`: explicitly document the v1.4-MILESTONE-AUDIT.md WARNING 2 disposition: "`SessionAdapter.index_session/4` has no in-tree caller in `consume_response/3`. Per Phase 40.1 D-01, this is the deliberate **host-owned linkage** policy — Relyra dispatches the callback but the host invokes it from the ACS controller after `Relyra.consume_response/3` returns, reading from `login_result.principal.session_index`. Documented in `guides/recipes/logout.md` per Phase 40.1 Plan 05 (D-02/D-03/D-04). This is NOT a gap — it is the explicit policy resolution per the CLAUDE.md 'behaviour seams are never bypassed' rule. The asymmetry with `consume_logout/3` auto-wiring `terminate_by_session_index/4` is defensible: terminate operates on inbound-message data; index needs the host's local session ID that Relyra does not have."
    15. Footer: `---` then `_Verified: <ISO 8601 timestamp>Z_` and `_Verifier: Claude (gsd-verifier, retroactive closure-phase artifact per Phase 40.1)_`.

    Do NOT modify any file other than the newly-created `38-VERIFICATION.md`. Do NOT modify any file under `lib/`, `test/`, or `mix.exs`. This is a `.planning/`-only edit.
  </action>
  <verify>
    <automated>test -f .planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md && grep -c '^| ' .planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md | awk '$1 >= 30 {exit 0} {exit 1}' && grep -F 'SLO-01' .planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md && grep -F 'test/protocol/logout_pipeline_test.exs' .planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md && grep -F 'host-owned linkage' .planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md && grep -E '^## Goal Achievement$' .planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md && grep -E '^### Observable Truths$' .planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md && grep -E '^### Gaps Summary$' .planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md` exists
    - File contains YAML frontmatter with `phase: 38-single-logout-slo-core-security` and `status: passed`
    - File contains `## Goal Achievement` followed by `### Observable Truths` with a markdown table
    - Observable Truths table has 8 to 12 data rows (between `| # | Truth | Status | Evidence |` header and the next `### ` heading)
    - Every Truth row's Evidence column contains at least one of: `grep`, `mix test`, `test -f`, or a `lib/`/`test/` file path with line range
    - File contains `test/protocol/logout_pipeline_test.exs` referenced at least 5 times (one per E2E flow)
    - File contains `### Requirements Coverage` section with `SLO-01` and `✓ SATISFIED`
    - File contains `### Gaps Summary` section with the literal phrase `host-owned linkage` (the D-01 disposition for WARNING 2)
    - File does NOT mark WARNING 2 as a gap — it explicitly documents the deliberate policy
    - No other file is modified: `git status --porcelain | grep -v '^?? .planning/phases/40.1-' | grep -v '38-VERIFICATION.md$' | grep -v '^M .planning/' | wc -l` returns 0 outside the new file
  </acceptance_criteria>
  <done>
    `.planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md` exists with full template structure (10+ sections), 8-12 verified Observable Truths with re-runnable evidence per D-09, SLO-01 marked SATISFIED in Requirements Coverage, and a Gaps Summary section that explicitly disposes of WARNING 2 as the deliberate D-01 host-owned-linkage policy (NOT a gap).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | No trust boundaries crossed — this task creates a `.planning/` documentation artifact only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-01 | n/a | `38-VERIFICATION.md` | accept | No new attack surface — documentation correction (no behavior change), runtime introspection in test (no privileged operations), VERIFICATION.md docs (no executable code). Phase 40.1 inherits Phase 38/39's threat posture without modification. |

No package installs; supply-chain checkpoint T-40.1-SC not applicable.
</threat_model>

<verification>
Phase-level verification chain (only this plan's slice):
1. `test -f .planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md` → exit 0
2. Manual read of the artifact confirms it follows the section structure of `40-VERIFICATION.md`
3. Every Truth row's Evidence command is independently re-runnable today (per D-09)
4. SLO-01 disposition is `✓ SATISFIED` (closure-phase pattern per D-10 — closes the verification orphan flagged by v1.4-MILESTONE-AUDIT.md)
</verification>

<success_criteria>
- `38-VERIFICATION.md` created with 8-12 Observable Truths, each with a re-runnable grep/test command in the Evidence column
- All structural sections present (frontmatter, Goal Achievement, Required Artifacts, Key Link Verification, Data-Flow Trace, Behavioral Spot-Checks, Probe Execution, Requirements Coverage, Anti-Patterns, Human Verification, Gaps Summary)
- WARNING 2 from v1.4-MILESTONE-AUDIT.md is explicitly resolved in Gaps Summary as the deliberate D-01 host-owned-linkage policy
- No files outside `.planning/phases/38-single-logout-slo-core-security/38-VERIFICATION.md` are touched
</success_criteria>

<output>
Create `.planning/phases/40.1-close-v1-4-audit-gaps-logout-md-signature-drift-index-sessio/40.1-01-SUMMARY.md` when done.
</output>
