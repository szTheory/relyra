---
phase: 42-stepwise-login-trace-liveview
plan: 04
type: execute
wave: 3
depends_on:
  - 42-02
files_modified:
  - lib/mix/tasks/relyra.trace.ex
  - test/security/login_trace_test.exs
  - mix.exs
  - test/security/ci_gate_integrity_test.exs
autonomous: true
requirements:
  - TRACE-02
  - TRACE-03
must_haves:
  truths:
    - "`mix relyra.trace --repo MyApp.Repo --connection CONNECTION_ID --last N` prints exported login traces."
    - "`test/security/login_trace_test.exs` fails if LiveView or CLI output contains forbidden material."
    - "`mix ci.security` runs login trace suite as dedicated `cmd mix test` process."
  artifacts:
    - path: "lib/mix/tasks/relyra.trace.ex"
      provides: "Headless trace inspection CLI."
    - path: "test/security/login_trace_test.exs"
      provides: "TRACE-02 security gate + TRACE-03 redaction equivalence."
---

<objective>
Close TRACE-02 and TRACE-03: headless `mix relyra.trace` task, security corpus proving redaction equivalence between LiveView and CLI, and hollow-gate wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-stepwise-login-trace-liveview/42-CONTEXT.md
@lib/mix/tasks/relyra.diagnostic.ex
@lib/relyra/login_trace/export.ex
@test/security/metadata_attribute_injection_test.exs
@test/security/ci_gate_integrity_test.exs
@mix.exs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| TM-01 | CLI prints raw assertion XML / PEM / SignatureValue | high | Security test drives real consume fixture; assert forbidden substrings absent from IO output |
| TM-02 | LiveView/CLI redaction drift | medium | Both call `LoginTrace.Export`; test compares exported maps used by each path |
| TM-05 | Hollow ci.security gate | high | Dedicated `cmd mix test test/security/login_trace_test.exs` + `@gated_suites` entry |
</threat_model>

<tasks>

<task type="auto">
  <name>Add mix relyra.trace task</name>
  <read_first>
    - lib/mix/tasks/relyra.diagnostic.ex
    - lib/relyra/live_admin/query.ex
  </read_first>
  <action>
    Create `lib/mix/tasks/relyra.trace.ex`:

    - `Mix.Task` with `@shortdoc` for headless login trace inspection
    - `run/1`: `Mix.Task.run("app.start")`
    - Required: `--repo` (string module), `--connection` (connection_id string)
    - Optional: `--last` (integer, default 20)
    - Resolve repo atom, fetch login traces via `Query.get_login_traces/4` with a synthetic admin scope matching existing diagnostic patterns (or internal `Query` helper used by tests)
    - Print human-readable lines: timestamp, action, hashed correlation, per-step outcome/error_code/duration
    - All rows through `LoginTrace.Export.export_login/1` only
    - Errors via `Mix.raise/1` when repo/connection missing

    Register task name `relyra.trace` (file `relyra.trace.ex`).
  </action>
  <verify>mix help relyra.trace</verify>
  <acceptance_criteria>
    - `mix help relyra.trace` lists `--repo` and `--connection` as required.
    - Module compiles without warnings.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Security corpus and ci.security lane</name>
  <read_first>
    - test/security/metadata_attribute_injection_test.exs
    - test/security/ci_gate_integrity_test.exs
    - mix.exs (ci.security alias)
    - test/relyra/telemetry/handlers/login_trace_test.exs
  </read_first>
  <action>
    Create `test/security/login_trace_test.exs`:

  1. Setup: attach `LoginTrace`, seed connection, run consume through `ValidationPipeline` or `Relyra.consume_response/3` with fixture that would contain sensitive material if mishandled (use FakeIdP signed response from existing security tests).

  2. **LiveView path:** render `ConnectionTraceLive` with conn, capture HTML, refute `-----BEGIN`, refute `<?xml`, refute `SignatureValue` literal from fixture, refute known cert base64 substring if present in fixture.

  3. **CLI path:** capture `Mix.Tasks.Relyra.Trace` output (or invoke run with IO device redirected), same refute list.

  4. **Equivalence:** for same connection, exported step maps from LiveView assign pipeline equal maps from CLI print path (compare `LoginTrace.Export.export_login/1` on same audit rows).

    Append to `mix.exs` `ci.security`:

    `"cmd mix test test/security/login_trace_test.exs --warnings-as-errors"`

    Add to `@gated_suites` in `ci_gate_integrity_test.exs`:

    `{"test/security/login_trace_test.exs", nil}`
  </action>
  <verify>mix test test/security/login_trace_test.exs test/security/ci_gate_integrity_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - `login_trace_test.exs` has at least 3 tests (LiveView refute, CLI refute, export equivalence).
    - `ci_gate_integrity_test.exs` passes.
    - `mix.exs` `ci.security` includes cmd line for login_trace_test.exs.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/security/login_trace_test.exs --warnings-as-errors` exits 0
- `mix test test/security/ci_gate_integrity_test.exs --warnings-as-errors` exits 0
</verification>

<success_criteria>
TRACE-02 and TRACE-03 satisfied: security corpus green, CLI exists, redaction equivalence proven, hollow-gate preserved.
</success_criteria>
