---
phase: 42-stepwise-login-trace-liveview
plan: 03
type: execute
wave: 3
depends_on:
  - 42-02
files_modified:
  - lib/relyra/live_admin/router.ex
  - lib/relyra/live_admin/connection_trace_live.ex
  - lib/relyra/live_admin/components/connection_detail.ex
  - test/relyra/live_admin/phase15_ui_contract_test.exs
autonomous: true
requirements:
  - TRACE-01
must_haves:
  truths:
    - "Operator can open `/relyra/admin/connections/:connection_id/trace` and see last 20 login attempts."
    - "Each login row expands to six steps with outcome, error_code, and duration."
    - "Empty state shows CONTEXT D-16 copy when no login rows exist."
  artifacts:
    - path: "lib/relyra/live_admin/connection_trace_live.ex"
      provides: "Trace LiveView page."
    - path: "lib/relyra/live_admin/router.ex"
      provides: "Trace route registration."
---

<objective>
Deliver the LiveAdmin login-trace LiveView (TRACE-01): route, page, navigation link, and UI contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-stepwise-login-trace-liveview/42-CONTEXT.md
@lib/relyra/live_admin/connection_metadata_live.ex
@lib/relyra/live_admin/components/connection_detail.ex
@lib/relyra/live_admin/router.ex
@test/relyra/live_admin/phase15_ui_contract_test.exs
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| TM-03 | LiveView renders pre-redaction audit fields | high | Render only `LoginTrace.Export.export_login/1` output, never raw `after_summary` |
| TM-04 | XSS via trace cause/summary strings | medium | Use HEEx text interpolation only (no `raw/1`); escape handled by Phoenix |
</threat_model>

<tasks>

<task type="auto">
  <name>Register trace route and LiveView</name>
  <read_first>
    - lib/relyra/live_admin/router.ex
    - lib/relyra/live_admin/connection_metadata_live.ex
    - lib/relyra/live_admin/query.ex
  </read_first>
  <action>
    1. In `router.ex`, add inside `:relyra_admin` live_session:

    `live("/connections/:connection_id/trace", Relyra.LiveAdmin.ConnectionTraceLive, :trace)`

    2. Create `lib/relyra/live_admin/connection_trace_live.ex` mirroring metadata LiveView patterns:

    - `mount/handle_params` load connection via `Query` scope helpers
    - `assign(:traces, Query.get_login_traces(repo, scope, connection_id, limit: 20))`
    - Render list of login cards; each uses `<details>` or toggle `phx-click` for expand (discretion)
    - Six step rows in fixed order (D-08): decode, validate, signature.verify, replay.check, user.map, session.establish
    - Display `outcome`, `error_code`, `duration_ms` per step from exported map
    - Login header: `inserted_at`, `action` (`:succeeded`/`:failed`), hashed `correlation_id`, optional `cause` one-liner
    - Empty state exact copy: `No login attempts recorded yet — traces appear after the first SAML response is consumed.`
    - `data-testid="login-trace-page"`, `data-testid="login-trace-row-{id}"`, `data-testid="login-trace-step-{name}"`
    - Inline styles consistent with `connection_detail.ex` / metadata LiveView

    3. In `connection_detail.ex`, add link beside metadata card:

    `View Login Trace` → `#{@base_path}/connections/#{@detail.connection.connection_id}/trace`
  </action>
  <verify>mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - `router.ex` contains `/connections/:connection_id/trace`.
    - `connection_trace_live.ex` exists and uses `LoginTrace.Export` for all trace assigns.
    - `connection_detail.ex` contains `View Login Trace` link text.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Extend Phase 15 UI contract tests</name>
  <read_first>
    - test/relyra/live_admin/phase15_ui_contract_test.exs
    - test/support/live_admin_case.ex (if exists)
  </read_first>
  <action>
    Extend `phase15_ui_contract_test.exs`:

    - Navigate to trace route for a connection with seeded `:login` audit row (use existing test repo helpers + `AuditWriter.append_event` in setup)
    - Assert `data-testid="login-trace-page"` present
    - Assert six `data-testid` step rows present for expanded login
    - Assert empty-state copy when no login rows

    Wire into `mix.exs` `ci.admin_ui` alias if trace tests are not already covered (only if alias exists and admin UI gate required).
  </action>
  <verify>mix test test/relyra/live_admin/phase15_ui_contract_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - `phase15_ui_contract_test.exs` includes at least one test hitting the trace route.
    - Test file exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/relyra/live_admin/phase15_ui_contract_test.exs --warnings-as-errors` exits 0
- `mix test test/relyra/live_admin/ --warnings-as-errors` exits 0
</verification>

<success_criteria>
TRACE-01 LiveView is mounted, navigable from connection detail, and covered by admin UI contract tests.
</success_criteria>
