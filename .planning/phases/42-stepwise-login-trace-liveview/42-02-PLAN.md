---
phase: 42-stepwise-login-trace-liveview
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - lib/relyra/login_trace/export.ex
  - lib/relyra/diagnostic/allow_list.ex
  - lib/relyra/live_admin/query.ex
  - test/relyra/login_trace/export_test.exs
autonomous: true
requirements:
  - TRACE-01
  - TRACE-03
must_haves:
  truths:
    - "LiveView and CLI will share `Relyra.LoginTrace.Export` for all rendered trace data."
    - "Connection detail audit timeline excludes `domain: :login` rows."
    - "`get_login_traces/4` returns last N login events ordered by `inserted_at` desc."
  artifacts:
    - path: "lib/relyra/login_trace/export.ex"
      provides: "Redaction-equivalent export for steps and login rows."
    - path: "lib/relyra/live_admin/query.ex"
      provides: "Filtered login trace query + trust-audit exclusion."
---

<objective>
Add shared trace export/redaction and LiveAdmin query functions so UI and CLI read the same safe shape from audit rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-stepwise-login-trace-liveview/42-CONTEXT.md
@.planning/phases/42-stepwise-login-trace-liveview/42-RESEARCH.md
@lib/relyra/diagnostic/allow_list.ex
@lib/relyra/live_admin/query.ex
@lib/relyra/ecto/audit_event.ex
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| TM-02 | Raw correlation_id leaked in UI/CLI | medium | `export_login/1` hashes via `AllowList.hash_correlation_id/1` |
| TM-01 | Step map contains PEM/XML at read time | high | Export walks maps and replaces values for sensitive keys + pattern match on `-----BEGIN`, `<saml` substrings |
</threat_model>

<tasks>

<task type="auto">
  <name>Create LoginTrace.Export module</name>
  <read_first>
    - lib/relyra/diagnostic/allow_list.ex
    - lib/relyra/ecto/audit_writer.ex (@sensitive_keys)
  </read_first>
  <action>
    Create `lib/relyra/login_trace/export.ex` with:

    - `@forbidden_patterns` ~r/-----BEGIN|<\s*saml/i (and similar from RESEARCH)
    - `export_step/1` — takes a step map, returns safe map (string keys, only `outcome`, `error_code`, `duration_ms`, redacted attribute summaries)
    - `export_login/1` — takes `%AuditEvent{}` or map with domain `:login`, returns `%{id:, action:, cause:, inserted_at:, correlation_id: hashed, steps: [export_step/1]}`; drop `actor`; hash `correlation_id` via `AllowList.hash_correlation_id/1`
    - Recursive scrub: any map key in AuditWriter sensitive set → `"[REDACTED]"`; any binary value matching forbidden patterns → `"[REDACTED]"`

    Optionally add thin delegates on `AllowList` (`export_login_trace/1` → `LoginTrace.Export.export_login/1`) for backward-compatible naming (CONTEXT D-11 discretion — pick one public API, not both with different behavior).
  </action>
  <verify>mix test test/relyra/login_trace/export_test.exs --warnings-as-errors</verify>
  <acceptance_criteria>
    - `export_test.exs` proves hashed correlation_id differs from raw input.
    - Fixture map with `response_xml` key exports as redacted.
    - `-----BEGIN CERTIFICATE-----` in a value becomes `"[REDACTED]"`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Query helpers and trust-audit filter</name>
  <read_first>
    - lib/relyra/live_admin/query.ex
    - lib/relyra/ecto/audit_event.ex
  </read_first>
  <action>
    In `query.ex`:

    1. `get_login_traces(repo, scope, connection_id, opts \\ [])` — `limit` default 20 (CONTEXT D-14), filter `domain == :login`, scope by connection, order `desc: inserted_at`, map each row through `LoginTrace.Export.export_login/1`.

    2. In `get_connection_detail/4` audit query, add `where(event.domain != :login)` (or `not in [:login]`) so trust timeline excludes login traces (CONTEXT D-07a).

    Add `@spec` and `@doc false` matching existing Query style.
  </action>
  <verify>mix test test/relyra/live_admin/ --warnings-as-errors</verify>
  <acceptance_criteria>
    - `get_connection_detail/4` query contains `domain != :login` or equivalent.
    - `get_login_traces/4` exists and defaults limit to 20.
    - Existing LiveAdmin tests pass.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/relyra/login_trace/export_test.exs --warnings-as-errors` exits 0
- `mix test test/relyra/live_admin/ --warnings-as-errors` exits 0
</verification>

<success_criteria>
Shared export module is the single redaction path; connection detail no longer mixes login traces into trust audit timeline.
</success_criteria>
