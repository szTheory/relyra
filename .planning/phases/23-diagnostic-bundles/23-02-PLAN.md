---
phase: 23-diagnostic-bundles
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - lib/mix/tasks/relyra.diagnostic.ex
  - lib/relyra/phoenix/controllers/diagnostic_controller.ex
  - lib/relyra/live_admin/router.ex
  - lib/relyra/live_admin/connections_live.ex
autonomous: true
requirements:
  - DIAG-01
must_haves:
  truths:
    - "Operator can generate bundle via mix task and save to disk."
    - "Operator can download bundle from LiveAdmin UI."
  artifacts:
    - path: "lib/mix/tasks/relyra.diagnostic.ex"
      provides: "CLI bundle trigger"
    - path: "lib/relyra/phoenix/controllers/diagnostic_controller.ex"
      provides: "HTTP download endpoint for the diagnostic zip"
    - path: "lib/relyra/live_admin/connections_live.ex"
      provides: "Download button in the connections list UI"
  key_links:
    - from: "lib/mix/tasks/relyra.diagnostic.ex"
      to: "lib/relyra/diagnostic.ex"
      via: "Calls create_bundle and writes to file"
      pattern: "Relyra\\.Diagnostic\\.create_bundle"
    - from: "lib/relyra/phoenix/controllers/diagnostic_controller.ex"
      to: "lib/relyra/diagnostic.ex"
      via: "Calls create_bundle and sends zip over HTTP"
      pattern: "send_download\\(.*:binary"
---

<objective>
Expose the diagnostic bundle generator to operators via both a CLI Mix task and a "Download Bundle" button in the LiveView Admin UI.

Purpose: Operators need accessible triggers (shell and web) to export the safe diagnostic bundle when debugging production SAML issues.
Output: CLI Task, Controller download endpoint, and Admin UI integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-diagnostic-bundles/23-CONTEXT.md
@.planning/phases/23-diagnostic-bundles/23-PATTERNS.md
@.planning/phases/23-diagnostic-bundles/RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI Download | Authenticated web request triggering a data export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Elevation of Privilege | `live_admin/router.ex` | mitigate | Ensure the new `get` route is placed correctly within the `relyra_admin_routes` scope so it inherits the host app's admin authentication pipeline. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create Mix task for CLI export</name>
  <files>lib/mix/tasks/relyra.diagnostic.ex</files>
  <action>
    Create `Mix.Tasks.Relyra.Diagnostic` behavior. Parse options (e.g. `--repo`). 
    Call `Relyra.Diagnostic.create_bundle(opts)`. On success, write the returned binary to `./relyra_diagnostic_bundle.zip` using `File.write!/2`.
    Output a helpful success message indicating the file path. Follow the analog pattern in `lib/mix/tasks/relyra.metadata.pin.ex` for app startup and repo handling.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Task successfully parses arguments, calls the core service, and writes the zip to disk.</done>
</task>

<task type="auto">
  <name>Task 2: Build HTTP download endpoint</name>
  <files>lib/relyra/phoenix/controllers/diagnostic_controller.ex, lib/relyra/live_admin/router.ex</files>
  <action>
    Create `Relyra.Phoenix.Controllers.DiagnosticController` with a `download/2` action. 
    It should call `Relyra.Diagnostic.create_bundle(repo: ...)`. 
    If `{:ok, zip_binary}`, use `Plug.Conn.send_download(conn, {:binary, zip_binary}, filename: "relyra_diagnostic_bundle.zip")`. 
    If error, render a 500 or appropriate error message.
    In `lib/relyra/live_admin/router.ex`, add `get("/diagnostic/bundle", Relyra.Phoenix.Controllers.DiagnosticController, :download, as: :diagnostic)` inside the `scope path, as: :relyra_admin` block (outside the `live_session` since it's a standard GET).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Controller is implemented and routed correctly to serve the zip binary.</done>
</task>

<task type="auto">
  <name>Task 3: Add UI button in LiveAdmin</name>
  <files>lib/relyra/live_admin/connections_live.ex</files>
  <action>
    Update the `ConnectionsLive` index template to include a "Download Diagnostic Bundle" button.
    This should be a standard HTML `<a>` link or `<.link href=...>` component pointing to the new route (e.g., `~p"/relyra/admin/diagnostic/bundle"` depending on how the host mounts it; you can use the router helper or raw path `\#{@base_path}/diagnostic/bundle`).
    Ensure it's styled consistently with other action buttons (e.g., the Bulk Actions UI) and does not use `phx-click` since it triggers a browser file download.
  </action>
  <verify>
    <automated>mix format lib/relyra/live_admin/connections_live.ex</automated>
  </verify>
  <done>Admin UI prominently features a link that triggers the bundle download.</done>
</task>

</tasks>

<success_criteria>
Operators can download the diagnostic bundle via `mix relyra.diagnostic` or clicking a button in the LiveAdmin UI.
</success_criteria>

<output>
After completion, create `.planning/phases/23-diagnostic-bundles/23-02-SUMMARY.md`
</output>
