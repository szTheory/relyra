---
phase: 14-mapping-audit-milestone-verification
plan: 14-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md
autonomous: false
requirements: [CFG-05]
tags:
  - verification
  - mapping
  - audit
  - cfg-05
must_haves:
  truths:
    - "Phase 11 has an authoritative `11-VERIFICATION.md` artifact that closes CFG-05 milestone verification."
    - "The exact 3-command serial proof packet (compile, focused mapping/audit suite, full suite) is recorded with ISO-8601 UTC timestamps and observed Mix output result strings."
    - "The 4-row D-07 CFG-05 behavior-to-test/evidence map is present verbatim with the exact row labels `Mapping persistence (live rows + revision ledger)`, `Cross-domain audit hardening (same-transaction capture)`, `Audited mapping mutation surface`, and `Runtime mapping_config hydration`."
    - "The two D-10 manual semantics checks are explicitly approved in prose before CFG-05 is considered closed."
    - "Evidence collection is serial only — no parallel Mix invocations were used."
  artifacts:
    - path: ".planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md"
      provides: "Authoritative CFG-05 verification packet with serial command results, 4-row CFG-05 evidence map, and human-approved manual sign-off"
      contains: "CFG-05"
    - path: ".planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md"
      provides: "Serial-only execution declaration that disqualifies parallel Mix evidence"
      contains: "Execution mode: serial only."
  key_links:
    - from: ".planning/phases/11-mapping-persistence-audit-hardening/11-VALIDATION.md"
      to: ".planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md"
      via: "exact serial packet (D-06) and the two manual checks (D-10) carried forward as the proof surface"
      pattern: "CFG-05|serial only|Manual Sign-Off"
    - from: ".planning/v0.2-MILESTONE-AUDIT.md"
      to: ".planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md"
      via: "closes the orphaned CFG-05 verification gap without mutating historical audit evidence per D-03"
      pattern: "11-VERIFICATION.md|CFG-05"
---

<objective>
Create the authoritative Phase 11 verification artifact `11-VERIFICATION.md` for CFG-05 from the locked 3-command serial proof packet (D-06) and a blocking 2-prompt manual semantics sign-off gate (D-10).

Purpose: implement D-01, D-04..D-11 by recording compact executable proof first, brief narrative second, mirroring `10-VERIFICATION.md`'s shape (Scope -> Requirement Traceability -> Phase-Audit Gap Closed -> Automated Evidence Packet -> Evidence Map -> Manual Sign-Off), and requiring human approval only for the two product-semantics judgments automation cannot replace per D-11.

Output: `.planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md` containing the exact serial commands with ISO-8601 UTC timestamps, observed result counts (`nn tests, 0 failures`), the 4-row CFG-05 behavior-to-test/evidence map (D-07), and approved human prose for the two D-10 semantics checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v0.2-MILESTONE-AUDIT.md
@.planning/phases/14-mapping-audit-milestone-verification/14-CONTEXT.md
@.planning/phases/14-mapping-audit-milestone-verification/14-RESEARCH.md
@.planning/phases/14-mapping-audit-milestone-verification/14-PATTERNS.md
@.planning/phases/14-mapping-audit-milestone-verification/14-VALIDATION.md
@.planning/phases/13-certificate-rollover-validation-verification/13-02-PLAN.md
@.planning/phases/10-certificate-inventory-rollover/10-VERIFICATION.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-VALIDATION.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-UAT.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-01-SUMMARY.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-02-SUMMARY.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-03-SUMMARY.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-04-SUMMARY.md
@lib/relyra/ecto/mapping_commands.ex
@lib/relyra/ecto/audit_writer.ex
@lib/relyra/ecto/audit_event.ex
@lib/relyra/ecto/connection_snapshot.ex
@lib/relyra/user_mapper/default_attribute.ex
@test/relyra/ecto/mapping_commands_test.exs
@test/relyra/ecto/audit_hardening_test.exs
@test/relyra/ecto/attribute_mapping_schema_test.exs
@test/relyra/ecto/group_mapping_schema_test.exs
@test/relyra/ecto/mapping_revision_schema_test.exs
@test/relyra/ecto/audit_event_schema_test.exs
@test/relyra/ecto/migration_constraints_test.exs
@test/relyra/connection_snapshot_test.exs
@test/relyra/user_mapper/default_attribute_test.exs
@test/relyra/ecto/ecto_connection_resolver_test.exs
@test/relyra/ecto/connection_record_test.exs
@test/relyra/ecto/metadata_apply_test.exs
@test/relyra/ecto/certificate_inventory_transition_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run packet command 1 (compile) and capture timestamp + result string</name>
  <files>(none — shell-captured values consumed by Task 4)</files>
  <read_first>
    - .planning/phases/14-mapping-audit-milestone-verification/14-CONTEXT.md
    - .planning/phases/14-mapping-audit-milestone-verification/14-RESEARCH.md
    - .planning/phases/13-certificate-rollover-validation-verification/13-02-PLAN.md
    - .planning/phases/10-certificate-inventory-rollover/10-VERIFICATION.md
  </read_first>
  <action>Capture an ISO-8601 UTC timestamp at command start using `date -u +'%Y-%m-%dT%H:%M:%SZ'`. Record this timestamp verbatim — it will be used as the order-1 row "Executed at (UTC)" cell in the Automated Evidence Packet table written by Task 4. Then run exactly: `mix compile --warnings-as-errors`. Per D-04 / D-05 do not parallelize. Record the literal result string `compile succeeded with no warnings` for the order-1 row "Result" cell, and record `passed` for the "Status" cell. If compile reports any warning or fails, stop the plan immediately and surface the exact Mix output rather than softening the artifact — the verification packet is invalid if step 1 does not pass clean. Persist these three values (timestamp, status, result) for use by Task 4 (e.g. into a scratch shell variable or a brief notes scratchpad — they will be transcribed verbatim into `11-VERIFICATION.md`).</action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exit code is 0.
    - The captured ISO-8601 UTC timestamp matches the regex `^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$` (verifiable with `printf '%s' "$ts" | rg -qE '^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$'`).
    - The captured result string is exactly `compile succeeded with no warnings` (verbatim from `10-VERIFICATION.md` line 34 precedent).
    - No parallel Mix invocations were used in this task.
  </acceptance_criteria>
  <done>Packet command 1 ran clean serially; timestamp + status + result string are captured for Task 4's Automated Evidence Packet table.</done>
</task>

<task type="auto">
  <name>Task 2: Run packet command 2 (focused mapping/audit serial command) and capture timestamp + Mix output count</name>
  <files>(none — shell-captured values consumed by Task 4)</files>
  <read_first>
    - .planning/phases/14-mapping-audit-milestone-verification/14-CONTEXT.md
    - .planning/phases/14-mapping-audit-milestone-verification/14-RESEARCH.md
    - .planning/phases/14-mapping-audit-milestone-verification/14-VALIDATION.md
    - .planning/phases/11-mapping-persistence-audit-hardening/11-VALIDATION.md
    - .planning/phases/13-certificate-rollover-validation-verification/13-02-PLAN.md
  </read_first>
  <action>Capture an ISO-8601 UTC timestamp at command start using `date -u +'%Y-%m-%dT%H:%M:%SZ'`. Record this timestamp verbatim — it will be used as the order-2 row "Executed at (UTC)" cell in the Automated Evidence Packet table written by Task 4. Then run exactly the focused mapping/audit serial command from D-06 step 2 (all 13 test files in this exact order, single Mix invocation, with `--warnings-as-errors`):

```
mix test test/relyra/ecto/mapping_commands_test.exs test/relyra/ecto/audit_hardening_test.exs test/relyra/ecto/attribute_mapping_schema_test.exs test/relyra/ecto/group_mapping_schema_test.exs test/relyra/ecto/mapping_revision_schema_test.exs test/relyra/ecto/audit_event_schema_test.exs test/relyra/ecto/migration_constraints_test.exs test/relyra/connection_snapshot_test.exs test/relyra/user_mapper/default_attribute_test.exs test/relyra/ecto/ecto_connection_resolver_test.exs test/relyra/ecto/connection_record_test.exs test/relyra/ecto/metadata_apply_test.exs test/relyra/ecto/certificate_inventory_transition_test.exs --warnings-as-errors
```

Per D-04 / D-05 do not parallelize this with anything; it runs strictly after Task 1 finishes (Landmine 1 — `schema_migrations` bootstrap race has already burned this codebase per `11-01-SUMMARY.md` "Deviations from Plan"). Capture the literal Mix output line of the form `nn tests, 0 failures` from the tail of the output (do NOT pre-fill `19`, `23`, `25`, or any other count — the actual count is wider than the audit-time `19 tests` figure because the packet now spans `connection_record_test.exs`, `metadata_apply_test.exs`, and `certificate_inventory_transition_test.exs`; see Landmine 5). Record `passed` for the order-2 "Status" cell. If the run reports any failure or any warning-as-error, stop the plan and surface the exact Mix output rather than softening the artifact.</action>
  <verify>
    <automated>mix test test/relyra/ecto/mapping_commands_test.exs test/relyra/ecto/audit_hardening_test.exs test/relyra/ecto/attribute_mapping_schema_test.exs test/relyra/ecto/group_mapping_schema_test.exs test/relyra/ecto/mapping_revision_schema_test.exs test/relyra/ecto/audit_event_schema_test.exs test/relyra/ecto/migration_constraints_test.exs test/relyra/connection_snapshot_test.exs test/relyra/user_mapper/default_attribute_test.exs test/relyra/ecto/ecto_connection_resolver_test.exs test/relyra/ecto/connection_record_test.exs test/relyra/ecto/metadata_apply_test.exs test/relyra/ecto/certificate_inventory_transition_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The focused 13-file serial command exit code is 0.
    - The captured ISO-8601 UTC timestamp matches `^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$`.
    - The captured Mix result line matches the regex `^[0-9]+ tests, 0 failures$` and is recorded verbatim (no pre-filled count).
    - No parallel Mix invocations were used; this command was issued strictly after Task 1 returned.
    - All 13 test files were exercised in the same single `mix test` invocation (not split across multiple Mix calls).
  </acceptance_criteria>
  <done>Packet command 2 ran clean serially against the locked 13-file mapping/audit set; timestamp + status + actual `nn tests, 0 failures` count are captured for Task 4's Automated Evidence Packet table.</done>
</task>

<task type="auto">
  <name>Task 3: Run packet command 3 (full suite) and capture timestamp + Mix output count</name>
  <files>(none — shell-captured values consumed by Task 4)</files>
  <read_first>
    - .planning/phases/14-mapping-audit-milestone-verification/14-CONTEXT.md
    - .planning/phases/14-mapping-audit-milestone-verification/14-RESEARCH.md
    - .planning/phases/13-certificate-rollover-validation-verification/13-02-PLAN.md
    - .planning/phases/10-certificate-inventory-rollover/10-VERIFICATION.md
  </read_first>
  <action>Capture an ISO-8601 UTC timestamp at command start using `date -u +'%Y-%m-%dT%H:%M:%SZ'`. Record this timestamp verbatim — it will be used as the order-3 row "Executed at (UTC)" cell in the Automated Evidence Packet table written by Task 4. Then run exactly: `mix test --warnings-as-errors`. Per D-04 / D-05 do not parallelize; run strictly after Task 2 returns. Capture the literal Mix output line of the form `nn tests, 0 failures` from the tail of the output (do NOT pre-fill `168 tests` from `10-VERIFICATION.md` line 36 — the suite total may have grown since Phase 13). Record `passed` for the order-3 "Status" cell. If the suite reports any failure or any warning-as-error, stop the plan and surface the exact Mix output rather than softening the artifact.</action>
  <verify>
    <automated>mix test --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test --warnings-as-errors` exit code is 0.
    - The captured ISO-8601 UTC timestamp matches `^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$`.
    - The captured full-suite result line matches `^[0-9]+ tests, 0 failures$` and is recorded verbatim.
    - This command ran strictly after Task 2 finished (no parallel Mix).
  </acceptance_criteria>
  <done>Packet command 3 ran clean serially; timestamp + status + actual full-suite `nn tests, 0 failures` count are captured for Task 4's Automated Evidence Packet table.</done>
</task>

<task type="auto">
  <name>Task 4: Author 11-VERIFICATION.md with the 6 locked sections in fixed order</name>
  <files>.planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md
    - .planning/phases/10-certificate-inventory-rollover/10-VERIFICATION.md
    - .planning/phases/14-mapping-audit-milestone-verification/14-RESEARCH.md
    - .planning/phases/14-mapping-audit-milestone-verification/14-PATTERNS.md
    - .planning/phases/11-mapping-persistence-audit-hardening/11-01-SUMMARY.md
    - .planning/phases/11-mapping-persistence-audit-hardening/11-02-SUMMARY.md
    - .planning/phases/11-mapping-persistence-audit-hardening/11-03-SUMMARY.md
    - .planning/phases/11-mapping-persistence-audit-hardening/11-04-SUMMARY.md
  </read_first>
  <action>Create `.planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md` mirroring `10-VERIFICATION.md` byte-for-byte structurally per D-08. The artifact MUST contain these 6 sections in this exact order (titles verbatim, no extra sections, no reordering):

1. `# Phase 11 Verification`

2. `## Scope` — one paragraph: `This artifact closes \`CFG-05\` milestone verification for Phase 11 by recording the compact serial evidence packet defined in Phase 14 and preserving the blocking manual approval gate for the two remaining mapping/audit semantics checks.`

3. `## Requirement Traceability` — opens with the literal line:

   `Requirement: \`CFG-05\` - User can persist attribute/group mapping configuration and review a durable audit history of trust changes.`

   Then the 4-row table from D-07 with the exact column header line `| CFG-05 behavior | Proof source | Evidence |` followed by `| --- | --- | --- |`. The 4 row labels (first column) MUST be verbatim:
   - `| Mapping persistence (live rows + revision ledger) | ... | ... |`
   - `| Cross-domain audit hardening (same-transaction capture) | ... | ... |`
   - `| Audited mapping mutation surface | ... | ... |`
   - `| Runtime mapping_config hydration | ... | ... |`

   Each row's "Proof source" cell names the focused 13-file packet command from Task 2 plus the relevant cross-reference summary file(s) per D-07 (row 1: `11-01-SUMMARY.md`, `11-02-SUMMARY.md`, `11-04-SUMMARY.md`; row 2: `11-03-SUMMARY.md`; row 3: `11-01-SUMMARY.md`, `11-04-SUMMARY.md`; row 4: `11-04-SUMMARY.md`). Each row's "Evidence" cell records the actual `nn tests, 0 failures` count from Task 2 (NOT pre-filled — see Landmine 5 in RESEARCH.md) plus a one-sentence narrative tying it back to the behavior.

4. `## Phase-Audit Gap Closed` — opens with: `Milestone audit gap being closed: \`.planning/v0.2-MILESTONE-AUDIT.md\` left \`CFG-05\` orphaned because Phase 11 had no \`11-VERIFICATION.md\`, and the focused mapping/audit suite passing \`19 tests, 0 failures\` at audit time was not enough to close milestone-level traceability.` Then a `Closure evidence:` bulleted list with exactly four bullets mirroring `10-VERIFICATION.md` lines 22-26: `This file now exists as the missing Phase 11 verification artifact.`; `The focused serial mapping/audit packet now passes against the current repo state.`; `The full serial suite now passes after the focused mapping/audit packet.`; `The remaining human approval gate is isolated to the two semantics judgments defined in Phase 14.` Per D-02 do NOT include any "validation still described missing Wave 0" clause — `11-VALIDATION.md` is already `wave_0_complete: true`.

5. `## Automated Evidence Packet` — opens with the verbatim serial-only declaration: `Execution mode: serial only. These commands were run one after the other and were not parallelized, matching the mapping/audit validation contract and the milestone-audit warning that parallel Mix evidence is invalid for this phase.` Then a 3-row execution table with the exact header line `| Order | Executed at (UTC) | Exact command | Status | Result |` followed by `| --- | --- | --- | --- | --- |`. The three rows correspond to Tasks 1/2/3 in order:
   - Order `1`: timestamp from Task 1, command `\`mix compile --warnings-as-errors\``, status `passed`, result `\`compile succeeded with no warnings\``.
   - Order `2`: timestamp from Task 2, command (backtick-wrapped, exact 13-file `mix test ... --warnings-as-errors` string from Task 2 action), status `passed`, result `` `nn tests, 0 failures` `` with the actual count captured in Task 2.
   - Order `3`: timestamp from Task 3, command `\`mix test --warnings-as-errors\``, status `passed`, result `` `nn tests, 0 failures` `` with the actual count captured in Task 3.
   Close with: `Serial execution note: order \`1\` completed successfully before order \`2\` started, and order \`2\` completed successfully before order \`3\` started. No parallel Mix commands were used for this verification packet.`

6. `## Evidence Map` — four `### <behavior>` subsections with titles matching the D-07 row labels exactly: `### Mapping persistence (live rows + revision ledger)`, `### Cross-domain audit hardening (same-transaction capture)`, `### Audited mapping mutation surface`, `### Runtime mapping_config hydration`. Each subsection: 1-3 bullets pointing at the proof tests + relevant `lib/relyra/ecto/*.ex` source(s) + the cross-reference summary file from D-07. The fourth subsection MUST also cite the `mix test --warnings-as-errors` full-suite confirmation from Task 3.

7. `## Manual Sign-Off` — opens with the verbatim line: `The automated packet is complete. The remaining approval gate is limited to the two semantics checks Phase 14 locked:` followed by a numbered list of these two D-10 prompts verbatim:

   1. `Confirm cross-domain audit rows read like a calm trust timeline — actor, cause, before/after view, redaction-safe payloads — so an operator can answer "who changed what, why" without leaking XML/PEM/key material.`

   2. `Confirm the runtime mapping_config contract stays persistence-agnostic — plain attribute_rules / group_rules, deterministic ordering, persisted-rules-first with fallback — so host-app Relyra.UserMapper consumers see stable values.`

   Then a `Human review instructions:` bulleted list pointing at the exact files/tests to review for each check (`audit_hardening_test.exs`, `connection_record_test.exs`, `metadata_apply_test.exs`, `certificate_inventory_transition_test.exs`, `lib/relyra/ecto/audit_writer.ex`, `lib/relyra/ecto/audit_event.ex` for check 1; `lib/relyra/ecto/connection_snapshot.ex`, `lib/relyra/user_mapper/default_attribute.ex`, `connection_snapshot_test.exs`, `ecto_connection_resolver_test.exs`, `user_mapper/default_attribute_test.exs` for check 2). Then a `Human review result:` placeholder block with `Approval pending` — Task 5 fills this with operator prose and appends the literal terminator line `Manual approval status: approved.` once both checks are approved.

Per D-03 do NOT mutate any historical audit artifact (no edits to `v0.2-MILESTONE-AUDIT.md`, no edits to any `11-*-SUMMARY.md`, no edits to `11-VALIDATION.md`, no edits to `11-UAT.md`). Per D-11 do NOT phrase any manual prompt as a functional re-test (`Confirm <X> works correctly` is FORBIDDEN — both prompts MUST stay in the `Confirm <X> reads like / stays <product invariant>` shape).</action>
  <verify>
    <automated>test -f .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '# Phase 11 Verification' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '## Scope' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '## Requirement Traceability' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- 'Requirement: `CFG-05` - User can persist attribute/group mapping configuration and review a durable audit history of trust changes.' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '| CFG-05 behavior | Proof source | Evidence |' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '| Mapping persistence (live rows + revision ledger) |' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '| Cross-domain audit hardening (same-transaction capture) |' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '| Audited mapping mutation surface |' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '| Runtime mapping_config hydration |' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '## Phase-Audit Gap Closed' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '## Automated Evidence Packet' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- 'Execution mode: serial only.' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- 'mix compile --warnings-as-errors' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- 'mix test test/relyra/ecto/mapping_commands_test.exs test/relyra/ecto/audit_hardening_test.exs test/relyra/ecto/attribute_mapping_schema_test.exs test/relyra/ecto/group_mapping_schema_test.exs test/relyra/ecto/mapping_revision_schema_test.exs test/relyra/ecto/audit_event_schema_test.exs test/relyra/ecto/migration_constraints_test.exs test/relyra/connection_snapshot_test.exs test/relyra/user_mapper/default_attribute_test.exs test/relyra/ecto/ecto_connection_resolver_test.exs test/relyra/ecto/connection_record_test.exs test/relyra/ecto/metadata_apply_test.exs test/relyra/ecto/certificate_inventory_transition_test.exs --warnings-as-errors' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- 'mix test --warnings-as-errors' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- 'compile succeeded with no warnings' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nE -- '`[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z`' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nE -- '`[0-9]+ tests, 0 failures`' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '## Evidence Map' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '### Mapping persistence (live rows + revision ledger)' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '### Cross-domain audit hardening (same-transaction capture)' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '### Audited mapping mutation surface' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '### Runtime mapping_config hydration' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '## Manual Sign-Off' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md` exists.
    - Title `# Phase 11 Verification` is present.
    - Section headers `## Scope`, `## Requirement Traceability`, `## Phase-Audit Gap Closed`, `## Automated Evidence Packet`, `## Evidence Map`, `## Manual Sign-Off` are all present and appear in this exact order (verifiable by sequential `rg -n` line numbers).
    - The literal line `Requirement: \`CFG-05\` - User can persist attribute/group mapping configuration and review a durable audit history of trust changes.` is present.
    - The traceability column header line `| CFG-05 behavior | Proof source | Evidence |` is present.
    - All 4 D-07 row labels appear verbatim: `| Mapping persistence (live rows + revision ledger) |`, `| Cross-domain audit hardening (same-transaction capture) |`, `| Audited mapping mutation surface |`, `| Runtime mapping_config hydration |`.
    - All 3 D-06 packet commands appear verbatim in the Automated Evidence Packet section: `mix compile --warnings-as-errors`; the full 13-file `mix test test/relyra/ecto/mapping_commands_test.exs test/relyra/ecto/audit_hardening_test.exs test/relyra/ecto/attribute_mapping_schema_test.exs test/relyra/ecto/group_mapping_schema_test.exs test/relyra/ecto/mapping_revision_schema_test.exs test/relyra/ecto/audit_event_schema_test.exs test/relyra/ecto/migration_constraints_test.exs test/relyra/connection_snapshot_test.exs test/relyra/user_mapper/default_attribute_test.exs test/relyra/ecto/ecto_connection_resolver_test.exs test/relyra/ecto/connection_record_test.exs test/relyra/ecto/metadata_apply_test.exs test/relyra/ecto/certificate_inventory_transition_test.exs --warnings-as-errors`; and `mix test --warnings-as-errors`.
    - The literal serial-only declaration `Execution mode: serial only.` is present.
    - At least one ISO-8601 UTC timestamp matching `` `[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z` `` is present (3 expected, one per execution row).
    - At least one Mix result line matching `` `[0-9]+ tests, 0 failures` `` is present.
    - The literal compile result `compile succeeded with no warnings` is present.
    - The four `### <behavior>` Evidence Map subsection titles are present verbatim.
    - The historical artifacts `.planning/v0.2-MILESTONE-AUDIT.md`, `.planning/phases/11-mapping-persistence-audit-hardening/11-VALIDATION.md`, `11-UAT.md`, and all `11-*-SUMMARY.md` files are unchanged (verifiable with `git diff --stat -- .planning/v0.2-MILESTONE-AUDIT.md .planning/phases/11-mapping-persistence-audit-hardening/11-VALIDATION.md .planning/phases/11-mapping-persistence-audit-hardening/11-UAT.md .planning/phases/11-mapping-persistence-audit-hardening/11-01-SUMMARY.md .planning/phases/11-mapping-persistence-audit-hardening/11-02-SUMMARY.md .planning/phases/11-mapping-persistence-audit-hardening/11-03-SUMMARY.md .planning/phases/11-mapping-persistence-audit-hardening/11-04-SUMMARY.md` returns no output).
  </acceptance_criteria>
  <done>`11-VERIFICATION.md` exists, names CFG-05 explicitly, contains the 6 locked sections in fixed order, embeds the 4 D-07 row labels verbatim, records the exact 3 D-06 commands verbatim with ISO-8601 UTC timestamps and observed result counts, and no historical audit artifact has been mutated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Obtain blocking sign-off for the two D-10 manual CFG-05 semantics checks</name>
  <files>.planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md
    - lib/relyra/ecto/audit_writer.ex
    - lib/relyra/ecto/audit_event.ex
    - lib/relyra/ecto/connection_snapshot.ex
    - lib/relyra/user_mapper/default_attribute.ex
    - test/relyra/ecto/audit_hardening_test.exs
    - test/relyra/ecto/connection_record_test.exs
    - test/relyra/ecto/metadata_apply_test.exs
    - test/relyra/ecto/certificate_inventory_transition_test.exs
    - test/relyra/connection_snapshot_test.exs
    - test/relyra/ecto/ecto_connection_resolver_test.exs
    - test/relyra/user_mapper/default_attribute_test.exs
    - .planning/phases/13-certificate-rollover-validation-verification/13-02-PLAN.md
  </read_first>
  <action>Present the two narrow D-10 manual checks as the only human approval gate for this phase. The exact prompt text is locked verbatim (matches the wording the Task 4 artifact embeds in `## Manual Sign-Off`):

  1. `Confirm cross-domain audit rows read like a calm trust timeline — actor, cause, before/after view, redaction-safe payloads — so an operator can answer "who changed what, why" without leaking XML/PEM/key material.`

  2. `Confirm the runtime mapping_config contract stays persistence-agnostic — plain attribute_rules / group_rules, deterministic ordering, persisted-rules-first with fallback — so host-app Relyra.UserMapper consumers see stable values.`

Ask the user to review the files in `<read_first>` plus the draft `11-VERIFICATION.md`. If the user approves, edit `11-VERIFICATION.md` `## Manual Sign-Off` section: replace the `Approval pending` placeholder with concise operator prose addressing each of the two checks (one short paragraph or bullet per check), record `Approved on \`<ISO-date>\`.` with the actual approval date (backtick-wrapped per `10-VERIFICATION.md` line 75), and append the literal terminator line `Manual approval status: approved.` as the final non-blank line of the file. Per D-11 do NOT use this gate to re-prove functional correctness the automated packet already proves; both prompts are semantics-only and stay in the `Confirm <X> reads like / stays <product invariant>` shape. If the user reports an issue with either check, record the blocking issue in the section and stop closure rather than softening the semantics or claiming completion — `14-02-PLAN.md` MUST NOT run if approval is incomplete.</action>
  <verify>
    <automated>rg -nF -- '## Manual Sign-Off' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- 'Confirm cross-domain audit rows read like a calm trust timeline — actor, cause, before/after view, redaction-safe payloads — so an operator can answer "who changed what, why" without leaking XML/PEM/key material.' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- 'Confirm the runtime mapping_config contract stays persistence-agnostic — plain attribute_rules / group_rules, deterministic ordering, persisted-rules-first with fallback — so host-app Relyra.UserMapper consumers see stable values.' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- 'Manual approval status: approved.' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md</automated>
  </verify>
  <how-to-verify>
    1. Open `lib/relyra/ecto/audit_writer.ex` and `lib/relyra/ecto/audit_event.ex` plus the four cross-domain audit tests (`audit_hardening_test.exs`, `connection_record_test.exs`, `metadata_apply_test.exs`, `certificate_inventory_transition_test.exs`). Confirm representative audit rows expose actor, cause, before/after view, and redaction-safe payloads — an operator can answer "who changed what, why" from a row sample without seeing raw XML, PEM, or key material.
    2. Open `lib/relyra/ecto/connection_snapshot.ex` and `lib/relyra/user_mapper/default_attribute.ex` plus `connection_snapshot_test.exs`, `ecto_connection_resolver_test.exs`, `user_mapper/default_attribute_test.exs`. Confirm `mapping_config` exposes plain `attribute_rules` / `group_rules`, deterministic ordering, persisted-rules-first with documented fallback — and Ecto rows do NOT leak into runtime consumers.
    3. Confirm the operator prose is written into `## Manual Sign-Off` of `.planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md`, an `Approved on \`<ISO-date>\`.` line is recorded, and the final terminator line is exactly `Manual approval status: approved.`.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md` contains the literal `## Manual Sign-Off` heading.
    - The exact D-10 prompt 1 sentence `Confirm cross-domain audit rows read like a calm trust timeline — actor, cause, before/after view, redaction-safe payloads — so an operator can answer "who changed what, why" without leaking XML/PEM/key material.` is present.
    - The exact D-10 prompt 2 sentence `Confirm the runtime mapping_config contract stays persistence-agnostic — plain attribute_rules / group_rules, deterministic ordering, persisted-rules-first with fallback — so host-app Relyra.UserMapper consumers see stable values.` is present.
    - The literal terminator line `Manual approval status: approved.` is present (verifiable with `rg -nF -- 'Manual approval status: approved.' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md`).
    - Neither prompt has been softened or rephrased into a functional re-test (D-11 violation): the strings `works correctly`, `passes correctly`, and `behaves correctly` MUST NOT appear inside the `## Manual Sign-Off` section (verifiable by inspecting the section content).
    - No historical artifact (`v0.2-MILESTONE-AUDIT.md`, `11-VALIDATION.md`, `11-UAT.md`, any `11-*-SUMMARY.md`) was modified.
  </acceptance_criteria>
  <resume-signal>Type `approved` to continue, or describe the blocking issue in one message.</resume-signal>
  <done>CFG-05 verification artifact includes explicit human-approved prose for the two locked D-10 semantics checks and ends with the literal `Manual approval status: approved.` terminator line, OR the phase remains blocked with the issue recorded and `14-02-PLAN.md` does not run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated Mix results -> milestone verification artifact | Serial test evidence must be captured into `11-VERIFICATION.md` without omission, distortion, or pre-filled counts. |
| human semantics review -> requirement closure | The two D-10 manual sign-off prompts decide whether the remaining product-semantics judgments are acceptable; functional correctness stays automated per D-11. |
| parallel Mix harness -> verification artifact | Parallel `mix test` invocations race the `schema_migrations` bootstrap and produce false-failure evidence (Landmine 1); the artifact MUST be authored from strictly serial proof only. |
| Phase 14 closure plan -> historical audit artifacts | `v0.2-MILESTONE-AUDIT.md`, `11-VALIDATION.md`, `11-UAT.md`, and `11-*-SUMMARY.md` are read-only point-in-time evidence (D-03); only the new `11-VERIFICATION.md` may be created. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-14-01-01 | R | `11-VERIFICATION.md` evidence chain | mitigate | Tasks 1/2/3 capture an ISO-8601 UTC timestamp at command start with `date -u +'%Y-%m-%dT%H:%M:%SZ'`, the literal Mix result string from each command's tail output, and `passed` status; Task 4 transcribes those values verbatim into the 3-row Automated Evidence Packet table. Acceptance criteria grep for the exact 3 D-06 commands, the timestamp regex, and the `nn tests, 0 failures` regex so any drift is caught at execute-plan time. |
| TM-14-01-02 | T | manual approval gate | mitigate | Task 5 is a `<task type="checkpoint:human-verify" gate="blocking">` whose acceptance criteria require both verbatim D-10 prompt sentences and the literal terminator `Manual approval status: approved.` to be present before closure. Per D-11, both prompts stay in the `Confirm <X> reads like / stays <product invariant>` shape and the words `works correctly` / `passes correctly` / `behaves correctly` MUST NOT appear inside the `## Manual Sign-Off` section, so the gate cannot be repurposed as a functional re-test. |
| TM-14-01-03 | D | Mix migration bootstrap (parallel race) | mitigate | The plan executes packet commands serially (Tasks 1 -> 2 -> 3, each strictly after the previous returns), and the artifact records the verbatim line `Execution mode: serial only.` plus the closing serial-execution note. Acceptance criteria grep for that literal serial-only declaration, locking the discipline into the artifact itself. |
| TM-14-01-04 | T | historical audit artifacts | mitigate | `files_modified` lists exactly one new file: `.planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md`. Task 4 + Task 5 acceptance criteria require `git diff --stat` against `v0.2-MILESTONE-AUDIT.md`, `11-VALIDATION.md`, `11-UAT.md`, and all four `11-*-SUMMARY.md` files to return no output. Per D-03, no historical audit artifact may be edited. |
</threat_model>

<verification>
Run the locked 3-command serial packet (Tasks 1 -> 2 -> 3) capturing ISO-8601 UTC timestamps and Mix result strings for each, author `11-VERIFICATION.md` with the 6 locked sections in fixed order (Task 4) embedding the 4 D-07 row labels and the 3 D-06 commands verbatim, then block until the user approves both D-10 semantics checks (Task 5) and the artifact ends with `Manual approval status: approved.`. No historical audit artifact may be modified at any point.
</verification>

<success_criteria>
Phase 11 has an authoritative `11-VERIFICATION.md` artifact for CFG-05 with serial proof recorded (timestamps + result counts), the 4 D-07 behavior-to-test rows present verbatim, and the two D-10 semantics gate prompts explicitly approved in prose ending with `Manual approval status: approved.` — and historical audit artifacts (`v0.2-MILESTONE-AUDIT.md`, `11-VALIDATION.md`, `11-UAT.md`, all `11-*-SUMMARY.md`) remain unmodified.
</success_criteria>

<output>
After completion, create `.planning/phases/14-mapping-audit-milestone-verification/14-01-SUMMARY.md`
</output>
