---
phase: 14-mapping-audit-milestone-verification
plan: 14-02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CFG-05]
tags:
  - milestone-truth
  - traceability
  - cfg-05
must_haves:
  truths:
    - "CFG-05 is no longer orphaned in live milestone truth once `11-VERIFICATION.md` exists with approved manual sign-off."
    - "REQUIREMENTS.md, ROADMAP.md, and STATE.md all agree that mapping/audit closure is verified, without mutating any historical audit artifact."
    - "Phase 14 closes only CFG-05 traceability and does not expand into broader milestone cleanup (PROJECT.md, v0.2-MILESTONE-AUDIT.md, other phase blocks, or unrelated state surfaces stay untouched)."
    - "Progress accounting in STATE.md frontmatter advances by exactly +1 phase and +2 plans (Phase 14 adds 14-01 and 14-02)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CFG-05 marked complete in milestone requirement truth and CFG-05 traceability row flipped to Complete"
      contains: "**CFG-05**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14 detail block extended with Status/Plans/Plan list mirroring Phase 13 closure shape"
      contains: "Status: complete (verified after Phase 14 execution)."
    - path: ".planning/STATE.md"
      provides: "Current milestone state advanced past pending Phase 14 plan-phase focus to post-execution closure truth"
      contains: "Phase: 14 (mapping-audit-milestone-verification) — COMPLETE"
  key_links:
    - from: ".planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "verification artifact closes CFG-05 orphan status; checkbox flips and traceability row marks Complete per D-12 / D-08"
      pattern: "CFG-05|Complete"
    - from: ".planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 14 success criteria become achieved after verification closure; detail block flips to closed shape"
      pattern: "Phase 14|verified|complete"
    - from: ".planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md"
      to: ".planning/STATE.md"
      via: "state moves from `CONTEXT GATHERED` to `COMPLETE` with progress accounting advanced and resume-file repointed at the new authoritative artifact"
      pattern: "Phase 14|COMPLETE|11-VERIFICATION.md"
---

<objective>
Refresh the minimum live milestone truth surfaces (`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`) so CFG-05 is visibly satisfied at milestone level after Phase 11 verification closure.

Purpose: implement D-01 / D-03 / D-08 / D-12 by making narrow in-place edits to exactly three live-truth files — and only those three — so CFG-05 can be marked satisfied from verification evidence rather than plan completion alone, while leaving every historical audit artifact (`v0.2-MILESTONE-AUDIT.md`, all `11-*` files except the new `11-VERIFICATION.md` produced by 14-01, `PROJECT.md`, every other phase detail block) untouched per D-03 and Landmine 2 of `14-RESEARCH.md`.

Output: live traceability files that mark CFG-05 complete, reflect Phase 14 closure with the byte-aligned `- Status: complete (verified after Phase 14 execution).` line, repoint STATE.md's resume file at the new authoritative `11-VERIFICATION.md`, and advance frontmatter progress accounting by exactly +1 phase and +2 plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v0.2-MILESTONE-AUDIT.md
@.planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md
@.planning/phases/14-mapping-audit-milestone-verification/14-CONTEXT.md
@.planning/phases/14-mapping-audit-milestone-verification/14-RESEARCH.md
@.planning/phases/14-mapping-audit-milestone-verification/14-PATTERNS.md
@.planning/phases/13-certificate-rollover-validation-verification/13-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip CFG-05 to satisfied in REQUIREMENTS.md (checkbox + traceability row + footer stamp)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/14-mapping-audit-milestone-verification/14-RESEARCH.md
    - .planning/phases/13-certificate-rollover-validation-verification/13-03-PLAN.md
    - .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md
  </read_first>
  <action>Make exactly three narrow in-place edits to `.planning/REQUIREMENTS.md`. Do not edit any other line. Do not touch the Coverage block (lines 56-59 — already correct), v1 Requirements, Out of Scope, or any CFG-01..CFG-04 row.

  Edit 1 — CFG-05 checkbox (currently line ~16):
  - BEFORE (verbatim): `- [ ] **CFG-05**: User can persist attribute/group mapping configuration and review a durable audit history of trust changes.`
  - AFTER  (verbatim): `- [x] **CFG-05**: User can persist attribute/group mapping configuration and review a durable audit history of trust changes.`

  Edit 2 — CFG-05 traceability row (currently line ~54):
  - BEFORE (verbatim): `| CFG-05 | Phase 14 | Pending |`
  - AFTER  (verbatim): `| CFG-05 | Phase 14 | Complete |`

  Edit 3 — Footer stamp (currently line ~63):
  - BEFORE (verbatim): `*Last updated: 2026-05-06 after Phase 13 execution*`
  - AFTER  (verbatim): `*Last updated: 2026-05-06 after Phase 14 execution*`

  This task may execute only after `14-01-PLAN.md` completes with `Manual approval status: approved.` written into `11-VERIFICATION.md`. Per D-03 / D-12 / Landmine 2: do NOT touch `.planning/v0.2-MILESTONE-AUDIT.md`, `.planning/PROJECT.md`, any other phase detail block, or any other section of REQUIREMENTS.md beyond the three exact lines above.</action>
  <verify>
    <automated>rg -nF -- 'Manual approval status: approved.' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '- [x] **CFG-05**: User can persist attribute/group mapping configuration and review a durable audit history of trust changes.' .planning/REQUIREMENTS.md && rg -nF -- '| CFG-05 | Phase 14 | Complete |' .planning/REQUIREMENTS.md && rg -nF -- '*Last updated: 2026-05-06 after Phase 14 execution*' .planning/REQUIREMENTS.md && ! rg -nF -- '- [ ] **CFG-05**: User can persist attribute/group mapping configuration and review a durable audit history of trust changes.' .planning/REQUIREMENTS.md && ! rg -nF -- '| CFG-05 | Phase 14 | Pending |' .planning/REQUIREMENTS.md && ! rg -nF -- '*Last updated: 2026-05-06 after Phase 13 execution*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `11-VERIFICATION.md` already contains `Manual approval status: approved.` (precondition; if absent this task MUST NOT run).
    - `REQUIREMENTS.md` contains the literal line `- [x] **CFG-05**: User can persist attribute/group mapping configuration and review a durable audit history of trust changes.`
    - `REQUIREMENTS.md` contains the literal line `| CFG-05 | Phase 14 | Complete |`
    - `REQUIREMENTS.md` contains the literal footer stamp `*Last updated: 2026-05-06 after Phase 14 execution*`
    - The OLD CFG-05 unchecked line `- [ ] **CFG-05**: User can persist attribute/group mapping configuration and review a durable audit history of trust changes.` is no longer present.
    - The OLD traceability row `| CFG-05 | Phase 14 | Pending |` is no longer present.
    - The OLD footer stamp `*Last updated: 2026-05-06 after Phase 13 execution*` is no longer present.
    - `git diff --stat -- .planning/REQUIREMENTS.md` reports exactly one file changed; `git diff --stat -- .planning/v0.2-MILESTONE-AUDIT.md .planning/PROJECT.md` returns no output.
  </acceptance_criteria>
  <done>REQUIREMENTS.md flips CFG-05 to satisfied at all three anchors; no other line is edited; no historical artifact is touched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ROADMAP.md Phase 14 detail block with Status/Plans/Plan list (byte-aligned closure shape)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/14-mapping-audit-milestone-verification/14-RESEARCH.md
    - .planning/phases/13-certificate-rollover-validation-verification/13-03-PLAN.md
    - .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md
  </read_first>
  <action>Extend the Phase 14 detail block (currently lines 102-108 of `.planning/ROADMAP.md`) so it ends up matching this exact shape (Phase 13 detail block lines 88-100 is the byte-aligned analog). Do NOT edit any other phase detail block (07-13). Do NOT edit the v0.2 Phases summary table at line 21. Do NOT edit the v0.1 archive line. Do NOT edit any other section.

  Target Phase 14 detail block content (verbatim, byte-aligned with the existing prose):

  ```
  **Phase 14: Mapping/audit milestone verification**
  - Goal: close the remaining verification gap for Phase 11 without reopening already-green implementation work.
  - Status: complete (verified after Phase 14 execution).
  - Plans: 2 plans.
  - Plan list:
  - [x] `14-01-PLAN.md` — create `11-VERIFICATION.md` from the locked serial packet and blocking manual sign-off gate.
  - [x] `14-02-PLAN.md` — update live milestone truth in `REQUIREMENTS.md`, `ROADMAP.md`, and `STATE.md` after CFG-05 verification closure.
  - Gap closure: resolves the audit orphan state for `CFG-05` by producing the missing phase verification artifact.
  - Success criteria:
    1. Phase 11 verification evidence exists for mapping persistence and audit hardening behavior.
    2. Milestone traceability can mark `CFG-05` complete from verification evidence rather than plan completion alone.
    3. v0.2 re-audit sees no remaining mapping/audit verification gap.
  ```

  Concretely: insert the `- Status: complete (verified after Phase 14 execution).` line immediately after the existing `- Goal: ...` line (this matches `13-03-PLAN.md` Task 1's grep verifier `rg -nF -- '- Status: complete (verified after Phase 13 execution).' .planning/ROADMAP.md` byte-aligned to Phase 14). Insert the `- Plans: 2 plans.` line and the 2-entry `- Plan list:` block (with `- [x]` checkboxes — both 14-01 and 14-02 are complete by the time this task runs) immediately after the new Status line. Keep the existing `- Gap closure: ...` line and the existing `- Success criteria: 1./2./3.` block exactly as they currently appear (do not rewrite them).

  This task may execute only after `14-01-PLAN.md` completes with `Manual approval status: approved.` written into `11-VERIFICATION.md`. Per D-03 / Landmine 2: do NOT touch `.planning/v0.2-MILESTONE-AUDIT.md`, the v0.2 Phases summary table, the v0.1 archive line, any Phase 07-13 detail block, or `PROJECT.md`.</action>
  <verify>
    <automated>rg -nF -- 'Manual approval status: approved.' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nF -- '**Phase 14: Mapping/audit milestone verification**' .planning/ROADMAP.md && rg -nF -- '- Status: complete (verified after Phase 14 execution).' .planning/ROADMAP.md && rg -nF -- '- Plans: 2 plans.' .planning/ROADMAP.md && rg -nF -- '- Plan list:' .planning/ROADMAP.md && rg -nF -- '- [x] `14-01-PLAN.md` — create `11-VERIFICATION.md` from the locked serial packet and blocking manual sign-off gate.' .planning/ROADMAP.md && rg -nF -- '- [x] `14-02-PLAN.md` — update live milestone truth in `REQUIREMENTS.md`, `ROADMAP.md`, and `STATE.md` after CFG-05 verification closure.' .planning/ROADMAP.md && rg -nF -- '- Gap closure: resolves the audit orphan state for `CFG-05` by producing the missing phase verification artifact.' .planning/ROADMAP.md && rg -nF -- '**Phase 13: Certificate rollover validation + verification**' .planning/ROADMAP.md && rg -nF -- '- Status: complete (verified after Phase 13 execution).' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `11-VERIFICATION.md` already contains `Manual approval status: approved.` (precondition).
    - `ROADMAP.md` contains the literal Phase 14 heading `**Phase 14: Mapping/audit milestone verification**`.
    - `ROADMAP.md` contains the literal status line `- Status: complete (verified after Phase 14 execution).` (byte-aligned with the Phase 13 verifier).
    - `ROADMAP.md` contains `- Plans: 2 plans.`
    - `ROADMAP.md` contains `- Plan list:`
    - `ROADMAP.md` contains `- [x] \`14-01-PLAN.md\` — create \`11-VERIFICATION.md\` from the locked serial packet and blocking manual sign-off gate.`
    - `ROADMAP.md` contains `- [x] \`14-02-PLAN.md\` — update live milestone truth in \`REQUIREMENTS.md\`, \`ROADMAP.md\`, and \`STATE.md\` after CFG-05 verification closure.`
    - The existing Gap closure line `- Gap closure: resolves the audit orphan state for \`CFG-05\` by producing the missing phase verification artifact.` is still present (untouched).
    - The Phase 13 detail block is still intact: `**Phase 13: Certificate rollover validation + verification**` and `- Status: complete (verified after Phase 13 execution).` both still appear (Phase 13 was NOT mutated — only Phase 14 was edited).
    - `git diff --stat -- .planning/v0.2-MILESTONE-AUDIT.md .planning/PROJECT.md` returns no output.
    - The v0.2 Phases summary table row `| 14 | Mapping/audit milestone verification | Produce the missing mapping/audit milestone verification artifacts and close traceability. | CFG-05 |` (existing, line ~21) is still present and unmodified.
  </acceptance_criteria>
  <done>ROADMAP.md Phase 14 detail block now matches the Phase 13 closure shape with `- Status: complete (verified after Phase 14 execution).` byte-aligned, the 2-entry `- Plan list:` populated with `[x]` checkboxes, and no other detail block, summary table row, or historical artifact has been mutated.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md frontmatter, current focus, and Current Position block to post-Phase-14 closure truth</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md
    - .planning/phases/14-mapping-audit-milestone-verification/14-RESEARCH.md
    - .planning/phases/13-certificate-rollover-validation-verification/13-03-PLAN.md
    - .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md
  </read_first>
  <action>Update `.planning/STATE.md` with these narrow edits and only these. Do NOT touch the Performance Metrics table, the Project Reference link, the Progress bar art (it stays at 100%), or the Core value line.

  Frontmatter edits:
  - `last_updated:` — replace `"2026-05-06T15:00:00Z"` with a fresh ISO-8601 UTC timestamp captured at execution time using `date -u +'%Y-%m-%dT%H:%M:%SZ'`. Keep the surrounding `last_updated: "..."` quoting style intact.
  - `last_activity:` — replace `2026-05-06 -- Phase 14 context gathered (assumptions mode); ready for plan-phase` with the literal string `2026-05-06 -- Phase 14 execution completed; CFG-05 verified and closed`.
  - `progress.total_plans:` — change `total_plans: 23` to `total_plans: 25` (Phase 14 adds 14-01 and 14-02 — exact +2 increment).
  - `progress.completed_plans:` — change `completed_plans: 23` to `completed_plans: 25`.
  - `progress.completed_phases:` — change `completed_phases: 6` to `completed_phases: 7` (Phase 14 closes — exact +1 increment).
  - `total_phases:` — leave at `7` (do NOT change — Phase 14 is the existing 7th phase, not a new entry).
  - `milestone:` and `milestone_name:` and top-level `status:` — leave unchanged (`milestone: v0.2`, `milestone_name: Phases`, `status: complete` — already at milestone-complete state from Phase 13 closure).

  Body edits:
  - Current focus line (currently line 23): replace `**Current focus:** Phase 14 — mapping-audit-milestone-verification (context gathered, ready for plan-phase)` with the literal string `**Current focus:** Phase 14 — mapping-audit-milestone-verification (complete)` (this is the exact byte-aligned closure marker from `14-RESEARCH.md` "Status / focus strings the planner should make grep-verifiable").
  - Current Position phase line (currently line 27): replace `Phase: 14 (mapping-audit-milestone-verification) — CONTEXT GATHERED` with the literal string `Phase: 14 (mapping-audit-milestone-verification) — COMPLETE` (exact byte-aligned closure marker from `14-RESEARCH.md`).
  - Plan count (currently line 28): replace `Plan: 0 of 0 (planning pending)` with the literal string `Plan: 2 of 2`.
  - Status line (currently line 29): replace `Status: 14-CONTEXT.md written (assumptions mode); ready for /gsd-plan-phase 14` with the literal string `Status: Execution complete; CFG-05 closed`.
  - Last activity line (currently line 30):
    - BEFORE (verbatim, currently in `.planning/STATE.md` line 30): `Last activity: 2026-05-06 -- Phase 14 context locked: two plans (11-VERIFICATION.md + live-truth refresh), three-command serial packet, four-row CFG-05 traceability map, two manual sign-off checks`
    - AFTER  (verbatim): `Last activity: 2026-05-06 -- Phase 14 execution completed; CFG-05 verified and closed` (matches the new frontmatter `last_activity:` value).
    - The replacement MUST be a full-line replacement — no fragment of the BEFORE string (in particular, the substring `Phase 14 context locked`) may remain in `.planning/STATE.md` after this edit. The acceptance-criteria negative grep `! rg -nF -- 'Phase 14 context locked' .planning/STATE.md` enforces this.
  - Resume file line (currently line 31): replace `.planning/phases/14-mapping-audit-milestone-verification/14-CONTEXT.md` with `.planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md` (the new authoritative artifact — Landmine 6 fix).

  This task may execute only after `14-01-PLAN.md` completes with `Manual approval status: approved.` written into `11-VERIFICATION.md`. Per D-03 / Landmine 2 / `14-RESEARCH.md` "What Phase 14 Live-Truth Refresh MUST NOT Touch": do NOT touch `.planning/v0.2-MILESTONE-AUDIT.md`, `.planning/PROJECT.md`, or any phase summary/plan/validation/verification/context file outside Phase 14 (other than producing `11-VERIFICATION.md` which is 14-01's responsibility, not this task's).</action>
  <verify>
    <automated>rg -nF -- 'Manual approval status: approved.' .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md && rg -nE -- '^last_updated: "[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z"$' .planning/STATE.md && rg -nF -- 'last_activity: 2026-05-06 -- Phase 14 execution completed; CFG-05 verified and closed' .planning/STATE.md && rg -nF -- 'total_plans: 25' .planning/STATE.md && rg -nF -- 'completed_plans: 25' .planning/STATE.md && rg -nF -- 'completed_phases: 7' .planning/STATE.md && rg -nF -- 'total_phases: 7' .planning/STATE.md && rg -nF -- '**Current focus:** Phase 14 — mapping-audit-milestone-verification (complete)' .planning/STATE.md && rg -nF -- 'Phase: 14 (mapping-audit-milestone-verification) — COMPLETE' .planning/STATE.md && rg -nF -- 'Plan: 2 of 2' .planning/STATE.md && rg -nF -- 'Status: Execution complete; CFG-05 closed' .planning/STATE.md && rg -nF -- 'Last activity: 2026-05-06 -- Phase 14 execution completed; CFG-05 verified and closed' .planning/STATE.md && rg -nF -- 'Resume file: .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md' .planning/STATE.md && ! rg -nF -- 'Phase: 14 (mapping-audit-milestone-verification) — CONTEXT GATHERED' .planning/STATE.md && ! rg -nF -- 'Plan: 0 of 0 (planning pending)' .planning/STATE.md && ! rg -nF -- '14-CONTEXT.md' .planning/STATE.md && ! rg -nF -- 'Phase 14 context locked' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `11-VERIFICATION.md` already contains `Manual approval status: approved.` (precondition).
    - STATE.md frontmatter `last_updated:` is a freshly-captured ISO-8601 UTC string of the form `"YYYY-MM-DDTHH:MM:SSZ"` (verifiable with the regex `^last_updated: "[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z"$`).
    - STATE.md frontmatter contains the literal `last_activity: 2026-05-06 -- Phase 14 execution completed; CFG-05 verified and closed`.
    - STATE.md frontmatter contains `total_plans: 25` and `completed_plans: 25` (exact +2 increment from 23).
    - STATE.md frontmatter contains `completed_phases: 7` and `total_phases: 7` (completed bumped by +1; total unchanged).
    - STATE.md body contains the literal closure marker `**Current focus:** Phase 14 — mapping-audit-milestone-verification (complete)`.
    - STATE.md body contains the literal closure marker `Phase: 14 (mapping-audit-milestone-verification) — COMPLETE` (exact byte-aligned with `14-RESEARCH.md`).
    - STATE.md body contains `Plan: 2 of 2`, `Status: Execution complete; CFG-05 closed`, `Last activity: 2026-05-06 -- Phase 14 execution completed; CFG-05 verified and closed`, and `Resume file: .planning/phases/11-mapping-persistence-audit-hardening/11-VERIFICATION.md`.
    - The OLD strings are gone: `Phase: 14 (mapping-audit-milestone-verification) — CONTEXT GATHERED`, `Plan: 0 of 0 (planning pending)`, and any reference to `14-CONTEXT.md` are NOT present in STATE.md.
    - The OLD line-30 fragment `Phase 14 context locked` is NOT present in STATE.md (verifiable with the negative grep `! rg -nF -- 'Phase 14 context locked' .planning/STATE.md`); this catches partial-replacement of the Last activity line.
    - `git diff --stat -- .planning/v0.2-MILESTONE-AUDIT.md .planning/PROJECT.md` returns no output.
    - `git diff --stat -- .planning/phases/11-mapping-persistence-audit-hardening/11-VALIDATION.md .planning/phases/11-mapping-persistence-audit-hardening/11-UAT.md .planning/phases/11-mapping-persistence-audit-hardening/11-01-SUMMARY.md .planning/phases/11-mapping-persistence-audit-hardening/11-02-SUMMARY.md .planning/phases/11-mapping-persistence-audit-hardening/11-03-SUMMARY.md .planning/phases/11-mapping-persistence-audit-hardening/11-04-SUMMARY.md` returns no output (no historical Phase 11 artifact mutated).
  </acceptance_criteria>
  <done>STATE.md frontmatter advances by exactly +1 phase and +2 plans, body shows the byte-aligned `Phase: 14 (mapping-audit-milestone-verification) — COMPLETE` and `**Current focus:** Phase 14 — mapping-audit-milestone-verification (complete)` markers, the resume file pointer is repointed at the new authoritative `11-VERIFICATION.md`, and no historical artifact has been mutated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification artifact (`11-VERIFICATION.md`) -> live milestone truth | Current planning truth must derive from approved CFG-05 evidence; live-truth flips only after `Manual approval status: approved.` is written. |
| live milestone truth -> future audits / re-audits | Updated `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md` are consumed as the canonical current state by `/gsd-audit-milestone` and downstream readers. |
| Phase 14 closure plan -> historical audit / unrelated docs | `v0.2-MILESTONE-AUDIT.md`, `PROJECT.md`, every other phase block, every `11-*` file (other than the new `11-VERIFICATION.md` produced by 14-01) are read-only here per D-03. |
| frontmatter progress accounting -> milestone math | `total_plans` / `completed_plans` / `completed_phases` are consumed as the canonical milestone progress signal; off-by-one drift propagates through the next audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-14-02-01 | T | live-truth files | mitigate | Tasks 1/2/3 each precondition-check `Manual approval status: approved.` exists in `11-VERIFICATION.md` (via `rg -nF`) before mutating any live-truth file, so live truth never advances ahead of approved evidence. Acceptance criteria grep for the literal post-edit strings AND assert the OLD strings are gone, catching partial edits or accidental fall-through. |
| TM-14-02-02 | R | milestone closure state | mitigate | Each task's automated verifier uses `rg -nF` for every literal post-edit anchor (CFG-05 checkbox, traceability row, footer stamp, Phase 14 status line, plan list entries, frontmatter counters, body closure markers), making any drift trivially detectable. Verifiers also assert progress accounting math (`total_plans: 25`, `completed_plans: 25`, `completed_phases: 7`) so off-by-one errors are caught at task verify time. |
| TM-14-02-03 | T | historical audit / unrelated docs | mitigate | `files_modified` lists exactly three files. Every task's acceptance criteria require `git diff --stat -- .planning/v0.2-MILESTONE-AUDIT.md .planning/PROJECT.md` to return no output. Task 3 additionally requires `git diff --stat` against `11-VALIDATION.md`, `11-UAT.md`, and all four `11-*-SUMMARY.md` files to return no output. Per D-03 and Landmine 2 of `14-RESEARCH.md`, mutating any historical artifact is a phase-scope violation. |
</threat_model>

<verification>
Before each task's edits land, confirm `11-VERIFICATION.md` contains `Manual approval status: approved.` (gates the live-truth flip). After each task, grep-verify with `rg -nF` that every literal post-edit anchor is present AND that every OLD pre-edit string is absent. Confirm no historical artifact (`v0.2-MILESTONE-AUDIT.md`, `PROJECT.md`, any `11-*` file other than `11-VERIFICATION.md`) was modified by running `git diff --stat` against them at the end of each task.
</verification>

<success_criteria>
Live milestone truth surfaces (`REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`) all agree CFG-05 is verified and complete; ROADMAP.md Phase 14 detail block contains the byte-aligned `- Status: complete (verified after Phase 14 execution).` line and the 2-entry `- Plan list:` block; STATE.md frontmatter advances `completed_phases: 6 -> 7`, `total_plans: 23 -> 25`, `completed_plans: 23 -> 25`; STATE.md body shows `Phase: 14 (mapping-audit-milestone-verification) — COMPLETE` and `**Current focus:** Phase 14 — mapping-audit-milestone-verification (complete)`; the STATE.md resume file pointer is repointed at the new authoritative `11-VERIFICATION.md`; and no historical audit artifact (`v0.2-MILESTONE-AUDIT.md`, `PROJECT.md`, every `11-*` file other than `11-VERIFICATION.md`, every other phase detail block) has been mutated.
</success_criteria>

<output>
After completion, create `.planning/phases/14-mapping-audit-milestone-verification/14-02-SUMMARY.md`
</output>
