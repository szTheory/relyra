---
phase: 38-single-logout-slo-core-security
plan: 04
type: execute
wave: 4
depends_on: ["38-03"]
files_modified:
  - lib/relyra.ex
  - test/protocol/logout_pipeline_test.exs
autonomous: true
requirements: [SLO-01]
must_haves:
  truths:
    - "Host apps can start and consume logout flows via Relyra facade"
    - "SLO flows are verified via end-to-end security pipeline tests"
  artifacts:
    - path: "lib/relyra.ex"
      provides: "start_logout/3 and consume_logout/3"
    - path: "test/protocol/logout_pipeline_test.exs"
      provides: "End-to-end testing"
  key_links:
    - from: "lib/relyra.ex"
      to: "lib/relyra/session_adapter.ex"
      via: "terminate_by_session_index on successful IdP-initiated logout"
---

<objective>
Wire the Single Logout capabilities into the primary `Relyra` API facade and verify the entire end-to-end flow.

Purpose: 
Consumers interact with `Relyra` exclusively through the facade. By wiring `start_logout` and `consume_logout`, we expose the SLO functionality to adopters. Finally, an end-to-end protocol test must confirm that all security invariants hold simultaneously.
Output: `Relyra.start_logout/3`, `Relyra.consume_logout/3`, and end-to-end tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-single-logout-slo-core-security/PATTERNS.md
@.planning/phases/38-single-logout-slo-core-security/38-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose SLO flows via Relyra facade</name>
  <files>lib/relyra.ex, test/relyra_test.exs</files>
  <behavior>
    - `start_logout/3` generates a valid signed/unsigned HTTP-Redirect URL or HTTP-POST form payload containing a `LogoutRequest`.
    - `consume_logout/3` routes incoming HTTP payloads (request or response) through `LogoutValidator`, and if it's an IdP-initiated LogoutRequest, invokes `SessionAdapter.terminate_by_session_index/4`.
  </behavior>
  <action>
    Implement `Relyra.start_logout(connection, session_index, opts \\ [])`.
    Implement `Relyra.consume_logout(connection, raw_payload, opts \\ [])`.
    Wrap both in appropriate `Relyra.Telemetry.span([:logout, :start])` and `[:logout, :consume]` tracking. Route `consume_logout`'s valid IdP-initiated requests automatically to the session adapter's terminate callback to guarantee session destruction.
  </action>
  <verify>
    <automated>mix test test/relyra_test.exs</automated>
  </verify>
  <done>Facade APIs exist, are telemetried, and route correctly to the validator and adapter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end security test for SLO Pipeline</name>
  <files>test/protocol/logout_pipeline_test.exs</files>
  <behavior>
    - Valid IdP-initiated LogoutRequest terminates session.
    - Tampered HTTP-Redirect signature mathematically rejects message.
    - Replayed `LogoutRequest` or `LogoutResponse` ID is rejected by `ReplayStore`.
  </behavior>
  <action>
    Create `test/protocol/logout_pipeline_test.exs`.
    Write comprehensive end-to-end tests exercising both HTTP-Redirect and HTTP-POST bindings using `Relyra.consume_logout/3`.
    Ensure tests explicitly verify the fail-closed states for invalid signatures and replayed IDs.
  </action>
  <verify>
    <automated>mix test test/protocol/logout_pipeline_test.exs</automated>
  </verify>
  <done>The entire SLO flow is proven secure end-to-end against signature and replay attacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application -> Relyra | The host application delegates SAML validation securely to the facade. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Information Disclosure | Facade error returns | accept | Error returns log `Relyra.Error.t()` securely; stack traces are contained. |
</threat_model>

<verification>
mix test --warnings-as-errors
mix ci.security
</verification>

<success_criteria>
- The host application can successfully consume and start logout flows.
- End-to-end pipeline verification prevents all specified attacks.
</success_criteria>

<output>
After completion, create `.planning/phases/38-single-logout-slo-core-security/38-04-SUMMARY.md`
</output>