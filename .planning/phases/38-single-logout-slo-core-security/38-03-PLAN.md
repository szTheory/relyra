---
phase: 38-single-logout-slo-core-security
plan: 03
type: execute
wave: 3
depends_on: ["38-02"]
files_modified:
  - lib/relyra/security/logout_validator.ex
  - test/relyra/security/logout_validator_test.exs
autonomous: true
requirements: [SLO-01]
must_haves:
  truths:
    - "Logout messages run through a strict Parse -> Verify -> Replay -> Execute pipeline"
    - "Replay protection is enforced on all consumed logout messages"
  artifacts:
    - path: "lib/relyra/security/logout_validator.ex"
      provides: "validate_logout_request/3 and validate_logout_response/3"
  key_links:
    - from: "lib/relyra/security/logout_validator.ex"
      to: "lib/relyra/replay_store.ex"
      via: "message ID uniqueness check"
---

<objective>
Enforce the strict validation pipeline for SAML Single Logout messages.

Purpose: 
Just like `ValidationPipeline` for SSO, SLO must rigorously sequence validation: Parse -> Signature Verify -> Replay Check -> Data Access. Reversing this sequence permits DoS or spoofing.
Output: `LogoutValidator` pipeline service.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-single-logout-slo-core-security/PATTERNS.md
@.planning/phases/38-single-logout-slo-core-security/38-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the strict Logout Validation Pipeline</name>
  <files>lib/relyra/security/logout_validator.ex, test/relyra/security/logout_validator_test.exs</files>
  <behavior>
    - `validate_logout_request/3` verifies the signature, enforces replay protection on the ID, and validates the issuer before returning the typed request.
    - `validate_logout_response/3` verifies the signature, enforces replay, checks status codes, and matches issuer.
  </behavior>
  <action>
    Create `Relyra.Security.LogoutValidator`. 
    Implement `validate_logout_request/3` (args: `raw_xml_or_query`, `connection`, `opts`).
    If POST binding: Run `PureBeam.parse_safely/2`, then `Signature.do_verify/4`, then check `ReplayStore`.
    If Redirect binding: Verify signature on raw query via `verify_redirect_signature/4`, parse xml via `PureBeam.parse_safely/2`, then check `ReplayStore`.
    
    Implement `validate_logout_response/3` (args: `raw_xml_or_query`, `connection`, `opts`) with identical strict sequencing: verify signature (POST via `do_verify/4` after parse, Redirect via `verify_redirect_signature/4` before parse), enforce replay protection via `ReplayStore`, check status codes, and match issuer.
    
    Reference the analog `lib/relyra/protocol/validation_pipeline.ex` (used for SSO) to ensure identical failure modes and typed `%Relyra.Error{}` returns.
    If valid, return `{:ok, %LogoutRequest{}}` or `{:ok, %LogoutResponse{}}`. Else `{:error, %Error{}}`.
  </action>
  <verify>
    <automated>mix test test/relyra/security/logout_validator_test.exs</automated>
  </verify>
  <done>Pipeline strictly guards data access behind mathematical and replay verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SAML Payload -> Pipeline | Incoming payloads transition from untrusted to trusted only after `Signature` and `ReplayStore` checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03 | Denial of Service | `LogoutValidator` | mitigate | Require `ReplayStore.check_and_store` for every logout message ID to prevent replayed logout attacks. |
| T-38-04 | Spoofing | `LogoutValidator` | mitigate | Strict execution order: verify signature mathematically BEFORE attempting to act on SessionIndex. |
</threat_model>

<verification>
mix test --warnings-as-errors
</verification>

<success_criteria>
- The validation pipeline rejects tampered, unsigned, or replayed logout messages with typed errors.
</success_criteria>

<output>
After completion, create `.planning/phases/38-single-logout-slo-core-security/38-03-SUMMARY.md`
</output>