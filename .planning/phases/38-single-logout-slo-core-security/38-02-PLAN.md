---
phase: 38-single-logout-slo-core-security
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - lib/relyra/protocol/logout_request.ex
  - lib/relyra/protocol/logout_response.ex
autonomous: true
requirements: [SLO-01]
must_haves:
  truths:
    - "LogoutRequest is strictly parsed and generated"
    - "LogoutResponse is strictly parsed and generated"
  artifacts:
    - path: "lib/relyra/protocol/logout_request.ex"
      provides: "Parser and struct"
    - path: "lib/relyra/protocol/logout_response.ex"
      provides: "Parser and struct"
  key_links:
    - from: "lib/relyra/protocol/logout_request.ex"
      to: "lib/relyra/security/xml/pure_beam.ex"
      via: "SaxyTree parser seam"
---

<objective>
Implement strictly-typed models and XML serializers/parsers for `LogoutRequest` and `LogoutResponse`.

Purpose: 
SLO flows pass structured messages over HTTP. We must rely exclusively on the `SaxyTree` (from `PureBeam.parse_safely/2`) to extract elements like `<samlp:SessionIndex>` and `<saml:NameID>`.
Output: `LogoutRequest` and `LogoutResponse` modules.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-single-logout-slo-core-security/PATTERNS.md
@.planning/phases/38-single-logout-slo-core-security/38-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement LogoutRequest protocol model</name>
  <files>lib/relyra/protocol/logout_request.ex, test/relyra/protocol/logout_request_test.exs</files>
  <behavior>
    - Can generate a valid `LogoutRequest` XML tree structure for a given `NameID` and `SessionIndex`.
    - Can parse a `LogoutRequest` from a `SaxyTree` root node, extracting Issuer, NameID, and SessionIndex safely.
  </behavior>
  <action>
    Create `Relyra.Protocol.LogoutRequest`.
    Implement `from_parsed_doc/1` that strictly traverses the `SaxyTree` structure to extract `<saml:NameID>` and `<samlp:SessionIndex>`.
    Implement `to_xml/1` to generate a SaxyTree representing the request to be serialized.
    Follow the exact design pattern of `Relyra.Protocol.AuthnRequest`.
  </action>
  <verify>
    <automated>mix test test/relyra/protocol/logout_request_test.exs</automated>
  </verify>
  <done>LogoutRequest supports parsing and generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LogoutResponse protocol model</name>
  <files>lib/relyra/protocol/logout_response.ex, test/relyra/protocol/logout_response_test.exs</files>
  <behavior>
    - Can generate a valid `LogoutResponse` XML tree structure indicating Success or Error.
    - Can parse a `LogoutResponse` from a `SaxyTree` root node, extracting Issuer and StatusCode.
  </behavior>
  <action>
    Create `Relyra.Protocol.LogoutResponse`.
    Implement `from_parsed_doc/1` and `to_xml/1` following the patterns in `Relyra.Protocol.Response`.
    Ensure `validate_issuer/2` pattern is implemented or leveraged properly.
  </action>
  <verify>
    <automated>mix test test/relyra/protocol/logout_response_test.exs</automated>
  </verify>
  <done>LogoutResponse supports parsing and generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extracted Data | Fields parsed from the SaxyTree must not be trusted until the signature pipeline completes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02 | Tampering | `LogoutRequest` parser | mitigate | Parse from the hardened `SaxyTree` only. Do not perform any side-effects during parsing. |
</threat_model>

<verification>
mix test --warnings-as-errors
</verification>

<success_criteria>
- `LogoutRequest` and `LogoutResponse` can be parsed from and serialized to XML trees natively without bypassing `PureBeam`.
</success_criteria>

<output>
After completion, create `.planning/phases/38-single-logout-slo-core-security/38-02-SUMMARY.md`
</output>
