---
phase: 66-demo-fakeidp-disposition
plan: 04
type: execute
wave: 3
depends_on:
  - 66-01
  - 66-02
files_modified:
  - guides/fake_idp_demo.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DEMO-03
user_setup: []

must_haves:
  truths:
    - "The FakeIdP browser flow is clearly documented for its demo purpose."
    - "Documentation explains how to access and use the FakeIdP."
    - "SEED-003 is formally resolved in `.planning/STATE.md`."
  artifacts:
    - path: "guides/fake_idp_demo.md"
      provides: "Documentation for FakeIdP demo flow"
      min_lines: 20
    - path: ".planning/STATE.md"
      contains: "SEED-003: RESOLVED"
  key_links: []
---

<objective>
If the user decided to **retain** the LedgerLoop FakeIdP in Plan 02, this plan will document its purpose and usage within the demo and update `STATE.md` to reflect the resolution of SEED-003.

Purpose: To clarify the role of the FakeIdP within the demo, ensuring it serves as an intentional and well-understood local testing or demonstration tool.
Output: Clear documentation for the FakeIdP flow and SEED-003 marked as resolved in `STATE.md`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-demo-fakeidp-disposition/66-01-SUMMARY.md
@.planning/phases/66-demo-fakeidp-disposition/66-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document FakeIdP Browser Flow</name>
  <files>
    guides/fake_idp_demo.md
  </files>
  <action>
    Create a new guide file `guides/fake_idp_demo.md`.
    Add content to this file explaining the LedgerLoop FakeIdP browser flow. Describe its purpose (e.g., local testing, demonstration of IdP-initiated login), how to access it (e.g., URL `/fake_idp/login`), and expected behavior.
    Include instructions on how to use it for development or testing purposes.
  </action>
  <verify>
    <automated>
      ls guides/fake_idp_demo.md
      grep -c "FakeIdP" guides/fake_idp_demo.md
    </automated>
  </verify>
  <done>
    The FakeIdP browser flow is comprehensively documented in `guides/fake_idp_demo.md`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve SEED-003 in STATE.md</name>
  <files>
    .planning/STATE.md
  </files>
  <action>
    Edit `.planning/STATE.md` to locate the reference to `SEED-003` and update its status to `RESOLVED`. Add a comment indicating its resolution by documentation and its clarified purpose.
  </action>
  <verify>
    <automated>
      grep -c "SEED-003: RESOLVED" .planning/STATE.md
    </automated>
  </verify>
  <done>
    SEED-003 is formally resolved in `.planning/STATE.md`, reflecting its clarification through documentation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo app → local filesystem | The demo application reads documentation files from its local filesystem. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
The documentation for the FakeIdP flow is created and clear, and SEED-003 is marked as resolved in `STATE.md`.
</verification>

<success_criteria>
The LedgerLoop FakeIdP browser flow's purpose is clearly documented, making it an intentional part of the demo, and SEED-003's resolution is documented.
</success_criteria>

<output>
Create `.planning/phases/66-demo-fakeidp-disposition/66-04-SUMMARY.md` when done
</output>
