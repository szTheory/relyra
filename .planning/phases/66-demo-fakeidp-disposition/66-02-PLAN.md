---
phase: 66-demo-fakeidp-disposition
plan: 02
type: execute
wave: 2
depends_on:
  - 66-01
files_modified: []
autonomous: false
requirements:
  - DEMO-02
user_setup: []

must_haves:
  truths:
    - "An explicit decision has been made regarding the FakeIdP flow (retain or remove)."
    - "A clear directive for SEED-003 resolution is recorded."
  artifacts: []
  key_links: []
---

<objective>
Present the audit findings for the LedgerLoop FakeIdP to the user and obtain an explicit decision on whether to retain or remove the feature from the demo.

Purpose: To make a critical project decision that dictates subsequent implementation steps for the phase, ensuring alignment with the overall demo strategy.
Output: A recorded user decision (retain or remove) for the FakeIdP browser flow, and the path for SEED-003 resolution.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-demo-fakeidp-disposition/66-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Decide on the disposition of the LedgerLoop FakeIdP browser flow and the resolution of SEED-003.</decision>
  <context>
    Based on the audit from Plan 01 (66-01-SUMMARY.md), the current state of the `/fake_idp` routes, controllers, views, tests, and the context of SEED-003 are understood.
    The goal of this phase is to ensure the demo has one intentional login story. This requires an explicit decision to either remove the FakeIdP, simplifying the demo, or retain it and ensure it's well-documented as an intentional local proof.
  </context>
  <options>
    <option id="remove_fakeidp">
      <name>Remove FakeIdP and related code</name>
      <pros>Simplifies the demo codebase, removes potential confusion, ensures a single canonical demo login path.</pros>
      <cons>Loses a potentially useful local IdP simulation for development/testing.</cons>
    </option>
    <option id="retain_fakeidp">
      <name>Retain FakeIdP and document its purpose</name>
      <pros>Keeps a local IdP simulation available, useful for specific demo/testing scenarios.</pros>
      <cons>Requires dedicated documentation to clarify its role and prevent confusion in the main demo flow.</cons>
    </option>
  </options>
  <resume-signal>
    Select one option: `remove_fakeidp` or `retain_fakeidp`.
    Also, state how SEED-003 should be resolved. For example:
    - If `remove_fakeidp`: "SEED-003 should be RESOLVED: Removed with FakeIdP"
    - If `retain_fakeidp`: "SEED-003 should be RESOLVED: Addressed by FakeIdP documentation"
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → orchestrator | User's decision is passed from the client (user) to the orchestrator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
The user's explicit decision regarding the FakeIdP disposition and the proposed resolution for SEED-003 are clearly recorded.
</verification>

<success_criteria>
An unambiguous decision is obtained for the FakeIdP browser flow, along with a plan for resolving SEED-003, which will direct subsequent implementation steps.
</success_criteria>

<output>
Create `.planning/phases/66-demo-fakeidp-disposition/66-02-SUMMARY.md` when done
</output>
