---
phase: 21.1
slug: close-gap-cfg-07-bulk-refresh-audit-correlation-id-forwardin
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-07
---

# Phase 21.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Sourced from `21.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit (Elixir 1.19, OTP 28) — `Relyra.TestSupport.MigrationCase, async: false` |
| **Config file** | `test/test_helper.exs` + `test/support/migration_case.ex` |
| **Quick run command** | `mix test test/relyra/metadata/refresh_bulk_audit_test.exs --warnings-as-errors` |
| **Full suite command** | `mix test --warnings-as-errors` |
| **Estimated runtime** | ~10s quick / ~60–90s full |

---

## Sampling Rate

- **After every task commit:** Run quick command
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green AND `mix compile --warnings-as-errors`
  AND `mix compile --no-optional-deps --warnings-as-errors`
- **Max feedback latency:** ~10 seconds (quick) / ~90 seconds (full)

---

## Per-Task Verification Map

> Each task in `21.1-01-PLAN.md` maps to one row here.
> Coverage hits the 5 required surfaces:
>
> 1. **Bulk audit-cohesion test** — 3 connections via `BulkActions.run/4` produce 3
>    `metadata.applied` audit-ledger rows that share the BulkActions-generated
>    `correlation_id` AND carry the bulk audit map's `actor` / `cause`.
> 2. **Single-connection top-level-opts regression test** — `Refresh.refresh/2` called
>    with top-level `actor:` / `cause:` (no `audit:` wrapper) still produces a valid
>    audit-ledger row preserving the actor/cause keyword opts.
> 3. **Failure-path no-audit invariant** — `record_refresh_failure/7` does NOT write to
>    the audit ledger today (only revision + health state). Test asserts no
>    `metadata.applied` audit row is appended on failure, even when `opts[:audit]` is
>    present.
> 4. **Phase 20 / Phase 21 regression baseline** — existing
>    `test/relyra/ecto/bulk_actions_test.exs`,
>    `test/relyra/metadata_refresh_test.exs`,
>    `test/relyra/ecto/metadata_apply_test.exs`,
>    `test/relyra/metadata/auto_refresh_test.exs`
>    continue to pass without modification.
> 5. **Dual-compile-lane gate (engineering DNA §3)** — both
>    `mix compile --warnings-as-errors` and `mix compile --no-optional-deps
>    --warnings-as-errors` are green.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| T1 | 01 | 0 | CFG-07 | T-21.1-01 | New file `test/relyra/metadata/refresh_bulk_audit_test.exs` exists with 3 RED tests (bulk audit-cohesion + single-connection regression + failure-path no-audit invariant); all 3 fail today (proving the gap is real before implementation lands) | integration | `mix test test/relyra/metadata/refresh_bulk_audit_test.exs --warnings-as-errors` (expect 3 failures pre-implementation) | ❌ W0 | ⬜ pending |
| T2 | 01 | 1 | CFG-07 | T-21.1-01 | `Refresh.refresh/2` forwards `opts[:audit]` into `MetadataApply.apply_revision/4` and `MetadataApply.record_attempt/3`; Wave-0 tests turn GREEN (3 audit rows share one correlation_id; single-connection still attributable; failure path remains ledger-silent) | integration | `mix test test/relyra/metadata/refresh_bulk_audit_test.exs test/relyra/metadata_refresh_test.exs test/relyra/ecto/metadata_apply_test.exs test/relyra/ecto/bulk_actions_test.exs test/relyra/metadata/auto_refresh_test.exs --warnings-as-errors` | ✅ post-T1 | ⬜ pending |
| T3 | 01 | 2 | CFG-07 | T-21.1-01 | Dual-compile-lane gate green: both `--warnings-as-errors` lanes compile cleanly with the new code; full suite passes | integration | `mix compile --warnings-as-errors && mix compile --no-optional-deps --warnings-as-errors && mix test --warnings-as-errors` | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] **Decision (planner):** Create new file `test/relyra/metadata/refresh_bulk_audit_test.exs`
      — one-concern-per-file matches project convention (`auto_refresh_test.exs`,
      `bulk_actions_test.exs`, `metadata_apply_test.exs`). Existing
      `metadata_refresh_test.exs` is the single-connection regression baseline; its
      3 tests are NOT modified.
- [ ] T1 creates `test/relyra/metadata/refresh_bulk_audit_test.exs` with 3 net-new
      RED tests + fixture helpers copied verbatim from
      `test/relyra/metadata_refresh_test.exs:100-218`.
- [ ] No new framework install — ExUnit + `Relyra.TestSupport.MigrationCase` already
      present.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|

*All phase behaviors have automated verification — no manual-only verifications.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s (full suite)
- [ ] Dual-compile-lane gate present in plan (engineering DNA §3)
- [ ] `nyquist_compliant: true` set in frontmatter (planner sets after Wave 0 complete)

**Approval:** pending
