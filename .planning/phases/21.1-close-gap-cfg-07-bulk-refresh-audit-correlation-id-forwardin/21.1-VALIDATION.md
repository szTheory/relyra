---
phase: 21.1
slug: close-gap-cfg-07-bulk-refresh-audit-correlation-id-forwardin
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-07
---

# Phase 21.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Sourced from `21.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit (Elixir 1.18+) — Relyra.MigrationCase, async: false |
| **Config file** | `test/test_helper.exs` + per-suite `Relyra.MigrationCase` |
| **Quick run command** | `mix test test/relyra/metadata_refresh_test.exs --warnings-as-errors` |
| **Full suite command** | `mix test --warnings-as-errors` |
| **Estimated runtime** | ~10s quick / ~60–90s full |

---

## Sampling Rate

- **After every task commit:** Run quick command
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green AND `mix compile --warnings-as-errors`
  AND `mix compile --no-optional-deps --warnings-as-errors`
- **Max feedback latency:** ~10 seconds (quick) / ~90 seconds (full)

---

## Per-Task Verification Map

> Filled in by planner. Each task in PLAN.md must map to one row here.
> Coverage MUST hit:
>
> 1. **Bulk audit-cohesion test** — 3 connections via `BulkActions.run/4` produce 3
>    `metadata.applied` audit-ledger rows that share the BulkActions-generated
>    `correlation_id` AND carry the bulk audit map's `actor` / `cause`.
> 2. **Single-connection top-level-opts regression test** — `Refresh.refresh/2` called
>    with top-level `actor:` / `cause:` (no `audit:` wrapper) still produces a valid
>    audit-ledger row preserving the actor/cause keyword opts (regression guard for
>    `LiveAdmin` single-action and `AutoRefresh.refresh/2` callers).
> 3. **Failure-path no-audit invariant** — `record_refresh_failure/7` does NOT write to
>    the audit ledger today (only revision + health state). Test asserts no
>    `metadata.applied` audit row is appended on failure, even when `opts[:audit]` is
>    present (preserves D-35 single-audit-writer-seam invariant).
> 4. **Phase 20 / Phase 21 regression baseline** — existing tests in
>    `test/relyra/ecto/bulk_actions_test.exs`, `test/relyra/metadata_refresh_test.exs`,
>    `test/relyra/metadata_apply_test.exs` continue to pass without modification.
> 5. **Dual-compile-lane gate (engineering DNA §3)** — both
>    `mix compile --warnings-as-errors` and `mix compile --no-optional-deps
>    --warnings-as-errors` are green.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| {planner-fills} | {N} | {N} | CFG-07 | — | {expected behavior} | integration | `{command}` | ✅ / ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Filled in by planner. Likely:
> - [ ] `test/relyra/metadata/refresh_bulk_audit_test.exs` (or extend existing
>       `test/relyra/metadata_refresh_test.exs`) — net-new bulk audit-cohesion + single-
>       connection regression + failure-path invariant tests
> - [ ] No new framework install — ExUnit + `Relyra.MigrationCase` already present.

*If the planner extends the existing file instead of creating a new one, Wave 0 is "tests
added inline; no new file" — still required to land before the implementation task.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|

*All phase behaviors have automated verification — no manual-only verifications expected.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s (full suite)
- [ ] Dual-compile-lane gate present in plan (engineering DNA §3)
- [ ] `nyquist_compliant: true` set in frontmatter (planner sets after Wave 0 complete)

**Approval:** pending
