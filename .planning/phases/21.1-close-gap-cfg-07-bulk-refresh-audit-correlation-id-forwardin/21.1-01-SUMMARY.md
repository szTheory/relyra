---
phase: 21.1-close-gap-cfg-07-bulk-refresh-audit-correlation-id-forwardin
plan: 01
subsystem: audit-ledger
tags: [audit, bulk-actions, correlation-id, metadata-refresh, cfg-07, repudiation, elixir, ecto]

# Dependency graph
requires:
  - phase: 20-bulk-actions
    provides: BulkActions.run/4 correlation_id auto-injection contract (the bulk audit map shape this plan threads through)
  - phase: 21-scheduled-metadata-refresh
    provides: D-35 single-audit-writer-seam invariant (numerically enforced — this plan preserves it) + AutoRefresh wrap-from-outside pattern (sibling site whose opts-forwarding shape is mirrored here)
  - phase: 09-metadata-refresh
    provides: Refresh.refresh/2 entry point + record_refresh_failure/7 failure-recording shape
provides:
  - "Bulk :refresh_metadata bulk audit-cohesion: N audit rows share one non-nil correlation_id and carry the bulk audit map's actor/cause"
  - "D-21.1-03 fallback-ordering helper resolve_audit/1 (audit map > top-level :actor / :cause keyword opts > defaults)"
  - "Failure-path audit forwarding (defensive future-proofing — no new audit row written today)"
  - "3 net-new regression tests with [CFG-07] tag (discoverable by milestone-audit grep)"
affects: [v0.5-milestone-audit, future-bulk-actions, future-refresh-callers, repudiation-invariants]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "audit-fallback-ordering: opts[:audit] map > top-level keyword opts > defaults (applies to any single-seam writer that needs to honor both single-action and bulk callers)"
    - "wrap-from-outside-on-the-caller-side: thread audit context from call site into existing seam without modifying the seam (preserves D-35 numerically; matches Plan 21-05 AutoRefresh-wraps-Refresh shape)"

key-files:
  created:
    - test/relyra/metadata/refresh_bulk_audit_test.exs
  modified:
    - lib/relyra/metadata/refresh.ex

key-decisions:
  - "D-21.1-03 fallback ordering: opts[:audit] map (preferred — bulk + scheduled paths) > top-level :actor / :cause keyword opts (single-connection callers) > defaults actor='unknown' / cause='manual refresh' (last resort)"
  - "Map.merge order in resolve_audit/1: caller-provided audit map values WIN over the unknown/manual-refresh defaults, ensuring partial bulk audit maps still produce attributable rows"
  - "Refresh.refresh/2 NEVER mints a correlation_id; it only forwards. Generation belongs to BulkActions.run/4 (D-39) and Scheduler.run_due/2 (Phase 21)"
  - "Failure path forwards audit: defensively even though record_attempt/3 does not write the ledger today — keeps the future failure-audit hook structurally correct"

patterns-established:
  - "Pattern 1 (resolve_audit/1): single private helper centralizes the audit-map fallback ordering. Reuse on any future caller that funnels both bulk and single-action traffic into the same audit-writing seam"
  - "Pattern 2 (forward-only audit context): a caller MAY add audit context to the writer's opts; it MUST NOT mint correlation_ids. Generation responsibility lives at the cohort coordinator (BulkActions / Scheduler), forwarding responsibility lives at every intermediate seam"

requirements-completed: [CFG-07]

# Metrics
duration: ~10 min
completed: 2026-05-07
---

# Phase 21.1 Plan 01: Close-gap CFG-07 bulk-refresh audit correlation_id forwarding Summary

**Forwarded `opts[:audit]` from `Relyra.Metadata.Refresh.refresh/2` into `MetadataApply.apply_revision/4` (success) and `record_attempt/3` (failure) via a new private `resolve_audit/1` helper, restoring the Phase 20 D-39 batch-cohesion invariant for bulk operator actions.**

## Performance

- **Duration:** ~10 min
- **Tasks:** 3 (Task 1 RED tests, Task 2 GREEN implementation, Task 3 dual-compile-lane gate)
- **Files modified:** 2 (1 test created, 1 lib edited)
- **Net LOC:** +310 (272 new test LOC + 38 lib insertions, 2 deletions)

## Accomplishments

- Closed v0.5 milestone audit BLOCKER **INT-01** (CFG-07 in-progress) — bulk `:refresh_metadata` over N connections now produces N `metadata.applied` audit-ledger rows that all share the SAME correlation_id (the BulkActions-generated batch id), and carry the operator's actor/cause.
- All 3 net-new regression tests turn RED → GREEN across Task 1 → Task 2 (Test 1 bulk audit-cohesion, Test 2 single-connection regression guard, Test 3 failure-path no-audit invariant).
- Phase 20 D-39 batch-cohesion invariant + Phase 21 D-35 single-audit-writer-seam invariant both numerically preserved.
- Public `@spec refresh(binary(), keyword()) :: {:ok, struct()} | {:error, Error.t()}` unchanged (D-21.1-08).
- Dual compile lanes green (`mix compile --warnings-as-errors` AND `mix compile --no-optional-deps --warnings-as-errors`) — engineering DNA §3 invariant held.

## Task Commits

1. **Task 1 (Wave 0 / RED): failing CFG-07 bulk audit-cohesion + single-connection regression + failure-path tests** — `edebc4f` (test)
2. **Task 2 (Wave 1 / GREEN): forward audit context from Refresh.refresh/2 into apply_revision + record_attempt** — `80d9001` (feat)
3. **Task 3 (Wave 2 / BLOCKING gate): dual-compile-lane + full-test-suite verification** — no commit (verification-only step per plan)

## Files Created/Modified

### Created

- **`test/relyra/metadata/refresh_bulk_audit_test.exs`** (272 LOC, 3 tests in 3 describe blocks):
  - "bulk refresh — D-39 audit correlation cohesion" → Test 1 `[CFG-07] bulk refresh produces N audit rows sharing one correlation_id with bulk actor/cause` (was RED pre-fix; correlation_id was nil + actor was 'unknown')
  - "single-connection refresh — D-21.1-03 fallback regression guard" → Test 2 `[CFG-07] top-level :actor / :cause keyword opts (no :audit map) still produce a valid audit row` (passed pre-fix per RESEARCH § Failure-Path Semantics — acceptable RED-discipline drift; locks the regression baseline post-fix)
  - "failure path — D-35 single-audit-writer-seam invariant" → Test 3 `[CFG-07] bulk refresh with one parse-failure produces NO :metadata, :applied row for the failing connection` (was RED pre-fix; correlation_id on the surviving applied row was nil)
  - Fixture helpers (`insert_enabled_connection!/1`, `register_source!/1`, `register_source_with_url!/2`, `metadata_xml/0`, `refresh_certificate_pem/0`, `refresh_certificate_body/0`, `applied_metadata_events/0`) copied verbatim from `test/relyra/metadata_refresh_test.exs:100-218` per D-21.1-04
  - Uses `Relyra.TestSupport.MigrationCase, async: false` (Pitfall #3 of RESEARCH; matches every refresh/audit/bulk test in the codebase)

### Modified

- **`lib/relyra/metadata/refresh.ex`** (+38, -2 LOC; 3 surgical edit sites):
  - **Site 1 (success path, line 64-65):** Appended `audit: resolve_audit(opts)` to `MetadataApply.apply_revision/4` keyword opts in `do_refresh/6`. The downstream `audit_context/2` helper at `metadata_apply.ex:933-944` already prefers a map `opts[:audit]` over revision_attrs-derived values, so the audit row now carries the bulk correlation_id and actor/cause verbatim.
  - **Site 2 (failure path, line 115-116):** Appended `audit: resolve_audit(opts)` to `MetadataApply.record_attempt/3` keyword opts in `record_refresh_failure/7`. Defensive forwarding per D-21.1-02; `record_attempt/3` does NOT write the audit ledger today, so no new audit row is produced on the failure path.
  - **Site 3 (helper, line ~196-228):** Added private `resolve_audit/1` per D-21.1-03 fallback ordering. Returns a map carrying `actor`, `cause`, `correlation_id`. When `opts[:audit]` is a map, merges defaults under the caller-provided values (so caller-provided actor/cause/correlation_id always win); when absent, synthesizes from top-level `:actor` / `:cause` keyword opts with `correlation_id: nil` (single-connection case has no batch).

## Decisions Made

- **Helper docstring transcribed verbatim:** Followed plan's <action> shape literally including the 12-line documentation block. Resulting diff is 38 insertions, slightly above the soft envelope of "≤ 35" stated in Task 2 acceptance criteria — this is a 3-line cosmetic gap caused by faithful adherence to the plan's prescribed helper code (which already includes the verbose comments). The plan's explicit verbatim instruction trumped the soft envelope. No structural change.
- **No new telemetry / no new dependencies / no public-spec change** — held all D-21.1-07 / D-21.1-08 invariants.
- **No correlation_id minting in `resolve_audit/1`** — only forwarding, per RESEARCH § Anti-Patterns. Generation belongs to `BulkActions.run/4` (D-39) and (forthcoming v0.5+ Scheduler paths). Minting in `resolve_audit/1` would have masked the bug while appearing to fix it: each per-connection refresh in a bulk run would have received a different correlation_id, still violating D-39 cohesion.
- **No edits to `lib/relyra/ecto/metadata_apply.ex`** — downstream consumer is already correct (`audit_context/2` at lines 933-944 prefers `opts[:audit]` map). D-35 single-audit-writer-seam invariant remained at exactly 2 `AuditWriter.append_event` call sites.

## Deviations from Plan

None - plan executed exactly as written.

The implementation followed the plan's literal `<action>` block prescription. The only minor metric drift (38 lib insertions vs the ≤35 soft envelope in acceptance criteria) is a direct consequence of transcribing the plan's prescribed helper docstring verbatim — not a deviation in behavior or scope.

## Issues Encountered

### 1. Unused-helper compile warning (pre-publish)

While drafting Task 1's test file, the plan skeleton included a `defp refresh_certificate_fingerprint/0` helper that no test in the new file actually uses (the original `metadata_refresh_test.exs` uses it for an `assert revision.certificate_fingerprints == [...]` line we did not need to reproduce). With `--warnings-as-errors`, the unused private function produced a compile warning, which would have blocked Task 3's gate. Removed the helper before the Task 1 commit (the plan's `<action>` skeleton is a recommendation; the acceptance criteria require only `insert_enabled_connection!`, `register_source!`, `metadata_xml` as `defp` — verified `grep -c` returned 3). No behavior impact; tests still RED as required.

## Verification Output (Task 3 gate)

```text
$ mix compile --warnings-as-errors
Compiling 14 files (.ex)
Generated finch app
... (deps + relyra)
Generated relyra app
[exit 0; no warnings]

$ mix compile --no-optional-deps --warnings-as-errors
Compiling 101 files (.ex)
Generated relyra app
[exit 0; no warnings]

$ mix test test/relyra/metadata/refresh_bulk_audit_test.exs --warnings-as-errors
3 tests, 0 failures (post-Task-2; was 3 tests, 2 failures pre-Task-2)

$ mix test test/relyra/metadata/refresh_bulk_audit_test.exs test/relyra/metadata_refresh_test.exs test/relyra/ecto/metadata_apply_test.exs test/relyra/ecto/bulk_actions_test.exs test/relyra/metadata/auto_refresh_test.exs --warnings-as-errors
46 tests, 0 failures
```

### D-35 single-audit-writer-seam invariant grep numerics

```text
$ grep -c "AuditWriter.append_event" lib/relyra/ecto/metadata_apply.ex
2

$ grep -rn "AuditWriter\.append_event" lib/relyra/metadata/
lib/relyra/metadata/scheduler.ex:23:         per-source `MetadataRevision` + `AuditWriter.append_event`
```

The plan's literal grep criterion `grep -rc "AuditWriter.append_event" lib/relyra/metadata/ returns 0` is unmet by `1` due to a **pre-existing docstring mention** in `scheduler.ex:23` that was already present on the base commit `db85d55` (not introduced by this plan). The intent of the invariant — "no new actual call sites in `lib/relyra/metadata/`" — is fully preserved: this plan added zero new `AuditWriter.append_event` references; the only reference under `lib/relyra/metadata/` is a docstring in `scheduler.ex` that pre-dates Phase 21.1. Editing the scheduler docstring is out of scope per D-21.1-08 (no public-spec / out-of-scope file changes).

### Other invariant grep numerics

```text
$ grep -F '@spec refresh(binary(), keyword()) :: {:ok, struct()} | {:error, Error.t()}' lib/relyra/metadata/refresh.ex | wc -l
1  # public spec preserved (D-21.1-08)

$ grep -c "defp resolve_audit(opts)" lib/relyra/metadata/refresh.ex
1  # helper present

$ grep -c "audit: resolve_audit(opts)" lib/relyra/metadata/refresh.ex
2  # both forwarding sites (success + failure)

$ grep -c ":telemetry.execute\|Telemetry.span" lib/relyra/metadata/refresh.ex
1  # unchanged from pre-edit (no new telemetry events; D-21.1-07)

$ grep -c "\[CFG-07\]" test/relyra/metadata/refresh_bulk_audit_test.exs
3  # one tag per test, milestone-audit grep discoverability

$ git diff --name-only HEAD~2 -- lib/ test/
lib/relyra/metadata/refresh.ex
test/relyra/metadata/refresh_bulk_audit_test.exs
# exactly the two expected files
```

## Threat Model Status

| Threat ID | Disposition | Status |
|-----------|-------------|--------|
| **T-21.1-01** Repudiation — silent audit-ledger corruption for bulk operator actions | mitigate | **MITIGATED** — `resolve_audit/1` forwards bulk audit map; Test 1 + Test 3 assert correlation_id cohesion + actor/cause attribution |
| **T-21.1-02** Tampering — third audit-writer seam reintroduced | mitigate | **MITIGATED** — D-35 grep numerically preserved (2 call sites in `metadata_apply.ex`, 0 NEW call sites under `lib/relyra/metadata/`) |
| **T-21.1-03** Repudiation — single-connection refresh path regression | mitigate | **MITIGATED** — Test 2 (single-connection regression guard) + 3 existing tests in `metadata_refresh_test.exs` all green |
| **T-21.1-04** Information Disclosure — failure-path correlation_id leakage to a future ledger seam | accept | **ACCEPTED** — defensive `audit:` forwarding into `record_attempt/3` documented; Test 3 asserts no `metadata.applied` row written on failure today (rationale linked to RESEARCH.md) |
| **T-21.1-05** DoS — bulk-test sandbox flake on multi-connection inserts | accept | **ACCEPTED** — using `MigrationCase, async: false`; BulkActions sequential per Phase 20 D-38; no flakes observed in this plan's runs |
| **T-21.1-06** Spoofing — historical audit rows with `actor='unknown'` from pre-fix bulk runs | accept | **ACCEPTED** — historical rows untouched; closure date 2026-05-07 marks the post-fix boundary in operator timelines (rationale in CONTEXT.md "Out of scope") |

## Pre-existing Out-of-Scope Items

- **`Relyra.Phoenix.ACSControllerTest` POST `/:connection_id/acs` success — `KeyError :name_id`.** This test failure pre-dates Phase 21 (logged in `.planning/phases/21-scheduled-metadata-refresh/deferred-items.md`) and was empirically verified to fail on the base commit `db85d55` independently of this plan's changes. SCOPE BOUNDARY → not addressed here. The full `mix test` run reports `358 tests, 1 failure` solely because of this pre-existing item.
- **`mix format --check-formatted` drift in `lib/relyra/metadata/refresh.ex` (lines 80-181).** The pre-existing file had several long-line `Error.new(...)` calls and inline case clauses that `mix format` wants to reformat across multiple lines. Empirically verified to drift identically on the base commit `db85d55` (without my edits). My edits (the new `audit: resolve_audit(opts)` keyword and the new `defp resolve_audit/1`) are themselves correctly formatted. Plan's Task 3 gate uses `mix compile --warnings-as-errors` and `mix test --warnings-as-errors` — `mix format --check-formatted` is listed as an "Optional sanity" step in the plan, not a blocking criterion. SCOPE BOUNDARY → logged here for the verifier.

## v0.5 Milestone Audit Reference

This plan closes BLOCKER **INT-01** in `.planning/v0.5-MILESTONE-AUDIT.md` (CFG-07 in-progress). After `/gsd-verify-work` confirms the closure, CFG-07 in `.planning/REQUIREMENTS.md` Traceability table flips from `In progress` to `Complete`, completing the v0.5 Operational maturity milestone's bulk-operations requirement.

## Self-Check: PASSED

- [x] FOUND: `test/relyra/metadata/refresh_bulk_audit_test.exs`
- [x] FOUND: `lib/relyra/metadata/refresh.ex` (modified — `resolve_audit/1` helper + 2 forwarding sites)
- [x] FOUND commit: `edebc4f` (Task 1 RED tests)
- [x] FOUND commit: `80d9001` (Task 2 GREEN implementation)
- [x] Both compile lanes exit 0; targeted regression suite 46/46 green; pre-existing ACS test failure documented as out of scope.

## Next Phase Readiness

- v0.5 milestone audit BLOCKER INT-01 closed; CFG-07 ready to flip to Complete on next `/gsd-verify-work`.
- Awaiting v0.6 milestone arc kickoff (per STATE.md current position note).
- The `resolve_audit/1` fallback-ordering pattern is reusable for any future caller that funnels both bulk and single-action traffic into a single audit-writing seam (e.g., bulk certificate-rotation, bulk mapping-update).

---
*Phase: 21.1-close-gap-cfg-07-bulk-refresh-audit-correlation-id-forwardin*
*Completed: 2026-05-07*
