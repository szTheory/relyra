---
phase: 21.1-close-gap-cfg-07-bulk-refresh-audit-correlation-id-forwardin
verified: 2026-05-07T13:55:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
---

# Phase 21.1: Close-gap CFG-07 Verification Report

**Phase Goal:** Restore Phase 20 D-39 batch-cohesion invariant for bulk
`:refresh_metadata` by forwarding `opts[:audit]` (actor + cause +
BulkActions-generated `correlation_id`) from
`Relyra.Metadata.Refresh.refresh/2` into `MetadataApply.apply_revision/4`
(success path) and `MetadataApply.record_attempt/3` (failure path), closing
v0.5 milestone audit BLOCKER INT-01.

**Verified:** 2026-05-07T13:55:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                                  | Status     | Evidence                                                                                                                                                                                                                                                                                                                                            |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Bulk `:refresh_metadata` over N connections produces N `metadata.applied` audit-ledger rows that all share the SAME correlation_id (the BulkActions-generated batch id).                                              | ✓ VERIFIED | Test 1 in `test/relyra/metadata/refresh_bulk_audit_test.exs:20-70` passes. Asserts `length(correlation_ids |> uniq) == 1` AND `hd(correlation_ids) != nil` after `BulkActions.run/4` over 3 connections. Wiring traced: `BulkActions.run` (auto-injects UUID at bulk_actions.ex:16) → `Metadata.refresh/2` (verbatim opts forward at metadata.ex:55) → `Refresh.refresh/2` → `apply_revision/4` opts now include `audit: resolve_audit(opts)` (refresh.ex:65) → `audit_context/2` returns map verbatim (metadata_apply.ex:933-944) → `append_metadata_audit` reads `Map.get(audit, :correlation_id)` (metadata_apply.ex:901). |
| 2   | Bulk audit rows carry actor/cause from `opts[:audit]` (the BulkActions audit map), not the `"unknown"` / `"manual refresh"` defaults.                                                                                  | ✓ VERIFIED | Test 1 assertion 4 passes: `assert Enum.all?(applied_events, &(&1.actor == "ops@example.com"))` and `&1.cause == "live_admin_bulk_refresh_metadata"`. Confirmed via `resolve_audit/1` `%{} = audit_map ->` branch using `Map.merge(defaults, audit_map)` so caller-provided values WIN.                                                              |
| 3   | Single-connection `Refresh.refresh/2` callers (top-level `:actor` / `:cause` keyword opts, no `:audit` map) still produce a valid `metadata.applied` ledger row with the correct actor/cause and `correlation_id: nil`. | ✓ VERIFIED | Test 2 in `refresh_bulk_audit_test.exs:73-98` passes. Asserts `event.actor == "ops@example.com"`, `event.cause == "live_admin_metadata_refresh"`, AND `is_nil(event.correlation_id)`. Confirmed via `resolve_audit/1` fallback branch (refresh.ex:219-227) which synthesizes `%{actor:..., cause:..., correlation_id: nil}`.                       |
| 4   | Failure-path `Refresh.refresh/2` (`record_refresh_failure/7`) writes a `MetadataRevision` row with non-`:applied` outcome but appends NO `metadata.applied` audit-ledger row (D-35 single-audit-writer-seam invariant preserved). | ✓ VERIFIED | Test 3 in `refresh_bulk_audit_test.exs:100-153` passes. Asserts `length(applied) == 1` (good only) AND `Enum.any?(failure_revisions, &(&1.outcome != :applied))`. D-35 numerics: `grep -c "AuditWriter.append_event" lib/relyra/ecto/metadata_apply.ex` = 2 (lines 250, 895); 0 NEW call sites in `lib/relyra/metadata/`. (Note: WR-01 — failure-path forwarding is a no-op today; see Anti-Patterns below.) |
| 5   | Existing `metadata_refresh_test.exs`, `metadata_apply_test.exs`, `bulk_actions_test.exs`, `auto_refresh_test.exs` all pass without modification (Phase 20 / Phase 21 regression baseline preserved per D-21.1-06).      | ✓ VERIFIED | Targeted regression run: `mix test test/relyra/metadata/refresh_bulk_audit_test.exs test/relyra/metadata_refresh_test.exs test/relyra/ecto/metadata_apply_test.exs test/relyra/ecto/bulk_actions_test.exs test/relyra/metadata/auto_refresh_test.exs --warnings-as-errors` → `46 tests, 0 failures`.                                                |
| 6   | Both compile lanes — `mix compile --warnings-as-errors` AND `mix compile --no-optional-deps --warnings-as-errors` — are green (engineering DNA §3 dual-lane gate).                                                       | ✓ VERIFIED | `mix compile --warnings-as-errors` exit 0 (no warnings). `mix compile --no-optional-deps --warnings-as-errors` exit 0, `Compiling 101 files (.ex)` → `Generated relyra app` (no warnings). Full-suite seed-0 run (`mix test --warnings-as-errors --seed 0`): `358 tests, 1 failure` (failure = pre-existing `ACSControllerTest :name_id` KeyError, documented at `.planning/phases/21-scheduled-metadata-refresh/deferred-items.md`, reproduced on parent commit `0842687` — out of scope per phase-21.1 instructions). |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact                                          | Expected                                                                                                                                                                  | Status     | Details                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test/relyra/metadata/refresh_bulk_audit_test.exs` | Bulk audit-cohesion test + single-connection regression test + failure-path no-audit invariant test; uses `Relyra.TestSupport.MigrationCase, async: false`; min 200 LOC | ✓ VERIFIED | Exists, 272 LOC. `grep -c "use Relyra.TestSupport.MigrationCase, async: false"` = 1 (line 11). `grep -c "\[CFG-07\]"` = 3 (one tag per test). 3 `describe` blocks. 3 fixture helpers (`insert_enabled_connection!`, `register_source!`, `metadata_xml`) defined as `defp`. Direct test-suite run: `3 tests, 0 failures` (originally 2 RED pre-Task-2). |
| `lib/relyra/metadata/refresh.ex`                  | `audit:` keyword forwarding into `MetadataApply.apply_revision/4` (success path) AND `MetadataApply.record_attempt/3` (failure path); D-21.1-03 fallback resolution helper | ✓ VERIFIED | Exists, 256 LOC. `grep -c "audit: resolve_audit(opts)"` = 2 (line 65 success, line 116 failure). `grep -c "defp resolve_audit(opts)"` = 1 (line 208). Public spec preserved: `@spec refresh(binary(), keyword()) :: {:ok, struct()} | {:error, Error.t()}` at line 12 (D-21.1-08).                                                            |

### Key Link Verification

| From                                              | To                                                | Via                                                                                                                                  | Status   | Details                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test/relyra/metadata/refresh_bulk_audit_test.exs` | `lib/relyra/metadata/refresh.ex`                  | `BulkActions.run/4 → Metadata.refresh/2 → Refresh.refresh/2 → MetadataApply.apply_revision/4`                                       | ✓ WIRED  | `BulkActions.run(@repo, ids, &Metadata.refresh/2, audit: audit, ...)` at refresh_bulk_audit_test.exs:39-43. Resolves correctly: `Metadata.refresh/2` (metadata.ex:53-56) calls `Refresh.refresh/2` (refresh.ex:15) which calls `apply_revision/4` (refresh.ex:52-66).                                                          |
| `lib/relyra/metadata/refresh.ex`                  | `lib/relyra/ecto/metadata_apply.ex`               | `audit: resolve_audit(opts)` keyword forwarded into `apply_revision/4` opts AND `record_attempt/3` opts                              | ✓ WIRED  | Both call sites verified: refresh.ex:65 (success) and refresh.ex:116 (failure). Helper `resolve_audit/1` at refresh.ex:208-228 returns map with `actor`, `cause`, and (in fallback branch) `correlation_id: nil`.                                                                                                                |
| `lib/relyra/ecto/metadata_apply.ex`               | `lib/relyra/ecto/audit_writer.ex`                 | `audit_context/2` (already-correct downstream consumer at metadata_apply.ex:933-944) → `AuditWriter.append_event/2`                  | ✓ WIRED  | metadata_apply.ex:933-944 prefers `Keyword.get(opts, :audit)` map over revision_attrs. metadata_apply.ex:895-907 calls `AuditWriter.append_event` reading `Map.get(audit, :actor)`, `Map.get(audit, :cause)`, `Map.get(audit, :correlation_id)`. NOT MODIFIED by phase 21.1 (downstream was already correct per CONTEXT D-39). |

### Data-Flow Trace (Level 4)

| Artifact                                                  | Data Variable                | Source                                                                                  | Produces Real Data | Status     |
| --------------------------------------------------------- | ---------------------------- | --------------------------------------------------------------------------------------- | ------------------ | ---------- |
| `lib/relyra/metadata/refresh.ex` `do_refresh/6` success path | `audit:` keyword to apply_revision | `resolve_audit(opts)` reads `opts[:audit]` (bulk path) or top-level `:actor`/`:cause` (single) | Yes                | ✓ FLOWING |
| `lib/relyra/metadata/refresh.ex` `record_refresh_failure/7` | `audit:` keyword to record_attempt | `resolve_audit(opts)` (same helper)                                                     | No (consumer ignores) | ⚠️ STATIC (acknowledged — see WR-01 below) |
| `lib/relyra/ecto/metadata_apply.ex` `audit_context/2`     | `opts[:audit]` map           | Forwarded from `Refresh.refresh/2`                                                      | Yes                | ✓ FLOWING |
| `lib/relyra/ecto/metadata_apply.ex` `append_metadata_audit/6` | `correlation_id`, `actor`, `cause` | `Map.get(audit, ...)` from forwarded opts                                            | Yes                | ✓ FLOWING |

The success-path data flow is intact — bulk runs produce 3 audit rows with shared correlation_id and bulk operator's actor/cause (Test 1 empirically demonstrates). The failure-path "STATIC" status is acknowledged in the plan itself (D-21.1-02 defensive future-proofing) and surfaced by code review WR-01; it does not block goal achievement because the failure-path forwarding is documented as defensive and Test 3 asserts the structurally-correct behavior (no audit row written today).

### Behavioral Spot-Checks

| Behavior                                                                                          | Command                                                                                                                                          | Result                                            | Status |
| ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------- | ------ |
| New bulk audit-cohesion test suite passes                                                         | `mix test test/relyra/metadata/refresh_bulk_audit_test.exs --warnings-as-errors`                                                                 | `3 tests, 0 failures`                             | ✓ PASS |
| Targeted regression baseline (5 test files, D-21.1-06) passes                                     | `mix test test/relyra/metadata/refresh_bulk_audit_test.exs test/relyra/metadata_refresh_test.exs test/relyra/ecto/metadata_apply_test.exs test/relyra/ecto/bulk_actions_test.exs test/relyra/metadata/auto_refresh_test.exs --warnings-as-errors` | `46 tests, 0 failures` | ✓ PASS |
| Compile lane 1 (present-deps)                                                                     | `mix compile --warnings-as-errors`                                                                                                               | exit 0, no warnings                               | ✓ PASS |
| Compile lane 2 (no-optional-deps, engineering DNA §3 invariant)                                   | `mix compile --no-optional-deps --warnings-as-errors`                                                                                            | exit 0, no warnings, 101 files compiled           | ✓ PASS |
| Full test suite (deterministic seed)                                                              | `mix test --warnings-as-errors --seed 0`                                                                                                         | `358 tests, 1 failure` (pre-existing ACS, OOS)    | ✓ PASS |
| D-35 invariant: 2 `AuditWriter.append_event` sites in metadata_apply.ex                          | `grep -c "AuditWriter.append_event" lib/relyra/ecto/metadata_apply.ex`                                                                          | `2`                                               | ✓ PASS |
| D-35 invariant: 0 NEW call sites in `lib/relyra/metadata/`                                       | `grep -rn "AuditWriter\.append_event" lib/relyra/metadata/`                                                                                      | 1 match — pre-existing docstring at scheduler.ex:23 (not a call site) | ✓ PASS |
| Public spec preserved                                                                             | `grep -nF '@spec refresh(binary(), keyword()) :: {:ok, struct()} \| {:error, Error.t()}' lib/relyra/metadata/refresh.ex`                          | `12: @spec refresh(binary(), keyword()) :: ...`   | ✓ PASS |
| Files modified by phase                                                                           | `git diff --stat edebc4f~1..HEAD -- lib/ test/`                                                                                                  | `lib/relyra/metadata/refresh.ex` (+38, -2); `test/relyra/metadata/refresh_bulk_audit_test.exs` (+272) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan         | Description                                                                                                | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                              |
| ----------- | ------------------- | ---------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CFG-07      | 21.1-01-PLAN.md     | "User can run bulk operations across multiple connections" (REQUIREMENTS.md:19) — was `In progress` per Traceability | ✓ SATISFIED | INT-01 (CFG-07's blocking integration gap per `.planning/v0.5-MILESTONE-AUDIT.md:30-46`) is empirically closed: bulk `:refresh_metadata` over N connections produces N audit rows with shared correlation_id and bulk operator actor/cause (Test 1). Phase 21.1 plan declared `requirements: [CFG-07]` and asserted `requirements-completed: [CFG-07]` in SUMMARY frontmatter. The CFG-07 row in `.planning/REQUIREMENTS.md:41` Traceability table is still marked "In progress" — this is the orchestrator's post-verify update step (not within verifier scope). |

All requirement IDs declared in PLAN frontmatter (CFG-07) are accounted for. No orphaned requirements from REQUIREMENTS.md mapped to phase 21.1.

### Anti-Patterns Found

| File                              | Line     | Pattern                                                                                          | Severity   | Impact                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------- | -------- | ------------------------------------------------------------------------------------------------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `lib/relyra/metadata/refresh.ex`  | 116      | Forwarding `audit: resolve_audit(opts)` into `record_attempt/3` is a no-op today (WR-01 in 21.1-REVIEW.md) | ℹ️ Info    | Plan acknowledges this as "defensive future-proofing per D-21.1-02". `record_attempt/3` does not consume `opts[:audit]` and writes no audit row. Test 3 assertion-set still locks the structural invariant (no `:metadata, :applied` row on failure). Not a blocker — failure-path goal-truth (#4) holds for the right reason (no audit-write call site, not because forwarding is consumed). |
| `lib/relyra/metadata/refresh.ex`  | 208-228  | `Map.merge(defaults, audit_map)` allows nil overrides (WR-02 in 21.1-REVIEW.md)                  | ℹ️ Info    | Latent — no current caller passes `audit: %{actor: nil, ...}`. BulkActions injects only `:correlation_id` (bulk_actions.ex:18); LiveView callers pass non-empty strings. Would surface as `AuditEvent` validation failure, not silent corruption. Not a blocker for goal achievement.                                                                                       |
| `test/relyra/metadata/refresh_bulk_audit_test.exs` | 23, 75, 102-103 | 27-character connection IDs vs ULID-26 invariant (WR-03 in 21.1-REVIEW.md)                       | ℹ️ Info    | Tests bypass changesets via `@repo.insert!/1`, so `validate_format`/`validate_length` never fire. DB column has no length constraint. No runtime impact today; would mask any future `CHECK (length(connection_id) = 26)` migration. Not a blocker — goal truths verified empirically.                                                                                |
| `lib/relyra/metadata/refresh.ex`  | 230-236  | Pre-existing `details = ... details` rebind (IN-04 in 21.1-REVIEW.md)                            | ℹ️ Info    | Pre-existing minor style; not introduced by phase 21.1. Out of scope.                                                                                                                                                                                                                                                                                                       |

No blocker or warning anti-patterns. The 3 review-flagged warnings (WR-01, WR-02, WR-03) are documented quality concerns but do not falsify any of the 6 must-have truths.

### Human Verification Required

None. All 6 must-have truths are empirically verified by automated tests (3 new tests RED→GREEN, 46 targeted regression tests, 358-test full suite at seed 0 with only the pre-existing/documented ACS failure). Both compile lanes pass `--warnings-as-errors`. The phase makes no UI / visual / real-time / external-service changes.

### Gaps Summary

No gaps. The phase goal is achieved: bulk `:refresh_metadata` now produces N audit rows sharing one non-nil correlation_id and carrying the bulk audit map's actor/cause (empirically demonstrated by Test 1, which would FAIL without the `audit: resolve_audit(opts)` forwarding at refresh.ex:65). The Phase 20 D-39 batch-cohesion invariant is restored. v0.5 milestone audit BLOCKER INT-01 is empirically closed.

Three quality concerns from `21.1-REVIEW.md` (WR-01, WR-02, WR-03) are noted in the Anti-Patterns section as `Info` — they are documented in the SUMMARY's "Decisions Made" / plan acknowledgments, do not falsify any must-have truth, and are appropriately scoped as latent / defensive / cosmetic. They could be addressed in follow-up work but do not block phase 21.1's goal achievement or v0.5 milestone closure.

### Pre-existing Out-of-Scope Failures

- `Relyra.Phoenix.ACSControllerTest` POST `/:connection_id/acs` success — `KeyError :name_id`. Documented in `.planning/phases/21-scheduled-metadata-refresh/deferred-items.md`. Reproduced on parent commit `0842687`. This is the single failure in `mix test --warnings-as-errors --seed 0` → `358 tests, 1 failure`. Not introduced by phase 21.1.

---

_Verified: 2026-05-07T13:55:00Z_
_Verifier: Claude (gsd-verifier)_
