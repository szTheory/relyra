---
phase: 34-validationpipeline-wiring-enc-01-complete
plan: 04
type: execute
wave: 2
depends_on: ["34-02", "34-03"]
files_modified:
  - test/security/xml_enc_adversarial_test.exs
  - mix.exs
autonomous: true
requirements: [ENC-01]

must_haves:
  truths:
    - "A valid FakeIdP.encrypted_response logs in {:ok, ...} with identity fields present ONLY post-verify (SC#1 positive control, end-to-end through ValidationPipeline)"
    - "All 7 ENC-01 fixtures each return their exact pinned %Error{type:} (never bare {:error, _})"
    - "The read-before-verify fixture returns a verification-stage typed error AND no identity field in the result (strongest auth-bypass guard)"
    - "The new corpus is wired into mix ci.security as its own `cmd mix test ... --warnings-as-errors` line (hollow-gate rule)"
    - "ci_gate_integrity_test.exs confirms the new line is not hollow"
  artifacts:
    - path: "test/security/xml_enc_adversarial_test.exs"
      provides: "Positive control + 7-fixture pipeline-level ENC-01 corpus, exact %Error{type:} pins"
      min_lines: 120
    - path: "mix.exs"
      provides: "ci.security alias gains the new corpus as its own cmd mix test line"
      contains: "xml_enc_adversarial_test.exs"
  key_links:
    - from: "test/security/xml_enc_adversarial_test.exs"
      to: "Relyra.Protocol.ValidationPipeline.run/4 (or Relyra.consume_response/3)"
      via: "end-to-end pipeline run of FakeIdP.encrypted_response fixtures"
      pattern: "ValidationPipeline.run|consume_response"
    - from: "mix.exs ci.security alias"
      to: "cmd mix test test/security/xml_enc_adversarial_test.exs --warnings-as-errors"
      via: "own subprocess line (Phase-30 hollow-gate rule)"
      pattern: "xml_enc_adversarial_test.exs"
---

<objective>
Land the 7-fixture pipeline-level ENC-01 adversarial corpus in a new dedicated
`test/security/xml_enc_adversarial_test.exs`, driving end-to-end through `ValidationPipeline.run/4`
(or `Relyra.consume_response/3`) using the Plan-02 `FakeIdP.encrypt`/`encrypted_response` generator and
the Plan-03 `:decrypt_assertion` pre-stage. Wire the file into `mix ci.security` as its OWN
`cmd mix test ... --warnings-as-errors` line (D-07, hollow-gate rule).

Purpose: ENC-01 / SC#1 + SC#5 — prove the full decrypt -> re-parse -> verify path with a positive
control, prove every failure mode returns the correct typed error (opaque `:decryption_failed` for the
five crypto/policy failures, distinct `:ambiguous_assertion` for cleartext-injection), and prove the
read-before-verify ordering (no identity field before Signature.verify/4 — CVE-2025-54419 class).
Output: The new corpus file + the ci.security wiring; `mix ci.security` green including the hollow-gate
meta-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-validationpipeline-wiring-enc-01-complete/34-CONTEXT.md
@.planning/phases/34-validationpipeline-wiring-enc-01-complete/34-RESEARCH.md
@.planning/phases/34-validationpipeline-wiring-enc-01-complete/34-PATTERNS.md
@.planning/phases/34-validationpipeline-wiring-enc-01-complete/34-VALIDATION.md

<interfaces>
<!-- Extracted from codebase + prior plans. -->

From Plan 02 (FakeIdP) — the canonical encrypted generator:
  FakeIdP.encrypt(signed_assertion_xml, sp_pub_key, opts \\ [])  # opts override key_transport_uri /
    content_uri / tag length / iv / target pubkey for the adversarial variants
  FakeIdP.encrypted_response(opts)  # full <Response> binary carrying the EncryptedAssertion
  FakeIdP.keypair/0, FakeIdP.self_signed_cert_pem/0
  URI attrs available for adversarial variants: rsa-1_5, aes256-cbc, rsa-oaep-mgf1p, aes256-gcm.

From Plan 03 (pipeline) — the entry under test:
  Relyra.Protocol.ValidationPipeline.run(response_payload, request_intent, connection, opts)
    :: {:ok, login_result_map} | {:error, %Error{}}
  (or drive via Relyra.consume_response/3 — relyra.ex:151-170 — for the most end-to-end read-before-verify proof)
  Returns :ambiguous_assertion (pre-crypto) / :decryption_failed (opaque) per Plan 03.

setup that wires the SP private key so the resolver can unwrap (copy xml_enc_test.exs:12-26 verbatim):
  Application.put_env(:relyra, :sp_private_key_pem, <pem from FakeIdP.keypair()>) + on_exit delete.

Connection for the pipeline: cert_chain must include FakeIdP.self_signed_cert_pem() so Signature.verify
accepts the genuine inner signature (idp_certificates or cert_chain key on the connection map; see
validation_pipeline.ex cert_chain/2 at 208-211).

ci.security alias (mix.exs ~155-181) — add the new line immediately after the existing
  "cmd mix test test/security/xml_enc_test.exs --warnings-as-errors" (mix.exs:173 form). The block
  comment at ~159-166 explains the hollow-gate rule — do NOT collapse to a bare `test` step.
  The meta-gate test/security/ci_gate_integrity_test.exs (run first in the alias) enforces this.

7-fixture -> error-pin map (RESEARCH Validation Architecture / VALIDATION.md):
  1 wrong-key            -> %Error{type: :decryption_failed}
  2 truncated GCM tag    -> %Error{type: :decryption_failed}
  3 PKCS1v1.5 transport  -> %Error{type: :decryption_failed}
  4 AES-CBC content      -> %Error{type: :decryption_failed}
  5 cleartext-injection  -> %Error{type: :ambiguous_assertion}  (must fire BEFORE decrypt)
  6 malformed ciphertext -> %Error{type: :decryption_failed}
  7 read-before-verify   -> verification-stage typed error (:invalid_signature or :digest_mismatch)
                            AND name_id/attributes NOT in the result map
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Positive control + 7-fixture pipeline corpus (SC#1, SC#5, read-before-verify)</name>
  <files>test/security/xml_enc_adversarial_test.exs</files>
  <read_first>
    - test/security/xml/adversarial_crypto_test.exs (PRIMARY analog — whole file: module header, @moduletag, connection/0 helper, positive control at 56-67, exact %Error{type:} pins at 90-125; NEVER weaken this corpus, model on it)
    - test/security/xml_enc_test.exs (setup at 12-26 wiring :sp_private_key_pem; failure-path tests at 63-144 — wrong padding, wrong content URI, 15-byte tag, bad base64 — adapt these into full-Response fixtures)
    - lib/relyra/test_support/fake_idp.ex (Plan 02 output — encrypt/2, encrypted_response/2, the adversarial URI attrs, keypair/0, self_signed_cert_pem/0)
    - lib/relyra/protocol/validation_pipeline.ex (Plan 03 output — run/4 + :decrypt_assertion pre-stage; cert_chain/2 at 208-211 for the connection shape)
    - lib/relyra/test_support/xmldsig_signer.ex (signed_response/1 at 94-130, tamper_name_id at 88-92/125-127 — for the read-before-verify tampered fixture)
    - lib/relyra.ex (do_consume_response/3 at 151-170 — for the most end-to-end read-before-verify drive)
    - .planning/phases/34-validationpipeline-wiring-enc-01-complete/34-VALIDATION.md (7-fixture -> error-pin map + read-before-verify guard)
  </read_first>
  <action>
    Create `test/security/xml_enc_adversarial_test.exs` modeled on adversarial_crypto_test.exs:
    `use ExUnit.Case, async: true`, alias `Relyra.Error` + `Relyra.TestSupport.FakeIdP` +
    `Relyra.Protocol.ValidationPipeline`, a local `connection/0` helper whose cert_chain/idp_certificates
    includes `FakeIdP.self_signed_cert_pem()`. Copy the `setup` from xml_enc_test.exs:12-26 (configure
    `:sp_private_key_pem` from FakeIdP.keypair(), `on_exit` delete). Write a POSITIVE CONTROL (SC#1):
    `FakeIdP.encrypted_response(...)` -> `ValidationPipeline.run/4` -> assert `{:ok, login_result}` with
    `login_result` carrying the expected NameID/attributes (proving decrypt -> re-parse -> verify ->
    identity-read ordering). Then 7 fixtures, each a `describe`/`test`, pinning EXACT `%Error{type:}`
    (never bare `{:error, _}`):
      1 wrong-key: encrypt CEK against a THROWAWAY pubkey (not the SP key) -> `:decryption_failed`.
      2 truncated GCM tag: build content CipherValue with a 15-byte tag (< 16) -> `:decryption_failed`
        (auth-tag guard, algorithm_policy.ex:158).
      3 PKCS1v1.5 key transport: EncryptedKey EncryptionMethod = `xmlenc#rsa-1_5` (the @rsa_pkcs1_uri attr)
        -> `:decryption_failed` (hard-reject, no escape hatch).
      4 AES-CBC content: EncryptedData EncryptionMethod = `xmlenc#aes256-cbc` (the @aes256_cbc_uri attr)
        -> `:decryption_failed` (policy reject, default has no CBC hatch).
      5 cleartext-injection: a Response carrying BOTH a sibling cleartext `<Assertion>` AND an
        `<EncryptedAssertion>` -> `:ambiguous_assertion`. Build the encrypted half with ciphertext that
        WOULD `:decryption_failed`, so the `:ambiguous_assertion` result proves the ambiguity check fired
        BEFORE decrypt (SC#2 ordering, Pitfall 2).
      6 malformed ciphertext: invalid base64 / sub-28-byte CipherValue -> `:decryption_failed`.
      7 read-before-verify (STRONGEST GUARD): encrypt a genuinely-signed-then-TAMPERED assertion (tamper
        the NameID via XmldsigSigner.signed_response(tamper_name_id: ...) BEFORE encrypting) and assert
        `{:error, %Error{type: t}}` where t in [:invalid_signature, :digest_mismatch] AND the returned
        result carries NO `:name_id` / `:attributes` (the auth-bypass-class proof — CVE-2025-54419). Drive
        this one through `Relyra.consume_response/3` for the most end-to-end coverage if practical, else
        ValidationPipeline.run/4; assert no identity leaks into the error/result.
    Use `FakeIdP.encrypt`/`encrypted_response` opts (Plan 02) to construct the algorithm/key variants —
    do NOT hand-roll a second encryption recipe (single canonical generator). Run `mix format`.
  </action>
  <verify>
    <automated>mix test test/security/xml_enc_adversarial_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/security/xml_enc_adversarial_test.exs --warnings-as-errors` exits 0 with test count >= 8 (positive control + 7 fixtures).
    - The file pins exact `%Error{type: :decryption_failed}` for fixtures 1,2,3,4,6 and `%Error{type: :ambiguous_assertion}` for fixture 5 (grep the file for both atoms; no bare `{:error, _}` on fixture assertions).
    - The read-before-verify test asserts a verification-stage typed error (`:invalid_signature` or `:digest_mismatch`) AND asserts the result contains no `:name_id`/`:attributes`.
    - The cleartext-injection fixture's encrypted half uses ciphertext that would otherwise fail decrypt, proving `:ambiguous_assertion` fires first.
    - `mix format --check-formatted` exits 0.
  </acceptance_criteria>
  <done>The 7-fixture pipeline-level ENC-01 corpus plus a positive control is green; every failure mode pins its exact typed error; the read-before-verify guard proves no identity is read before Signature.verify/4.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the corpus into mix ci.security (hollow-gate rule) + verify meta-gate</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (the `ci.security` alias, ~152-181 — the per-suite `cmd mix test` lines; the block comment at ~159-166 explaining the hollow-gate rule; the existing `cmd mix test test/security/xml_enc_test.exs --warnings-as-errors` line at ~173)
    - test/security/ci_gate_integrity_test.exs (the meta-gate that enforces each suite is its own cmd line — run it to confirm the new line is not hollow)
    - .planning/phases/34-validationpipeline-wiring-enc-01-complete/34-RESEARCH.md (Pitfall 5 — hollow-gate regression)
  </read_first>
  <action>
    In `mix.exs`, add `"cmd mix test test/security/xml_enc_adversarial_test.exs --warnings-as-errors"` to
    the `ci.security` alias as its OWN line, immediately after the existing
    `"cmd mix test test/security/xml_enc_test.exs --warnings-as-errors"` line (D-07 / Pitfall 5 — the
    Phase-30 hollow-gate rule; NEVER collapse into a bare `test` step, NEVER fold into another `cmd mix
    test` line). Do not modify the block comment or any other alias step. Run `mix format`. Then run the
    meta-gate `mix test test/security/ci_gate_integrity_test.exs --warnings-as-errors` to confirm the new
    line registers as a non-hollow security suite.
  </action>
  <verify>
    <automated>mix test test/security/ci_gate_integrity_test.exs --warnings-as-errors && mix ci.security</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` ci.security alias contains a line equal to `"cmd mix test test/security/xml_enc_adversarial_test.exs --warnings-as-errors"` (its own line, not folded).
    - `mix test test/security/ci_gate_integrity_test.exs --warnings-as-errors` exits 0 (meta-gate confirms non-hollow).
    - `mix ci.security` exits 0 (the full security gate including the new corpus line is green).
    - `mix format --check-formatted` exits 0.
  </acceptance_criteria>
  <done>The ENC-01 corpus is wired into mix ci.security as its own subprocess line; the hollow-gate meta-gate confirms it actually runs; the full security gate is green (SC#5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adversarial fixture -> full SP pipeline | The corpus drives crafted encrypted Responses end-to-end through ValidationPipeline to prove every rejection path and the decrypt-then-verify ordering. |
| CI gate -> shipped build | A hollow corpus line would let an ENC-01 regression ship green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-12 | Elevation of Privilege / Spoofing | read-before-verify auth bypass (CVE-2025-54419, CRITICAL) | mitigate | The read-before-verify fixture drives end-to-end, asserts a verification-stage typed error AND that NO identity field is in the result — the strongest end-to-end proof of decrypt-then-verify ordering. |
| T-34-13 | Information Disclosure | error/timing oracle (HIGH) | mitigate | Fixtures 1,2,3,4,6 all pin the SINGLE opaque `:decryption_failed` — the corpus enforces the no-oracle property; a future distinct-atom regression flips a fixture red. |
| T-34-14 | Tampering | cleartext-injection ordering (CVE-2026-2092, HIGH) | mitigate | Fixture 5 uses would-fail-decrypt ciphertext so `:ambiguous_assertion` proves the pre-crypto reject fired first (SC#2). |
| T-34-15 | Tampering | hollow CI gate (Phase-30 class) | mitigate | New corpus is its own `cmd mix test` line; ci_gate_integrity_test.exs meta-gate confirms it's non-hollow (Pitfall 5). |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — zero new Hex deps (OTP stdlib + already-vendored saxy only); RESEARCH Package Legitimacy Audit: not applicable. No supply-chain checkpoint required. |
</threat_model>

<verification>
- `mix test test/security/xml_enc_adversarial_test.exs --warnings-as-errors` green (positive control + 7 fixtures).
- `mix test test/security/ci_gate_integrity_test.exs --warnings-as-errors` green (non-hollow line).
- `mix ci.security` green (full security gate, SC#5).
- `mix test --warnings-as-errors` green (no regression, SC#3).
- `mix format --check-formatted` exits 0.
</verification>

<success_criteria>
SC#1 (valid EncryptedAssertion -> login, identity readable only post-verify) and SC#5 (all 7 fixtures
wired into ci.security, each returns the correct typed error) satisfied. The read-before-verify fixture
proves the decrypt-then-verify ordering end-to-end (CVE-2025-54419 class closed). The corpus is a
permanent, non-hollow CI gate.
</success_criteria>

<output>
Create `.planning/phases/34-validationpipeline-wiring-enc-01-complete/34-04-SUMMARY.md` when done.
</output>
