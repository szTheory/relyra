---
phase: 34-validationpipeline-wiring-enc-01-complete
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/protocol/validation_pipeline.ex
autonomous: true
requirements: [ENC-01]

must_haves:
  truths:
    - "A Response with zero EncryptedAssertion elements flows through do_run/4 with NO re-parse and NO XMLEnc call (byte-identical no-op, D-02)"
    - "A Response with both a cleartext <Assertion> and an <EncryptedAssertion> returns {:error, %Error{type: :ambiguous_assertion}} BEFORE any XMLEnc.decrypt/3 call (D-03, SC#2)"
    - "A Response with >1 <EncryptedAssertion> returns :ambiguous_assertion (same exactly-one invariant)"
    - "A single EncryptedAssertion is decrypted, the plaintext is string-spliced into the Response binary, re-parsed via parse_safely/2, and handed to the unchanged do_run_validations/6"
    - "The splice locator matches BOTH an unprefixed <EncryptedAssertion> and a namespace-prefixed <saml:EncryptedAssertion>...</saml:EncryptedAssertion> (prefix-aware), and rejects/detects a Response carrying two <EncryptedAssertion> substrings (exactly-one-match guard, RESEARCH A1)"
    - "A decryption failure returns {:error, %Error{type: :decryption_failed}} (opaque, no oracle); NO identity field is read in the pre-stage"
  artifacts:
    - path: "lib/relyra/protocol/validation_pipeline.ex"
      provides: ":decrypt_assertion pre-stage in do_run/4 + detector + :ambiguous_assertion typed error"
      contains: ":decrypt_assertion"
  key_links:
    - from: "validation_pipeline.ex do_run/4"
      to: "Relyra.Security.XMLEnc.decrypt/3"
      via: "pass resolver MODULE atom + connection in opts; splice plaintext; re-parse_safely"
      pattern: "XMLEnc.decrypt"
    - from: "validation_pipeline.ex :decrypt_assertion detector"
      to: "parsed_doc.parse_tree (SaxyTree.Node root)"
      via: "prefix-agnostic find_first/find_all by local name"
      pattern: "EncryptedAssertion"
    - from: "validation_pipeline.ex pre-stage"
      to: "Relyra.Security.XML.PureBeam.parse_safely/2"
      via: "re-parse recomposed binary through the SAME hardened seam (one parse path)"
      pattern: "PureBeam.parse_safely"
---

<objective>
Insert the `:decrypt_assertion` pre-stage into `ValidationPipeline.do_run/4` (D-01): detect
`EncryptedAssertion` from the outer parse tree, reject the cleartext+encrypted ambiguity BEFORE any
crypto (D-03), decrypt a single `EncryptedAssertion` via the unchanged Phase-33 `XMLEnc.decrypt/3`,
string-splice the plaintext into the Response binary, re-parse through the SAME `parse_safely/2` seam,
and hand the re-parsed `parsed_doc` to the unchanged `do_run_validations/6`. The no-op path stays
byte-identical (D-02).

Purpose: ENC-01 — wire decrypt-then-reparse so decrypted bytes pass `parse_safely/2` AND
`Signature.do_verify/4` before any identity field is read (CVE-2025-54419 class). This is the security
core of the phase.
Output: An extended `do_run/4` with a private `:decrypt_assertion` pre-stage, a tree-walk detector,
and the new `:ambiguous_assertion` typed error — all returning the existing three-tuple contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-validationpipeline-wiring-enc-01-complete/34-CONTEXT.md
@.planning/phases/34-validationpipeline-wiring-enc-01-complete/34-RESEARCH.md
@.planning/phases/34-validationpipeline-wiring-enc-01-complete/34-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

do_run/4 host site (validation_pipeline.ex:62-76) — the pre-stage inserts between the {:ok, parsed_doc}
match (line 67) and the do_run_validations/6 call (line 68). do_run_validations/6 is UNCHANGED (D-02).

Three-tuple contract every do_run/4 exit MUST preserve (lines 69-70, 74):
  {:ok, login_result, assertion_count} | {:error, %Error{}, assertion_count}
  assertion_count/1 (234-238) reads :signed_candidates off the parsed_doc in scope (outer doc for the
  ambiguity/decrypt-fail case since no re-parse happened; re-parsed doc for the success case).

Helpers already in this file:
  - parse_opts/1 (line 206) :: Keyword.take(opts, [:max_bytes]) — reuse for the re-parse
  - cert_chain/2 (208) — unchanged
  - Error alias already present (line 4)

Re-parse seam (CLAUDE.md invariant #2 — the ONLY parse path):
  Relyra.Security.XML.PureBeam.parse_safely(recomposed, parse_opts(opts)) :: {:ok, parsed_doc} | {:error, %Error{}}
  parsed_doc exposes :parse_tree (the SaxyTree.Node root; pure_beam.ex:257)

Detector (RESEARCH code example + PATTERNS — copy the prefix-agnostic shape, do NOT add a parser):
  find_first(parse_tree, "EncryptedAssertion") nil -> :none (D-02 no-op);
  non-nil AND find_first(parse_tree, "Assertion") non-nil -> :ambiguous;
  length(find_all(parse_tree, "EncryptedAssertion")) > 1 -> :ambiguous;
  else {:single, node}.
  Model on pure_beam.ex:584-590 (find_first) + 614-618 (find_all), or xml_enc.ex:170-176 (map form).

XMLEnc.decrypt/3 (CONSUMED UNCHANGED, xml_enc.ex:11): decrypt(enc_bytes, key_resolver_MODULE, opts)
  :: {:ok, binary()} | :decryption_failed
  - PASS THE MODULE atom (RESEARCH Pattern 3): Keyword.get(opts, :key_resolver, Relyra.KeyResolver.Default)
    — do NOT call KeyResolver.resolve/2 (decrypt/3 re-resolves via apply at xml_enc.ex:107).
  - THREAD connection: Keyword.put(opts, :connection, connection) (xml_enc.ex:16 reads connection from opts).

Splice (RESEARCH Pattern 1): replace the <EncryptedAssertion>...</EncryptedAssertion> substring in the
ORIGINAL response_payload with the decrypted plaintext, then re-parse_safely. String.replace(..., global: false).
EncryptedAssertion cannot nest another EncryptedAssertion (XML-Enc) so a single substring match is
unambiguous; ASSUMPTION A1 — the splice locator MUST be PREFIX-AWARE (match an unprefixed
<EncryptedAssertion>...</EncryptedAssertion> AND a namespace-prefixed
<saml:EncryptedAssertion>...</saml:EncryptedAssertion>) and MUST guard exactly-one-match (reject/detect
two EncryptedAssertion substrings rather than silently splicing the first). connection is already a
do_run/4 parameter.

Error construction (this file already uses Error.new(type, message, %{}) at 121-125, 145-153, 166-173):
  Error.new(:ambiguous_assertion, "Response contains both cleartext and encrypted assertions", %{})
  Error.new(:decryption_failed, "Encrypted assertion could not be decrypted", %{})
  Precedent for the new atom: pure_beam.ex:550-554 (:ambiguous_signed_node).

Identity-read prohibition (CLAUDE.md #4 / Pitfall 3): the pre-stage returns ONLY the re-parsed
parsed_doc. ALL identity reads stay in login_result/5 (181-204), reached only after Signature.verify/4
succeeds inside do_run_validations/6's with chain (81-82). NO Map.get(parsed_doc, :name_id|:attributes)
inside the pre-stage.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add :decrypt_assertion detector + pre-stage to do_run/4</name>
  <files>lib/relyra/protocol/validation_pipeline.ex</files>
  <read_first>
    - lib/relyra/protocol/validation_pipeline.ex (whole file — do_run/4 at 62-76, do_run_validations/6 at 78-111 UNCHANGED, parse_opts/1 at 206, assertion_count/1 at 234-238, login_result/5 at 181-204, Error alias at 4)
    - lib/relyra/security/xml_enc.ex (lines 11-30, 104-113, 117-176 — decrypt/3 contract, resolve_key via apply, find_first map form to model the detector)
    - lib/relyra/security/xml/pure_beam.ex (lines 39-58 parse_safely, 250-269 :parse_tree, 540-554 :ambiguous_signed_node precedent, 584-618 find_first/find_all Node-typed)
    - lib/relyra/key_resolver.ex (resolve/2 dispatch at 14-27 — shows the Keyword.get default; PASS THE MODULE not the result)
    - lib/relyra.ex (do_consume_response/3 at 151-170 — :key_resolver + connection flow into ValidationPipeline.run via consume_opts at 161)
    - .planning/phases/34-validationpipeline-wiring-enc-01-complete/34-RESEARCH.md (Architecture Pattern 1 + Pattern 3 + the two code examples + Pitfalls 1,2,3 + Assumptions Log A1)
    - .planning/phases/34-validationpipeline-wiring-enc-01-complete/34-PATTERNS.md (validation_pipeline.ex section — host site, three-tuple contract, detector, error construction, identity-read prohibition)
  </read_first>
  <behavior>
    - detect_encrypted(parse_tree): nil EncryptedAssertion -> :none; EncryptedAssertion + sibling cleartext Assertion -> :ambiguous; >1 EncryptedAssertion -> :ambiguous; exactly one -> {:single, node}.
    - :none path: original parsed_doc flows to do_run_validations/6 unchanged — NO parse_safely re-call, NO XMLEnc.decrypt invoked. PROOF (deterministic, dependency-free — no Mox/:meck, none added): pass a `:key_resolver` test module (e.g. `RaiseIfInvoked`) whose `resolve/1` RAISES if called; assert that running a non-encrypted Response through the pipeline with that resolver does NOT raise — the absence of the raise proves the :none branch never reached XMLEnc.decrypt/3 (which dispatches the resolver via apply). Complementary check: the protocol regression suite stays green AND the detector returns :none.
    - splice locator is PREFIX-AWARE: it matches the EncryptedAssertion substring whether the element is written <EncryptedAssertion>...</EncryptedAssertion> OR <saml:EncryptedAssertion>...</saml:EncryptedAssertion> (or any prefix), and it enforces an EXACTLY-ONE-MATCH guard — a Response binary containing two EncryptedAssertion substrings is rejected/detected (consistent with detect_encrypted's >1 -> :ambiguous), never silently splicing the first match (RESEARCH A1).
    - :ambiguous returns {:error, %Error{type: :ambiguous_assertion}, assertion_count} and is produced WITHOUT calling XMLEnc.decrypt/3 (ordering: detect -> reject -> only then decrypt).
    - {:single, _}: XMLEnc.decrypt/3 is called with the resolver MODULE and connection in opts; on {:ok, plaintext} the plaintext replaces the EncryptedAssertion substring (global: false) and parse_safely/2 re-parses; the re-parsed parsed_doc goes to do_run_validations/6.
    - :decryption_failed returns {:error, %Error{type: :decryption_failed}, assertion_count}.
    - The pre-stage reads NO identity field (no :name_id / :attributes access).
  </behavior>
  <action>
    Insert a private `:decrypt_assertion` pre-stage between the `{:ok, parsed_doc}` match (line 67) and
    the `do_run_validations/6` call (line 68) in `do_run/4` (D-01). Add a private `detect_encrypted/1`
    over `parsed_doc.parse_tree` using a prefix-agnostic `find_first`/`find_all` by local name (copy the
    shape from pure_beam.ex:584-618 or xml_enc.ex:170-176 — do NOT add a second parser, CLAUDE.md #2).
    Detector returns `:none | :ambiguous | {:single, node}` per the RESEARCH code example (D-02/D-03;
    treat >1 EncryptedAssertion as `:ambiguous` too — RESEARCH open-question 1, same exactly-one invariant).
    Add `decrypt_assertion/4` (response_payload, parsed_doc, connection, opts):
      - `:none` -> `{:ok, parsed_doc}` (D-02 byte-identical no-op — NO re-parse, NO XMLEnc call).
      - `:ambiguous` -> `{:error, Error.new(:ambiguous_assertion, "Response contains both cleartext and
        encrypted assertions", %{})}` — produced BEFORE any decrypt call (D-03, SC#2, Pitfall 2).
      - `{:single, node}` -> resolve module via `Keyword.get(opts, :key_resolver, Relyra.KeyResolver.Default)`
        (PASS THE MODULE — RESEARCH Pattern 3); locate the EncryptedAssertion substring in
        `response_payload` with a PREFIX-AWARE locator that matches both `<EncryptedAssertion>...
        </EncryptedAssertion>` and `<saml:EncryptedAssertion>...</saml:EncryptedAssertion>` (any prefix),
        and GUARD exactly-one-match — if two EncryptedAssertion substrings are present, return
        `:ambiguous_assertion` (consistent with detect_encrypted's >1 branch) rather than splicing the
        first (RESEARCH A1). Then call
        `Relyra.Security.XMLEnc.decrypt(enc_bytes, resolver, Keyword.put(opts, :connection, connection))`.
        On `{:ok, plaintext}`: `String.replace(response_payload, enc_bytes, plaintext, global: false)` then
        `Relyra.Security.XML.PureBeam.parse_safely(recomposed, parse_opts(opts))` (re-parse seam,
        invariant #2/#3 — DTD/entity/size guards re-applied for free). On `:decryption_failed`:
        `{:error, Error.new(:decryption_failed, "Encrypted assertion could not be decrypted", %{})}` (opaque).
    Rewire `do_run/4` so the `{:ok, parsed_doc}` arm calls `decrypt_assertion/4` first, then dispatches:
    on `{:ok, effective_doc}` run `do_run_validations(effective_doc, ...)` and wrap with
    `assertion_count(effective_doc)`; on `{:error, %Error{} = err}` return `{:error, err,
    assertion_count(parsed_doc)}` (outer doc for the count — no re-parse occurred). Preserve the exact
    three-tuple contract; do NOT touch `do_run_validations/6` (D-02). The pre-stage must read NO identity
    field (Pitfall 3 / CLAUDE.md #4). Run `mix format`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/relyra/protocol/ --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/relyra/protocol/validation_pipeline.ex` source contains `:decrypt_assertion` (or `decrypt_assertion`), `:ambiguous_assertion`, `:decryption_failed`, `XMLEnc.decrypt`, and a second `PureBeam.parse_safely` call.
    - NO-OP PROVEN WITHOUT A MOCK: a test passing a `:key_resolver` module whose `resolve/1` RAISES (a `RaiseIfInvoked`-style module, zero new deps) shows that running an UNENCRYPTED Response through the pipeline does NOT raise — proving the :none branch never reached XMLEnc.decrypt/3. The complementary "regression suite stays green + detector returns :none" check also holds (SC#3).
    - PREFIX-AWARE + EXACTLY-ONE SPLICE: the prefix-aware locator (matches `<saml:EncryptedAssertion>` as well as `<EncryptedAssertion>`) and the exactly-one-match guard (two substrings -> :ambiguous_assertion, never silent-splice-first) are exercised by the Task 2 fixtures and asserted there (RESEARCH A1).
    - `do_run_validations/6` body is byte-unchanged from the pre-task version (diff shows only do_run/4 + new private fns added).
    - Source does NOT add any `Map.get(parsed_doc, :name_id)` or `:attributes` read inside the pre-stage.
    - The resolver is obtained as the MODULE via `Keyword.get(opts, :key_resolver, Relyra.KeyResolver.Default)`; source does NOT call `KeyResolver.resolve` in the pre-stage.
    - `mix compile --warnings-as-errors` exits 0; `mix test test/relyra/protocol/ --warnings-as-errors` green (no-op regression intact, SC#3).
    - `mix format --check-formatted` exits 0.
  </acceptance_criteria>
  <done>do_run/4 has a :decrypt_assertion pre-stage that no-ops on unencrypted Responses (proven via a raise-if-invoked resolver), rejects ambiguity pre-crypto with :ambiguous_assertion, decrypts-then-reparses a single EncryptedAssertion via a prefix-aware exactly-one-match splice; three-tuple contract preserved; do_run_validations/6 untouched; protocol regression green.</done>
</task>

<task type="auto">
  <name>Task 2: Unit-level guards — no-op (raise-if-invoked), ambiguity-before-crypto, prefix-aware splice, decrypt-fail typing</name>
  <files>lib/relyra/protocol/validation_pipeline.ex</files>
  <read_first>
    - lib/relyra/protocol/validation_pipeline.ex (the extended file from Task 1)
    - test/security/xml/adversarial_crypto_test.exs (exact-error-pin pattern at 90-125; connection/0 helper; module header conventions)
    - lib/relyra/test_support/fake_idp.ex (sign/2, build_response — to produce the unencrypted positive/no-op fixtures; NOTE: the encrypted fixtures come from Plan 02's FakeIdP.encrypt/encrypted_response and are exercised in Plan 04 — keep THIS task's coverage to the no-op + ambiguity-ordering + prefix/exactly-one splice guards that need no real encrypted fixture)
  </read_first>
  <action>
    Add focused unit coverage for the pre-stage's branch logic that does NOT require a real encrypted
    fixture (the full encrypted corpus is Plan 04). Put these in an existing pipeline test file (e.g.
    `test/relyra/protocol/validation_pipeline_test.exs` if present) or a small new
    `test/relyra/protocol/decrypt_assertion_test.exs`. Cover:
    (1) NO-OP (deterministic, dependency-free PROOF) — define a tiny in-test `:key_resolver` module
        (e.g. `defmodule RaiseIfInvoked do def resolve(_), do: raise "decrypt path reached" end`, no Mox /
        no :meck — zero new deps) and run an unencrypted `FakeIdP.sign(...)` Response through
        `ValidationPipeline.run/4` with `key_resolver: RaiseIfInvoked`; assert it does NOT raise and still
        validates exactly as before (SC#3 at the unit level). The non-raise proves the :none branch never
        reached XMLEnc.decrypt/3.
    (2) AMBIGUITY — construct a Response binary carrying BOTH a sibling cleartext `<Assertion>` and an
        `<EncryptedAssertion>` (a hand-built XML string is fine — it must only reach the detector, which
        rejects before decrypt) and assert `{:error, %Error{type: :ambiguous_assertion}}` from
        `ValidationPipeline.run/4`. Use garbage ciphertext that WOULD `:decryption_failed`, so seeing
        `:ambiguous_assertion` proves the ambiguity check fired first (Pitfall 2 / SC#2).
    (3) >1 EncryptedAssertion -> `:ambiguous_assertion`.
    (4) PREFIX-AWARE SPLICE FIXTURE (RESEARCH A1) — build a hand-crafted Response whose single encrypted
        element uses a NAMESPACE PREFIX (`<saml:EncryptedAssertion>...</saml:EncryptedAssertion>`, since all
        FakeIdP fixtures emit the UNPREFIXED tag and would leave the prefixed path unverified) and assert
        it travels the splice/re-parse path (the prefixed locator matches it — drive it far enough to prove
        the locator found the prefixed substring; a decrypt-failing or trivially-spliceable body is fine, the
        point is the prefix-aware match, not a successful decrypt).
    (5) EXACTLY-ONE-MATCH GUARD (RESEARCH A1) — build a Response carrying TWO `<EncryptedAssertion>`
        substrings and assert `{:error, %Error{type: :ambiguous_assertion}}` (the guard rejects/detects two
        rather than silently splicing the first).
    Pin EXACT `%Error{type:}` on every error assertion (never bare `{:error, _}` — copy
    adversarial_crypto_test.exs:90-125).
  </action>
  <verify>
    <automated>mix test test/relyra/protocol/ --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/relyra/protocol/ --warnings-as-errors` exits 0 with the new tests counted.
    - The no-op test uses a raise-if-invoked `:key_resolver` module (no Mox/:meck) and proves the unencrypted Response validates without raising (SC#3, no-mock proof).
    - The ambiguity test pins `{:error, %Error{type: :ambiguous_assertion}}` and uses a fixture whose ciphertext would otherwise `:decryption_failed`, proving ordering (SC#2).
    - A prefix-aware fixture using `<saml:EncryptedAssertion>...</saml:EncryptedAssertion>` exercises the splice/re-parse locator (proves the prefixed path, not just the unprefixed FakeIdP tag).
    - A two-`<EncryptedAssertion>` fixture pins `{:error, %Error{type: :ambiguous_assertion}}`, proving the exactly-one-match guard (RESEARCH A1).
    - `mix format --check-formatted` exits 0.
  </acceptance_criteria>
  <done>The pre-stage's no-op (raise-if-invoked proof), ambiguity-before-crypto, prefix-aware splice, and exactly-one-match (two-encrypted) branches are pinned with exact typed errors; SC#2 ordering and SC#3 no-op proven at the unit level without any mock dependency (the full encrypted corpus lands in Plan 04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IdP Response binary -> SP pipeline | Untrusted, possibly-encrypted Response crosses into do_run/4. Decrypt + re-parse + verify all happen before any identity read. |
| decrypted plaintext -> re-parse seam | Decrypted bytes are still untrusted until Signature.verify/4 passes; they re-enter parse_safely/2 with full pre-parse guards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Elevation of Privilege / Spoofing | decrypt-before-verify auth bypass (CVE-2025-54419 class, CRITICAL) | mitigate | The pre-stage returns ONLY the re-parsed parsed_doc; ALL identity reads stay in login_result/5, reached only after Signature.verify/4 in do_run_validations/6. Acceptance criteria forbid any :name_id/:attributes read in the pre-stage. End-to-end proof is Plan 04's read-before-verify fixture. (CLAUDE.md #4.) |
| T-34-08 | Tampering / Spoofing | cleartext+encrypted ambiguity injection (CVE-2026-2092 / Keycloak, HIGH) | mitigate | detect_encrypted/1 rejects with :ambiguous_assertion BEFORE any XMLEnc.decrypt/3 call (D-03, Pitfall 2); >1 EncryptedAssertion also rejected (detector branch + prefix-aware exactly-one-match splice guard, RESEARCH A1). Task 2 proves ordering with a would-fail-decrypt fixture and pins the two-encrypted reject. |
| T-34-09 | Information Disclosure | padding/error oracle on decrypt failure (HIGH) | mitigate | All decrypt/policy failures collapse to the opaque `:decryption_failed` atom (XMLEnc already does this; the pipeline returns the same opaque type). :ambiguous_assertion is a PRE-crypto structural reject (no oracle) and stays distinct (D-03). |
| T-34-10 | Tampering | namespace-context corruption breaking signature digest during recompose (HIGH) | mitigate | String-splice (NOT tree-rebuild) preserves the IdP-signed bytes; the verifier re-canonicalizes the re-parsed node at verify time (signature.ex:315,347). No canonical tree serializer is invented. Self-contained-ns assertions (Plan 02) keep canonical bytes stable. The prefix-aware locator handles namespace-prefixed EncryptedAssertion tags so the correct substring is spliced. (RESEARCH Pattern 1 + A1.) |
| T-34-11 | Tampering | second XML parse path / parser differential | mitigate | The recompose re-uses parse_safely/2 (the ONLY seam) — no new parser; DTD/entity/size pre-parse guards re-applied to the decrypted plaintext for free (CLAUDE.md #2/#3). |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/relyra/protocol/ --warnings-as-errors` green (no-op regression + new pre-stage guards: raise-if-invoked no-op, ambiguity ordering, prefix-aware splice, exactly-one-match).
- `mix format --check-formatted` exits 0.
- Wave-merge regression: `mix test --warnings-as-errors` stays fully green (SC#3 — frozen Phase-29 corpus + all signed-Response tests unchanged).
</verification>

<success_criteria>
SC#2 (ambiguity rejected before crypto) and SC#3 (no-op path structurally unchanged) satisfied;
the decrypt-then-reparse wiring for SC#1 is in place (end-to-end SC#1 + the 7-fixture corpus close
in Plan 04). The splice locator is prefix-aware and exactly-one-match-guarded (RESEARCH A1). Identity
is never read before Signature.verify/4 (CVE-2025-54419 class closed).
</success_criteria>

<output>
Create `.planning/phases/34-validationpipeline-wiring-enc-01-complete/34-03-SUMMARY.md` when done.
</output>
