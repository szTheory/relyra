---
phase: 36-generic-saml-runbook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/getting_started.md
  - guides/recipes/generic_saml.md
autonomous: true
requirements:
  - DOCS-02
tags:
  - phase-36
  - docs-02
  - docs
  - generic-saml
  - runbook

must_haves:
  truths:
    - "The repo exposes one explicit custom/generic-SAML route that points to guides/recipes/generic_saml.md from both README.md and guides/getting_started.md."
    - "guides/recipes/generic_saml.md explains Relyra's actual SP/runtime seams in operator language, not abstract SAML-spec prose detached from the code."
    - "The runbook includes the core DOCS-02 sections needed before any vendor-specific decoder tables: support posture, ownership boundaries, SP metadata field reference, IdP metadata import checklist, NameID guidance, and signing/encryption triggers."
    - "The runbook explicitly keeps batteries-included support limited to shipped presets and presents generic SAML as the fallback path."
  artifacts:
    - path: "guides/recipes/generic_saml.md"
      provides: "Authoritative generic/custom-SAML Day-1 runbook"
      contains: "## SP metadata field reference"
    - path: "README.md"
      provides: "Top-level routing into the generic/custom-SAML path"
      contains: "guides/recipes/generic_saml.md"
    - path: "guides/getting_started.md"
      provides: "Day-1 branch to the generic/custom-SAML fallback path"
      contains: "guides/recipes/generic_saml.md"
  key_links:
    - from: "README.md"
      to: "guides/recipes/generic_saml.md"
      via: "Custom SAML routing section"
      pattern: "guides/recipes/generic_saml\\.md"
    - from: "guides/getting_started.md"
      to: "guides/recipes/generic_saml.md"
      via: "Custom SAML fallback branch"
      pattern: "guides/recipes/generic_saml\\.md"
    - from: "guides/recipes/generic_saml.md"
      to: "lib/relyra/protocol/metadata.ex"
      via: "Plain-English explanation of real SP metadata output"
      pattern: "AuthnRequestsSigned|KeyDescriptor|acs_url|sp_entity_id"
---

<objective>
Create the canonical generic/custom-SAML route and the core generic runbook skeleton.

Purpose: turn the repo's existing "custom SAML" fallback into one authoritative operator path
without expanding support claims. The output is a new `guides/recipes/generic_saml.md` plus
explicit routing links from `README.md` and `guides/getting_started.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-generic-saml-runbook/36-CONTEXT.md
@.planning/phases/36-generic-saml-runbook/36-RESEARCH.md
@README.md
@guides/getting_started.md
@guides/recipes/okta.md
@guides/recipes/entra.md
@guides/recipes/google_workspace.md
@guides/recipes/adfs.md

<interfaces>
From lib/relyra/connection.ex:
```elixir
defstruct [
  :idp_entity_id,
  :sp_entity_id,
  :acs_url,
  :idp_sso_url,
  :idp_certificates,
  :name_id_format,
  sign_authn_requests: false,
  signed_request_encoding: nil
]
```

From lib/relyra/protocol/metadata.ex:
```elixir
signing_descriptor = signing_key_descriptor(sign_authn_requests, signing_cert_b64)
authn_requests_attr = authn_requests_signed_attr(sign_authn_requests)
<md:KeyDescriptor use="encryption">...</md:KeyDescriptor>
<md:AssertionConsumerService ... Location="#{acs_url}" ... />
```

From lib/relyra/protocol/authn_request.ex:
```elixir
@default_name_id_format "urn:oasis:names:tc:SAML:1.1:nameid-format:unspecified"
```

From lib/relyra.ex:
```elixir
sign = read_field(connection, :sign_authn_requests) == true
encoding = read_field(connection, :signed_request_encoding) || :rfc3986_upper
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route custom/generic-SAML adopters to the new runbook</name>
  <files>README.md, guides/getting_started.md</files>
  <action>Update the existing custom-SAML language in `README.md` and `guides/getting_started.md` so both files explicitly point non-preset adopters at `guides/recipes/generic_saml.md`. Preserve the existing support taxonomy from Phases 27 and 31: batteries-included remains only Okta / Microsoft Entra ID / Google Workspace, ADFS keeps its specialized recipe, and generic/custom SAML is the fallback path for everything else. Do not let the routing text imply that IBM, CyberArk, Oracle, Ping, SiteMinder, ADFS custom, or Shibboleth now ship preset-backed support.</action>
  <verify><automated>rg -n "guides/recipes/generic_saml.md|Custom SAML|generic SAML" README.md guides/getting_started.md && rg -n "Okta|Microsoft Entra ID|Google Workspace" README.md guides/getting_started.md</automated></verify>
  <done>The repo has one explicit generic/custom-SAML route, and the support taxonomy stays narrow and truthful.</done>
</task>

<task type="auto">
  <name>Task 2: Create the core generic runbook with real Relyra seams</name>
  <files>guides/recipes/generic_saml.md</files>
  <action>Create `guides/recipes/generic_saml.md` as the authoritative fallback runbook for non-preset or partially-supported IdPs. Mirror the operator tone and section discipline of the existing recipe docs, but keep the spine reference-heavy rather than vendor-specific. The file must include these exact H2 sections: `## Overview`, `## Relyra owns / IdP owns / Host owns`, `## SP metadata field reference`, `## IdP metadata import checklist`, `## NameID format decision guide`, and `## When to enable signing and encryption`. The SP metadata section must explain the real Relyra seams and outputs: `sp_entity_id`, `acs_url`, `AuthnRequestsSigned`, signing/encryption `KeyDescriptor`, encryption algorithm advertisement, and the AuthnRequest NameID policy. The import checklist must stay grounded in what `Relyra.Metadata.Import` / metadata apply actually consume today: IdP entity ID, SSO URL, signing certs, metadata URL/import flow, and feature toggles implied by metadata or operator policy.</action>
  <verify><automated>rg -n "^## Overview$|^## Relyra owns / IdP owns / Host owns$|^## SP metadata field reference$|^## IdP metadata import checklist$|^## NameID format decision guide$|^## When to enable signing and encryption$" guides/recipes/generic_saml.md && rg -n "sp_entity_id|acs_url|AuthnRequestsSigned|KeyDescriptor|idp_entity_id|idp_sso_url|sign_authn_requests|EncryptedAssertion" guides/recipes/generic_saml.md</automated></verify>
  <done>The generic runbook exists, is operator-first, and explains the actual Relyra configuration and metadata seams required for a safe first integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> operator config | The generic runbook shapes how operators configure unfamiliar IdPs |
| support claims -> adopter expectation | Generic-path wording can accidentally read as preset-backed vendor support |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | S | README / Getting Started routing | mitigate | Route non-preset adopters to one explicit generic path while keeping batteries-included claims limited to shipped presets |
| T-36-02 | T | generic runbook field reference | mitigate | Derive every metadata/config explanation from concrete code seams (`Connection`, `Protocol.Metadata`, `AuthnRequest`, `start_login/3`) instead of free-floating SAML prose |
| T-36-03 | I | support posture language | mitigate | Keep ADFS as a specialized recipe and state that other vendors are decoder-table guidance, not preset-backed support |
</threat_model>

<verification>
- `rg -n "guides/recipes/generic_saml.md|Custom SAML|generic SAML" README.md guides/getting_started.md`
- `rg -n "Okta|Microsoft Entra ID|Google Workspace" README.md guides/getting_started.md`
- `rg -n "^## Overview$|^## Relyra owns / IdP owns / Host owns$|^## SP metadata field reference$|^## IdP metadata import checklist$|^## NameID format decision guide$|^## When to enable signing and encryption$" guides/recipes/generic_saml.md`
- `rg -n "sp_entity_id|acs_url|AuthnRequestsSigned|KeyDescriptor|idp_entity_id|idp_sso_url|sign_authn_requests|EncryptedAssertion" guides/recipes/generic_saml.md`
</verification>

<success_criteria>
- The repo exposes one authoritative generic/custom-SAML route.
- The generic runbook explains real Relyra metadata and config seams in operator language.
- Support claims remain narrow and do not imply new shipped presets.
</success_criteria>

<output>
After completion, create `.planning/phases/36-generic-saml-runbook/36-01-SUMMARY.md`
</output>
