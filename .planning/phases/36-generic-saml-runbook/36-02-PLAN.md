---
phase: 36-generic-saml-runbook
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - guides/recipes/generic_saml.md
  - mix.exs
autonomous: true
requirements:
  - DOCS-02
tags:
  - phase-36
  - docs-02
  - docs
  - generic-saml
  - ci

must_haves:
  truths:
    - "guides/recipes/generic_saml.md contains the full DOCS-02 payload: vendor decoder tables, ADFS and Shibboleth subsections, minimum-safe checklist, debugging flow, and certificate rotation procedure."
    - "Vendor decoder content is clearly framed as dated/version-scoped operator translation help, not a claim of preset-backed support."
    - "The debugging and certificate-rotation sections describe Relyra's actual staged metadata/cert workflows and strict defaults, not speculative future tooling."
    - "ci.docs fails if guides/recipes/generic_saml.md is missing."
  artifacts:
    - path: "guides/recipes/generic_saml.md"
      provides: "Complete DOCS-02 operator guide for non-preset IdPs"
      contains: "## Minimum-safe checklist"
    - path: "mix.exs"
      provides: "Docs-lane presence gate for the generic runbook"
      contains: "guides/recipes/generic_saml.md"
  key_links:
    - from: "guides/recipes/generic_saml.md"
      to: "guides/recipes/adfs.md"
      via: "ADFS-specific cross-link from the generic path"
      pattern: "guides/recipes/adfs\\.md"
    - from: "guides/recipes/generic_saml.md"
      to: "lib/relyra/metadata/import.ex"
      via: "IdP metadata import and rotation guidance grounded in current import/apply workflow"
      pattern: "metadata|certificate|refresh|rotation"
    - from: "mix.exs"
      to: "guides/recipes/generic_saml.md"
      via: "ci.docs presence gate"
      pattern: "cmd test -f guides/recipes/generic_saml\\.md"
---

<objective>
Complete the generic runbook with vendor decoder tables and the durable operator-safety sections,
then wire the docs lane so the file cannot disappear silently.

Purpose: close the full DOCS-02 acceptance surface and make the guide maintainable.
Output: one finished `guides/recipes/generic_saml.md` plus a `ci.docs` presence check in `mix.exs`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/FEATURES.md
@.planning/research/SUMMARY.md
@.planning/phases/36-generic-saml-runbook/36-CONTEXT.md
@.planning/phases/36-generic-saml-runbook/36-RESEARCH.md
@guides/recipes/generic_saml.md
@guides/recipes/adfs.md
@guides/recipes/okta.md
@guides/recipes/entra.md
@guides/recipes/google_workspace.md
@mix.exs

<interfaces>
From lib/relyra/metadata/import.ex:
```elixir
Candidate.new(%{
  idp_entity_id: Map.fetch!(parsed, :entity_id),
  idp_sso_url: service_url,
  certificates: certificates
})
```

From lib/relyra/ecto/metadata_apply.ex:
```elixir
append_metadata_audit(...)
maybe_reset_health_state_on_apply(...)
```

From lib/relyra/diagnostic/allow_list.ex:
```elixir
def export_connection(connection) do
  %{connection_id: ..., sp_entity_id: ..., acs_url: ..., idp_entity_id: ..., idp_sso_url: ...}
end
```

From mix.exs:
```elixir
"ci.docs": [
  "cmd test -f guides/batteries_included.md",
  "cmd test -f BATTERIES_INCLUDED.md",
  "cmd test -f guides/recipes/adfs.md"
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vendor decoder tables and provider-specific footgun notes</name>
  <files>guides/recipes/generic_saml.md</files>
  <action>Extend `guides/recipes/generic_saml.md` with the vendor-translation payload that makes this guide uniquely useful. Add a `## Vendor decoder tables` section covering IBM Security Verify, CyberArk, Oracle Access Manager, PingFederate, and CA SiteMinder. For each vendor, map Relyra's runtime concepts to the vendor vocabulary and common claim names for email, given name, surname, groups, and NameID defaults. Date or version-scope the tables in the file so label drift is explicit. Also add `## ADFS-specific notes` and `## Shibboleth-specific notes` sections: ADFS should cross-link `guides/recipes/adfs.md` and focus on why the generic path differs; Shibboleth should call out the `AuthnRequestsSigned` / IdP-initiated footgun and encryption posture without pretending a preset exists.</action>
  <verify><automated>rg -n "^## Vendor decoder tables$|^## ADFS-specific notes$|^## Shibboleth-specific notes$" guides/recipes/generic_saml.md && rg -n "IBM Security Verify|CyberArk|Oracle Access Manager|PingFederate|CA SiteMinder|guides/recipes/adfs.md|AuthnRequestsSigned" guides/recipes/generic_saml.md</automated></verify>
  <done>The guide contains the long-tail vendor translation content and the ADFS/Shibboleth footgun callouts that DOCS-02 requires.</done>
</task>

<task type="auto">
  <name>Task 2: Add the safety/debugging/rotation spine and gate the doc in ci.docs</name>
  <files>guides/recipes/generic_saml.md, mix.exs</files>
  <action>Finish `guides/recipes/generic_saml.md` with these exact H2 sections: `## Minimum-safe checklist`, `## Debugging flow`, and `## Certificate rotation`. The minimum-safe checklist must document strict defaults and what operators must not weaken: configured IdP cert trust only, replay protection in production, signed assertions/responses expectations, SHA-1/CBC rejection posture, and signing-vs-encryption certificate separation. The debugging flow must follow the adoption spine already locked in context: `FakeIdP proof -> metadata values -> signing certs -> NameID/claims -> signed requests -> encrypted assertions -> rotation drift`. The rotation section must describe the existing staged metadata/apply overlap model rather than inventing manual cert swaps. Then extend `mix.exs` `ci.docs` with `cmd test -f guides/recipes/generic_saml.md` and verify the alias still runs cleanly.</action>
  <verify><automated>rg -n "^## Minimum-safe checklist$|^## Debugging flow$|^## Certificate rotation$" guides/recipes/generic_saml.md && rg -n "FakeIdP|replay|SHA-1|CBC|signing|encryption|metadata|rotation" guides/recipes/generic_saml.md && rg -n "cmd test -f guides/recipes/generic_saml.md" mix.exs && mix ci.docs && mix test --warnings-as-errors</automated></verify>
  <done>The runbook closes the operator-safety loop, and CI now fails if the generic guide is missing or if the docs lane regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook -> operator trust decisions | The guide can cause unsafe signing/encryption or rotation behavior if it drifts from real library posture |
| docs artifact -> CI | Without a docs gate, the generic guide can be removed without detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | T | vendor decoder tables | mitigate | Date/version-scope the tables and frame them as translation guidance rather than evergreen vendor guarantees |
| T-36-05 | I | signing/encryption guidance | mitigate | Ground the minimum-safe checklist and footgun sections in current strict defaults and existing recipes rather than speculative toggles |
| T-36-06 | D | docs presence in CI | mitigate | Add a `cmd test -f guides/recipes/generic_saml.md` line to `ci.docs` so accidental deletion fails closed |
</threat_model>

<verification>
- `rg -n "^## Vendor decoder tables$|^## ADFS-specific notes$|^## Shibboleth-specific notes$" guides/recipes/generic_saml.md`
- `rg -n "IBM Security Verify|CyberArk|Oracle Access Manager|PingFederate|CA SiteMinder|guides/recipes/adfs.md|AuthnRequestsSigned" guides/recipes/generic_saml.md`
- `rg -n "^## Minimum-safe checklist$|^## Debugging flow$|^## Certificate rotation$" guides/recipes/generic_saml.md`
- `rg -n "FakeIdP|replay|SHA-1|CBC|signing|encryption|metadata|rotation" guides/recipes/generic_saml.md`
- `rg -n "cmd test -f guides/recipes/generic_saml.md" mix.exs`
- `mix ci.docs`
- `mix test --warnings-as-errors`
</verification>

<success_criteria>
- The generic runbook contains the full DOCS-02 payload, including vendor decoder tables and operator safety sections.
- ADFS and Shibboleth are documented as generic-path special cases without implying new presets.
- `ci.docs` fails if `guides/recipes/generic_saml.md` is missing.
</success_criteria>

<output>
After completion, create `.planning/phases/36-generic-saml-runbook/36-02-SUMMARY.md`
</output>
