---
phase: 51-demo-app-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/ledger_loop/.formatter.exs
  - demo/ledger_loop/mix.exs
  - demo/ledger_loop/config/config.exs
  - demo/ledger_loop/config/dev.exs
  - demo/ledger_loop/config/runtime.exs
  - demo/ledger_loop/config/test.exs
  - demo/ledger_loop/lib/ledger_loop/application.ex
  - demo/ledger_loop/lib/ledger_loop/repo.ex
  - demo/ledger_loop/lib/ledger_loop_web/endpoint.ex
autonomous: true
requirements:
  - DEMO-01
must_haves:
  truths:
    - "Evaluator can compile a conventional Phoenix app at demo/ledger_loop with Relyra loaded by local path dependency."
    - "The demo app is repo-local and does not require MIX_ENV=test, Relyra.TestSupport, FakeIdP, or Keycloak to boot."
    - "The generated foundation supplies the repo, application supervision tree, endpoint, and environment config required by later Phase 51 route plans."
  artifacts:
    - path: "demo/ledger_loop/mix.exs"
      provides: "Phoenix host app dependency and alias configuration"
      contains: "{:relyra, path: \"../..\"}"
    - path: "demo/ledger_loop/lib/ledger_loop/application.ex"
      provides: "LedgerLoop OTP application supervision entry"
      contains: "LedgerLoopWeb.Endpoint"
    - path: "demo/ledger_loop/lib/ledger_loop/repo.ex"
      provides: "Host Ecto repo used by LiveAdmin and readiness checks"
      contains: "use Ecto.Repo"
    - path: "demo/ledger_loop/lib/ledger_loop_web/endpoint.ex"
      provides: "Phoenix endpoint for the demo host"
      contains: "LedgerLoopWeb.Router"
  key_links:
    - from: "demo/ledger_loop/mix.exs"
      to: "Relyra repository root"
      via: "path dependency"
      pattern: "{:relyra, path: \"../..\"}"
    - from: "demo/ledger_loop/lib/ledger_loop/application.ex"
      to: "LedgerLoop.Repo"
      via: "supervision tree"
      pattern: "LedgerLoop.Repo"
    - from: "demo/ledger_loop/lib/ledger_loop_web/endpoint.ex"
      to: "LedgerLoopWeb.Router"
      via: "endpoint plug"
      pattern: "plug\\(LedgerLoopWeb.Router\\)"
---

<objective>
Create the conventional Phoenix host-app foundation for LedgerLoop without wiring Relyra route behavior yet.

Purpose: Phase 51 needs a runnable host app boundary before route, UI, readiness, and package-boundary plans can prove adoption evidence.
Output: A compilable `demo/ledger_loop` Phoenix app foundation with local Relyra dependency, repo, endpoint, application supervision, and environment config.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-demo-app-foundation/51-CONTEXT.md
@.planning/phases/51-demo-app-foundation/51-RESEARCH.md
@.planning/phases/51-demo-app-foundation/51-VALIDATION.md
@.planning/phases/51-demo-app-foundation/51-PATTERNS.md
@.planning/phases/51-demo-app-foundation/51-UI-SPEC.md
@AGENTS.md
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold LedgerLoop Phoenix foundation</name>
  <files>demo/ledger_loop/.formatter.exs, demo/ledger_loop/mix.exs, demo/ledger_loop/config/config.exs, demo/ledger_loop/config/dev.exs, demo/ledger_loop/config/runtime.exs, demo/ledger_loop/config/test.exs, demo/ledger_loop/lib/ledger_loop/application.ex, demo/ledger_loop/lib/ledger_loop/repo.ex, demo/ledger_loop/lib/ledger_loop_web/endpoint.ex</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/phases/51-demo-app-foundation/51-CONTEXT.md`
    - `.planning/phases/51-demo-app-foundation/51-RESEARCH.md`
    - `.planning/phases/51-demo-app-foundation/51-VALIDATION.md`
    - `.planning/phases/51-demo-app-foundation/51-PATTERNS.md`
    - `.planning/phases/51-demo-app-foundation/51-UI-SPEC.md`
    - `mix.exs`
    - `demo/ledger_loop/mix.exs` if it already exists
    - every `demo/ledger_loop` file listed in `<files>` that already exists
  </read_first>
  <action>Create a conventional Phoenix app under `demo/ledger_loop` for app `:ledger_loop` and module `LedgerLoop`, per D-01. Use the generator command `mix phx.new demo/ledger_loop --app ledger_loop --module LedgerLoop --database postgres --no-assets --no-dashboard --no-mailer --no-gettext --no-agents-md --no-install` when `demo/ledger_loop/mix.exs` is absent. If any scaffold file already exists, preserve user changes and bring the listed foundation files to the same target state manually. In `demo/ledger_loop/mix.exs`, add `{:relyra, path: "../.."}` and keep Phoenix, Phoenix Ecto, Ecto SQL, Postgrex, Phoenix LiveView, Bandit, Jason, telemetry, and generated test dependencies as normal host-app dependencies. Keep `LedgerLoop.Repo` configured as the app repo and `LedgerLoopWeb.Endpoint` plugged to `LedgerLoopWeb.Router`; downstream Phase 51 plans own the route modules, templates, CSS, route tests, health endpoints, and package-boundary script. Per D-02, D-08, D-09, D-12, and D-13, do not add Tailwind, daisyUI, esbuild, shadcn, React, a component registry, `Relyra.TestSupport`, FakeIdP, Keycloak, durable Relyra store proof, request/replay store wrappers, ETS happy-path proof, customer setup workflows, Docker orchestration, CI demo lanes, or public Relyra API changes.</action>
  <verify>
    <automated>test -f demo/ledger_loop/mix.exs && grep -q '{:relyra, path: "../.."}' demo/ledger_loop/mix.exs && ! grep -R "tailwind\\|daisyui\\|esbuild\\|shadcn\\|react\\|Relyra.TestSupport\\|FakeIdP\\|Keycloak" demo/ledger_loop/mix.exs demo/ledger_loop/config demo/ledger_loop/lib 2>/dev/null</automated>
    <automated>cd demo/ledger_loop && mix deps.get && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `demo/ledger_loop/mix.exs` contains `app: :ledger_loop`.
    - Source assertion: `demo/ledger_loop/mix.exs` contains `{:relyra, path: "../.."}`.
    - Source assertion: `demo/ledger_loop/mix.exs`, `demo/ledger_loop/config`, and `demo/ledger_loop/lib` contain no `tailwind`, `daisyui`, `esbuild`, `shadcn`, `react`, `Relyra.TestSupport`, `FakeIdP`, or `Keycloak` tokens outside comments.
    - Source assertion: `demo/ledger_loop/lib/ledger_loop/application.ex` supervises `LedgerLoop.Repo` and `LedgerLoopWeb.Endpoint`.
    - CLI assertion: `cd demo/ledger_loop && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>`demo/ledger_loop` compiles as a generated Phoenix host app and loads Relyra from the repository path without forbidden frontend tooling or test-only proof dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo app -> root Relyra package | Host app resolves the local repository through a path dependency. |
| Developer shell -> Phoenix generator | Generator output becomes repo-local demo application code. |
| LedgerLoop application -> database | Generated repo config establishes the storage boundary used by later readiness and Relyra store plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Spoofing | `demo/ledger_loop/mix.exs` | mitigate | Use `{:relyra, path: "../.."}` only; do not vendor Relyra, publish a demo package, or import `Relyra.TestSupport` for app boot. |
| T-51-02 | Tampering | Phoenix scaffold | mitigate | Generate with `--no-assets` and preserve normal Phoenix config/repo/endpoint conventions; route and UI plans own their files. |
| T-51-03 | Information Disclosure | generated config | mitigate | Do not add PEM, XML, assertion, request parameter, FakeIdP, Keycloak, or secret material to Phase 51 config. |
| T-51-SC | Tampering | Hex dependency scaffold | mitigate | Use packages approved in `51-RESEARCH.md` Package Legitimacy Audit; no npm/pip/cargo installs, no Tailwind/daisyUI/esbuild/shadcn/React packages. |
</threat_model>

<verification>
Run after this plan:
- `cd demo/ledger_loop && mix deps.get && mix compile --warnings-as-errors`
</verification>

<success_criteria>
- `demo/ledger_loop/mix.exs` contains `{:relyra, path: "../.."}` and no forbidden frontend or test-only proof dependencies.
- The generated LedgerLoop app has repo, endpoint, config, and supervision files needed by downstream Phase 51 route/UI/readiness plans.
- This plan does not modify `lib/relyra/security/*`, `lib/relyra/behaviours/*`, `Relyra.start_login/3`, `Relyra.consume_response/3`, configured-certificate trust, XML parsing, signature verification, audit co-commit, or replay policy.
</success_criteria>

<output>
Create `.planning/phases/51-demo-app-foundation/51-01-SUMMARY.md` when done.
</output>
