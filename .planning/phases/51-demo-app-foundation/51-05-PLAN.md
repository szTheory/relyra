---
phase: 51-demo-app-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 51-03
  - 51-04
files_modified:
  - demo/ledger_loop/lib/ledger_loop_web/components/layouts.ex
  - demo/ledger_loop/lib/ledger_loop_web/components/layouts/root.html.heex
  - demo/ledger_loop/priv/static/assets/css/app.css
  - demo/ledger_loop/test/ledger_loop_web/controllers/page_controller_test.exs
autonomous: true
requirements:
  - DEMO-03
must_haves:
  truths:
    - "The workspace uses the approved Phase 51 plain Phoenix/app-local CSS contract."
    - "Evaluator can open / and see all required workspace labels, route scopes, and primary route affordances."
    - "The first screen remains free of raw protocol content, secrets, FakeIdP, and Keycloak copy."
  artifacts:
    - path: "demo/ledger_loop/priv/static/assets/css/app.css"
      provides: "Phase 51 app-local CSS tokens and layout"
      contains: "--ll-accent: #0066cc"
    - path: "demo/ledger_loop/lib/ledger_loop_web/components/layouts/root.html.heex"
      provides: "Root layout asset link"
      contains: "/assets/css/app.css"
    - path: "demo/ledger_loop/test/ledger_loop_web/controllers/page_controller_test.exs"
      provides: "Workspace copy/link/security regression coverage"
  key_links:
    - from: "demo/ledger_loop/lib/ledger_loop_web/components/layouts/root.html.heex"
      to: "demo/ledger_loop/priv/static/assets/css/app.css"
      via: "stylesheet link"
      pattern: "/assets/css/app.css"
    - from: "demo/ledger_loop/test/ledger_loop_web/controllers/page_controller_test.exs"
      to: "demo/ledger_loop/lib/ledger_loop_web/controllers/page_html/home.html.heex"
      via: "GET / response assertions"
      pattern: "LedgerLoop Workspace"
---

<objective>
Apply the Phase 51 app-local UI styling contract and create Wave 0 workspace tests.

Purpose: The first screen needs executable UI evidence, accessible operational styling, and regression coverage for labels, route links, and forbidden raw protocol content.
Output: Root layout/CSS assets and `page_controller_test.exs`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-demo-app-foundation/51-CONTEXT.md
@.planning/phases/51-demo-app-foundation/51-RESEARCH.md
@.planning/phases/51-demo-app-foundation/51-VALIDATION.md
@.planning/phases/51-demo-app-foundation/51-PATTERNS.md
@.planning/phases/51-demo-app-foundation/51-UI-SPEC.md
@AGENTS.md
@demo/ledger_loop/lib/ledger_loop_web/controllers/page_html/home.html.heex
@demo/ledger_loop/test/support/conn_case.ex
@test/phoenix/metadata_controller_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply app-local operational styling</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/components/layouts.ex, demo/ledger_loop/lib/ledger_loop_web/components/layouts/root.html.heex, demo/ledger_loop/priv/static/assets/css/app.css</files>
  <read_first>
    - `.planning/phases/51-demo-app-foundation/51-UI-SPEC.md`
    - `.planning/phases/51-demo-app-foundation/51-PATTERNS.md`
    - `demo/ledger_loop/lib/ledger_loop_web/components/layouts.ex`
    - `demo/ledger_loop/lib/ledger_loop_web/components/layouts/root.html.heex`
    - `demo/ledger_loop/priv/static/assets/css/app.css` if it already exists
  </read_first>
  <action>Create or update app-local CSS for the Phase 51 UI contract without adding an asset build pipeline. Link `/assets/css/app.css` from the root layout if the generated no-assets scaffold does not already include it. Define CSS variables with these exact values: `--ll-bg: #ffffff`, `--ll-panel: #f0f4f8`, `--ll-accent: #0066cc`, `--ll-danger: #c62828`, `--ll-success: #2e7d32`, `--ll-warning: #b87600`, `--ll-text: #333333`, `--ll-muted: #555555`, and `--ll-border: #d1d9e1`. Use Helvetica, Arial, sans-serif. Use only 14px, 16px, 20px, and 28px font sizes in Phase 51 workspace CSS. Use a two-zone shell with a top workspace header and a responsive operational grid whose mobile order is header, tenant status, setup/login/admin/support links, route mounts, health/readiness. Keep panels as direct children of the main grid; do not create nested cards, gradients, decorative blobs, hero illustrations, Tailwind classes, shadcn classes, React mounts, third-party icons, or third-party UI registries. Ensure focus outlines are visible and standalone links/buttons have at least 44px by 44px clickable area.</action>
  <verify>
    <automated>test -f demo/ledger_loop/priv/static/assets/css/app.css && grep -q -- "--ll-accent: #0066cc" demo/ledger_loop/priv/static/assets/css/app.css && ! grep -R "tailwind\\|daisyui\\|shadcn\\|React\\|linear-gradient\\|radial-gradient\\|blob" demo/ledger_loop/lib/ledger_loop_web demo/ledger_loop/priv/static/assets/css/app.css 2>/dev/null</automated>
    <automated>cd demo/ledger_loop && mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app.css` contains all nine `--ll-*` variables listed in the action with exact color values.
    - Source assertion: workspace CSS contains no `linear-gradient`, `radial-gradient`, `tailwind`, `daisyui`, `shadcn`, or `React` tokens.
    - Source assertion: `root.html.heex` references `/assets/css/app.css`.
    - Source assertion: CSS includes a visible `:focus-visible` rule.
    - Source assertion: standalone link/button styles satisfy at least 44px by 44px hit targets.
    - CLI assertion: `cd demo/ledger_loop && mix format --check-formatted` exits 0.
  </acceptance_criteria>
  <done>The workspace uses the approved Phase 51 plain Phoenix/app-local CSS contract and remains free of forbidden frontend stacks.</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 workspace tests</name>
  <files>demo/ledger_loop/test/ledger_loop_web/controllers/page_controller_test.exs</files>
  <read_first>
    - `.planning/phases/51-demo-app-foundation/51-VALIDATION.md`
    - `.planning/phases/51-demo-app-foundation/51-PATTERNS.md`
    - `.planning/phases/51-demo-app-foundation/51-UI-SPEC.md`
    - `test/phoenix/metadata_controller_test.exs`
    - `demo/ledger_loop/lib/ledger_loop_web/controllers/page_html/home.html.heex`
    - `demo/ledger_loop/test/support/conn_case.ex`
    - `demo/ledger_loop/test/ledger_loop_web/controllers/page_controller_test.exs` if it already exists
  </read_first>
  <action>Create the Wave 0 page controller test required by `51-VALIDATION.md` for DEMO-03. The test must request `GET /` through `LedgerLoopWeb.ConnCase`, assert status 200, assert the response contains the exact labels `LedgerLoop Workspace`, `Northstar Health SSO status`, `Open SSO Setup`, `Start Test Login`, `Open Relyra Admin`, `Open Support Scenario`, `Mounted SAML routes: /saml`, `Mounted operator routes: /relyra/admin`, `Demo health`, and `Demo readiness`, assert response links or hrefs include `/setup/sso`, `/login/test`, `/relyra/admin`, `/support/scenario`, `/healthz`, and `/readyz`, and refute `BEGIN CERTIFICATE`, `SAMLResponse`, `Assertion`, `RelayState=`, `FakeIdP`, and `Keycloak` in the response body.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop_web/controllers/page_controller_test.exs --warnings-as-errors</automated>
    <automated>cd demo/ledger_loop && mix test --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: `page_controller_test.exs` checks every exact UI label listed in the action.
    - Test assertion: `page_controller_test.exs` checks hrefs or route references for `/setup/sso`, `/login/test`, `/relyra/admin`, `/support/scenario`, `/healthz`, and `/readyz`.
    - Test assertion: `page_controller_test.exs` refutes `BEGIN CERTIFICATE`, `SAMLResponse`, `Assertion`, `RelayState=`, `FakeIdP`, and `Keycloak`.
    - CLI assertion: `cd demo/ledger_loop && mix test --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Wave 0 workspace validation gap from `51-VALIDATION.md` is covered and the demo app suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> LedgerLoop workspace | Untrusted evaluator/browser receives styled operational status and route affordances. |
| Root layout -> static CSS | Browser loads app-local CSS from `priv/static` without a build pipeline. |
| Test suite -> workspace response | Automated tests inspect rendered HTML for required evidence and forbidden protocol content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-15 | Tampering | CSS/layout assets | mitigate | Use app-local `app.css` only; no third-party UI registry, Tailwind, shadcn, React, or external asset pipeline in Phase 51. |
| T-51-16 | Information Disclosure | `page_controller_test.exs` coverage | mitigate | Tests must refute `BEGIN CERTIFICATE`, `SAMLResponse`, `Assertion`, `RelayState=`, `FakeIdP`, and `Keycloak` in the rendered first screen. |
| T-51-17 | Denial of Service | First-screen navigation | accept | Static workspace and route affordance links are low-cost Phoenix responses; deeper flow load belongs to Phases 52-55. |
| T-51-SC | Tampering | UI dependencies | mitigate | Do not install npm/pip/cargo packages; no shadcn registry blocks; Hex dependencies already approved in `51-RESEARCH.md`. |
</threat_model>

<verification>
Run after this plan:
- `cd demo/ledger_loop && mix test test/ledger_loop_web/controllers/page_controller_test.exs --warnings-as-errors`
- `cd demo/ledger_loop && mix test --warnings-as-errors`
- `cd demo/ledger_loop && mix format --check-formatted`
- Manual end-of-phase check: boot the demo app, open `/`, and compare the first screen against `51-UI-SPEC.md` first-screen labels, route affordances, and forbidden raw protocol content.
</verification>

<success_criteria>
- `GET /` returns 200 and includes all required UI-SPEC labels and four primary route affordances.
- The workspace CSS follows the Phase 51 token, typography, layout, focus, and no-registry constraints.
- The first screen contains health/readiness text labels and no raw protocol/secrets/test-IdP copy.
- The UI is implemented with plain Phoenix templates/components and app-local CSS only.
</success_criteria>

<output>
Create `.planning/phases/51-demo-app-foundation/51-05-SUMMARY.md` when done.
</output>
