---
phase: 51-demo-app-foundation
plan: 06
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - mix.exs
  - scripts/check_demo_package_exclusion.sh
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
must_haves:
  truths:
    - "The demo app remains runnable from the repository."
    - "The root Hex package excludes demo/ledger_loop contents."
    - "Package exclusion is inspectable by an automated command without running Hex publish commands."
  artifacts:
    - path: "scripts/check_demo_package_exclusion.sh"
      provides: "Automated unpacked-package exclusion proof for demo/"
      contains: "mix hex.build --unpack"
    - path: "mix.exs"
      provides: "Explicit root Hex package whitelist"
      contains: "files:"
    - path: "demo/ledger_loop/mix.exs"
      provides: "Repo-local runnable demo dependency boundary"
      contains: "{:relyra, path: \"../..\"}"
  key_links:
    - from: "scripts/check_demo_package_exclusion.sh"
      to: "mix.exs package.files"
      via: "mix hex.build --unpack"
      pattern: "mix hex.build --unpack"
    - from: "demo/ledger_loop/mix.exs"
      to: "Relyra repository root"
      via: "path dependency"
      pattern: "{:relyra, path: \"../..\"}"
---

<objective>
Prove the package boundary for the repo-local demo app while keeping root release behavior intact.

Purpose: Phase 51 must demonstrate that evaluators can run `demo/ledger_loop` from the repository without accidentally shipping demo evidence in the Hex package.
Output: A focused package-exclusion check script and any required preservation of the root `mix.exs` explicit package whitelist.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-demo-app-foundation/51-CONTEXT.md
@.planning/phases/51-demo-app-foundation/51-RESEARCH.md
@.planning/phases/51-demo-app-foundation/51-VALIDATION.md
@.planning/phases/51-demo-app-foundation/51-PATTERNS.md
@AGENTS.md
@mix.exs
@lib/mix/tasks/verify.release_parity.ex
@demo/ledger_loop/mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add package exclusion check script</name>
  <files>scripts/check_demo_package_exclusion.sh</files>
  <read_first>
    - `.planning/phases/51-demo-app-foundation/51-CONTEXT.md`
    - `.planning/phases/51-demo-app-foundation/51-RESEARCH.md`
    - `.planning/phases/51-demo-app-foundation/51-VALIDATION.md`
    - `.planning/phases/51-demo-app-foundation/51-PATTERNS.md`
    - `AGENTS.md`
    - `mix.exs`
    - `lib/mix/tasks/verify.release_parity.ex`
    - `demo/ledger_loop/mix.exs`
    - `scripts/check_demo_package_exclusion.sh` if it already exists
  </read_first>
  <action>Create executable `scripts/check_demo_package_exclusion.sh` for the narrow Phase 51 package boundary proof, per D-10 and D-11. The script must run from the repository root, remove `/tmp/relyra-package-check`, run `mix hex.build --unpack --output /tmp/relyra-package-check`, fail if any unpacked path matches `*/demo/*`, fail if `demo/ledger_loop/mix.exs` is missing, fail if `demo/ledger_loop/mix.exs` does not contain `{:relyra, path: "../.."}`, and print a success line containing `demo package exclusion: ok` when all checks pass. Do not run Hex publish commands. Do not add a root `scripts/demo` command or `mix ci.demo_app`; Phase 55 owns Docker/demo command orchestration and CI lanes.</action>
  <verify>
    <automated>test -x scripts/check_demo_package_exclusion.sh && grep -q "mix hex.build --unpack --output /tmp/relyra-package-check" scripts/check_demo_package_exclusion.sh && grep -q "demo package exclusion: ok" scripts/check_demo_package_exclusion.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `scripts/check_demo_package_exclusion.sh` contains `mix hex.build --unpack --output /tmp/relyra-package-check`.
    - Source assertion: `scripts/check_demo_package_exclusion.sh` contains a `find /tmp/relyra-package-check` check for `*/demo/*`.
    - Source assertion: `scripts/check_demo_package_exclusion.sh` contains `demo package exclusion: ok`.
    - Source assertion: `scripts/check_demo_package_exclusion.sh` does not contain `hex.publish`, `scripts/demo`, or `ci.demo_app`.
    - CLI assertion: `test -x scripts/check_demo_package_exclusion.sh` exits 0.
  </acceptance_criteria>
  <done>A repo-root command exists to inspect the Hex package boundary without publishing or implementing Phase 55 demo orchestration.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve root whitelist and prove repo-local runnability</name>
  <files>mix.exs, scripts/check_demo_package_exclusion.sh</files>
  <read_first>
    - `.planning/phases/51-demo-app-foundation/51-CONTEXT.md`
    - `.planning/phases/51-demo-app-foundation/51-RESEARCH.md`
    - `.planning/phases/51-demo-app-foundation/51-VALIDATION.md`
    - `.planning/phases/51-demo-app-foundation/51-PATTERNS.md`
    - `AGENTS.md`
    - `mix.exs`
    - `demo/ledger_loop/mix.exs`
    - `scripts/check_demo_package_exclusion.sh`
  </read_first>
  <action>Inspect root `mix.exs` `package/0` and preserve the existing explicit `files:` whitelist model, per D-10. If `mix.exs` has gained `demo`, `demo/ledger_loop`, `"."`, `"*"`, or a wildcard that would include `demo/`, remove that inclusion while preserving the existing package files for Relyra library release. Do not replace the whitelist with ignore-file assumptions. Do not modify `CHANGELOG.md`, release-please files, `mix ci.security`, `Relyra.start_login/3`, `Relyra.consume_response/3`, crypto seams, XML seams, audit writer, behaviours, or replay policy. Run the package exclusion check and the demo compile check to prove both sides of DEMO-01 and DEMO-02: repository-local demo commands work and unpacked Hex contents exclude `demo/`.</action>
  <verify>
    <automated>! grep -nE '"demo"|demo/ledger_loop|"[.]"|"[*]"' mix.exs</automated>
    <automated>scripts/check_demo_package_exclusion.sh</automated>
    <automated>cd demo/ledger_loop && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: root `mix.exs` `package/0` does not add `demo`, `demo/ledger_loop`, `"."`, `"*"`, or a wildcard that includes `demo/`.
    - CLI assertion: `scripts/check_demo_package_exclusion.sh` exits 0 and prints `demo package exclusion: ok`.
    - CLI assertion: `cd demo/ledger_loop && mix compile --warnings-as-errors` exits 0.
    - Source assertion: no file edited by this task contains `mix hex.publish`.
  </acceptance_criteria>
  <done>The demo is runnable from the repository and absent from unpacked Hex package contents, with the root package whitelist preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository tree -> Hex package builder | Local files are selected into the publishable package by root `mix.exs` package metadata. |
| Developer shell -> package proof script | The script runs local build commands and inspects `/tmp/relyra-package-check`. |
| Demo app -> root Relyra package | Demo depends on the repository path but must not become part of the published package payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-18 | Information Disclosure | Hex package contents | mitigate | `scripts/check_demo_package_exclusion.sh` builds and unpacks with `mix hex.build --unpack` and fails on any `*/demo/*` path. |
| T-51-19 | Tampering | root `mix.exs` package whitelist | mitigate | Preserve explicit `files:` whitelist; remove any `demo`, `"."`, `"*"`, or wildcard inclusion that would ship the demo. |
| T-51-20 | Repudiation | package proof evidence | mitigate | Script prints `demo package exclusion: ok` only after checking unpacked contents and local path dependency. |
| T-51-21 | Elevation of Privilege | release commands | mitigate | The script and plan must not run Hex publish commands; Release Please remains the publishing path. |
| T-51-SC | Tampering | package proof tooling | accept | No new npm/pip/cargo packages or Hex dependencies are added; proof uses existing Mix/Hex tooling. |
</threat_model>

<verification>
Run after this plan:
- `scripts/check_demo_package_exclusion.sh`
- `cd demo/ledger_loop && mix compile --warnings-as-errors`
- `mix format --check-formatted`
- `mix test --warnings-as-errors`
</verification>

<success_criteria>
- `scripts/check_demo_package_exclusion.sh` exits 0 and prints `demo package exclusion: ok`.
- Unpacked package contents under `/tmp/relyra-package-check` contain no path matching `*/demo/*`.
- `demo/ledger_loop/mix.exs` still contains `{:relyra, path: "../.."}`.
- Root `mix.exs` package whitelist remains explicit and does not include demo paths or wildcard package expansion.
</success_criteria>

<output>
Create `.planning/phases/51-demo-app-foundation/51-06-SUMMARY.md` when done.
</output>
