---
phase: 51-demo-app-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 51-02
files_modified:
  - demo/ledger_loop/lib/ledger_loop_web/controllers/page_controller.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/page_html.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/page_html/home.html.heex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/setup.html.heex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/support.html.heex
autonomous: true
requirements:
  - DEMO-03
  - DEMO-04
  - DEMO-05
must_haves:
  truths:
    - "Evaluator can open / and see the actual LedgerLoop Workspace, not a marketing landing page."
    - "The first screen exposes tenant/status information and setup, login, admin, and support affordances."
    - "The first screen visibly identifies host-owned /saml and /relyra/admin route scopes."
    - "The workspace displays demo health/readiness states with explicit text, not color-only indicators."
  artifacts:
    - path: "demo/ledger_loop/lib/ledger_loop_web/controllers/page_html/home.html.heex"
      provides: "LedgerLoop workspace first screen"
      contains: "LedgerLoop Workspace"
    - path: "demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/setup.html.heex"
      provides: "Stable setup route affordance page"
      contains: "host-owned"
    - path: "demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex"
      provides: "Stable login route affordance page"
      contains: "Relyra verifies SAML trust"
    - path: "demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/support.html.heex"
      provides: "Stable support route affordance page"
      contains: "support"
  key_links:
    - from: "demo/ledger_loop/lib/ledger_loop_web/controllers/page_html/home.html.heex"
      to: "/setup/sso"
      via: "Open SSO Setup link"
      pattern: "/setup/sso"
    - from: "demo/ledger_loop/lib/ledger_loop_web/controllers/page_html/home.html.heex"
      to: "/login/test"
      via: "Start Test Login link or disabled route affordance"
      pattern: "/login/test"
    - from: "demo/ledger_loop/lib/ledger_loop_web/controllers/page_html/home.html.heex"
      to: "/relyra/admin"
      via: "Open Relyra Admin link"
      pattern: "/relyra/admin"
    - from: "demo/ledger_loop/lib/ledger_loop_web/controllers/page_html/home.html.heex"
      to: "/support/scenario"
      via: "Open Support Scenario link"
      pattern: "/support/scenario"
---

<objective>
Build the Phase 51 LedgerLoop workspace content and stable route affordance destinations.

Purpose: Evaluators need visible host-app evidence immediately after boot: tenant status, route ownership, probe states, and affordances into setup, login, admin, and support.
Output: Phoenix controllers/templates for the approved Phase 51 first-screen and route-affordance content.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-demo-app-foundation/51-CONTEXT.md
@.planning/phases/51-demo-app-foundation/51-RESEARCH.md
@.planning/phases/51-demo-app-foundation/51-VALIDATION.md
@.planning/phases/51-demo-app-foundation/51-PATTERNS.md
@.planning/phases/51-demo-app-foundation/51-UI-SPEC.md
@AGENTS.md
@demo/ledger_loop/lib/ledger_loop_web/router.ex
@demo/ledger_loop/lib/ledger_loop_web/controllers/health_controller.ex

<interfaces>
Route and probe surface created by Plans 51-02 and 51-03:
- `GET /` renders the workspace.
- `GET /setup/sso`, `GET /login/test`, and `GET /support/scenario` are host-owned Phase 51 route affordances for Phase 53/54 ownership.
- `GET /healthz` returns text `booted`.
- `GET /readyz` returns text `ready` or `unavailable`.
- `/saml` and `/relyra/admin` are mounted route scopes that must be visible in the workspace.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LedgerLoop workspace content</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/controllers/page_controller.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/page_html.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/page_html/home.html.heex</files>
  <read_first>
    - `.planning/phases/51-demo-app-foundation/51-CONTEXT.md`
    - `.planning/phases/51-demo-app-foundation/51-RESEARCH.md`
    - `.planning/phases/51-demo-app-foundation/51-VALIDATION.md`
    - `.planning/phases/51-demo-app-foundation/51-PATTERNS.md`
    - `.planning/phases/51-demo-app-foundation/51-UI-SPEC.md`
    - `AGENTS.md`
    - `demo/ledger_loop/lib/ledger_loop_web/router.ex`
    - `demo/ledger_loop/lib/ledger_loop_web/controllers/health_controller.ex`
    - every file listed in `<files>` that already exists
  </read_first>
  <action>Render `/` as the actual `LedgerLoop Workspace` shell, per D-03 and D-04. The first screen must include these exact visible labels from `51-UI-SPEC.md`: `LedgerLoop Workspace`, `Northstar Health SSO status`, `Open SSO Setup`, `Start Test Login`, `Open Relyra Admin`, `Open Support Scenario`, `Mounted SAML routes: /saml`, `Mounted operator routes: /relyra/admin`, `Demo health`, and `Demo readiness`. Include the exact empty-state copy `No tenant data loaded` and `Run the demo reset command, then refresh this workspace to load Northstar Health and its SSO status.` because Phase 52 owns deterministic seed data. Link `Open SSO Setup` to `/setup/sso`, `Start Test Login` to `/login/test`, `Open Relyra Admin` to `/relyra/admin`, and `Open Support Scenario` to `/support/scenario`. Include the phrases `host-owned` and `Relyra verifies SAML trust` when explaining the boundary, per D-05, D-06, D-12, and D-13. Display explicit text states for `Demo health` and `Demo readiness`, with route references to `/healthz` and `/readyz`; do not rely on color alone. Do not expose raw XML, PEM blocks, secrets, unredacted assertions, request parameters, FakeIdP details, Keycloak details, ETS request/replay proof, durable store proof, setup workflow behavior, or Docker/CI command orchestration.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix compile --warnings-as-errors</automated>
    <automated>grep -R "LedgerLoop Workspace\\|Northstar Health SSO status\\|Open SSO Setup\\|Start Test Login\\|Open Relyra Admin\\|Open Support Scenario\\|Mounted SAML routes: /saml\\|Mounted operator routes: /relyra/admin\\|Demo health\\|Demo readiness" demo/ledger_loop/lib/ledger_loop_web/controllers/page_html</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `home.html.heex` contains `LedgerLoop Workspace`.
    - Source assertion: `home.html.heex` contains links or route references for `/setup/sso`, `/login/test`, `/relyra/admin`, and `/support/scenario`.
    - Source assertion: `home.html.heex` contains `Mounted SAML routes: /saml` and `Mounted operator routes: /relyra/admin`.
    - Source assertion: `home.html.heex` contains `Demo health`, `Demo readiness`, `/healthz`, and `/readyz`.
    - Source assertion: first-screen template content contains no `BEGIN CERTIFICATE`, `SAMLResponse`, `Assertion`, `RelayState=`, `FakeIdP`, or `Keycloak` tokens.
    - CLI assertion: `cd demo/ledger_loop && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The root route is a usable LedgerLoop workspace shell with all required Phase 51 affordances and stable route destinations for Phases 52-55.</done>
</task>

<task type="auto">
  <name>Task 2: Implement setup, login, and support route affordance content</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/setup.html.heex, demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/login.html.heex, demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/support.html.heex</files>
  <read_first>
    - `.planning/phases/51-demo-app-foundation/51-CONTEXT.md`
    - `.planning/phases/51-demo-app-foundation/51-RESEARCH.md`
    - `.planning/phases/51-demo-app-foundation/51-VALIDATION.md`
    - `.planning/phases/51-demo-app-foundation/51-PATTERNS.md`
    - `.planning/phases/51-demo-app-foundation/51-UI-SPEC.md`
    - `AGENTS.md`
    - `demo/ledger_loop/lib/ledger_loop_web/router.ex`
    - every file listed in `<files>` that already exists
  </read_first>
  <action>Implement route affordance pages for setup, login, and support with plain Phoenix controllers/templates. These pages must preserve the route destinations created in Plan 51-02 and explicitly state ownership boundaries: customer/admin setup workflows are Phase 53, local browser login proof is Phase 54, seeded support scenarios and receipts are Phase 53, durable Ecto store proof is Phase 52, and FakeIdP/Keycloak proof is Phase 54/55. Include `host-owned` on setup/support pages and `Relyra verifies SAML trust` on the login page. The pages may link back to `/` and may link to `/relyra/admin`; they must not initiate SAML, call FakeIdP or Keycloak, create or mutate seeded tenant data, configure durable request/replay stores, create audit rows, run reset commands, or alter Relyra public APIs.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix compile --warnings-as-errors</automated>
    <automated>grep -R "host-owned\\|Relyra verifies SAML trust\\|Phase 52\\|Phase 53\\|Phase 54" demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `setup.html.heex` contains `host-owned` and states setup workflows are Phase 53.
    - Source assertion: `login.html.heex` contains `Relyra verifies SAML trust` and states browser login proof is Phase 54.
    - Source assertion: `support.html.heex` contains `host-owned` and states seeded support scenarios and receipts are Phase 53.
    - Source assertion: route affordance templates contain no `BEGIN CERTIFICATE`, `SAMLResponse`, `Assertion`, `RelayState=`, `FakeIdP`, or `Keycloak` tokens.
    - CLI assertion: `cd demo/ledger_loop && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Setup, login, and support destinations are reachable host-owned pages with correct Phase 51 boundaries and no Phase 52-55 behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> LedgerLoop workspace | Untrusted evaluator/browser receives operational status and route affordances. |
| Workspace -> route affordance pages | Users can navigate to setup/login/support route affordances before those flows are implemented. |
| Workspace -> LiveAdmin route | Users can navigate from LedgerLoop host UI into mounted Relyra operator UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-11 | Information Disclosure | `home.html.heex` | mitigate | Tests in Plan 51-05 must refute `BEGIN CERTIFICATE`, `SAMLResponse`, `Assertion`, `RelayState=`, `FakeIdP`, and `Keycloak`; do not render raw XML, PEM, secrets, assertions, or request params. |
| T-51-12 | Spoofing | `/login/test` route affordance | mitigate | Login route copy must state browser login proof is owned by Phase 54; Phase 51 must not present FakeIdP or Keycloak as production behavior. |
| T-51-13 | Repudiation | Workspace boundary copy | mitigate | The first screen must include `host-owned` and `Relyra verifies SAML trust` so evaluator evidence preserves host/Relyra responsibility boundaries. |
| T-51-14 | Information Disclosure | route affordance pages | mitigate | Route pages must not expose raw protocol content, PEM blocks, secrets, assertions, request params, FakeIdP details, or Keycloak details. |
| T-51-SC | Tampering | UI templates | mitigate | Do not install npm/pip/cargo packages; no shadcn registry blocks; Hex dependencies already approved in `51-RESEARCH.md`. |
</threat_model>

<verification>
Run after this plan:
- `cd demo/ledger_loop && mix compile --warnings-as-errors`
</verification>

<success_criteria>
- `GET /` can render all required UI-SPEC labels and four primary route affordances.
- The first screen visibly states `/saml` and `/relyra/admin` as mounted route scopes.
- The first screen contains health/readiness text labels and no raw protocol/secrets/test-IdP copy.
- Setup, login, and support route pages preserve host ownership and phase boundaries without implementing Phase 52-55 behavior.
</success_criteria>

<output>
Create `.planning/phases/51-demo-app-foundation/51-04-SUMMARY.md` when done.
</output>
