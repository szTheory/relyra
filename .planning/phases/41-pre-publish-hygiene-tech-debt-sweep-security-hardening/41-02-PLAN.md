---
phase: 41-pre-publish-hygiene-tech-debt-sweep-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
autonomous: true
requirements:
  - TD-02
must_haves:
  truths:
    - "Production Hex package file list excludes `lib/relyra/test_support` paths."
    - "Production compile paths exclude `lib/relyra/test_support` modules."
    - "Local `mix hex.build` tarball contains no `test_support` entries."
  artifacts:
    - path: "mix.exs"
      provides: "Aligned `elixirc_paths(:prod)` and `package/0` `files` that omit test_support."
---

<objective>
Close TD-02 (WR-04): ensure `Relyra.TestSupport` never ships in the production artifact via both compile-path and package-file exclusion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/41-pre-publish-hygiene-tech-debt-sweep-security-hardening/41-CONTEXT.md
@.planning/phases/41-pre-publish-hygiene-tech-debt-sweep-security-hardening/41-RESEARCH.md
@mix.exs
@lib/relyra/test_support/fake_idp.ex
</context>

<tasks>

<task type="auto">
  <name>Exclude test_support from prod compile paths</name>
  <read_first>
    - mix.exs (elixirc_paths/1, package/0)
    - lib/relyra/test_support/
  </read_first>
  <action>
    Replace `defp elixirc_paths(_), do: ["lib"]` prod branch with explicit paths under `lib/` that compile all production modules but omit `lib/relyra/test_support`. Keep `elixirc_paths(:test)` as `["lib", "test/support"]`. Use a private helper e.g. `prod_elixirc_paths/0` that globs `lib/**/*.ex` directories or lists `lib/relyra` subtrees excluding `test_support` — document choice in a one-line comment. Verify `:dev` still compiles test_support (dev uses non-:test env — ensure dev/test both retain test_support access; if `:dev` used `["lib"]` before, keep dev compiling test_support via `elixirc_paths(:dev)` returning `["lib"]` or equivalent).
  </action>
  <verify>MIX_ENV=prod mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - `MIX_ENV=prod mix compile` succeeds.
    - `_build/prod/lib/relyra/ebin` (or equivalent) contains no `Elixir.Relyra.TestSupport` beams after prod compile.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tighten package.files whitelist</name>
  <read_first>
    - mix.exs package/0
  </read_first>
  <action>
    Remove bare `"lib"` from `package/0` `files` list. Build `files` from existing non-lib entries plus an explicit lib file list (glob `lib/**/*.ex` and required `lib` assets) that excludes any path containing `test_support`. Preserve all current non-lib package entries (`priv`, `docs`, `guides`, etc.).
  </action>
  <verify>MIX_ENV=prod mix hex.build --warnings-as-errors 2>&1; TARBALL=$(ls -t relyra-*.tar.gz 2>/dev/null | head -1); tar -tzf "$TARBALL" | grep -c test_support || true</verify>
  <acceptance_criteria>
    - `mix hex.build` succeeds.
    - `tar -tzf` on built package prints zero lines matching `test_support`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `MIX_ENV=prod mix compile --warnings-as-errors`
- `MIX_ENV=prod mix hex.build` then `tar -tzf relyra-*.tar.gz | grep test_support` returns exit 1 (no matches)
</verification>

<success_criteria>
TD-02 satisfied: dual-layer exclusion inspectable locally before Phase 45 published-tarball check.
</success_criteria>
