---
phase: 16-metadata-management-ui
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - lib/relyra/live_admin/connection_metadata_live.ex
  - lib/relyra/live_admin/connections_live.ex
  - lib/relyra/live_admin/components/connection_detail.ex
  - lib/relyra/live_admin/query.ex
autonomous: true
requirements:
  - MDUI-01
must_haves:
  truths:
    - Operator can import XML and get feedback.
    - Operator can register a URL and get feedback.
    - Operator can see the 10 most recent metadata revisions.
    - Metadata forms are removed from the main connection details view.
  artifacts:
    - path: lib/relyra/live_admin/connection_metadata_live.ex
      provides: Forms for XML and URL input, and a history stream
    - path: lib/relyra/live_admin/components/connection_detail.ex
      provides: Cleaned up UI, pointing to the new route
  key_links:
    - from: lib/relyra/live_admin/connection_metadata_live.ex
      to: Relyra.Metadata
      via: import_xml and register_source function calls
      pattern: Metadata\.import_xml
---

<objective>
Migrate the existing metadata import/register functionality to the dedicated LiveView and implement a stream-based UI for reviewing the 10 most recent metadata revisions.

Purpose: Fully extract metadata logic from `ConnectionsLive` to clear up bloat, and provide operators with an audit trail of metadata changes.
Output: Working forms in the new route, historical audit trail stream, and a clean main connection view.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-metadata-management-ui/16-RESEARCH.md
@.planning/phases/16-metadata-management-ui/16-PATTERNS.md
@.planning/phases/16-metadata-management-ui/16-01-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Migrate Forms and Core Event Handlers</name>
  <files>lib/relyra/live_admin/connection_metadata_live.ex, lib/relyra/live_admin/connections_live.ex, lib/relyra/live_admin/components/connection_detail.ex</files>
  <behavior>
    - Test 1: Submitting `import_metadata` successfully triggers XML import in core and updates flash.
    - Test 2: Submitting `register_metadata_source` registers the URL and updates flash.
    - Test 3: Old forms no longer exist in `connection_detail.ex`.
  </behavior>
  <action>
    Move `import_metadata` and `register_metadata_source` event handler logic from `ConnectionsLive` to `ConnectionMetadataLive`.
    Update the `ConnectionMetadataLive` render logic to include the actual forms (moved from `connection_detail.ex`), rendered conditionally based on `@mode`.
    In `connection_detail.ex`, delete the old metadata forms and add a link/button pointing to `/connections/:connection_id/metadata`.
    Remove any orphaned `handle_event` clauses for these operations in `ConnectionsLive`.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_metadata_test.exs</automated>
  </verify>
  <done>Forms render correctly in their respective tabs, submit successfully, and main connection detail view no longer contains metadata forms.</done>
</task>

<task type="auto">
  <name>Task 2: Metadata History Stream</name>
  <files>lib/relyra/live_admin/connection_metadata_live.ex, lib/relyra/live_admin/query.ex</files>
  <action>
    Update `Relyra.LiveAdmin.Query` to expose a function fetching the 10 most recent `MetadataRevision` records for a connection, preloading the `metadata_source` (you can extract this logic from `get_connection_detail/4` or reuse existing if applicable).
    In `ConnectionMetadataLive.mount/3` (or `handle_params`), call this query and assign the results to a LiveView stream: `stream(socket, :metadata_revisions, revisions)`.
    In the template, render a table/list iterating over `@streams.metadata_revisions`. Highlight the row if it's the active revision (e.g. status is active). 
    Ensure it gracefully handles empty states.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_metadata_test.exs</automated>
  </verify>
  <done>The UI displays the 10 most recent metadata revisions using a LiveView stream, highlighting the last known good state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Admin API | Authenticated operator submitting metadata XML or URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03   | Tampering| Metadata Input | mitigate | Pass data directly to core `Relyra.Metadata.import_xml` and `register_source` which handle Ecto validation and sanitization. |
</threat_model>

<verification>
mix test passes. Operator can perform XML and URL import/registration successfully in the new UI.
</verification>

<success_criteria>
Metadata import forms and logic are successfully migrated without regression, and the history stream is visible.
</success_criteria>

<output>
After completion, create `.planning/phases/16-metadata-management-ui/16-02-SUMMARY.md`
</output>