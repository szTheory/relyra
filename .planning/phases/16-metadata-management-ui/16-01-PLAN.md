---
phase: 16-metadata-management-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/live_admin/router.ex
  - lib/relyra/live_admin/connection_metadata_live.ex
  - test/phoenix/live_admin_metadata_test.exs
autonomous: true
requirements:
  - MDUI-01
must_haves:
  truths:
    - Operator can navigate to /connections/:connection_id/metadata.
    - URL parameters (?mode=xml and ?mode=url) toggle the view state.
  artifacts:
    - path: lib/relyra/live_admin/connection_metadata_live.ex
      provides: Metadata management LiveView controller
    - path: test/phoenix/live_admin_metadata_test.exs
      provides: Route and tab logic testing
  key_links:
    - from: lib/relyra/live_admin/router.ex
      to: lib/relyra/live_admin/connection_metadata_live.ex
      via: Router DSL
      pattern: live "/connections/:connection_id/metadata"
---

<objective>
Establish the routing, test scaffold, and URL-driven tab structure for the Metadata Management UI, keeping view state in the URL via `?mode=xml` and `?mode=url`.

Purpose: Adhere to LiveView best practices by isolating metadata workflows in their own thin LiveView route and maintaining distinct tab states in the URL.
Output: Working dedicated route with empty panels for URL and XML modes, validated by automated tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-metadata-management-ui/16-RESEARCH.md
@.planning/phases/16-metadata-management-ui/16-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Route & LiveView Skeleton</name>
  <files>lib/relyra/live_admin/router.ex, lib/relyra/live_admin/connection_metadata_live.ex, test/phoenix/live_admin_metadata_test.exs</files>
  <behavior>
    - Test 1: Route `/connections/:connection_id/metadata` successfully loads for admin.
    - Test 2: `?mode=xml` query parameter sets mode to `xml`.
    - Test 3: `?mode=url` query parameter sets mode to `url`.
  </behavior>
  <action>
    Define `Relyra.LiveAdmin.ConnectionMetadataLive`. Implement `mount/3` (assigning basic metadata like connection_id and retrieving auth from session as done in `ConnectionsLive`). 
    Implement `handle_params/3` to extract the `mode` parameter (defaulting to `"xml"`) and assign it to the socket.
    Implement a `render/1` function containing basic structure with a tab switcher and two conditionally rendered panels based on `@mode`.
    Update `Relyra.LiveAdmin.Router` to add `live "/connections/:connection_id/metadata", Relyra.LiveAdmin.ConnectionMetadataLive, :metadata` inside the `live_session`.
    Write tests in `live_admin_metadata_test.exs` ensuring the page mounts successfully and parameters switch the view.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_metadata_test.exs</automated>
  </verify>
  <done>LiveView loads successfully on the route, parameters control the active tab, and tests pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Admin API | Authenticated operator accessing the dedicated metadata route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01   | Spoofing | ConnectionMetadataLive | mitigate | Ensure standard authentication helper (e.g. ensure_admin_assigns) is invoked in mount/3. |
| T-16-02   | Tampering| Router                 | mitigate | Rely on Phoenix LiveView signed session/params. |
</threat_model>

<verification>
mix test test/phoenix/live_admin_metadata_test.exs passes.
</verification>

<success_criteria>
The metadata management skeleton exists, responds to URL-driven state, and route is accessible under the admin scope.
</success_criteria>

<output>
After completion, create `.planning/phases/16-metadata-management-ui/16-01-SUMMARY.md`
</output>