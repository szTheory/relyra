---
phase: 16-metadata-management-ui
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - lib/relyra/live_admin/connection_metadata_live.ex
  - lib/relyra/live_admin/connections_live.ex
  - test/phoenix/live_admin_metadata_test.exs
autonomous: true
requirements:
  - MDUI-02
must_haves:
  truths:
    - Operator can trigger a manual metadata refresh from the UI.
    - The UI does not freeze during the network request.
    - Operator sees typed feedback upon completion.
  artifacts:
    - path: lib/relyra/live_admin/connection_metadata_live.ex
      provides: start_async and handle_async callbacks for refresh
  key_links:
    - from: lib/relyra/live_admin/connection_metadata_live.ex
      to: Relyra.Metadata.Refresh
      via: start_async
      pattern: start_async.*Metadata\.Refresh\.refresh
---

<objective>
Implement non-blocking manual metadata refresh using LiveView's `start_async` functionality to provide immediate, typed feedback without freezing the UI process.

Purpose: Allow operators to manually poll the metadata source URL while keeping the UI responsive, avoiding heavy dependencies like Oban.
Output: "Refresh Metadata" button that triggers an async task, displays a loading state, and updates the UI with the result via flash messages.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-metadata-management-ui/16-RESEARCH.md
@.planning/phases/16-metadata-management-ui/16-PATTERNS.md
@.planning/phases/16-metadata-management-ui/16-02-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Async Manual Refresh Handling</name>
  <files>lib/relyra/live_admin/connection_metadata_live.ex, lib/relyra/live_admin/connections_live.ex, test/phoenix/live_admin_metadata_test.exs</files>
  <behavior>
    - Test 1: `refresh_metadata` event sets the UI to a loading state and calls `start_async`.
    - Test 2: Successful async result adds a success flash message and clears loading state.
    - Test 3: Failed async result adds an error flash message and clears loading state.
  </behavior>
  <action>
    In `ConnectionMetadataLive`, implement the `refresh_metadata` event handler. Use `start_async(:metadata_refresh, fn -> Relyra.Metadata.Refresh.refresh(...) end)` to trigger the refresh without blocking the LiveView process.
    Pass `repo: socket.assigns.relyra_admin_repo` and `req: socket.assigns.relyra_admin_req` (if available, or rely on defaults) as options to `refresh/2`.
    Implement `handle_async(:metadata_refresh, {:ok, result}, socket)` to handle the completion, updating flash messages and reloading the metadata stream. Set and clear a `@refresh_status` assign to toggle a loading spinner or disable the button in the UI.
    Ensure the UI displays a clear text indicator next to the "Refresh Metadata" button explaining that newly fetched trust material (like certificates) is not implicitly promoted and requires a manual rollover step.
    Remove any residual `refresh_metadata` event handlers from `ConnectionsLive`.
    Update tests to verify the async behavior handles success and error cases.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_metadata_test.exs</automated>
  </verify>
  <done>Clicking "Refresh Metadata" triggers an async task without freezing the UI, providing success/error feedback once the network call completes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Admin API | Authenticated operator initiating a network call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04   | Denial of Service | start_async | mitigate | LiveView natively manages async tasks and prevents rogue process accumulation. Metadata refresh relies on `Req` which has built-in timeouts. |
</threat_model>

<verification>
mix test passes. The refresh process correctly shows loading states and handles errors without crashing the LiveView.
</verification>

<success_criteria>
Manual metadata refresh operates asynchronously and presents clear results to the operator.
</success_criteria>

<output>
After completion, create `.planning/phases/16-metadata-management-ui/16-03-SUMMARY.md`
</output>