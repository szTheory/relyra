---
phase: 68-build-caching-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/ledger_loop/Dockerfile.dev
  - demo/ledger_loop/docker-entrypoint.sh
  - .dockerignore
autonomous: true
requirements: [DKR-01, DKR-03]
user_setup: []

must_haves:
  truths:
    - "demo/ledger_loop/Dockerfile.dev builds from the pinned base hexpm/elixir:1.15.7-erlang-26.1.2-alpine-3.18.4 (D-06) and copies ONLY mix.exs+mix.lock before fetching deps, so a source-only edit leaves the dependency layer CACHED on rebuild (DKR-01)."
    - "demo/ledger_loop/docker-entrypoint.sh exists, is executable, and implements the lock-hash gate (sha256 of mix.lock vs _build/.docker/mix.lock.sha) plus the ecto ordering ledger_loop.relyra.migrate BEFORE ecto.migrate, then exec \"$@\" (D-08/D-09, DKR-03)."
    - "A repo-root .dockerignore excludes .git/ and .planning/ so secrets/planning docs never enter the build context (D-07)."
  artifacts:
    - path: "demo/ledger_loop/Dockerfile.dev"
      provides: "Cached dev image: pinned FROM, apk deps, COPY mix.exs+mix.lock first, BuildKit cache-mount deps.get/deps.compile, COPY+chmod entrypoint, ENTRYPOINT"
      contains: "syntax=docker/dockerfile:1.7"
    - path: "demo/ledger_loop/docker-entrypoint.sh"
      provides: "Boot gate: local.hex/rebar --if-missing, lock-hash dep gate, ecto.create, relyra.migrate, ecto.migrate, seeds, exec \"$@\""
      contains: "mix.lock.sha"
    - path: ".dockerignore"
      provides: "Repo-root build-context exclusions (.git, .planning, _build, deps, node_modules, etc.)"
      contains: ".planning/"
  key_links:
    - from: "demo/ledger_loop/Dockerfile.dev"
      to: "demo/ledger_loop/docker-entrypoint.sh"
      via: "COPY demo/ledger_loop/docker-entrypoint.sh into the image + ENTRYPOINT referencing it"
      pattern: "ENTRYPOINT.*docker-entrypoint"
  prohibitions:
    - "Dockerfile.dev MUST NOT contain `COPY . .` (D-01 — source arrives via bind mount; relyra path dep compiles at runtime)."
    - "Dockerfile.dev MUST NOT compile the relyra path dep at build time (no `mix compile` of the parent; only the Hex deps tree)."
    - "The pinned base image FROM line MUST NOT be bumped off 1.15.7-erlang-26.1.2-alpine-3.18.4 (D-06)."
    - "No file under lib/ or test/ is created or modified by this plan (milestone invariant)."
---

<objective>
Author the cached, arch-correct container build for the `demo/ledger_loop` Phoenix app: a dedicated `Dockerfile.dev` that compiles ONLY the Hex deps tree behind a COPY-before-source dependency layer with BuildKit cache mounts, the `docker-entrypoint.sh` boot gate it installs (lock-hash dep re-resolution gate + correct ecto migration ordering), and the repo-root `.dockerignore` that scopes the build context.

Purpose: A source-only edit must never invalidate the dependency layer (DKR-01), and the entrypoint must own dep re-resolution and DB setup idempotently (DKR-03, authored here, exercised under volumes in Plan 02).

Output: `demo/ledger_loop/Dockerfile.dev`, `demo/ledger_loop/docker-entrypoint.sh` (executable), repo-root `.dockerignore`.

**Milestone invariant (HARD):** demo + docker only — ZERO changes to `lib/`, `test/`, public API, behaviour callbacks, protocol surface, or the Hex package whitelist (`mix.exs` `package.files`). Every file this plan touches lives under `demo/ledger_loop/` or the repo root (`.dockerignore`). Final check: `git diff --stat` shows NO `lib/` or `test/` path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-build-caching-correctness/68-CONTEXT.md
@.planning/phases/68-build-caching-correctness/68-RESEARCH.md
@.planning/phases/68-build-caching-correctness/68-VALIDATION.md

# Source-of-truth files this plan reads/derives from:
@docker-compose.yml
@demo/ledger_loop/mix.exs
@demo/ledger_loop/lib/mix/tasks/ledger_loop.relyra.migrate.ex
@demo/ledger_loop/.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write demo/ledger_loop/Dockerfile.dev (cached dep layer, pinned base)</name>
  <files>demo/ledger_loop/Dockerfile.dev</files>
  <read_first>
    - .planning/phases/68-build-caching-correctness/68-RESEARCH.md (§Code Examples "Dockerfile.dev", §Standard Stack apk list, Pitfall 2/4)
    - .planning/phases/68-build-caching-correctness/68-CONTEXT.md (D-01, D-03, D-06)
    - demo/ledger_loop/mix.exs (the `{:relyra, path: "../.."}` path dep — proves why no build-time relyra compile)
    - docker-compose.yml (the inline `image:`/`command:` block being replaced; `working_dir: /app/demo/ledger_loop`)
  </read_first>
  <action>
    Create demo/ledger_loop/Dockerfile.dev per D-01/D-03/D-06 and RESEARCH §Code Examples. First line MUST be the syntax directive for Dockerfile 1.7 so BuildKit cache mounts parse (Pitfall 2). FROM the pinned base hexpm/elixir:1.15.7-erlang-26.1.2-alpine-3.18.4 (D-06 — do not bump). `apk add --no-cache` the package set from D-03: inotify-tools bash postgresql-client build-base git openssl (postgresql-client is a virtual provide of postgresql15-client in Alpine 3.18 — do NOT "fix" the 404, per Pitfall 4; a one-line comment noting it provides the PG15 client is fine). Set ENV MIX_ENV=dev and WORKDIR /app/demo/ledger_loop. COPY ONLY the two mix files first — `COPY demo/ledger_loop/mix.exs demo/ledger_loop/mix.lock ./` (paths are repo-root-relative because the build context is the repo root; the compose `build.context` in Plan 02 will be `.`). Then a single RUN with two BuildKit cache mounts (`--mount=type=cache,id=relyra-hex,target=/root/.hex,sharing=locked` and `--mount=type=cache,id=relyra-rebar3,target=/root/.cache/rebar3,sharing=locked`) that runs `mix local.hex --force && mix local.rebar --force && mix deps.get && mix deps.compile`. This compiles ONLY the Hex deps tree (the relyra path dep is absent from mix.lock and compiles at runtime via the entrypoint). After the dep layer, COPY demo/ledger_loop/docker-entrypoint.sh to /usr/local/bin/docker-entrypoint.sh, `chmod +x` it, and set `ENTRYPOINT ["/usr/local/bin/docker-entrypoint.sh"]`. Cache-mount id/sharing wording is planner/executor discretion per CONTEXT. Do NOT add `COPY . .` and do NOT compile the parent relyra lib at build time (D-01).
  </action>
  <acceptance_criteria>
    - `head -1 demo/ledger_loop/Dockerfile.dev` is exactly `# syntax=docker/dockerfile:1.7`.
    - `grep -c 'hexpm/elixir:1.15.7-erlang-26.1.2-alpine-3.18.4' demo/ledger_loop/Dockerfile.dev` returns 1 on the FROM line (pin preserved, D-06).
    - `grep -vE '^[[:space:]]*#' demo/ledger_loop/Dockerfile.dev | grep -cE '^[[:space:]]*COPY[[:space:]]+\. \.' ` returns 0 (no `COPY . .`, D-01 — comments excluded).
    - The mix-files COPY (`COPY demo/ledger_loop/mix.exs demo/ledger_loop/mix.lock ./`) appears in the file BEFORE the `RUN ... mix deps.get` line (COPY-before-source ordering).
    - `grep -c 'mount=type=cache' demo/ledger_loop/Dockerfile.dev` returns 2 (Hex + rebar3 cache mounts present).
    - `grep -c 'docker-entrypoint.sh' demo/ledger_loop/Dockerfile.dev` returns at least 2 (the COPY and the ENTRYPOINT).
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^# syntax=docker/dockerfile:1.7' demo/ledger_loop/Dockerfile.dev && grep -q 'hexpm/elixir:1.15.7-erlang-26.1.2-alpine-3.18.4' demo/ledger_loop/Dockerfile.dev && [ "$(grep -vE '^[[:space:]]*#' demo/ledger_loop/Dockerfile.dev | grep -cE 'COPY[[:space:]]+\. \.')" -eq 0 ] && [ "$(grep -c 'mount=type=cache' demo/ledger_loop/Dockerfile.dev)" -eq 2 ] && echo PASS</automated>
  </verify>
  <done>Dockerfile.dev exists with the 1.7 syntax header, the pinned FROM, the D-03 apk list, a COPY-before-source mix-files layer, two BuildKit cache mounts running deps.get+deps.compile, and a COPY+chmod+ENTRYPOINT for docker-entrypoint.sh. No `COPY . .`, no build-time relyra compile.</done>
</task>

<task type="auto">
  <name>Task 2: Write demo/ledger_loop/docker-entrypoint.sh (lock-hash gate + ecto ordering)</name>
  <files>demo/ledger_loop/docker-entrypoint.sh</files>
  <read_first>
    - .planning/phases/68-build-caching-correctness/68-RESEARCH.md (§Code Examples "docker-entrypoint.sh", Pitfall 5 ecto ordering, Pitfall 6 stamp location)
    - .planning/phases/68-build-caching-correctness/68-CONTEXT.md (D-08, D-09, Specifics — stamp path `_build/.docker/mix.lock.sha`)
    - demo/ledger_loop/mix.exs (the `ecto.setup` alias order: ecto.create → ledger_loop.relyra.migrate → ecto.migrate → run priv/repo/seeds.exs — the entrypoint MUST match this order)
    - demo/ledger_loop/lib/mix/tasks/ledger_loop.relyra.migrate.ex (the custom task the entrypoint runs before ecto.migrate; accepts --quiet)
  </read_first>
  <action>
    Create demo/ledger_loop/docker-entrypoint.sh per D-08/D-09 and RESEARCH §Code Examples. Shebang `#!/usr/bin/env bash` with `set -euo pipefail` (bash is in the D-03 apk list). Steps in order: (1) `mix local.hex --force --if-missing` and `mix local.rebar --force --if-missing`. (2) Lock-hash gate — set STAMP to `_build/.docker/mix.lock.sha` (inside the relyra_build named volume so it survives down/up but is wiped by volume rm, per Pitfall 6), compute the current sha256 of mix.lock via `sha256sum mix.lock | awk '{print $1}'`, `mkdir -p "$(dirname "$STAMP")"`, and if the stamp is missing OR its contents differ from current, run `mix deps.get` then `mix deps.compile` and rewrite the stamp with the current hash. The relyra path dep compiles implicitly on the first mix task that touches it. (3) `mix ecto.create --quiet || true`. (4) `mix ledger_loop.relyra.migrate` — this MUST run BEFORE ecto.migrate (Pitfall 5: relyra's audit/connection/replay tables; skipping it 500s every SAML/admin path). (5) `mix ecto.migrate`. (6) `mix run priv/repo/seeds.exs` (idempotent Reset.reset!(), safe every boot). (7) `exec "$@"` so the compose `command` (mix phx.server, set in Plan 02) becomes PID 1 with clean signals (D-09). After writing, make it executable: `chmod +x demo/ledger_loop/docker-entrypoint.sh`. Shell style is executor discretion provided D-08 ordering holds.
  </action>
  <acceptance_criteria>
    - `test -x demo/ledger_loop/docker-entrypoint.sh` succeeds (file is executable).
    - The line invoking `ledger_loop.relyra.migrate` appears BEFORE the line invoking `ecto.migrate` (verified by line-number comparison) — Pitfall 5 ordering.
    - `grep -c '_build/.docker/mix.lock.sha' demo/ledger_loop/docker-entrypoint.sh` returns at least 1 (stamp path inside the build volume, Pitfall 6).
    - `grep -c 'exec "\$@"' demo/ledger_loop/docker-entrypoint.sh` returns 1 (PID-1 handoff, D-09).
    - `grep -c 'sha256sum mix.lock' demo/ledger_loop/docker-entrypoint.sh` returns at least 1 (lock-hash gate present).
    - `bash -n demo/ledger_loop/docker-entrypoint.sh` exits 0 (valid bash syntax).
  </acceptance_criteria>
  <verify>
    <automated>chmod +x demo/ledger_loop/docker-entrypoint.sh; bash -n demo/ledger_loop/docker-entrypoint.sh && test -x demo/ledger_loop/docker-entrypoint.sh && relyra_ln=$(grep -n 'ledger_loop.relyra.migrate' demo/ledger_loop/docker-entrypoint.sh | head -1 | cut -d: -f1) && ecto_ln=$(grep -n 'ecto.migrate' demo/ledger_loop/docker-entrypoint.sh | grep -v relyra | head -1 | cut -d: -f1) && [ "$relyra_ln" -lt "$ecto_ln" ] && grep -q '_build/.docker/mix.lock.sha' demo/ledger_loop/docker-entrypoint.sh && grep -q 'exec "$@"' demo/ledger_loop/docker-entrypoint.sh && echo PASS</automated>
  </verify>
  <done>docker-entrypoint.sh is executable, passes `bash -n`, runs local.hex/rebar --if-missing, gates dep re-resolution on the mix.lock sha stamp at `_build/.docker/mix.lock.sha`, runs ecto.create → ledger_loop.relyra.migrate → ecto.migrate → seeds in that exact order, and ends with `exec "$@"`.</done>
</task>

<task type="auto">
  <name>Task 3: Write repo-root .dockerignore (build-context exclusions)</name>
  <files>.dockerignore</files>
  <read_first>
    - .planning/phases/68-build-caching-correctness/68-RESEARCH.md (§Code Examples ".dockerignore", Security Domain "Build context leaks", D-07 placement note)
    - .planning/phases/68-build-caching-correctness/68-CONTEXT.md (D-07)
    - demo/ledger_loop/.gitignore (basis for the artifact dirs to exclude)
  </read_first>
  <action>
    Create a SINGLE repo-root `.dockerignore` (NOT a demo-dir file — Docker resolves `.dockerignore` relative to the build-context root, which is the repo root because the path dep spans `../..`; a demo-dir file would be silently inert, D-07). Exclude at minimum, per D-07 and RESEARCH §Code Examples: `.git/`, `.planning/`, `_build/`, `deps/`, `demo/ledger_loop/_build/`, `demo/ledger_loop/deps/`, `node_modules/`, `priv/static/assets/`, `docker-compose*.yml`, `*.tar`, and OS cruft (`.DS_Store`, `erl_crash.dump`). The `.git/` and `.planning/` exclusions are security-relevant: they keep the git history and planning docs out of the build context so nothing sensitive leaks into image layers (Security Domain "Build context leaks").
  </action>
  <acceptance_criteria>
    - `.dockerignore` exists at the repo root (`test -f .dockerignore`).
    - `grep -c '^\.git/' .dockerignore` returns 1 (git history excluded — security).
    - `grep -c '^\.planning/' .dockerignore` returns 1 (planning docs excluded — security).
    - All of these patterns are present (one grep each returns >=1): `_build/`, `deps/`, `node_modules/`, `docker-compose`, `*.tar`.
  </acceptance_criteria>
  <verify>
    <automated>test -f .dockerignore && grep -q '^\.git/' .dockerignore && grep -q '^\.planning/' .dockerignore && grep -q '_build/' .dockerignore && grep -q 'deps/' .dockerignore && grep -q 'node_modules/' .dockerignore && grep -q 'docker-compose' .dockerignore && grep -q '\*\.tar' .dockerignore && echo PASS</automated>
  </verify>
  <done>Repo-root .dockerignore exists and excludes .git/, .planning/, _build/, deps/ (root + demo), node_modules/, priv/static/assets/, docker-compose*.yml, *.tar, and OS cruft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → Docker build context | Files copied/visible to `docker build` can be baked into image layers. The `.dockerignore` is the only control scoping what crosses this boundary. |
| host filesystem → container (build time) | Only `mix.exs`/`mix.lock` are COPYed in at build time (D-01); no source, no secrets. |

NOTE: This plan changes NO `lib/` crypto, parse, replay, audit, auth, or protocol code (milestone invariant). Relyra's verification seams are untouched. The honest threat surface is the dev build context only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Information Disclosure | Docker build context (`.git/`, `.planning/` entering image layers) | mitigate | Repo-root `.dockerignore` (Task 3) excludes `.git/` and `.planning/` so git history and planning docs never enter the build context or image layers (D-07). Verified by acceptance criteria grepping both exclusions. |
| T-68-02 | Tampering | Base image provenance | mitigate | FROM is pinned to an exact tag `hexpm/elixir:1.15.7-erlang-26.1.2-alpine-3.18.4` (D-06); no floating `:latest`. Acceptance criterion asserts the pin. |
| T-68-03 | Tampering | OS package installs (apk add) | accept | Dev-only image; apk packages audited in RESEARCH §Package Legitimacy Audit (all Alpine 3.18 main, OK). No language-package (Hex/npm) installs are added by this phase; `mix.exs` deps are unchanged and already locked. Low-value dev target, no production exposure. |
| T-68-04 | Information Disclosure | Dev secrets baked into an image layer | accept | No secrets are COPYed at build time (only `mix.exs`/`mix.lock`, D-01). The pre-existing dev-only `POSTGRES_PASSWORD`/`secret_key_base` live in compose env, not in any image layer; not introduced or worsened here. |

No high-severity threats present. This is dev/demo Docker tooling with no trust-boundary, crypto, auth, or `lib/` change; the only security-relevant control is the `.dockerignore` build-context exclusion (T-68-01), which is enforced as a Task 3 acceptance criterion.
</threat_model>

<verification>
## Phase-level (manual Docker receipts — see VALIDATION.md)

This plan produces the build inputs; the build receipt is run here, the runtime receipts (DKR-02/03/04) are run in Plan 02 once compose wires the volumes.

**DKR-01 receipt (run on macOS host with BuildKit/Compose v2):**
1. `docker compose --profile core build` — first build compiles the dep layer (`mix deps.get`/`deps.compile` runs).
2. Edit any `.ex` source file under `demo/ledger_loop/lib/` (NOT mix.exs/mix.lock).
3. `docker compose --profile core build` again — the dependency layer shows `CACHED`; `mix deps.get` does NOT re-run (the COPY-before-source split + cache mounts keep it cached). DKR-01 satisfied.

NOTE: the compose `build:`/`command:`/`volumes:` wiring that makes `docker compose build` target Dockerfile.dev is authored in Plan 02. Until then, an equivalent direct check is `DOCKER_BUILDKIT=1 docker build -f demo/ledger_loop/Dockerfile.dev -t relyra-demo-dev .` from the repo root (build context `.`).

**Invariant check (every task):** `git diff --stat` shows NO path under `lib/` or `test/` — only `demo/ledger_loop/Dockerfile.dev`, `demo/ledger_loop/docker-entrypoint.sh`, and `.dockerignore`.
</verification>

<success_criteria>
- demo/ledger_loop/Dockerfile.dev: 1.7 syntax header, pinned FROM (D-06), D-03 apk list, COPY-before-source mix-files layer, two BuildKit cache mounts running deps.get+deps.compile, COPY+chmod+ENTRYPOINT for the entrypoint. NO `COPY . .`, NO build-time relyra compile.
- demo/ledger_loop/docker-entrypoint.sh: executable, valid bash, local.hex/rebar --if-missing, lock-hash gate at `_build/.docker/mix.lock.sha`, ecto.create → ledger_loop.relyra.migrate → ecto.migrate → seeds in order, ends with `exec "$@"`.
- repo-root .dockerignore: excludes .git/, .planning/, _build/, deps/ (root + demo), node_modules/, priv/static/assets/, docker-compose*.yml, *.tar, OS cruft.
- DKR-01 build receipt passes: source edit → dep layer CACHED, no deps.get.
- `git diff --stat` shows no `lib/` or `test/` path (milestone invariant).
</success_criteria>

<output>
Create `.planning/phases/68-build-caching-correctness/68-01-SUMMARY.md` when done.
</output>
