---
phase: 27-adopter-onboarding-polish-and-case-studies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/getting_started.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "A new adopter sees one obvious Day-1 route from README into Getting Started, not competing entry points."
    - "The docs claim batteries-included support only for Okta, Microsoft Entra ID, and Google Workspace."
    - "Each Day-1 stage ends with a concrete success receipt before the reader moves on."
  artifacts:
    - path: "README.md"
      provides: "Canonical router into the Day-1 spine"
      min_lines: 40
    - path: "guides/getting_started.md"
      provides: "Ordered onboarding narrative with explicit branch to one provider runbook"
      min_lines: 80
  key_links:
    - from: "README.md"
      to: "guides/getting_started.md"
      via: "top-level Getting Started pointer"
      pattern: "guides/getting_started\\.md"
    - from: "guides/getting_started.md"
      to: "guides/recipes/(okta|entra|google_workspace)\\.md"
      via: "provider branch section"
      pattern: "guides/recipes/"
---

<objective>
Establish the canonical Day-1 onboarding information architecture for Phase 27.

Purpose: Make the first adopter path obvious, ordered, and exact-claims only.
Output: Updated `README.md` and `guides/getting_started.md` that route install -> scaffold -> local proof -> provider runbook -> production tail.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.0-REQUIREMENTS.md
@.planning/phases/27-adopter-onboarding-polish-and-case-studies/27-CONTEXT.md
@.planning/phases/27-adopter-onboarding-polish-and-case-studies/27-RESEARCH.md
@README.md
@guides/getting_started.md
@SECURITY.md

<interfaces>
From lib/relyra/provider.ex:
```elixir
@presets %{
  okta: Relyra.Provider.Okta,
  entra: Relyra.Provider.Entra,
  google_workspace: Relyra.Provider.GoogleWorkspace
}

@spec list() :: [atom()]
def list, do: Map.keys(@presets) |> Enum.sort()

@spec guide_url(atom()) :: String.t()
def guide_url(preset_id) when is_atom(preset_id) do
  fetch!(preset_id).guide_url()
end
```

From lib/relyra/test_support.ex:
```elixir
defdelegate fake_idp_metadata(), to: Relyra.TestSupport.FakeIdP, as: :metadata
defdelegate build_saml_response(opts \\ []), to: Relyra.TestSupport.FakeIdP, as: :build_response
defdelegate sign_saml_response(builder, opts \\ []), to: Relyra.TestSupport.FakeIdP, as: :sign
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework README into the canonical router</name>
  <files>README.md</files>
  <action>Audit and rewrite every adopter-facing support-positioning section in `README.md`, not just the Day-1 slice, so the file implements D-01, D-02, D-06, and D-10 exactly. The README must expose one obvious ordered route, limit batteries-included support to Okta / Microsoft Entra ID / Google Workspace, and move every non-first-class provider claim under explicit custom/generic SAML or not-yet-shipped language. Keep advanced operations lower in the file or as links so they do not compete with onboarding, and remove or rewrite any stale provider coverage wording that could leave broader support claims behind outside the Day-1 section.</action>
  <verify><automated>rg -n "guides/getting_started.md|Batteries included|custom SAML|not yet shipped|Okta|Microsoft Entra ID|Google Workspace" README.md && ! rg -n "Ping|OneLogin|ADFS|Shibboleth|Keycloak" README.md</automated></verify>
  <done>`README.md` routes directly into `guides/getting_started.md`, names the supported-provider taxonomy honestly, and no longer reads like a second full onboarding guide.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Getting Started into the ordered Day-1 spine</name>
  <files>guides/getting_started.md</files>
  <action>Rewrite `guides/getting_started.md` per D-01, D-03, D-04, D-05, D-07, and D-08 so the guide has one falsifiable structure rather than a loose narrative. Use explicit numbered sections in adopter order: `1. Install`, `2. Scaffold`, `3. Prove local login with FakeIdP`, `4. Choose one provider runbook`, and `5. Production follow-ons`. End each major section with a `Receipt` marker or equivalent literal success checkpoint, frame LiveAdmin as optional and late, require exactly one provider-branch section that links only to the three first-class runbooks before returning to the production tail, and place all non-first-class IdPs under a generic/custom SAML fallback instead of peer support claims.</action>
  <verify><automated>rg -n "^## 1\\. Install|^## 2\\. Scaffold|^## 3\\. Prove local login with FakeIdP|^## 4\\. Choose one provider runbook|^## 5\\. Production follow-ons" guides/getting_started.md && test \"$(rg -c \"Receipt\" guides/getting_started.md)\" -ge 5 && test \"$(rg -o \"guides/recipes/(okta|entra|google_workspace)\\.md\" guides/getting_started.md | wc -l | tr -d ' ')\" -eq 3 && ! rg -n \"guides/recipes/(ping|onelogin|adfs|shibboleth|keycloak)\\.md|^## (Okta|Microsoft Entra ID|Google Workspace)$\" guides/getting_started.md</automated></verify>
  <done>`guides/getting_started.md` becomes the canonical narrative spine, includes concrete success checkpoints at each stage, and branches to exactly one verified provider runbook before returning to production follow-ons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter -> docs | Readers may make trust-boundary decisions based on documentation claims |
| docs -> support contract | Public wording can overstate shipped provider coverage if not constrained |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | S | `README.md` support matrix | mitigate | Limit batteries-included claims to the three preset modules and link generic/custom SAML separately per D-06/D-07/D-10 |
| T-27-02 | T | `guides/getting_started.md` onboarding flow | mitigate | End each major step with a literal receipt or command so prose cannot drift into unverifiable promises per D-05/D-17 |
| T-27-03 | I | optional admin/ops surfaces | mitigate | Frame LiveAdmin and day-2 operations as optional later-stage surfaces so adopters do not mistake them for mandatory trust seams per D-04 |
</threat_model>

<verification>
- `rg -n "guides/getting_started.md|Batteries included|custom SAML|not yet shipped|Okta|Microsoft Entra ID|Google Workspace" README.md && ! rg -n "Ping|OneLogin|ADFS|Shibboleth|Keycloak" README.md`
- `rg -n "^## 1\\. Install|^## 2\\. Scaffold|^## 3\\. Prove local login with FakeIdP|^## 4\\. Choose one provider runbook|^## 5\\. Production follow-ons" guides/getting_started.md`
- `test "$(rg -c "Receipt" guides/getting_started.md)" -ge 5`
- `test "$(rg -o "guides/recipes/(okta|entra|google_workspace)\\.md" guides/getting_started.md | wc -l | tr -d ' ')" -eq 3`
- `! rg -n "guides/recipes/(ping|onelogin|adfs|shibboleth|keycloak)\\.md|^## (Okta|Microsoft Entra ID|Google Workspace)$" guides/getting_started.md`
</verification>

<success_criteria>
- The README top section is a router, not a competing guide.
- The Getting Started guide expresses one ordered Day-1 path with explicit receipts.
- Support claims match the shipped preset registry exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/27-adopter-onboarding-polish-and-case-studies/27-01-SUMMARY.md`
</output>
