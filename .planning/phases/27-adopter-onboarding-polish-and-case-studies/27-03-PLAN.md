---
phase: 27-adopter-onboarding-polish-and-case-studies
plan: 03
type: execute
wave: 3
depends_on:
  - 27-01
  - 27-02
files_modified:
  - guides/batteries_included.md
  - BATTERIES_INCLUDED.md
  - lib/mix/tasks/relyra.batteries_included.ex
  - test/mix/tasks/relyra_batteries_included_test.exs
  - test/mix/relyra_install_test.exs
  - test/test_support_demo_test.exs
  - mix.exs
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "The repo contains one first-class batteries-included proof journey that starts local-first with FakeIdP and ends in observable receipts."
    - "The batteries-included claim is backed by focused executable tests and a drift-checkable artifact."
    - "CI enforces the proof lane so the docs claim cannot silently drift."
  artifacts:
    - path: "guides/batteries_included.md"
      provides: "Human-facing proof journey"
      min_lines: 70
    - path: "BATTERIES_INCLUDED.md"
      provides: "Generated or drift-checked proof receipt artifact"
      min_lines: 40
    - path: "lib/mix/tasks/relyra.batteries_included.ex"
      provides: "Proof artifact generator/checker"
      exports: ["run"]
    - path: "test/mix/tasks/relyra_batteries_included_test.exs"
      provides: "Task-level drift and output tests"
    - path: "mix.exs"
      provides: "Docs-proof CI alias wiring"
  key_links:
    - from: "guides/batteries_included.md"
      to: "BATTERIES_INCLUDED.md"
      via: "proof/evidence handoff"
      pattern: "BATTERIES_INCLUDED\\.md"
    - from: "lib/mix/tasks/relyra.batteries_included.ex"
      to: "BATTERIES_INCLUDED.md"
      via: "rendered markdown output"
      pattern: "BATTERIES_INCLUDED\\.md"
    - from: "mix.exs"
      to: "test/mix/relyra_install_test.exs"
      via: "docs proof lane"
      pattern: "relyra_install_test\\.exs"
---

<objective>
Create the executable batteries-included proof journey, back it with focused tests, and wire it into CI.

Purpose: Turn the Day-1 adoption promise into a falsifiable artifact that survives drift.
Output: One proof guide, one drift-checkable evidence artifact, focused proof tests, and CI alias enforcement.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-adopter-onboarding-polish-and-case-studies/27-CONTEXT.md
@.planning/phases/27-adopter-onboarding-polish-and-case-studies/27-RESEARCH.md
@CONFORMANCE.md
@SECURITY_REVIEW_EVIDENCE.md
@mix.exs
@lib/mix/tasks/relyra.install.ex
@lib/relyra/test_support.ex
@lib/relyra/test_support/fake_idp.ex
@test/mix/relyra_install_test.exs
@test/test_support_demo_test.exs

<interfaces>
From lib/mix/tasks/relyra.install.ex:
```elixir
def run(args) do
  OptionParser.parse(args,
    switches: [module: :string, router: :string, repo: :string, live_admin: :boolean,
               admin_path: :string, no_config: :boolean, force: :boolean]
  )
end
```

From lib/relyra/test_support.ex:
```elixir
@spec setup_saml_connection(Plug.Conn.t(), keyword()) :: Plug.Conn.t()
@spec post_saml_response(Plug.Conn.t(), String.t(), keyword()) :: Plug.Conn.t()
@spec saml_login(Plug.Conn.t()) :: {:ok, term()} | {:error, atom()}
```

From lib/relyra/test_support/fake_idp.ex:
```elixir
@spec metadata() :: String.t()
@spec build_response(keyword()) :: Builder.t()
@spec sign(Builder.t() | keyword(), keyword()) :: String.t()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the drift-checkable batteries-included evidence artifact</name>
  <files>lib/mix/tasks/relyra.batteries_included.ex, test/mix/tasks/relyra_batteries_included_test.exs, BATTERIES_INCLUDED.md</files>
  <behavior>
    - Test 1: `mix relyra.batteries_included --output tmp/file.md` renders deterministic markdown with claim -> seam -> proof command rows.
    - Test 2: `mix relyra.batteries_included --check` passes when `BATTERIES_INCLUDED.md` matches generated content and fails on drift.
    - Test 3: The artifact includes local-first proof, supported-provider scope, and day-2 receipts rather than marketing language.
  </behavior>
  <action>Implement a new Mix task modeled on the existing `relyra.conformance` / `relyra.security_review` pattern per D-15, D-17, D-18, and the research recommendation. Generate or drift-check `BATTERIES_INCLUDED.md` with exact claim -> seam -> proof command -> artifact mappings. The report must start with the local-first `FakeIdP` path and keep batteries-included scope limited to the three first-class providers.</action>
  <verify><automated>mix test test/mix/tasks/relyra_batteries_included_test.exs --warnings-as-errors && mix relyra.batteries_included --check</automated></verify>
  <done>A checked-in `BATTERIES_INCLUDED.md` exists, is generated from repo state, and fails closed on drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the human-facing proof guide and strengthen the local-first proof lane</name>
  <files>guides/batteries_included.md, test/mix/relyra_install_test.exs, test/test_support_demo_test.exs, mix.exs</files>
  <behavior>
    - Test 1: The install proof test asserts the blessed scaffold path creates the host surface the proof guide references.
    - Test 2: The FakeIdP demo test proves the first successful local round trip the guide asks adopters to run.
    - Test 3: The CI alias runs the focused docs-proof lane and the artifact drift check without broadening ordinary `mix test`.
  </behavior>
  <action>Create `guides/batteries_included.md` as the first-class human journey per D-15 and D-16: install, Phoenix mount, local `FakeIdP` proof, one provider preset branch, optional admin surface, metadata/certificate lifecycle, audit/telemetry, scheduled refresh, and diagnostic bundle. Strengthen the existing install/demo tests so the guide’s first receipts stay executable. Wire a narrow `ci.docs` alias in `mix.exs` and call it from an appropriate existing CI lane so the proof guide, focused tests, and `mix relyra.batteries_included --check` stay enforced.</action>
  <verify><automated>mix test test/mix/relyra_install_test.exs test/test_support_demo_test.exs --warnings-as-errors && rg -n "FakeIdP|mix relyra.install|Okta|Microsoft Entra ID|Google Workspace|audit|telemetry|scheduled refresh|diagnostic" guides/batteries_included.md && mix ci.docs</automated></verify>
  <done>The repo exposes one first-class proof journey with executable receipts, and CI enforces the guide plus focused proof lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof docs -> release claim | The v1.0 batteries-included promise depends on these artifacts staying exact |
| generated artifact -> CI | Drift between docs and executable state must fail closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | T | `BATTERIES_INCLUDED.md` | mitigate | Generate or check the artifact from repo state with a dedicated Mix task and `--check` drift mode |
| T-27-08 | R | `guides/batteries_included.md` | mitigate | Tie each major claim to a literal receipt, focused test, or generated artifact so the proof journey remains auditable |
| T-27-09 | D | CI docs lane | mitigate | Keep the lane narrow (`ci.docs`) and file-scoped so enforcement stays reliable without destabilizing unrelated suites |
</threat_model>

<verification>
- `mix test test/mix/tasks/relyra_batteries_included_test.exs --warnings-as-errors`
- `mix relyra.batteries_included --check`
- `mix test test/mix/relyra_install_test.exs test/test_support_demo_test.exs --warnings-as-errors`
- `mix ci.docs`
</verification>

<success_criteria>
- The first proof receipt is local-first via `Relyra.TestSupport.FakeIdP`.
- The batteries-included claim is backed by focused tests plus a drift-checkable artifact.
- CI fails if the proof lane or artifact goes stale.
</success_criteria>

<output>
After completion, create `.planning/phases/27-adopter-onboarding-polish-and-case-studies/27-03-SUMMARY.md`
</output>
