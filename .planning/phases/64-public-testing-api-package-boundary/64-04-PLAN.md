---
phase: 64-public-testing-api-package-boundary
plan: 04
type: execute
wave: 3
depends_on:
  - 64-01
  - 64-02
  - 64-03
files_modified:
  - test/mix/tasks/verify_release_parity_test.exs
autonomous: true
requirements:
  - TEST-01
  - PKG-01
must_haves:
  truths:
    - "Package tests prove `lib/relyra/testing*` files ship."
    - "Package tests prove `lib/relyra/test_support*` files remain excluded."
    - "Release parity filtering continues to use the existing package path mechanism."
    - "Local package proof inspects packaged file lists, not source-tree existence alone."
  artifacts:
    - path: "test/mix/tasks/verify_release_parity_test.exs"
      provides: "Package boundary tests for public testing API"
      contains: "lib/relyra/testing.ex"
  key_links:
    - from: "test/mix/tasks/verify_release_parity_test.exs"
      to: "lib/mix/tasks/verify.release_parity.ex"
      via: "`ReleaseParity.filter_package_paths/1`"
      pattern: "filter_package_paths"
    - from: "test/mix/tasks/verify_release_parity_test.exs"
      to: "mix.exs"
      via: "`Mix.Project.config()[:package][:files]`"
      pattern: "Mix\\.Project\\.config"
---

<objective>
Lock the package boundary for the new public testing API.

Purpose: Hex adopters must receive `lib/relyra/testing*` while private `lib/relyra/test_support*` remains excluded from production compilation and package files.
Output: Release parity tests that assert public testing inclusion and private support exclusion against package mechanisms.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-public-testing-api-package-boundary/64-CONTEXT.md
@.planning/phases/64-public-testing-api-package-boundary/64-RESEARCH.md
@.planning/phases/64-public-testing-api-package-boundary/64-VALIDATION.md
@.planning/phases/64-public-testing-api-package-boundary/64-PATTERNS.md
@mix.exs
@lib/mix/tasks/verify.release_parity.ex
@test/mix/tasks/verify_release_parity_test.exs

<interfaces>
Package boundary contracts:
- `mix.exs` package files are built from explicit root paths plus `package_lib_files/0`, which rejects paths containing `test_support`.
- `Mix.Tasks.Verify.ReleaseParity.filter_package_paths/1` rejects `test_support` paths and keeps package-scope `lib/`, `priv/repo/migrations/`, `docs/`, `guides/`, and root docs.
- `Mix.Tasks.Verify.ReleaseParity.paths_contain_test_support?/1` must continue to detect any `test_support` path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend release parity unit tests for public testing files</name>
  <files>test/mix/tasks/verify_release_parity_test.exs</files>
  <read_first>
    - test/mix/tasks/verify_release_parity_test.exs
    - .planning/phases/64-public-testing-api-package-boundary/64-CONTEXT.md
    - .planning/phases/64-public-testing-api-package-boundary/64-RESEARCH.md
    - mix.exs
    - lib/mix/tasks/verify.release_parity.ex
  </read_first>
  <behavior>
    - `ReleaseParity.filter_package_paths/1` keeps `lib/relyra/testing.ex` and files under `lib/relyra/testing/`.
    - `ReleaseParity.filter_package_paths/1` rejects `lib/relyra/test_support.ex` and files under `lib/relyra/test_support/`.
    - `Mix.Project.config()[:package][:files]` includes actual public testing files created by prior plans and contains no `test_support` path.
  </behavior>
  <action>Extend `test/mix/tasks/verify_release_parity_test.exs` per D-12 and D-13. In the existing `filter_package_paths/1` describe block, add public testing examples for `lib/relyra/testing.ex`, `lib/relyra/testing/fixture.ex`, `lib/relyra/testing/signer.ex`, `lib/relyra/testing/adapters.ex`, and `lib/relyra/testing/phoenix.ex`; assert these survive filtering while `lib/relyra/test_support.ex` and `lib/relyra/test_support/fake_idp.ex` are excluded. Add a package config test that reads `Mix.Project.config()[:package][:files]` and asserts all actual public testing file paths exist in that list and no entry contains `test_support`. Do not change `lib/mix/tasks/verify.release_parity.ex` filtering rules unless the tests expose a real failure in the existing mechanism.</action>
  <verify>
    <automated>mix test test/mix/tasks/verify_release_parity_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test/mix/tasks/verify_release_parity_test.exs` contains the exact strings `lib/relyra/testing.ex`, `lib/relyra/testing/fixture.ex`, `lib/relyra/testing/signer.ex`, `lib/relyra/testing/adapters.ex`, and `lib/relyra/testing/phoenix.ex`.
    - Behavior assertion: the package config test asserts `Enum.any?(package_files, &String.contains?(&1, "test_support"))` is false.
    - CLI assertion: `mix test test/mix/tasks/verify_release_parity_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Release parity unit tests prove public testing files are in package scope and private support files remain outside package scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add local unpacked package proof</name>
  <files>test/mix/tasks/verify_release_parity_test.exs</files>
  <read_first>
    - test/mix/tasks/verify_release_parity_test.exs
    - .planning/phases/64-public-testing-api-package-boundary/64-VALIDATION.md
    - mix.exs
    - lib/mix/tasks/verify.release_parity.ex
  </read_first>
  <behavior>
    - A local package build/unpack proof inspects unpacked files and finds public testing files.
    - The same proof finds no `lib/relyra/test_support*` files in the unpacked package.
  </behavior>
  <action>Add a local package-boundary test to `test/mix/tasks/verify_release_parity_test.exs` that creates a temporary directory, invokes `System.cmd("mix", ["hex.build", "--unpack", "-o", tmp], stderr_to_stdout: true)`, recursively lists regular files under the unpacked package root, and asserts the unpacked paths include every public testing file from Task 1 and no path containing `lib/relyra/test_support`. Keep the existing live Hex `@tag :integration` canary unchanged; this new proof must use the local package build for current source contents.</action>
  <verify>
    <automated>mix test test/mix/tasks/verify_release_parity_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the new test calls `System.cmd("mix", ["hex.build", "--unpack", "-o", tmp]`.
    - Behavior assertion: the test fails if any unpacked relative path contains `lib/relyra/test_support`.
    - Behavior assertion: the test fails if any expected `lib/relyra/testing*` file is absent from the unpacked package.
    - CLI assertion: `mix test test/mix/tasks/verify_release_parity_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Local package artifact proof confirms the package ships public testing files and excludes private support files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree -> Hex package files | Source files are filtered into package artifacts by `mix.exs` package configuration. |
| private test support -> public package | Private helper modules must not cross into package contents. |
| release parity tests -> package proof | Tests must inspect package mechanisms and local artifacts, not only source existence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T64-04 | Information Disclosure | `mix.exs` package files and release parity tests | mitigate | Assert package config and local unpacked package include `lib/relyra/testing*` and exclude all `lib/relyra/test_support*`. |
| T64-06 | Denial of Service | package dependency posture | mitigate | Package proof includes optional `lib/relyra/testing/phoenix.ex` without making core package checks depend on Phoenix-only runtime behavior. |
</threat_model>

<verification>
Run `mix test test/mix/tasks/verify_release_parity_test.exs --warnings-as-errors`. After the focused suite passes, run `mix hex.build --unpack -o /tmp/relyra_phase64_pkg_check` and inspect the file list for `lib/relyra/testing*` inclusion and `lib/relyra/test_support*` absence.
</verification>

<success_criteria>
- Release parity tests prove public testing files are package-visible.
- Release parity tests prove private support files remain package-excluded.
- Package proof uses the existing `mix.exs` whitelist and `verify.release_parity` filtering model.
</success_criteria>

<output>
Create `.planning/phases/64-public-testing-api-package-boundary/64-04-SUMMARY.md` when done.
</output>
