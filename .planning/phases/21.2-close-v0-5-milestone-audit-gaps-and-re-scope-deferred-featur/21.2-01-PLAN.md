---
phase: 21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
autonomous: true
# Phase 21.2 has no functional REQ-IDs (doc-only closure phase per CONTEXT.md additional_context)
requirements: []
commit: per-task
must_haves:
  truths:
    - "PROJECT.md ## Current Milestone v0.5 Target features lists exactly 2 bullets: bulk operations + scheduled metadata refresh (no Debug bundles, no Expiry alerts) — per D-21.2-04, D-21.2-05."
    - "PROJECT.md ### Active block no longer contains a DIAG-01 line — per D-21.2-04."
    - "PROJECT.md ### Validated v0.5 block contains both CFG-07 AND CFG-08 (CFG-07 promoted from Active) — per D-21.2-04."
    - "PROJECT.md ## Current Milestone Deferred-to-later-milestones v0.6 entry mentions both Debug bundles (DIAG-01) AND Expiry alerts (CERT-EXP-01) — per D-21.2-04, D-21.2-05."
    - "PROJECT.md footer Last updated line cites Phase 21.2 + 2026-05-07 — per D-21.2-16."
    - "PROJECT.md ## Key Decisions table gains a v0.5 closure-phase pattern row capturing the 21.1 + 21.2 extension (Site 6, REQUIRED per RESEARCH § Site 6 + D-21.2-01 — durable engineering DNA captured at peak salience)."
    - "Cross-doc coherence rows it owns: 'PROJECT.md no longer lists DIAG-01 in Active', 'PROJECT.md no longer lists Debug bundles in Target', 'DIAG-01 deferred consistently' (PROJECT.md half), 'CERT-EXP-01 deferred consistently' (PROJECT.md half)."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Re-scoped Active + Current Milestone Target/Deferred + Validated v0.5 + footer + Key Decisions table row"
      contains: "v0.5 Operational maturity"
      min_lines: 170
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "REQ-ID coherence (CFG-07 / CFG-08 / DIAG-01 / CERT-EXP-01)"
      pattern: "DIAG-01|CERT-EXP-01"
    - from: ".planning/PROJECT.md"
      to: ".planning/MILESTONE-ARC.md"
      via: "v0.6 carryover narrative coherence (Phase 21.2 re-scope)"
      pattern: "Phase 21.2"
---

<objective>
Re-scope `.planning/PROJECT.md` to reflect the v0.5 milestone audit closure: move
"Debug bundles" (DIAG-01) and "Expiry alerts" out of v0.5 Target features and Active
requirements; promote CFG-07 to Validated; refresh footer; record the v0.5
closure-phase pattern in the Key Decisions table.

Purpose: PROJECT.md is the milestone-scope source of truth (per Doc Authority Map in
21.2-RESEARCH.md). A re-scope that updates REQUIREMENTS.md / MILESTONE-ARC.md but
leaves PROJECT.md stale would re-introduce the v0.5 milestone audit `scope_drift` gap.

Output: a single mechanical edit pass on PROJECT.md touching 6 sites specified
verbatim in 21.2-RESEARCH.md § "1. PROJECT.md Re-scope".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.5-MILESTONE-AUDIT.md
@.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-CONTEXT.md
@.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-RESEARCH.md

<interfaces>
<!--
This is a doc-only edit. There are no code interfaces. The "interface" is the exact
shape of each text block being edited; verbatim before/after text is in the action
block below (lifted from 21.2-RESEARCH.md § 1).
-->
None — doc-only phase per D-21.2-03 (no `lib/` changes).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PROJECT.md — re-scope v0.5 (6 sites)</name>
  <files>.planning/PROJECT.md</files>

  <read_first>
    - .planning/PROJECT.md (full file — 175 lines; verify current state matches the "current text" quotes below before applying changes)
    - .planning/v0.5-MILESTONE-AUDIT.md (gap motivation — INT-01 closed by 21.1; INT-02 + scope_drift closed by this phase + sibling 21.2 plans)
    - .planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-CONTEXT.md (LOCKED decisions D-21.2-01, D-21.2-04, D-21.2-05, D-21.2-16)
    - .planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-RESEARCH.md § "1. PROJECT.md Re-scope" (verbatim diff shapes; THIS is the source of truth for current/target text)
  </read_first>

  <behavior>
    - D-21.2-04: DIAG-01 moves to v0.6 with REQ-ID intact; PROJECT.md ### Active loses the DIAG-01 line.
    - D-21.2-05: New REQ-ID CERT-EXP-01 surfaces in PROJECT.md's v0.6 deferred block alongside DIAG-01.
    - D-21.2-16: Phase verification = artifact deltas + cross-doc coherence (NOT a milestone re-audit). The Last-updated footer cites Phase 21.2.
    - D-21.2-01: Single-phase bundle ratifies the closure pattern; the Key Decisions row records this for posterity (Site 6 — required, not optional).
    - Cross-doc coherence rows owned by this plan (lifted verbatim from 21.2-RESEARCH.md § "Cross-Doc Coherence Invariants"):
      - `grep -E "^- \[ \] \*\*DIAG-01" .planning/PROJECT.md` MUST return 0 matches (DIAG-01 removed from Active).
      - `awk '/^## Current Milestone: v0.5/,/^## Requirements/' .planning/PROJECT.md | grep -c "Debug bundles"` MUST return 0 matches IN THE TARGET-FEATURES SLICE (Debug bundles still appears once in the Deferred sub-block of the same slice — see "Special note" in acceptance_criteria).
      - DIAG-01 + CERT-EXP-01 both grep-locatable in PROJECT.md (`grep -F "DIAG-01" .planning/PROJECT.md` ≥ 1; `grep -F "CERT-EXP-01" .planning/PROJECT.md` ≥ 1).
  </behavior>

  <action>
    Edit `.planning/PROJECT.md` at 6 sites. Each site's verbatim "current text" and
    "target text" is lifted from 21.2-RESEARCH.md § "1. PROJECT.md Re-scope". Apply
    each edit using the Edit tool with the strings below; do NOT paraphrase, re-flow
    paragraphs, or change unrelated lines.

    ────────────────────────────────────────────────────────────────────────────
    SITE 1 — `## Current State` block, line 20
    ────────────────────────────────────────────────────────────────────────────

    CURRENT TEXT (find this exact line):
    ```
    - **v0.5 in progress** — Operational maturity. Phase 20 (bulk operations, CFG-07), Phase 21 (scheduled metadata refresh, CFG-08), and Phase 21.1 (CFG-07 bulk-refresh audit correlation_id forwarding — closes v0.5 audit BLOCKER INT-01) shipped 2026-05-06 / 2026-05-07; Debug bundles and Expiry alerts remain.
    ```

    TARGET TEXT (replace with):
    ```
    - **v0.5 in progress** — Operational maturity. Phase 20 (bulk operations, CFG-07), Phase 21 (scheduled metadata refresh, CFG-08), Phase 21.1 (CFG-07 bulk-refresh audit correlation_id forwarding), and Phase 21.2 (audit-gap closure + scope re-alignment) shipped 2026-05-06 / 2026-05-07. Debug bundles (DIAG-01) and Expiry alerts (CERT-EXP-01) re-scoped to v0.6 per the v0.5 milestone audit.
    ```

    ────────────────────────────────────────────────────────────────────────────
    SITE 2 — `## Current Milestone: v0.5` Target features (lines 26-31)
    ────────────────────────────────────────────────────────────────────────────

    CURRENT TEXT (find this exact 6-line block including the heading line and the 4 bullets — the order and bullet text must match exactly):
    ```
    **Target features:**

    - **Bulk operations** — User can run operations (enable/disable/refresh) across multiple connections at once (CFG-07).
    - **Scheduled metadata refresh** — User can enable background refresh automation with security guardrails (CFG-08).
    - **Debug bundles** — Operator can export a redacted diagnostic bundle for a connection to facilitate support.
    - **Expiry alerts** — System emits events/logs for upcoming certificate expirations.
    ```

    TARGET TEXT (replace with — the Debug bundles + Expiry alerts bullets are removed):
    ```
    **Target features:**

    - **Bulk operations** — User can run operations (enable/disable/refresh) across multiple connections at once (CFG-07).
    - **Scheduled metadata refresh** — User can enable background refresh automation with security guardrails (CFG-08).
    ```

    ────────────────────────────────────────────────────────────────────────────
    SITE 2b — `## Current Milestone: v0.5` Deferred-to-later-milestones (lines 35-38)
    ────────────────────────────────────────────────────────────────────────────

    CURRENT TEXT:
    ```
    **Deferred to later milestones per arc:**

    - **v0.6** — SLO (Single Logout).
    - **v1.0** — External security review + conformance + docs polish.
    ```

    TARGET TEXT (v0.6 line gains DIAG-01 + CERT-EXP-01; v1.0 unchanged):
    ```
    **Deferred to later milestones per arc:**

    - **v0.6** — SLO (Single Logout) plus operational-maturity carryover: Debug bundles (DIAG-01) and Expiry alerts (CERT-EXP-01).
    - **v1.0** — External security review + conformance + docs polish.
    ```

    ────────────────────────────────────────────────────────────────────────────
    SITE 3 — `## Requirements` Validated v0.5 block (lines 69-71) AND ### Active block (lines 72-77)
    ────────────────────────────────────────────────────────────────────────────

    Two coupled edits (do them as ONE Edit pass to preserve adjacency):

    CURRENT TEXT (the Validated `**v0.5:**` sub-block + the entire ### Active block, contiguous, lines 69-77):
    ```
    **v0.5:**
    - ✓ **CFG-08** — Scheduled metadata refresh automation with guardrails — v0.5 (Phase 21; CFG-08 verified 2026-05-07; 22/22 must-haves verified, 344 tests + 8 ci.oban_smoke green, dual compile lanes green)

    ### Active

    <!-- Carried forward; building toward these next. -->

    - [ ] **CFG-07**: User can run bulk operations across multiple connections.
    - [ ] **DIAG-01**: Operator can generate a redacted debug bundle for troubleshooting.
    ```

    TARGET TEXT (CFG-07 promoted to Validated v0.5; DIAG-01 removed; ### Active comment retained, list emptied — matches PROJECT.md's prior v0.4 close pattern):
    ```
    **v0.5:**
    - ✓ **CFG-07** — Bulk operations across multiple connections — v0.5 (Phase 20; CFG-07 verified 2026-05-06; Phase 21.1 closed audit BLOCKER INT-01 by forwarding bulk correlation_id through Refresh.refresh/2)
    - ✓ **CFG-08** — Scheduled metadata refresh automation with guardrails — v0.5 (Phase 21; CFG-08 verified 2026-05-07; 22/22 must-haves verified, 344 tests + 8 ci.oban_smoke green, dual compile lanes green)

    ### Active

    <!-- Carried forward; building toward these next. -->
    ```

    Result: ### Active block becomes the heading + comment + a blank line (no bullets).
    This matches the "Active is empty between milestones" convention used at v0.4 close.

    ────────────────────────────────────────────────────────────────────────────
    SITE 4 — `## Out of Scope` (lines 81-89)
    ────────────────────────────────────────────────────────────────────────────

    NO CHANGE. Per 21.2-RESEARCH.md § Site 4: deferred items are tracked in the
    Deferred section (Site 2b above), NOT in Out of Scope. Do not add DIAG-01 or
    CERT-EXP-01 to Out of Scope.

    ────────────────────────────────────────────────────────────────────────────
    SITE 5 — Footer (line 175)
    ────────────────────────────────────────────────────────────────────────────

    CURRENT TEXT:
    ```
    *Last updated: 2026-05-07 — Phase 21.1 (CFG-07 bulk-refresh audit correlation_id forwarding) shipped, closing v0.5 milestone audit BLOCKER INT-01; v0.5 Operational maturity in progress.*
    ```

    TARGET TEXT:
    ```
    *Last updated: 2026-05-07 — Phase 21.2 closed v0.5 milestone audit gaps; v0.5 re-scoped to bulk ops + scheduled refresh shipped; DIAG-01 + CERT-EXP-01 re-targeted to v0.6.*
    ```

    ────────────────────────────────────────────────────────────────────────────
    SITE 6 — `## Key Decisions` table (REQUIRED — durable engineering DNA per D-21.2-01)
    ────────────────────────────────────────────────────────────────────────────

    APPEND a new row at the END of the existing Key Decisions table (currently the
    last row is the v0.2 closure-phase pattern row, line 154; do not delete or
    re-order existing rows):

    NEW ROW TO APPEND (insert immediately AFTER the last existing table row, BEFORE
    the closing `## Evolution` heading):
    ```
    | **v0.5: closure-phase pattern extended (21.1 → INT-01 BLOCKER closure, 21.2 → audit-gap + scope-rescope closure)** | When an audit surfaces a BLOCKER + scope drift, prefer producing closure phases over re-opening implementation. Phase 21.1 closed the security-adjacent BLOCKER; Phase 21.2 closed the doc/scope gaps. Cleaner audit trail; smaller blast radius; milestone audit re-runs cleanly. | ✓ Good (closed v0.5 audit gaps without re-opening Phase 20 or Phase 21; pattern reusable for v0.6+) |
    ```

    Rationale: matches the precedent at PROJECT.md:154 (v0.2 closure-phase pattern
    row); reinforces closure-phase pattern as durable engineering DNA. Site 6 is
    REQUIRED — the corresponding `must_haves.truths` entry is non-optional and the
    plan-checker enforces it.

    ────────────────────────────────────────────────────────────────────────────
    Critical guardrails (per RESEARCH § Don't Hand-Roll + Common Pitfalls):
    ────────────────────────────────────────────────────────────────────────────

    1. DO NOT create `.planning/milestones/v0.5-REQUIREMENTS.md` (D-21.2-09 — that
       is `/gsd-complete-milestone v0.5`'s job, not 21.2's). Verified by sibling
       plan 21.2-02; this plan must not create any file under
       `.planning/milestones/`.
    2. DO NOT add DIAG-01 or CERT-EXP-01 to `## Out of Scope` (Site 4 above —
       deferred ≠ out of scope; PROJECT.md distinguishes the two).
    3. DO NOT touch Phase 19 / IDP-INIT-01 / v0.4 entries — those are stable
       milestone-archive content.
    4. DO NOT touch any file under `lib/`, `test/`, or `.planning/milestones/`
       (D-21.2-03 — phase produces no code; sibling 21.2-02 owns REQUIREMENTS.md).
    5. DO NOT delete or reorder Key Decisions table rows (Site 6 is APPEND-only).
  </action>

  <verify>
    <automated>
# Site 1 — Current State block updated to cite Phase 21.2
grep -F "Phase 21.2 (audit-gap closure + scope re-alignment)" .planning/PROJECT.md && \
# Site 2 — ensure Debug bundles bullet is GONE from Target features slice
test "$(awk '/^\*\*Target features:\*\*/,/^\*\*Multi-milestone arc:\*\*/' .planning/PROJECT.md | grep -c 'Debug bundles')" = "0" && \
test "$(awk '/^\*\*Target features:\*\*/,/^\*\*Multi-milestone arc:\*\*/' .planning/PROJECT.md | grep -c 'Expiry alerts')" = "0" && \
# Site 2b — v0.6 Deferred bullet mentions both Debug bundles AND DIAG-01 AND Expiry alerts AND CERT-EXP-01
grep -E "^- \*\*v0\.6\*\*.*Debug bundles.*DIAG-01.*Expiry alerts.*CERT-EXP-01" .planning/PROJECT.md && \
# Site 3 — CFG-07 promoted to Validated v0.5 block (must appear in the **v0.5:** Validated block)
grep -F "✓ **CFG-07** — Bulk operations across multiple connections — v0.5" .planning/PROJECT.md && \
# Site 3 — DIAG-01 removed from Active (zero `[ ] **DIAG-01**` lines)
test "$(grep -cE '^- \[ \] \*\*DIAG-01\*\*' .planning/PROJECT.md)" = "0" && \
# Site 3 — CFG-07 removed from Active (zero `[ ] **CFG-07**` lines; it's now in Validated)
test "$(grep -cE '^- \[ \] \*\*CFG-07\*\*' .planning/PROJECT.md)" = "0" && \
# Site 5 — Footer cites Phase 21.2 + 2026-05-07
tail -3 .planning/PROJECT.md | grep -E "Phase 21\.2.*2026-05-07" && \
# Site 6 — Key Decisions row mentions v0.5 closure-phase pattern (REQUIRED per D-21.2-01)
grep -F "v0.5: closure-phase pattern extended" .planning/PROJECT.md && \
# Negative-existence: no v0.5 archive created by this plan
test ! -f .planning/milestones/v0.5-REQUIREMENTS.md && \
# Files touched by this plan = exactly PROJECT.md (post-commit window per commit_docs: true + commit: per-task)
test "$(git diff --name-only HEAD~1 HEAD -- .planning/ | grep -v '^\.planning/phases/21\.2-' | tr '\n' ' ')" = ".planning/PROJECT.md "
    </automated>
  </verify>

  <acceptance_criteria>
    - `.planning/PROJECT.md` exists and is ≥ 170 lines (was 175 pre-edit; expect 175-178 lines post-edit — +1 row added at Site 6, ±1 line from the Active-block emptying, ±1 footer rewrite).
    - `grep -F "Phase 21.2 (audit-gap closure + scope re-alignment)" .planning/PROJECT.md` returns exactly 1 match (Site 1).
    - `awk '/^\*\*Target features:\*\*/,/^\*\*Multi-milestone arc:\*\*/' .planning/PROJECT.md | grep -c "Debug bundles"` returns 0 (Site 2 — Debug bundles removed from Target features slice).
    - `awk '/^\*\*Target features:\*\*/,/^\*\*Multi-milestone arc:\*\*/' .planning/PROJECT.md | grep -c "Expiry alerts"` returns 0 (Site 2 — Expiry alerts removed from Target features slice).
    - `grep -E "^- \*\*v0\.6\*\* — SLO \(Single Logout\) plus operational-maturity carryover: Debug bundles \(DIAG-01\) and Expiry alerts \(CERT-EXP-01\)\." .planning/PROJECT.md` returns exactly 1 match (Site 2b).
    - `grep -F "✓ **CFG-07** — Bulk operations across multiple connections — v0.5 (Phase 20; CFG-07 verified 2026-05-06; Phase 21.1 closed audit BLOCKER INT-01 by forwarding bulk correlation_id through Refresh.refresh/2)" .planning/PROJECT.md` returns exactly 1 match (Site 3 — CFG-07 promoted to Validated v0.5).
    - `grep -cE "^- \[ \] \*\*DIAG-01\*\*" .planning/PROJECT.md` returns `0` (Site 3 — DIAG-01 removed from Active).
    - `grep -cE "^- \[ \] \*\*CFG-07\*\*" .planning/PROJECT.md` returns `0` (Site 3 — CFG-07 removed from Active; lives in Validated v0.5 now).
    - `tail -3 .planning/PROJECT.md | grep -E "^\*Last updated: 2026-05-07 — Phase 21\.2 closed v0\.5 milestone audit gaps"` returns 1 match (Site 5).
    - `grep -F "v0.5: closure-phase pattern extended" .planning/PROJECT.md` returns 1 match (Site 6 — REQUIRED per D-21.2-01).
    - Negative-existence (Pitfall 5): `test ! -f .planning/milestones/v0.5-REQUIREMENTS.md` succeeds (this plan must not create the milestone-archive file).
    - File-touch invariant (D-21.2-03), post-commit window: `git diff --name-only HEAD~1 HEAD -- .planning/PROJECT.md lib/ test/ .planning/milestones/` shows exactly `.planning/PROJECT.md` (no `lib/`, no `test/`, no `.planning/milestones/`).
    - **Special note for the awk-grep slice "Debug bundles in Target features":** The string `Debug bundles` may legitimately remain in the v0.6 Deferred sub-block (`- **v0.6** — ... Debug bundles (DIAG-01) ...`) which is INSIDE the same `## Current Milestone: v0.5` section. The acceptance check above uses a tighter slice `/^\*\*Target features:\*\*/,/^\*\*Multi-milestone arc:\*\*/` to exclude the Deferred sub-block (which appears AFTER `**Multi-milestone arc:**`). Verify by running the acceptance grep — it should return 0 even though `grep -c "Debug bundles" .planning/PROJECT.md` overall returns 1 (the Deferred row).
  </acceptance_criteria>

  <done>
    - `.planning/PROJECT.md` modified at exactly 6 sites.
    - All acceptance_criteria greps green.
    - Commit landed: `docs(21.2-01): re-scope PROJECT.md v0.5 (move DIAG-01 + Expiry alerts → v0.6 deferred; promote CFG-07 to Validated; refresh footer; record closure-phase pattern in Key Decisions)`
    - SUMMARY.md notes the 6 sites edited and confirms negative-existence + file-touch invariants.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — doc-only artifact change) | No executable surface, no input validation, no auth boundary, no schema. The "boundary" being managed is the documentation invariant between PROJECT.md and the rest of the planning artifacts (REQUIREMENTS.md, MILESTONE-ARC.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.2-01-01 | Spoofing | n/a (no auth surface) | n/a | Doc-only artifact change; no executable identity surface. |
| T-21.2-01-02 | Tampering | `.planning/PROJECT.md` historical state | mitigate | Footer (Site 5) cites Phase 21.2 + 2026-05-07 as the last-updater stamp, preserving an append-only audit trail of milestone-scope evolution. Prior text (Site 6 Key Decisions table is APPEND-only; Out of Scope is unchanged) is not deleted, only added to. |
| T-21.2-01-03 | Repudiation | `.planning/PROJECT.md` Key Decisions table (Site 6) | mitigate | Site 6 row records the v0.5 closure-phase pattern at peak salience. Captured in Key Decisions for durable engineering DNA — future operators see the closure-phase pattern without needing to read the milestone-archive snapshot. |
| T-21.2-01-04 | Information Disclosure | `.planning/PROJECT.md` Active + Deferred blocks | mitigate | New text mentions DIAG-01 + CERT-EXP-01 only by REQ-ID + short public-safe descriptions; no internal-only feature names, no secrets, no operational hostnames. PROJECT.md is already public-facing (committed to the repo); the change does not raise the disclosure surface. |
| T-21.2-01-05 | Denial of Service | n/a | n/a | Doc edit; no runtime impact. |
| T-21.2-01-06 | Elevation of Privilege | n/a (no privilege boundary) | n/a | Doc edit; no executable code path. |

</threat_model>

<verification>
Plan-level success criteria (all must be true before plan-checker / phase verify):

1. All acceptance_criteria greps in Task 1 return their expected counts.
2. Negative-existence: `test ! -f .planning/milestones/v0.5-REQUIREMENTS.md` succeeds.
3. File-touch invariant (post-commit window): only `.planning/PROJECT.md` is modified by this plan
   (no `lib/`, no `test/`, no `.planning/milestones/`, no other `.planning/` files).
4. The cross-doc coherence rows owned by this plan (per 21.2-RESEARCH.md § "Cross-Doc
   Coherence Invariants") are independently green:
   - "PROJECT.md no longer lists DIAG-01 in Active" → 0 matches.
   - "PROJECT.md no longer lists Debug bundles in Target" (sliced) → 0 matches.
   - "DIAG-01 deferred consistently" (PROJECT.md half) → ≥ 1 match.
   - "CERT-EXP-01 deferred consistently" (PROJECT.md half) → ≥ 1 match.
5. Threat register dispositions are all `n/a` or `mitigate`; no HIGH-severity unmitigated.
</verification>

<success_criteria>

The plan is complete when:

- [ ] PROJECT.md ## Current State block cites Phase 21.2 + the v0.6 re-scope.
- [ ] PROJECT.md ## Current Milestone Target features lists exactly Bulk operations + Scheduled metadata refresh (2 bullets).
- [ ] PROJECT.md ## Current Milestone Deferred-to-later-milestones v0.6 entry lists Debug bundles (DIAG-01) + Expiry alerts (CERT-EXP-01).
- [ ] PROJECT.md ## Requirements ### Validated **v0.5:** sub-block lists CFG-07 + CFG-08.
- [ ] PROJECT.md ## Requirements ### Active block has no DIAG-01 line.
- [ ] PROJECT.md footer Last updated cites Phase 21.2 + 2026-05-07.
- [ ] PROJECT.md ## Key Decisions table appends the v0.5 closure-phase pattern row (REQUIRED per D-21.2-01).
- [ ] No file under `lib/`, `test/`, or `.planning/milestones/` was modified by this plan.
- [ ] Commit landed with message `docs(21.2-01): re-scope PROJECT.md v0.5 (move DIAG-01 + Expiry alerts → v0.6 deferred; promote CFG-07 to Validated; refresh footer; record closure-phase pattern in Key Decisions)`.

</success_criteria>

<output>
After completion, create
`.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-01-SUMMARY.md`
documenting:

- Which 6 sites were edited in PROJECT.md (line numbers + diff recap).
- Confirmation of all acceptance_criteria greps (paste output).
- Confirmation of negative-existence (no `.planning/milestones/v0.5-REQUIREMENTS.md` created).
- Confirmation of file-touch invariant (only PROJECT.md modified by this plan).
- Cross-doc coherence rows owned by this plan reported green.
- Threat register dispositions (all `n/a` / `mitigate`).
- Reference to the v0.5 milestone audit (`.planning/v0.5-MILESTONE-AUDIT.md` scope_drift) as the gap-closure motivation.
</output>
