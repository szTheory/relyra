# Phase 21.2: Close v0.5 milestone-audit gaps and re-scope deferred features to v0.6 — Context

**Gathered:** 2026-05-07 (assumptions mode)
**Status:** Ready for planning
**Source:** v0.5 milestone audit (`.planning/v0.5-MILESTONE-AUDIT.md`) — closure-phase context

<domain>
## Phase Boundary

This is the **second closure phase** for `/gsd-audit-milestone v0.5` (the first, Phase 21.1, closed BLOCKER INT-01 — bulk-refresh audit `correlation_id` forwarding). Phase 21.2 ships the remaining audit-gap closures and the formal re-scope of deferred v0.5 features to v0.6, so the milestone re-audit can flip from `gaps_found` → `passed` and `/gsd-complete-milestone v0.5` can archive cleanly.

**In scope (5 work items, all in `.planning/` or `README.md`):**

1. **`PROJECT.md` re-scope** — Move "Debug bundles" (DIAG-01) and "Expiry alerts" from `## Current Milestone: v0.5` target features into the "Deferred to later milestones" block, retargeted to v0.6. Remove DIAG-01 from `### Active` requirements (it's no longer this milestone's work). Update the trailing `*Last updated:*` footer.
2. **`MILESTONE-ARC.md` update** — v0.6 currently scopes only "SLO (Single Logout)". Update the arc table + the v0.4/v0.5 prose to reflect: v0.6 = SLO + ops-maturity carryovers (DIAG-01 debug bundles + CERT-EXP-01 cert-expiry alerts). Annotate per the doc's own "Evolution" rule (date + trigger + rationale; do not delete prior text).
3. **`REQUIREMENTS.md` frontmatter refresh** — Stale frontmatter still says `Relyra v0.4`. Refresh in place to v0.5 with the actual shipped state: CFG-07 + CFG-08 satisfied (Phase 20 + 21 complete), DIAG-01 + CERT-EXP-01 listed as deferred to v0.6. Do NOT create `.planning/milestones/v0.5-REQUIREMENTS.md` here — that is `/gsd-complete-milestone v0.5`'s archival job (matches v0.1/v0.2/v0.3 pattern).
4. **`README.md` INT-02 fix** — Add a short "Operations: Bulk actions" subsection (or extend the existing operations section) covering multi-select + the three bulk actions (`enable` / `disable` / `refresh_metadata`). Brand-voice + operator-narrative coherent with the existing "Operations: Scheduled metadata refresh" section.
5. **Phase 20 Nyquist** — Run `/gsd-validate-phase 20` to produce `phases/20-bulk-operations/20-VALIDATION.md`, flipping the milestone Nyquist score from PARTIAL → compliant.

**Out of scope (explicitly deferred / handled elsewhere):**

- **`/gsd-audit-milestone v0.5` re-run + `/gsd-complete-milestone v0.5` + `/gsd-new-milestone v0.6`** — Sequential downstream commands after this phase ships; not part of 21.2's verification surface.
- **v0.4 milestone-archive backfill** — `.planning/milestones/` has v0.1/v0.2/v0.3 archives but no v0.4 archive. The v0.5 audit does not flag this as a v0.5 gap; it is a separate meta-task.
- **v0.6 phase planning** (debug-bundle phase, expiry-alerts phase) — Belongs to `/gsd-new-milestone v0.6`, not this closure phase.
- **Code-level fixes inside Phase 20 / Phase 21** — Phase 21.1 already closed the only BLOCKER. Tech debt from Phase 21 (pre-existing `ACSControllerTest :name_id` KeyError, `mix format` drift in `connections_live.ex`) is logged in `phases/21-…/deferred-items.md`, not 21.2 work.
- **New REQ-ID assignment for SLO-01** — Already exists in `REQUIREMENTS.md:24` ("SLO-01") under v0.6 deferred; no rename.

</domain>

<decisions>
## Implementation Decisions

### Scope shape (LOCKED)

- **D-21.2-01:** Phase 21.2 bundles all 5 in-scope items above into a single closure phase. Splitting into Phase 21.3 / 21.4 micro-phases for 5-10 line doc fixes is rejected (process tax > work content).
- **D-21.2-02:** v0.4 milestone-archive backfill is OUT of scope. Track as a separate meta-task if needed; do NOT pull into v0.5 closure.
- **D-21.2-03:** This phase produces NO new code under `lib/`. Only `.planning/` artifacts and `README.md` change. (`/gsd-validate-phase 20` may produce a new `20-VALIDATION.md` and may write Wave-0 test stubs only if Phase 20 already had a validation gap; in that case stubs are produced by the Nyquist auditor inside its own commit, not by 21.2 plans.)

### Re-scoping mechanics (LOCKED)

- **D-21.2-04:** Move **DIAG-01** to v0.6 with the existing REQ-ID. No rename. PROJECT.md `### Active` block loses the `DIAG-01` line; v0.6 deferred block in `REQUIREMENTS.md` gains it.
- **D-21.2-05:** Assign new REQ-ID **`CERT-EXP-01`** to the previously-orphaned "Expiry alerts" feature. Naming follows the existing CFG-/CERT-/MAP- prefix convention. Initial description: "User can receive operator-facing alerts/logs/events for upcoming certificate expirations."
- **D-21.2-06:** Update `MILESTONE-ARC.md`:
  - The milestone-arc table row for v0.6 changes from "SLO (Single Logout)" to "SLO + ops-maturity carryover (DIAG-01 + CERT-EXP-01)"
  - Add an entry to the doc's bottom "Evolution" annotation list with date (2026-05-07), trigger ("v0.5 milestone audit re-scope per Phase 21.2"), and the new ordering. Per the doc's own rule, prior text is annotated, not deleted.
- **D-21.2-07:** Do NOT plan v0.6 phases in 21.2 (no new entries in ROADMAP.md beyond the v0.6 milestone deferred-features block). v0.6 phase planning is `/gsd-new-milestone v0.6` work.

### REQUIREMENTS.md treatment (LOCKED)

- **D-21.2-08:** Refresh `.planning/REQUIREMENTS.md` in place from `Relyra v0.4` to `Relyra v0.5`:
  - Frontmatter `# Requirements: Relyra v0.5`, milestone block updated, Defined date 2026-05-06, Last updated 2026-05-07
  - "v0.5 Requirements" section: list CFG-07 (Phase 20) + CFG-08 (Phase 21) as completed, with phase + verification date
  - "Deferred To Later Milestones" → v0.6 list: DIAG-01 (Debug bundles), CERT-EXP-01 (Expiry alerts), SLO-01 (Single Logout)
  - Traceability table: rows for CFG-07 + CFG-08 (Complete), DIAG-01 + CERT-EXP-01 (Deferred to v0.6), SLO-01 (Deferred to v0.6)
- **D-21.2-09:** Do NOT create `.planning/milestones/v0.5-REQUIREMENTS.md` in this phase. The archive snapshot is produced by `/gsd-complete-milestone v0.5` (matches v0.1/v0.2/v0.3 pattern).

### INT-02 README addition (LOCKED)

- **D-21.2-10:** Add a "Operations: Bulk actions" subsection to `README.md`, placed near the existing "Operations: Scheduled metadata refresh" section (line 51 today; new section may go just before or just after — planner picks).
- **D-21.2-11:** Content shape: a short narrative paragraph + a recipe for each of the three bulk actions (`enable` / `disable` / `refresh_metadata`) reachable from the LiveView connections list multi-select. Match the operator-narrative voice + brand-voice rules already used in the scheduled-refresh section. No new code samples beyond what `Relyra.Ecto.BulkActions.run/4` already exposes.
- **D-21.2-12:** Reference the audit-cohesion guarantee delivered by Phase 21.1 (one shared `correlation_id` per bulk run) in the audit-related blurb so the README narrative reflects the closed BLOCKER.

### Phase 20 Nyquist VALIDATION.md (LOCKED)

- **D-21.2-13:** Run `/gsd-validate-phase 20` as a 21.2 ride-along to produce `phases/20-bulk-operations/20-VALIDATION.md`. The phase verification is already 4/4 clean (per `20-VERIFICATION.md`); the validation auditor's job here is mechanical — re-trace coverage and stamp the artifact. If unexpected gaps surface during validation (low-risk), surface them in the phase plan rather than silently widening 21.2.
- **D-21.2-14:** The Nyquist artifact production may be split into its own plan (`21.2-XX-phase-20-nyquist-validation`) so its commits are atomic + bounded.

### Closure flow (NOT in scope, but documented for downstream agents)

- **D-21.2-15:** After 21.2 ships, the next-step command sequence is:
  1. `/gsd-audit-milestone v0.5` → expect `passed`
  2. `/gsd-complete-milestone v0.5` → archives `.planning/milestones/v0.5-REQUIREMENTS.md` + `v0.5-ROADMAP.md` (and refreshes the existing `v0.5-MILESTONE-AUDIT.md` with the post-closure state)
  3. `/gsd-new-milestone v0.6` → kicks off v0.6 with the carryover scope baked in
- **D-21.2-16:** Phase verification (21.2-VERIFICATION.md) checks that the 5 in-scope artifact deltas are present + coherent — NOT that the milestone audit re-runs `passed`. The re-audit happens after this phase, not inside it.

### Claude's Discretion (planner-level mechanical choices)

- Plan-file layout: 1 plan per artifact vs. bundled — planner picks; bundled is acceptable for tightly-coupled doc fixes (e.g., PROJECT.md + REQUIREMENTS.md in one plan, MILESTONE-ARC.md + README in another, Nyquist in its own plan).
- Exact placement of the new README subsection (before vs after the scheduled-refresh section).
- Wording of CERT-EXP-01's REQUIREMENTS.md description (within the constraints of D-21.2-05).
- Order of artifact updates within a plan, as long as commit boundaries are atomic.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Audit / motivation
- `.planning/v0.5-MILESTONE-AUDIT.md` — full audit; especially:
  - lines 13-30 (DIAG-01 + EXPIRY-ALERTS gaps)
  - lines 47-59 (INT-02 README gap)
  - lines 62-66 (scope_drift on REQUIREMENTS.md frontmatter)
  - lines 75-80 (Nyquist Phase 20 gap)
  - lines 153-161 (Section 6 Option A — recommended path forward, the basis for 21.2)

### Artifacts to update
- `.planning/PROJECT.md` — re-scope Active + Current Milestone + Deferred sections; refresh footer
- `.planning/REQUIREMENTS.md` — frontmatter refresh `v0.4 → v0.5` in place
- `.planning/MILESTONE-ARC.md` — arc table v0.6 row + Evolution annotation
- `.planning/ROADMAP.md` — Phase 21.2 plans block (will populate after planning)
- `README.md` — new "Operations: Bulk actions" subsection near line 51
- `phases/20-bulk-operations/20-VALIDATION.md` — to be produced by `/gsd-validate-phase 20`

### Closure-phase precedent (shape reference)
- `.planning/phases/21.1-close-gap-cfg-07-bulk-refresh-audit-correlation-id-forwardin/21.1-CONTEXT.md` — closure-phase CONTEXT.md template + scope-tightness pattern (the v0.2 closure-phase pattern, now applied at v0.5)
- `.planning/phases/21.1-close-gap-cfg-07-bulk-refresh-audit-correlation-id-forwardin/21.1-PLAN.md` — closure-phase plan shape (single tightly-scoped plan)

### Bulk-action surface (for INT-02 README content)
- `lib/relyra/ecto/bulk_actions.ex` — `BulkActions.run/4` public surface (correlation_id generation, sequential execution, audit-map forwarding)
- `lib/relyra/live_admin/connections_live.ex:110-125` — LiveView seam (multi-select wiring + the three bulk action_fns)
- `lib/relyra/connections.ex` — `Connections.enable/2` / `disable/2` (the two non-`refresh_metadata` bulk actions)
- `lib/relyra/metadata.ex:55` — `Metadata.refresh/2` (now correctly forwarding `audit:` after Phase 21.1)

### Milestone archival pattern (REQUIREMENTS.md treatment reference)
- `.planning/milestones/v0.3-REQUIREMENTS.md` — archive snapshot pattern (what `/gsd-complete-milestone v0.5` will produce later, NOT what 21.2 produces)
- `.planning/milestones/v0.2-MILESTONE-AUDIT.md:33-43` — `re_audit` block pattern (precedent for "gaps_found → passed" milestone-audit transition after closure phases)

### Brand voice / README narrative reference
- `prompts/relyra-brand-book.md` — voice + do/don't summary
- `README.md:51` — existing "Operations: Scheduled metadata refresh" section (style + tone reference for the new subsection)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `Relyra.Ecto.BulkActions.run/4` (`lib/relyra/ecto/bulk_actions.ex`) — already documents itself well enough that the README addition can paraphrase its public spec without re-deriving semantics.
- `phases/21-scheduled-metadata-refresh/21-VALIDATION.md` — comparator artifact for what `20-VALIDATION.md` should look like (same Nyquist auditor produces both).
- v0.1-/v0.2-/v0.3-`-REQUIREMENTS.md` archives in `.planning/milestones/` — reference structure for the in-flight `REQUIREMENTS.md` refresh.

### Established Patterns
- **Closure-phase pattern** (PROJECT.md key-decisions table, v0.2 entry "12 → 09's verification, 13 → 10's, 14 → 11's"; extended in v0.5 with 21.1 + 21.2): when an audit surfaces gaps, prefer producing the missing artifacts over re-opening implementation. Cleaner audit trail; smaller blast radius. Phase 21.2 is this pattern's second v0.5 application.
- **Doc-only phases commit by artifact**: each artifact (PROJECT.md, REQUIREMENTS.md, etc.) is its own atomic commit per GSD convention; planner can group tightly-coupled artifact updates into a single plan but keeps commit boundaries clean.
- **Brand-voice grep invariants** (Phase 21-06 D-29): operator-facing surfaces never render `polling`, `cron job`, `blocked`, `retry`, `circuit breaker`, `MaxBackoff`. Applies to the new README subsection too — extend the existing assertion if any test covers README narrative (most likely none does, but planner should grep).

### Integration Points
- **PROJECT.md ↔ REQUIREMENTS.md ↔ MILESTONE-ARC.md** — these three docs must move together for the re-scope to be coherent. A planner that updates one and skips another reintroduces drift.
- **REQUIREMENTS.md ↔ ROADMAP.md ↔ phase 21.2 ROADMAP entry** — REQUIREMENTS.md traceability table must list CFG-07 and CFG-08 as Complete; ROADMAP.md 21.2 entry currently has `Requirements: TBD` (line 137), which becomes "Documentation / scope re-alignment (closes audit gaps; no new functional REQ-ID)" or similar after planning.
- **`/gsd-validate-phase 20` ↔ Phase 20 SUMMARYs** — the Nyquist auditor reads `20-01-SUMMARY.md` + `20-02-SUMMARY.md` + `20-VERIFICATION.md` to produce `20-VALIDATION.md`. No code under `lib/` is touched by this command.

</code_context>

<specifics>
## Specific Ideas

- **CERT-EXP-01 description draft:** "User can receive operator-facing alerts (events + log entries) for upcoming SAML signing certificate expirations, configurable per connection, with thresholds aligned to the staged-rollover cadence." Planner refines as the v0.6 phase is later spec'd.
- **MILESTONE-ARC.md update wording:** Use the doc's own Evolution prescription: "2026-05-07 — v0.5 milestone audit (Phase 21.2) re-scoped DIAG-01 + expiry alerts (now CERT-EXP-01) into v0.6; v0.6 is now SLO + ops-maturity carryovers."
- **README "Operations: Bulk actions" placement:** Recommend placing it BEFORE the existing "Operations: Scheduled metadata refresh" section (operator workflow read-order matches the v0.5 ship order: bulk ops landed in Phase 20, scheduled refresh in Phase 21). Planner may swap if ordering by frequency-of-use makes more sense.
- **Nyquist audit expectations:** Phase 20 verification is 4/4 clean; expect a clean `20-VALIDATION.md` with `nyquist_compliant: true`. Wave-0 stubs are unlikely to be needed retroactively; if the auditor flags any, treat as out-of-scope for 21.2 and surface as a deferred item.

</specifics>

<deferred>
## Deferred Ideas

- **v0.6 phase planning** — Phase for DIAG-01 (debug bundles) and Phase for CERT-EXP-01 (expiry alerts) — kicked off by `/gsd-new-milestone v0.6` after `/gsd-complete-milestone v0.5`.
- **v0.4 milestone-archive backfill** — `.planning/milestones/` is missing `v0.4-REQUIREMENTS.md` + `v0.4-ROADMAP.md` + `v0.4-MILESTONE-AUDIT.md`. Out of scope for 21.2; track as a separate meta-task if it becomes a process-discipline concern.
- **Phase 21 tech debt** — `ACSControllerTest :name_id` KeyError + `mix format` drift in `connections_live.ex` (per `phases/21-…/deferred-items.md`). Both pre-date Phase 21; tracked there; not 21.2 work.
- **MappingCommands explicit `repo.rollback/1`** — v0.2 carryover tech debt; PROJECT.md key-decisions table marks "⚠️ Revisit". Still not reproducible against current dep set; not 21.2 work.
- **`/gsd-audit-milestone v0.5` re-run + `/gsd-complete-milestone v0.5`** — Downstream commands; documented in D-21.2-15 but explicitly NOT 21.2's work.

### Reviewed Todos (not folded)

- (None — list-todos returned empty for this project.)

</deferred>

---

*Phase: 21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur*
*Context gathered: 2026-05-07 (assumptions mode) — 5 assumptions surfaced from the v0.5 milestone audit, all confirmed without correction. Calibration: standard.*
