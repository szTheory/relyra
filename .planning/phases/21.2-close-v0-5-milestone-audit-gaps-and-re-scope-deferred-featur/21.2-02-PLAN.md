---
phase: 21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
# Phase 21.2 has no functional REQ-IDs (doc-only closure phase per CONTEXT.md additional_context)
requirements: []
commit: per-task
must_haves:
  truths:
    - "REQUIREMENTS.md frontmatter title is `# Requirements: Relyra v0.5` (refreshed in place from v0.4 — per D-21.2-08)."
    - "REQUIREMENTS.md milestone block reads `**Milestone:** v0.5 — Operational maturity` — per D-21.2-08."
    - "REQUIREMENTS.md `## v0.5 Requirements` section lists CFG-07 + CFG-08 both with `[x]` checkbox + `**Complete**` status — per D-21.2-08."
    - "REQUIREMENTS.md `## Deferred To Later Milestones` v0.6 sub-section lists DIAG-01, CERT-EXP-01 (newly assigned), AND SLO-01 — per D-21.2-08, D-21.2-05."
    - "CERT-EXP-01 description in REQUIREMENTS.md uses the public-safe wording from CONTEXT.md `<specifics>` line 149 — per D-21.2-05."
    - "REQUIREMENTS.md Traceability table no longer references IDP-INIT-01 (milestone-scoped per RESEARCH Open Question 2)."
    - "REQUIREMENTS.md Traceability table includes CFG-07 (Phase 20 + Phase 21.1 closure) + CFG-08 (Phase 21) as Complete; DIAG-01 + CERT-EXP-01 + SLO-01 as Deferred — per D-21.2-08."
    - "REQUIREMENTS.md Out of Scope table no longer contains the `Bulk operations across many connections | Explicitly deferred to v0.5` row OR the `Scheduled metadata refresh automation | Explicitly deferred to v0.5` row (those features shipped — they are no longer out of scope)."
    - "REQUIREMENTS.md footer cites Phase 21.2 + 2026-05-07 — per D-21.2-08."
    - "Negative existence (Pitfall 5): `.planning/milestones/v0.5-REQUIREMENTS.md` was NOT created by this plan — per D-21.2-09."
    - "Cross-doc coherence rows it owns: `[x] **CFG-07` + `[x] **CFG-08` greppable in REQUIREMENTS.md (1 each); 'DIAG-01 deferred consistently' (REQUIREMENTS.md half); 'CERT-EXP-01 deferred consistently' (REQUIREMENTS.md half)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "v0.5-scoped REQUIREMENTS doc — title, milestone block, requirements list, deferred list, traceability table, footer all reflect v0.5"
      contains: "Requirements: Relyra v0.5"
      min_lines: 40
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/PROJECT.md"
      via: "REQ-ID coherence (CFG-07 / CFG-08 Complete; DIAG-01 / CERT-EXP-01 / SLO-01 Deferred)"
      pattern: "DIAG-01|CERT-EXP-01|SLO-01"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/MILESTONE-ARC.md"
      via: "v0.6 carryover REQ-ID list (DIAG-01 + CERT-EXP-01)"
      pattern: "v0\\.6"
---

<objective>
Refresh `.planning/REQUIREMENTS.md` in place from `Relyra v0.4` to `Relyra v0.5`,
reflecting the actual shipped state (CFG-07 + CFG-08 Complete) and the re-scoped
deferred items (DIAG-01 + CERT-EXP-01 + SLO-01 → v0.6).

Per D-21.2-09, this plan MUST refresh the live `.planning/REQUIREMENTS.md` file in
place — it MUST NOT create `.planning/milestones/v0.5-REQUIREMENTS.md`. The archive
snapshot is produced by `/gsd-complete-milestone v0.5`, NOT by this closure phase
(matches v0.1 / v0.2 / v0.3 archival pattern).

Purpose: REQUIREMENTS.md is the per-REQ-ID-status source of truth (per Doc Authority
Map in 21.2-RESEARCH.md). The current file is stale — frontmatter still says "Relyra
v0.4" — and was the trigger for the v0.5 milestone-audit `scope_drift` finding.

Output: a single in-place rewrite of REQUIREMENTS.md touching all 7 blocks (title,
section heading, requirements list, deferred section, out-of-scope table,
traceability table, footer), each with verbatim before/after text from
21.2-RESEARCH.md § "2. REQUIREMENTS.md In-Place Frontmatter Refresh".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/v0.5-MILESTONE-AUDIT.md
@.planning/milestones/v0.3-REQUIREMENTS.md
@.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-CONTEXT.md
@.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-RESEARCH.md

<interfaces>
<!--
This is a doc-only edit. There are no code interfaces. The "interface" is the exact
shape of each REQUIREMENTS.md block; verbatim before/after text is in the action
block below (lifted from 21.2-RESEARCH.md § 2).
-->
None — doc-only phase per D-21.2-03 (no `lib/` changes).

REQUIREMENTS.md shape pattern reference: `.planning/milestones/v0.3-REQUIREMENTS.md`
(read-only; do not modify) — confirms milestone-scoped Traceability convention
(no cross-milestone REQ-IDs in the live REQUIREMENTS.md Traceability table).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: REQUIREMENTS.md — in-place v0.4 → v0.5 refresh (7 blocks)</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (full file — 51 lines; verify current state matches the "current text" quotes below before applying changes)
    - .planning/v0.5-MILESTONE-AUDIT.md (gap motivation — `scope_drift` block at lines 62-66; the audit explicitly flagged the stale v0.4 frontmatter as the drift signal)
    - .planning/milestones/v0.3-REQUIREMENTS.md (shape reference — read-only; confirms milestone-scoped Traceability table convention used by the prior milestone)
    - .planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-CONTEXT.md (LOCKED decisions D-21.2-05, D-21.2-08, D-21.2-09 + `<specifics>` line 149 = CERT-EXP-01 description draft)
    - .planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-RESEARCH.md § "2. REQUIREMENTS.md In-Place Frontmatter Refresh" (verbatim 7-block diff shape; THIS is the source of truth for current/target text)
  </read_first>

  <behavior>
    - D-21.2-08: refresh frontmatter, milestone block, requirements list, deferred section, traceability table, and footer in place.
    - D-21.2-09: do NOT create `.planning/milestones/v0.5-REQUIREMENTS.md`. The `test ! -f` check below enforces this.
    - D-21.2-05: introduce REQ-ID `CERT-EXP-01` with the description from CONTEXT.md `<specifics>` line 149 (verbatim below). Naming follows existing CFG-/CERT-/MAP- prefix convention.
    - RESEARCH Open Question 2 (resolved by recommendation): REQUIREMENTS.md is milestone-scoped — IDP-INIT-01 (v0.4 REQ) is REMOVED from the live REQUIREMENTS.md Traceability table; cross-milestone retrospection lives in PROJECT.md `## Requirements ### Validated`.
    - Threat register I/D consideration: REQUIREMENTS.md is public-facing — keep CERT-EXP-01 description public-safe (no internal-only feature names, no operational hostnames).
    - Cross-doc coherence rows owned by this plan:
      - `grep "\[x\] \*\*CFG-07" .planning/REQUIREMENTS.md` returns 1 match.
      - `grep "\[x\] \*\*CFG-08" .planning/REQUIREMENTS.md` returns 1 match.
      - `grep -F "DIAG-01" .planning/REQUIREMENTS.md` returns ≥ 1 match.
      - `grep -F "CERT-EXP-01" .planning/REQUIREMENTS.md` returns ≥ 1 match.
  </behavior>

  <action>
    Edit `.planning/REQUIREMENTS.md` as a 7-block in-place refresh. Each block's
    verbatim "current text" and "target text" is lifted from 21.2-RESEARCH.md
    § "2. REQUIREMENTS.md In-Place Frontmatter Refresh". Apply edits using the Edit
    tool block-by-block; do NOT paraphrase, do NOT re-flow paragraphs, do NOT change
    block ordering.

    The current REQUIREMENTS.md is 51 lines (verified). Expected post-edit length:
    ~50-65 lines depending on traceability table row count.

    ────────────────────────────────────────────────────────────────────────────
    BLOCK 1 — Title + frontmatter (lines 1-5)
    ────────────────────────────────────────────────────────────────────────────

    CURRENT TEXT:
    ```
    # Requirements: Relyra v0.4

    **Defined:** 2026-05-06
    **Milestone:** v0.4 — IdP-initiated SSO
    **Core Value:** Every SAML login ends in a verified trust path or a typed rejection — never a silent compromise.
    ```

    TARGET TEXT:
    ```
    # Requirements: Relyra v0.5

    **Defined:** 2026-05-06
    **Milestone:** v0.5 — Operational maturity
    **Core Value:** Every SAML login ends in a verified trust path or a typed rejection — never a silent compromise.
    ```

    ────────────────────────────────────────────────────────────────────────────
    BLOCK 2 — Section heading + intro (lines 7-9)
    ────────────────────────────────────────────────────────────────────────────

    CURRENT TEXT:
    ```
    ## v0.4 Requirements

    Committed scope for v0.4: IdP-initiated SSO and opaque RelayState handling, allowing adopters to serve environments requiring enterprise IdP dashboards and legacy workforce SAML flows.
    ```

    TARGET TEXT:
    ```
    ## v0.5 Requirements

    Committed scope for v0.5 (re-scoped 2026-05-07 by Phase 21.2): bulk operations across connections and scheduled metadata refresh automation. Debug bundles (DIAG-01) and Expiry alerts (CERT-EXP-01) re-targeted to v0.6 per the v0.5 milestone audit.
    ```

    ────────────────────────────────────────────────────────────────────────────
    BLOCK 3 — Per-section requirements list (lines 11-13)
    ────────────────────────────────────────────────────────────────────────────

    CURRENT TEXT:
    ```
    ### Protocol Surface

    - [ ] **IDP-INIT-01**: User can initiate login from the IdP and rely on opaque RelayState handling.
    ```

    TARGET TEXT (section heading shifts from `Protocol Surface` to `Adoption And Operations` to match v0.5 theme):
    ```
    ### Adoption And Operations

    - [x] **CFG-07**: User can run bulk operations across multiple connections. **Complete** — Phase 20 shipped 2026-05-06; Phase 21.1 closed audit BLOCKER INT-01 by forwarding bulk correlation_id through `Refresh.refresh/2`.
    - [x] **CFG-08**: User can enable scheduled metadata refresh automation with guardrails. **Complete** — Phase 21 shipped 2026-05-07.
    ```

    ────────────────────────────────────────────────────────────────────────────
    BLOCK 4 — Deferred section (lines 15-24)
    ────────────────────────────────────────────────────────────────────────────

    CURRENT TEXT:
    ```
    ## Deferred To Later Milestones

    ### Adoption And Operations (v0.5)

    - **CFG-07**: User can run bulk operations across multiple connections. Deferred to v0.5 operational maturity.
    - [x] **CFG-08**: User can enable scheduled metadata refresh automation with guardrails. **Complete** — Phase 21 shipped 2026-05-07.

    ### Protocol Surface (v0.6)

    - **SLO-01**: User can complete standards-compliant Single Logout flows. Deferred to v0.6.
    ```

    TARGET TEXT (the v0.5 sub-section disappears — those items are now in Block 3 above; the v0.6 sub-section gains DIAG-01 + CERT-EXP-01):
    ```
    ## Deferred To Later Milestones

    ### Operational Maturity (v0.6)

    - **DIAG-01**: Operator can generate a redacted debug bundle for troubleshooting. Re-scoped from v0.5 to v0.6 by Phase 21.2 per the v0.5 milestone audit.
    - **CERT-EXP-01**: User can receive operator-facing alerts (events + log entries) for upcoming SAML signing certificate expirations, configurable per connection, with thresholds aligned to the staged-rollover cadence. New REQ-ID assigned by Phase 21.2 (formerly orphaned "Expiry alerts" feature).

    ### Protocol Surface (v0.6)

    - **SLO-01**: User can complete standards-compliant Single Logout flows. Deferred to v0.6.
    ```

    The CERT-EXP-01 description above is verbatim from CONTEXT.md `<specifics>` line
    149 + RESEARCH § Block 4. It is the public-safe wording (Threat Register I/D
    consideration in `<threat_model>` below).

    ────────────────────────────────────────────────────────────────────────────
    BLOCK 5 — Out of Scope table (lines 26-34)
    ────────────────────────────────────────────────────────────────────────────

    Two specific rows are deleted (those features shipped — no longer "out of
    scope"). Other rows unchanged.

    CURRENT ROWS to DELETE (find these two consecutive lines in the Out of Scope
    table; the other rows in the table are kept verbatim):
    ```
    | Bulk operations across many connections | Explicitly deferred to v0.5. |
    | Scheduled metadata refresh automation | Explicitly deferred to v0.5. |
    ```

    Both rows must be removed. The row immediately above them (`Hosted admin
    service or broker runtime | ...`) and the row immediately below them
    (`Single Logout | Explicitly deferred to v0.6 per the active milestone arc.`)
    must remain unchanged.

    ────────────────────────────────────────────────────────────────────────────
    BLOCK 6 — Traceability table (lines 36-47)
    ────────────────────────────────────────────────────────────────────────────

    CURRENT TEXT (the entire `## Traceability` block, including the 2 narrative
    `**Coverage:**` lines below the table):
    ```
    | Requirement | Phase | Status |
    |-------------|-------|--------|
    | IDP-INIT-01 | Phase 19 | Complete |
    | CFG-07 | Phase 20 | Complete |
    | CFG-08 | Phase 21 | Complete (Phase 21 shipped 2026-05-07: schema + pure helpers + trust-boundary helpers + audit seam + telemetry + scheduler + wrapper + worker + live-admin-surface + mix-tasks-telemetry-docs) |

    **Coverage:**
    - v0.4 requirements: 1 total
    - Mapped to phases: 1
    - Unmapped: 0
    ```

    TARGET TEXT (IDP-INIT-01 removed — milestone-scoped per RESEARCH Open Question 2;
    DIAG-01 + CERT-EXP-01 + SLO-01 added as Deferred; CFG-07 row gains the
    `+ Phase 21.1 closure` annotation):
    ```
    | Requirement | Phase | Status |
    |-------------|-------|--------|
    | CFG-07 | Phase 20 (+ Phase 21.1 closure) | Complete |
    | CFG-08 | Phase 21 | Complete (Phase 21 shipped 2026-05-07: schema + pure helpers + trust-boundary helpers + audit seam + telemetry + scheduler + wrapper + worker + live-admin-surface + mix-tasks-telemetry-docs) |
    | DIAG-01 | (deferred — v0.6) | Deferred |
    | CERT-EXP-01 | (deferred — v0.6) | Deferred |
    | SLO-01 | (deferred — v0.6) | Deferred |

    **Coverage:**
    - v0.5 requirements: 2 total
    - Mapped to phases: 2
    - Unmapped: 0
    - Deferred to v0.6: 3
    ```

    ────────────────────────────────────────────────────────────────────────────
    BLOCK 7 — Footer (lines 49-51)
    ────────────────────────────────────────────────────────────────────────────

    CURRENT TEXT:
    ```
    ---
    *Requirements defined: 2026-05-06*
    *Last updated: 2026-05-06 after milestone v0.4 completion*
    ```

    TARGET TEXT:
    ```
    ---
    *Requirements defined: 2026-05-06*
    *Last updated: 2026-05-07 by Phase 21.2 — refreshed in place from v0.4 to v0.5 with re-scoped deferred items*
    ```

    ────────────────────────────────────────────────────────────────────────────
    Critical guardrails (per RESEARCH § Common Pitfalls 5 + 6):
    ────────────────────────────────────────────────────────────────────────────

    1. **PITFALL 5 (CRITICAL): DO NOT create `.planning/milestones/v0.5-REQUIREMENTS.md`.**
       D-21.2-09 is explicit: the milestone-archive snapshot is `/gsd-complete-milestone
       v0.5`'s job, NOT this closure phase's. The acceptance criteria below include
       the negative-existence check `test ! -f .planning/milestones/v0.5-REQUIREMENTS.md`.
       If you find yourself reaching for `Write` against
       `.planning/milestones/v0.5-REQUIREMENTS.md`, halt — you are operating outside
       this plan's scope.
    2. DO NOT touch `.planning/PROJECT.md` (sibling plan 21.2-01 owns it; same-wave
       file-touch overlap would force serialization).
    3. DO NOT touch `.planning/MILESTONE-ARC.md` (sibling plan 21.2-03 owns it).
    4. DO NOT touch any file under `lib/`, `test/`, `.planning/milestones/`, or
       `.planning/phases/` other than this plan's own SUMMARY.md (D-21.2-03).
    5. CERT-EXP-01 description MUST use the public-safe wording in BLOCK 4 above.
       No internal hostnames, no internal feature codenames, no operational
       configuration values. Verified safe by RESEARCH § Threat Register
       I/D row.
    6. The Out of Scope table (Block 5) requires DELETING two rows ONLY. Do not
       delete the entire table; do not delete other rows; do not add rows.
    7. PITFALL 6: `requirements: []` is the correct frontmatter for this plan
       (this plan's own frontmatter, not REQUIREMENTS.md content). The plan-checker
       should accept the empty array; if it flags it, surface to operator with the
       inline-comment justification rather than inventing a fake REQ-ID.
  </action>

  <verify>
    <automated>
# Block 1 — Title refreshed to v0.5
head -1 .planning/REQUIREMENTS.md | grep -F "# Requirements: Relyra v0.5" && \
# Block 1 — Milestone block updated
grep -F "**Milestone:** v0.5 — Operational maturity" .planning/REQUIREMENTS.md && \
# Block 2 — Intro paragraph updated
grep -F "Committed scope for v0.5 (re-scoped 2026-05-07 by Phase 21.2)" .planning/REQUIREMENTS.md && \
# Block 3 — CFG-07 marked Complete (cross-doc invariant row "CFG-07 marked Complete in REQUIREMENTS")
grep -E "^- \[x\] \*\*CFG-07\*\*" .planning/REQUIREMENTS.md && \
test "$(grep -cE '^- \[x\] \*\*CFG-07\*\*' .planning/REQUIREMENTS.md)" = "1" && \
# Block 3 — CFG-08 marked Complete (cross-doc invariant row "CFG-08 marked Complete in REQUIREMENTS")
grep -E "^- \[x\] \*\*CFG-08\*\*" .planning/REQUIREMENTS.md && \
test "$(grep -cE '^- \[x\] \*\*CFG-08\*\*' .planning/REQUIREMENTS.md)" = "1" && \
# Block 3 — IDP-INIT-01 NOT in v0.5 Requirements list (milestone-scoped)
test "$(grep -cE '^- \[ \] \*\*IDP-INIT-01' .planning/REQUIREMENTS.md)" = "0" && \
# Block 4 — DIAG-01 in v0.6 Deferred (cross-doc invariant row "DIAG-01 deferred consistently")
grep -F "**DIAG-01**: Operator can generate a redacted debug bundle" .planning/REQUIREMENTS.md && \
# Block 4 — CERT-EXP-01 in v0.6 Deferred (cross-doc invariant row "CERT-EXP-01 deferred consistently")
grep -F "**CERT-EXP-01**: User can receive operator-facing alerts" .planning/REQUIREMENTS.md && \
# Block 4 — SLO-01 in v0.6 Deferred (preserved)
grep -F "**SLO-01**: User can complete standards-compliant Single Logout" .planning/REQUIREMENTS.md && \
# Block 5 — Bulk-ops + scheduled-refresh rows REMOVED from Out of Scope table
test "$(grep -cF "Bulk operations across many connections | Explicitly deferred to v0.5." .planning/REQUIREMENTS.md)" = "0" && \
test "$(grep -cF "Scheduled metadata refresh automation | Explicitly deferred to v0.5." .planning/REQUIREMENTS.md)" = "0" && \
# Block 5 — surviving Out of Scope row anchor (positive existence — guards against an over-aggressive table delete)
grep -F "Single Logout | Explicitly deferred to v0.6" .planning/REQUIREMENTS.md && \
# Block 6 — Traceability table rows
grep -F "| CFG-07 | Phase 20 (+ Phase 21.1 closure) | Complete |" .planning/REQUIREMENTS.md && \
grep -F "| CFG-08 | Phase 21 |" .planning/REQUIREMENTS.md && \
grep -F "| DIAG-01 | (deferred — v0.6) | Deferred |" .planning/REQUIREMENTS.md && \
grep -F "| CERT-EXP-01 | (deferred — v0.6) | Deferred |" .planning/REQUIREMENTS.md && \
grep -F "| SLO-01 | (deferred — v0.6) | Deferred |" .planning/REQUIREMENTS.md && \
# Block 6 — Coverage block updated to v0.5
grep -F "v0.5 requirements: 2 total" .planning/REQUIREMENTS.md && \
grep -F "Deferred to v0.6: 3" .planning/REQUIREMENTS.md && \
# Block 6 — IDP-INIT-01 row removed
test "$(grep -cF "| IDP-INIT-01 |" .planning/REQUIREMENTS.md)" = "0" && \
# Block 7 — Footer cites Phase 21.2 + 2026-05-07
tail -2 .planning/REQUIREMENTS.md | grep -F "Last updated: 2026-05-07 by Phase 21.2" && \
# CRITICAL Pitfall 5 — negative existence: no v0.5 archive created
test ! -f .planning/milestones/v0.5-REQUIREMENTS.md && \
# File-touch invariant: only REQUIREMENTS.md modified by this plan
test "$(git diff --name-only HEAD~1 HEAD -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/MILESTONE-ARC.md README.md lib/ test/ .planning/milestones/ | tr '\n' ' ')" = ".planning/REQUIREMENTS.md "
    </automated>
  </verify>

  <acceptance_criteria>
    - `head -1 .planning/REQUIREMENTS.md` returns exactly `# Requirements: Relyra v0.5` (Block 1).
    - `grep -F "**Milestone:** v0.5 — Operational maturity" .planning/REQUIREMENTS.md` returns exactly 1 match (Block 1).
    - `grep -F "Committed scope for v0.5 (re-scoped 2026-05-07 by Phase 21.2)" .planning/REQUIREMENTS.md` returns exactly 1 match (Block 2).
    - `grep -cE "^- \[x\] \*\*CFG-07\*\*" .planning/REQUIREMENTS.md` returns `1` (Block 3 — cross-doc invariant "CFG-07 marked Complete in REQUIREMENTS").
    - `grep -cE "^- \[x\] \*\*CFG-08\*\*" .planning/REQUIREMENTS.md` returns `1` (Block 3 — cross-doc invariant "CFG-08 marked Complete in REQUIREMENTS").
    - `grep -cE "^- \[ \] \*\*IDP-INIT-01" .planning/REQUIREMENTS.md` returns `0` (Block 3 — IDP-INIT-01 removed; milestone-scoped per RESEARCH Open Question 2).
    - `grep -F "**DIAG-01**: Operator can generate a redacted debug bundle" .planning/REQUIREMENTS.md` returns ≥ 1 match (Block 4 — cross-doc invariant "DIAG-01 deferred consistently" REQUIREMENTS.md half).
    - `grep -F "**CERT-EXP-01**: User can receive operator-facing alerts" .planning/REQUIREMENTS.md` returns ≥ 1 match (Block 4 — cross-doc invariant "CERT-EXP-01 deferred consistently" REQUIREMENTS.md half).
    - `grep -F "**SLO-01**: User can complete standards-compliant Single Logout" .planning/REQUIREMENTS.md` returns 1 match (Block 4 preserved).
    - `grep -cF "Bulk operations across many connections | Explicitly deferred to v0.5." .planning/REQUIREMENTS.md` returns `0` (Block 5 — row deleted).
    - `grep -cF "Scheduled metadata refresh automation | Explicitly deferred to v0.5." .planning/REQUIREMENTS.md` returns `0` (Block 5 — row deleted).
    - `grep -F "Single Logout | Explicitly deferred to v0.6" .planning/REQUIREMENTS.md` returns 1 match (Block 5 — surviving Out of Scope row anchor; guards against an over-aggressive table delete).
    - `grep -F "| CFG-07 | Phase 20 (+ Phase 21.1 closure) | Complete |" .planning/REQUIREMENTS.md` returns 1 match (Block 6 — Traceability row).
    - `grep -F "| CFG-08 | Phase 21 |" .planning/REQUIREMENTS.md` returns 1 match (Block 6).
    - `grep -F "| DIAG-01 | (deferred — v0.6) | Deferred |" .planning/REQUIREMENTS.md` returns 1 match (Block 6).
    - `grep -F "| CERT-EXP-01 | (deferred — v0.6) | Deferred |" .planning/REQUIREMENTS.md` returns 1 match (Block 6).
    - `grep -F "| SLO-01 | (deferred — v0.6) | Deferred |" .planning/REQUIREMENTS.md` returns 1 match (Block 6).
    - `grep -cF "| IDP-INIT-01 |" .planning/REQUIREMENTS.md` returns `0` (Block 6 — IDP-INIT-01 traceability row removed).
    - `grep -F "v0.5 requirements: 2 total" .planning/REQUIREMENTS.md` returns 1 match (Block 6 Coverage).
    - `grep -F "Deferred to v0.6: 3" .planning/REQUIREMENTS.md` returns 1 match (Block 6 Coverage).
    - `tail -2 .planning/REQUIREMENTS.md | grep -F "Last updated: 2026-05-07 by Phase 21.2"` returns 1 match (Block 7).
    - **Negative existence (Pitfall 5):** `test ! -f .planning/milestones/v0.5-REQUIREMENTS.md` succeeds. THIS IS A BLOCKING ACCEPTANCE CRITERION — if it fails (i.e., the file exists), the plan must NOT be marked complete.
    - File-touch invariant (D-21.2-03 + D-21.2-09): `git diff --name-only HEAD~1 HEAD -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/MILESTONE-ARC.md README.md lib/ test/ .planning/milestones/` shows EXACTLY `.planning/REQUIREMENTS.md` (no other paths).
    - `.planning/REQUIREMENTS.md` total line count is between 40 and 65 lines (was 51 pre-edit).
  </acceptance_criteria>

  <done>
    - `.planning/REQUIREMENTS.md` modified at all 7 blocks specified above.
    - All acceptance_criteria greps green.
    - Negative-existence check green (no `.planning/milestones/v0.5-REQUIREMENTS.md` created).
    - File-touch invariant green (only REQUIREMENTS.md modified by this plan).
    - Commit landed: `docs(21.2-02): refresh REQUIREMENTS.md in place v0.4 → v0.5 (CFG-07 + CFG-08 Complete; DIAG-01 + CERT-EXP-01 + SLO-01 deferred to v0.6)`
    - SUMMARY.md confirms each of the 7 blocks edited and reports negative-existence + file-touch invariants green.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/REQUIREMENTS.md` ↔ public Git history | REQUIREMENTS.md is committed to a public OSS repository. Any change is visible to anyone who clones the repo. The relevant boundary is the public-safety of the new CERT-EXP-01 description and the historical accuracy of the milestone-scope record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.2-02-01 | Spoofing | n/a (no auth surface) | n/a | Doc-only artifact change; no executable identity surface. |
| T-21.2-02-02 | Tampering | `.planning/REQUIREMENTS.md` historical state | mitigate | Block 7 footer cites `Phase 21.2 — refreshed in place from v0.4 to v0.5 with re-scoped deferred items` as the last-updater stamp. Git history preserves the pre-21.2 state for full audit reconstruction. The in-place refresh (D-21.2-08) is the LOCKED choice over a milestone-archive-then-snapshot pattern; that pattern is reserved for `/gsd-complete-milestone v0.5`. |
| T-21.2-02-03 | Repudiation | Per-REQ-ID status (DIAG-01, CERT-EXP-01, SLO-01) | mitigate | Each deferred REQ-ID's status is recorded in BOTH the `## Deferred To Later Milestones` section AND the Traceability table. Cross-reference via `grep -F "DIAG-01" .planning/REQUIREMENTS.md` returns ≥ 2 matches, eliminating "is it really deferred or just dropped?" ambiguity. |
| T-21.2-02-04 | Information Disclosure | New CERT-EXP-01 description in Block 4 | mitigate | Description text is verbatim from CONTEXT.md `<specifics>` line 149 + RESEARCH § Block 4. Public-safe wording (no internal hostnames, no internal connection IDs, no operational config values, no proprietary feature codenames). The description names only the SAML domain concept ("signing certificate expirations", "staged-rollover cadence") which is already public-domain SAML terminology and is referenced verbatim in PROJECT.md. |
| T-21.2-02-05 | Denial of Service | n/a | n/a | Doc edit; no runtime impact. |
| T-21.2-02-06 | Elevation of Privilege | n/a (no privilege boundary) | n/a | Doc edit; no executable code path. |

</threat_model>

<verification>
Plan-level success criteria (all must be true before plan-checker / phase verify):

1. All 7 blocks edited per the verbatim before/after text in the action block.
2. All acceptance_criteria greps green.
3. **Negative-existence (BLOCKING):** `.planning/milestones/v0.5-REQUIREMENTS.md` does NOT exist after this plan ships.
4. File-touch invariant: only `.planning/REQUIREMENTS.md` is modified by this plan.
5. Cross-doc coherence rows owned by this plan (per 21.2-RESEARCH.md § "Cross-Doc
   Coherence Invariants") are independently green:
   - "CFG-07 marked Complete in REQUIREMENTS" → 1 match.
   - "CFG-08 marked Complete in REQUIREMENTS" → 1 match.
   - "DIAG-01 deferred consistently" (REQUIREMENTS.md half) → ≥ 1 match.
   - "CERT-EXP-01 deferred consistently" (REQUIREMENTS.md half) → ≥ 1 match.
6. Threat register dispositions are all `n/a` or `mitigate`; no HIGH-severity unmitigated.
7. CERT-EXP-01 description is public-safe (no internal hostnames, no internal codenames).
</verification>

<success_criteria>

The plan is complete when:

- [ ] REQUIREMENTS.md frontmatter says `Relyra v0.5` and `v0.5 — Operational maturity`.
- [ ] REQUIREMENTS.md `## v0.5 Requirements` section lists CFG-07 + CFG-08 both as `[x] **Complete**`.
- [ ] REQUIREMENTS.md `## Deferred To Later Milestones` v0.6 sub-section lists DIAG-01 + CERT-EXP-01 + SLO-01.
- [ ] REQUIREMENTS.md Out of Scope table no longer references "Bulk operations" or "Scheduled metadata refresh" as deferred-to-v0.5.
- [ ] REQUIREMENTS.md Traceability table contains: CFG-07 (Complete), CFG-08 (Complete), DIAG-01 (Deferred), CERT-EXP-01 (Deferred), SLO-01 (Deferred). No IDP-INIT-01 row.
- [ ] REQUIREMENTS.md Coverage block reads `v0.5 requirements: 2 total` and `Deferred to v0.6: 3`.
- [ ] REQUIREMENTS.md footer cites Phase 21.2 + 2026-05-07.
- [ ] **`.planning/milestones/v0.5-REQUIREMENTS.md` was NOT created** (D-21.2-09 + Pitfall 5).
- [ ] No file under `lib/`, `test/`, `.planning/milestones/`, `.planning/PROJECT.md`, `.planning/MILESTONE-ARC.md`, or `README.md` was modified by this plan.
- [ ] Commit landed with message `docs(21.2-02): refresh REQUIREMENTS.md in place v0.4 → v0.5 (CFG-07 + CFG-08 Complete; DIAG-01 + CERT-EXP-01 + SLO-01 deferred to v0.6)`.

</success_criteria>

<output>
After completion, create
`.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-02-SUMMARY.md`
documenting:

- Each of the 7 blocks edited (line numbers in the pre-edit file + diff recap).
- Confirmation of all acceptance_criteria greps (paste output).
- Confirmation of the **negative-existence** (Pitfall 5) check: no
  `.planning/milestones/v0.5-REQUIREMENTS.md` was created. Reference
  D-21.2-09 + RESEARCH § Pitfall 5 explicitly in the SUMMARY.
- Confirmation of file-touch invariant (only REQUIREMENTS.md modified).
- Cross-doc coherence rows owned by this plan reported green.
- Threat register dispositions (all `n/a` / `mitigate`); explicit confirmation
  that the new CERT-EXP-01 description is public-safe.
- Reference to the v0.5 milestone audit (`.planning/v0.5-MILESTONE-AUDIT.md`
  scope_drift block) as the gap-closure motivation.
</output>
</content>
</invoke>