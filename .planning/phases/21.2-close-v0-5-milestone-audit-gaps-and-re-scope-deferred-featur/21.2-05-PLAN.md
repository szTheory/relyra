---
phase: 21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-bulk-operations/20-VALIDATION.md
autonomous: false
# Phase 21.2 has no functional REQ-IDs (doc-only closure phase per CONTEXT.md additional_context)
requirements: []
commit: per-task
must_haves:
  truths:
    - "File `.planning/phases/20-bulk-operations/20-VALIDATION.md` exists post-plan — per D-21.2-13."
    - "20-VALIDATION.md frontmatter contains `nyquist_compliant: true` (expected; Phase 20 verification is 4/4 clean per 20-VERIFICATION.md) — per D-21.2-13 + RESEARCH § 5 expected outcome."
    - "20-VALIDATION.md is produced by `/gsd-validate-phase 20` (the auditor) — NOT hand-written. The auditor reads 20-CONTEXT.md, 20-RESEARCH.md, 20-01-PLAN.md, 20-01-SUMMARY.md, 20-02-PLAN.md, 20-02-SUMMARY.md, 20-VERIFICATION.md and emits 20-VALIDATION.md following the 21-VALIDATION.md analog shape — per D-21.2-13 + RESEARCH § 5 + 'Don't Hand-Roll' table."
    - "If the auditor surfaces unexpected gaps (i.e., `nyquist_compliant: false` or PARTIAL), the plan HALTS and surfaces a planning question to the operator rather than expanding 21.2 scope — per D-21.2-13 contingency + RESEARCH § 5."
    - "Files modified by this plan are exactly `phases/20-bulk-operations/20-VALIDATION.md` (the auditor commits its own artifact via `commit_docs: true`); no `lib/` changes (D-21.2-03), no other `.planning/` artifacts touched."
    - "Cross-doc coherence rows it owns: 'Phase 20 has Nyquist artifact' (`test -f` succeeds); 'Phase 20 Nyquist is compliant' (`grep -E '^nyquist_compliant: true' ...` returns 1 match — expected)."
  artifacts:
    - path: ".planning/phases/20-bulk-operations/20-VALIDATION.md"
      provides: "Phase 20 Nyquist validation artifact — produced by /gsd-validate-phase auditor; mirrors 21-VALIDATION.md shape (Test Infrastructure / Sampling Rate / Per-Task Verification Map / Wave 0 Requirements / Manual-Only Verifications / Validation Sign-Off)"
      contains: "nyquist_compliant: true"
      min_lines: 50
  key_links:
    - from: ".planning/phases/20-bulk-operations/20-VALIDATION.md"
      to: ".planning/phases/20-bulk-operations/20-VERIFICATION.md"
      via: "Auditor reads VERIFICATION (4/4 clean) and stamps the Nyquist artifact"
      pattern: "nyquist_compliant"
    - from: ".planning/phases/20-bulk-operations/20-VALIDATION.md"
      to: ".planning/phases/21-scheduled-metadata-refresh/21-VALIDATION.md"
      via: "Structural template — same auditor produces both, same shape"
      pattern: "Per-Task Verification Map"
---

<objective>
Close v0.5 milestone audit Nyquist gap (PARTIAL — Phase 20 has no `20-VALIDATION.md`,
Phase 21 does) by running `/gsd-validate-phase 20` as a 21.2 ride-along. The auditor
reads Phase 20's existing artifacts (CONTEXT, RESEARCH, both plans, both SUMMARYs,
VERIFICATION 4/4 clean) and emits `phases/20-bulk-operations/20-VALIDATION.md`
following the 21-VALIDATION.md structural analog (verified at
`.planning/phases/21-scheduled-metadata-refresh/21-VALIDATION.md`, 121 lines).

Purpose: Phase 20's verification is already 4/4 clean (per `20-VERIFICATION.md`); the
auditor's job here is mechanical — re-trace coverage and stamp the artifact. This
closes the Nyquist coverage gap that flipped the milestone score to PARTIAL.

Per D-21.2-14 ("Nyquist artifact production may be split into its own plan so its
commits are atomic + bounded"), this is a single-task plan with the auditor as the
sole executor. **The plan does NOT hand-write `20-VALIDATION.md`** — that would
diverge from the auditor's canonical structure (RESEARCH § Don't Hand-Roll).

Output: `.planning/phases/20-bulk-operations/20-VALIDATION.md`, expected
`nyquist_compliant: true` (per RESEARCH § 5 expected outcome). Contingency: if the
auditor surfaces unexpected gaps, the plan halts and surfaces a planning question
to the operator rather than auto-fixing (per D-21.2-13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-bulk-operations/20-VERIFICATION.md
@.planning/phases/21-scheduled-metadata-refresh/21-VALIDATION.md
@.planning/v0.5-MILESTONE-AUDIT.md
@.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-CONTEXT.md
@.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-RESEARCH.md

<interfaces>
<!--
This plan invokes a Skill (`/gsd-validate-phase 20`) rather than editing files
directly. The "interface" is the auditor's input contract (what files it reads)
and output contract (what it writes + commits).
-->

Auditor input contract — files the auditor reads to produce 20-VALIDATION.md:
- `.planning/phases/20-bulk-operations/20-CONTEXT.md`
- `.planning/phases/20-bulk-operations/20-RESEARCH.md`
- `.planning/phases/20-bulk-operations/20-01-PLAN.md`
- `.planning/phases/20-bulk-operations/20-01-SUMMARY.md`
- `.planning/phases/20-bulk-operations/20-02-PLAN.md`
- `.planning/phases/20-bulk-operations/20-02-SUMMARY.md`
- `.planning/phases/20-bulk-operations/20-VERIFICATION.md` (4/4 clean — verified;
  status: complete; score: 4/4 must-haves verified; gaps: [])

Auditor output contract — file the auditor writes (committing its own artifact via
`commit_docs: true` in `.planning/config.json`):
- `.planning/phases/20-bulk-operations/20-VALIDATION.md` — frontmatter +
  Test Infrastructure block + Sampling Rate block + Per-Task Verification Map +
  Wave 0 Requirements + Manual-Only Verifications + Validation Sign-Off
  (analog: `.planning/phases/21-scheduled-metadata-refresh/21-VALIDATION.md`)

Expected `20-VALIDATION.md` frontmatter shape (matches 21-VALIDATION.md):
```yaml
---
phase: 20
slug: bulk-operations
status: draft               # template default; auditor may set to complete if work is finalized
nyquist_compliant: true     # expected — Phase 20 VERIFICATION is 4/4 clean
wave_0_complete: false      # template default for non-Wave-0-using phases
created: 2026-05-07
---
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run /gsd-validate-phase 20 ride-along — produce 20-VALIDATION.md</name>
  <files>.planning/phases/20-bulk-operations/20-VALIDATION.md</files>

  <read_first>
    - .planning/phases/20-bulk-operations/20-VERIFICATION.md (full file — confirms 4/4 must-haves verified, gaps: [], status: complete; this is the auditor's primary input)
    - .planning/phases/21-scheduled-metadata-refresh/21-VALIDATION.md (full file — 121 lines; structural analog; the auditor will produce 20-VALIDATION.md following the same section ordering: frontmatter / Test Infrastructure / Sampling Rate / Per-Task Verification Map / Wave 0 Requirements / Manual-Only Verifications / Validation Sign-Off)
    - .planning/v0.5-MILESTONE-AUDIT.md lines 75-80 + 144-149 (Nyquist coverage section — the audit explicitly flagged Phase 20's missing 20-VALIDATION.md as the PARTIAL signal)
    - .planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-CONTEXT.md (LOCKED decisions D-21.2-13, D-21.2-14)
    - .planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-RESEARCH.md § "5. Phase 20 Nyquist Validation Ride-Along" (auditor invocation pattern + expected outcome + contingency rules)
    - .planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-RESEARCH.md § "Don't Hand-Roll" + "Code Examples" (verification commands)
    - .planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-RESEARCH.md § "Environment Availability" (confirms `/gsd-validate-phase` skill is assumed available; if unavailable, halt + surface)
  </read_first>

  <behavior>
    - D-21.2-13: invoke `/gsd-validate-phase 20`. The auditor reads the 7 input files listed in `<interfaces>` above and emits `20-VALIDATION.md`.
    - D-21.2-14: this is the dedicated plan for the Nyquist artifact — atomic + bounded, no other concerns folded in.
    - D-21.2-13 contingency: if the auditor returns `nyquist_compliant: true` (expected — Phase 20 verification is 4/4 clean), the task is done. If the auditor returns `nyquist_compliant: false` or PARTIAL or surfaces unexpected gaps, the task HALTS and surfaces a planning question to the operator; do NOT auto-widen 21.2 to fix Phase 20 Wave-0 gaps.
    - The auditor commits its own artifact (per `commit_docs: true` in `.planning/config.json`); this task does NOT manually create a separate commit for `20-VALIDATION.md`.
    - This plan touches NO file under `lib/`, NO file under `test/`, and NO file under `.planning/` other than the new `20-VALIDATION.md` (D-21.2-03). The auditor's authority is bounded to its own artifact.
    - RESEARCH § Don't Hand-Roll is explicit: "Hand-write `20-VALIDATION.md`" is the anti-pattern. Use `/gsd-validate-phase 20` (which knows the canonical structure) instead. The plan-checker will flag any hand-rolled `20-VALIDATION.md` content.
    - Cross-doc coherence rows owned by this plan:
      - `test -f .planning/phases/20-bulk-operations/20-VALIDATION.md` exits 0.
      - `grep -E "^nyquist_compliant: true" .planning/phases/20-bulk-operations/20-VALIDATION.md` returns 1 match (expected — Phase 20 VERIFICATION is 4/4 clean).
  </behavior>

  <action>
    Run `/gsd-validate-phase 20` and let the auditor produce
    `phases/20-bulk-operations/20-VALIDATION.md`. The auditor reads the existing
    Phase 20 artifacts and emits the canonical Nyquist validation file.

    ────────────────────────────────────────────────────────────────────────────
    PRE-FLIGHT CHECKS (run before invoking the auditor)
    ────────────────────────────────────────────────────────────────────────────

    1. Confirm Phase 20 input artifacts exist:
       ```bash
       test -f .planning/phases/20-bulk-operations/20-VERIFICATION.md && \
       test -f .planning/phases/20-bulk-operations/20-01-PLAN.md && \
       test -f .planning/phases/20-bulk-operations/20-01-SUMMARY.md && \
       test -f .planning/phases/20-bulk-operations/20-02-PLAN.md && \
       test -f .planning/phases/20-bulk-operations/20-02-SUMMARY.md
       ```
       If any input is missing, HALT and surface to operator (the auditor cannot
       run on incomplete inputs; this would indicate a Phase 20 gap outside 21.2's
       scope).

    2. Confirm `20-VALIDATION.md` does NOT already exist (sanity check — should be
       absent pre-plan):
       ```bash
       test ! -f .planning/phases/20-bulk-operations/20-VALIDATION.md
       ```
       If it already exists, HALT and surface to operator (something else
       produced it; do not overwrite without operator confirmation).

    3. Confirm the auditor skill is available:
       ```bash
       # Check that /gsd-validate-phase or the gsd-validate-phase Skill is
       # accessible. If neither is available, HALT and surface — do NOT
       # hand-write 20-VALIDATION.md (RESEARCH § Don't Hand-Roll + Pitfall: the
       # hand-rolled file would diverge from the canonical structure the
       # auditor expects to produce).
       ```

    4. Confirm Phase 20 VERIFICATION is 4/4 clean (expected):
       ```bash
       grep -E "^score: 4/4 must-haves verified" .planning/phases/20-bulk-operations/20-VERIFICATION.md && \
       grep -E "^gaps: \[\]" .planning/phases/20-bulk-operations/20-VERIFICATION.md
       ```
       If the score has changed since 21.2 planning (it should not — Phase 20 is
       complete), HALT and surface (this is outside 21.2's scope).

    ────────────────────────────────────────────────────────────────────────────
    INVOKE THE AUDITOR
    ────────────────────────────────────────────────────────────────────────────

    Run `/gsd-validate-phase 20`. The standard invocation is via the GSD slash-command
    or the equivalent Skill (`Skill(skill="gsd-validate-phase", args="20 ${GSD_WS}")`
    per RESEARCH § 5). The auditor:

    1. Reads the 7 Phase 20 input files listed in `<interfaces>` above.
    2. Constructs the canonical `20-VALIDATION.md` frontmatter (`phase: 20`, `slug:
       bulk-operations`, `status:` draft, `nyquist_compliant:` value reflecting
       VERIFICATION coverage — expected `true`, `wave_0_complete:` value reflecting
       Phase 20's Wave-0 posture, `created: 2026-05-07`).
    3. Populates the body sections in the order established by 21-VALIDATION.md:
       Test Infrastructure / Sampling Rate / Per-Task Verification Map / Wave 0
       Requirements / Manual-Only Verifications / Validation Sign-Off.
    4. Writes the artifact to
       `.planning/phases/20-bulk-operations/20-VALIDATION.md`.
    5. Commits the artifact via `commit_docs: true` (per `.planning/config.json`;
       confirmed in RESEARCH § 5 by the precedent that `21-VALIDATION.md` was
       committed alongside Phase 21 wrap-up commits).

    ────────────────────────────────────────────────────────────────────────────
    POST-FLIGHT CHECKS (run after auditor completes)
    ────────────────────────────────────────────────────────────────────────────

    Run the verification commands in `<verify>` below. The two BLOCKING checks
    (per RESEARCH § Code Examples) are:
    ```bash
    test -f .planning/phases/20-bulk-operations/20-VALIDATION.md
    grep -E "^nyquist_compliant: true" .planning/phases/20-bulk-operations/20-VALIDATION.md
    ```

    If `nyquist_compliant: true` is present, the plan is complete (the auditor
    found Phase 20's coverage acceptable, matching the 4/4 VERIFICATION score).

    ────────────────────────────────────────────────────────────────────────────
    CONTINGENCY (D-21.2-13)
    ────────────────────────────────────────────────────────────────────────────

    If the auditor returns `nyquist_compliant: false` or PARTIAL, OR surfaces
    unexpected gaps (e.g., missing Wave-0 stubs, sampling-rate violations, etc.):

    1. **HALT — do NOT auto-widen 21.2.**
    2. Surface the gap to the operator with the exact text the auditor produced.
    3. Frame as a planning question: "The Phase 20 Nyquist auditor surfaced
       {N} unexpected gaps: {paste-list}. These appear to be Phase 20 issues
       outside 21.2's stated scope (D-21.2-13 explicitly flags this contingency).
       Treat as a deferred item and proceed with milestone closure, or expand
       21.2 / insert a new Phase 20 closure plan?"
    4. Do NOT delete or modify the partially-produced 20-VALIDATION.md — it is
       the auditor's contemporaneous gap report and should be preserved as
       evidence.

    ────────────────────────────────────────────────────────────────────────────
    Critical guardrails (per RESEARCH § Don't Hand-Roll + Common Pitfalls):
    ────────────────────────────────────────────────────────────────────────────

    1. **DO NOT hand-write `20-VALIDATION.md`.** RESEARCH § Don't Hand-Roll is
       explicit: the auditor knows the canonical structure (frontmatter fields,
       section ordering, sign-off checklist). Hand-rolled content would diverge
       and trip downstream consumers (the orchestrator's `nyquist_validation`
       gate at workflow step 5.5). Use `/gsd-validate-phase 20` instead.
    2. **DO NOT auto-widen 21.2 if the auditor surfaces unexpected gaps.** The
       contingency handler (D-21.2-13) explicitly forbids silently expanding the
       phase scope to fix Phase 20 issues from inside 21.2.
    3. **DO NOT commit a separate "manual" commit for `20-VALIDATION.md`.** The
       auditor commits its own artifact via `commit_docs: true`; a manual commit
       would create a redundant commit and break the auditor's atomic-commit
       contract.
    4. DO NOT touch any file under `lib/`, `test/`, `.planning/PROJECT.md`,
       `.planning/REQUIREMENTS.md`, `.planning/MILESTONE-ARC.md`, `README.md`, or
       `.planning/milestones/` (D-21.2-03; sibling plans 21.2-01..04 own those).
    5. DO NOT overwrite `20-VERIFICATION.md` (it is the auditor's INPUT, not its
       output). The auditor writes only `20-VALIDATION.md`.
    6. **PITFALL 4 sanity:** before any commit, verify no pre-commit hook is
       registered (`git config --get core.hooksPath` returns empty). The auditor
       inherits the same git context as the planner; if `mix format` drift in
       `lib/relyra/live_admin/connections_live.ex` (pre-existing per
       21.2-CONTEXT.md `<deferred>`) blocks the commit, halt + surface.
    7. **Pitfall 5 cross-phase invariant:** verify post-plan
       `test ! -f .planning/milestones/v0.5-REQUIREMENTS.md` still succeeds (the
       auditor must not preempt the milestone-archive snapshot).
  </action>

  <verify>
    <automated>
# BLOCKING — auditor produced the artifact (cross-doc invariant "Phase 20 has Nyquist artifact")
test -f .planning/phases/20-bulk-operations/20-VALIDATION.md && \
# BLOCKING — auditor stamped Phase 20 as Nyquist-compliant (cross-doc invariant "Phase 20 Nyquist is compliant")
grep -E "^nyquist_compliant: true" .planning/phases/20-bulk-operations/20-VALIDATION.md && \
# Frontmatter sanity — phase number is 20
grep -E "^phase: 20" .planning/phases/20-bulk-operations/20-VALIDATION.md && \
# Frontmatter sanity — slug is bulk-operations
grep -E "^slug: bulk-operations" .planning/phases/20-bulk-operations/20-VALIDATION.md && \
# Structural sanity — auditor produced the canonical sections (matches 21-VALIDATION.md analog)
grep -F "## Test Infrastructure" .planning/phases/20-bulk-operations/20-VALIDATION.md && \
grep -F "## Sampling Rate" .planning/phases/20-bulk-operations/20-VALIDATION.md && \
grep -F "## Per-Task Verification Map" .planning/phases/20-bulk-operations/20-VALIDATION.md && \
grep -F "## Validation Sign-Off" .planning/phases/20-bulk-operations/20-VALIDATION.md && \
# File-touch invariant: only 20-VALIDATION.md added by this plan; no lib/ or test/ changes
test "$(git diff --name-only HEAD~1 HEAD -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/MILESTONE-ARC.md README.md lib/ test/ .planning/milestones/ 2>/dev/null | tr '\n' ' ')" = "" && \
# Negative-existence (Pitfall 5 cross-phase invariant): no v0.5 archive
test ! -f .planning/milestones/v0.5-REQUIREMENTS.md && \
# Sanity: 20-VERIFICATION.md was NOT modified (auditor reads, does not write)
test "$(git diff --name-only HEAD -- .planning/phases/20-bulk-operations/20-VERIFICATION.md | tr '\n' ' ')" = ""
    </automated>
  </verify>

  <acceptance_criteria>
    - **BLOCKING — file existence:** `test -f .planning/phases/20-bulk-operations/20-VALIDATION.md` exits 0 (cross-doc invariant "Phase 20 has Nyquist artifact").
    - **BLOCKING — Nyquist-compliant flag:** `grep -E "^nyquist_compliant: true" .planning/phases/20-bulk-operations/20-VALIDATION.md` returns exactly 1 match (cross-doc invariant "Phase 20 Nyquist is compliant"; expected per RESEARCH § 5 since Phase 20 VERIFICATION is 4/4 clean).
    - Frontmatter `phase: 20`: `grep -cE "^phase: 20" .planning/phases/20-bulk-operations/20-VALIDATION.md` returns ≥ 1.
    - Frontmatter `slug: bulk-operations`: `grep -cE "^slug: bulk-operations" .planning/phases/20-bulk-operations/20-VALIDATION.md` returns 1.
    - Structural sanity (matches 21-VALIDATION.md analog at `.planning/phases/21-scheduled-metadata-refresh/21-VALIDATION.md`):
      - `grep -F "## Test Infrastructure" .planning/phases/20-bulk-operations/20-VALIDATION.md` returns 1 match.
      - `grep -F "## Sampling Rate" .planning/phases/20-bulk-operations/20-VALIDATION.md` returns 1 match.
      - `grep -F "## Per-Task Verification Map" .planning/phases/20-bulk-operations/20-VALIDATION.md` returns 1 match.
      - `grep -F "## Validation Sign-Off" .planning/phases/20-bulk-operations/20-VALIDATION.md` returns 1 match.
    - File-length sanity: `wc -l .planning/phases/20-bulk-operations/20-VALIDATION.md` returns ≥ 50 lines (Phase 20 has 2 plans → expected smaller than Phase 21's 121-line VALIDATION but still has all canonical sections).
    - File-touch invariant (D-21.2-03): no file under `lib/`, `test/`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/MILESTONE-ARC.md`, `README.md`, or `.planning/milestones/` was modified by this plan or by the auditor on this plan's behalf. (The auditor's commit will include only `phases/20-bulk-operations/20-VALIDATION.md`; `git log --name-only HEAD` should show that.)
    - Phase 20 input integrity: `git diff --name-only HEAD -- .planning/phases/20-bulk-operations/20-VERIFICATION.md` returns empty (the auditor reads VERIFICATION but does NOT modify it).
    - Negative-existence (Pitfall 5 cross-phase invariant): `test ! -f .planning/milestones/v0.5-REQUIREMENTS.md` succeeds.
    - **Contingency check (D-21.2-13):** if `nyquist_compliant: true` is NOT present (i.e., auditor surfaced a gap), the plan-checker MUST flag this as a HALT condition; SUMMARY.md must include the auditor's gap output and the operator's decision before the plan is marked complete.
  </acceptance_criteria>

  <done>
    - `.planning/phases/20-bulk-operations/20-VALIDATION.md` exists with `nyquist_compliant: true` (or, if the auditor surfaced unexpected gaps, the plan halted and the operator decision is captured in SUMMARY.md).
    - All acceptance_criteria greps green (in the expected `nyquist_compliant: true` path).
    - The auditor committed its own artifact via `commit_docs: true`; no manual commit was created for `20-VALIDATION.md`.
    - File-touch invariant green (no `lib/`, no `test/`, no other `.planning/` artifacts modified by this plan).
    - SUMMARY.md confirms the auditor invocation, the resulting frontmatter values, the canonical-section list, and the contingency disposition (compliant / halted-with-gaps).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/gsd-validate-phase` auditor ↔ Phase 20 historical artifacts | The auditor reads Phase 20's existing CONTEXT / RESEARCH / PLAN / SUMMARY / VERIFICATION and emits a Nyquist stamp. The relevant boundary is the auditor's authority bound to its own output artifact (`20-VALIDATION.md`); it must not modify any input. Acceptance grep enforces `20-VERIFICATION.md` integrity post-run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.2-05-01 | Spoofing | n/a (auditor identity is the local skill invocation; no auth boundary) | n/a | Local skill invocation; no remote-identity surface. |
| T-21.2-05-02 | Tampering | Phase 20 historical artifacts (VERIFICATION / SUMMARYs) | mitigate | Acceptance criterion verifies `git diff --name-only HEAD -- .planning/phases/20-bulk-operations/20-VERIFICATION.md` returns empty post-run — the auditor reads VERIFICATION but MUST NOT modify it. The auditor's authority is bounded to producing `20-VALIDATION.md`. |
| T-21.2-05-03 | Repudiation | Nyquist coverage stamp | mitigate | The auditor's frontmatter `created: 2026-05-07` + git commit timestamp record exactly when the Nyquist stamp was applied, allowing future audit reconstruction. The artifact is committed (commit_docs: true) so the historical record cannot be silently re-stamped. |
| T-21.2-05-04 | Information Disclosure | `20-VALIDATION.md` content | accept | The artifact is structural (canonical section headings + a Per-Task Verification Map populated from existing PLAN/SUMMARY data). It surfaces only information already present in committed Phase 20 artifacts; no new disclosure surface. |
| T-21.2-05-05 | Denial of Service | Auditor invocation | accept | Local skill invocation; if the auditor is unavailable, the contingency handler in `<action>` (pre-flight check 3) halts the plan and surfaces to the operator. Acceptable risk; no DoS amplification. |
| T-21.2-05-06 | Elevation of Privilege | n/a (no privilege boundary) | n/a | Doc-producing skill; no executable code path beyond reading existing files + writing one new artifact. |
| T-21.2-05-07 | Tampering — auto-widening 21.2 | This plan's scope | **mitigate** | The contingency handler (D-21.2-13) explicitly forbids auto-widening 21.2 if the auditor surfaces unexpected gaps. Acceptance criterion includes a HALT condition for the `nyquist_compliant: false` / PARTIAL case; SUMMARY.md must capture the auditor's gap output for operator review. The plan-checker will block any silent expansion of 21.2's stated scope. |

</threat_model>

<verification>
Plan-level success criteria (all must be true before plan-checker / phase verify):

1. **BLOCKING:** `.planning/phases/20-bulk-operations/20-VALIDATION.md` exists post-plan.
2. **BLOCKING:** `grep -E "^nyquist_compliant: true" .planning/phases/20-bulk-operations/20-VALIDATION.md` returns 1 match (expected per RESEARCH § 5; if not, the contingency handler must have recorded the gap output and operator decision).
3. The artifact was produced by `/gsd-validate-phase 20`, not hand-written (RESEARCH § Don't Hand-Roll). The structural-sanity acceptance criteria verify the canonical sections are present in the canonical order.
4. Phase 20 input artifacts (especially `20-VERIFICATION.md`) are unchanged post-run — auditor reads, does not write.
5. No file under `lib/`, `test/`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/MILESTONE-ARC.md`, `README.md`, or `.planning/milestones/` was modified by this plan.
6. `.planning/milestones/v0.5-REQUIREMENTS.md` was NOT created (Pitfall 5 cross-phase invariant).
7. Cross-doc coherence rows owned by this plan (per 21.2-RESEARCH.md § "Cross-Doc
   Coherence Invariants") are independently green:
   - "Phase 20 has Nyquist artifact" (`test -f` succeeds).
   - "Phase 20 Nyquist is compliant" (`grep -E "^nyquist_compliant: true" ...` returns 1 match — expected).
8. Threat register dispositions: T-21.2-05-02 + T-21.2-05-03 + T-21.2-05-07 are `mitigate`; T-21.2-05-04 + T-21.2-05-05 are `accept` with rationale; others `n/a`. No HIGH-severity unmitigated.
9. Contingency disposition recorded in SUMMARY.md: either "compliant — `nyquist_compliant: true`" OR "halted-with-gaps — operator decision: {…}". No silent auto-widening.
</verification>

<success_criteria>

The plan is complete when:

- [ ] `/gsd-validate-phase 20` was invoked (the auditor; not hand-rolled).
- [ ] `.planning/phases/20-bulk-operations/20-VALIDATION.md` exists.
- [ ] `20-VALIDATION.md` frontmatter contains `nyquist_compliant: true` (expected) — OR the contingency handler halted the plan and recorded the operator's decision in SUMMARY.md.
- [ ] `20-VALIDATION.md` follows the canonical 21-VALIDATION.md structural template (Test Infrastructure / Sampling Rate / Per-Task Verification Map / Wave 0 Requirements / Manual-Only Verifications / Validation Sign-Off).
- [ ] `20-VERIFICATION.md` is unchanged post-run (auditor read it but did not modify it).
- [ ] No file under `lib/`, `test/`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/MILESTONE-ARC.md`, `README.md`, or `.planning/milestones/` was modified by this plan.
- [ ] `.planning/milestones/v0.5-REQUIREMENTS.md` was NOT created.
- [ ] Auditor's auto-commit landed (per `commit_docs: true`); no separate manual commit was created for `20-VALIDATION.md`.

</success_criteria>

<output>
After completion, create
`.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-05-SUMMARY.md`
documenting:

- The exact auditor invocation (slash-command vs. Skill) used.
- Pre-flight checks and their outcomes (4 checks listed in the action block).
- The auditor's verdict: `nyquist_compliant: true` (expected) OR `false`/PARTIAL with the auditor's gap text pasted verbatim.
- If compliant: the canonical sections produced + line count; paste the frontmatter block.
- If non-compliant: the contingency disposition (operator question framed; auditor output preserved as evidence; recommended next-step e.g., "treat as deferred", "expand 21.2", "insert a new Phase 20 closure plan"). DO NOT auto-fix from inside this SUMMARY.
- Confirmation that `20-VERIFICATION.md` was not modified.
- Confirmation of file-touch invariant (no `lib/`, no `test/`, no sibling `.planning/` artifacts touched by this plan or its auditor).
- Confirmation of negative-existence (no `.planning/milestones/v0.5-REQUIREMENTS.md`).
- Cross-doc coherence rows owned by this plan reported green (or, if non-compliant, the rows that DID land plus the gap that did not).
- Threat register dispositions (T-21.2-05-02 + T-21.2-05-03 + T-21.2-05-07 mitigate; others accept/`n/a`).
- Reference to the v0.5 milestone audit (`.planning/v0.5-MILESTONE-AUDIT.md` § 5 Nyquist coverage) as the gap-closure motivation; reference to D-21.2-13 + D-21.2-14 as the LOCKED contracts implemented.
- Reference to RESEARCH § Don't Hand-Roll explicitly (this plan delegates to the auditor; it does NOT hand-write the artifact).
</output>
</content>
</invoke>