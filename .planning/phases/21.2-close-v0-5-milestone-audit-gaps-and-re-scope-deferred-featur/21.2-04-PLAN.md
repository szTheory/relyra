---
phase: 21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
# Phase 21.2 has no functional REQ-IDs (doc-only closure phase per CONTEXT.md additional_context)
requirements: []
commit: per-task
must_haves:
  truths:
    - "README.md gains a new `## Operations: Bulk actions` heading inserted BEFORE the existing `## Operations: Scheduled metadata refresh` heading at the existing line 51 — per D-21.2-10 + RESEARCH § Site 4 placement."
    - "The new subsection contains a narrative paragraph + a 3-action recipe (Enable / Disable / Refresh Metadata) drawn from `Relyra.Ecto.BulkActions.run/4` — per D-21.2-11."
    - "The new subsection references the audit-cohesion guarantee delivered by Phase 21.1 (one shared `correlation_id` per bulk run) — per D-21.2-12."
    - "The new subsection is brand-voice clean: zero matches for `polling`, `cron job`, `blocked`, `retry`, `circuit breaker`, `MaxBackoff`, `magic`, `bulletproof`, `military-grade` within the slice between `## Operations: Bulk actions` and the next `##` heading — per RESEARCH § brand-voice grep invariants + Pitfall 2."
    - "The new subsection includes exactly one `BulkActions.run/4` Elixir code-fence example (no other code samples) — per D-21.2-11 + RESEARCH § 'What the planner should NOT include'."
    - "The example uses placeholder values (`MyApp.Repo`, `\"01JT...\"`, `\"ops@example.com\"`) — no real connection IDs, no internal hostnames — per Threat Register I row + CONTEXT additional_context (security gate)."
    - "The new subsection does NOT reference v0.6 features (Debug bundles, Expiry alerts) — per RESEARCH § 'What the planner should NOT include'."
    - "Cross-doc coherence rows it owns: 'README has Bulk actions section' (1 match for `^## Operations: Bulk actions`); 'README does NOT use brand-banned terms in the new section' (0 matches in the awk slice)."
  artifacts:
    - path: "README.md"
      provides: "Operations: Bulk actions subsection — narrative + 3-action recipe + audit-cohesion blurb + 1 BulkActions.run/4 Elixir example"
      contains: "Operations: Bulk actions"
      min_lines: 160
  key_links:
    - from: "README.md"
      to: "lib/relyra/ecto/bulk_actions.ex"
      via: "BulkActions.run/4 public surface paraphrased in narrative + Elixir example"
      pattern: "BulkActions\\.run"
    - from: "README.md"
      to: "lib/relyra/live_admin/connections_live.ex"
      via: "LiveView multi-select narrative (3-button Bulk Actions menu)"
      pattern: "Bulk [Aa]ctions"
---

<objective>
Close v0.5 milestone audit gap **INT-02** (WARNING — Phase 20 bulk-actions surface
has zero README coverage) by adding a new `## Operations: Bulk actions` subsection
to `README.md` placed immediately BEFORE the existing `## Operations: Scheduled
metadata refresh` section (currently at line 51). The new subsection covers
multi-select + the three bulk actions (Enable / Disable / Refresh Metadata) and
references the audit-cohesion guarantee delivered by Phase 21.1 (shared
`correlation_id` per bulk run).

Purpose: README.md is the adopter-facing entry point (per Doc Authority Map in
21.2-RESEARCH.md). Adopters currently discover bulk-refresh only by clicking around
the LiveView — `grep -in "bulk\|multi.select" README.md` → 0 matches today (verified
in v0.5-MILESTONE-AUDIT.md:131-133). The new subsection closes the operator-narrative
coherence gap.

Output: a single insertion into README.md at line 51 (before the existing
`## Operations: Scheduled metadata refresh` heading), shape lifted verbatim from
21.2-RESEARCH.md § "4. README INT-02 Fix" recommended draft.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@prompts/relyra-brand-book.md
@.planning/v0.5-MILESTONE-AUDIT.md
@lib/relyra/ecto/bulk_actions.ex
@lib/relyra/live_admin/connections_live.ex
@.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-CONTEXT.md
@.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-RESEARCH.md

<interfaces>
<!--
The new README subsection paraphrases the BulkActions.run/4 public surface — the
shape below is verbatim from lib/relyra/ecto/bulk_actions.ex (29 lines total)
verified in 21.2-RESEARCH.md § 4 source-of-truth references. The single Elixir
code-fence in the new section uses these signatures.
-->

From lib/relyra/ecto/bulk_actions.ex (DO NOT MODIFY — paraphrased only):
```elixir
def run(repo, ids, action_fun, opts) do
  audit = Keyword.get(opts, :audit, %{})
  correlation_id = Map.get(audit, :correlation_id) || Ecto.UUID.generate()
  opts_with_audit = Keyword.put(opts, :audit, Map.put(audit, :correlation_id, correlation_id))
  opts_with_repo = Keyword.put_new(opts_with_audit, :repo, repo)
  results = Enum.map(ids, fn id -> {id, action_fun.(id, opts_with_repo)} end) |> Map.new()
  {:ok, results}
end
```
Key facts the README narrative paraphrases (verbatim from RESEARCH § 4):
- Sequential execution (`Enum.map`, not `Task.async_stream`).
- One shared `correlation_id` per run, auto-generated if absent.
- Returns `{:ok, %{id => result}}`.

From lib/relyra/live_admin/connections_live.ex:113-117 (DO NOT MODIFY — narrated only):
```elixir
action_fn =
  case action do
    "enable" -> &Connections.enable/2
    "disable" -> &Connections.disable/2
    "refresh_metadata" -> &Metadata.refresh/2
  end
```
The 3 bulk actions are exactly: Enable, Disable, Refresh Metadata.

Brand voice constraints (lifted from prompts/relyra-brand-book.md + RESEARCH § 4
brand-voice grep invariants):
- Banned terms in the new section: `polling`, `cron job`, `blocked`, `retry`, `circuit breaker`, `MaxBackoff`.
- Banned brand-marketing terms (PROJECT.md:134): `magic`, `bulletproof`, `military-grade`, "SAML is easy".
- Always use Relyra title case (never `reLyra`, `ReLyra`, `RELYRA`).
- No lyre/music/constellation/shield/padlock/key/flame/bird imagery in copy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: README.md — insert Operations: Bulk actions subsection at line 51</name>
  <files>README.md</files>

  <read_first>
    - README.md (full file or at minimum lines 40-150 — verify the line-51 placement assumption: existing `## Operations: Scheduled metadata refresh` heading is at line 51; insertion point is the blank line immediately ABOVE that heading)
    - prompts/relyra-brand-book.md §15 microcopy + §17 voice examples + §22 do/don't (banned terms enforced — RESEARCH § 4 brand-voice grep invariants section); confirm "always Relyra title case", "no magic/bulletproof/military-grade", no lyre/music/constellation imagery
    - lib/relyra/ecto/bulk_actions.ex (full file — 29 lines; the README narrative paraphrases this surface)
    - lib/relyra/live_admin/connections_live.ex (lines 106-131 + 356-361 — multi-select state + Bulk Actions menu render; narrate the LiveView surface, do not modify)
    - .planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-CONTEXT.md (LOCKED decisions D-21.2-10, D-21.2-11, D-21.2-12 + `<specifics>` line 151 = placement recommendation)
    - .planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-RESEARCH.md § "4. README INT-02 Fix" (verbatim recommended draft + brand-voice grep invariants + "What the planner should NOT include"; THIS is the source of truth for the new subsection's content)
    - .planning/v0.5-MILESTONE-AUDIT.md:47-59 (INT-02 motivation; confirms `grep -in "bulk\|multi.select" README.md` returns 0 matches today)
  </read_first>

  <behavior>
    - D-21.2-10: insert new `## Operations: Bulk actions` subsection. Placement: immediately BEFORE the existing `## Operations: Scheduled metadata refresh` heading at line 51 (CONTEXT `<specifics>` line 151 + RESEARCH § 4 placement recommendation).
    - D-21.2-11: content = short narrative paragraph + recipe for each of the three bulk actions (Enable / Disable / Refresh Metadata) + one Elixir code-fence example using BulkActions.run/4. No new code samples beyond what BulkActions.run/4 already exposes.
    - D-21.2-12: reference the audit-cohesion guarantee delivered by Phase 21.1 (one shared `correlation_id` per bulk run) in the audit-related blurb.
    - Brand-voice invariants (RESEARCH § 4 + Pitfall 2): the new subsection must contain ZERO matches for `polling`, `cron job`, `blocked`, `retry`, `circuit breaker`, `MaxBackoff` (case-insensitive). Plus PROJECT.md:134 brand-marketing bans (`magic`, `bulletproof`, `military-grade`).
    - Threat register I/D: example values use placeholders (`MyApp.Repo`, `"01JT..."`, `"ops@example.com"`, `"scripted_bulk_refresh"`) — no real connection IDs, no internal hostnames.
    - The new subsection does NOT reference v0.6 features (Debug bundles, Expiry alerts) — RESEARCH § 4 "What the planner should NOT include".
    - Cross-doc coherence rows owned by this plan:
      - `grep -E "^## Operations: Bulk actions" README.md` returns 1 match.
      - `awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -iE "polling|cron job|blocked|retry|circuit breaker|MaxBackoff|magic|bulletproof|military-grade"` returns 0 matches.
  </behavior>

  <action>
    Insert the new `## Operations: Bulk actions` subsection into `README.md`
    immediately BEFORE the existing `## Operations: Scheduled metadata refresh`
    heading at line 51. The exact insertion shape is verbatim from 21.2-RESEARCH.md
    § "4. README INT-02 Fix" recommended draft.

    ────────────────────────────────────────────────────────────────────────────
    INSERTION POINT (verify before editing)
    ────────────────────────────────────────────────────────────────────────────

    The existing `## Operations: Scheduled metadata refresh` heading is at line 51
    (verified in 21.2-RESEARCH.md § 4 + by direct read in this plan's preparation).
    The blank line immediately above it (line 50) is the insertion-point anchor.

    Find this exact 2-line context window in README.md:
    ```
    - [Security policy](SECURITY.md)

    ## Operations: Scheduled metadata refresh
    ```

    The blank line between `[Security policy]` and `## Operations: Scheduled
    metadata refresh` is where the new subsection content goes — INSERT the new
    subsection so the `## Operations: Scheduled metadata refresh` line is pushed
    further down.

    ────────────────────────────────────────────────────────────────────────────
    NEW SUBSECTION TEXT TO INSERT (verbatim from RESEARCH § 4 recommended draft)
    ────────────────────────────────────────────────────────────────────────────

    Insert the following block AS A WHOLE (do not edit the prose, do not add
    commentary, do not include a fenced code block other than the single
    BulkActions.run/4 example shown). After this block, the existing
    `## Operations: Scheduled metadata refresh` heading must remain on its own
    line, unchanged:

    ```markdown
    ## Operations: Bulk actions

    Phase 20 ships a multi-select bulk-actions surface in the LiveView admin so operators can run lifecycle and metadata actions across many connections in one operator action. Each bulk run produces one shared audit `correlation_id` so the resulting per-connection ledger rows can be reconstructed as a single batch — Phase 21.1 closed the audit forwarding gap so this guarantee holds for every supported action.

    From the connections list, select one or more connections via the row checkboxes. The Bulk Actions menu appears once at least one row is selected and exposes three actions:

    - **Enable** — Move every selected connection to the `enabled` lifecycle state. Each connection's trust change co-commits its own audit row inside its own transaction.
    - **Disable** — Move every selected connection to the `disabled` lifecycle state. Existing application sessions are not revoked.
    - **Refresh Metadata** — Re-fetch metadata for every selected connection from its registered remote source. The bulk path uses the same operator-triggered manual refresh seam as a single-connection refresh; new signing certificates stage as `:next` rather than promoting implicitly.

    Bulk actions execute sequentially (one connection at a time) to keep database pressure bounded and audit ordering explicit. The aggregate flash message reports per-connection success and failure counts; partial failures do not abort the batch.

    Audit trail: every per-connection row in a bulk run shares the bulk `correlation_id` and the operator-attributed `actor` / `cause`, so an operator reviewing the audit ledger can reconstruct "who ran which bulk action across which connections" from a single shared identifier.

    The seam is `Relyra.Ecto.BulkActions.run/4`. Hosts that want to drive bulk actions from a Mix task, a script, or a custom admin UI can call it directly with a list of `connection_id` values and one of the three action functions:

    ```elixir
    Relyra.Ecto.BulkActions.run(
      MyApp.Repo,
      ["01JT...", "01JT...", "01JT..."],
      &Relyra.Metadata.refresh/2,
      audit: %{actor: "ops@example.com", cause: "scripted_bulk_refresh"}
    )
    ```

    Returns `{:ok, %{connection_id => {:ok, _} | {:error, %Relyra.Error{}}}}`. The `correlation_id` is auto-generated if not supplied in the audit map.

    ```

    NOTE: the `markdown` opening fence (` ```markdown `) and the trailing closing
    fence are part of THIS PLAN'S annotation only — they MUST NOT appear in the
    actual README.md. The actual file gets the prose + the inner `elixir` code
    block (with its own `\`\`\`elixir` and `\`\`\`` fences) plus blank lines as
    shown above.

    To insert cleanly, use the Edit tool to replace this CURRENT TEXT (the 2 lines
    around the insertion point):
    ```
    - [Security policy](SECURITY.md)

    ## Operations: Scheduled metadata refresh
    ```

    With this TARGET TEXT (the original 2 lines + the new subsection between them):
    ```
    - [Security policy](SECURITY.md)

    ## Operations: Bulk actions

    Phase 20 ships a multi-select bulk-actions surface in the LiveView admin so operators can run lifecycle and metadata actions across many connections in one operator action. Each bulk run produces one shared audit `correlation_id` so the resulting per-connection ledger rows can be reconstructed as a single batch — Phase 21.1 closed the audit forwarding gap so this guarantee holds for every supported action.

    From the connections list, select one or more connections via the row checkboxes. The Bulk Actions menu appears once at least one row is selected and exposes three actions:

    - **Enable** — Move every selected connection to the `enabled` lifecycle state. Each connection's trust change co-commits its own audit row inside its own transaction.
    - **Disable** — Move every selected connection to the `disabled` lifecycle state. Existing application sessions are not revoked.
    - **Refresh Metadata** — Re-fetch metadata for every selected connection from its registered remote source. The bulk path uses the same operator-triggered manual refresh seam as a single-connection refresh; new signing certificates stage as `:next` rather than promoting implicitly.

    Bulk actions execute sequentially (one connection at a time) to keep database pressure bounded and audit ordering explicit. The aggregate flash message reports per-connection success and failure counts; partial failures do not abort the batch.

    Audit trail: every per-connection row in a bulk run shares the bulk `correlation_id` and the operator-attributed `actor` / `cause`, so an operator reviewing the audit ledger can reconstruct "who ran which bulk action across which connections" from a single shared identifier.

    The seam is `Relyra.Ecto.BulkActions.run/4`. Hosts that want to drive bulk actions from a Mix task, a script, or a custom admin UI can call it directly with a list of `connection_id` values and one of the three action functions:

    ```elixir
    Relyra.Ecto.BulkActions.run(
      MyApp.Repo,
      ["01JT...", "01JT...", "01JT..."],
      &Relyra.Metadata.refresh/2,
      audit: %{actor: "ops@example.com", cause: "scripted_bulk_refresh"}
    )
    ```

    Returns `{:ok, %{connection_id => {:ok, _} | {:error, %Relyra.Error{}}}}`. The `correlation_id` is auto-generated if not supplied in the audit map.

    ## Operations: Scheduled metadata refresh
    ```

    ────────────────────────────────────────────────────────────────────────────
    Critical guardrails (per RESEARCH § Common Pitfalls 2 + 4):
    ────────────────────────────────────────────────────────────────────────────

    1. **PITFALL 2 (Brand-voice drift):** the new subsection must contain ZERO
       matches (case-insensitive) for: `polling`, `cron job`, `blocked`, `retry`,
       `circuit breaker`, `MaxBackoff`, `magic`, `bulletproof`, `military-grade`.
       The recommended draft above is verified clean by RESEARCH § 4. If you
       deviate from the verbatim shape, re-run the brand-voice grep before
       committing.
    2. **DO NOT introduce additional code samples beyond the single
       `BulkActions.run/4` example.** D-21.2-11 + RESEARCH § 4 "What the planner
       should NOT include" is explicit.
    3. **DO NOT reference v0.6 features** (Debug bundles, Expiry alerts) in the
       new subsection — those are not yet shipped; they belong in v0.6 phase
       READMEs.
    4. **DO NOT modify the existing `## Operations: Scheduled metadata refresh`
       section** (lines 51-148 today; pushed down post-edit). It stays
       byte-identical.
    5. **DO NOT modify any other README section** (Guides list, Security section,
       installation snippets, etc.).
    6. Use placeholder values in the example: `MyApp.Repo`, `"01JT..."`,
       `"ops@example.com"`, `"scripted_bulk_refresh"`. NEVER real connection IDs,
       NEVER internal hostnames (Threat Register I row).
    7. Always "Relyra" title case (PROJECT.md:134 brand discipline). Verified in
       the recommended draft (every occurrence is `Relyra`).
    8. **PITFALL 4 sanity check:** before the first commit of this plan, verify
       no pre-commit hook is registered (`git config --get core.hooksPath` returns
       empty AND `ls .git/hooks/pre-commit*` shows no executable). If a hook is
       registered, halt and surface to operator (RESEARCH § Pitfall 4).
    9. DO NOT touch `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`,
       `.planning/MILESTONE-ARC.md`, `lib/`, `test/`, or `.planning/milestones/`
       (sibling plans + D-21.2-03).
  </action>

  <verify>
    <automated>
# Cross-doc invariant — README has Bulk actions section
test "$(grep -cE "^## Operations: Bulk actions" README.md)" = "1" && \
# Existing Scheduled metadata refresh section preserved
test "$(grep -cE "^## Operations: Scheduled metadata refresh" README.md)" = "1" && \
# New section appears BEFORE the existing scheduled-refresh section
test "$(grep -nE "^## Operations: Bulk actions" README.md | cut -d: -f1)" -lt "$(grep -nE "^## Operations: Scheduled metadata refresh" README.md | cut -d: -f1)" && \
# D-21.2-12 — audit-cohesion + Phase 21.1 cited
awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -F "Phase 21.1 closed the audit forwarding gap" && \
awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -F "shared audit \`correlation_id\`" && \
# D-21.2-11 — 3 bulk actions narrated (Enable / Disable / Refresh Metadata)
awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -F "**Enable**" && \
awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -F "**Disable**" && \
awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -F "**Refresh Metadata**" && \
# D-21.2-11 — exactly 1 Elixir code-fence example in the new section
test "$(awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -cE '^```elixir')" = "1" && \
# Example uses BulkActions.run/4
awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -F "Relyra.Ecto.BulkActions.run(" && \
# Threat I/D — example uses placeholders, not real values
awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -F "MyApp.Repo" && \
awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -F "01JT..." && \
awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -F "ops@example.com" && \
# Cross-doc invariant — README does NOT use brand-banned terms in the new section
test "$(awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -ciE 'polling|cron job|blocked|retry|circuit breaker|MaxBackoff|magic|bulletproof|military-grade')" = "0" && \
# Always Relyra title case
test "$(awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -cE 'reLyra|ReLyra|RELYRA')" = "0" && \
# RESEARCH § 4 "What the planner should NOT include" — no v0.6 feature references
test "$(awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -cF 'Debug bundles')" = "0" && \
test "$(awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -cF 'Expiry alerts')" = "0" && \
# File-touch invariant: only README.md modified by this plan
test "$(git diff --name-only HEAD~1 HEAD -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/MILESTONE-ARC.md README.md lib/ test/ .planning/milestones/ | tr '\n' ' ')" = "README.md " && \
# Negative-existence (Pitfall 5 cross-phase invariant): no v0.5 archive
test ! -f .planning/milestones/v0.5-REQUIREMENTS.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE "^## Operations: Bulk actions" README.md` returns `1` (cross-doc invariant "README has Bulk actions section").
    - `grep -cE "^## Operations: Scheduled metadata refresh" README.md` returns `1` (existing section preserved unchanged).
    - The new section appears BEFORE the existing scheduled-refresh section: `grep -nE "^## Operations: Bulk actions" README.md | cut -d: -f1` < `grep -nE "^## Operations: Scheduled metadata refresh" README.md | cut -d: -f1` (placement check per CONTEXT `<specifics>` line 151).
    - D-21.2-12 audit-cohesion blurb present: `awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -F "Phase 21.1 closed the audit forwarding gap"` returns 1 match.
    - D-21.2-12 shared-`correlation_id` blurb present: `awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -F "shared audit \`correlation_id\`"` returns 1 match.
    - D-21.2-11 — 3 bulk actions narrated (each on its own bullet): the awk slice contains `**Enable**`, `**Disable**`, AND `**Refresh Metadata**`.
    - D-21.2-11 — exactly 1 Elixir code-fence in the new section: `awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -cE '^\`\`\`elixir'` returns `1`.
    - The example calls `Relyra.Ecto.BulkActions.run(`: 1 match in the awk slice.
    - **Threat I/D — placeholder values:** awk slice contains `MyApp.Repo`, `01JT...`, AND `ops@example.com` (no real connection IDs, no internal hostnames).
    - **Brand-voice grep (cross-doc invariant "README does NOT use brand-banned terms in the new section"):** `awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -ciE 'polling|cron job|blocked|retry|circuit breaker|MaxBackoff|magic|bulletproof|military-grade'` returns `0`.
    - Brand title-case: `awk '/^## Operations: Bulk actions/,/^## Operations: Scheduled metadata refresh/' README.md | grep -cE 'reLyra|ReLyra|RELYRA'` returns `0`.
    - **No v0.6 feature references:** `grep -cF 'Debug bundles' awk-slice` returns `0` AND `grep -cF 'Expiry alerts' awk-slice` returns `0` (RESEARCH § 4 "What the planner should NOT include").
    - File-touch invariant (D-21.2-03): `git diff --name-only HEAD~1 HEAD -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/MILESTONE-ARC.md README.md lib/ test/ .planning/milestones/` shows exactly `README.md` (no other paths).
    - Negative-existence (Pitfall 5 cross-phase invariant): `test ! -f .planning/milestones/v0.5-REQUIREMENTS.md` succeeds.
    - `wc -l README.md` returns ≥ 160 (was ~152 pre-edit; post-edit expect ~175-180 with new section).
  </acceptance_criteria>

  <done>
    - README.md gains a new `## Operations: Bulk actions` subsection placed immediately before `## Operations: Scheduled metadata refresh`.
    - All acceptance_criteria greps green.
    - The existing `## Operations: Scheduled metadata refresh` section is byte-identical to its pre-edit state.
    - Brand-voice grep clean (zero banned-term matches in the new section).
    - File-touch invariant green (only README.md modified by this plan).
    - Commit landed: `docs(21.2-04): add README Operations: Bulk actions subsection (closes v0.5 audit INT-02)`
    - SUMMARY.md confirms placement, brand-voice cleanliness, and threat-mitigation values.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `README.md` ↔ public Git history | README.md is the adopter-facing public entry point; its content is shipped alongside every Hex release. The relevant boundaries are: (a) brand-voice contract (PROJECT.md:134 + brand book §22) — banned terms must not leak into operator-facing copy; (b) operational-secrets surface — example values must be placeholders, not real connection IDs / internal hostnames. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.2-04-01 | Spoofing | n/a (no auth surface) | n/a | Doc-only artifact change; no executable identity surface. |
| T-21.2-04-02 | Tampering | `README.md` historical state | mitigate | Insertion is APPEND-shape (new subsection inserted; existing scheduled-refresh section preserved byte-identical). Acceptance grep verifies the existing section's heading is still present at exactly 1 occurrence. Git history preserves the pre-21.2 state. |
| T-21.2-04-03 | Repudiation | n/a | n/a | No identity claims, no audit-ledger interaction. |
| T-21.2-04-04 | **Information Disclosure (PRIMARY THREAT for this plan)** | `BulkActions.run/4` Elixir example | **mitigate** | Per CONTEXT additional_context security gate: example values are PLACEHOLDERS — `MyApp.Repo` (not the host's real repo module), `"01JT..."` (truncated ULID-like string, not a real connection ID), `"ops@example.com"` (RFC-2606 placeholder domain — `example.com` is reserved by IANA for documentation use), `"scripted_bulk_refresh"` (descriptive cause string, not internal codename). Acceptance grep verifies each placeholder is present; deviation from the verbatim recommended draft requires re-verification. |
| T-21.2-04-05 | **Information Disclosure** | New subsection prose | **mitigate** | Subsection does NOT reference v0.6 features (Debug bundles, Expiry alerts) per RESEARCH § 4 "What the planner should NOT include" — those are unshipped; surfacing them in adopter docs would mis-set expectations. Acceptance grep enforces zero matches for `Debug bundles` and `Expiry alerts` in the awk slice. |
| T-21.2-04-06 | Denial of Service | n/a | n/a | Doc edit; no runtime impact. |
| T-21.2-04-07 | Elevation of Privilege | n/a (no privilege boundary) | n/a | Doc edit; no executable code path. |

</threat_model>

<verification>
Plan-level success criteria (all must be true before plan-checker / phase verify):

1. New `## Operations: Bulk actions` heading exists at exactly 1 occurrence.
2. Existing `## Operations: Scheduled metadata refresh` heading exists at exactly 1
   occurrence; the existing section content is byte-identical to its pre-edit state.
3. New section is positioned BEFORE the existing scheduled-refresh section.
4. New section narrates the 3 bulk actions (Enable / Disable / Refresh Metadata) +
   one BulkActions.run/4 Elixir code-fence + audit-cohesion blurb citing Phase 21.1.
5. **Brand-voice grep clean (BLOCKING):** zero matches for `polling`, `cron job`,
   `blocked`, `retry`, `circuit breaker`, `MaxBackoff`, `magic`, `bulletproof`,
   `military-grade` in the new-section awk slice.
6. **Threat I/D placeholders (BLOCKING):** example uses `MyApp.Repo`, `"01JT..."`,
   `"ops@example.com"` — no real connection IDs, no internal hostnames.
7. No v0.6 feature references (Debug bundles, Expiry alerts) in the new section.
8. File-touch invariant: only `README.md` modified by this plan.
9. Cross-doc coherence rows owned by this plan (per 21.2-RESEARCH.md § "Cross-Doc
   Coherence Invariants") are independently green:
   - "README has Bulk actions section" → 1 match.
   - "README does NOT use brand-banned terms in the new section" → 0 matches.
10. Threat register dispositions: T-21.2-04-04 + T-21.2-04-05 are `mitigate`
    (the I/D primary threats); all others `n/a`.
</verification>

<success_criteria>

The plan is complete when:

- [ ] README.md has a new `## Operations: Bulk actions` subsection placed BEFORE the existing `## Operations: Scheduled metadata refresh` heading.
- [ ] The new subsection contains: a narrative paragraph + 3-bullet recipe (Enable / Disable / Refresh Metadata) + an audit-cohesion blurb citing Phase 21.1 + a single Elixir code-fence example using `Relyra.Ecto.BulkActions.run/4`.
- [ ] The Elixir example uses placeholder values (`MyApp.Repo`, `"01JT..."`, `"ops@example.com"`, `"scripted_bulk_refresh"`).
- [ ] Brand-voice grep returns 0 matches for banned terms (`polling`, `cron job`, `blocked`, `retry`, `circuit breaker`, `MaxBackoff`, `magic`, `bulletproof`, `military-grade`).
- [ ] No v0.6 feature references (Debug bundles, Expiry alerts) in the new subsection.
- [ ] The existing `## Operations: Scheduled metadata refresh` section is byte-identical to its pre-edit state.
- [ ] No file under `lib/`, `test/`, `.planning/milestones/`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, or `.planning/MILESTONE-ARC.md` was modified by this plan.
- [ ] `.planning/milestones/v0.5-REQUIREMENTS.md` was NOT created.
- [ ] Commit landed with message `docs(21.2-04): add README Operations: Bulk actions subsection (closes v0.5 audit INT-02)`.

</success_criteria>

<output>
After completion, create
`.planning/phases/21.2-close-v0-5-milestone-audit-gaps-and-re-scope-deferred-featur/21.2-04-SUMMARY.md`
documenting:

- The exact insertion point in README.md (line number where the new heading lands).
- The line-count delta (pre vs. post).
- Confirmation of all acceptance_criteria greps (paste output, especially the brand-voice grep showing 0 banned-term matches).
- **Explicit Threat I/D confirmation:** placeholder values used in the example (paste the awk-slice grep results).
- Confirmation that the existing `## Operations: Scheduled metadata refresh` section is byte-identical (run `git diff` on a sed-extracted slice or describe).
- Confirmation that no v0.6 features (Debug bundles, Expiry alerts) are referenced in the new subsection.
- Confirmation of file-touch invariant (only README.md modified).
- Cross-doc coherence rows owned by this plan reported green.
- Threat register dispositions (T-21.2-04-04 + T-21.2-04-05 mitigate; others `n/a`).
- Reference to the v0.5 milestone audit (`.planning/v0.5-MILESTONE-AUDIT.md` INT-02) as the gap-closure motivation.
</output>
</content>
</invoke>