---
phase: 43-hex-publish-prep-version-bump-changelog-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .release-please-manifest.json
  - guides/getting_started.md
  - CHANGELOG.md
autonomous: true
requirements:
  - PUB-01
  - PUB-02
must_haves:
  truths:
    - "`mix.exs` `@version`, `.release-please-manifest.json`, and `guides/getting_started.md` install pin all reflect `1.4.0` / `~> 1.4` before Phase 44 runs release-please."
    - "`CHANGELOG.md` contains hand-written `[1.4.0]` and `[1.3.0]` sections (newest-first, above `[1.2.0]`) with Keep-a-Changelog `Added` / `Changed` / `Security` headings."
    - "The `[1.4.0]` section opens with the single-jump `1.2.0` → `1.4.0` rationale; `[1.3.0]` is labelled as historical milestone archaeology with no Hex release."
    - "`## [Unreleased]` remains present (empty OK); no `v1.4.0` git tag and no `mix hex.publish` in this phase."
    - "`mix test --warnings-as-errors` and `mix ci.release` stay green after the bump."
  artifacts:
    - path: "mix.exs"
      provides: "@version \"1.4.0\" driving project version and ExDoc source_ref."
    - path: ".release-please-manifest.json"
      provides: "Release-please baseline \"1.4.0\" for Phase 44 automation."
    - path: "guides/getting_started.md"
      provides: "Canonical Day-1 install pin `{:relyra, \"~> 1.4\"}`."
    - path: "CHANGELOG.md"
      provides: "Backfilled [1.4.0] and [1.3.0] milestone narratives for adopters and release-please."
---

<objective>
Stage all repo-side release-prep artifacts for a release-please-driven `1.4.0` publish: bump version sources, fix the stale install pin, and hand-write `[1.3.0]` / `[1.4.0]` CHANGELOG sections from milestone evidence — in one atomic commit. Defer git tag creation and Hex publish to Phase 44.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/43-hex-publish-prep-version-bump-changelog-backfill/43-CONTEXT.md
@.planning/phases/43-hex-publish-prep-version-bump-changelog-backfill/43-RESEARCH.md
@.planning/milestones/v1.3-ROADMAP.md
@.planning/milestones/v1.4-ROADMAP.md
@mix.exs
@.release-please-manifest.json
@guides/getting_started.md
@CHANGELOG.md
@CLAUDE.md
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| TM-01 | Manual `mix hex.publish` bypasses CI gates and OSS discipline | **high** | Phase 43 explicitly forbids publish; verification step greps for absence of publish commands in plan execution; Phase 44 owns automation only (CLAUDE.md) |
| TM-02 | Creating `v1.4.0` git tag in Phase 43 fires publish workflow on wrong commit state | **high** | D-02: no `git tag` in any task; acceptance criteria include `git tag -l v1.4.0` empty |
| TM-03 | `mix.exs` / manifest version mismatch breaks publish-hex grep check | **high** | Task 1 sets both to `"1.4.0"` in same commit; paired grep acceptance criteria |
| TM-04 | Removing or renaming `## [Unreleased]` breaks `mix ci.release` | medium | Task 3 preserves existing `## [Unreleased]` section verbatim; `release_hardening_test.exs` gate |
| TM-05 | Stale `~> 0.1.0` install pin misleads adopters installing from guides | medium | Task 2 sets `~> 1.4`; repo-wide grep confirms no other Relyra `~> 0.1.0` pins |
| TM-06 | CHANGELOG omits material security invariants (ENC read-before-verify, trace redaction) | medium | Task 3/4 Security subsections include CVE-class guards and `ci.security` corpus callouts per RESEARCH outline |
| TM-07 | Treating `[1.3.0]` as a skipped Hex release without rationale confuses adopters | medium | Task 3 requires jump-rationale paragraph at top of `[1.4.0]` (D-07) |

**Block rule:** TM-01, TM-02, TM-03 are **high** — any task that touches version sources must not run `mix hex.publish` or `git tag v1.4.0`; executor must halt if either command is requested.
</threat_model>

<tasks>

<task type="auto">
  <name>Bump version sources to 1.4.0</name>
  <read_first>
    - mix.exs (line 6 @version, docs/0 source_ref)
    - .release-please-manifest.json
    - .planning/phases/43-hex-publish-prep-version-bump-changelog-backfill/43-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
    1. In `mix.exs` line 6, change `@version "1.2.0"` to `@version "1.4.0"`. Do **not** edit `source_ref:` in `docs/0` — it derives via `"v#{@version}"`.

    2. In `.release-please-manifest.json`, change `"."` value from `"1.2.0"` to `"1.4.0"`.

    3. Do **not** run `git tag v1.4.0` or `mix hex.publish` (Phase 44 only).

    4. Confirm no other edits to `mix.exs` in this task.
  </action>
  <verify>grep -n '@version "1.4.0"' mix.exs && grep '"1.4.0"' .release-please-manifest.json && ! rg '@version "1\.2\.0"' mix.exs .release-please-manifest.json</verify>
  <acceptance_criteria>
    - `grep -n '@version "1.4.0"' mix.exs` exits 0 (line 6).
    - `grep '"1.4.0"' .release-please-manifest.json` exits 0.
    - `rg '@version "1\.2\.0"' mix.exs .release-please-manifest.json` returns no matches.
    - `git tag -l v1.4.0` returns empty (tag deferred to Phase 44).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Fix getting_started install pin</name>
  <read_first>
    - guides/getting_started.md (line 26)
    - .planning/phases/43-hex-publish-prep-version-bump-changelog-backfill/43-CONTEXT.md (D-03)
  </read_first>
  <action>
    In `guides/getting_started.md` line 26, replace `{:relyra, "~> 0.1.0"}` with `{:relyra, "~> 1.4"}` inside the deps example block. Do not edit README.md (Phase 46 DX-01).
  </action>
  <verify>grep -n '~> 1.4' guides/getting_started.md && ! rg '~> 0\.1\.0' guides/</verify>
  <acceptance_criteria>
    - `grep '~> 1.4' guides/getting_started.md` exits 0.
    - `rg '~> 0\.1\.0' guides/` returns no matches.
    - Only `guides/getting_started.md` changed for the install pin in this plan.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Backfill CHANGELOG [1.4.0] section</name>
  <read_first>
    - CHANGELOG.md (insert above `## [1.2.0]` at line 8; preserve `## [Unreleased]` at line 235)
    - .planning/phases/43-hex-publish-prep-version-bump-changelog-backfill/43-RESEARCH.md (section 3 `[1.4.0]` outline)
    - .planning/milestones/v1.4-ROADMAP.md
    - .planning/phases/41-pre-publish-hygiene-tech-debt-sweep-security-hardening/41-*-SUMMARY.md
    - .planning/phases/42-stepwise-login-trace-liveview/42-*-SUMMARY.md
  </read_first>
  <action>
    Insert a new `## [1.4.0]` section **immediately above** the existing `## [1.2.0]` line (newest-first). Use Keep-a-Changelog headings `### Added`, `### Changed`, `### Security` — **not** release-please commit links (D-04).

    **Opening paragraph (required first, before `### Added`):** State that Hex publishes **1.4.0** directly from **1.2.0** with no intermediate **1.3.0** Hex release for adopter clarity; one install line `{:relyra, "~> 1.4"}` receives Advanced Federation + SLO + trace UI; the `[1.3.0]` section below is changelog archaeology for the v1.3 milestone only (D-07).

    **`### Added` bullets — v1.4 milestone (Phases 38–40.1):**
    - **Single Logout (SLO-01):** `SessionAdapter.index_session/4` + `terminate_by_session_index/4`; SP- and IdP-initiated logout via `Relyra.consume_logout/3`; HTTP-Redirect + HTTP-POST bindings; strict logout validation pipeline (`Parse → Verify → Replay → Execute`); `LogoutRequest`/`LogoutResponse` on `SaxyTree` (single parse path).
    - **Documentation (DOCS-04):** `guides/recipes/logout.md` — ITP/ETP/Privacy Sandbox caveats, durable session prerequisite, absolute-timeout boundary, host-owned session-index linkage.
    - **Documentation (DOCS-05):** `guides/operations/incident_playbook.md` — six Triage→Diagnose→Recover scenarios.
    - **Documentation (DOCS-06):** `guides/troubleshooting.md` Error Atom Decoder (78 atoms, 7 buckets); bidirectional drift test in `ci.docs`.
    - **Audit closure (40.1):** `logout_recipe_drift_test.exs` AST arity gate; retroactive `38-VERIFICATION.md` / `39-VERIFICATION.md`.

    **`### Added` bullets — v1.5 pre-publish shipped inside 1.4.0 tarball (Phases 41–42):**
    - **Login trace UI (TRACE-01):** `ConnectionTraceLive` at `/connections/:connection_id/trace`; six-step expandable rows from audit + telemetry.
    - **Headless trace (TRACE-03):** `mix relyra.trace --repo --connection [--last N]`.
    - **Shared export:** `Relyra.LoginTrace.Export` redaction path shared by LiveView and CLI.
    - **Pre-publish hygiene (TD-01..05):** metadata attribute escaping + security corpus; `test_support` excluded from prod compile and Hex `package.files`; parse-tree byte spans for encrypted assertion wire extraction (regex retired); README/doc preset honesty; adversarial crypto test formatting.

    **`### Changed` bullets:**
    - Trust audit timeline excludes `domain: :login` rows (login traces separate from trust mutations).
    - `LoginResult.validation_trace` populated on successful consume via `LoginTrace` telemetry handler.
    - Production `elixirc_paths` uses explicit lib file list (excludes `test_support`).

    **`### Security` bullets:**
    - **Logout crypto:** XMLDSig verification before session termination; redirect signatures verified against raw query octets; replay protection on logout messages.
    - **Login trace redaction (TRACE-02):** `test/security/login_trace_test.exs` — LiveView and CLI never render raw XML, PEM, cert bodies, signature values, or key material; dedicated `cmd mix test` in `ci.security`.
    - **Metadata XSS defense-in-depth (TD-01):** interpolated SP metadata attributes XML-escaped (`metadata_attribute_injection_test.exs`).
    - **One trust path (TD-03):** `locate_encrypted_assertion` uses parse-tree byte spans only — no regex alongside tree.

    Do **not** edit the existing `[1.2.0]` release-please section. Do **not** move or remove `## [Unreleased]`.
  </action>
  <verify>grep -n '## \[1.4.0\]' CHANGELOG.md && grep -A2 '## \[1.4.0\]' CHANGELOG.md | head -5</verify>
  <acceptance_criteria>
    - `grep '## \[1.4.0\]' CHANGELOG.md` exits 0.
    - `[1.4.0]` section appears **above** `## [1.2.0]` (`grep -n '## \[1.4.0\]\|## \[1.2.0\]' CHANGELOG.md` shows 1.4.0 line number less than 1.2.0).
    - Section contains `### Added`, `### Changed`, and `### Security` headings.
    - Opening paragraph mentions no intermediate **1.3.0** Hex release and `~> 1.4`.
    - Bullets reference SLO-01, DOCS-04, DOCS-05, DOCS-06, login trace, and TD-01..05 keywords (`rg 'SLO-01|DOCS-0[456]|login.?trace|TRACE-0[123]|TD-0[1-5]' CHANGELOG.md` matches within `[1.4.0]` block).
    - `grep '## \[Unreleased\]' CHANGELOG.md` still exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Backfill CHANGELOG [1.3.0] section</name>
  <read_first>
    - CHANGELOG.md (insert directly below `[1.4.0]`, above `[1.2.0]`)
    - .planning/phases/43-hex-publish-prep-version-bump-changelog-backfill/43-RESEARCH.md (section 3 `[1.3.0]` outline)
    - .planning/milestones/v1.3-ROADMAP.md
  </read_first>
  <action>
    Insert a new `## [1.3.0]` section **between** `[1.4.0]` and `[1.2.0]`. Add a one-line note after the heading: this section records the v1.3 Advanced Federation milestone only — **no Hex release at 1.3.0** (historical record).

    Use Keep-a-Changelog headings `### Added`, `### Changed`, `### Security`.

    **`### Added` bullets:**
    - **Encrypted assertions (ENC-01/02):** `KeyResolver` behaviour + `KeyResolver.Default` (SP private key from app config only); `Relyra.Security.XMLEnc.decrypt/3` with RSA-OAEP + AES-GCM behind `AlgorithmPolicy`; decrypt-then-reparse pipeline stage in `ValidationPipeline` (`:decrypt_assertion` pre-stage); cleartext+encrypted ambiguity guard (`:ambiguous_assertion` before crypto); SP metadata encryption `KeyDescriptor`; 7-fixture ENC-01 adversarial corpus in `mix ci.security`.
    - **Signed AuthnRequests (AUTHN-01):** HTTP-Redirect query signing (`sign_redirect_query/3` raw-octet invariant); `sign_authn_requests` connection toggle; SP metadata `AuthnRequestsSigned` + signing `KeyDescriptor`; ADFS provider preset + `guides/recipes/adfs.md`; 5-fixture AUTHN-01 adversarial corpus in `mix ci.security`.
    - **AlgorithmPolicy + schema:** Key-transport and content-encryption enforcement; RSA-PKCS1v1.5 blocked; AES-CBC blocked by default with time-boxed escape hatch; GCM auth-tag length guard; cert `party`/`use` columns; `sign_authn_requests` migration.
    - **Documentation (DOCS-02):** `guides/recipes/generic_saml.md` — SP/IdP metadata reference, decoder tables for IBM Security Verify, CyberArk, Oracle Access Manager, PingFederate, CA SiteMinder; security checklist, debugging flow, cert rotation.
    - **Documentation (DOCS-03):** `guides/identity_mapping_and_provisioning.md` — NameID vs attribute mapping patterns, JIT decision tree, `UserMapper` examples, SCIM non-goal.

    **`### Changed` bullets:**
    - `PureBeam.build_parsed_doc/1` tolerates encrypted-only Responses pre-decrypt (`encrypted_pending` path) without weakening cleartext gates.
    - SP metadata build order: signing + encryption `KeyDescriptor`s before ACS (schema-valid).

    **`### Security` bullets:**
    - **Decrypt-then-reparse invariant:** decrypted bytes MUST pass `PureBeam.parse_safely/2` AND `Signature.do_verify/4` before identity fields — CVE-2025-54419 class read-before-verify rejected by adversarial corpus.
    - **Single opaque `:decryption_failed`** for all decryption failure modes (no padding oracle via distinct atoms).
    - **Document `KeyInfo` ignored** for decryption key material — configured `KeyResolver` only.
    - **Ambiguity guard:** cleartext + encrypted assertion → `:ambiguous_assertion` pre-crypto (CVE-2026-2092 class).
    - **Redirect AuthnRequest signing:** golden corpus enforces no re-serialization before sign; ADFS `+`-encoding variant covered.
    - **AlgorithmPolicy:** RSA-OAEP SHA-256 URI blocked pending OTP support; zero new Hex deps for XML-Enc (OTP stdlib only).

    Do **not** edit existing `[1.2.0]` or lower sections.
  </action>
  <verify>grep -n '## \[1.3.0\]' CHANGELOG.md && rg 'ENC-01|AUTHN-01|DOCS-02|DOCS-03' CHANGELOG.md</verify>
  <acceptance_criteria>
    - `grep '## \[1.3.0\]' CHANGELOG.md` exits 0.
    - `[1.3.0]` line number is greater than `[1.4.0]` and less than `[1.2.0]` line numbers.
    - Section contains `### Added`, `### Changed`, and `### Security` headings.
    - `rg 'ENC-01|AUTHN-01|DOCS-02|DOCS-03' CHANGELOG.md` matches within the `[1.3.0]` narrative.
    - Section notes no Hex release at 1.3.0.
    - `grep '## \[Unreleased\]' CHANGELOG.md` still exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify gates and commit release-prep batch</name>
  <read_first>
    - test/release/release_hardening_test.exs
    - .planning/phases/43-hex-publish-prep-version-bump-changelog-backfill/43-CONTEXT.md (D-09, D-10)
    - CLAUDE.md (no manual hex.publish)
  </read_first>
  <action>
    1. Run `mix test test/release/release_hardening_test.exs --warnings-as-errors`.

    2. Run `mix ci.release`.

    3. Run `mix test --warnings-as-errors` (full suite — D-10).

    4. Run version coherence checklist:
       ```bash
       grep -n '@version "1.4.0"' mix.exs
       grep '"1.4.0"' .release-please-manifest.json
       grep '~> 1.4' guides/getting_started.md
       grep '## \[1.3.0\]' CHANGELOG.md
       grep '## \[1.4.0\]' CHANGELOG.md
       grep '## \[Unreleased\]' CHANGELOG.md
       rg '@version "1\.2\.0"' mix.exs .release-please-manifest.json  # expect no matches
       ```

    5. Confirm `rg '1\.2\.0' test/` returns no matches (no literal version tests to update).

    6. Stage exactly these four files and commit in one atomic batch (D-09):
       - `mix.exs`
       - `.release-please-manifest.json`
       - `guides/getting_started.md`
       - `CHANGELOG.md`

       Commit message:
       ```
       chore(release): prep v1.4.0 version bump and CHANGELOG backfill

       Stage mix.exs, release-please manifest, getting_started pin, and
       hand-written [1.3.0]/[1.4.0] CHANGELOG sections for Phase 44
       release-please automation. No git tag or Hex publish in this commit.

       Co-Authored-By: Cursor <noreply@cursor.example>
       ```

    7. Do **not** run `mix hex.publish` or `git tag v1.4.0`.
  </action>
  <verify>mix test --warnings-as-errors && mix ci.release</verify>
  <acceptance_criteria>
    - `mix test test/release/release_hardening_test.exs --warnings-as-errors` exits 0.
    - `mix ci.release` exits 0.
    - `mix test --warnings-as-errors` exits 0.
    - Single commit contains all four modified files (`git show --stat HEAD` lists only those paths).
    - Commit message body mentions no tag/publish in this commit.
    - `git tag -l v1.4.0` still empty after commit.
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Version coherence
grep -n '@version "1.4.0"' mix.exs
grep '"1.4.0"' .release-please-manifest.json
grep '~> 1.4' guides/getting_started.md
! rg '@version "1\.2\.0"' mix.exs .release-please-manifest.json

# CHANGELOG structure
grep '## \[1.4.0\]' CHANGELOG.md
grep '## \[1.3.0\]' CHANGELOG.md
grep '## \[Unreleased\]' CHANGELOG.md
grep -n '## \[1.4.0\]\|## \[1.3.0\]\|## \[1.2.0\]' CHANGELOG.md  # order: 1.4.0 < 1.3.0 < 1.2.0

# Milestone keyword spot-check (PUB-02 prose)
rg 'ENC-01|AUTHN-01|DOCS-02|DOCS-03' CHANGELOG.md
rg 'SLO-01|DOCS-0[456]|TRACE-0[123]|login.?trace' CHANGELOG.md

# Automated gates
mix test test/release/release_hardening_test.exs --warnings-as-errors
mix ci.release
mix test --warnings-as-errors

# Explicit deferrals (must NOT exist yet)
git tag -l v1.4.0   # empty
```

**Explicitly do NOT run:** `mix hex.publish`, `git tag v1.4.0`
</verification>

<success_criteria>
All four release-prep files land in one commit at version `1.4.0` with fully backfilled CHANGELOG sections, green test/release gates, and Phase 44-ready state — without creating a git tag or publishing to Hex. PUB-01 satisfied except the `v1.4.0` tag (Phase 44); PUB-02 satisfied in full.
</success_criteria>

## PLANNING COMPLETE
