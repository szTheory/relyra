---
phase: 72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps
plan: "04"
subsystem: demo-e2e
tags: [keycloak, playwright, basic-auth, docker, security-gates]
requires:
  - phase: 72.1-02
    provides: "Runtime-only admin credential forwarding"
provides:
  - "Denied-then-authenticated Keycloak Login Trace browser proof"
  - "Per-run credential generation, partial-input rejection, and retained-diagnostic redaction coverage"
affects: [keycloak-proxy-harness, evaluator-trace-evidence]
tech-stack:
  added: []
  patterns: ["Runtime Basic Auth trace entry", "Ephemeral credential-pair preflight", "Secret-free retained diagnostics"]
key-files:
  created: []
  modified:
    - demo/ledger_loop/test/browser/keycloak.spec.ts
    - scripts/test_keycloak_proxy_e2e.sh
key-decisions:
  - "Keycloak trace evidence navigates to the exact protected trace without credentials before entering only through /login/admin."
  - "The owned harness preserves complete operator pairs, generates absent pairs per run, and rejects partial input."
metrics:
  duration: 19min
  completed: 2026-08-27
status: complete
---

# Phase 72.1 Plan 04: Keycloak Credentialed Trace Proof Summary

**The optional Keycloak proof now demonstrates a denied trace boundary followed by runtime-credentialed `/login/admin` access to the persisted verifier trace.**

## Accomplishments

- Added an unauthenticated exact-trace navigation assertion that receives `401` and cannot render login-trace rows before the browser supplies Basic Auth.
- Retained the signed ACS, durable receipt, and canonical validation/signature/replay trace proof after `/login/admin` redirects into the mounted admin scope.
- Hardened the owned Keycloak harness: complete caller credentials are preserved, absent credentials become fresh per-run pairs, partial pairs fail preflight, and both credential fields are redacted from diagnostics.
- Added deterministic harness self-tests for credential forwarding into Compose and Playwright, redaction, absent-pair generation, and partial-pair rejection.

## Task Commits

1. **Task 1 RED: Pin the Keycloak credential policy contract** — `46bf50f` (`test`)
2. **Task 1 GREEN: Prove credentialed Keycloak trace access** — `0ec49a8` (`feat`)

## Verification

- `scripts/test_keycloak_proxy_e2e.sh` — passed: Playwright reported `1 passed`; harness confirmed signed ACS, durable receipt, and canonical trace, then cleaned up its owned Compose project.
- Harness static, diagnostic, artifact-policy, and credential-policy self-tests — passed.
- `mix ci.demo_app` — passed (90 tests, 0 failures).
- `mix ci.docs` and `mix qa` — passed; `mix qa` reported 794 tests, 0 failures (10 excluded).
- `mix ci.security` — passed (exit 0; final security lane 5 tests, 0 failures; Sobelow scan complete).
- `mix format --check-formatted` and `git diff --exit-code -- lib mix.exs test/security/xml/adversarial_crypto_test.exs` — passed.

## Decisions Made

- The browser test authenticates only through `/login/admin`; it does not introduce a seeded session, query flag, or test-only trace bypass.
- Generated admin usernames and passwords are unique to a harness run and remain process-local; neither is logged or retained.

## TDD Gate Compliance

- RED: `46bf50f` records the credential-policy test-first contract, which initially failed with `credential_policy_self_test: command not found` before implementation.
- GREEN: `0ec49a8` adds the browser proof; all owned and phase gates passed.
- REFACTOR: Not needed.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED

- Confirmed both modified implementation/test files exist.
- Confirmed task commits `46bf50f` and `0ec49a8` exist with no tracked-file deletions.
