---
phase: 72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex
  - demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex
  - demo/ledger_loop/test/ledger_loop_web/live/setup_live_test.exs
  - demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs
  - demo/ledger_loop/test/ledger_loop_web/controllers/route_affordance_controller_test.exs
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-01: Setup displays metadata, login, and ACS URLs for one enabled connection under `/saml/:connection_id/...`."
    - "D-02: Tests dispatch the displayed metadata/login URLs and complete a signed response through the displayed ACS contract; rendered-text suffix checks are not the sole evidence."
    - "D-03/D-05: `/login/admin` and the exact connection trace remain fail-closed for missing, partial, and invalid credentials, while configured valid credentials establish admin scope and reach the trace."
  artifacts:
    - path: demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex
      provides: "single connection-scoped SAML URL-set derivation used by rendering and test login"
      contains: "/saml/"
    - path: demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex
      provides: "readonly metadata, login, and ACS settings from LiveView assigns"
      contains: "readonly"
    - path: demo/ledger_loop/test/ledger_loop_web/live/setup_live_test.exs
      provides: "render-to-router dispatch evidence"
      contains: "metadata"
    - path: demo/ledger_loop/test/ledger_loop_web/controllers/route_affordance_controller_test.exs
      provides: "trace-path Basic Auth allow/deny matrix"
      contains: "trace"
  key_links:
    - from: demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex
      to: demo/ledger_loop/lib/ledger_loop_web/router.ex
      via: "Phoenix verified `/saml/:connection_id/{metadata,login,acs}` paths"
      pattern: "saml.*connection_id"
    - from: demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex
      to: demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex
      via: "one URL-set assign"
      pattern: "saml_urls"
    - from: demo/ledger_loop/test/ledger_loop_web/controllers/route_affordance_controller_test.exs
      to: demo/ledger_loop/lib/ledger_loop_web/plugs/demo_admin_auth.ex
      via: "credentialed `/login/admin` entry before trace navigation"
      pattern: "login/admin"
---

<objective>
Close the evaluator's route and authenticated-trace gap with one production-quality end-to-end slice (D-01, D-02, D-03, D-05).

Purpose: The setup UI must advertise routes Phoenix actually mounts, and the trace proof must enter through the existing host-owned Basic Auth boundary.
Output: Route-derived Setup settings and deterministic metadata/login/ACS plus trace allow/deny evidence.
</objective>

<api_coverage_decision>
The phase has no mapped requirement IDs, so the SPEC-less requirement probe is visibly skipped; no REQ IDs are invented. The installed detector returned `detected: true` for the false-positive phrase `consumes ... endpoint`; this phase integrates no external API or SDK, so `COVERAGE.md` records the required reasoned declaration instead of a fabricated capability matrix.
</api_coverage_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-CONTEXT.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-RESEARCH.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-PATTERNS.md
@.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-VALIDATION.md
</context>

## Artifacts this phase produces

- New private helper `saml_urls/1` (or an equivalently named private helper) in `LedgerLoopWeb.SetupLive`; it returns metadata, login, and ACS absolute URLs for one enabled `connection_id`.
- New socket assign `:saml_urls`; no public Elixir symbol is created.
- Modified Setup readonly controls for metadata/entity ID, login, and ACS.
- Expanded tests in `setup_live_test.exs`, `fake_idp_flow_test.exs`, and `route_affordance_controller_test.exs` for real dispatch and Basic Auth trace entry.
- No new route, dependency, config key, schema, `lib/` file, public API, behavior callback, or published package artifact.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove displayed scoped endpoints and credentialed trace entry end to end</name>
  <reversibility rating="reversible">The private URL helper, template assigns, and demo tests can be reverted without migration, published-contract break, or security-posture change.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/v1.10-v1.10-MILESTONE-AUDIT.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-CONTEXT.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-RESEARCH.md
    - .planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-PATTERNS.md
    - demo/ledger_loop/lib/ledger_loop_web/router.ex
    - demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex
    - demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex
    - demo/ledger_loop/lib/ledger_loop_web/plugs/demo_admin_auth.ex
    - demo/ledger_loop/test/ledger_loop_web/live/setup_live_test.exs
    - demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs
    - demo/ledger_loop/test/ledger_loop_web/controllers/route_affordance_controller_test.exs
  </read_first>
  <files>demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex, demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex, demo/ledger_loop/test/ledger_loop_web/live/setup_live_test.exs, demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs, demo/ledger_loop/test/ledger_loop_web/controllers/route_affordance_controller_test.exs</files>
  <behavior>
    - "D-01: the seeded enabled connection supplies all three displayed absolute URLs and the Start Test Login redirect."
    - "D-02: a test requests the rendered metadata path and receives metadata, requests the rendered login path and reaches FakeIdP, and the signed FakeIdP round trip posts to the same scoped ACS family."
    - "D-03/D-05: missing, username-only, password-only, empty, absent-header, and invalid credentials receive the Basic challenge with no admin session keys on `/login/admin` and the seeded connection trace."
    - "D-03/D-05: configured valid credentials enter through `/login/admin`, establish the fixed host-owned admin session, and reach `/relyra/admin/connections/:connection_id/trace`."
  </behavior>
  <action>
    Start RED by replacing the existing suffix-only Setup assertions with a contract based on `Fixtures.relyra_enabled_scenario_id/0`: assert exact absolute metadata, login, and ACS values, dispatch metadata and login through `LedgerLoopWeb.Endpoint`, and retain the signed FakeIdP ACS round trip as behavioral evidence. Extend the route-affordance matrix to the exact seeded trace URL and require successful access only after a Basic-authenticated GET to `/login/admin` establishes the admin session.

    Implement D-01/D-02 in `LedgerLoopWeb.SetupLive` with one private URL-set helper derived from the selected enabled `%Relyra.Ecto.Connection{connection_id: ...}` and Phoenix verified routes under `/saml`. Assign the set during mount, render metadata/entity ID, test-login, and ACS from that assign, and use the set's login path for `handle_event("test_login", ...)`. When no enabled connection exists, preserve a non-actionable state rather than inventing an unscoped route. Do not add or alter router entries.

    Implement the D-03/D-05 proof without modifying `DemoAdminAuth`: exercise `/login/admin` as the only scope-establishing entry, carry its returned session into the trace request, and assert missing/partial/invalid configurations remain 401 with the existing `WWW-Authenticate` challenge and nil admin session keys. Remove any test shortcut that manually seeds admin session keys for trace access. Keep the SAML verifier, XML/crypto seams, configured certificate trust, algorithm policy, replay protection, and all files under root `lib/` unchanged.
  </action>
  <acceptance_criteria>
    - "D-01 source: SetupLive contains one connection-derived URL-set boundary and the HEEx template consumes assigns rather than literal operator endpoint values."
    - "D-02 behavior: focused tests perform real GET dispatch for metadata and login and a signed POST to the scoped ACS path."
    - "D-03/D-05 denial: missing, partial, empty, absent-header, and invalid credentials return 401 on both `/login/admin` and the connection trace with no admin session established."
    - "D-03/D-05 allow: valid configured credentials authenticate at `/login/admin`, redirect to `/relyra/admin`, and the resulting session reaches the exact trace path."
    - "Scope: `git diff --exit-code -- lib mix.exs test/security/xml/adversarial_crypto_test.exs` succeeds."
  </acceptance_criteria>
  <verify>
    <automated>mix --cd demo/ledger_loop test test/ledger_loop_web/live/setup_live_test.exs test/ledger_loop_web/fake_idp_flow_test.exs test/ledger_loop_web/controllers/route_affordance_controller_test.exs &amp;&amp; mix ci.demo_app &amp;&amp; mix format --check-formatted &amp;&amp; git diff --exit-code -- lib mix.exs test/security/xml/adversarial_crypto_test.exs</automated>
  </verify>
  <done>D-01, D-02, D-03, and D-05 are proven through mounted route behavior and the existing credentialed admin entry, with denied variants closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enabled connection → Setup UI | Persisted connection identity determines operator-facing SAML endpoints. |
| browser → SAML router | Displayed endpoint values cross into mounted metadata/login/ACS behavior. |
| browser credentials → DemoAdminAuth | Untrusted Basic Auth input determines access to admin session and traces. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72.1-01 | Spoofing / Tampering | Setup SAML URL set | high | mitigate | Derive all values from one enabled connection and Phoenix verified scoped routes; dispatch them in focused tests. |
| T-72.1-02 | Elevation of privilege | `/login/admin` and trace mount | high | mitigate | Preserve `DemoAdminAuth`, enter only through credentialed `/login/admin`, and test allow/deny behavior on the exact trace path. |
| T-72.1-03 | Information disclosure | admin session and trace | medium | mitigate | Assert denial leaves all admin session keys nil and add no secret, payload, cookie, or key logging. |
| T-72.1-04 | Tampering | SAML cryptographic boundary | high | mitigate | Do not modify root `lib/`, route macro implementation, XML/crypto seams, policy, trust material, or replay behavior; retain signed ACS evidence and security gates. |
</threat_model>

<verification>
- Focused route/auth tracer command passes.
- `mix ci.demo_app` and `mix format --check-formatted` pass after the task.
- Root security/package seams have no diff.
</verification>

<success_criteria>
- A user copies one coherent connection-scoped metadata/login/ACS set from Setup.
- Each endpoint family member has behavioral evidence, not rendered-string evidence alone.
- The operator trace is accessible only through configured Basic Auth, with all denied variants challenged.
</success_criteria>

<source_coverage_audit>

| SOURCE | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | Mounted evaluator endpoints and authenticated trace journey | 72.1-01..04 | COVERED |
| REQ | — | No phase requirement IDs; SPEC-less probe visibly skipped and no IDs invented | all | EXCLUDED — source has none |
| RESEARCH | R-ROUTES | One route-derived URL set plus dispatch evidence | 72.1-01 | COVERED |
| RESEARCH | R-AUTH | Existing Basic Auth boundary and explicit runtime handoff | 72.1-01..04 | COVERED |
| CONTEXT | D-01 | Connection-scoped displayed URLs | 72.1-01, 72.1-03 | COVERED |
| CONTEXT | D-02 | Route behavior evidence | 72.1-01 | COVERED |
| CONTEXT | D-03 | Fail-closed Basic Auth | 72.1-01, 72.1-02, 72.1-04 | COVERED |
| CONTEXT | D-04 | Visible credential prerequisite/recovery | 72.1-02, 72.1-03 | COVERED |
| CONTEXT | D-05 | Valid and denied evidence across journeys | 72.1-01..04 | COVERED |

Deferred ideas: none. Package installs: none. Schema-push patterns: none. Assumption-delta detector: false.
</source_coverage_audit>

<output>
Create `.planning/phases/72.1-close-v1-10-evaluator-endpoint-and-trace-auth-gaps/72.1-01-SUMMARY.md` when done.
</output>
