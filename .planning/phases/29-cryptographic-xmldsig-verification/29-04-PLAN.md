---
phase: 29-cryptographic-xmldsig-verification
plan: 04
type: execute
wave: 3
depends_on: ["29-03"]
files_modified:
  - lib/relyra/test_support/xmldsig_signer.ex
  - test/relyra/security/signature_crypto_test.exs
  - test/protocol/consume_response_pipeline_test.exs
  - test/conformance/sp_conformance_test.exs
autonomous: true
requirements: [SIGV-01, SIGV-02]
must_haves:
  truths:
    - "A genuinely-signed Assertion (real DigestValue + real SignatureValue from FakeIdP's keypair) verifies as {:ok, %SignedNode{}}"
    - "A response signed by a different key is rejected with :invalid_signature"
    - "A response whose NameID is tampered after signing is rejected with :digest_mismatch"
    - "The signer canonicalizes with the SAME C14N engine the verifier uses (no divergent signer)"
    - "Every pre-existing test that asserted {:ok} on a structure-only signature is triaged: re-pointed at the genuine positive control OR updated to assert the new typed crypto-rejection"
    - "Full `mix test --warnings-as-errors` is green after the triage (the phase gate is met)"
  artifacts:
    - path: "lib/relyra/test_support/xmldsig_signer.ex"
      provides: "Minimal real XMLDSig signer (D-11) reusing FakeIdP's RSA-2048 keypair; emits real ds:DigestValue + ds:SignatureValue; prod-guarded; promotable into FakeIdP in Phase 30"
      contains: ":public_key.sign"
    - path: "test/relyra/security/signature_crypto_test.exs"
      provides: "Positive control ({:ok}) + wrong-key/tampered-NameID negatives using the genuine signer"
    - path: "test/protocol/consume_response_pipeline_test.exs"
      provides: "Triaged {:ok}-asserting login tests — re-pointed at the genuine signer OR asserting the new typed rejection"
      contains: "XmldsigSigner"
  key_links:
    - from: "lib/relyra/test_support/xmldsig_signer.ex"
      to: "Relyra.Security.XML.C14N.serialize / PureBeam.canonicalize"
      via: "canonicalize SignedInfo + referenced element with the verifier's engine, then :public_key.sign"
      pattern: "C14N|canonicalize"
    - from: "test/relyra/security/signature_crypto_test.exs"
      to: "Signature.verify/4"
      via: "genuinely-signed Response → {:ok, %SignedNode{}}; tampered/wrong-key → {:error}"
      pattern: "Signature.verify"
    - from: "test/protocol/consume_response_pipeline_test.exs + test/conformance/sp_conformance_test.exs"
      to: "Relyra.TestSupport.XmldsigSigner / new typed rejection"
      via: "{:ok}-asserting login tests re-pointed at the genuine signer or updated to assert the crypto reject"
      pattern: "XmldsigSigner|:invalid_signature|:digest_mismatch"
---

<objective>
Build the Phase-29-local genuine XMLDSig signer (D-11) and use it to prove the POSITIVE control — a
genuinely-signed Assertion verifies as `{:ok, %SignedNode{}}` (ROADMAP success #3) — plus the two
negative controls that require a real signature: wrong-key (`:invalid_signature`) and tampered-NameID
(`:digest_mismatch`). Then TRIAGE every pre-existing `{:ok}`-asserting test that fed a structure-only
"signature" so the full suite goes green under the new reject-by-default behavior (RESEARCH Runtime
State Inventory; VALIDATION.md phase gate).

Purpose: Without a positive control, an "always-reject" verifier would pass every negative test in
Plan 03. The genuine signer is the ONLY input that should remain `{:ok}` after the crypto lands. Per
D-12 it MUST canonicalize with the SAME C14N engine the verifier uses and be written so Phase 30 can
PROMOTE it into `FakeIdP` (no divergent second signer that would canonicalize differently). It reuses
`FakeIdP`'s EXISTING RSA-2048 keypair (D-11 — do NOT generate a second), filling the `DigestValue` /
`SignatureValue` that `FakeIdP.response_xml` omits today (fake_idp.ex:126-131). The triage task closes
the gap RESEARCH flagged: existing end-to-end tests (e.g. `consume_response_pipeline_test.exs`,
`sp_conformance_test.exs`) that asserted login `{:ok}` on structure-only signatures will now CORRECTLY
reject — they must be re-pointed at the genuine signer or updated to assert the new typed rejection so
`mix test --warnings-as-errors` is green before `/gsd:verify-work`.

Output: `Relyra.TestSupport.XmldsigSigner` (prod-guarded, test-support home) + positive/negative crypto
assertions feeding `Signature.verify/4` + a triaged, fully-green suite. Depends on the C14N Option-a
fix (Plan 01) and the crypto wiring (Plan 03) both being in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-cryptographic-xmldsig-verification/29-CONTEXT.md
@.planning/phases/29-cryptographic-xmldsig-verification/29-PATTERNS.md
@.planning/phases/29-cryptographic-xmldsig-verification/29-RESEARCH.md

<interfaces>
<!-- FakeIdP keypair reuse (D-11): FakeIdP.keypair() (fake_idp.ex:77-83) -> {:RSAPrivateKey, ...}. -->
<!--   ensure_keypair!/0 (fake_idp.ex:85-95) persistent_term cached. ensure_not_prod!/0 (97-101) prod guard. -->
<!-- FakeIdP.response_xml/2 (fake_idp.ex:103-136) emits an EMPTY SignedInfo (no DigestValue/SignatureValue) — the gap to fill. -->

<!-- Verifier engine to reuse (D-12, anti-divergent-signer): -->
<!--   Relyra.Security.XML.C14N.serialize(%Node{}, prefix_list: [...]) -> {:ok, bytes} -->
<!--   Relyra.Security.XML.PureBeam.parse_safely/2, select_signed_node/2, canonicalize/2 -->
<!-- Sign: :public_key.sign(c14n_signed_info, :sha256, private_key) -> der_sig (default padding == RSASSA-PKCS1-v1_5, RESEARCH §5). -->

<!-- Self-signed cert for the configured cert_chain: derive from the SAME keypair (openssl OR in-process :public_key). -->
<!-- Signature.verify(parsed_doc, connection, cert_chain, opts) -> {:ok, %SignedNode{}} | {:error, %Relyra.Error{}}. -->

<!-- Existing {:ok}-asserting login tests to triage (grep-confirmed): -->
<!--   test/protocol/consume_response_pipeline_test.exs (assert {:ok, login_result} at 128,143,220,313) -->
<!--   test/conformance/sp_conformance_test.exs (assert {:ok, login_result} at 83,96) -->
<!-- These feed full Responses through ValidationPipeline.run expecting login success on structure-only signatures. -->
<!-- Trust-GATE tests STAY green (gates run before crypto): test/security/signed_node_binding_test.exs, signature_test.exs. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the genuine XMLDSig signer (D-11) reusing FakeIdP's keypair and the verifier's C14N engine</name>
  <files>lib/relyra/test_support/xmldsig_signer.ex</files>
  <read_first>
    - lib/relyra/test_support/fake_idp.ex (FULL — keypair/0 at 77-83, ensure_keypair!/0 at 85-95, ensure_not_prod!/0 at 97-101, response_xml/2 at 103-136 with the EMPTY SignedInfo at 126-131)
    - lib/relyra/security/xml/c14n.ex (serialize/2 at 80-97; canonicalize_reference/4 at 130-141)
    - lib/relyra/security/xml/pure_beam.ex (parse_safely/2, select_signed_node/2, canonicalize/2 at 294-330)
    - .planning/phases/29-cryptographic-xmldsig-verification/29-RESEARCH.md (Code Examples §5; Anti-Patterns "A second divergent signer"; Assumption A2)
    - .planning/phases/29-cryptographic-xmldsig-verification/29-PATTERNS.md (D-11 signer section, lines 202-232)
  </read_first>
  <action>
    Create `Relyra.TestSupport.XmldsigSigner` in `lib/relyra/test_support/xmldsig_signer.ex` (the
    `lib/relyra/test_support/` home — Claude's-Discretion resolved: co-locate with FakeIdP so it shares
    `FakeIdP.keypair()` and Phase 30 promotes it in place per D-12). Mirror FakeIdP's `@prod_build` +
    `ensure_not_prod!/0` discipline so signing code never compiles/runs in `:prod`.

    The signer produces a genuinely-signed SAML Response (enveloped signature over the `<Assertion>`):
    1. Build the Response/Assertion XML. Keep it whitespace-free OR rely on the Plan 01 Option-a fix
       (Assumption A2) — prefer producing exactly the byte shape the C14N engine will canonicalize.
    2. Locate the referenced `<Assertion>` and compute its real DigestValue: canonicalize it with the
       VERIFIER's path (`PureBeam.canonicalize/2` over the bound node, enveloped-sig + exc-c14n, OR
       `C14N.canonicalize_reference/4` directly) → `:crypto.hash(:sha256, bytes)` → `Base.encode64` =
       real `ds:DigestValue`.
    3. Build `SignedInfo` embedding that DigestValue (with the rsa-sha256 SignatureMethod + sha256
       DigestMethod URIs and the exc-c14n CanonicalizationMethod) and canonicalize it with
       `C14N.serialize/2` (D-12 — the SAME engine the verifier uses; this is the anti-divergent-signer
       guarantee).
    4. `ds:SignatureValue` = `Base.encode64(:public_key.sign(c14n_signed_info, :sha256, private_key))`
       where `private_key = FakeIdP.keypair()` (D-11 — reuse, do NOT generate a second keypair).
    5. Provide a self-signed cert PEM for the configured `cert_chain` derived from that SAME keypair
       (in-process via `:public_key` preferred — no openssl dependency; openssl acceptable if simpler).

    Expose a small API the tests can drive, e.g. `signed_response(opts)` returning
    `{response_xml, cert_chain_pems}`, and a knob to TAMPER the NameID AFTER signing (rewrite the NameID
    text in the emitted XML without re-signing) for the digest-mismatch negative. Document at the top
    that this module is the Phase-30 promotion target (D-12) and MUST stay byte-aligned with the C14N
    engine.
  </action>
  <verify>
    <automated>mix test test/relyra/security/signature_crypto_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/relyra/test_support/xmldsig_signer.ex` defines `Relyra.TestSupport.XmldsigSigner` with `ensure_not_prod!`-style prod guarding
    - it calls `:public_key.sign(` over `C14N.serialize/2`-canonicalized SignedInfo and `Base.encode64`s a real `ds:SignatureValue`
    - it computes a real `ds:DigestValue` via the verifier's canonicalize path + `:crypto.hash(:sha256, ...)` (NOT a placeholder/empty value)
    - it reuses `FakeIdP.keypair()` (no second `:public_key.generate_key` call in this module)
    - it exposes a NameID-tamper-after-signing knob for the digest-mismatch negative
    - `mix compile --warnings-as-errors` succeeds
  </acceptance_criteria>
  <done>A prod-guarded genuine XMLDSig signer exists, reuses FakeIdP's keypair, emits real DigestValue/SignatureValue canonicalized by the verifier's own C14N engine, and can tamper the NameID post-signing — ready for Phase 30 promotion.</done>
</task>

<task type="auto">
  <name>Task 2: Positive control ({:ok}) + wrong-key and tampered-NameID negatives through Signature.verify/4</name>
  <files>test/relyra/security/signature_crypto_test.exs</files>
  <read_first>
    - test/relyra/security/signature_crypto_test.exs (the negative-control file created in Plan 03 — extend it)
    - lib/relyra/test_support/xmldsig_signer.ex (Task 1 — the signer API)
    - lib/relyra/security/signature.ex (verify/4 entry at 8-34; the do_verify path)
    - lib/relyra/security/xml/pure_beam.ex (parse_safely/2 + select_signed_node/2 to build parsed_doc from the signed XML)
    - .planning/phases/29-cryptographic-xmldsig-verification/29-VALIDATION.md (Positive vs Negative Control Discipline; SIGV-01/02 rows)
  </read_first>
  <action>
    Add three assertions to `test/relyra/security/signature_crypto_test.exs` driving the full path
    (`PureBeam.parse_safely` → `select_signed_node` → `Signature.verify/4` with the signer's cert_chain):
    - **POSITIVE control (SIGV-01/02, success #3):** a genuinely-signed Response from
      `XmldsigSigner.signed_response/1` verifies → `assert {:ok, %SignedNode{}} = Signature.verify(...)`.
      This is the ONLY input that should return `{:ok}`.
    - **Wrong-key negative (SIGV-01):** verify the genuinely-signed Response against a DIFFERENT cert
      (a self-signed cert from a second, throwaway keypair generated in the test — NOT FakeIdP's) →
      `{:error, %Relyra.Error{type: :invalid_signature}}`.
    - **Tampered-NameID negative (SIGV-02):** take the signed Response, tamper the NameID AFTER signing
      (the Task 1 knob), verify against the genuine cert_chain → `{:error, %Relyra.Error{type: :digest_mismatch}}`
      (the SignatureValue is otherwise well-formed; only the digest fails).

    Use the genuine `cert_chain` PEM the signer emits for the positive + tampered cases. Keep the test
    `async: true` if the keypair persistent-term reuse allows it; otherwise scope appropriately. Do NOT
    weaken any Plan 03 negative-control assertions.
  </action>
  <verify>
    <automated>mix test test/relyra/security/signature_crypto_test.exs test/security/signed_node_binding_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - genuinely-signed Response → `assert {:ok, %Relyra.Security.SignedNode{}} = Signature.verify(parsed_doc, connection, cert_chain)` passes
    - same Response verified against a DIFFERENT cert → `{:error, %Relyra.Error{type: :invalid_signature}}`
    - NameID tampered after signing → `{:error, %Relyra.Error{type: :digest_mismatch}}`
    - all Plan 03 negative controls still pass (forged/non-base64 sig, truncated digest, ECDSA, malformed cert)
    - `mix test test/relyra/security/signature_crypto_test.exs test/security/signed_node_binding_test.exs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>The genuine positive control verifies as `{:ok, %SignedNode{}}`; wrong-key and tampered-NameID are rejected with the named typed errors; the verifier is proven not to be an "always-reject" stub.</done>
</task>

<task type="auto">
  <name>Task 3: Triage every pre-existing {:ok}-asserting structure-only test so the full suite is green</name>
  <files>test/protocol/consume_response_pipeline_test.exs, test/conformance/sp_conformance_test.exs</files>
  <read_first>
    - test/protocol/consume_response_pipeline_test.exs (the `assert {:ok, login_result}` cases at 128,143,220,313 — these feed full Responses through ValidationPipeline.run expecting login success)
    - test/conformance/sp_conformance_test.exs (the `assert {:ok, login_result}` login cases at 83,96)
    - lib/relyra/test_support/xmldsig_signer.ex (Task 1 — the genuine signer + cert_chain the re-pointed positive tests use)
    - lib/relyra/protocol/validation_pipeline.ex (run/4 + cert_chain/2 at 208-211 — how cert_chain is sourced from opts/connection)
    - .planning/phases/29-cryptographic-xmldsig-verification/29-RESEARCH.md (Runtime State Inventory; A5 — no genuinely-signed fixture exists in the repo today)
    - .planning/phases/29-cryptographic-xmldsig-verification/29-VALIDATION.md (Sampling Rate — full `mix test --warnings-as-errors` green is the phase gate)
  </read_first>
  <action>
    First, grep the suite for every test that asserts `{:ok}` on a signature/login path and feeds a
    structure-only (non-genuine) signature: run `grep -rnE "assert \{:ok" test/ --include="*.exs"` and
    cross-reference against `Signature.verify` / `ValidationPipeline.run` / `consume_response` callers.
    The confirmed set is `test/protocol/consume_response_pipeline_test.exs` and
    `test/conformance/sp_conformance_test.exs` (login `{:ok}` cases); include any other file the grep
    surfaces that feeds a structure-only Response and expects `{:ok}`. Trust-GATE tests
    (`signed_node_binding_test.exs`, `signature_test.exs`) STAY as-is — they assert pre-crypto gate
    rejections and remain green; do NOT touch them.

    Then, per identified test, choose exactly one disposition and apply it:
    - **Re-point at the genuine positive control:** replace the structure-only Response with one produced
      by `Relyra.TestSupport.XmldsigSigner.signed_response/1`, passing the signer's emitted `cert_chain`
      into `ValidationPipeline.run` (via the `:cert_chain` opt or the connection's `:idp_certificates`,
      whichever the test already uses) so the login still asserts `{:ok}` — now for the RIGHT reason
      (genuine signature). Prefer this for tests whose intent is "a valid login succeeds end-to-end".
    - **Update to assert the new typed rejection:** for tests whose intent is purely a non-signature
      assertion (status/destination/audience/etc.) and that incidentally relied on the structure-only
      `{:ok}`, change the assertion to the new typed crypto error (`:invalid_signature` /
      `:digest_mismatch`), OR re-point them at the genuine signer if the non-signature assertion still
      needs a successful path. Document the chosen disposition inline per test with a one-line comment
      naming the reason.

    Do NOT relax `--warnings-as-errors`, do NOT delete coverage, and do NOT weaken any Plan 03/Task 2
    crypto assertions. The genuine signer (Task 1) is the ONLY input that should remain `{:ok}`.
  </action>
  <verify>
    <automated>mix test --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "assert \{:ok" test/ --include="*.exs"` was run and every structure-only signature/login `{:ok}` case is accounted for (re-pointed at the genuine signer OR updated to the typed rejection)
    - `test/protocol/consume_response_pipeline_test.exs` and `test/conformance/sp_conformance_test.exs` no longer assert login `{:ok}` on a structure-only signature (either reference `XmldsigSigner` for a genuine positive, or assert `:invalid_signature`/`:digest_mismatch`)
    - trust-GATE tests (`signed_node_binding_test.exs`, `signature_test.exs`) are unchanged and still green
    - no Plan 03 / Task 2 crypto assertion is weakened; no test is deleted to satisfy the gate
    - `mix test --warnings-as-errors` exits 0 (full-suite phase gate met)
  </acceptance_criteria>
  <done>Every pre-existing `{:ok}`-asserting structure-only test is triaged to the genuine positive control or the new typed rejection; trust-gate tests stay green; the full `mix test --warnings-as-errors` suite is green, meeting the phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signer ↔ verifier C14N | the signer MUST canonicalize with the verifier's engine; a divergent signer would make the positive control pass for the wrong reason |
| test keypair → prod | signing code is test-only; it must never compile/run in `:prod` |
| stale test fixtures → trust contract | structure-only "signed" fixtures that asserted {:ok} must be triaged; leaving them silently green-via-relaxation would mask a broken crypto path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-14 | Spoofing | "Always-reject" verifier passes all negatives but breaks real logins | mitigate | The D-11 positive control proves a genuine signature verifies `{:ok}` (the verifier still WORKS) |
| T-29-15 | Tampering | Divergent second signer canonicalizes differently → false positive | mitigate | Signer reuses the verifier's `C14N` engine (D-12) and FakeIdP's keypair; promotable into FakeIdP |
| T-29-16 | Spoofing | Wrong-key acceptance | mitigate | Wrong-key negative asserts `:invalid_signature` against a different cert |
| T-29-17 | Tampering | Post-signing content tamper (NameID swap) | mitigate | Tampered-NameID negative asserts `:digest_mismatch` (digest recompute catches it) |
| T-29-18 | Elevation | Signing code reachable in prod | mitigate | `@prod_build` + `ensure_not_prod!` guard mirroring FakeIdP |
| T-29-19 | Tampering | Stale structure-only fixtures masked by suite relaxation hide a broken/bypassed crypto path | mitigate | Triage task re-points {:ok}-asserting tests at the genuine signer or the typed rejection; no `--warnings-as-errors` relaxation, no deleted coverage |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No package installs — OTP `:public_key`/`:crypto` only |
</threat_model>

<verification>
- `mix test test/relyra/security/signature_crypto_test.exs test/security/signed_node_binding_test.exs --warnings-as-errors` passes (positive + all negatives + gate regression).
- `mix test --warnings-as-errors` is GREEN (full-suite phase gate after triage — RESEARCH Runtime State Inventory + VALIDATION.md Sampling Rate).
- The signer module compiles under `--warnings-as-errors` and contains no second `:public_key.generate_key`.
</verification>

<success_criteria>
- A genuinely-signed Assertion verifies as `{:ok, %SignedNode{}}` (the verifier is not an always-reject stub).
- Wrong-key → `:invalid_signature`; tampered-NameID → `:digest_mismatch`.
- The signer reuses FakeIdP's keypair + the verifier's C14N engine and is prod-guarded and promotable (D-11/D-12).
- Every pre-existing structure-only `{:ok}`-asserting test is triaged; full `mix test --warnings-as-errors` is green.
</success_criteria>

<output>
Create `.planning/phases/29-cryptographic-xmldsig-verification/29-04-SUMMARY.md` when done
</output>
</content>
