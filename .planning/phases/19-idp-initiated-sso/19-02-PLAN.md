---
phase: 19-idp-initiated-sso
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/relyra/security/redirect.ex
  - test/security/redirect_test.exs
autonomous: true
requirements:
  - IDP-INIT-01
must_haves:
  truths:
    - "System provides a utility to safely redirect based on RelayState"
  artifacts:
    - path: "lib/relyra/security/redirect.ex"
      provides: "safe_local_redirect/2"
  key_links:
    - from: "lib/relyra/security/redirect.ex"
      to: "host application"
      via: "exposed utility function"
---

<objective>
Implement a utility to safely validate RelayState as a local redirect path.
Purpose: Provide developers a paved path to avoiding Open Redirect vulnerabilities when handling opaque RelayState values.
Output: `Relyra.Security.Redirect` module.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Safe Local Redirect</name>
  <files>lib/relyra/security/redirect.ex, test/security/redirect_test.exs</files>
  <behavior>
    - Case 1: path starting with `//` -> `{:error, %Relyra.Error{}}`
    - Case 2: path starting with `http://` or `https://` -> `{:error, %Relyra.Error{}}`
    - Case 3: path starting with `/` -> `{:ok, path}`
    - Case 4: nil -> `{:error, %Relyra.Error{}}`
    - Case 5: string not starting with `/` -> `{:error, %Relyra.Error{}}`
  </behavior>
  <action>
    Create `Relyra.Security.Redirect.safe_local_redirect/2` taking `(path, opts \\ [])`.
    Validate the string against the rules above. Use `Relyra.Error` for error returns (e.g., type `:invalid_redirect`).
    Analog reference: `lib/relyra/security/relay_state.ex` and utilize the `rejected/2` error handling shared pattern if applicable.
  </action>
  <verify>
    <automated>mix test test/security/redirect_test.exs</automated>
  </verify>
  <done>safe_local_redirect/2 accurately validates relay states and returns formatted Relyra.Error on failures.</done>
</task>
</tasks>