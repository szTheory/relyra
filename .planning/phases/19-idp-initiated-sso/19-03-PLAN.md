---
phase: 19-idp-initiated-sso
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - lib/relyra.ex
  - lib/relyra/login_result.ex
  - lib/relyra/protocol/validation_pipeline.ex
  - test/relyra_test.exs
  - test/protocol/consume_response_pipeline_test.exs
autonomous: true
requirements:
  - IDP-INIT-01
must_haves:
  truths:
    - "System accepts responses without InResponseTo if connection allows it"
    - "System rejects responses without InResponseTo if connection does not allow it"
    - "System extracts opaque RelayState to LoginResult"
  artifacts:
    - path: "lib/relyra/protocol/validation_pipeline.ex"
      provides: "Updated correlation validation"
    - path: "lib/relyra.ex"
      provides: "Updated consume_response to handle nil request intent"
    - path: "lib/relyra/login_result.ex"
      provides: "RelayState field in login result"
  key_links:
    - from: "lib/relyra.ex"
      to: "lib/relyra/protocol/validation_pipeline.ex"
      via: "consume_response passes nil intent correctly"
---

<objective>
Update core validation pipeline and facade to support IdP-initiated flows when configured.
Purpose: Bypass InResponseTo constraints selectively and securely, and surface RelayState on the LoginResult.
Output: Updated `Relyra` and `ValidationPipeline` modules.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/relyra/protocol/validation_pipeline.ex
@lib/relyra.ex
@lib/relyra/login_result.ex
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Support nil request intent and bypass InResponseTo if allowed</name>
  <files>lib/relyra/protocol/validation_pipeline.ex, lib/relyra.ex, test/protocol/consume_response_pipeline_test.exs</files>
  <behavior>
    - Test 1: `ValidationPipeline` rejects if `request_intent` is `nil` and `allow_idp_initiated` is false.
    - Test 2: `ValidationPipeline` accepts if `request_intent` is `nil`, `allow_idp_initiated` is true, and `InResponseTo` is missing.
    - Test 3: `ValidationPipeline` rejects if `request_intent` is `nil`, `allow_idp_initiated` is true, but `InResponseTo` is present.
    - Test 4: `Relyra.consume_response` handles `nil` for `request_intent_or_opts` parameter correctly.
  </behavior>
  <action>
    Update `Relyra.Protocol.ValidationPipeline.validate_request_correlation/4` (update signature to accept `connection` if not already, e.g. `defp validate_request_correlation(parsed_doc, request_intent, connection, _opts)`) to implement the `allow_idp_initiated` logic.
    Follow the validation pattern from `lib/relyra/protocol/validation_pipeline.ex` in 19-PATTERNS.md:
    ```elixir
    # modify pattern to check `connection.allow_idp_initiated?` if `request_intent` is `nil` and `actual_id` is missing
    defp validate_request_correlation(parsed_doc, request_intent, connection, _opts) do
      # ...
    ```
    Update `expected_connection_id`, `expected_destination`, `expected_recipient` in `ValidationPipeline` to handle `request_intent == nil` securely (e.g., using `&&` or falling back to connection fields instead of `Map.get`).
    Update `Relyra` module functions like `resolve_request_intent`, `validate_request_intent`, `consume_request_intent` to gracefully handle `nil` request intent without failing clauses. Fix any `Map.get(nil, ...)` calls.
    Follow the core request intent resolution pattern from `lib/relyra.ex` in 19-PATTERNS.md:
    ```elixir
    defp resolve_request_intent(opts, []) when is_list(opts) do
      # adapt pattern to return `{:ok, nil, opts}` if intent is nil and let pipeline proceed
      # ...
    ```
    Ensure `relay_state` is extracted from `opts` or payload and threaded through to `login_result` in `ValidationPipeline` so it appears in the final result map.
  </action>
  <verify>
    <automated>mix test test/protocol/consume_response_pipeline_test.exs</automated>
  </verify>
  <done>Pipeline securely handles IdP-initiated flows when enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Expose RelayState on LoginResult</name>
  <files>lib/relyra.ex, lib/relyra/login_result.ex, test/relyra_test.exs</files>
  <action>
    Ensure `relay_state` is properly set on the `Relyra.LoginResult` struct constructed in `Relyra.consume_response/3`.
    If `ValidationPipeline` returns it in its map, map it to the struct. `lib/relyra/login_result.ex` already has the `:relay_state` field defined.
    Follow the analog pattern for the `Relyra` facade from `lib/relyra.ex` in 19-PATTERNS.md, adapting it so the `relay_state` flows directly from the pipeline into the `LoginResult` creation.
    Update `test/relyra_test.exs` to verify that `relay_state` from `opts` (e.g., passed as `[relay_state: "/foo"]`) appears on the returned `LoginResult`.
  </action>
  <verify>
    <automated>mix test test/relyra_test.exs</automated>
  </verify>
  <done>RelayState is accessible to the host application upon successful login.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IdP -> App | Unverified HTTP POST body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Spoofing | `ValidationPipeline` | mitigate | Explicitly block responses containing `InResponseTo` if processing as IdP-initiated, to prevent signature wrapping or replay attacks. |
</threat_model>

<verification>
mix test passes successfully.
</verification>

<success_criteria>
IdP-initiated flows are accepted only when connection opts in. RelayState is properly exfiltrated.
</success_criteria>