---
phase: 19-idp-initiated-sso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/*_add_allow_idp_initiated_to_relyra_connections.exs
  - lib/relyra/ecto/connection.ex
  - test/relyra/ecto/connection_test.exs
autonomous: true
requirements:
  - IDP-INIT-01
must_haves:
  truths:
    - "System persists `allow_idp_initiated` flag for connections"
  artifacts:
    - path: "priv/repo/migrations/*_add_allow_idp_initiated_to_relyra_connections.exs"
      provides: "Migration for allow_idp_initiated"
    - path: "lib/relyra/ecto/connection.ex"
      provides: "Schema and changeset for allow_idp_initiated"
  key_links:
    - from: "lib/relyra/ecto/connection.ex"
      to: "database table"
      via: "Ecto schema"
---

<objective>
Update the data model to persist the `allow_idp_initiated` connection-level setting.
Purpose: Opt-in explicit requirement for IdP-initiated flows (defaulting to false) to maintain a fail-closed posture against Login CSRF.
Output: Migration file and updated Connection Ecto schema.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/relyra/ecto/connection.ex
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add allow_idp_initiated field</name>
  <files>priv/repo/migrations/add_allow_idp_initiated_to_relyra_connections.exs, lib/relyra/ecto/connection.ex, test/relyra/ecto/connection_test.exs</files>
  <behavior>
    - Test 1: By default, a new connection has `allow_idp_initiated: false`.
    - Test 2: The `draft_changeset` and `update_changeset` permit updating `allow_idp_initiated`.
  </behavior>
  <action>
    Generate a new Ecto migration (timestamped correctly in the filename pattern) to add `allow_idp_initiated: boolean, default: false, null: false` to the `relyra_connections` table.
    Use the analog pattern from `priv/repo/migrations/20260505130000_add_metadata_revision_pointers_to_relyra_connections.exs`:
    ```elixir
    def change do
      alter table(:relyra_connections) do
        add :allow_idp_initiated, :boolean, default: false, null: false
      end
    end
    ```

    Update `Relyra.Ecto.Connection` to include `field :allow_idp_initiated, :boolean, default: false`.
    Add `:allow_idp_initiated` to the list of allowed fields in `draft_changeset` and `update_changeset`.
    Use the analog pattern from `lib/relyra/ecto/connection.ex` for the schema and changeset:
    ```elixir
    schema "relyra_connections" do
      # ...
      field :allow_idp_initiated, :boolean, default: false
    ```
    ```elixir
    def draft_changeset(connection, attrs) do
      connection
      |> cast(attrs, [
        # ...
        :allow_idp_initiated
      ])
    ```
    Write tests in `connection_test.exs` to verify this field works as expected.
  </action>
  <verify>
    <automated>mix test test/relyra/ecto/connection_test.exs</automated>
  </verify>
  <done>Ecto schema, migration, and tests are complete and passing.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database -> App | Loading connection configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | `lib/relyra/ecto/connection.ex` | mitigate | Defaulting `allow_idp_initiated` to false ensures we fail-closed against Login CSRF. |
</threat_model>

<verification>
Migration runs without errors and Ecto tests pass.
</verification>

<success_criteria>
`allow_idp_initiated` field exists in Ecto schema, changesets, and database.
</success_criteria>