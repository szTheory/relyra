---
phase: 57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex
  - demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs
autonomous: true
requirements: [WR-03, WR-04, IN-03, WR-01, WR-02]

must_haves:
  truths:
    - "Unknown idp_action value resolves to the success path with HTTP 200 (no CaseClauseError/500) — WR-03"
    - "An oversized-inflating SAMLRequest yields in_response_to: nil with no crash/hang (64 KiB ceiling, fail-closed) — WR-04"
    - "Garbled SAMLRequest bytes still yield nil (fail-closed contract preserved) — WR-04"
    - "A non-NCName InResponseTo ID is rejected (extractor returns nil) — IN-03/WR-01"
    - "The valid-login radio label renders sarah@northstar.example.com and never evaluator@example.com — WR-02"
  artifacts:
    - path: "demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex"
      provides: "Catch-all idp_action (WR-03); bounded safeInflate loop (WR-04); NCName-tightened extractor (IN-03/WR-01)"
      contains: "safeInflate"
    - path: "demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex"
      provides: "Corrected valid-login label (WR-02)"
      contains: "sarah@northstar.example.com"
    - path: "demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs"
      provides: "WR-03, WR-04 (oversized + garbled), IN-03, WR-02 adversarial rows"
  key_links:
    - from: "fake_idp_controller.ex sso/2"
      to: "success path catch-all"
      via: "case action do \"failure\" -> ... ; _ -> ... end"
      pattern: "_ ->"
    - from: "fake_idp_controller.ex inflate/1"
      to: ":zlib.safeInflate/2 byte ceiling"
      via: "bounded decompression loop"
      pattern: "safeInflate"
    - from: "fake_idp_controller.ex extract_in_response_to/1"
      to: "NCName grammar"
      via: "tightened ID regex"
      pattern: "A-Za-z_"
---

<objective>
Harden the demo FakeIdP controller's untrusted-input boundary (unauthenticated GET/POST) and correct
the lying login label:

- WR-03: add a catch-all `idp_action` clause so any non-`"failure"` value (including arrays/maps from
  crafted multipart) resolves to the success path — no `CaseClauseError`/500.
- WR-04: replace single-shot `:zlib.inflate/2` with a bounded `:zlib.safeInflate/2` loop capped at
  64 KiB, failing closed to `:error` → `nil` (preserving the existing garbled→nil contract).
- IN-03/WR-01: tighten the `extract_in_response_to/1` ID regex to the xsd:ID/NCName grammar so
  non-name characters are rejected before reaching the signer template, with a comment documenting the
  first-match (root `AuthnRequest/@ID`) assumption.
- WR-02: relabel the valid-login radio to `sarah@northstar.example.com` (the actual emitted subject;
  `evaluator@example.com` is not a seeded `SAMLIdentity`).

Purpose: close the decompression-bomb (DoS), unhandled-input-crash (DoS), and XML-injection input
surfaces on the FakeIdP's unauthenticated endpoints, and restore label fidelity — all confined to
`demo/ledger_loop` with the Relyra verifier untouched.

Output: hardened `fake_idp_controller.ex`, corrected `login.html.heex`, new adversarial controller rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-CONTEXT.md
@.planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-RESEARCH.md
@.planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-VALIDATION.md
@demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex
@demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex

<interfaces>
WR-04 bounded inflate (RESEARCH Code Examples + Assumption A3): keep the existing try/rescue/after shape
that closes the zlib port. Replace single-shot `:zlib.inflate/2` with a `:zlib.safeInflate/2` loop that
accumulates output, aborts to `:error` when accumulated bytes exceed `@max_inflated_bytes` (64 KiB), and
on a malformed stream is caught by `rescue _ -> :error`. The `inflate/1` contract stays `{:ok, bin} | :error`
so `extract_in_response_to/1`'s existing `with`/`else -> nil` collapses overflow to nil. CONFIRM the exact
safeInflate continuation-drain idiom (feeding `[]` on `{:continue, _}`) against OTP 28 docs at implementation;
the byte-ceiling and :error→nil contract are the load-bearing parts.

IN-03/WR-01 tightened extractor (RESEARCH Code Examples): replace `~r/\bID="([^"]+)"/` with
`~r/\bID="([A-Za-z_][A-Za-z0-9._-]*)"/` (ASCII NCName: leading letter or `_`, then letters/digits/`.`/`-`/`_`).
This MUST still accept relyra's own AuthnRequest IDs (e.g. `_<hex>`, `id-<...>`) — verify against the real
binding ID via the existing flow test (RESEARCH Pitfall 4). Add a comment documenting the first-match =
root AuthnRequest/@ID assumption (satisfies IN-03 per CONTEXT — tightened regex + comment is sufficient).

WR-03 catch-all: in sso/2, change `case action do "success" -> ... ; "failure" -> ... end` to
`case action do "failure" -> <tampered> ; _ -> <success> end`. Treat any non-"failure" value as success.

WR-04 oversized fixture: craft a deflate stream that inflates >64 KiB by deflating a large repetitive
payload at window -15 (`:zlib.deflateInit(z, level, :deflated, -15, 8, :default)` or the simpler
zip/compress path used elsewhere), base64-encode (padding: false) — inline fixture in the controller test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-04 bounded inflate + IN-03/WR-01 NCName-tightened extractor</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex, demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs</files>
  <read_first>
    - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex (the file being modified — extract_in_response_to/1 at lines 114-124, inflate/1 at lines 126-139, the ID regex at line 119)
    - demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs (existing rows; new WR-04 + IN-03 rows extend this)
    - demo/ledger_loop/test/ledger_loop_web/fake_idp_flow_test.exs (the SUCCESS round-trip that exercises the REAL relyra binding ID — must keep its in_response_to non-nil after the regex tightening; RESEARCH Pitfall 4)
    - .planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-RESEARCH.md (Code Examples WR-04 + WR-01/IN-03; Pitfalls 2 + 4; Assumptions A2/A3)
  </read_first>
  <behavior>
    - WR-04 oversized: a crafted SAMLRequest whose inflated output exceeds 64 KiB → `extract_in_response_to/1`
      returns nil, GET /fake_idp/login responds 200, no crash/hang.
    - WR-04 garbled: non-decodable/garbled SAMLRequest bytes still return nil (fail-closed contract preserved).
    - IN-03/WR-01 malformed ID: a SAMLRequest whose ID attribute contains a non-NCName char (`<`, `&`, space)
      → extractor returns nil.
    - IN-03 success preserved: the existing flow-test SUCCESS round-trip still extracts the real relyra
      AuthnRequest ID (in_response_to non-nil) — the NCName regex must accept `_`/`-`-containing IDs.
  </behavior>
  <action>
    Replace single-shot `inflate/1` (fake_idp_controller.ex lines 126-139) with a bounded
    `:zlib.safeInflate/2` loop. Add `@max_inflated_bytes 64 * 1024` module attribute. Keep
    `:zlib.open()`, `:zlib.inflateInit(z, -15)`, the `try/rescue _ -> :error/after :zlib.close(z)` shape.
    Inside the try, drive a recursive `inflate_loop/4` (z, input, acc, total) over `:zlib.safeInflate(z, compressed)`:
    on `{:continue, output}` accumulate `byte_size`, return `:error` if `total > @max_inflated_bytes` else
    recurse feeding `[]` to drain; on `{:finished, output}` accumulate, return `:error` on overflow else
    `{:ok, IO.iodata_to_binary(...)}`. Do NOT add a raise — overflow returns `:error` so the existing
    `with`/`else -> nil` in `extract_in_response_to/1` maps it to nil (RESEARCH Pitfall 2; preserve the
    garbled→nil contract). Confirm the safeInflate drain idiom against OTP 28 at implementation (A3).

    Tighten the extractor regex (line 119) from `~r/\bID="([^"]+)"/` to
    `~r/\bID="([A-Za-z_][A-Za-z0-9._-]*)"/`. Add a comment that the first `ID` attribute in a well-formed
    SP request is the root `AuthnRequest/@ID` (IN-03 first-match assumption documented). Verify the real
    relyra binding ID still matches via the flow test (Pitfall 4 — IDs like `_<hex>` must survive).

    Add controller rows to fake_idp_controller_test.exs: WR-04 oversized (inline fixture: deflate a large
    repetitive payload at window -15, base64 padding:false, GET /fake_idp/login, assert 200 + no
    in_response_to hidden field / nil), WR-04 garbled (GET with junk SAMLRequest → 200, nil), IN-03
    malformed ID (a deflated AuthnRequest whose ID contains `<` or a space → extracted in_response_to nil).
  </action>
  <verify>
    <automated>mix --cd demo/ledger_loop test test/ledger_loop_web/controllers/fake_idp_controller_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION row WR-04 (oversized): `mix --cd demo/ledger_loop test test/ledger_loop_web/controllers/fake_idp_controller_test.exs` passes a row where an >64 KiB-inflating SAMLRequest yields in_response_to nil and HTTP 200, no hang.
    - VALIDATION row WR-04 (garbled): same command passes a row where garbled bytes yield nil (contract preserved).
    - VALIDATION row IN-03: same command passes a row where a non-NCName ID is rejected (extractor returns nil).
    - VALIDATION row IN-03 (success preserved): `mix --cd demo/ledger_loop test test/ledger_loop_web/fake_idp_flow_test.exs` SUCCESS round-trip still has a non-nil in_response_to (real binding ID survives the NCName regex).
    - Source assertion: `grep -c "safeInflate" demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex` returns ≥1; `grep -c "max_inflated_bytes" demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex` returns ≥1.
    - Source assertion: `grep -c "A-Za-z_" demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex` returns ≥1 (NCName regex present); single-shot `:zlib.inflate/2` no longer present.
    - Source assertion: `inflate/1` still returns `:error` (not a raise) on overflow (`grep -v '^#' demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex | grep -c ":error"` ≥1).
  </acceptance_criteria>
  <done>Controller test green with WR-04 (oversized + garbled) and IN-03 rows; the flow-test SUCCESS path keeps a non-nil in_response_to; inflate fails closed to nil on overflow; no raise introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-03 catch-all idp_action + WR-02 corrected login label</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex, demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs</files>
  <read_first>
    - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex (the file being modified — sso/2 case at lines 65-98)
    - demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex (the file being modified — valid-login label at line 34)
    - demo/ledger_loop/test/ledger_loop_web/controllers/fake_idp_controller_test.exs (existing rows; new WR-03 + WR-02 rows extend this)
    - .planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-RESEARCH.md (Code Examples WR-03; Runtime State Inventory — only sarah@/chen@ are seeded; conn_fields/0 hardcodes sarah@)
  </read_first>
  <behavior>
    - WR-03 unknown action: POST /fake_idp/sso with `idp_action=foo` → success path, HTTP 200 with a
      SAMLResponse rendered (no CaseClauseError/500).
    - WR-03 failure still tampers: `idp_action=failure` still produces the tampered Response (existing
      behaviour preserved).
    - WR-02 label: GET /fake_idp/login renders `sarah@northstar.example.com` and never
      `evaluator@example.com`.
  </behavior>
  <action>
    In `sso/2` (fake_idp_controller.ex), restructure the `case action do` so the `"failure"` clause keeps
    building the valid Response then `Signer.tamper/1`, and a catch-all `_ ->` clause builds the success
    `Signer.signed_response(...)`. Treat any non-"failure" value (unknown string, array, map) as success —
    fail safe, no CaseClauseError (WR-03). The `action = params["idp_action"] || "success"` default-on-
    absence line can stay; the catch-all is what guards crafted values.

    In `login.html.heex` line 34, change the valid-login radio label text from
    `Valid Login (evaluator@example.com)` to `Valid Login (sarah@northstar.example.com)` — matching the
    emitted subject (conn_fields/0 hardcodes `name_id: "sarah@northstar.example.com"`; `evaluator@…` is
    NOT a seeded SAMLIdentity). Do NOT invent a new seeded subject; the single source of truth stays
    conn_fields/0 (WR-02).

    Add controller rows to fake_idp_controller_test.exs: WR-03 unknown action (`post conn,
    ~p"/fake_idp/sso", %{"idp_action" => "foo", ...}` → assert 200 and the rendered body contains a
    SAMLResponse field), WR-02 label (GET /fake_idp/login → `assert response =~ "sarah@northstar.example.com"`
    and `refute response =~ "evaluator@example.com"`).
  </action>
  <verify>
    <automated>mix --cd demo/ledger_loop test test/ledger_loop_web/controllers/fake_idp_controller_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION row WR-03: `mix --cd demo/ledger_loop test test/ledger_loop_web/controllers/fake_idp_controller_test.exs` passes a row where POST with `idp_action=foo` returns HTTP 200 with a SAMLResponse (no CaseClauseError/500).
    - Behavior assertion: `idp_action=failure` still tampers (existing failure-path coverage stays green).
    - VALIDATION row WR-02: same command passes a row asserting GET /fake_idp/login body =~ "sarah@northstar.example.com" and refute =~ "evaluator@example.com".
    - Source assertion: `grep -c "evaluator@example.com" demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex` returns 0; `grep -c "sarah@northstar.example.com" demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex` returns ≥1.
    - Source assertion: the sso/2 case has a catch-all (`grep -v '^#' demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex | grep -c "_ ->"` ≥1) and no longer a bare `"success" ->` clause that omits the catch-all.
  </acceptance_criteria>
  <done>Controller test green with WR-03 and WR-02 rows; unknown idp_action resolves to success (200, no 500); the label reads sarah@northstar.example.com and never evaluator@example.com.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /fake_idp/login | Deflated base64 `SAMLRequest` (untrusted) is decoded, decompressed, and regex-scanned for an ID — the decompression + extraction boundary |
| browser → POST /fake_idp/sso | `idp_action` (untrusted; may be a crafted string/array/map from multipart) dispatches the Response build |
| FakeIdP UI → evaluator | The login label is a trust-fidelity claim about the emitted subject |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57.1-04 | Denial of Service | fake_idp_controller.ex `inflate/1` | mitigate | WR-04 — bounded `:zlib.safeInflate/2` loop with a 64 KiB output ceiling, fail closed to `:error` → nil; bounds the decompression-bomb amplification ratio on an unauthenticated GET |
| T-57.1-05 | Denial of Service | fake_idp_controller.ex `sso/2` | mitigate | WR-03 — catch-all `case` clause maps any crafted/unknown `idp_action` to the success path; no `CaseClauseError`/500 on an unauthenticated POST |
| T-57.1-06 | Tampering | fake_idp_controller.ex `extract_in_response_to/1` | mitigate | IN-03/WR-01 — tighten the ID capture to the xsd:ID/NCName grammar so non-name chars are rejected before reaching the signer template (defense in depth with the response_xml/3 escaping in Plan 01) |
| T-57.1-07 | (Trust fidelity) | login.html.heex label | mitigate | WR-02 — relabel to the actual emitted subject; the harness no longer advertises an unseeded subject |

**Invariant check:** None of these touch the six CLAUDE.md non-negotiable invariants. The verifier
(`lib/relyra/**`) is untouched. WR-04 preserves the existing garbled→nil fail-closed contract (no new
error mode). IN-03 must keep the real relyra AuthnRequest ID accepted (success round-trip intact). No
`Relyra.TestSupport.*` reference is introduced.
</threat_model>

<verification>
- `mix --cd demo/ledger_loop test test/ledger_loop_web/controllers/fake_idp_controller_test.exs` green
  (WR-04, IN-03, WR-03, WR-02 rows).
- `mix --cd demo/ledger_loop test test/ledger_loop_web/fake_idp_flow_test.exs` green (SUCCESS round-trip
  keeps non-nil in_response_to; failure path still :digest_mismatch).
- Wave gate: `mix ci.demo_app` green; `mix qa` exits 0 with no unstaged format changes; `mix ci.security` green.
</verification>

<success_criteria>
- Unknown `idp_action` → success path, HTTP 200, no 500 (WR-03).
- Oversized-inflating SAMLRequest → nil, no crash/hang; garbled → nil (WR-04, contract preserved).
- Non-NCName ID rejected; real relyra ID still accepted (IN-03/WR-01).
- Label renders `sarah@northstar.example.com`, never `evaluator@example.com` (WR-02).
- No file under `lib/relyra/` modified; no `Relyra.TestSupport.*` reference added.
</success_criteria>

<output>
Create `.planning/phases/57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin/57.1-02-SUMMARY.md` when done.
</output>
