---
phase: 57.1-address-phase-57-tech-debt-tamper-guard-label-input-hardenin
verified: 2026-06-14T06:20:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
re_verification: # No previous VERIFICATION.md — initial verification
---

# Phase 57.1: Address Phase 57 Tech Debt (Tamper Guard, Label, Input Hardening) Verification Report

**Phase Goal:** Close the correctness, robustness, and hygiene debt the Phase 57 code review surfaced on the demo FakeIdP browser-login proof — confined to demo/ledger_loop, no Relyra core/API/security-posture change. Harden the tamper guard so it can never silently fail, correct the lying login label, escape/bound/catch all untrusted input on the unauthenticated FakeIdP endpoints, and clean up dangling repo state.
**Verified:** 2026-06-14T06:20:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth (Success Criterion) | Status | Evidence |
|---|---------------------------|--------|----------|
| 1 | `tamper/1` raises (not silently no-ops) when it cannot locate `<NameID>`; today's output still tampers; drifted template raises | ✓ VERIFIED | `signer.ex:107-112` — broadened regex `<NameID[^>]*>([^<]+)</NameID>` + `if tampered == xml, do: raise "tamper/1 failed to locate <NameID>; template drifted"`. Test rows: "WR-05 tampering changes bytes (guard does not fire)" (still-tampers) + raise-on-no-op assert. signer_test.exs runs 0 failures. |
| 2 | Valid-login label matches emitted name_id (sarah@northstar.example.com); no label advertises an unseeded subject | ✓ VERIFIED | `login.html.heex:34` reads `Valid Login (sarah@northstar.example.com)`. grep `evaluator@example.com` = 0, `sarah@northstar.example.com` = 1. Matches `conn_fields/0` hardcoded `name_id`. Test WR-02 asserts label present + refutes evaluator. |
| 3 | `response_xml/3` XML-escapes interpolated values; InResponseTo extractor rejects non-ID-grammar input; crafted SAMLRequest no longer produces malformed emitted XML | ✓ VERIFIED | `signer.ex:156-194` — attrs via `AttributeEscape.escape_attribute/1` (7 sites), element text via `xml_text/1` (&,<,> escaping). `fake_idp_controller.ex:132` NCName regex `[A-Za-z_][A-Za-z0-9._-]*`. Test "WR-01 metacharacter in_response_to a<b>&c returns binary without crashing" + IN-03 malformed-ID-yields-nil rows pass. |
| 4 | Unknown idp_action values resolve to success path (no CaseClauseError/500) | ✓ VERIFIED | `fake_idp_controller.ex:82-106` — `case action do "failure" -> tamper ; _ -> success end`. Test "WR-03 unknown idp_action=foo resolves to success path (HTTP 200, SAMLResponse rendered)" passes; failure-still-tampers row preserved. |
| 5 | `inflate/1` fails closed to nil on oversized decompressed output (no unbounded zip-bomb amplification) | ✓ VERIFIED | `fake_idp_controller.ex:139-182` — `:zlib.safeInflate/2` loop, `@max_inflated_bytes 64*1024`, returns `:error` on overflow (both `{:continue,_}` and `{:finished,_}`), mapped to nil by `with/else`. Single-shot `:zlib.inflate(` grep = 0. Test WR-04 oversized (65 KiB payload) + garbled both yield nil, HTTP 200, no hang. |
| 6 | `keypair.ex` raises a descriptive error on an unexpected PEM shape | ✓ VERIFIED | `keypair.ex:59-70` — `case :public_key.pem_decode/1` matching `[{:RSAPrivateKey,_,:not_encrypted}=entry]`, catch-all raises `"expected a single unencrypted RSA private key in #{@key_path}, got: ..."`. Bare `[entry] =` MatchError gone (grep=0). Test IN-02 multi-entry PEM `assert_raise RuntimeError, ~r/expected a single unencrypted/` passes. |
| 7 | `test/support/poll.ex` committed (or removed with rationale); stale wip/demo-fake-idp branch deleted | ✓ VERIFIED | `git ls-files test/support/poll.ex` returns path (tracked). `git branch --list wip/demo-fake-idp` empty (deleted). `phase/57-demo-fake-idp` intact (rev-parse succeeds). poll.ex has real consumer: `poll_test.exs` (3 tests, green) exercising success-immediate, success-after-retry, exhausted paths. |
| 8 | `mix qa` and `mix ci.demo_app` stay green | ✓ VERIFIED | `mix qa` → 744 tests, 0 failures (10 excluded), format check exit 0. `mix ci.demo_app` → 68 tests, 0 failures. No unstaged changes; demo format check exit 0. Ran independently by verifier, not trusting SUMMARY. |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `demo/ledger_loop/lib/ledger_loop/fake_idp/signer.ex` | Raise-on-no-op tamper (WR-05); escaped response_xml/3 (WR-01) | ✓ VERIFIED | `if tampered == xml` raise present; AttributeEscape used 7×; xml_text/1 helper present; wired (signed_response/1 calls response_xml/3 ×3) |
| `demo/ledger_loop/lib/ledger_loop/fake_idp/keypair.ex` | Descriptive PEM-decode error (IN-02) | ✓ VERIFIED | `decode_pem_key/1` public delegate with typed case + descriptive raise; load_private_key/0 delegates; correct demo key still decodes (signer/flow tests green) |
| `demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_controller.ex` | Catch-all idp_action (WR-03); bounded safeInflate (WR-04); NCName extractor (IN-03) | ✓ VERIFIED | safeInflate ×4 refs; @max_inflated_bytes; `_ ->` catch-all; NCName regex; wired in login/2 + sso/2 |
| `demo/ledger_loop/lib/ledger_loop_web/controllers/fake_idp_html/login.html.heex` | Corrected valid-login label (WR-02) | ✓ VERIFIED | Line 34 renders sarah@; evaluator@ absent |
| `test/support/poll.ex` | Relyra.TestSupport.Poll.retry_until!/4 — committed | ✓ VERIFIED | git-tracked; def retry_until! present; consumed by poll_test.exs (not a dead module) |
| `test/relyra/test_support/poll_test.exs` | Unit coverage (success + exhausted) | ✓ VERIFIED | 3 tests green; Poll.retry_until! referenced; assert_raise on exhausted path |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| signer.ex response_xml/3 | AttributeEscape.escape_attribute/1 | attribute escaping | ✓ WIRED | 7 call sites inside response_xml/3 |
| signer.ex tamper/1 | raise on tampered == xml | no-op detection | ✓ WIRED | `if tampered == xml` (1 hit) |
| fake_idp_controller sso/2 | success catch-all | `_ -> ...` | ✓ WIRED | catch-all builds signed_response on any non-failure value |
| fake_idp_controller inflate/1 | :zlib.safeInflate/2 ceiling | bounded loop | ✓ WIRED | inflate_loop/4 over safeInflate; 64 KiB ceiling |
| fake_idp_controller extract_in_response_to/1 | NCName grammar | tightened regex | ✓ WIRED | `[A-Za-z_]...` regex in with-chain |
| poll_test.exs | Poll.retry_until!/4 | direct unit test | ✓ WIRED | 3 test invocations |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Signer/keypair/controller/flow suites | `mix --cd demo/ledger_loop test <4 files> --warnings-as-errors` | 31 tests, 0 failures | ✓ PASS |
| Poll unit test | `mix test test/relyra/test_support/poll_test.exs --warnings-as-errors` | 3 tests, 0 failures | ✓ PASS |
| Demo app CI gate | `mix ci.demo_app` | 68 tests, 0 failures | ✓ PASS |
| Root QA gate | `mix qa` | 744 tests, 0 failures (10 excl) | ✓ PASS |
| Format (root + demo) | `mix format --check-formatted` | exit 0 / exit 0 | ✓ PASS |
| Flow SUCCESS round-trip in_response_to non-nil (IN-03 success-preserved) | flow_test.exs:70 `byte_size(in_response_to) > 0` | asserted, green | ✓ PASS |
| Flow TAMPERED → :digest_mismatch (WR-05 proof intact) | flow_test.exs:186 error_code == "digest_mismatch" | asserted, green | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| WR-05 | 57.1-01 | Tamper raise-on-no-op | ✓ SATISFIED | signer.ex:110-112 + tests |
| WR-01 | 57.1-01/02 | Escape interpolated XML / NCName extractor | ✓ SATISFIED | signer.ex escaping + controller NCName regex |
| IN-02 | 57.1-01 | Descriptive PEM error | ✓ SATISFIED | keypair.ex:67 raise |
| WR-04 | 57.1-02 | Bounded inflate (zip-bomb) | ✓ SATISFIED | controller safeInflate loop |
| WR-03 | 57.1-02 | Catch-all idp_action | ✓ SATISFIED | controller sso/2 `_ ->` |
| WR-02 | 57.1-02 | Corrected login label | ✓ SATISFIED | login.html.heex:34 |
| IN-03 | 57.1-02 | NCName ID grammar | ✓ SATISFIED | controller:132 regex + comment |
| Hygiene | 57.1-03 | poll.ex committed + branch pruned | ✓ SATISFIED | git ls-files / branch deleted |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | No TBD/FIXME/XXX/TODO/HACK debt markers in modified files | — | "placeholder" matches are legitimate prose describing empty-digest XML build steps, not stub markers |

### Human Verification Required

None. All success criteria are observable in code and exercised by green test rows (TDD adversarial coverage). No visual/real-time/external-service behavior requires human confirmation — the FakeIdP is a demo harness whose every behavior is asserted by ExUnit controller/flow round-trips.

### Gaps Summary

No gaps. All 8 ROADMAP success criteria are implemented in actual code, wired, and behaviorally verified by independently-run test suites (not trusting SUMMARY claims). The Relyra core/verifier (`lib/relyra/**`) is untouched — `git diff main...HEAD -- lib/relyra/` is empty, satisfying the "no core/security-posture change" constraint.

**Code-review (57.1-REVIEW.md) warnings assessed — none falsify a success criterion:**
- Review WR-01 (extractor matches first `ID=` anywhere, not anchored to root): the NCName grammar IS enforced and the value is escaped downstream; the success criterion "rejects non-ID-grammar input" is met. The root-vs-first-match nuance is a demo-scope robustness concern, not a goal miss.
- Review WR-02 (tamper replacement drops a hypothetical `Format` attr; dead capture group): today's live template emits a bare `<NameID>`, so tampering still produces well-formed XML and the digest still mismatches — verified by the green TAMPERED round-trip (:digest_mismatch). Latent maintainability concern only.
- Review WR-03 (`InResponseTo=""` on nil direct visit): not covered by any success criterion; the escaping criterion (nil → escaped `""`) is met. Diagnostic nuance.
- Review INFO items (unreachable xml_text(nil), hardcoded ACS path, dead `compressed` fixture binding, persistent_term cache): cosmetic, demo-scope; none blocks the goal.

These review warnings are appropriate follow-up polish for the demo harness but do not block phase completion — the phase goal (close the surfaced correctness/robustness/hygiene debt) is achieved.

---

_Verified: 2026-06-14T06:20:00Z_
_Verifier: Claude (gsd-verifier)_
