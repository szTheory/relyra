---
phase: 53
plan: 02
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - demo/ledger_loop/test/ledger_loop_web/live/setup_live_test.exs
  - demo/ledger_loop/lib/ledger_loop_web/router.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex
  - demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/setup.html.heex
  - demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex
  - demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - UX-01
must_haves:
  truths:
    - User can navigate a multi-step LiveView checklist for setup
    - User receives a redacted setup receipt that uses actual connection data instead of raw protocol artifacts
  artifacts:
    - path: demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex
      provides: "Setup checklist state management"
      contains: "defmodule LedgerLoopWeb.SetupLive"
    - path: demo/ledger_loop/test/ledger_loop_web/live/setup_live_test.exs
      provides: "Test coverage for setup wizard flow"
  key_links:
    - from: demo/ledger_loop/lib/ledger_loop_web/router.ex
      to: "LedgerLoopWeb.SetupLive"
      via: "live route"
      pattern: "live.*/setup/sso"
---

<objective>
Replace static setup placeholder with a stateful Phoenix LiveView checklist handling SSO setup.

Purpose: Provide a clear operator UX for configuration, intake, mapping, and testing, concluding with a safe redacted enablement receipt generated from real Ecto records.
Output: Fully functioning non-linear LiveView setup checklist.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-setup-and-operator-ux/53-RESEARCH.md
@.planning/phases/53-setup-and-operator-ux/53-VALIDATION.md
@.planning/phases/53-setup-and-operator-ux/53-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write SetupLive tests</name>
  <files>demo/ledger_loop/test/ledger_loop_web/live/setup_live_test.exs</files>
  <behavior>
    - Test 1: Setup checklist renders and user can navigate between steps via phx-click (Scenario 1)
    - Test 2: The receipt step securely renders redacted verification fields queried directly from an actual Relyra.Ecto.Connection record, asserting that no dummy data, fixture placeholders, or raw XML/PEM is displayed (Scenario 2)
  </behavior>
  <action>Create the LiveView test module to ensure `SetupLive` initializes cleanly, correctly manages active step state, and securely renders receipts without leaking underlying secrets. The tests MUST insert a real connection using `LedgerLoop.Demo.Fixtures` into the database and assert the LiveView fetches and renders its real redacted fields (e.g., status, connection ID) rather than hardcoded dummy values.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop_web/live/setup_live_test.exs</automated>
  </verify>
  <done>Failing test written covering LiveView checklist transitions and real-data receipt rendering</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup Placeholder Setup Affordance</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/router.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex, demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_html/setup.html.heex</files>
  <action>Remove `setup` from `RouteAffordanceController`. Delete `setup.html.heex`. In `router.ex`, replace the old `get "/setup/sso"` with `live "/setup/sso", LedgerLoopWeb.SetupLive, :index`.</action>
  <verify>
    <automated>grep -v "def setup" demo/ledger_loop/lib/ledger_loop_web/controllers/route_affordance_controller.ex | grep -c "defmodule"</automated>
  </verify>
  <done>Static setup route and dead templates removed</done>
</task>

<task type="auto">
  <name>Task 3: Implement SetupLive Checklist</name>
  <files>demo/ledger_loop/lib/ledger_loop_web/live/setup_live.ex, demo/ledger_loop/lib/ledger_loop_web/live/setup_live.html.heex</files>
  <action>Implement `LedgerLoopWeb.SetupLive` with steps (SP Settings, IdP Metadata, Mapping Preview, Test & Enable). Adhere to `53-UI-SPEC.md` formatting. Ensure state transitions work via `phx-click`. For the enablement receipt on the final step, you MUST query actual connection state from `Relyra.Ecto.Connection` to display the results of verification. DO NOT use dummy or fixture data for the display text—it must be dynamically loaded from the database record. Ensure no raw XML or PEM content is exposed, complying fully with D-04.</action>
  <verify>
    <automated>cd demo/ledger_loop && mix test test/ledger_loop_web/live/setup_live_test.exs</automated>
  </verify>
  <done>LiveView renders the checklist, supports step navigation, handles receipt generation safely using actual DB records, and passes tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Setup UI → Host App | Protocol artifact exposure from SAML processes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02 | Information Disclosure | SetupLive | mitigate | Strictly render redacted struct details from the DB rather than Raw XML/PEM or underlying library primitives in the receipt view. |
</threat_model>

<verification>
mix test passes and checklist LiveView operates safely without cryptographic exposure.
</verification>

<success_criteria>
Customer can configure their SSO setup via nonlinear checklist with clear, redacted enablement receipts sourced from actual connection data.
</success_criteria>

<output>
Create .planning/phases/53-setup-and-operator-ux/53-02-SUMMARY.md when done
</output>