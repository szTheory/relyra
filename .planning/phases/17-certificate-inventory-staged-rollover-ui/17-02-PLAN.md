---
phase: 17-certificate-inventory-staged-rollover-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - lib/relyra/live_admin/components/connection_detail.ex
  - lib/relyra/live_admin/connections_live.ex
autonomous: true
requirements:
  - CERT-02
must_haves:
  truths:
    - Operator can promote the next signing certificate via a 3-step staged rollover.
    - Operator must type the first 6 characters of the fingerprint to confirm promotion.
    - Retirement uses a standard confirmation.
  artifacts:
    - path: lib/relyra/live_admin/components/connection_detail.ex
      provides: Typed verification modal using native HTML dialog and daisyUI
    - path: lib/relyra/live_admin/connections_live.ex
      provides: Event handler for modal confirmation submission
  key_links:
    - from: lib/relyra/live_admin/components/connection_detail.ex
      to: lib/relyra/live_admin/connections_live.ex
      via: phx-submit for verified promotion
---

<objective>
Implement a 3-Step Staged Rollover with Typed Verification for promote/retire actions.

Purpose: Prevent operators from inadvertently breaking trust and forcing acknowledgment of the distributed nature of the trust change via typed validation.
Output: HTML dialog modal confirming typed certificate promotion and its related backend event wiring.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-certificate-inventory-staged-rollover-ui/17-RESEARCH.md
@.planning/phases/17-certificate-inventory-staged-rollover-ui/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Typed Verification Modal</name>
  <files>lib/relyra/live_admin/components/connection_detail.ex</files>
  <action>Introduce a native HTML `<dialog>` with daisyUI for the staged rollover confirmation. The "Promote next" button should now open this modal instead of submitting directly. The modal must require the operator to type the first 6 characters of the fingerprint into an input. Only enable the Confirm button when the input matches. For the "Retire" action, implement a standard confirmation dialog (e.g., using `data-confirm` or a simpler modal) that does not require typing the fingerprint.</action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Modal appears on click and enforces fingerprint typing before promotion</done>
</task>

<task type="auto">
  <name>Task 2: Wire Modal Event Verification</name>
  <files>lib/relyra/live_admin/connections_live.ex</files>
  <action>Add a `phx-submit` handler for the modal form (`confirm_activate_certificate`). The handler should verify the input matches the target certificate fingerprint's first 6 characters, then perform the activation mutation. The old `activate_certificate` handler can be replaced or updated to just open the modal state. Crucially, preserve the `Ecto.StaleEntryError` rescue logic (optimistic locking conflict handling) from the original activation handler inside the new `confirm_activate_certificate` handler.</action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Promotion only occurs when the handler successfully validates the typed input</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → Database | Admin trust actions directly modify SAML signature validation behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Elevation of Privilege | connection_detail.ex | mitigate | Require explicit 6-character typed fingerprint confirmation before allowing certificate promotion |
</threat_model>

<verification>
mix test test/phoenix/live_admin_test.exs
</verification>

<success_criteria>
- Typed verification modal presents for activate_certificate
- Promotion is only performed when the first 6 chars of fingerprint match
</success_criteria>

<output>
After completion, create `.planning/phases/17-certificate-inventory-staged-rollover-ui/17-02-SUMMARY.md`
</output>