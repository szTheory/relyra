---
phase: 17-certificate-inventory-staged-rollover-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/live_admin/components/connection_detail.ex
  - lib/relyra/live_admin/connections_live.ex
autonomous: true
requirements:
  - CERT-01
must_haves:
  truths:
    - Operator can view a connection's certificate inventory with active, next, and retired entries distinguished clearly.
    - Operator can see expiry facts and rollover context for each certificate without opening raw PEM material.
    - Operator receives typed conflict-safe feedback when a rollover action loses an optimistic-lock race.
  artifacts:
    - path: lib/relyra/live_admin/components/connection_detail.ex
      provides: Semantic slot-based timeline for certificates
    - path: lib/relyra/live_admin/connections_live.ex
      provides: StaleEntryError rescue logic for certificate actions
  key_links:
    - from: lib/relyra/live_admin/components/connection_detail.ex
      to: lib/relyra/live_admin/connections_live.ex
      via: phx-click events for promote/retire
---

<objective>
Build the Semantic Slot-Based Timeline UI for certificates and handle optimistic locking conflicts gracefully.

Purpose: Operators need a clear visual distinction between active, next, and retired certificates, along with expiration context and conflict-safe trust state mutations.
Output: Timeline UI rendering in the live admin panel and conflict resilience for certificate modifications.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-certificate-inventory-staged-rollover-ui/17-RESEARCH.md
@.planning/phases/17-certificate-inventory-staged-rollover-ui/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Semantic Slot-Based Timeline UI</name>
  <files>lib/relyra/live_admin/components/connection_detail.ex</files>
  <action>Replace generic certificate list (`<section style="border: 1px solid #ddd; padding: 16px;">` block) with fixed-layout semantic slots: Next (Staged), Active, and Retired. Use tabular numerals for fingerprints and dates. Apply "Warning" amber color (`#B45309`) and a clock icon to certificates expiring in less than 30 days. Display dashed placeholder for Next if empty. Explicitly preserve the existing `rollback_certificate` button functionality when moving items into these semantic slots.</action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>UI correctly renders semantic slots and amber warnings for expiring certificates</done>
</task>

<task type="auto">
  <name>Task 2: Handle Optimistic Locking Conflicts</name>
  <files>lib/relyra/live_admin/connections_live.ex</files>
  <action>Update event handlers (`activate_certificate`, `retire_certificate`) to rescue `Ecto.StaleEntryError`. When caught, return `{:noreply, socket |> put_flash(:error, "The connection was modified by another operator. Please review the updated trust state.") |> assign(:connection, reloaded_connection)}`. Ensure the true state is reloaded immediately upon conflict.</action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Conflict correctly flashes error and state is reloaded without crashing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → Database | Admin trust actions directly modify SAML signature validation behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | connections_live.ex | mitigate | Enforce optimistic locking (Ecto.StaleEntryError) to prevent concurrent state overwrite |
</threat_model>

<verification>
mix test test/phoenix/live_admin_test.exs
</verification>

<success_criteria>
- Timeline UI slots display Active, Next, and Retired properly
- Missing Next slot prompts replacement
- OptimisticLocking conflicts gracefully handle the state via StaleEntryError rescue without crashing LiveView process
</success_criteria>

<output>
After completion, create `.planning/phases/17-certificate-inventory-staged-rollover-ui/17-01-SUMMARY.md`
</output>