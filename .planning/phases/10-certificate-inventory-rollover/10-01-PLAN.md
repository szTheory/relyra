---
phase: 10-certificate-inventory-rollover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/metadata/import.ex
  - lib/relyra/ecto/certificate_inventory.ex
  - lib/relyra/ecto/certificate.ex
  - test/relyra/ecto/certificate_inventory_expiry_test.exs
autonomous: true
requirements: [CFG-04]
must_haves:
  truths:
    - "Imported and staged connection certificates persist real X.509 validity facts before operators use them for rollover decisions."
    - "Expiry tracking reflects certificate facts on inventory rows instead of inferred operator timelines or metadata-revision-only history."
    - "Metadata-driven staging keeps new signing certificates non-active until an explicit promotion changes the trust window."
  artifacts:
    - path: "lib/relyra/metadata/import.ex"
      provides: "Metadata candidate shaping that carries certificate validity facts derived from PEM inputs"
    - path: "lib/relyra/ecto/certificate_inventory.ex"
      provides: "Staging path that persists `not_before` and `not_after` on durable certificate rows"
    - path: "test/relyra/ecto/certificate_inventory_expiry_test.exs"
      provides: "Wave 0 regression coverage for validity extraction and persistence"
  key_links:
    - from: "lib/relyra/metadata/import.ex"
      to: "lib/relyra/ecto/certificate_inventory.ex"
      via: "candidate certificate attrs passed into staged inventory writes"
      pattern: "certificate_(pems|fingerprints)|not_before|not_after"
    - from: "lib/relyra/ecto/certificate_inventory.ex"
      to: "lib/relyra/ecto/certificate.ex"
      via: "Certificate.changeset persists validity timestamps on staged rows"
      pattern: "not_before|not_after"
---

<objective>
Persist expiry facts on connection certificate inventory rows so staged rollover decisions are based on real X.509 validity windows, not empty columns or inferred metadata history.

Purpose: close the remaining `CFG-04` expiry-tracking gap while preserving the already-shipped staged `:next` lifecycle model.
Output: validity-aware metadata candidate shaping, staged inventory persistence, and Wave 0 expiry regression tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-certificate-inventory-rollover/10-CONTEXT.md
@.planning/phases/10-certificate-inventory-rollover/10-RESEARCH.md
@.planning/phases/10-certificate-inventory-rollover/10-PATTERNS.md
@.planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md
@.planning/phases/09-metadata-import-export-refresh/09-CONTEXT.md
@lib/relyra/metadata/import.ex
@lib/relyra/ecto/certificate_inventory.ex
@lib/relyra/ecto/certificate.ex

<interfaces>
From lib/relyra/metadata/import.ex:
```elixir
@spec import_xml(binary(), binary(), keyword()) :: {:ok, struct()} | {:error, Error.t()}
@spec build_candidate(map()) :: Candidate.t()
```

From lib/relyra/ecto/certificate_inventory.ex:
```elixir
@spec stage_metadata_certificates(module(), struct(), struct(), map()) ::
        :ok | {:error, Error.t()}
```

From lib/relyra/ecto/certificate.ex:
```elixir
schema "relyra_connection_certificates" do
  field :not_before, :utc_datetime_usec
  field :not_after, :utc_datetime_usec
  field :lifecycle_state, Ecto.Enum, values: [:active, :next, :retired], default: :active
end
```
</interfaces>
</context>

<tasks>

<task type="auto" id="10-01-T01" tdd="true">
  <name>Task 1: Add Wave 0 expiry persistence regressions</name>
  <read_first>
    - /Users/jon/projects/relyra/.planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md
    - /Users/jon/projects/relyra/.planning/phases/10-certificate-inventory-rollover/10-RESEARCH.md
    - /Users/jon/projects/relyra/lib/relyra/metadata/import.ex
    - /Users/jon/projects/relyra/lib/relyra/ecto/certificate_inventory.ex
    - /Users/jon/projects/relyra/lib/relyra/ecto/certificate.ex
  </read_first>
  <files>test/relyra/ecto/certificate_inventory_expiry_test.exs</files>
  <behavior>
    - Test 1: metadata import or direct staging persists `not_before` and `not_after` for every staged signing certificate per D-01 and D-05.
    - Test 2: persisted validity timestamps match the PEM’s X.509 facts rather than `staged_at`, metadata revision timestamps, or any inferred operator window.
    - Test 3: malformed or non-decodable PEM input fails with a typed error and leaves no partial staged certificate rows behind.
  </behavior>
  <action>Create `test/relyra/ecto/certificate_inventory_expiry_test.exs` as the Phase 10 Wave 0 contract from `10-VALIDATION.md`. Exercise the real metadata/import-to-inventory path rather than a fake helper so the regression covers the end-to-end seam that D-01, D-03, and D-05 care about: metadata revisions stay provenance-only, staged rows carry live validity facts, and staged certificates remain `:next` until later promotion. Use fixture PEMs whose validity dates are asserted explicitly in UTC datetimes. Include a failure-path test that proves invalid PEM parsing aborts the write path cleanly.</action>
  <acceptance_criteria>
    - `test/relyra/ecto/certificate_inventory_expiry_test.exs` exists and names `not_before`, `not_after`, and staged `:next` expectations directly.
    - `rg -n "not_before|not_after|lifecycle_state: :next|typed error" test/relyra/ecto/certificate_inventory_expiry_test.exs` finds the expiry and failure assertions.
    - The test file covers both success and malformed-PEM failure paths from the same metadata/import seam.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/relyra/ecto/certificate_inventory_expiry_test.exs test/relyra/ecto/metadata_apply_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The missing Wave 0 expiry coverage exists and fails until production code starts persisting validity facts correctly.</done>
</task>

<task type="auto" id="10-01-T02" tdd="true">
  <name>Task 2: Extract and persist X.509 validity facts during staged inventory writes</name>
  <read_first>
    - /Users/jon/projects/relyra/.planning/phases/10-certificate-inventory-rollover/10-CONTEXT.md
    - /Users/jon/projects/relyra/.planning/phases/10-certificate-inventory-rollover/10-RESEARCH.md
    - /Users/jon/projects/relyra/lib/relyra/metadata/import.ex
    - /Users/jon/projects/relyra/lib/relyra/ecto/certificate_inventory.ex
    - /Users/jon/projects/relyra/lib/relyra/ecto/certificate.ex
    - /Users/jon/projects/relyra/test/relyra/ecto/certificate_inventory_expiry_test.exs
  </read_first>
  <files>lib/relyra/metadata/import.ex, lib/relyra/ecto/certificate_inventory.ex, lib/relyra/ecto/certificate.ex, test/relyra/ecto/certificate_inventory_expiry_test.exs</files>
  <behavior>
    - Test 1: successful metadata-driven staging writes `not_before` and `not_after` onto the created or updated certificate row.
    - Test 2: the persisted timestamps remain stable on later reads and can support future expiry signaling without re-parsing metadata history.
    - Test 3: candidate shaping and staging preserve `lifecycle_state: :next` for newly discovered certificates while adding validity facts.
  </behavior>
  <action>Implement expiry-fact extraction per D-01, D-02, D-03, and D-05 without re-planning the existing lifecycle enums or staged-row semantics. Extend the metadata candidate or staging attrs so each PEM carries decoded `not_before` and `not_after` values derived with OTP `:public_key` APIs, then persist those fields through `Relyra.Ecto.CertificateInventory.stage_metadata_certificates/4` onto the durable certificate rows described in D-02. Keep metadata revisions as provenance only per D-03; do not move rollover authority into revision rows. If parsing fails, return a typed `%Relyra.Error{}` and abort the stage path rather than inventing placeholder validity windows. Do not broaden runtime trust selection in this plan; D-07 through D-09 stay satisfied by the existing active-only resolver seam.</action>
  <acceptance_criteria>
    - `rg -n "not_before|not_after|public_key|pem_decode|pkix_decode_cert" lib/relyra/metadata/import.ex lib/relyra/ecto/certificate_inventory.ex` shows explicit validity extraction and persistence wiring.
    - `rg -n "field :not_before|field :not_after" lib/relyra/ecto/certificate.ex` still exposes the persisted validity columns used by the staging path.
    - `mix test test/relyra/ecto/certificate_inventory_expiry_test.exs --warnings-as-errors` passes.
    - `mix test test/relyra/ecto/metadata_apply_test.exs --warnings-as-errors` passes as a smoke check that Phase 09 metadata apply remains compatible.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/relyra/ecto/certificate_inventory_expiry_test.exs test/relyra/ecto/metadata_apply_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Phase 10 has real expiry tracking on durable certificate inventory rows, with staged metadata certificates remaining non-active until explicit promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metadata XML input -> certificate inventory | untrusted PEM material crosses into persisted trust facts |
| staged certificate rows -> future rollover decisions | empty or incorrect validity windows could mislead operators or later automation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | T | `lib/relyra/metadata/import.ex` | mitigate | Decode PEM and derive validity facts from X.509 structures directly; reject malformed inputs instead of persisting guessed timestamps. |
| T-10-01-02 | R | `lib/relyra/ecto/certificate_inventory.ex` | mitigate | Persist `not_before` and `not_after` on the durable inventory row so later rollover decisions are attributable to stored facts rather than transient parse output. |
| T-10-01-03 | I | `lib/relyra/ecto/certificate_inventory.ex` | mitigate | Keep staged certificates in `:next` state per D-05 so new metadata cannot silently widen the active runtime trust set while expiry facts are being introduced. |
</threat_model>

<verification>
- `mix test test/relyra/ecto/certificate_inventory_expiry_test.exs test/relyra/ecto/metadata_apply_test.exs --warnings-as-errors`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- Imported or staged certificates store real `not_before` and `not_after` values on certificate inventory rows.
- Invalid PEM parsing fails closed with a typed error and no partial staged writes.
- Existing metadata apply behavior still stages new certificates as non-active inventory rows.
</success_criteria>

<output>
After completion, create `.planning/phases/10-certificate-inventory-rollover/10-01-SUMMARY.md`
</output>
