---
phase: 10-certificate-inventory-rollover
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - lib/relyra/ecto/connection.ex
  - lib/relyra/ecto/certificate_inventory.ex
  - priv/repo/migrations/20260505183000_harden_relyra_certificate_lifecycle_invariants.exs
  - test/relyra/ecto/certificate_inventory_transition_test.exs
  - test/relyra/ecto/certificate_inventory_concurrency_test.exs
autonomous: true
requirements: [CFG-04]
must_haves:
  truths:
    - "Only explicit, legal certificate lifecycle transitions succeed for staged rollover operations."
    - "Concurrent promotion, retirement, or rollback attempts fail closed and preserve one deliberate active runtime trust set."
    - "Runtime hydration continues exposing only active signing certificates even while richer rollover rows exist for bookkeeping."
  artifacts:
    - path: "lib/relyra/ecto/certificate_inventory.ex"
      provides: "Bounded lifecycle transition rules plus atomic concurrency handling for activate, retire, and rollback"
    - path: "lib/relyra/ecto/connection.ex"
      provides: "Connection-level locking or optimistic versioning support used by rollover transitions"
    - path: "priv/repo/migrations/20260505183000_harden_relyra_certificate_lifecycle_invariants.exs"
      provides: "Database invariant support for lifecycle hardening"
    - path: "test/relyra/ecto/certificate_inventory_transition_test.exs"
      provides: "Wave 0 transition-matrix regression coverage"
    - path: "test/relyra/ecto/certificate_inventory_concurrency_test.exs"
      provides: "Wave 0 concurrency regression coverage"
  key_links:
    - from: "lib/relyra/ecto/certificate_inventory.ex"
      to: "lib/relyra/ecto/connection.ex"
      via: "Repo.transact/2 guarded transition flow with connection-level concurrency contract"
      pattern: "Repo\\.transact|lock_version|optimistic_lock"
    - from: "lib/relyra/ecto/certificate_inventory.ex"
      to: "test/relyra/ecto/certificate_inventory_transition_test.exs"
      via: "source->target lifecycle matrix"
      pattern: "invalid_lifecycle_transition|last_active_certificate"
    - from: "lib/relyra/ecto/certificate_inventory.ex"
      to: "test/relyra/ecto/certificate_inventory_concurrency_test.exs"
      via: "conflict handling preserves active runtime trust set"
      pattern: "stale|conflict|retry"
---

<objective>
Harden the existing rollover helpers with bounded lifecycle rules, an explicit concurrency contract, and validation coverage that proves runtime trust windows stay deliberate under conflict.

Purpose: finish `CFG-04` by turning the already-shipped activate/retire/rollback primitives into invariant-safe operations instead of loosely checked row updates.
Output: transition matrix enforcement, concurrency-aware rollover writes, migration-backed invariant support, and new Wave 0 transition/concurrency tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-certificate-inventory-rollover/10-CONTEXT.md
@.planning/phases/10-certificate-inventory-rollover/10-RESEARCH.md
@.planning/phases/10-certificate-inventory-rollover/10-PATTERNS.md
@.planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md
@lib/relyra/ecto/certificate_inventory.ex
@lib/relyra/ecto/connection.ex
@lib/relyra/ecto/connection_snapshot.ex
@test/relyra/ecto/ecto_connection_resolver_test.exs

<interfaces>
From lib/relyra/ecto/certificate_inventory.ex:
```elixir
@spec activate_signing_certificate(module(), binary(), binary(), keyword()) ::
        {:ok, Certificate.t()} | {:error, Error.t()}
@spec retire_signing_certificate(module(), binary(), binary(), keyword()) ::
        {:ok, Certificate.t()} | {:error, Error.t()}
@spec rollback_signing_certificate(module(), binary(), binary(), binary(), keyword()) ::
        {:ok, [Certificate.t()]} | {:error, Error.t()}
```

From lib/relyra/ecto/connection_snapshot.ex:
```elixir
snapshot.idp_certificates == active signing certificate PEMs only
```
</interfaces>
</context>

<tasks>

<task type="auto" id="10-03-T01" tdd="true">
  <name>Task 1: Add Wave 0 transition-matrix and concurrency regressions</name>
  <read_first>
    - /Users/jon/projects/relyra/.planning/phases/10-certificate-inventory-rollover/10-VALIDATION.md
    - /Users/jon/projects/relyra/.planning/phases/10-certificate-inventory-rollover/10-RESEARCH.md
    - /Users/jon/projects/relyra/lib/relyra/ecto/certificate_inventory.ex
    - /Users/jon/projects/relyra/lib/relyra/ecto/connection.ex
    - /Users/jon/projects/relyra/test/relyra/ecto/metadata_apply_test.exs
    - /Users/jon/projects/relyra/test/relyra/ecto/ecto_connection_resolver_test.exs
  </read_first>
  <files>test/relyra/ecto/certificate_inventory_transition_test.exs, test/relyra/ecto/certificate_inventory_concurrency_test.exs</files>
  <behavior>
    - Test 1: only the intended lifecycle edges succeed: `:next -> :active` for promotion, `:active -> :retired` for retirement, and rollback restores a retired certificate while retiring the replacement per D-06.
    - Test 2: invalid edges such as `:active -> :active`, `:retired -> :retired`, or direct retirement of a staged `:next` certificate fail with typed errors that identify `details.reason`.
    - Test 3: concurrent promotion/retirement/rollback attempts surface a deterministic conflict failure and leave resolver-facing `idp_certificates` on one explicit active trust set per D-07, D-08, and D-09.
  </behavior>
  <action>Create `test/relyra/ecto/certificate_inventory_transition_test.exs` and `test/relyra/ecto/certificate_inventory_concurrency_test.exs` as the Wave 0 contract required by `10-VALIDATION.md`. Separate state-matrix coverage from concurrency coverage so failures point to the correct gap. Reuse the current explicit activate/retire/rollback API surface rather than inventing new entrypoints. The concurrency tests should model real conflicting writes, then assert both the returned error semantics and the post-conflict runtime snapshot that `Relyra.ConnectionResolver.Ecto` or `Relyra.Ecto.ConnectionSnapshot` exposes. This task implements the missing test coverage for D-06 through D-09 without re-planning the already-shipped active-only runtime filter itself.</action>
  <acceptance_criteria>
    - `test/relyra/ecto/certificate_inventory_transition_test.exs` exists and names invalid transition reasons explicitly.
    - `test/relyra/ecto/certificate_inventory_concurrency_test.exs` exists and asserts both conflict behavior and the surviving active runtime trust set.
    - `rg -n "invalid_lifecycle_transition|last_active_certificate|conflict|retry|stale" test/relyra/ecto/certificate_inventory_transition_test.exs test/relyra/ecto/certificate_inventory_concurrency_test.exs` finds the required coverage.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/relyra/ecto/certificate_inventory_transition_test.exs test/relyra/ecto/certificate_inventory_concurrency_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The missing transition and concurrency regressions exist and fail until the rollover services enforce a bounded lifecycle and conflict contract.</done>
</task>

<task type="auto" id="10-03-T02" tdd="true">
  <name>Task 2: Enforce lifecycle invariants and serialize trust-changing transitions</name>
  <read_first>
    - /Users/jon/projects/relyra/.planning/phases/10-certificate-inventory-rollover/10-CONTEXT.md
    - /Users/jon/projects/relyra/.planning/phases/10-certificate-inventory-rollover/10-RESEARCH.md
    - /Users/jon/projects/relyra/lib/relyra/ecto/certificate_inventory.ex
    - /Users/jon/projects/relyra/lib/relyra/ecto/connection.ex
    - /Users/jon/projects/relyra/lib/relyra/ecto/connection_snapshot.ex
    - /Users/jon/projects/relyra/test/relyra/ecto/certificate_inventory_transition_test.exs
    - /Users/jon/projects/relyra/test/relyra/ecto/certificate_inventory_concurrency_test.exs
  </read_first>
  <files>lib/relyra/ecto/connection.ex, lib/relyra/ecto/certificate_inventory.ex, priv/repo/migrations/20260505183000_harden_relyra_certificate_lifecycle_invariants.exs, test/relyra/ecto/certificate_inventory_transition_test.exs, test/relyra/ecto/certificate_inventory_concurrency_test.exs</files>
  <behavior>
    - Test 1: transition helpers accept only the bounded lifecycle matrix needed for promotion, retirement, and rollback.
    - Test 2: concurrent trust-changing operations fail closed under an explicit connection-level concurrency contract.
    - Test 3: runtime hydration still emits only active signing certificates even after conflict or invalid-transition failures.
  </behavior>
  <action>Harden the existing rollover primitives rather than rebuilding them. In `Relyra.Ecto.CertificateInventory`, replace the loose transition checks with an explicit source->target matrix per D-06: promotion activates only `:next` signing certificates, retirement retires only `:active` signing certificates when another active certificate remains, and rollback restores the retired predecessor while retiring the replacement atomically. Adopt the research recommendation for concurrency by adding optimistic locking on the parent connection row and using `Repo.transact/2` to guard every trust-changing operation; update `Relyra.Ecto.Connection` and add a migration-backed invariant file to support that contract. Normalize stale/conflict failures to typed `%Relyra.Error{}` results with actionable `details.reason`, and keep D-07 through D-09 as regression requirements: the persistence-to-runtime seam must continue exposing active signing certificates only, even when transition attempts race or fail. Do not expand scope into scheduled automation, admin UX, or a new rollover table.</action>
  <acceptance_criteria>
    - `rg -n "optimistic_lock|lock_version|Repo\\.transact|invalid_lifecycle_transition" lib/relyra/ecto/connection.ex lib/relyra/ecto/certificate_inventory.ex` shows the chosen concurrency and lifecycle enforcement contract.
    - `priv/repo/migrations/20260505183000_harden_relyra_certificate_lifecycle_invariants.exs` exists and supports the lifecycle/concurrency hardening described in the code.
    - `mix test test/relyra/ecto/certificate_inventory_transition_test.exs test/relyra/ecto/certificate_inventory_concurrency_test.exs test/relyra/ecto/ecto_connection_resolver_test.exs --warnings-as-errors` passes.
    - `mix test test/relyra/connection_snapshot_test.exs --warnings-as-errors` still passes, proving the active-only runtime trust-set rule remains intact.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/relyra/ecto/certificate_inventory_transition_test.exs test/relyra/ecto/certificate_inventory_concurrency_test.exs test/relyra/ecto/ecto_connection_resolver_test.exs test/relyra/connection_snapshot_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Rollover transitions are bounded, concurrent writes fail closed, and the resolver seam continues exposing only the deliberate active trust set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rollover API call -> certificate inventory rows | trust-changing mutations can accidentally activate, retire, or restore the wrong certificate |
| concurrent rollover writes -> runtime trust set | races can produce ambiguous active trust windows if not serialized or versioned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | T | `lib/relyra/ecto/certificate_inventory.ex` | mitigate | Enforce an explicit lifecycle matrix so only intended `next->active`, `active->retired`, and rollback edges succeed. |
| T-10-03-02 | D | `lib/relyra/ecto/certificate_inventory.ex` | mitigate | Keep the last-active-certificate guard and extend it with atomic rollback/promotion semantics inside one `Repo.transact/2` boundary. |
| T-10-03-03 | E | `lib/relyra/ecto/connection.ex` | mitigate | Add connection-level optimistic locking so concurrent trust-changing writes fail closed with typed conflict results instead of silently overwriting each other. |
| T-10-03-04 | I | `lib/relyra/ecto/connection_snapshot.ex` | mitigate | Preserve the active-only runtime trust filter as a regression requirement so staged or retired rows never leak into request-time signature verification. |
</threat_model>

<verification>
- `mix test test/relyra/ecto/certificate_inventory_transition_test.exs test/relyra/ecto/certificate_inventory_concurrency_test.exs test/relyra/ecto/ecto_connection_resolver_test.exs test/relyra/connection_snapshot_test.exs --warnings-as-errors`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- Invalid lifecycle transitions fail with typed, machine-readable reasons.
- Concurrent rollover writes fail closed and preserve one explicit active runtime trust set.
- Active-only runtime certificate hydration remains intact after all lifecycle hardening.
</success_criteria>

<output>
After completion, create `.planning/phases/10-certificate-inventory-rollover/10-03-SUMMARY.md`
</output>
