---
phase: 30-adversarial-crypto-assurance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/test_support/fake_idp.ex
autonomous: true
requirements: [ASSUR-02]
nyquist_compliant: true

must_haves:
  truths:
    - "FakeIdP.sign emits a Response carrying a real ds:DigestValue + ds:SignatureValue (not the structure-only empty SignedInfo)"
    - "A FakeIdP-signed Response verifies {:ok, %SignedNode{}} against FakeIdP's own cert through the full PureBeam.parse_safely -> Signature.verify/4 path"
    - "FakeIdP exposes its trust certificate so callers can configure cert_chain"
    - "Production crypto is FROZEN: lib/relyra/security/signature.ex and lib/relyra/security/xml/pure_beam.ex are not touched (D-10)"
  artifacts:
    - path: "lib/relyra/test_support/fake_idp.ex"
      provides: "sign/2 delegating to XmldsigSigner.sign_response/1; response_xml with <CanonicalizationMethod> and no whitespace-collapse; self_signed_cert_pem/0 delegate"
      contains: "Relyra.TestSupport.XmldsigSigner.sign_response"
  key_links:
    - from: "lib/relyra/test_support/fake_idp.ex"
      to: "Relyra.TestSupport.XmldsigSigner.sign_response/1"
      via: "delegation in sign/2 (D-01 promotion seam)"
      pattern: "XmldsigSigner.sign_response"
    - from: "lib/relyra/test_support/fake_idp.ex response_xml"
      to: "<SignedInfo>"
      via: "added <CanonicalizationMethod> as first child (D-02)"
      pattern: "CanonicalizationMethod"
---

<objective>
Promote `Relyra.TestSupport.FakeIdP` to perform REAL cryptographic XMLDSig signing (ASSUR-02). Today `FakeIdP.sign` base64-encodes a structure-only Response with an empty `<SignedInfo>` (no `DigestValue`/`SignatureValue`). After this plan, `FakeIdP.sign` delegates to the existing Phase-29 genuine signer `Relyra.TestSupport.XmldsigSigner.sign_response/1`, which injects a real `DigestValue` + `SignatureValue` in place, so the test suite exercises real verification rather than structure-only acceptance.

Purpose: ASSUR-02 — the FakeIdP positive control (proven in Plan 02) can only verify `{:ok}` if FakeIdP signs for real. This is the load-bearing integration change; the corpus suite (Plan 02) drives through it.
Output: Modified `lib/relyra/test_support/fake_idp.ex` with three reconciled changes (delegation, shape, cert exposure).

This is a PROMOTION, not a rewrite. Do NOT re-implement crypto inside FakeIdP — a second signer is a divergent canonicalizer and the positive control would pass for the wrong reason (T-29-15). Reuse the existing signer verbatim (D-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-adversarial-crypto-assurance/30-CONTEXT.md
@.planning/phases/30-adversarial-crypto-assurance/30-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. Use these directly — no exploration needed. -->

The delegation target — Relyra.TestSupport.XmldsigSigner (lib/relyra/test_support/xmldsig_signer.ex):
  @spec sign_response(String.t()) :: %{response_xml: String.t(), cert_chain: [String.t()]}
    Genuinely signs an EXISTING <Response> XML in place. Requires:
      - an <Assertion ID="..."> matching a <Reference URI="#assertion_id">
      - a <SignedInfo> carrying a <Reference URI="#..."> with a <DigestMethod> and no <DigestValue> yet
    It parses the input with the verifier's parser, computes the genuine DigestValue over
    the canonicalized referenced <Assertion> (PureBeam.canonicalize), injects it into the
    Reference, re-parses, canonicalizes the <SignedInfo> (C14N.serialize), signs it with
    FakeIdP.keypair(), and injects <SignatureValue> after </SignedInfo>. RAISES if the
    structure cannot be located.
  @spec self_signed_cert_pem() :: String.t()
    The self-signed cert PEM (single-element chain) the verifier must trust, derived from
    FakeIdP.keypair() via :public_key.pkix_test_root_cert/2.

The signer's canonical <SignedInfo> shape (xmldsig_signer.ex:252-264) — FakeIdP's must align:
    <Signature xmlns="http://www.w3.org/2000/09/xmldsig#">
      <SignedInfo>
        <CanonicalizationMethod Algorithm="http://www.w3.org/2001/10/xml-exc-c14n#"/>   <-- FakeIdP MISSING THIS
        <SignatureMethod Algorithm="http://www.w3.org/2001/04/xmldsig-more#rsa-sha256"/>
        <Reference URI="#assertion_id"><DigestMethod Algorithm="..."/><DigestValue/></Reference>
      </SignedInfo>
      <SignatureValue/>
    </Signature>

Current FakeIdP shape facts (lib/relyra/test_support/fake_idp.ex):
  - sign(%Builder{} = builder, opts) at :68-73 — base64-encodes response_xml directly (structure-only)
  - response_xml/2 at :103-136 — its <SignedInfo> (:127-130) has <SignatureMethod> + <Reference><DigestMethod/></Reference>
    but NO <CanonicalizationMethod>; the whole template ends with String.replace(~r/\s+/, " ") (:134) then String.trim()
  - SAML xmlns="urn:oasis:names:tc:SAML:2.0:assertion" on <Issuer> (:112) and <Assertion> (:114) — KEEP THESE
  - keypair/0 at :77-83 — the single :persistent_term RSA-2048 key (the signer reuses it; do NOT add a second)
  - @prod_build + ensure_not_prod!/0 (:11,97-101) — preserve the prod guard

Blast radius (RESEARCH Runtime State Inventory): FakeIdP.sign/2 is consumed ONLY by
  test/test_support_demo_test.exs:31 (via Relyra.TestSupport.sign_saml_response/2,
  test_support.ex:92) which POSTs to a STUB controller (Relyra.TestSupportDemoController)
  that assigns :current_user directly and does NOT run real verification. The shape change
  does not break it. auto_refresh_test.exs:531 uses FakeIdP.keypair() directly (inline
  signer), not sign/2 — unaffected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile FakeIdP's response_xml shape for genuine signing (D-02)</name>
  <files>lib/relyra/test_support/fake_idp.ex</files>
  <read_first>
    - lib/relyra/test_support/fake_idp.ex (the file being modified — response_xml/2 at :103-136, especially the <SignedInfo> at :127-130 and the whitespace-collapse at :134)
    - lib/relyra/test_support/xmldsig_signer.ex (the analog shape — response_xml/3 at :236-264, note <CanonicalizationMethod> at :254 as first child of <SignedInfo>)
  </read_first>
  <action>
    Make two precise edits to `response_xml/2` so the emitted bytes are genuinely signable by `XmldsigSigner.sign_response/1` (D-02):

    (1) ADD `<CanonicalizationMethod Algorithm="http://www.w3.org/2001/10/xml-exc-c14n#"/>` as the FIRST child of `<SignedInfo>`, immediately before the existing `<SignatureMethod>` element — mirroring `xmldsig_signer.ex:254`. This is load-bearing (Pitfall 5): `sign_response/1` canonicalizes the SignedInfo and the verifier reads `signed_info_prefix_list/1` from it; an absent CanonicalizationMethod makes the SignedInfo malformed relative to the XMLDSig contract and the FakeIdP positive control returns `:invalid_signature`/`:digest_mismatch` despite a genuine signature.

    (2) DROP the `String.replace(~r/\s+/, " ")` whitespace-collapse at the end of the template (:134), matching the signer's whitespace-free convention (D-02). Keep `String.trim()`.

    Do NOT strip the SAML `xmlns="urn:oasis:names:tc:SAML:2.0:assertion"` declarations on `<Issuer>` (:112) and `<Assertion>` (:114) — `sign_response/1` self-parses the emitted bytes so namespaces stay consistent between signer and verifier; they make FakeIdP realistic (KEEP).

    Leave the `<Reference URI="#assertion_id"><DigestMethod .../></Reference>` exactly as-is (no `<DigestValue>` placeholder — `sign_response/1` injects it before `</Reference>`). Preserve `@prod_build` + `ensure_not_prod!/0`. Do NOT touch `lib/relyra/security/*` (D-10 scope fence).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `FakeIdP.response_xml` (via `build_response |> sign` rendering) contains exactly one `<CanonicalizationMethod Algorithm="http://www.w3.org/2001/10/xml-exc-c14n#"/>` as the first child element of `<SignedInfo>`.
    - The template no longer applies `String.replace(~r/\s+/, " ")`.
    - The SAML `xmlns="urn:oasis:names:tc:SAML:2.0:assertion"` declarations on `<Issuer>`/`<Assertion>` are still present.
    - `mix compile --warnings-as-errors` exits 0 (no unused alias/attribute — Pitfall 4).
    - No file under `lib/relyra/security/` is modified.
  </acceptance_criteria>
  <done>FakeIdP's emitted SignedInfo carries a CanonicalizationMethod, is whitespace-collapse-free, and keeps SAML namespaces; compile is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delegate sign/2 to the genuine signer + expose the trust cert (D-01/D-03)</name>
  <files>lib/relyra/test_support/fake_idp.ex</files>
  <read_first>
    - lib/relyra/test_support/fake_idp.ex (the file being modified — sign/2 at :65-75, keypair/0 at :77-83)
    - lib/relyra/test_support/xmldsig_signer.ex (the delegation target — sign_response/1 at :161-194, self_signed_cert_pem/0 at :204-211)
    - .planning/phases/29-cryptographic-xmldsig-verification/29-04-SUMMARY.md (the "promote XmldsigSigner into FakeIdP" guidance, :171)
  </read_first>
  <action>
    Promote `FakeIdP.sign/2` (D-01): in the `sign(%Builder{} = builder, opts)` clause (:68-73), after building `xml = response_xml(builder, opts)`, route the XML through `Relyra.TestSupport.XmldsigSigner.sign_response/1`, take the `:response_xml` field from its returned map, and `Base.encode64(signed_xml, padding: false)`. Preserve `ensure_not_prod!()` + `ensure_keypair!()` and the `sign(opts, extra_opts) when is_list(opts)` clause unchanged. The signer reuses `FakeIdP.keypair()` internally — do NOT generate a second key.

    Expose the trust cert (D-03): add `defdelegate self_signed_cert_pem(), to: Relyra.TestSupport.XmldsigSigner` so callers can configure `cert_chain` with FakeIdP's own cert. (The signer derives it from `FakeIdP.keypair()`.)

    Reference the existing `Relyra.TestSupport.XmldsigSigner` module fully-qualified (or add an `alias` only if it stays used — keep imports tight per Pitfall 4; `--warnings-as-errors` aborts on an unused alias). Do NOT re-implement digest/signature inside FakeIdP (D-01 — anti-pattern: second signer). Do NOT touch `lib/relyra/security/*`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/test_support_demo_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `FakeIdP.sign(%Builder{}, opts)` delegates to `Relyra.TestSupport.XmldsigSigner.sign_response/1` and base64-encodes the signer's `:response_xml` output (no padding).
    - `FakeIdP.self_signed_cert_pem/0` exists and returns the same PEM as `XmldsigSigner.self_signed_cert_pem/0`.
    - There is NO `:public_key.generate_key` and NO bespoke digest/signature computation inside `fake_idp.ex` (grep -v '^#' lib/relyra/test_support/fake_idp.ex | grep -c 'public_key.sign\|crypto.hash' returns 0).
    - `mix test test/test_support_demo_test.exs --warnings-as-errors` passes (the stub-controller demo test, blast-radius regression, stays green).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>FakeIdP.sign produces a genuinely-signed (real DigestValue + SignatureValue) base64 Response via the promoted signer, exposes self_signed_cert_pem/0, and the demo test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FakeIdP emitted bytes -> verifier | The signed Response bytes must canonicalize identically for signer and verifier; a divergence is the assurance-regression risk |
| test-support -> production compilation | FakeIdP/XmldsigSigner must never compile/run in :prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | FakeIdP canonicalizer differential (the T-29-15 false-positive) — a positive control that passes for the wrong reason because signing used a different C14N than verification | mitigate | D-01 single-signing-path: FakeIdP.sign delegates to XmldsigSigner.sign_response/1, which self-parses the emitted bytes and binds the verifier's own C14N nodes. NO second signer inside FakeIdP. Proven by Plan 02's positive control asserting {:ok, %SignedNode{}}. |
| T-30-02 | Spoofing | Missing <CanonicalizationMethod> makes the SignedInfo malformed; positive control silently fails despite a genuine signature | mitigate | D-02: add <CanonicalizationMethod> as the first child of <SignedInfo> (Pitfall 5). Verified by Plan 02 positive control returning {:ok}. |
| T-30-03 | Elevation | Signing code compiles/runs in :prod | mitigate | Preserve @prod_build + ensure_not_prod!/0 on every FakeIdP entry point (existing discipline, T-29-18). |
| T-30-04 | Tampering | Scope creep into frozen production crypto (signature.ex / pure_beam.ex) | mitigate | D-10 scope fence enforced as acceptance criterion: no file under lib/relyra/security/ is modified by this plan. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — OTP :public_key/:crypto only; no slopcheck surface (RESEARCH Package Legitimacy Audit: Not applicable). |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/test_support_demo_test.exs --warnings-as-errors` green (blast-radius regression).
- The FakeIdP-driven positive control proving `{:ok, %SignedNode{}}` lands in Plan 02 (Wave 2) — this plan's signing change is exercised end-to-end there.
- No file under `lib/relyra/security/` touched (D-10).
</verification>

<success_criteria>
- ASSUR-02 (success #1): `FakeIdP.sign` emits a real `DigestValue` + `SignatureValue` via the promoted signer.
- FakeIdP exposes `self_signed_cert_pem/0` (D-03) for cert_chain configuration.
- Production crypto frozen (D-10).
</success_criteria>

<output>
Create `.planning/phases/30-adversarial-crypto-assurance/30-01-SUMMARY.md` when done.
</output>
