---
phase: 30-adversarial-crypto-assurance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/security_corpus.json
  - CONFORMANCE.md
autonomous: true
requirements: [ASSUR-01]
nyquist_compliant: true

must_haves:
  truths:
    - "A new c14n-differential REJECTION row exists in priv/security_corpus.json asserting :canonicalization_failed (NOT :digest_mismatch — the JSON evaluator never reaches Signature.verify/4)"
    - "The new row carries full provenance / requirement_ids / family / source_ref (corpus_security_test.exs:126-141 enforcement passes)"
    - "CONFORMANCE.md is regenerated so mix relyra.conformance --check passes (no manifest/doc drift)"
  artifacts:
    - path: "priv/security_corpus.json"
      provides: "c14n-differential-rejection-002 row (parser_differential_and_c14n class, canonicalization_failed)"
      contains: "c14n-differential-rejection-002"
    - path: "CONFORMANCE.md"
      provides: "regenerated regression coverage reflecting the new row"
  key_links:
    - from: "priv/security_corpus.json new row"
      to: "corpus_security_test.exs evaluate_fixture/1 parser_differential_and_c14n branch"
      via: "class routing to parse_safely -> canonicalize (never Signature.verify/4)"
      pattern: "parser_differential_and_c14n"
    - from: "priv/security_corpus.json"
      to: "CONFORMANCE.md"
      via: "mix relyra.conformance regeneration + ci.conformance --check drift gate"
      pattern: "relyra.conformance"
---

<objective>
Add the c14n-differential REJECTION row to the static JSON corpus (D-09) and regenerate `CONFORMANCE.md` so the conformance drift gate stays green. This is the COMPLEMENTARY half of the c14n-differential proof: the `:digest_mismatch` crypto proof lives in the new adversarial suite (Plan 02, Task 2); this JSON row asserts `:canonicalization_failed` because the JSON corpus evaluator never reaches `Signature.verify/4`.

Purpose: ASSUR-01 — wire a c14n-differential rejection into the static corpus + conformance manifest (the "wired into corpus_gate + conformance manifest" leg of ASSUR-01). The crypto `:digest_mismatch` row CANNOT live here (Pitfall 1, HIGHEST RISK).
Output: One new row in `priv/security_corpus.json` + a regenerated `CONFORMANCE.md`.

CRITICAL ROUTING CONSTRAINT (Pitfall 1): the `evaluate_fixture/1` branch for `class: "parser_differential_and_c14n"` (corpus_security_test.exs:186-189) runs ONLY `parse_safely -> canonicalize` and NEVER calls `Signature.verify/4`. A row asserting `:digest_mismatch` would FAIL the manifest-to-error-type test (the canonicalize-only path can never produce `:digest_mismatch`). The new row MUST assert `expected_error_type: "canonicalization_failed"`, mirroring the existing `c14n-differential-001` row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-adversarial-crypto-assurance/30-CONTEXT.md
@.planning/phases/30-adversarial-crypto-assurance/30-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. The new row mirrors the existing c14n-differential-001 row. -->

The JSON evaluator routing — test/security/xml/corpus_security_test.exs:
  evaluate_fixture/1 for class "parser_differential_and_c14n" (:186-189):
    with {:ok, parsed_doc} <- PureBeam.parse_safely(xml, opts) do
      PureBeam.canonicalize(parsed_doc, [])   # <-- bare parsed_doc map handle; NEVER Signature.verify/4
    end
  -> On a complete <Response> whose Reference holds only a DigestMethod, PureBeam.canonicalize on
     the bare parsed_doc map fails closed with :canonicalization_failed (the SAME mechanism the
     existing c14n-differential-001 row already proves).
  Row schema enforcement (:126-141): requirement_ids non-empty list; family non-empty string;
    provenance non-empty map; source_ref non-empty string.
  Manifest-to-error-type test (:11-17): evaluate_fixture result type MUST equal
    String.to_atom(fixture["expected_error_type"]).

The analog row — priv/security_corpus.json c14n-differential-001 (:62-76):
  {
    "id": "c14n-differential-001",
    "class": "parser_differential_and_c14n",
    "family": "signature_wrapping",
    "requirement_ids": ["CVE-REG-01"],
    "expected_error_type": "canonicalization_failed",
    "provenance": { "source": "...", "kind": "ported-fixture", "captured_at": "2026-05-07" },
    "source_ref": "purebeam:c14n-differential",
    "xml": "<Response ...>...<Assertion ID=\"assertion-c14n\">...</Assertion><Signature><SignedInfo><SignatureMethod .../><Reference URI=\"#assertion-c14n\"><DigestMethod .../></Reference></SignedInfo></Signature></Response>",
    "notes": "..."
  }

The conformance generator — lib/mix/tasks/relyra.conformance.ex:
  mix relyra.conformance          # regenerate CONFORMANCE.md (NO --check) from priv/security_corpus.json
  mix relyra.conformance --check  # byte-for-byte compare on-disk CONFORMANCE.md vs freshly rendered;
                                  # Mix.raise "drift detected ... rerun mix relyra.conformance" on mismatch.
  ci.conformance runs `relyra.conformance --check` and is the FIRST step of ci.security (mix.exs:153).
  The generator reads family, class, expected_error_type, provenance.source/kind, notes, requirement_ids;
  adding a security row changes the cve_summary_lines count AND the CVE-REG-01 regression table.
  Required conformance keys on a row (conformance_fixtures.ex): id, requirement_ids, provenance.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the c14n-differential REJECTION row to priv/security_corpus.json (D-09)</name>
  <files>priv/security_corpus.json</files>
  <read_first>
    - priv/security_corpus.json (the file being modified — the existing c14n-differential-001 row at :62-76 is the exact template to mirror; the top-level array structure)
    - test/security/xml/corpus_security_test.exs (the evaluator + schema enforcement — evaluate_fixture/1 :161-194, provenance enforcement :126-141, manifest-to-error-type test :11-17)
    - .planning/phases/30-adversarial-crypto-assurance/30-RESEARCH.md (Pitfall 1 + the Code Examples row template)
  </read_first>
  <action>
    Append ONE new element to the top-level array in `priv/security_corpus.json`, mirroring the existing `c14n-differential-001` row (D-09). Required fields:
    - `id`: "c14n-differential-rejection-002"
    - `class`: "parser_differential_and_c14n" (routes through the canonicalize-only evaluator branch)
    - `family`: "signature_wrapping"
    - `requirement_ids`: ["CVE-REG-01", "ASSUR-01"] (multiple IDs allowed; tag ASSUR-01 to trace the row to this phase)
    - `expected_error_type`: "canonicalization_failed" (NOT "digest_mismatch" — Pitfall 1, the highest-risk plan bug; the JSON evaluator never reaches Signature.verify/4)
    - `provenance`: { "source": "Phase 30 adversarial crypto assurance", "kind": "ported-fixture", "captured_at": "2026-05-24" }
    - `source_ref`: "relyra:phase-30:c14n-differential-rejection"
    - `xml`: a complete `<Response>` mirroring `c14n-differential-001`'s shape (a `<Response>` with an `<Assertion ID="...">` and a `<Signature><SignedInfo>` whose `<Reference URI="#...">` holds only a `<DigestMethod>`, no `<DigestValue>`), so the canonicalize-only branch fails closed on the incomplete canonicalization handle. Use a distinct assertion ID (e.g. "assertion-c14n-diff") to avoid duplicate-ID collisions with other rows. Escape inner quotes as JSON string content (mirror the existing row's escaping).
    - `notes`: explain that this is the c14n-differential REJECTION (the seam fails closed with :canonicalization_failed on an incomplete canonicalization handle) and that the :digest_mismatch crypto proof lives in the adversarial_crypto suite (ASSUR-01).

    Keep the JSON valid (the file is decoded by `:json.decode` and `Jason.decode!`). Do NOT add a row with `expected_error_type: "digest_mismatch"` under this class — it would fail the manifest-to-error-type test.
  </action>
  <verify>
    <automated>mix test test/security/xml/corpus_security_test.exs --only security_corpus --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `priv/security_corpus.json` is valid JSON containing a row with `"id": "c14n-differential-rejection-002"`.
    - That row's `"expected_error_type"` is exactly `"canonicalization_failed"` (NOT `"digest_mismatch"`).
    - That row's `"class"` is `"parser_differential_and_c14n"`, `"family"` is non-empty, `"requirement_ids"` includes `"ASSUR-01"`, `"provenance"` is a non-empty map, `"source_ref"` is non-empty.
    - `mix test test/security/xml/corpus_security_test.exs --only security_corpus --warnings-as-errors` passes (the manifest-to-error-type test :11-17, the provenance enforcement :126-141, and the parser_differential_and_c14n binary gate :37-61 all stay green with the new row).
  </acceptance_criteria>
  <done>The static corpus carries a new c14n-differential rejection row asserting :canonicalization_failed; the corpus security suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate CONFORMANCE.md and verify the drift gate (D-09)</name>
  <files>CONFORMANCE.md</files>
  <read_first>
    - lib/mix/tasks/relyra.conformance.ex (the generator + --check drift logic — check_report!/2 :58-77, security_rows_table :156-165, cve_summary_lines :114-126)
    - lib/relyra/conformance_fixtures.ex (load_manifest! required keys: id, requirement_ids, provenance)
    - .planning/phases/30-adversarial-crypto-assurance/30-PATTERNS.md (the CONFORMANCE.md regenerate section — procedure order matters, Pitfall 3)
  </read_first>
  <action>
    Regenerate `CONFORMANCE.md` so it reflects the new JSON row (Pitfall 3 — order matters):

    1. Run `mix relyra.conformance` (NO `--check`) to regenerate `CONFORMANCE.md` from the updated manifest. This updates BOTH the cve_summary_lines count ("fixtures pinned: N") and the CVE-REG-01 Regression Coverage table.
    2. Run `mix relyra.conformance --check` to confirm byte-for-byte equality (no drift). It must report "matches generated manifest state" and exit 0.

    Commit the regenerated `CONFORMANCE.md`. Do NOT hand-edit `CONFORMANCE.md` — it is generated; hand-edits cause drift. If `--check` fails with "drift detected ... rerun mix relyra.conformance", re-run step 1. Touch only `CONFORMANCE.md` (the generator writes it); do NOT modify the generator or `lib/relyra/security/*`.
  </action>
  <verify>
    <automated>mix relyra.conformance --check</automated>
  </verify>
  <acceptance_criteria>
    - `CONFORMANCE.md` is regenerated (not hand-edited) and reflects the new `c14n-differential-rejection-002` row in the CVE-REG-01 Regression Coverage table.
    - `mix relyra.conformance --check` exits 0 with "matches generated manifest state" (no drift) — this is the FIRST step of `ci.security` via `ci.conformance`.
  </acceptance_criteria>
  <done>CONFORMANCE.md is regenerated and the conformance drift gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON manifest row -> evaluator routing | the asserted error type must match the route the class takes (canonicalize-only, not verify) |
| priv/security_corpus.json -> CONFORMANCE.md | the generated doc must stay in sync with the manifest or the drift gate fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-11 | Tampering | c14n-differential routing false-positive (Pitfall 1, HIGHEST RISK) — a JSON row asserting :digest_mismatch under parser_differential_and_c14n fails the manifest-to-error-type test because the evaluator stops at canonicalize and never reaches Signature.verify/4 | mitigate | D-09: the JSON row asserts :canonicalization_failed (mirrors the existing c14n-differential-001 row); the :digest_mismatch proof lives in the adversarial_crypto suite (Plan 02). Acceptance criterion pins expected_error_type = canonicalization_failed. |
| T-30-12 | Tampering | conformance drift undetected — JSON row added but CONFORMANCE.md not regenerated, so the doc claims fewer fixtures than the manifest and ci.security aborts at its first step | mitigate | D-09: mix relyra.conformance regen + mix relyra.conformance --check; acceptance criterion asserts --check exits 0. |
| T-30-13 | Spoofing | a row missing provenance/requirement_ids/family/source_ref slips a non-attributable fixture into the gate | mitigate | corpus_security_test.exs:126-141 enforces all four; acceptance criterion lists each required field. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No package installs — OTP :json + already-vendored Jason; no slopcheck surface (RESEARCH Package Legitimacy Audit: Not applicable). |
</threat_model>

<verification>
- `mix test test/security/xml/corpus_security_test.exs --only security_corpus --warnings-as-errors` green with the new row.
- `mix relyra.conformance --check` exits 0 (no drift).
- The new row asserts `canonicalization_failed`, NOT `digest_mismatch` (Pitfall 1).
- No edit to the generator or `lib/relyra/security/*`.
</verification>

<success_criteria>
- ASSUR-01 (the corpus_gate + conformance-manifest leg): a c14n-differential rejection is wired into the static corpus + conformance manifest, with full provenance, and the drift gate is green.
</success_criteria>

<output>
Create `.planning/phases/30-adversarial-crypto-assurance/30-03-SUMMARY.md` when done.
</output>
