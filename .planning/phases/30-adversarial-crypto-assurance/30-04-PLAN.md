---
phase: 30-adversarial-crypto-assurance
plan: 04
type: execute
wave: 3
depends_on: ["30-02", "30-03"]
files_modified:
  - mix.exs
autonomous: true
requirements: [ASSUR-01]
nyquist_compliant: true

must_haves:
  truths:
    - "The new :adversarial_crypto suite is named in the ci.security alias and runs with --warnings-as-errors"
    - "mix ci.security is green end-to-end (no skipped/pending crypto assertions) — the corpus gates every build"
    - "mix ci.security actually EXECUTES the new suite (failing one adversarial assertion would fail the alias)"
    - "The full suite mix test --warnings-as-errors stays green (no regression to the existing baseline; the new suite raises the count)"
  artifacts:
    - path: "mix.exs"
      provides: "ci.security alias line naming test/security/xml/adversarial_crypto_test.exs --only adversarial_crypto --warnings-as-errors"
      contains: "adversarial_crypto"
  key_links:
    - from: "mix.exs ci.security alias"
      to: "test/security/xml/adversarial_crypto_test.exs"
      via: "named test line with --only adversarial_crypto --warnings-as-errors"
      pattern: "adversarial_crypto_test.exs"
---

<objective>
Name the new `:adversarial_crypto` suite into the `ci.security` alias (D-08) so the adversarial corpus GATES every build, then prove the full gate is green. Today `ci.security` (mix.exs:152-169) runs the parse-layer corpus + the gate02_c14n golden but does NOT run any crypto-verify suite — `signature_crypto_test.exs` is currently OUTSIDE the gate. Without this edit, ASSUR-01 success #4 is unmet and the gate is hollow (a crypto regression would ship green).

Purpose: ASSUR-01 (success #4) — the corpus is wired into the gate and green under `mix ci.security`. This is the FINAL dependent wave: it requires the new suite (Plan 02) and the JSON row + regenerated CONFORMANCE.md (Plan 03) to already be in place, because `mix ci.security` runs `ci.conformance --check` FIRST and then the named test lines.
Output: One added line in the `ci.security` alias list in `mix.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-adversarial-crypto-assurance/30-CONTEXT.md
@.planning/phases/30-adversarial-crypto-assurance/30-PATTERNS.md

<interfaces>
<!-- The exact ci.security alias to edit. Insert one line after the existing corpus/gate02 lines. -->

mix.exs aliases — current ci.security (mix.exs:152-169), insertion point after :161:
  "ci.security": [
    "ci.conformance",                                            # runs relyra.conformance --check FIRST (drift gate)
    "cmd test -f SECURITY_REVIEW.md",
    "cmd test -f docs/security_boundary.md",
    "cmd grep -nE \"docs/security_findings.md|Findings Ledger\" SECURITY_REVIEW.md",
    "relyra.security_review --check",
    "test test/security/strict_default_proof_test.exs --warnings-as-errors",
    "test test/relyra/ecto/escape_hatch_audit_test.exs --warnings-as-errors",
    "test test/security/xml/corpus_security_test.exs test/relyra/security/xml/corpus_gate_test.exs --only security_corpus --warnings-as-errors",   # :160
    "test test/security/xml/corpus_security_test.exs --only gate02_c14n --warnings-as-errors",                                                       # :161
    # <-- INSERT the new adversarial suite line HERE
    "deps.audit --ignore-advisory-ids GHSA-rhv4-8758-jx7v",
    "hex.audit",
    "sobelow --config"
  ]

The new suite (from Plan 02): test/security/xml/adversarial_crypto_test.exs, tagged @tag :adversarial_crypto.
The JSON row + regenerated CONFORMANCE.md (from Plan 03): make ci.conformance --check pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Name the :adversarial_crypto suite into ci.security (D-08)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (the file being modified — the ci.security alias at :152-169, the corpus/gate02 lines at :160-161 as the insertion neighborhood)
    - test/security/xml/adversarial_crypto_test.exs (from Plan 02 — confirm the @tag :adversarial_crypto and the file path the alias must name)
    - .planning/phases/30-adversarial-crypto-assurance/30-PATTERNS.md (the mix.exs section + Pitfall 2)
  </read_first>
  <action>
    In the `ci.security` alias list in `mix.exs`, INSERT one new test line immediately after the existing `gate02_c14n` line (:161):

    `"test test/security/xml/adversarial_crypto_test.exs --only adversarial_crypto --warnings-as-errors",`

    The `--warnings-as-errors` flag is mandatory (Pitfall 4 — the whole lane runs with it). Do NOT remove or reorder the existing lines (especially keep `ci.conformance` first — it runs the conformance drift check). Do NOT add the suite to the existing `security_corpus`/`gate02_c14n` lines' path lists (the new suite uses a distinct tag and warrants its own line for clarity, D-08). Touch only the `ci.security` alias.
  </action>
  <verify>
    <automated>grep -v '^#' mix.exs | grep -c 'adversarial_crypto_test.exs --only adversarial_crypto --warnings-as-errors'</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` `ci.security` alias contains exactly one line: `test test/security/xml/adversarial_crypto_test.exs --only adversarial_crypto --warnings-as-errors`.
    - The grep gate `grep -v '^#' mix.exs | grep -c 'adversarial_crypto_test.exs --only adversarial_crypto --warnings-as-errors'` returns 1.
    - `ci.conformance` is still the first entry of the `ci.security` list; no existing lines removed or reordered.
    - `mix compile --warnings-as-errors` exits 0 (the aliases list is valid Elixir).
  </acceptance_criteria>
  <done>The adversarial suite is named in ci.security with --warnings-as-errors; the alias compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the full gate is green (mix ci.security + full mix test)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (the edited ci.security alias from Task 1)
    - .planning/phases/30-adversarial-crypto-assurance/30-VALIDATION.md (the phase-gate sampling rate — mix ci.security green AND mix test --warnings-as-errors no regression)
    - .planning/phases/30-adversarial-crypto-assurance/30-RESEARCH.md (Pitfall 2 — a suite not in the alias does not gate; verify the adversarial count appears)
  </read_first>
  <action>
    Run the full phase gate and confirm the new suite actually executes inside it (Pitfall 2):

    1. `mix ci.security` — must exit 0 end-to-end. Confirm the adversarial suite ran (its test count appears in the run; a non-zero `:adversarial_crypto` test count). `ci.conformance --check` (first step) must pass (relies on Plan 03's regenerated CONFORMANCE.md). No skipped/pending crypto assertions.
    2. `mix test --warnings-as-errors` — the full suite must stay green (no regression to the existing baseline; the new suite raises the count, never lowers it).

    If `ci.security` aborts at the first step with "drift detected ... rerun mix relyra.conformance", that is a Plan 03 regression — coordinate (re-run `mix relyra.conformance`); do not hand-edit CONFORMANCE.md. If `--warnings-as-errors` aborts on an unused alias/import, fix the offending suite/module (Pitfall 4). Do NOT weaken any assertion, do NOT add `--exclude`/skip to silence a red test, and do NOT modify `lib/relyra/security/*` (D-10).
  </action>
  <verify>
    <automated>mix ci.security && mix test --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix ci.security` exits 0 end-to-end, with `ci.conformance --check` passing (no drift) and the `:adversarial_crypto` suite executing with a non-zero test count and 0 failures.
    - The new adversarial assertions are GATED: removing/failing one adversarial assertion would fail `mix ci.security` (the suite is named in the alias, not just runnable via bare `mix test`).
    - `mix test --warnings-as-errors` exits 0 (full suite green, no regression to the existing baseline; count >= the pre-phase baseline).
    - No assertion weakened, no skip/exclude added to silence a red test, no file under `lib/relyra/security/` modified.
  </acceptance_criteria>
  <done>mix ci.security and the full mix test are green; the adversarial corpus gates every build (ASSUR-01 success #4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> CI gate | a crypto proof only gates if the alias actually runs it |
| ci.security ordering -> conformance drift | ci.conformance must run first; a stale CONFORMANCE.md aborts the whole lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-14 | Tampering | suite-outside-the-gate (Pitfall 2) — crypto proofs exist but are not named in ci.security, so a regression ships green | mitigate | D-08: add the adversarial suite line to the ci.security alias; acceptance criterion asserts mix ci.security executes the suite (non-zero adversarial count) and that failing one assertion fails the alias. |
| T-30-15 | Tampering | green-by-skip — a red adversarial assertion silenced via --exclude/skip or a weakened assertion to force a green gate | mitigate | Acceptance criterion forbids weakening assertions or adding skip/exclude; the gate must be green for the right reason. |
| T-30-16 | Tampering | conformance drift aborts the gate at step 1 (depends on Plan 03's regenerated CONFORMANCE.md) | mitigate | depends_on Plan 03; if drift surfaces, re-run mix relyra.conformance (no hand-edit). Acceptance criterion asserts ci.conformance --check passes. |
| T-30-17 | Elevation | scope creep into frozen crypto to "make the gate pass" (D-10) | mitigate | Acceptance criterion: no file under lib/relyra/security/ modified; the verify path is frozen — fix test wiring, never the verifier. |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No package installs — alias edit only; no slopcheck surface (RESEARCH Package Legitimacy Audit: Not applicable). |
</threat_model>

<verification>
- `mix ci.security` green end-to-end, with the `:adversarial_crypto` suite executing (non-zero count, 0 failures) and `ci.conformance --check` passing.
- `mix test --warnings-as-errors` green (no regression).
- No skip/exclude/weakening; no edit to `lib/relyra/security/*` (D-10).
</verification>

<success_criteria>
- ASSUR-01 (success #4): the corpus is wired into the gate and green under `mix ci.security` (no skipped/pending crypto assertions).
- The adversarial corpus gates every build (a crypto regression now fails CI).
</success_criteria>

<output>
Create `.planning/phases/30-adversarial-crypto-assurance/30-04-SUMMARY.md` when done.
</output>
