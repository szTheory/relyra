---
phase: 30-adversarial-crypto-assurance
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - test/security/xml/adversarial_crypto_test.exs
autonomous: true
requirements: [ASSUR-01, ASSUR-02]
nyquist_compliant: true

must_haves:
  truths:
    - "A permanent adversarial corpus proves rejection of forged-signature, wrong-key, tampered-content, digest-mismatch, and canonicalization-differential, each asserting {:error, %Relyra.Error{type: ...}}"
    - "A positive control proves a genuinely FakeIdP-signed Response verifies {:ok, %SignedNode{}}"
    - "The positive control + at least one negative drive END-TO-END through FakeIdP.sign (literally exercising the ASSUR-02 real-signing path)"
    - "The ECDSA fail-closed assertion is carried into the gated suite as an explicit 6th assertion (:unsupported_signature_algorithm), closing the algorithm-substitution sample"
    - "Production crypto is FROZEN: signature.ex and pure_beam.ex are not touched; no XSW-shaped input tempts a WR-03 fix (D-10)"
  artifacts:
    - path: "test/security/xml/adversarial_crypto_test.exs"
      provides: "@tag :adversarial_crypto suite — 5 ASSUR-01 categories + positive control + ECDSA carry-over, FakeIdP-driven"
      min_lines: 80
      contains: "adversarial_crypto"
  key_links:
    - from: "test/security/xml/adversarial_crypto_test.exs positive control"
      to: "FakeIdP.sign / FakeIdP.self_signed_cert_pem/0"
      via: "end-to-end drive through the promoted real-signing path (ASSUR-02)"
      pattern: "FakeIdP.sign"
    - from: "test/security/xml/adversarial_crypto_test.exs negatives"
      to: "Signature.verify/4"
      via: "PureBeam.parse_safely -> Signature.verify/4 (the frozen Phase-29 verify path)"
      pattern: "Signature.verify"
---

<objective>
Create the permanent, FakeIdP-driven, `@tag :adversarial_crypto` adversarial corpus suite (ASSUR-01) that proves the XMLDSig verifier rejects every named attack category and accepts only a genuine signature. Every recipe already exists and passes in `test/relyra/security/signature_crypto_test.exs` — this plan PROMOTES them into a permanent gated suite (consolidate, do not re-derive), adds the genuinely-NEW c14n-differential `:digest_mismatch` case (D-06), and carries the ECDSA fail-closed assertion into the gate.

Purpose: ASSUR-01 — a permanent corpus proving the Phase-29 verify path is real, not structure-only. ASSUR-02 — the positive control + one negative drive through `FakeIdP.sign` end-to-end (the promoted real-signing path from Plan 01).
Output: New `test/security/xml/adversarial_crypto_test.exs` (the gate wiring into `ci.security` is Plan 04).

The verify path (`Signature.verify/4`) is FROZEN. This suite EXERCISES it; it does not change it. Do NOT modify `lib/relyra/security/signature.ex` or `lib/relyra/security/xml/pure_beam.ex` (D-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-adversarial-crypto-assurance/30-CONTEXT.md
@.planning/phases/30-adversarial-crypto-assurance/30-PATTERNS.md
@.planning/phases/30-adversarial-crypto-assurance/30-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Copy the existing recipes verbatim from the analog. -->

The verify path under test — Relyra.Security.Signature (lib/relyra/security/signature.ex, FROZEN):
  Signature.verify(parsed_doc, connection, cert_chain) ->
    {:ok, %Relyra.Security.SignedNode{}} | {:error, %Relyra.Error{type: atom}}
  SignedNode fields used in assertions: .signature_method, .digest_method, .xml_id
  Crypto error atoms (from lib/relyra/security/xml.ex union):
    :invalid_signature  (forged / wrong-key)
    :digest_mismatch    (tampered-content / c14n-differential)
    :unsupported_signature_algorithm (ECDSA / unknown)

The genuine signer — Relyra.TestSupport.XmldsigSigner (lib/relyra/test_support/xmldsig_signer.ex):
  signed_response(opts \\ []) :: %{response_xml: String.t(), cert_chain: [pem]}
    opts: :assertion_id (default "assertion-1"), :name_id (default "user@example.com"),
          :tamper_name_id (rewrites <NameID> AFTER signing -> :digest_mismatch), + protocol overrides
  self_signed_cert_pem() :: String.t()  (FakeIdP-derived trust cert)

The promoted FakeIdP (after Plan 01) — Relyra.TestSupport.FakeIdP:
  build_response(opts \\ []) :: %Builder{}
  sign(%Builder{} | keyword(), keyword()) :: String.t()  (base64, padding: false — now genuinely signed)
  self_signed_cert_pem() :: String.t()  (delegated to XmldsigSigner)

PureBeam.parse_safely(xml, opts) :: {:ok, parsed_doc} | {:error, %Relyra.Error{}}

EXISTING recipes to PROMOTE (from test/relyra/security/signature_crypto_test.exs — copy verbatim):
  - forged-sig (:81-90): replace <SignatureValue> with Base.encode64(:crypto.strong_rand_bytes(N))
      where N = byte_size of the genuine signature bytes -> :invalid_signature
  - wrong-key (:215-224 + throwaway_cert_pem/0 helper :260-264): verify a genuine doc against a
      throwaway self-signed cert -> :invalid_signature
  - tampered-content (:226-235): XmldsigSigner.signed_response(tamper_name_id: "attacker@evil.example.com")
      -> :digest_mismatch
  - positive control (:203-213): genuine signed_response -> {:ok, %SignedNode{}}
  - ECDSA carry-over (:131-141): %{parsed_doc | signature_method: "...#ecdsa-sha256"}
      -> :unsupported_signature_algorithm
  Local helpers to copy: connection/0 (:379) = %{connection_id: "conn-crypto"};
    throwaway_cert_pem/0 (:260-264); put_candidate/3 (:374-377) for the forged-sig recipe.

C14N-PRESERVED mutation catalog (D-06, from PROVENANCE.md) — for the NEW c14n-differential case:
  USE (changes the recomputed digest -> :digest_mismatch):
    - added non-namespace attribute on the <Assertion> apex (RECOMMENDED, simplest, unambiguous)
    - added child element inside the Assertion subtree
  AVOID (C14N normalizes away -> digest UNCHANGED -> falsely passes {:ok}):
    - attribute reordering (C14N sorts attrs)
    - unused inherited namespace declaration (exclusive-C14N omits non-visibly-utilized ns)
    - self-closing <-> expanded empty element (C14N always expands)
    - re-escaping equivalent text (&#65; vs A)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the suite with the 5 promoted recipes + positive control + ECDSA carry-over</name>
  <files>test/security/xml/adversarial_crypto_test.exs</files>
  <read_first>
    - test/relyra/security/signature_crypto_test.exs (the source of EVERY recipe — preamble :19-26, forged :81-90, ECDSA :131-141, positive :203-213, wrong-key :215-224, tampered :226-235, helpers throwaway_cert_pem/0 :260-264, connection/0 :379, put_candidate/3 :374-377)
    - lib/relyra/test_support/xmldsig_signer.ex (signed_response/1 :94-130, tamper_name_id knob :88-92, self_signed_cert_pem/0 :204-211)
    - lib/relyra/test_support/fake_idp.ex (the PROMOTED sign/2 + self_signed_cert_pem/0 from Plan 01 — drive the positive control through these)
  </read_first>
  <action>
    Create `test/security/xml/adversarial_crypto_test.exs`, module `Relyra.Security.AdversarialCryptoTest`, `use ExUnit.Case, async: true`, tag every test (or use `@moduletag`) with `@tag :adversarial_crypto` so Plan 04 can name `--only adversarial_crypto` in `ci.security`. Alias only what is used (keep tight — `--warnings-as-errors` aborts on an unused alias, Pitfall 4): `Relyra.Error`, `Relyra.Security.Signature`, `Relyra.Security.XML.PureBeam`, `Relyra.TestSupport.FakeIdP`, `Relyra.TestSupport.XmldsigSigner`.

    Implement these assertions, copying recipes verbatim from the analog:

    1. POSITIVE CONTROL (ASSUR-02 end-to-end, D-01 discretion): drive through the PROMOTED `FakeIdP.sign` — `b64 = FakeIdP.sign(FakeIdP.build_response())`, `{:ok, xml} = Base.decode64(b64, padding: false)`, `{:ok, parsed_doc} = PureBeam.parse_safely(xml, [])`, then assert `{:ok, %Relyra.Security.SignedNode{} = node} = Signature.verify(parsed_doc, connection(), [FakeIdP.self_signed_cert_pem()])` and assert `node.signature_method == "http://www.w3.org/2001/04/xmldsig-more#rsa-sha256"`. This literally exercises the ASSUR-02 real-signing path.

    2. forged-sig -> `:invalid_signature` (analog :81-90 + put_candidate/3): mint genuine via `XmldsigSigner.signed_response()`, parse, replace the candidate's `:signature_value_b64` with same-length random base64.

    3. wrong-key -> `:invalid_signature` (analog :215-224): verify a genuine `signed_response()` doc against `[throwaway_cert_pem()]`.

    4. tampered-content -> `:digest_mismatch` (analog :226-235): `XmldsigSigner.signed_response(tamper_name_id: "attacker@evil.example.com")`, parse, verify against `signed.cert_chain`.

    5. ECDSA carry-over -> `:unsupported_signature_algorithm` (analog :131-141, the explicit 6th ASSERTION not a 6th category): genuine doc parsed, set `%{parsed_doc | signature_method: "http://www.w3.org/2001/04/xmldsig-more#ecdsa-sha256"}`, verify -> the typed reject.

    Copy local helpers verbatim: `connection/0`, `throwaway_cert_pem/0`, `put_candidate/3`. Drive at least the positive control through `FakeIdP.sign` (done above); the negatives may use `XmldsigSigner.signed_response/1` directly (cleaner mutation seam) — both go through the SAME signing path post-promotion (D-01). Assert the EXACT `%Error{type: ...}` per case, never bare `{:error, _}`. Do NOT touch `lib/relyra/security/*`.
  </action>
  <verify>
    <automated>mix test test/security/xml/adversarial_crypto_test.exs --only adversarial_crypto --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The suite file exists at `test/security/xml/adversarial_crypto_test.exs` and is tagged `:adversarial_crypto`.
    - Positive control: `{:ok, %Relyra.Security.SignedNode{}}` via `FakeIdP.sign(...)` decoded + `PureBeam.parse_safely` + `Signature.verify(parsed_doc, connection(), [FakeIdP.self_signed_cert_pem()])`, with `signature_method == "http://www.w3.org/2001/04/xmldsig-more#rsa-sha256"`.
    - forged-sig asserts `{:error, %Error{type: :invalid_signature}}`.
    - wrong-key asserts `{:error, %Error{type: :invalid_signature}}`.
    - tampered-content asserts `{:error, %Error{type: :digest_mismatch}}`.
    - ECDSA carry-over asserts `{:error, %Error{type: :unsupported_signature_algorithm}}`.
    - `mix test test/security/xml/adversarial_crypto_test.exs --only adversarial_crypto --warnings-as-errors` passes with a non-zero test count and 0 failures (no compile warnings — Pitfall 4).
    - No file under `lib/relyra/security/` is modified.
  </acceptance_criteria>
  <done>Five-of-six assertions (positive + forged + wrong-key + tampered + ECDSA) pass under the `:adversarial_crypto` tag; the positive control drives through the promoted FakeIdP.sign end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Add the NEW c14n-differential :digest_mismatch case (D-06)</name>
  <files>test/security/xml/adversarial_crypto_test.exs</files>
  <read_first>
    - test/security/xml/adversarial_crypto_test.exs (the file from Task 1)
    - lib/relyra/test_support/xmldsig_signer.ex (maybe_tamper_name_id :317-328 — the post-signing mutation mechanism to mirror; signed_response default assertion_id "assertion-1" :58)
    - test/fixtures/security/xml/parser_differential_and_c14n/PROVENANCE.md (the C14N pitfall catalog — confirm the chosen mutation is PRESERVED, not normalized away)
    - .planning/phases/30-adversarial-crypto-assurance/30-RESEARCH.md (Pattern 3 + "C14N pitfall mutations to AVOID")
  </read_first>
  <action>
    Add the genuinely-NEW c14n-differential case the existing suite does NOT cover (D-06). Mint a genuine doc via `XmldsigSigner.signed_response()` (default `assertion_id` "assertion-1"), then apply a post-signing C14N-PRESERVED mutation into the signed `<Assertion>` subtree WITHOUT recomputing the digest/signature — mirroring `maybe_tamper_name_id` (a `String.replace`-style injection on `signed.response_xml`). RECOMMENDED mutation: add a non-namespace attribute to the `<Assertion>` apex element, e.g. replace `<Assertion ID="assertion-1">` with `<Assertion ID="assertion-1" Foo="bar">` (`global: false`). Adding a non-namespace attribute is unambiguously C14N-PRESERVED (PROVENANCE: attrs are sorted by resolved-URI-then-local but NEVER dropped — Pitfall 8), so the recomputed digest differs.

    Then `{:ok, parsed_doc} = PureBeam.parse_safely(mutated_xml, [])` and assert `{:error, %Error{type: :digest_mismatch}} = Signature.verify(parsed_doc, connection(), signed.cert_chain)`.

    MUST assert `:digest_mismatch` EXPLICITLY (never `{:ok}` or bare `{:error, _}`) so a no-op (C14N-normalized) mutation surfaces immediately as a `{:ok}` failure. Do NOT use any mutation from the AVOID list (attribute reordering, unused-ns decl, empty-element expansion, text re-escaping) — those canonicalize identically and would falsely pass.

    SCOPE FENCE (D-10 / Pitfall 6): do NOT design an XSW-shaped input (genuine signature over assertion-A + injected attacker assertion-B) that brushes WR-03 (Reference/@URI not bound to consumed node). If any planned input touches that territory, assert CURRENT behavior and annotate "WR-03 follow-up — do not fix in-phase." Do NOT touch `lib/relyra/security/xml/pure_beam.ex` `signed_candidates/1` or `lib/relyra/security/signature.ex`.
  </action>
  <verify>
    <automated>mix test test/security/xml/adversarial_crypto_test.exs --only adversarial_crypto --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - A new test applies a post-signing C14N-PRESERVED mutation (added non-namespace attribute on the `<Assertion>` apex, or added child element) to a genuinely-signed Response.
    - The mutation is NOT one C14N normalizes away (not attribute reordering / unused-ns / empty-element-expansion / text re-escaping).
    - The test asserts EXACTLY `{:error, %Error{type: :digest_mismatch}}` — a `{:ok}` result fails the test.
    - No XSW-shaped input that would tempt a WR-03 fix; no edit to `pure_beam.ex` `signed_candidates/1` or `signature.ex`.
    - `mix test test/security/xml/adversarial_crypto_test.exs --only adversarial_crypto --warnings-as-errors` passes (all 6 assertions: positive + forged + wrong-key + tampered + ECDSA + c14n-differential), 0 failures.
  </acceptance_criteria>
  <done>The new c14n-differential case proves a C14N-preserved post-signing mutation changes the recomputed digest, asserting :digest_mismatch explicitly; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| genuine signed bytes -> mutated input -> verifier | each adversarial recipe crosses this; the verifier must reject everything except the genuine input |
| chosen c14n mutation -> recomputed digest | the mutation must be C14N-PRESERVED or the rejection case falsely passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Tampering | c14n-no-op tamper false-negative — a mutation C14N normalizes away leaves the digest unchanged, so the rejection case falsely passes {:ok} | mitigate | D-06: choose a C14N-PRESERVED mutation (added non-namespace attribute on <Assertion>), verified against the PROVENANCE pitfall catalog; acceptance criterion asserts {:error, :digest_mismatch} EXPLICITLY so a no-op surfaces as a {:ok} failure. |
| T-30-06 | Spoofing | forged/wrong-key acceptance — the verifier accepts a forged SignatureValue or a wrong-key signature | mitigate | Promote the forged-sig (same-length random base64) and wrong-key (throwaway cert) recipes; each asserts {:error, :invalid_signature}, exercising :public_key.verify math and the configured-cert trust source. |
| T-30-07 | Tampering | content tamper acceptance — a NameID swap with an otherwise well-formed signature is accepted | mitigate | Promote the tamper_name_id recipe asserting {:error, :digest_mismatch} (the DigestValue recompute catches it). |
| T-30-08 | Spoofing | algorithm-substitution sample falls outside the gate (:unsupported_signature_algorithm) | mitigate | Carry the existing ECDSA fail-closed assertion into the gated suite as an explicit 6th assertion. |
| T-30-09 | Tampering | divergent-signer false-positive in the positive control | mitigate | Positive control drives through FakeIdP.sign (promoted in Plan 01 to delegate to the single signer); asserts {:ok, %SignedNode{}} only for a genuine signature. |
| T-30-10 | Tampering | scope creep into frozen crypto / WR-03 fix (D-10) | mitigate | Acceptance criteria forbid edits to signature.ex / pure_beam.ex and forbid XSW-shaped inputs that tempt a WR-03 fix (assert current behavior + annotate instead). |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No package installs — OTP :public_key/:crypto only (RESEARCH Package Legitimacy Audit: Not applicable). |
</threat_model>

<verification>
- `mix test test/security/xml/adversarial_crypto_test.exs --only adversarial_crypto --warnings-as-errors` green, 6 assertions (positive + 5 categories where digest-mismatch is split into tampered + c14n-differential, plus ECDSA carry-over), 0 failures.
- Every assertion pins an exact `%Error{type: ...}` (or `%SignedNode{}` for the positive control).
- No file under `lib/relyra/security/` modified (D-10).
- Per-wave merge check: `mix test --only security_corpus --only gate02_c14n --only adversarial_crypto --warnings-as-errors`.
</verification>

<success_criteria>
- ASSUR-01 (success #2): rejection of forged-sig / wrong-key / tampered-content / digest-mismatch / c14n-differential, each `{:error, %Relyra.Error{}}`.
- ASSUR-01 (success #3): positive control `{:ok}` via a genuinely-signed Response.
- ASSUR-02: positive control drives through `FakeIdP.sign` end-to-end.
- ECDSA fail-closed carried into the gate (closes the algorithm-substitution sample).
- Gating itself (naming the suite into `ci.security`) is Plan 04.
</success_criteria>

<output>
Create `.planning/phases/30-adversarial-crypto-assurance/30-02-SUMMARY.md` when done.
</output>
