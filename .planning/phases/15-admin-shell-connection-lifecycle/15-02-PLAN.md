---
phase: "15-admin-shell-connection-lifecycle"
plan: 2
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - lib/relyra/live_admin/connections_live.ex
  - lib/relyra/live_admin/components/connection_form.ex
  - lib/relyra/live_admin/components/preset_picker.ex
autonomous: true
requirements: ["ADM-02"]
must_haves:
  truths:
    - Operator can create a new connection from a preset, seeing defaults prefilled before save.
    - URL reflects the chosen preset.
  artifacts:
    - path: "lib/relyra/live_admin/components/connection_form.ex"
      provides: "New/edit connection form component"
    - path: "lib/relyra/live_admin/components/preset_picker.ex"
      provides: "Provider preset selection component"
  key_links:
    - from: "lib/relyra/live_admin/connections_live.ex"
      to: "lib/relyra/live_admin/components/connection_form.ex"
      via: "Passes preset-derived attributes as assigns"
    - from: "lib/relyra/live_admin/connections_live.ex"
      to: "lib/relyra/live_admin/components/preset_picker.ex"
      via: "Embeds component and handles preset selection"
---

<objective>
Extract the connection editor into separate form and preset picker components, and implement URL-driven preset selection so that operators can clearly see prefilled defaults before saving (D-05, D-06, D-07, D-08, D-09).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-admin-shell-connection-lifecycle/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract form and preset picker components</name>
  <files>
    - lib/relyra/live_admin/components/connection_form.ex
    - lib/relyra/live_admin/components/preset_picker.ex
  </files>
  <action>
    Create two new components `Relyra.LiveAdmin.Components.ConnectionForm` and `PresetPicker`.
    Extract the form generation and provider preset selection logic from `ConnectionsLive` into these modules.
    Ensure `PresetPicker` displays explicit provider choices and a "Custom" option.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Components exist, compile cleanly, tests pass, and expose `connection_form(assigns)` and `preset_picker(assigns)`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire URL-driven presets to the form</name>
  <files>lib/relyra/live_admin/connections_live.ex</files>
  <action>
    Modify `ConnectionsLive.handle_params/3` for the `:new` live action to read `params["preset"]`.
    If a preset is present, resolve its defaults via `Relyra.Provider` and assign them as initial form data for the `ConnectionForm` component.
    Treat changing the preset as a reset-level action (discarding previously entered data).
    Replace the old monolithic inline editor rendering with the new `PresetPicker` and `ConnectionForm` components.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Tests pass, and LiveView appropriately parses preset query params to seed form data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin -> Form -> Ecto | Processing operator input for connection creation/editing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Spoofing | `ConnectionsLive` handle_params | mitigate | Parameter validation ensures preset matches known providers via `Relyra.Provider` registry. |
</threat_model>

<verification>
mix test test/phoenix/live_admin_test.exs
</verification>

<success_criteria>
- The connection editor uses extracted components.
- LiveView state correctly maps `?preset=xyz` into the form defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-shell-connection-lifecycle/15-02-SUMMARY.md`
</output>
