---
phase: "15-admin-shell-connection-lifecycle"
plan: 3
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - lib/relyra/live_admin/connections_live.ex
  - lib/relyra/live_admin/components/connection_detail.ex
  - lib/relyra/live_admin/components/connection_form.ex
  - test/phoenix/live_admin_test.exs
autonomous: true
requirements: ["ADM-02", "RISK-01"]
must_haves:
  truths:
    - Operator can move a connection between draft, enabled, and disabled states from the admin UI.
    - Operator sees explicit immediate status badges (draft, enabled, disabled) alongside readiness blockers.
    - Operator sees an always-visible risk panel on detail and edit views when compatibility overrides are active.
  artifacts:
    - path: "lib/relyra/live_admin/connections_live.ex"
      provides: "Event handlers for enable and disable"
    - path: "lib/relyra/live_admin/components/connection_detail.ex"
      provides: "Buttons to trigger enable/disable, explicit status badges, and display of readiness blockers"
    - path: "test/phoenix/live_admin_test.exs"
      provides: "Integration tests for the lifecycle events and status rendering"
  key_links:
    - from: "lib/relyra/live_admin/components/connection_detail.ex"
      to: "lib/relyra/live_admin/connections_live.ex"
      via: "phx-click enable_connection and disable_connection events"
    - from: "lib/relyra/live_admin/connections_live.ex"
      to: "Relyra.Ecto.Connections"
      via: "enable/2 and disable/2 calls"
    - from: "lib/relyra/live_admin/components/connection_detail.ex"
      to: "lib/relyra/live_admin/components/risk_panel.ex"
      via: "Embeds component to display risk flags"
    - from: "lib/relyra/live_admin/components/connection_form.ex"
      to: "lib/relyra/live_admin/components/risk_panel.ex"
      via: "Embeds component to display risk flags"
---

<objective>
Wire up the `Enable` and `Disable` lifecycle transitions to the Ecto command boundary, surfacing readiness blockers when enabling fails, adding explicit status badges, and ensuring the risk panel is visible across both detail and edit views (D-10, D-11, D-12, D-13, D-14, D-15).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-admin-shell-connection-lifecycle/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire lifecycle events to Ecto boundaries and add status badges</name>
  <files>
    - lib/relyra/live_admin/connections_live.ex
    - lib/relyra/live_admin/components/connection_detail.ex
  </files>
  <action>
    In `ConnectionDetail`, ensure "Enable" and "Disable" buttons emit `enable_connection` and `disable_connection` events. 
    Add explicit immediate status badges (e.g. draft, enabled, disabled) to the detail view to reflect the connection's current state (D-11).
    Display any readiness blockers (from `detail.connection.readiness_errors`) clearly near the Enable button so operators know what is missing.
    In `ConnectionsLive`, handle these events by calling `Relyra.Ecto.Connections.enable/2` and `disable/2`. **CRITICAL:** Explicitly pass the audit context/scope to the Ecto command functions (e.g., `audit: audit_context` from the socket/session) to ensure verifiable ledger entries per D-14 and T-15-05.
    Handle their return values: on success, reload the detail assign and flash a success message; on error (e.g. `{:error, changeset}` with readiness errors), assign the errors to be displayed by the UI.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Lifecycle buttons successfully toggle connection state through the Ecto boundary with audit context, explicit status badges show correct state, or explicitly block with readiness errors.</done>
</task>

<task type="auto">
  <name>Task 2: Embed risk panel across relevant views</name>
  <files>
    - lib/relyra/live_admin/components/connection_detail.ex
    - lib/relyra/live_admin/components/connection_form.ex
  </files>
  <action>
    Ensure both `ConnectionDetail` and `ConnectionForm` components receive the `detail.risk_flags` and render the `<RiskPanel>` component prominently at the top of their respective layouts if risk flags are present.
    This fulfills the requirement (D-15, D-17) that risk is persistent and visible during editing.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>RiskPanel is correctly rendered in both detail and edit component templates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin -> Ecto | Enabling a connection promotes its configuration to be trusted by the host app for logins. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Elevation of Privilege | `ConnectionsLive` enable event | mitigate | Must use `Relyra.Ecto.Connections.enable` to ensure strict readiness validation occurs on the server, rather than trusting the UI state. |
| T-15-05 | Repudiation | `ConnectionsLive` enable/disable | mitigate | Admin scope MUST be passed down into `audit: audit` parameter of Ecto context functions to ensure verifiable ledger entries. |
</threat_model>

<verification>
mix test test/phoenix/live_admin_test.exs
</verification>

<success_criteria>
- Connections can be toggled between enabled/disabled.
- Readiness failures explicitly block enabling.
- Risk warnings are prominently visible on detail and edit views.
- Status badges are explicitly displayed on the connection.
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-shell-connection-lifecycle/15-03-SUMMARY.md`
</output>
