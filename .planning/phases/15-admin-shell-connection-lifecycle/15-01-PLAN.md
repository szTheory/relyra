---
phase: "15-admin-shell-connection-lifecycle"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/live_admin/query.ex
  - lib/relyra/live_admin/connections_live.ex
  - lib/relyra/live_admin/components/connection_list.ex
  - lib/relyra/live_admin/components/connection_detail.ex
  - lib/relyra/live_admin/components/risk_panel.ex
autonomous: true
requirements: ["ADM-01", "RISK-01"]
must_haves:
  truths:
    - Operator sees a persistent shell layout with a connection list and a detail view.
    - Operator sees risk flags prominently displayed in the detail view.
    - Compatibility overrides in risk flags are normalized to user-facing language.
  artifacts:
    - path: "lib/relyra/live_admin/components/connection_list.ex"
      provides: "Sidebar navigation component"
    - path: "lib/relyra/live_admin/components/connection_detail.ex"
      provides: "Main detail view component"
    - path: "lib/relyra/live_admin/components/risk_panel.ex"
      provides: "Always-visible risk warning component"
  key_links:
    - from: "lib/relyra/live_admin/connections_live.ex"
      to: "lib/relyra/live_admin/components/connection_list.ex"
      via: "Function component rendering in render/1"
    - from: "lib/relyra/live_admin/connections_live.ex"
      to: "lib/relyra/live_admin/components/connection_detail.ex"
      via: "Function component rendering in render/1"
    - from: "lib/relyra/live_admin/connections_live.ex"
      to: "lib/relyra/live_admin/components/risk_panel.ex"
      via: "Function component rendering in render/1"
---

<objective>
Extract the monolithic view code from `ConnectionsLive` into focused function components for the list, detail, and risk panel.
Purpose: Establish the persistent, routable LiveView admin shell structure required for Phase 15 and future phases (D-01, D-03, D-04). Normalizes compatibility override naming (D-18).
Output: A cleaner `connections_live.ex` and three new reusable UI components, alongside updated query mapping.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-admin-shell-connection-lifecycle/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create connection list, detail components and normalize risk flag names</name>
  <files>
    - lib/relyra/live_admin/query.ex
    - lib/relyra/live_admin/components/connection_list.ex
    - lib/relyra/live_admin/components/connection_detail.ex
    - lib/relyra/live_admin/components/risk_panel.ex
  </files>
  <action>
    Update `Relyra.LiveAdmin.Query` to map internal compatibility overrides (e.g. `legacy_sha1`) to normalized, user-facing roadmap language before they reach the UI (D-18).
    Create three new modules (`Relyra.LiveAdmin.Components.ConnectionList`, `ConnectionDetail`, and `RiskPanel`) with standard Phoenix `use Phoenix.Component`.
    Copy the HTML structural intent from the existing `render_connection_list`, `render_connection_detail`, and risk block logic currently in `lib/relyra/live_admin/connections_live.ex`.
    Expose them as `connection_list(assigns)`, `connection_detail(assigns)`, and `risk_panel(assigns)` respectively.
    Ensure `risk_panel` accepts a `risk_flags` attribute and loops over it to display warnings (per D-15, D-17).
    Explicitly add tests for the query risk flag mapping logic in `test/phoenix/live_admin_test.exs` or the relevant query test file to verify the normalization.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Component modules compile successfully, export the core rendering functions, query normalizes risk flags, and the mapping logic is covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ConnectionsLive to use components</name>
  <files>lib/relyra/live_admin/connections_live.ex</files>
  <action>
    Alias the new components in `ConnectionsLive`.
    Replace the monolithic inline `render_connection_list`, `render_connection_detail`, and local risk logic with calls to the new components (e.g., `<Relyra.LiveAdmin.Components.ConnectionList.connection_list connections={@connections} current_id={@connection_id} />`).
    Preserve existing routing functionality (list on left, detail on right). Do NOT modify the form/editor HTML yet; that happens in plan 02.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Tests pass, proving the LiveView still mounts and renders properly after component extraction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin -> LiveView | Rendering sensitive connection and risk data to authorized admins. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `ConnectionsLive` | mitigate | Phoenix HTML auto-escapes output to prevent XSS. Use safe HEEx templates. |
| T-15-02 | Info Disclosure | `ConnectionsLive` | mitigate | Ensure admin scopes remain enforced via existing `Relyra.LiveAdmin.Query` boundaries. |
</threat_model>

<verification>
mix test test/phoenix/live_admin_test.exs
</verification>

<success_criteria>
- `ConnectionsLive` uses extracted `ConnectionList`, `ConnectionDetail`, and `RiskPanel` components.
- Existing tests continue to pass.
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-shell-connection-lifecycle/15-01-SUMMARY.md`
</output>
