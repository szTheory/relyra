---
phase: 18-mapping-editor-audit-timeline-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/relyra/live_admin/components/connection_detail.ex
  - lib/relyra/live_admin/connections_live.ex
  - test/phoenix/live_admin_test.exs
autonomous: true
requirements: [MAP-01]
must_haves:
  truths:
    - "Operator can edit attribute and group mapping rules using a dynamic, strictly typed form"
    - "Operator cannot submit invalid mapping structures or arbitrary JSON"
    - "Operator can see an explicit 'Active' badge on the current mapping revision"
  artifacts:
    - path: "lib/relyra/live_admin/connections_live.ex"
      provides: "Changeset-backed form state for mappings"
    - path: "lib/relyra/live_admin/components/connection_detail.ex"
      provides: "Dynamic inputs_for form and revision badging"
  key_links:
    - from: "lib/relyra/live_admin/components/connection_detail.ex"
      to: "lib/relyra/live_admin/connections_live.ex"
      via: "form submission with nested attributes"
---

<objective>
Replace the legacy JSON textarea for mapping rules with a strictly typed, dynamic LiveView form and clearly badge the active mapping revision.

Purpose: Prevent mapping errors caused by manual JSON editing (SSO outages) and remove ambiguity around which mapping revision is currently active.
Output: Dynamic LiveView form utilizing `inputs_for` backed by Ecto changesets, and declarative UI badging for the active mapping.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-mapping-editor-audit-timeline-hardening/18-CONTEXT.md
@.planning/phases/18-mapping-editor-audit-timeline-hardening/18-PATTERNS.md
@.planning/phases/18-mapping-editor-audit-timeline-hardening/18-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create typed form handlers in ConnectionsLive</name>
  <files>lib/relyra/live_admin/connections_live.ex, test/phoenix/live_admin_test.exs</files>
  <behavior>
    - Form submission event for mappings casts parameters using a schemaless Ecto Changeset (or embedded schema) rather than decoding JSON.
    - Handles "add" and "remove" mapping rule events to support dynamic form rows.
  </behavior>
  <action>
    Introduce an embedded schema or schemaless changeset (e.g. `Relyra.LiveAdmin.MappingForm`) to structure mapping rule validations. Update the `"save_attribute_mappings"` and `"save_group_mappings"` handlers in `ConnectionsLive` to accept standard form params, validate via the changeset, and pass the struct/map to `MappingCommands`. Add `"add_mapping"` and `"remove_mapping"` events to modify the changeset in the socket assigns.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Changeset state handles mapping rules correctly and invalid submissions are rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dynamic form and Active badge in ConnectionDetail</name>
  <files>lib/relyra/live_admin/components/connection_detail.ex</files>
  <action>
    Replace the JSON textarea inputs with `Phoenix.Component.inputs_for/1` blocks to render individual fields for mapping rules (e.g. `attribute_name`, `target`).
    In the mapping revision history loop, add a prominent pill/badge reading "Active" conditionally when `index == 0` (using the pattern documented in 18-PATTERNS.md).
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>UI correctly uses `inputs_for` for mappings and applies the active badge.</done>
</task>

<task type="auto">
  <name>Task 3: Verify form behavior in LiveView tests</name>
  <files>test/phoenix/live_admin_test.exs</files>
  <action>
    Add or update LiveView integration tests to mount the detail view, manipulate the mapping form (add/remove row), submit the valid form, and verify that the "Active" badge is rendered in the HTML for the current mapping revision.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Tests pass and prove mapping forms are strictly typed and history is badged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator Browser → LiveView | Operator submitting mapping modifications crosses into validated server state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `ConnectionsLive` | mitigate | Reject arbitrary JSON, force input validation through strictly typed `Ecto.Changeset` form casts. |
</threat_model>

<verification>
mix test test/phoenix/live_admin_test.exs
</verification>

<success_criteria>
- Operators can add, remove, and edit mappings using specific input fields.
- JSON textareas are completely removed from mapping editing.
- "Active" badge is visually rendered on the first/active revision in history.
</success_criteria>

<output>
After completion, create `.planning/phases/18-mapping-editor-audit-timeline-hardening/18-01-SUMMARY.md`
</output>
