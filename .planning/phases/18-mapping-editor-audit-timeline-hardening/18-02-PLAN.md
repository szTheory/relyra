---
phase: 18-mapping-editor-audit-timeline-hardening
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - lib/relyra/live_admin/connections_live.ex
  - lib/relyra/live_admin/components/connection_detail.ex
  - test/phoenix/live_admin_test.exs
autonomous: true
requirements: [AUD-01, SAFE-01]
must_haves:
  truths:
    - "Operator can browse the audit ledger and expand rows to view inline diff_summary"
    - "Expanding an audit row does not require a round-trip to the server"
    - "Failed admin mutations result in a typed flash error, with trust-state cleanly rolled back"
    - "Operator can filter the audit ledger by connection, actor, and event-type"
  artifacts:
    - path: "lib/relyra/live_admin/components/connection_detail.ex"
      provides: "Expandable JS toggle rows for audit events and filter inputs"
    - path: "test/phoenix/live_admin_test.exs"
      provides: "Integration tests verifying rollback behavior for failed audits and filtering"
    - path: "lib/relyra/live_admin/connections_live.ex"
      provides: "Event handler for audit filtering"
  key_links:
    - from: "lib/relyra/live_admin/components/connection_detail.ex"
      to: "Phoenix.LiveView.JS"
      via: "toggle attribute on row expansion button"
    - from: "lib/relyra/live_admin/components/connection_detail.ex"
      to: "lib/relyra/live_admin/connections_live.ex"
      via: "phx-change on filter form"
---

<objective>
Implement expandable rows for the audit ledger to reveal trust-mutation diffs, implement audit filters (connection, actor, event-type), and explicitly verify that admin-triggered trust mutations correctly roll back upon audit failures.

Purpose: Allow operators to inspect timeline changes contextually, filter the ledger efficiently (AUD-01), and mathematically prove that the system is safe from partial trust-state commits (SAFE-01).
Output: Audit ledger UI with inline expandable details, filtering capabilities, backed by tests proving atomicity.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-mapping-editor-audit-timeline-hardening/18-CONTEXT.md
@.planning/phases/18-mapping-editor-audit-timeline-hardening/18-PATTERNS.md
@.planning/phases/18-mapping-editor-audit-timeline-hardening/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JS.toggle expandable rows to audit ledger</name>
  <files>lib/relyra/live_admin/components/connection_detail.ex</files>
  <action>
    Modify the audit ledger table in the connection detail view. Add a toggle button (e.g., "View Details") to each row. Insert a hidden sibling row (`display: none;`) below it containing a `<pre><code>` block that renders `inspect(@audit.diff_summary, pretty: true)`. Wire the button to use `Phoenix.LiveView.JS.toggle/1` targeting the unique ID of the hidden row, ensuring expansion happens entirely client-side.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Audit rows can be expanded inline to reveal the diff_summary without sending events to the server.</done>
</task>

<task type="auto">
  <name>Task 2: Implement audit filters (connection, actor, event-type)</name>
  <files>lib/relyra/live_admin/components/connection_detail.ex, lib/relyra/live_admin/connections_live.ex</files>
  <action>
    Add a form above the audit ledger table with select/text inputs for "Connection", "Actor", and "Event Type". Configure the form to use `phx-change="filter_audits"` to trigger live filtering without page reloads. Add a `handle_event("filter_audits", %{"filters" => filters}, socket)` callback to `ConnectionsLive` and update the data fetching logic for audits to apply the provided criteria to the audit query. 
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Filter UI form is present, emits events on change, and the backend correctly scopes the audit list.</done>
</task>

<task type="auto">
  <name>Task 3: Verify safe rollback mechanics (SAFE-01) and filtering (AUD-01)</name>
  <files>test/phoenix/live_admin_test.exs</files>
  <action>
    Write integration tests for the two new ledger behaviors. 
    1. The transactional boundary (tech debt) in `MappingCommands` and `ConnectionsLive`. The test should attempt a mapping update where the subsequent `AuditWriter.append_event` (or DB constraint) is mocked/forced to fail. Assert that the LiveView correctly catches the error (flashes a typed error message) and that the database trust-state (mapping rules) remains unchanged (rolled back).
    2. Test that changing the filters via the `"filter_audits"` event correctly updates the displayed audit events on the page.
  </action>
  <verify>
    <automated>mix test test/phoenix/live_admin_test.exs</automated>
  </verify>
  <done>Test passes, mathematically proving that partial state commits cannot occur during admin-triggered mutations and that filtering correctly scopes the list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView → Ecto Repo | Database transactions ensure atomic trust mutations across boundaries |
| Operator Browser → LiveView | Operator submitting filter criteria |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Information Disclosure | `connection_detail.ex` | accept | The `diff_summary` is inherently sensitive but required for admin forensics. Operator access is governed by `admin_scope` controls (V4). |
| T-18-03 | Tampering | `MappingCommands` | mitigate | Explicit Ecto transactional rollbacks ensure any failed audit write strictly rolls back the corresponding trust mutation. |
| T-18-04 | Spoofing | `ConnectionsLive` | mitigate | Ensure filter criteria are sanitized and only fetch audits within the operator's authorized scope. |
</threat_model>

<verification>
mix test test/phoenix/live_admin_test.exs
</verification>

<success_criteria>
- Operators can expand audit ledger rows contextually to see the `diff_summary`.
- Operators can filter the audit ledger by connection, actor, and event-type.
- Tests prove that `append_audit` failures result in atomic rollbacks of trust mutations.
- The UI properly handles and flashes the resulting rollback errors.
</success_criteria>

<output>
After completion, create `.planning/phases/18-mapping-editor-audit-timeline-hardening/18-02-SUMMARY.md`
</output>