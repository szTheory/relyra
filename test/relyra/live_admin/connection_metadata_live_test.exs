defmodule Relyra.LiveAdmin.ConnectionMetadataLiveTest do
  @moduledoc """
  Wave 0 stub for the auto-refresh health card and "Resume now" button extension
  on `Relyra.LiveAdmin.ConnectionMetadataLive` (Phase 21 W4 — `21-06-live-admin-surface`).

  This file exists so PLAN files can reference an `<automated>` verify command
  pointing at this path from day one. The corresponding production extension
  will be added in Wave 4 (`21-06`); see
  `.planning/phases/21-scheduled-metadata-refresh/21-VALIDATION.md` Per-Task
  Verification Map for the wave assignment.
  """
  use ExUnit.Case, async: true

  @moduletag :pending

  @tag :pending
  test "Wave 0 stub: replaced by Wave 4 task in Phase 21" do
    flunk("Wave 0 stub — implement in the wave that introduces the health card and Resume button")
  end
end
